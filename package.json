{
  "name": "folioteca",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "[ -f .env ] || install -m 600 .env.example .env; docker compose up -d --wait && pnpm -r --parallel --if-present dev"
  }
}
