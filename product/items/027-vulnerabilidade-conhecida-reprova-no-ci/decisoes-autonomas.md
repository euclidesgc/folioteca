# Decisões tomadas sem o humano — 027-vulnerabilidade-conhecida-reprova-no-ci

O dono autorizou autonomia para o roadmap inteiro. Este arquivo é o que ele lê
de manhã: **uma linha por decisão**, com a alternativa descartada e o porquê.
Nada aqui foi aprovado por ele.

Se alguma decisão estiver errada, todas são reversíveis — o ponto de retorno
limpo é o commit `49e2672ded160eb1ea714d31d4996e47f6312963`, anterior a
qualquer trabalho deste item.

## Decisões

| # | Estágio ou fase | Decidido | Alternativa descartada | Por quê |
|---|---|---|---|---|
| D1 | discovery | **O motor da auditoria é `pnpm audit --audit-level=high --json`** | `osv-scanner`, que é a ferramenta que a skill `security-baseline` do harness nomeia para este trabalho | As duas foram executadas hoje sobre o mesmo `pnpm-lock.yaml` e **concordaram nos 923 pacotes e nos zero achados** — para pacotes de npm, a base OSV importa do GitHub Advisory quase tudo o que teria a dizer, e a diferença material entre elas é pequena. O que as separa é o custo: `pnpm audit` é subcomando do `pnpm@11.25.0` que o `packageManager` já fixa e que os cinco fluxos já instalam, enquanto `osv-scanner` entra pelo binário de release com `sha256` fixado — o padrão de `scripts/ci/instalar-gitleaks.sh`, decisão `D6` de `023` —, o que é mais um artefato de terceiro baixado no CI, mais um par versão/checksum a manter e, pelo gatilho mecânico do Example Mapping, é o que levaria este item à trilha completa por causa da ferramenta e não do problema. Acrescentar superfície de cadeia de suprimentos para medir cadeia de suprimentos é a troca que este item existe para evitar. **A escolha é reversível dentro do portão**: o motor é uma função, e trocá-la não mexe em onde o portão é chamado nem no que ele imprime. Quando o repositório tiver o que `pnpm audit` não lê — imagem de contêiner, módulo Go, pacote de Python —, é o osv-scanner que entra, e aí ele se paga |
| D2 | discovery | **O piso é `high`, e ele cobre `critical` junto; `moderate` e `low` contam mas não reprovam** | Reprovar a partir de `moderate`; ou reprovar só em `critical` | `high` é literalmente o que a linha do roadmap pede — *"aviso de severidade alta"* — e mudar o piso mudaria o item, que é do dono. Sobre o mérito: `moderate` em npm é populoso e majoritariamente inalcançável a partir do código real (negação de serviço por expressão regular em ferramenta de build, por exemplo), e um portão que fica vermelho por ruído ensina a ignorar o vermelho do portão ao lado. `critical` sozinho deixaria de fora a maioria das execuções remotas de código, que a base classifica como `high`. A contagem das quatro severidades é impressa em toda execução, então o número que não reprova continua visível |
| D3 | discovery | **A lista de isenções mora numa constante do próprio `scripts/gates/vulnerabilidade.sh`, no formato identificador-do-aviso mais data de vencimento** | `auditConfig.ignoreGhsas` do pnpm, que é a forma nativa; ou um arquivo próprio de configuração; ou o `pnpm-workspace.yaml`, onde a quarentena já declara a dela | A forma nativa é lida pela configuração **fundida** do pnpm, que soma o `.npmrc` da máquina de quem executa ao arquivo do repositório — e é exatamente esse buraco que o item `044` do roadmap existe para tapar na quarentena, onde a lista de isenções ganhou só metade da proteção que o número ganhou. Repetir agora o desenho que já tem item aberto para consertá-lo seria criar a segunda ocorrência do mesmo defeito de propósito. A constante no script é uma fonte só, versionada, que aparece no diff do PR de quem isenta e não pode ser sobreposta por arquivo de HOME. Custa o filtro do JSON ser escrito à mão, que são poucas linhas de `jq` — `jq 1.7` está na máquina e no runner |
| D4 | discovery | **O universo auditado é o lockfile inteiro: `dependencies`, `devDependencies` e `optionalDependencies`** | Auditar só o que vai para produção, com `pnpm audit --prod` | São 686 pacotes de desenvolvimento contra 199 de produção — separar deixaria três em cada quatro fora da medição. E a premissa de que dependência de desenvolvimento é menos perigosa não se sustenta neste repositório: elas rodam na máquina de quem programa, com as chaves dela, e no runner do CI, onde o `checkout` já gravou o token do repositório em disco — que é o mesmo raciocínio que levou `023` a fixar as 27 ações em SHA. A linha do roadmap diz *"uma dependência do lockfile"*, e o lockfile é um só |
| D5 | discovery | **O portão roda no fluxo `portoes.yml`, que não tem filtro de `paths`, e não num passo condicionado ao `pnpm-lock.yaml`** | Rodar a auditoria só quando o lockfile mudar, que é mais barato | Um aviso novo aparece **sem o lockfile mudar**: a base de vulnerabilidades é externa e se move sozinha. Condicionado ao arquivo, o portão nunca acusaria a vulnerabilidade publicada depois do último PR que mexeu no lockfile — que é o caso mais comum, e é precisamente a falha que este item existe para fechar. O custo é uma chamada de rede por PR |
| D6 | discovery | **Trilha rápida** | Trilha completa, como `023` | Avaliação mecânica dos quatro gatilhos, com a evidência de cada um na tabela do `00-discovery.md`: zero perguntas em aberto, nenhuma frente de produto tocada, nada no OpenAPI e nenhuma dependência nova. Os quatro verdadeiros obrigam a rápida — não existe "quase rápida", e escolher a completa por prudência custaria dois documentos e duas aprovações sem acrescentar verificação nenhuma. O que a trilha rápida corta é documentação: critério tipado, validador cego e revisão continuam valendo |
| D7 | brief | **A branch deste estágio chama-se `027-vulnerabilidade-conhecida-reprova-no-ci/brief`** | Reutilizar o nome `.../planejamento`, que é a convenção do processo para estágio de documento | O nome `.../planejamento` já foi consumido pelo PR #26, o do discovery, que está mergeado e teve a branch remota apagada. Recriá-lo daria dois PRs diferentes com o mesmo nome no histórico, e é o nome que a pilha usa para se orientar. Nomear o estágio mantém a família `<item>/<coisa>` e diz, na lista de branches, qual documento cada PR carrega. A alternativa de apagar a branch local antiga foi recusada pelo hook de comando destrutivo — e com razão: o `-D` apaga sem conferir, e o conteúdo dela só está em `develop` por squash |
| D8 | brief | **`RF-15` — isenção que não nomeia um aviso reprova o portão *antes* de auditar**, junto da verificação de lockfile e de binário | Recusar a entrada só no momento de aplicá-la, quando um achado a invocasse | O exemplo `E3.3` do discovery diz que a isenção nomeia um aviso e nunca um pacote, mas não diz quando a recusa acontece. Antes de auditar é onde ela custa nada — é leitura da própria constante, não tem chamada de rede — e é falha fechada no padrão do `exige_caminho`: a lista mal escrita é acusada mesmo no dia em que a auditoria não encontra nada para ela esconder. Recusar só na aplicação deixaria a entrada `lodash` dormindo no arquivo até o dia do achado, que é exatamente o dia em que ninguém quer descobrir que a isenção era inválida |
| D9 | brief | **O ponteiro do comentário de `.github/dependabot.yml` vira requisito (`RF-18`) deste item** | Deixá-lo como observação de escopo, sem `RF-nn` | O comentário do arquivo nomeia hoje `027` como dono da conta que falta a quem julga um PR de robô; quando este item fechar, quem herda é `041`. Sem `RF-nn` nenhum critério do plano o cobre, e um ponteiro apontando para item fechado é a forma mais barata de documento mentir. Com identificador, o `criteria-auditor` cobra a cobertura antes de a fase começar. É a regra 8 do `CLAUDE.md` — reconciliação de doc no mesmo PR da mudança |
| D10 | brief | **As âncoras do `00-discovery.md` passam a apontar conteúdo em vez de número de linha** | Corrigir os números para os de hoje | Três das quatro âncoras de linha do discovery já não conferiam uma sessão depois de escritas: `gates_runner.sh:220-228` cai dentro do heredoc Python (o bloco de portões diretos são as linhas 238-239), e `portoes.yml:59-62` e `:86-89` derivaram para 63-66 e 90-91 quando o PR #30 mexeu no fluxo. Corrigir os números repõe a mesma armadilha para a próxima sessão, porque a linha rota a cada commit e ninguém revalida citação de documento. Âncora por conteúdo — *o bloco onde `quarentena.sh` e `acoes_em_sha.sh` são chamados com o código de saída propagado* — sobrevive à edição do arquivo, e é a forma que o `01-brief.md` usa desde o começo |
| D11 | brief | **O merge deste PR esbarrou num defeito da tranca, e o conserto virou o PR #32, irmão deste e não empilhado sobre ele** | Empilhar o conserto sobre este PR; ou registrar item de roadmap e mergear por outra via | `scripts/merge-se-liberado.sh 31` mediu tudo, liberou, e o `gh stack merge` recusou: `#31 is not a stack number or a stacked pull request`. A causa é o predicado — `gh stack view` responde pela pilha **local**, que existe a partir de uma branch, e a pilha do GitHub só nasce a partir de dois PRs, então todo PR sozinho caía na via que o GitHub recusa. Empilhar o conserto sobre este PR faria a pilha passar a ter dois PRs e o defeito sumiria por acidente, sem nunca ser testado no caso que o produz; um item de roadmap seria inventar futuro para o que fecha agora, contra a orientação da corrida. O PR irmão mantém os escopos separados — este é `docs`, aquele é `fix` — e o merge dele é a própria prova da correção, porque ele também é um PR sozinho numa pilha. O que **não** se fez foi mergear à mão por `gh pr merge`: a via existe para não haver via de exceção |
| D12 | brief | **O merge falhou uma segunda vez, e o PR #32 passou a consertar a classe: a tranca mede todas as pré-condições do merge, e a corrida submete com `gh stack submit --open`** | Marcar este PR como pronto com `gh pr ready` e seguir, que resolvia a noite | Com a via corrigida, o merge foi recusado de novo — `Pull Request is still a draft`, porque `gh stack submit` cria rascunho quando o terminal não é interativo, que é o da corrida. Duas recusas do mesmo tipo em seguida — a tranca diz *liberado* e o merge não sai — é a regra da reincidência: a segunda ocorrência vira causa raiz, não terceiro remendo. `mergeStateStatus` responde pela branch e diz `CLEAN` de um PR em rascunho e de um já fechado, então as três pré-condições passam a ser lidas juntas e recusadas pelo nome, com teste que exige a **mensagem** de cada recusa — sem isso o caso do rascunho ficaria verde contra o script quebrado, pelo motivo errado. A origem do rascunho ficou escrita em `product/prompt-da-proxima-sessao.md`, que é o texto que toda sessão lê antes de agir |
| D13 | plan | **A contagem das quatro severidades não é impressa no caminho em que o portão não conseguiu auditar** | Imprimir a linha de contagem sempre, com zeros quando não houve auditoria, que é a leitura literal do "em toda execução" do `RF-06` | O `RF-06` pede o número de pacotes *lido de `metadata.totalDependencies`*, e nesse caminho o campo não existe: imprimir zeros ali é literalmente o `0 achados` que o `RF-07` proíbe, e é o defeito que `scripts/gates/medir.sh` existe para matar — portão que responde igual para "procurei e não achei" e para "não consegui procurar". Então o caminho de impossibilidade imprime a razão nomeada mais `não consegui auditar`, e nada de contagem, com um critério que afirma a **ausência** da cadeia `critical:` nessa saída. Não é reconciliação do brief: é o `RF-06` lido dentro do limite que o `RF-07` já tinha posto |
| D14 | plan | **O predicado que recusa isenção mal escrita exige `identificador:AAAA-MM-DD`, e isso é cobertura do `RF-11`, não extensão do brief** | Recusar só o identificador que casa com mais de um aviso, que é a letra do `RF-15`, deixando o prazo sem validação | O `plan-writer` apresentou o formato do prazo como a única invenção do plano; não é. O `RF-11` diz, literalmente, *"cada entrada no formato identificador do aviso mais data de vencimento"* — a data está no requisito. E sem validá-la, uma entrada como `GHSA-4mjr-xmp4-gh2g` sem data, ou com data ilegível, **nunca venceria**, contra o que o `RF-14` afirma no exemplo dele: *"a isenção vence sozinha; ninguém precisa lembrar de removê-la"*. O critério passou a rastrear `RF-11` junto de `RF-15`, e nada foi acrescentado ao brief |
| D15 | plan | **Isenção vencida cujo aviso a auditoria já não encontra imprime `VENCIDA em <data>` e não reprova** | Reprovar pela linha morta, que é o que `scripts/gates/quarentena.sh` faz com a lista dele | O brief é silencioso: o `RF-14` só fala do caso em que o aviso continua no lockfile. A quarentena reprova por linha morta porque lá a lista é **comparada com uma constante esperada**, e uma lista que mudou é o próprio achado dela; aqui o alvo é vulnerabilidade, e ficar vermelho por higiene de arquivo faria o portão da severidade cobrar o assunto de outro portão — a via mais rápida para o vermelho que se aprende a ignorar. A linha `VENCIDA` sai em toda execução pelo `RF-13`, então a isenção morta é visível sem travar PR nenhum |
| D16 | plan | **A pilha deste estágio nasceu por `gh stack init`, e as três pilhas mortas foram largadas do rastreamento local com `gh stack unstack --local`, com a regra escrita em `product/prompt-da-proxima-sessao.md`** | Criar a quarta pilha e seguir, que resolvia esta sessão em um comando | `gh stack add` recusou: `branch "develop" belongs to multiple stacks; use an interactive terminal to select one`. A causa é acúmulo: `.git/gh-stack` guardava **três** pilhas com trunk `develop` — a longa dos PRs 13 a 26, a do `brief` (#31) e a do conserto da tranca (#32) —, e `add` não tem flag que escolha entre elas. Medi os três PRs com `gh pr view`: todos `MERGED`. Seguir com `init` funcionaria e deixaria quatro, e a próxima sessão travaria igual com cinco — é a terceira vez que esta corrida esbarra no `gh stack` sem terminal interativo, depois do `merge` da `D11` e do rascunho da `D12`, então a regra da reincidência manda consertar a classe. `--local` mantém as pilhas no GitHub e mexe só no clone; a faxina veio depois do commit, com a árvore limpa, para trocar de branch não arriscar trabalho não salvo; e `.git/gh-stack` foi copiado antes. Prova do conserto: `gh stack view`, que falhava com a mesma ambiguidade, passou a desenhar a pilha e sair `0`. O que **não** se fez foi editar `.git/gh-stack` à mão — há via oficial, e ela não toca o GitHub |
| D17 | execute, fase 1 | **A fase 1 foi implementada na thread principal, sem despacho de implementer** | Despachar `nest-implementer` ou `react-implementer`, que são os implementers que o harness oferece | O item declara `stacks: []` e não toca `apps/`: o que ele escreve é shell de portão, fixture de JSON e YAML de CI. Os dois implementers existentes carregam as skills de uma frente de produto — módulo NestJS, feature bulletproof-react — e nenhuma delas tem o que dizer sobre um portão de `scripts/gates`. Despachar um deles gastaria o carregamento das skills erradas para produzir um arquivo que o plano já descreve em pseudocódigo, linha por linha, nas etapas 1.1 a 1.6. O que **não** foi dispensado é a verificação: `security-auditor` e `phase-validator` cego seguem valendo, e é deles que sai o veredicto |
| D18 | execute, fase 1 | **Os comandos de montagem dos critérios comportamentais foram executados com `rm -r` no lugar de `rm -rf`** | Pedir exceção de critério e reescrever os treze comandos do plano | O hook de comando destrutivo deste ambiente recusa `rm -rf` em voz alta, e o plano aprovado escreve `rm -rf "$d"` na montagem de cada fixture. A diferença entre as duas formas, sobre um diretório que existe e é gravável, é nenhuma: `-f` só suprime o aviso de alvo inexistente e a pergunta interativa, e a montagem segue com `mkdir -p` logo depois, então o estado final medido é idêntico. Reescrever o plano por causa de andaime de fixture custaria `exception-open`, uma volta do `plan-writer` e uma reaprovação, sem mudar uma única asserção do que está sendo medido. Quem reexecutar os critérios à mão neste ambiente troca a mesma letra |
| D19 | execute, fase 1 | **O motor da auditoria ganhou teto de tempo — `TETO_DA_AUDITORIA=600` e `exige_comando timeout` —, que o plano não pedia** | Registrar item de roadmap e entregar o portão sem teto, como as etapas descrevem | Medido nesta máquina, no lockfile de verdade: uma execução do portão levou **242 segundos** e a anterior não teve resposta em 180. `pnpm audit` fala com o registro npm, e chamada de rede sem teto não reprova nem aprova — pendura o job até o limite do runner, que é de seis horas por omissão, e nenhum dos dois jobs de `portoes.yml` declara `timeout-minutes`. O repositório já tem a norma e o idioma: `scripts/merge-se-liberado.sh` põe `timeout` em toda chamada de rede, e existe exatamente porque portão que não conseguiu medir tem de reprovar. Entregar o portão sem teto seria reintroduzir a classe que aquele script fechou, em todo PR desde o primeiro dia — e a norma da corrida é resolver na branch onde o problema nasceu quando ele cabe ali, em vez de inventar item de roadmap para o que fecha agora. O teto cai no caminho de reprovação que já existia: o processo morre, a saída padrão fica vazia, e o portão diz `não consegui auditar`. Nenhum critério do plano é contrariado — os dois estruturais que citam o motor continuam casando, e o teste ganhou dois casos que provam que o teto morde, com a cópia física de teto recuado a um segundo contra um `pnpm` que dorme cinco |
| D20 | execute, fase 1 | **O JSON cujo corpo é `{"error": {...}}` reprova nomeando o erro da ferramenta, e não como "auditoria de nenhum pacote"** | Deixar esse JSON cair no ramo de `metadata.totalDependencies = 0`, que já o reprovava | Medido contra o registro de verdade, três vezes nesta máquina: quando ele não responde, `pnpm audit --json` **não** fica sem saída — devolve um JSON válido cujo corpo inteiro é `{"error": {"code": 23, "message": "The operation was aborted due to timeout"}}`. Ele passa por qualquer teste de forma, e `metadata.totalDependencies` cai no `// 0` do filtro. O portão já reprovava, e reprovava pelo caminho certo — falha fechada, como o `RF-07` manda —, mas dizia a razão errada: *auditoria de nenhum pacote não é lockfile limpo*, que manda quem lê procurar um lockfile vazio que não existe. Dizer a razão errada é o defeito irmão de dizer `0 achados`: as duas fazem consertar a coisa errada. O plano não previu esta forma porque ela só aparece com o registro fora de alcance, e o brief mediu a auditoria com rede boa. A correção é um ramo antes das contagens, mais a sexta fixture com o corpo real medido e quatro casos — um deles afirmando a **ausência** da frase antiga nessa saída. Nenhum critério é contrariado: os cinco arquivos de fixture que o critério estrutural nomeia continuam existindo, e a saída deste caminho segue sem `critical:` e sem `0 achados` |
| D21 | execute, fase 1 | **O portão confronta `metadata.vulnerabilities` com a lista de achados que leu, e reprova por não ter medido quando as duas divergem** | Registrar o buraco como item de roadmap e entregar a fase como estava, que era o caminho sem revalidação | A auditoria de segurança mediu, e eu confirmei no `dist/pnpm.mjs` do pnpm 11.25.0: `pnpm audit --json` poda `.advisories` **duas vezes** antes de serializar — pela isenção da configuração (`pickBy` sobre `ignoreGhsas`) e pelo piso de nível — e serializa `metadata` intacta. Um PR que acrescentasse quatro linhas de `audit.ignore` ao `pnpm-workspace.yaml` fazia este portão imprimir `high: 2` e aprovar dizendo `0 achados` **na linha seguinte** — e `pnpm audit --ignore`, o comando documentado, grava essa configuração sozinho, sem data e sem dono, então o caminho acidental era mais provável que o deliberado. Medido antes e depois contra a mesma fixture: o portão do commit `4c5ff96` saía `0` com `high: 2` na tela; o corrigido reprova. O confronto é exato e não tem falso positivo: `vulnerabilities[adv.severity] += 1` sobe uma vez por aviso, no mesmo laço que preenche `.advisories`, então a diferença é sempre poda. Ele fecha junto o segundo achado alto — `jq` que erra ou formato que muda deixava a lista vazia e o portão aprovava —, e fecha o rebaixamento do piso por configuração, que poda pelo mesmo caminho. Entregar sem isso seria entregar um portão que este item existe para não ser: um que aprova por não ter medido. Custo pago: a árvore mudou depois do primeiro veredicto, então a fase foi revalidada do zero por um segundo validador cego |
| D22 | execute, fase 1 | **O portão reprova quando o `.npmrc` da raiz auditada redireciona o registro, medindo o arquivo do repositório** | Interrogar a configuração efetiva com `pnpm config get registry`, que é o idioma da quarentena | Quem responde a auditoria é escolhido pelo `.npmrc` da raiz, que é arquivo do PR: um espelho que devolve nada faria o portão imprimir os 923 pacotes — a contagem é local, lida do lockfile — e aprovar com zero achados, de quebra entregando a árvore de dependências ao servidor que o autor do PR escolheu. Nenhum portão do repositório lia esse arquivo. A alternativa nativa foi descartada por medição, não por gosto: `pnpm config get` sob o `pnpm` de mentira dos testes responde vazio, o que reprovaria **todos** os treze critérios comportamentais aprovados do plano. Ler o arquivo da raiz mede exatamente o que o PR controla, sem invocar ferramenta nenhuma, e a configuração da máquina de quem executa continua coberta pelo confronto das contagens da `D21`. A verificação é em bash puro, sem `grep`, para não acrescentar binário à lista que os critérios de PATH mínimo já fixam |
| D23 | execute, fase 1 | **O caso de teste que dizia provar que `auditConfig.ignoreGhsas` não isenta nada foi renomeado para o que ele de fato prova, e a defesa real ganhou fixture própria** | Deixar o caso como estava, já que ele passa | O caso escrevia `auditConfig.ignoreGhsas` no `pnpm-workspace.yaml` e `audit-level=critical` no `.npmrc`, mas o `pnpm` no PATH era o stub — um `sh` que dá `cat` numa fixture e não lê configuração nenhuma. Ele passava verde qualquer que fosse o comportamento do pnpm de verdade, e era **por ele** que o buraco da `D21` atravessava a revisão: teste que não pode falhar é aprovação por não ter medido, dentro do arquivo cuja tese é que isso nunca aconteça. Agora ele se chama pelo que mede — o portão não lê a configuração para esconder achado — e quem prova a defesa é a fixture `pnpm-audit-podado.json`, que é literalmente o JSON que o pnpm emite com a isenção na configuração. A suíte foi de 49 para 63 casos |
| D24 | execute, fase 1 | **A mensagem que vem da ferramenta passa a atravessar `em_uma_linha` antes do log, o teto ganhou `-k 30` e a saída de erro do pnpm deixou de ser descartada** | Deixar os três para depois, já que nenhum muda o veredicto do portão | São os dois achados de severidade baixa da auditoria, e custam quatro linhas. A mensagem de erro vem de fora — do registro, ou de quem responder por ele — e ia crua para o log do runner: com quebra de linha preservada, `::stop-commands::` no início de uma linha desliga o processamento de anotações do job inteiro e `::add-mask::` esconde o que o revisor precisava ler. Medido: a linha plantada saía sozinha na saída, e agora não sai — há caso de teste que mede a **posição**, porque comando de fluxo só vale no começo da linha. O `-k 30` fecha o processo que ignora o `SIGTERM`, que é justamente o cenário que o teto existe para eliminar. E o `2>/dev/null` descartava a razão real de toda falha de rede: o portão reprovava certo e não dizia o que consertar |
| D25 | execute, fase 1 | **A insistência contra o registro npm vira o item `053` do roadmap, e não conserto desta fase** | Acrescentar a retentativa aqui, junto do resto | Levantado pelo validador cego e pela auditoria: das quatro execuções contra o registro de verdade nesta sessão, uma terminou em `The operation was aborted due to timeout`, e isso reprova o PR de quem não mexeu em dependência nenhuma. É defeito real, mas não é o mesmo defeito dos outros: os que consertei aqui são o portão **aprovando** sem medir, e este é o portão **reprovando** quando não mediu — que é o comportamento correto, só áspero demais. Quantas tentativas, com qual espera e o que imprimir entre elas é desenho com régua própria, e enfiá-lo na mesma fase junto de quatro correções de segurança faria a revalidação julgar duas coisas ao mesmo tempo. O item nasce logo depois de `052`, que é o outro item de robustez de CI nascido desta fase |
| D26 | execute, fase 1 | **A peneira do `pnpm` no fluxo de portões vira o item `054`, e não conserto desta fase** | Trocar `pnpm/action-setup` por `scripts/ci/instalar-pnpm.sh` aqui mesmo | A auditoria apresentou como suspeita a confirmar, não como achado: o job `medir` usa `pnpm/action-setup` sem `with: version`, então a versão vem do `packageManager` do PR sem passar pela peneira que os três fluxos por frente usam. Medi que a linha é **anterior** a esta fase — ela não está no diff, veio do PR que criou o fluxo — e a ação está fixada em SHA. Mexer nela aqui alargaria o diff de uma fase de segurança para dentro de um passo que nenhum critério cobre, e a auditoria mesma diz que falta confirmar se a ação já valida o campo. O item registra a pergunta junto da correção, para quem o pegar não decidir sem medir |
| D27 | execute, fase 1 | **A peneira de registro passou a admitir espaço em volta do `=`, a ler também o `pnpm-workspace.yaml` e a aceitar o registro oficial declarado à mão** | Mergear com a peneira como estava e mandar o resto para o roadmap | A auditoria mediu as três formas com `pnpm config get registry` e as três redirecionam de verdade: `registry=espelho` reprovava, `registry = espelho` **passava** — o `ini` que o npm e o pnpm leem apara o espaço, o `case` do shell não — e `registry:` no `pnpm-workspace.yaml` passava, sendo essa a casa onde este repositório já guarda `minimumReleaseAge` e `overrides`. O agravante não é a exploração, que exige linha visível em arquivo rastreado: é a linha `medido: 0 redirecionamento(s)` saindo com um redirecionamento em vigor. Portão que afirma ter medido o que não mediu é exatamente o defeito que `medir.sh` existe para matar, e deixá-lo no repositório contradiria a tese do item inteiro. De quebra, a peneira passou a comparar **para onde** o registro aponta em vez da existência da declaração, porque fixar `https://registry.npmjs.org` à mão é endurecimento e a versão anterior o reprovava |
| D28 | execute, fase 1 | **O arquivo onde a saída de erro da auditoria é lida passou a vir de `mktemp`, com remoção por `trap EXIT`** | Deixar o nome derivado do PID, que só é explorável em máquina multiusuário | Nome previsível em diretório compartilhado é arquivo que outro usuário planta como link antes, e o `2>` do shell segue link. A auditoria encontrou um arquivo sobrando de uma execução interrompida, o que prova que a remoção do caminho feliz não cobria a interrupção. No runner efêmero do GitHub não há segundo usuário, então isto não bloqueava nada; entrou porque custa quatro linhas e porque a máquina de quem programa é multiusuário |
| D29 | execute, fase 1 | **O sétimo critério do plano foi devolvido ao `plan-writer` sob `exception-open`, e não registrado como divergência** | Abrir uma divergência `normal`, ratificar como autônomo e reconciliar | O critério traduzia o `RF-11` — *a lista de isenções mora no portão, e não em `auditConfig.ignoreGhsas`* — como a ausência das cadeias `npmrc`, `ignoreGhsas` e `auditConfig` no código. A proxy confunde **honrar** a configuração com **recusá-la**, e as correções da auditoria fazem o segundo: o portão lê o `.npmrc` para recusar registro desviado, e a mensagem do confronto nomeia `auditConfig.ignoreGhsas` porque é o que torna a reprovação acionável. Medido: o predicado imprimia três linhas, todas de código que existe para não delegar. Não é divergência porque o **brief não está errado** — o `RF-11` continua valendo palavra por palavra, e é a tradução dele em predicado de `grep` que ficou grosseira. A distinção tem consequência prática: divergência aprovada em trilha rápida promove o item à trilha completa e manda desdobrar o brief em PRD e spec, o que seria pagar dois documentos por um `grep` mal calibrado, sobre uma premissa — a de que o item é simples — que continua verdadeira. `exception-open` é o mecanismo que o harness tem para exatamente isto, e ele exige a reaprovação do plano, que aconteceu |
| D30 | execute, fase 1 | **Na mesma janela de exceção, o plano ganhou critério para as três defesas que a auditoria obrigou e que nenhum critério cobria** | Fechar a exceção com o critério 7 corrigido e entregar as defesas sem critério | O próprio `plan-writer` apontou a deriva: o plano descrevia um portão menor que o da árvore — sem `timeout`, sem `mktemp`, sem peneira de registro e sem o confronto entre a contagem e a lista de avisos, que é a correção central de segurança desta fase. Entregar assim significaria que a próxima sessão pode remover qualquer uma delas e nenhum critério acusa, o que é a mesma classe de defeito que este item existe para fechar, só que aplicada ao próprio plano. Ampliar a janela de um critério para quatro é mais edição em documento aprovado do que eu queria fazer sem o dono; fiz porque a alternativa é pior e porque o `sha` da reaprovação amarra o resultado |
| D31 | execute, fase 1 | **O brief ganhou `RF-19` e `RF-20`, os dois requisitos que a auditoria de segurança obrigou e que nenhum texto descrevia** | Entregar os dois critérios novos citando `RF-07` por extensão, que custava zero agora | O `plan-writer` apontou, e o `doc-writer` confirmou: o antecedente do `RF-07` é *"se `pnpm audit` termina sem produzir JSON"*, e nos dois casos novos **há** JSON, bem formado — o relatório podado e o relatório vindo de registro desviado passam por qualquer teste de forma. Citar `RF-07` ali era mapeamento por extensão, e o `criteria-auditor` teria material para contestá-lo com razão. Requisito que nasceu de auditoria de segurança e não virou texto é requisito que ninguém reconstitui seis meses depois: o código existiria, o teste passaria, e a razão de os dois existirem moraria só na conversa de uma madrugada. Os dois entraram na seção `### Não conseguir auditar`, ao lado de `RF-07` e `RF-08`, que são os outros casos de "há resposta e ela não permite medir", e a numeração seguiu a identidade em vez da posição — renumerar quebraria as referências do plano e dos veredictos |
| D32 | execute, fase 1 | **O `RF-06` do brief passou a delimitar a linha de contagem aos caminhos que chegaram a ler o relatório, e o brief foi reaprovado** | Registrar divergência `normal` entre o `RF-06` e o critério que afirma a ausência da contagem nos caminhos de impossibilidade | O `RF-06` dizia *"em toda execução"*, e o critério `comportamental — RF-06, RF-07, RF-08` afirma a **ausência** da cadeia `critical:` nos caminhos em que o portão não conseguiu medir. Era contradição literal entre dois documentos aprovados, e a `D13` já tinha decidido o comportamento certo sem corrigir o texto que o nega. A alternativa descartada custa caro e por regra: divergência aprovada em trilha rápida promove o item à trilha completa e manda desdobrar o brief em PRD e spec — dois documentos por uma frase, sobre uma premissa de item simples que continua verdadeira. O brief mudou de qualquer forma nesta sessão, porque a `D31` o tinha editado sem reaprovar, então o custo marginal desta correção foi zero. O `doc-writer` trocou o molde EARS de ubíquo para dirigido a evento — *"quando o portão chega ao veredicto por resultado da auditoria"* — e escreveu a exclusão como *"todo caminho que reprova por impossibilidade de medir"*, e não como "`RF-07` e `RF-08`", porque os caminhos sem contagem são também os do `RF-09`, `RF-10`, `RF-19` e `RF-20` |
| D33 | execute, fase 1 | **A mesma janela fechou a frase gêmea na seção *Riscos* do brief** | Deixá-la, já que é prosa de justificativa e ninguém deriva critério de aceite dela | O `doc-writer` a encontrou sozinho e recomendou corrigi-la na mesma leva: a linha do risco *"Decisão tomada contra reprovar a partir de `moderate`"* afirmava que a contagem *"é impressa em toda execução"*, que é literalmente o que o `RF-06` passou a negar. Aceitei pelo custo comparado, não pela estética: fechá-la agora custou uma frase dentro de uma janela já aberta, e encontrá-la depois custaria outra auditoria de cobertura inteira — foi uma auditoria que gastou um ciclo para achar a primeira ocorrência. Medido depois da edição: a cadeia `em toda execução` não ocorre mais em nenhum lugar do arquivo |
| D34 | execute, fase 1 | **O critério estrutural das fixtures passou de cinco arquivos para os nove que a árvore tem, com asserção de conteúdo em cada um dos quatro novos** | Acrescentar só `pnpm-audit-podado.json`, que era o único que uma dependência quebrada apontava | O `criteria-auditor` achou o defeito concreto: o critério `comportamental — RF-11, RF-19` dá `cat` em `pnpm-audit-podado.json`, e nenhum critério estrutural exigia esse arquivo nem nenhuma etapa mandava criá-lo — uma sessão futura podia apagá-lo com todo critério estrutural verde. Medida a árvore, o buraco era maior: as fixtures são nove, e quatro nasceram das correções `D20`, `D21` e `D24` sem entrar no plano. Corrigir só a que a dependência apontava deixaria as outras três na mesma condição, que é remendar o caso em vez da classe. O `plan-writer` acrescentou asserção de **conteúdo** além da existência, porque fixture esvaziada passa por critério que só olha o caminho, e a citação passou a incluir `RF-07` e `RF-19`, que são os requisitos que as quatro novas sustentam |
| D35 | execute, fase 1 | **O critério de `RF-17` trocou o predicado aberto pela enumeração fechada das chaves proibidas, e passou a admitir `types:` quando o valor é o padrão do GitHub** | Manter o predicado e deixar o validador julgar o que conta como reduzir o gatilho | Este é o achado que mudaria o veredicto: o critério proibia *"nenhuma outra chave que reduza o gatilho a um subconjunto dos pull requests"*, e `.github/workflows/portoes.yml:16` traz `types: [opened, reopened, synchronize]` — que é **exatamente** o conjunto que o GitHub Actions aplica quando `types` é omitido. Na leitura literal, o critério reprovava a implementação correta que está na árvore; na leitura generosa, ele pedia julgamento de quem valida, que é a forma que produz `CRITERIO_INVALIDO` — o veredicto que abriu esta janela. Agora a lista é fechada — `branches:`, `branches-ignore:`, `paths:`, `paths-ignore:` —, `types:` é admitido só com os três eventos padrão, e o arquivo inteiro segue sem `paths:` nem `paths-ignore:`, que é o que o `RF-17` proíbe nominalmente |
| D36 | execute, fase 1 | **O critério de `RF-16` passou a provar a vizinhança das três invocações, e o terceiro caso das isenções mal escritas passou a citar `RF-14`** | Deixar os dois como estavam, já que nenhum reprovava a árvore | Duas correções de rastreabilidade que a mesma auditoria levantou. O `RF-16` pede a chamada *"no bloco de portões diretos onde `quarentena.sh` e `acoes_em_sha.sh` já são chamados"*, e o critério só ancorava antes do `if [ "$SEM_ARTEFATOS" -eq 1 ]` — o que não prova bloco nenhum; agora ele compara os números de linha das quatro cadeias, medidos em 240, 241, 242 e 244. E o terceiro caso do critério de isenção larga — `GHSA-4mjr-xmp4-gh2g:amanha` — não é falha de `RF-15`, que cobra *nomear um aviso*, e essa entrada nomeia um: quem ela quebra é o `RF-14`, porque um vencimento que não é data nunca chega, e a isenção que não vence sozinha é o que aquele requisito proíbe. Critério que rastreia o requisito errado sobrevive à auditoria de hoje e reabre o assunto na próxima |
| D37 | execute, fase 1 | **Os predicados novos foram executados contra a árvore antes da reaprovação, e não depois** | Reaprovar o plano com a palavra do `plan-writer` e deixar a medição para o `phase-validator` | É a lição do próprio `RF-17`: o critério anterior tinha sido aprovado sem nunca ter sido executado, e era por isso que ninguém sabia que ele reprovava a implementação correta. Medido nesta sessão, antes da aprovação: as nove fixtures existem com os valores afirmados; as quatro cadeias do `gates_runner.sh` saem `count=1` nas linhas 240, 241, 242 e 244, na ordem que o critério exige; o bloco `pull_request:` do `portoes.yml` traz só `types: [opened, reopened, synchronize]`, o `grep` de `paths`/`paths-ignore` imprime `0`, e os quatro passos estão em 54, 70, 72 e 74; e os quatro comandos do critério `comando — RF-01, RF-11` respondem `147`, nada, nada e `1`. Isto **não** substitui o `phase-validator` cego, que é quem emite o veredicto da fase e ainda não rodou sobre esta árvore |

| D38 | execute, fase 1 | **O portão declara o tempo de uma tentativa da auditoria, em 240 000 ms, acima dos 60 000 ms que o pnpm assume por omissão** | Deixar o valor de omissão e aceitar o vermelho, ou trocar o motor por `osv-scanner`, reabrindo a `D1` | Medido nesta sessão, e é a causa do CI vermelho que a rodada anterior não chegou a ver: a resposta do endpoint de auditoria sobre estes 923 pacotes leva **71 segundos** numa chamada isolada, contra os 60 de `fetch-timeout` que o pnpm assume. A tentativa é abortada a 11 segundos do fim, o pnpm reencadeia, e o que chega à saída padrão é `{"error":{"code":23,"message":"The operation was aborted due to timeout"}}` — um JSON válido de erro, depois de 250 s. O portão então reprova dizendo `não consegui auditar` em **toda** execução do CI, num repositório sem vulnerabilidade nenhuma; os três jobs vermelhos do PR #36 são esse caso, e o verde na máquina de desenvolvimento vinha do cache quente, que esconde a chamada. Portão que reprova sempre não é rigoroso, é ignorado, e a saída seria desligá-lo. Trocar o motor era a alternativa cara: reabriria a `D1`, traria binário de terceiro para o CI e não é o que o número pede — o que faltava era tempo, não ferramenta. Quem continua fechando o caso do registro morto é o teto do processo, de 600 s, que não depende de configuração de ferramenta; as duas tentativas de 240 s cabem debaixo dele |
| D39 | execute, fase 1 | **A forma do relatório é cobrada por presença de chave, e o filtro de achados perdeu o `// {}`** | Manter os valores de omissão, que já reprovavam a fixture de forma desconhecida | Defeito achado na revisão desta sessão e provado por mutação: `jq` erra por **tipo** errado e nunca por **chave** ausente, então um relatório que renomeasse `metadata.vulnerabilities` e `advisories` — mantendo `totalDependencies` — atravessava os dois `//` e fazia o portão imprimir `medido: 923 pacote(s) auditado(s) — critical: 0, high: 0` e sair **0**. É a frase `0 achados` dita pela ferramenta em vez de pelo lockfile, que é o defeito que este arquivo inteiro existe para fechar, e o disfarce era perfeito porque a contagem de pacotes continuava verdadeira. A reprovação que as duas linhas diziam dar era inalcançável |
| D40 | execute, fase 1 | **O erro da ferramenta é decidido pela presença de `.error`, e não pelo texto de `.error.message`** | Manter `.error.message // empty`, que cobre o caso medido do tempo esgotado | Um erro sem `message` — `{"error":{"code":23}}`, verificado — caía adiante no ramo de `totalDependencies = 0` e culpava *"auditoria de nenhum pacote não é lockfile limpo"*: falha fechada, mas mandando quem lê consertar lockfile quando o que houve foi rede. Dizer a razão errada é o defeito irmão de dizer `0 achados`, e o próprio cabeçalho do arquivo já o nomeava |
| D41 | execute, fase 1 | **A medição de registro passou a incluir `~/.npmrc` e as variáveis `npm_config_registry` e `NPM_CONFIG_REGISTRY`** | Medir só as duas casas da raiz auditada, que são as que o autor de um PR controla | A linha `medido: 0 registro(s) declarado(s)` afirmava mais do que media: as duas casas de fora não são risco de quem abre o PR, mas são o buraco simétrico — o portão ficaria verde na máquina de quem tem o espelho configurado, sobre uma resposta que ninguém verificou. É o mesmo *"verde para quem tem a máquina certa"* que o cabeçalho deste arquivo recusa repetir para as isenções, e mantê-lo aberto enquanto o comentário diz o contrário é portão que declara ter medido o que não mediu |
| D42 | execute, fase 1 | **A suíte ganhou nove casos e três fixtures, e cada um foi provado por mutação antes de ser aceito** | Aceitar os casos novos porque passam, que é como os dois defeitos da `D39` e do casamento de isenção sobreviveram à rodada anterior | Caso que passa não prova nada; caso que falha quando o comportamento some, prova. As três mutações foram aplicadas ao portão e revertidas: repor os dois valores de omissão reprova 3 casos; trocar `[ "$isento" = "$id" ]` por `[ -n "$isento" ]` — qualquer isenção vigente escondendo **todo** achado — reprova 3; e remover o tempo de uma tentativa reprova 1. Antes desta sessão, as três passavam com a suíte inteira verde. Entraram junto o controle positivo do marcador `chamou-audit`, sem o qual os quatro casos de *"reprova antes de chamar a auditoria"* passariam a vácuo no dia em que o stub deixasse de escrevê-lo, e a fixture chaveada por número, que é a forma real do `pnpm audit` e a única que exercita o ramo do identificador sem GHSA |
| D43 | execute, fase 1 | **A auditoria é tentada até três vezes, com espera declarada de 30 s, e o portão imprime quantas tentativas gastou** | Deixar uma tentativa só e aceitar o vermelho intermitente, ou trocar o motor agora, reabrindo a `D1` no meio da fase | O tempo de uma tentativa (`D38`) resolvia o caso do lockfile grande, e não o que a medição seguinte mostrou: **três chamadas idênticas, espaçadas por 45 segundos, responderam em 74 s, em 90 s e em nenhum tempo**. O endpoint limita por volume, e um pull request dispara três jobs que auditam ao mesmo tempo — foi assim que os três ficaram vermelhos juntos, no mesmo minuto, com o lockfile limpo. A repetição é mitigação honesta: ela não aprova o que não mediu, e esgotadas as três tentativas a reprovação continua sendo por impossibilidade de medição, agora dizendo quantas vezes tentou. Isto fecha o item `053` do roadmap **nesta fase**, e não numa futura, porque a norma manda resolver na branch onde o problema nasceu o que se consegue fechar agora. A causa continua de pé e é a `D-001` |
| D44 | execute, fase 1 | **A troca de motor virou a divergência `D-001`, ratificada em modo autônomo, e o item `055` do roadmap** | Trocar para `osv-scanner` dentro desta fase, ou registrar só no roadmap sem divergência | Trocar o motor contradiz a `D1` deste arquivo e o `RF-01` do brief aprovado, que nomeiam a ferramenta — é reabrir documento canônico, e o protocolo de divergência existe exatamente para isso. Como `normal`, a fase segue na opção recomendada e o PR nasce e permanece `blocked-on-D-001` até um humano ratificar; é o mecanismo funcionando, e o `check` já lista essa espera. Fazer a troca aqui dentro seria decidir sozinho por cima de um documento aprovado, com uma reescrita de portão, fixtures e instalador de binário que não cabe no que sobrou desta fase — e sem o olhar do dono sobre a decisão de trazer mais um artefato de terceiro para o CI, que é o que a `D1` tinha recusado |
| D45 | execute, fase 1 | **Todo filtro `jq` do portão passou a exigir um único valor JSON na entrada, e a peneira de registro passou a seguir `npm_config_userconfig`** | Aceitar o veredicto `APROVADO` como está e mandar os dois apontamentos para o roadmap | Os dois vieram do `phase-validator` cego, que aprovou a fase e apontou por cima. O segundo é defeito real e **medido nas duas direções**: `npm_config_userconfig` reloca o arquivo de configuração inteiro, e a peneira ancorada em `$HOME/.npmrc` media o arquivo que ninguém lê enquanto o espelho respondia pela auditoria — o portão aprovava com o registro redirecionado. Removida a correção, dois casos da suíte falham. O primeiro apontamento — `jq` com mais de um valor na entrada devolvendo o status do último — **não se reproduziu**: montei o caso nas duas ordens possíveis, com o valor extra antes e depois do relatório, e as duas versões do portão, com e sem a guarda, reprovaram dizendo `não consegui auditar`. A guarda de valor único entrou assim mesmo, porque é uma linha, é estritamente mais restritiva e nomeia um requisito que antes era suposto; o que **não** entrou foi a afirmação de que ela conserta um furo aberto, e o comentário do caso de teste diz exatamente o que o caso prova |
| D46 | execute, fase 1 | **Os três resíduos que a validação cega mediu viraram o item `056`, e a fase fechou com `APROVADO`** | Corrigi-los agora, numa terceira rodada de correção e revalidação | O validador cego aprovou a fase e apontou por cima: soma de contagens que se anula com valor negativo, `npm_config_globalconfig` como quinta casa de registro não coberta, e quatro casos da suíte que passam com o ramo do `.error` removido porque a checagem de forma reprova logo adiante. Nenhum dos três é aprovação que o portão **declare falsamente ter medido** — o primeiro exige um registro que mente, que a própria peneira fecha para configuração vinda do PR; o segundo é cobertura faltando, e a linha `medido:` nomeia só o que leu; o terceiro é manutenção de suíte, com o comportamento ainda coberto por dois casos que morreram na mutação. Corrigir agora significaria mudar a árvore depois do veredicto e revalidar pela terceira vez, e cada volta dessas gasta uma sessão inteira para fechar resíduo que o próprio validador classificou como não-bloqueante. O que **não** se faz é deixá-los só na prosa de um veredicto que ninguém relê: por isso o item, com o que medir e como fechar cada um || D47 | encerramento | **A pendência de produto sobre o GitHub Actions é reescrita no presente, e não apagada** | Apagar a entrada, porque o runner voltou a executar em 04/09/2026 | O que voltou foi a execução, não a leitura da causa: `settings/billing/actions` continua exigindo o escopo `user`, que este token não tem, e a corrida consome minutos em toda fase. Apagar deixaria o dono sem a única linha que diz onde olhar quando o CI sumir de novo — e o sintoma da parada é **ausência**, que não acusa erro em lugar nenhum. A entrada passa a nomear o que é verdade hoje: a cota é ilegível daqui, e a escolha entre pagar o excedente, elevar o teto ou tornar o repositório público continua sendo dele |
| D48 | encerramento | **As validações de campo que se verificaram ficam escritas numa subseção `Verificadas`, com a evidência** | Tirá-las da lista, que é de pendências, e deixar a prova só no PR de encerramento | O run que serve de evidência expira da aba Actions antes do item que o citou, e o PR fica num histórico que ninguém relê. Sem a linha, a próxima sessão que ler *"só o runner prova"* reabre uma dúvida já respondida e gasta uma medição para chegar ao mesmo lugar. Duas fecharam aqui: o `gitleaks` no runner (`023`, fase 4) e o portão de vulnerabilidade sob o limite de taxa (`027`, fase 1), esta medida nas duas direções — reprovou sem a repetição, mediu com ela |
| D49 | encerramento | **O encerramento é um PR próprio, empilhado sobre o da fase 1** | Commitar o fechamento na própria branch da fase, que já está aberta | É o precedente de `023`, e separa duas leituras diferentes: o diff do portão, que é código a revisar, e o fechamento do item, que é decisão de fila. Empilhado, ele herda o bloqueio do fundo — que é o comportamento correto, porque a `D-001` espera ratificação humana e nada deste item mergeia antes dela |
| D50 | encerramento | **O vermelho do fluxo `Site` no PR #36 vira evidência no `055`, e não um quarto remendo no portão** | Baixar o piso de tentativas para mais que três, esticar o teto de tempo, ou tirar a auditoria dos fluxos por frente para o vermelho sumir agora | É a terceira vez hoje que o mesmo defeito reprova o CI — 06:42, 09:31 e 11:12 —, e a regra 20 do `CLAUDE.md` manda parar de remendar o caso na segunda. A causa está medida e nomeada: o endpoint não responde a quatro chamadas do mesmo lockfile no mesmo minuto, e trocar o motor contradiz a `D1` e o `RF-01`, que é por que a `D-001` espera o dono. Mais tentativa é mais espera pelo mesmo silêncio: o portão local mediu hoje **na terceira**, e o `Site` gastou 13m34s para não medir. Tirar a auditoria dos fluxos por frente mexeria no `gates_runner.sh` que a fase 1 validou, sem plano que o cubra |

## Aprovações autônomas

| Estágio | Arquivo | Quando |
|---|---|---|
| `plan` | `product/items/027-vulnerabilidade-conhecida-reprova-no-ci/03-plan.md`, `sha` `6fc2235911c86f2c2451cce872403a1dff9c836e` | 2026-09-04T09:25:13Z, `--por autonomo`. Segunda aprovação da mesma sessão: a primeira, `sha` `435ae51d` às 09:23:38Z, foi refeita porque o cabeçalho do plano ainda dizia que as decisões fixadas iam de `D1` a `D31`, e as seis desta sessão são justamente as que explicam por que cinco critérios têm a forma que têm. Reaprovação que fecha a janela de exceção aberta em 2026-09-04T07:55:26Z. As cinco correções da `D34` a `D36` entraram sob essa janela, depois de o `criteria-auditor` reprovar o bloco por uma dependência quebrada — o critério de `RF-19` usava uma fixture que nenhuma etapa criava — e por quatro defeitos de rastreabilidade. Os predicados foram executados contra a árvore antes da aprovação (`D37`). O bloco tem 23 critérios, 6 `estrutural`, 4 `comando` e 13 `comportamental`, e os vinte requisitos do brief têm critério que os cubra. A fase 1 segue `em_execucao`: falta a validação cega |
| `brief` | `product/items/027-vulnerabilidade-conhecida-reprova-no-ci/01-brief.md`, `sha` `6469d94931b28115dae47e7ac1f5033172b52096` | 2026-09-04T09:07:25Z, `--por autonomo`. Reaprovação que amarra o `RF-19` e o `RF-20` acrescentados pela `D31`, que tinham sido escritos sem reaprovar — o `check` acusava o brief mudado depois da aprovação. Junto entraram a delimitação do `RF-06` (`D32`) e a frase gêmea da seção *Riscos* (`D33`). Os vinte requisitos continuam no presente, nenhum foi renumerado, e o não-escopo não mudou |
| `plan` | `product/items/027-vulnerabilidade-conhecida-reprova-no-ci/03-plan.md`, `sha` `18813a2af24bd0d6747a08d2d47ce4e2751b19ba` | 2026-09-04T05:32:13Z, `--por autonomo`. Aprovado depois das duas redes que precedem a aprovação: `criteria_lint.py` passou limpo com zero aviso, e o `criteria-auditor` reprovou na primeira volta — `RF-06` sem prova de ordem e sem polaridade decidida no caminho da impossibilidade, `RF-17` sem a cláusula do `pull_request:` — e devolveu **LIMPO sem ressalva** depois da correção, tendo comparado as duas versões do bloco inteiro para garantir que nenhuma asserção se perdeu na edição. Os dezoito requisitos do brief têm critério que os cubra, em fase única. O estágio seguinte, `execute`, está aberto |
| `brief` | `product/items/027-vulnerabilidade-conhecida-reprova-no-ci/01-brief.md`, `sha` `e10ce415b870ad946c411a3612cc66d2e7d6d57b` | 2026-09-04T04:24:02Z, `--por autonomo`. Aprovado contra o `00-discovery.md` e o `CLAUDE.md`: os dezoito requisitos saem dos treze exemplos medidos do Example Mapping, nenhuma das seis decisões anteriores foi reaberta, e o não-escopo nomeia os cinco itens de roadmap vizinhos que este item não faz. O estágio seguinte, `plan`, está aberto |

## Achados fora do escopo deste item

| O que | Onde foi parar |
|---|---|
| O guard de documento aprovado do plugin (`guard_write.py`) **só cobre as ferramentas `Write` e `Edit`**: um `sed -i` pelo `Bash` altera um documento canônico aprovado sem recusa nenhuma. Medido nesta sessão sem querer — atualizei a faixa de decisões no cabeçalho do `03-plan.md` por `sed` esperando ser barrado, e o comando saiu `0`. A rede que sobra é o `sha` da aprovação, que o `state.py check` compara depois: ele acusa *"mudou depois da aprovação"*, mas **só na próxima vez que alguém rodar o `check`**, e a essa altura o documento já mudou. O `guard_write.py` é hook de `PreToolUse` das ferramentas de escrita, e o shell é a porta que ele não vigia | É defeito do plugin `generic-harness`, não do produto, então não virou item do `product/roadmap.md`. Enfileirado para a sessão de controle `generic-harness-b0`, junto do defeito do `tool_ladder_guard.py` da linha abaixo. O conserto tem duas metades e a segunda é a que importa: cobrir `Bash` num hook que case `sed -i`, `tee`, `>` e `cp` sobre caminho aprovado é peneira que sempre vaza, então o que segura de verdade é o `check` do fim de sessão, que já existe e já acusa — o que falta é ele rodar **antes** do commit, e não depois. Nesta sessão a mudança foi deliberada e imediatamente reaprovada, então nada ficou incoerente: o `check` fecha `ok: true` |
| `semgrep` não roda em lugar nenhum do repositório — nem fluxo, nem script, nem arquivo de configuração, confirmado por `git grep` e por busca de arquivo. Das três ferramentas que a skill `security-baseline` nomeia, `gitleaks` entrou em `023` e a auditoria de dependência entra aqui; a análise estática de padrão inseguro no código é a que fica sem nenhuma | Item `048-o-codigo-passa-por-analise-estatica-de-seguranca` no `product/roadmap.md`, logo depois de `040`, que é o fim do bloco de infraestrutura de segurança |
| A isenção nominal de `qs` na quarentena **vence em 2026-09-05**, e a partir desse dia `scripts/gates/quarentena.sh` reprova por vencimento — todo PR do repositório, não só o que mexe em dependência. O par a limpar é `pnpm-workspace.yaml:48-49` mais a constante `ISENCOES_ESPERADAS` de `scripts/gates/quarentena.sh:43`. Medido no registro: `qs@6.16.0` foi publicada em `2026-08-29T23:50Z`, então ela só completa os sete dias em `2026-09-05T23:50Z` — uma reconstrução de lockfile feita antes desse horário cairia de volta na versão vulnerável | Item `049-a-isencao-de-qs-vence-e-alguem-precisa-fecha-la` no `product/roadmap.md`, logo depois de `023`, que é de onde ele nasce |
| O hook `tool_ladder_guard.py` do plugin nega ferramenta à **thread principal** quando um agent está em voo. `_hooklib.active_agent()` lê `agent_type` do evento e, quando ele falta, cai no arquivo efêmero de estado do último agent despachado — e para a thread principal `agent_type` falta **de direito**, o que é indistinguível de "versão que não preenche o campo", que é o caso para o qual a rede foi escrita. Medido nesta sessão: um `sed -n` meu, no mesmo bloco de um despacho, foi recusado com `[escada] Bash está fora do conjunto de ferramentas de criteria-auditor`. Com agents em segundo plano — o padrão desta corrida — a janela é o tempo de vida do agent inteiro, e o sintoma é a thread principal perdendo `Bash`, `Write` e `Edit` sem motivo aparente | É defeito do plugin `generic-harness`, não do produto, então não virou item do `product/roadmap.md`. Relatado à sessão de controle `generic-harness-b0`, que cuida do plugin, para entrar na fila do PR `euclidesgc/generic-harness#2`. Nada foi consertado aqui: o plugin em uso não é o instalado neste repositório, e trocá-lo no meio da corrida é risco sem ganho. Contorno desta sessão: enquanto um agent está em voo, a thread principal usa `Read`, `Grep` e `Glob`, que quase todo agent do harness tem liberados, e guarda `Bash`, `Write` e `Edit` para depois que ele termina. **Segunda ocorrência medida na mesma sessão:** retomar um agent por `SendMessage` arma o arquivo efêmero igual a um despacho novo, e a recusa voltou idêntica — o que confirma que é classe e não caso. A causa raiz não foi consertada aqui de propósito: ela mora no repositório do plugin, que não é este, tem PR aberto (`euclidesgc/generic-harness#2`) e sessão dona (`generic-harness-b0`), e escrever nele no meio da corrida criaria conflito com o que já está na fila — o teste que falta lá é o que despacha um agent e prova que `Bash` da thread principal passa enquanto ele vive. **Terceira ocorrência, com a causa raiz agora nomeada:** a recusa voltou na sessão seguinte, no primeiro despacho. O diagnóstico mediu por que as três camadas de `active_agent` (`_hooklib.py:197-250`) falham juntas. A primeira testa `if "agent_type" in event` na linha 231, supondo a chave presente e vazia na thread principal; o esquema do payload do Claude Code 2.1.260 diz que a chave é **ausente** ali, e que o campo que separa subagent de thread principal é `agent_id` — que hook nenhum do harness lê. A segunda compara `session_id`, que o subagent herda do pai, então nunca separa os dois. A terceira é o teto de tempo, que vale 1800 s e não os 90 s da marca inferida, porque o `SubagentStart` grava `inferred: false`. O conserto é de uma linha, trocando o campo lido; o teste que o segura é o já descrito. Um segundo defeito, que essa linha não alcança: o slot de estado é único por raiz (`_hooklib.py:31-32`), então dois agents em voo se sobrescrevem e o segundo a começar responde pela política do primeiro a parar — o que morde numa corrida que despacha em paralelo. Desbloqueio de emergência para quem ficar preso: apagar `.harness/runtime/agent-active.json`, porque a identidade da thread principal é a ausência desse arquivo. Tudo isto foi enviado a `generic-harness-b0` nesta sessão |
