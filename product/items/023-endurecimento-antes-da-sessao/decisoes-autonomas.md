# Decisões tomadas sem o humano — 023-endurecimento-antes-da-sessao

O dono autorizou autonomia para o roadmap inteiro. Este arquivo é o que ele lê
de manhã: **uma linha por decisão**, com a alternativa descartada e o porquê.
Nada aqui foi aprovado por ele.

Se alguma decisão estiver errada, todas são reversíveis — o ponto de retorno
limpo é o commit `7a6bdbcced67d699998fc05510acf7b14d8f0228`, anterior a
qualquer trabalho deste item.

## Decisões

| # | Estágio ou fase | Decidido | Alternativa descartada | Por quê |
|---|---|---|---|---|
| D1 | discovery | **A política de conteúdo do `apps/web` é injetada como `<meta http-equiv>` no `index.html` durante o build, e os demais cabeçalhos saem de `server.headers`/`preview.headers` do `vite.config.ts`** | Escolher um host de produção e declarar os cabeçalhos nele (`_headers`, `nginx.conf`, regra de CDN); ou pôr a meta fixa no `index.html` versionado | `apps/web` é SPA estática e **não tem servidor**: `vite.config.ts:26-29` só declara porta, e o e2e usa `vite dev`. Escolher host é decisão de deploy, e a régua manda parar antes disso. A meta é a única forma que atravessa qualquer host e chega ao navegador em produção. Injetá-la só no build, e não no arquivo versionado, é o que preserva o recarregamento a quente — a política de produção proíbe o script embutido de que o HMR do Vite depende, e uma meta válida em dev e em produção seria frouxa nos dois. **O que a meta não cobre — `frame-ancestors`, `X-Frame-Options` e HSTS em produção — vira item de roadmap**, porque só o host entrega |
| D2 | discovery | **A política é `default-src 'self'`; `script-src 'self'`; `style-src 'self'`; `img-src 'self' data:`; `connect-src 'self' <VITE_API_URL>`; `object-src 'none'`; `base-uri 'self'`; `form-action 'self'`; `frame-ancestors 'none'`** | Começar com `Content-Security-Policy-Report-Only` e apertar depois | Report-only é a escolha certa para app que já existe e cujo inventário de recursos ninguém conhece. Aqui o app tem uma página e um bundle, o inventário é conhecido, e não há endpoint que receba relatório — report-only seria uma política que não bloqueia nada e cujos relatórios ninguém lê. `'unsafe-inline'` e `'unsafe-eval'` ficam de fora dos dois: com eles, a política deixa de impedir a classe de ataque que a justifica |
| D3 | discovery | **`WEB_ORIGIN` mantém o nome e passa a aceitar valores separados por vírgula**, com o padrão `^https?:\/\/[^/]+$` valendo por item | Renomear para `WEB_ORIGINS`, que é mais honesto no plural | O schema roda com `allowUnknown: true` (decisão D17 de `001`). Renomeando, um `.env` já materializado continuaria trazendo `WEB_ORIGIN`, que viraria chave desconhecida e **passaria em silêncio**, enquanto a API subiria com o default `http://localhost:5173` e ninguém seria avisado. O ganho é de leitura; o custo é a classe de falha muda que este repositório passou quatro fases fechando. A vírgula é a convenção mais comum para lista em variável de ambiente |
| D4 | discovery | **Quarentena de sete dias — `minimumReleaseAge: 10080` no `pnpm-workspace.yaml`** | 14 ou 30 dias; ou nenhuma quarentena, confiando no `pnpm audit` | Sete dias é a janela em que as campanhas recentes de publicação maliciosa em npm foram detectadas e as versões despublicadas, e é o valor que a documentação do pnpm usa como exemplo. Mais que isso começa a atrasar correção de segurança legítima, que também chega por versão nova. Verificado que `pnpm@11.25.0` lê a chave do `pnpm-workspace.yaml`: `pnpm config get minimumReleaseAge` devolve `10080` com ela declarada e `undefined` com um caractere trocado — é esse par que dá ao portão uma medição, e não uma suposição |
| D5 | discovery | **Dependabot semanal para `github-actions`, apontando para `develop`** | Renovate; ou fixar em SHA sem rotina nenhuma | A Fase 5 de `001` encaminhou este achado exatamente porque metade dele é pior que nenhum: SHA sem rotina congela o repositório em versões com defeito conhecido. Dependabot entende `uses: dono/acao@<sha> # v4.2.2` e atualiza os dois juntos, é nativo do GitHub e sai apagando um arquivo. Renovate faz mais e cobra um app de terceiro instalado na conta, que é permissão que não é minha para dar. **É a decisão mais externa deste item** — passa a existir um robô abrindo PR no repositório —, e por isso está aqui em vez de dentro de uma fase |
| D6 | discovery | **A varredura é `gitleaks`, instalado no CI pelo binário de release com versão fixada e checksum verificado** | `gitleaks/gitleaks-action`; ou `trufflehog`; ou `semgrep` | A ação oficial do gitleaks exige licença para organização e é mais uma ação de terceiro a fixar em SHA — o item ganharia a dívida que ele está pagando. O binário com checksum é a forma que não acrescenta superfície. `gitleaks 8.30.1` **já está na máquina de desenvolvimento**, então o portão local funciona no primeiro dia. `trufflehog` verifica credencial contra o serviço real, o que é mais forte e manda o segredo achado para fora — inaceitável num portão. `semgrep` é análise de código, não de segredo |
| D7 | discovery | **O portão de segredo entra em `.harness/gates.json` e roda também no `gates_runner.sh` local**, e reprova em voz alta quando `gitleaks` falta | Deixá-lo só no CI, para não obrigar ninguém a instalar ferramenta | A regra 19 do `CLAUDE.md` é literal: portão que não conseguiu medir reprova. Um portão que existe só no CI é um portão que descobre o problema depois do push, e a norma inteira deste repositório é falhar barato antes de falhar caro. A reprovação nomeia a ferramenta e a versão, então quem topar com ela sabe o que fazer em uma linha |
| D8 | discovery | **A varredura da árvore de fontes rastreadas entra neste item, junto com a do artefato** | Item de roadmap separado para a regra 14 | O achado da auditoria da Fase 1 de `001` — *"nenhum portão cobre a regra 14; a regra é sustentada só por disciplina"* — nunca virou item e ficaria órfão. É a mesma ferramenta, o mesmo portão e o mesmo arquivo de configuração; separar pagaria duas vezes a instalação para medir dois universos de uma varredura só. O roadmap diz "o artefato de build é medido contra segredo", e medir só o artefato deixaria passar o `.env` rastreado, que é o caso mais provável dos dois |
| D9 | discovery | **A política do hotsite não referencia `NEXT_PUBLIC_APP_URL` nem `NEXT_PUBLIC_SITE_URL`** | Incluir a origem do app em `connect-src` desde já | Nenhum arquivo de `apps/site` lê as duas variáveis hoje — elas existem só no `.env.example`. Uma política que nomeia origem que ninguém chama é permissão concedida sem uso, e ela seria herdada sem revisão pelo item `015`, que é quem dá conteúdo ao hotsite. Quando o hotsite chamar algo, a linha entra com o motivo |
| D10 | discovery | **Quatro fases: (1) origem em lista na API, (2) cabeçalhos e política nas frentes de navegador, (3) portão de segredo, (4) cadeia de suprimentos** | Uma fase por assunto (cinco); ou duas fases grandes (navegador e CI) | A ordem é por dependência real: a política de conteúdo da fase 2 precisa nomear a origem da API, que a fase 1 fixa; e a fase 4 fixa em SHA todas as linhas `uses:`, incluindo as que a fase 3 acrescenta ao CI — na ordem inversa, a mesma linha seria escrita duas vezes. Quarentena e SHA ficam juntos na fase 4 porque são o mesmo assunto, cadeia de suprimentos, e o mesmo arquivo de configuração |
| D11 | discovery | **O item segue inteiro, com cinco assuntos, em vez de ser quebrado** | Propor a quebra em quatro ou cinco itens, como o INVEST manda quando *pequeno* falha | *Pequeno* não falhou: nenhum dos cinco tem lógica de domínio, os cinco são verificáveis por comando, e cabem em quatro fases curtas. Além disso a decisão D27 da Fase 3 de `001` já pesou essa quebra e a descartou, e refazê-la agora seria **mudar o roadmap** — que a norma da corrida reserva ao dono. A tensão está registrada no `00-discovery.md`, na nota sobre *pequeno*, para o caso de ele discordar |
| D12 | discovery | **O portão de segredo é construído mesmo existindo o `GitGuardian`, e os dois convivem** | Considerar a regra 14 já coberta pela verificação externa e cortar R4 do item | O `GitGuardian` apareceu como verificação obrigatória do PR desta sessão, e não está em arquivo nenhum do repositório — é app instalado na conta, invisível a qualquer varredura de fonte. Ele olha o diff que chega ao GitHub: **não** roda antes do push, **não** enxerga artefato de build (nenhum PR carrega `dist/`) e vive fora do controle daqui. O portão faz o oposto nos três pontos. Corrigi a tabela de linha de base do `00-discovery.md` no mesmo PR, que é a regra 8 |
| D13 | prd | **A varredura de segredo não olha o histórico do git: mede o que está rastreado hoje e o que o build produziu** | Rodar `gitleaks` sobre todos os commits, que é o modo padrão da ferramenta | O custo de varrer o histórico cresce a cada push e o portão passaria a ficar mais lento para sempre, medindo a mesma coisa de novo. E o remédio para achado antigo não é do portão: é reescrita de histórico e rotação de credencial, decisão do dono. O portão fecha a porta de entrada; a limpeza do que já entrou, se um dia for necessária, é ato deliberado e não efeito colateral de um portão de PR |
| D14 | prd | **O Dependabot cobre só `github-actions`; o ecossistema `npm` fica de fora deste item** | Declarar os dois ecossistemas de uma vez, já que o arquivo é o mesmo | Quem julga um PR de atualização de dependência precisa saber se a versão de saída tem vulnerabilidade conhecida, e essa conta é do item `027-vulnerabilidade-conhecida-reprova-no-ci`, que depende deste. Ligar `npm` antes disso produz PRs semanais que ninguém sabe aprovar ou recusar por critério escrito — e PR de robô que fica parado ensina a ignorar PR de robô. Reversível: são quatro linhas no mesmo arquivo |
| D15 | prd | **Corrigi no `roadmap.md` a contagem de referências `uses:` do item: de 25 para 27** | Deixar o número como está, já que o `00-discovery.md` explica a diferença | O roadmap afirmava um número que hoje é falso, e a regra 8 manda o documento não mentir. As duas a mais entraram em `portoes.yml` pela divergência `D-019` da Fase 5 de `001`, depois de a nota do roadmap ter sido escrita. **É a única edição que fiz no `roadmap.md` além de marcar o item como em andamento**: corrige um fato medido, não muda fila, escopo nem precedência — isso continua sendo seu |

| D16 | spec | **A política de conteúdo do hotsite é emitida por `middleware.ts`, com nonce gerado por requisição; os quatro cabeçalhos constantes ficam em `headers()`** | Manter os cinco em `headers()` com `script-src 'self' 'unsafe-inline'`, que preserva o prerender estático; ou fixar o hash dos scripts embutidos; ou tirar a política do hotsite deste item | Medi o build: `apps/site/.next/server/app/index.html` carrega dois `<script>` sem `src` — o payload de streaming do App Router, `self.__next_f.push(…)` —, e `script-src 'self'` sem nonce bloqueia os dois e a página não hidrata. `headers()` é estático por construção e não conhece a requisição, e nonce repetido é nonce ausente. Descartei `'unsafe-inline'` porque adotar no hotsite exatamente o que o PRD recusa no app, no mesmo item e com a justificativa escrita ao lado, é incoerência que a norma não sustenta; descartei o hash porque o payload muda a cada build e a automação que o recalcula falha aberta. **Custo aceito: as rotas que consomem o nonce deixam de ser prerenderizadas.** Isto contraria o PRD aprovado, então virou a divergência `D-001` — ver `04-divergencias/D-001.md` |
| D17 | spec | **`Strict-Transport-Security: max-age=31536000; includeSubDomains` no hotsite, sem `preload`** | Incluir `preload`, que é o valor que os geradores de configuração sugerem | `preload` inscreve o domínio na lista embutida dos navegadores, que é distribuída no binário e leva meses para sair — é a única parte do cabeçalho que não se desfaz editando um arquivo. Não há domínio escolhido, e um item de endurecimento não é o lugar de tomar decisão irreversível sobre um domínio que ninguém registrou. Um ano de `max-age` é o valor de referência; `includeSubDomains` é o que faz o cabeçalho valer para o que vier depois |
| D18 | spec | **`X-Frame-Options: DENY` entra em `server.headers` e `preview.headers` do `vite.config.ts`** | Deixá-lo fora de `apps/web` inteiro, lendo o não-escopo do PRD ao pé da letra | O não-escopo recusa os três cabeçalhos que **`<meta>` não entrega** — e a justificativa escrita lá é essa mesma. Ela fala do `dist/` servido em produção por um host que ninguém escolheu. Os dois servidores do Vite existem hoje e emitem cabeçalho de resposta como qualquer servidor: negá-los ali seria recusar o que já é possível por causa de um limite que é de outro lugar. O não-escopo continua valendo, e inteiro, para o artefato em produção |
| D19 | spec | **O endurecimento do `scripts/gates/gates_runner.sh` entra neste item, como etapa do plano** | Abrir item de roadmap separado para o defeito do dispatcher | O `doc-writer` mediu: o runner só entrega a cada portão os arquivos que sobrevivem ao filtro `applies_to` **e** a `os.path.isfile`, e o universo de partida são os arquivos rastreados. `apps/web/dist` e `apps/site/.next` são ignorados pelo git, nunca entram, e quando a lista de alvos fica vazia o dispatcher faz `continue` — **pula o portão em silêncio**. Sem isso corrigido, o portão de segredo que o PRD manda rodar no runner local não roda, e o item entregaria um verde que não mediu nada. Não é escopo novo: é o que torna verdadeiro o que o PRD já aprovou. As duas falhas irmãs da mesma linha — script de portão ausente que imprime `gate não cobrado` sem reprovar, e `.harness/gates.json` ausente saindo `0` — se consertam junto, porque são a mesma classe que o `CLAUDE.md` cataloga e consertar a terceira ocorrência custa mais que a classe inteira |
| D20 | spec | **A referência errada à tabela das três formas de portão que mentem vira item de roadmap, em vez de correção nesta branch** | Corrigir agora as duas linhas que a citam | `scripts/gates/medir.sh:74` e `product/items/001-esqueleto-do-monorepo/03-plan.md:615` apontam a tabela para o `CLAUDE.md` da raiz, e ela não está lá — vive no template do plugin do harness. É violação da regra 8, e é real. Mas uma das duas linhas está num plano **aprovado** de outro item, que não se edita fora de janela de exceção, e a outra é código de portão fora do escopo deste item. O roadmap é onde a pendência sobrevive a um PR que ninguém relê |
| D21 | spec | **O ponto cego do `state.py check` vira pendência de produto aberta, e não correção nesta branch** | Corrigir o laço do `check` para percorrer `item["divergences"]` inteiro | Ao ratificar `D-001` descobri que o `check` alcança divergência num ponto só — o `blocked_on` das **fases**. Divergência nascida em estágio de documento tem `phase: null`, o item ainda não tem fase, e o laço nunca a visita: `check` responde `ok` com uma ratificação autônoma esperando olho humano. Provado nesta corrida. Não corrigi porque `state.py` mora no plugin, fora deste repositório, e a norma daqui é que a retrospectiva proponha a mudança do harness em vez de aplicá-la de dentro de um item — é a mesma razão pela qual o defeito do `templates/ci/harness.yml` está registrado e não aplicado. A trava do merge não depende disso: quem trava é o rótulo `blocked-on-D-001`, que o `bloqueio.yml` faz valer, e ele está posto |

| D22 | plan | **O plano tem cinco fases — uma por frente —, e não as quatro de `D10`** | Manter as quatro, com a segunda juntando `apps/site`, `apps/web` e o `helmet` da API | A skill de plano é literal: fase que toca três stacks é três fases, e um PR que ninguém consegue revisar não é revisado. O assunto "cabeçalhos e política" tocaria três frentes de arquivos inteiramente disjuntos. Os **assuntos** continuam sendo os quatro de `D10`; o que mudou é que o do navegador vira três PRs, um por frente, paralelizáveis por `git worktree`. O `helmet` desceu para a fase da API porque toca o mesmo `apps/api/src/bootstrap.ts` que a lista de origens |
| D23 | plan | **A API não emite `Strict-Transport-Security` fora de produção**: `helmet` entra com `strictTransportSecurity` ligado só quando `NODE_ENV === "production"` | Deixar o padrão do `helmet`, que emite HSTS em toda resposta | Emitida em `http://localhost:3000`, a HSTS fixa no navegador de quem desenvolve uma regra que força `https` para aquele host e persiste em cache — é a mesma razão que `RF-06.2` escreve para o hotsite. A spec não trata do assunto na API: `RF-09` cobra `nosniff` presente, `X-Powered-By` e política de conteúdo ausentes, e cala sobre HSTS. O critério existe na Fase 1 rotulado como decisão, e não como requisito |
| D24 | plan | **O portão de segredo é invocado direto pelo `gates_runner.sh`, que ganha o modo `--sem-artefatos` para os três jobs por frente do CI** | Declará-lo em `.harness/gates.json`, como os oito portões que já existem | O molde do `gates.json` é arquivos por stdin, uma violação por linha de stdout e **código de saída ignorado** — um portão que precisa reprovar por *não ter conseguido medir* vira aprovação silenciosa nesse molde, que é metade do que `RF-16` pede. E os jobs de gates de `ci-react.yml`, `ci-nestjs.yml` e `ci-site.yml` rodam o runner sem ter construído as outras duas frentes: sem o modo, reprovariam por artefato ausente em todo PR — reprovação verdadeira sobre uma pergunta que aquele job não deveria estar fazendo. A execução sem flag, que é a que `RF-17.1` nomeia, continua cobrando tudo |
| D25 | plan | **O portão de segredo entra no CI como passos do job que já existe em `portoes.yml`, e não como job novo** | Um job dedicado, que é a forma mais legível | Job novo traz `actions/checkout`, `actions/setup-node` e `pnpm/action-setup`, e a contagem de referências `uses:` deixaria de ser 27 — contra a `RNF-01` da spec, que afirma que o portão de segredo entra sem acrescentar nenhuma. `portoes.yml` é o fluxo sem filtro de `paths`, então é onde a varredura roda em todo PR; ele ganha também o gatilho `push`, que `RF-17.2` cobra e que ele hoje não tem |
| D26 | plan | **A verificação da política do hotsite é um script de shell versionado em `apps/site/scripts/`, com as asserções de `medir.sh`** | Instalar Vitest em `apps/site` e testar a função `middleware` como unidade | `apps/site` não tem pack nem runner de teste, e tudo o que a spec cobra aqui é observável na resposta HTTP — só o servidor de verdade prova o que `RF-23.2` pede, que é o nonce do cabeçalho estampado no atributo `nonce` de um `<script>` do mesmo HTML. Um runner instalado só para este item traria dependência e configuração que nenhuma outra frente usa. A função de comparação fica atrás de uma guarda de `BASH_SOURCE`, com teste próprio, porque `RF-23.3` cobra que ela **reprove** — e asserção que ninguém verificou é asserção que pode não morder |
| D27 | plan | **O `.gitleaks.toml` nasce com duas entradas de allowlist: o caminho `.env.example` e a linha de `POSTGRES_PASSWORD` de `ci-nestjs.yml`** | Nascer sem allowlist e descobrir os falsos positivos na primeira execução do portão | `RF-15.1` exige código zero sobre repositório limpo, e esses são os dois únicos valores hoje rastreados que a ferramenta acusa sem haver segredo: `.env.example` traz `postgresql://folioteca:senha@localhost:5433/folioteca` como exemplo de máquina local, e a senha do Postgres do CI é `${{ github.run_id }}-${{ github.run_attempt }}`, descartável e alcançável só pelo runner efêmero. **Allowlist é permissão concedida**: cada entrada carrega o motivo na linha acima, e nenhuma entra sem o mesmo tratamento. É a decisão deste estágio que mais merece o seu olho |
| D28 | plan | **O gatilho de `push` do fluxo que cobra o portão de segredo é `main` e `develop`** | `on: push` sem filtro de branch, que é o literal de `RF-17.2` | É a convenção dos outros quatro fluxos deste repositório, e sem filtro a varredura roda duas vezes em todo PR — uma pelo `push` da branch, outra pelo `synchronize`. Estreita `RF-17.2`: um push a branch de trabalho **sem PR aberto** não fica coberto. Reversível apagando duas linhas do `portoes.yml` |
| D29 | plan | **`ci-react.yml` ganha uma asserção permanente sobre a política no `apps/web/dist/index.html`, sem que nenhum `RF-nn` a peça** | Verificar `RF-10`, `RF-11` e `RF-07.4` só no dia da fase, como a spec literalmente pede | Regra 19: verificação que nunca roda de novo deixa de valer no primeiro PR que mexer no `index.html` ou no plugin do Vite. O hotsite tem `RF-23.3`, que obriga o mecanismo de verificação a existir; o app não tem o equivalente, e a assimetria é omissão da spec, não decisão dela. O passo roda no job que já constrói o app, e não acrescenta `uses:` |
| D30 | plan | **Removi do plano as 73 ocorrências de `rtk proxy` que o `plan-writer` escreveu nos comandos dos critérios** | Deixá-las, já que é a ferramenta configurada no ambiente desta sessão | `rtk` não é dependência declarada deste repositório — regra 15 —, e o `03-plan.md` é documento canônico cujos comandos o validador de fase executa e você repete à mão. Um plano que só roda na máquina onde uma ferramenta pessoal está instalada é um plano que reprova por ambiente e chama isso de resultado |
| D31 | fase 1 | **Os oito achados da revisão e da auditoria de segurança entram nesta fase, e nenhum vira item de roadmap** | Fechar a fase no que o plano pediu e encaminhar os seis não-bloqueantes como itens futuros | A norma da corrida é literal: o que cabe no trabalho em andamento se resolve na branch onde o problema nasceu, e item de roadmap é para o que é de verdade futuro. Os oito moram nos mesmos cinco arquivos que a fase já abriu, e cinco deles — CORP, `X-Frame-Options`, `NODE_ENV` sem domínio, `WEB_ORIGIN` que se autopreenche, e o padrão de origem permissivo — são a fronteira externa que **esta** fase existe para endurecer. Adiá-los seria entregar o endurecimento com a porta que ele veio fechar ainda encostada |
| D32 | fase 1 | **`isWebOriginList` passa a exigir duas peneiras: o padrão `^https?:\/\/[^/]+$` **e** `new URL(item)` com `url.origin === item`** | Substituir o padrão por `new URL`, que é um caminho de validação só | A spec aprovada nomeia o padrão pelo literal em `RF-01.2` e `RF-04.1`. Substituir tornaria as duas frases falsas e obrigaria a reabrir e reaprovar a spec para ganhar elegância de implementação. Somando, os dois requisitos continuam verdadeiros e os nove valores que a auditoria mediu como aprovados em silêncio — credencial embutida, query, fragmento, espaço, CRLF, `*`, caixa alta, homóglifo cirílico — passam a derrubar o boot nomeando `WEB_ORIGIN`. Registrado como `D-002` |
| D33 | fase 1 | **O `.env.example` da raiz entra no escopo desta fase**, embora o plano tenha declarado `apps/api/**` | Deixá-lo para depois, respeitando o escopo de arquivos escrito no plano | Regra 8: docs não mentem, e a reconciliação é no mesmo PR. O comentário de `WEB_ORIGIN` descrevia uma origem única e a variável passou a ser lista; um `.env.example` que ensina o formato errado é o documento que mais gente lê e o único que se copia para dentro de um `.env` de verdade |
| D34 | fase 1 | **A chave privada literal que o plano embute no critério da Fase 4 passa a ser gerada em tempo de execução** | Nascer o `.gitleaks.toml` com o `03-plan.md` na allowlist | O plano é arquivo rastreado, e o portão que a Fase 4 constrói varre fontes rastreadas: como está, a Fase 4 nasce reprovando no documento que a define, contra o `RF-15.1` do próprio item. Allowlist é permissão concedida, e engordá-la para acomodar um defeito de redação abriria um caminho onde chave privada deixa de ser acusada — exatamente o oposto do que o item entrega. Registrado como `D-004`, aplicado pela Fase 4 |
| D35 | fase 1 | **A política de recurso entre origens fica no padrão `same-origin` do `helmet`** | Afrouxá-la para `cross-origin`, como o primeiro bloqueante da revisão pedia e como a `D-005` chegou a ratificar | A justificativa do bloqueante — que `same-origin` derrubaria a leitura da resposta pelo `apps/web`, em outra porta — foi medida em Chromium real e é falsa: requisição em modo CORS lê o corpo igual nas duas políticas, porque a checagem só vale para resposta opaca de requisição `no-cors`. O afrouxamento não destravava nada e abria o caso que a política existe para fechar. **É a decisão em que um agent do processo estava errado e outro provou, com navegador de verdade** — vale a leitura, porque mostra por que a medição vence o argumento. Registrado como `D-006` |
| D36 | fase 1 | **`main.ts` sai explicitamente com código 1 quando o boot rejeita** | Continuar em `void bootstrap()`, apoiado no comportamento padrão do Node para rejeição não tratada, que a auditoria mediu funcionando em cinco combinações | A garantia funciona hoje e não está escrita em lugar nenhum: ela é emprestada do runtime. A norma deste repositório pede log estruturado numa fase futura, e a forma natural de implementá-lo é um `process.on('unhandledRejection', …)` — registrado sem sair, ele apaga a garantia em silêncio, deixando o processo vivo, sem porta e sem falha reportada. Três linhas fixam no código o que hoje depende de um padrão que alguém pode desligar sem perceber |
| D37 | fase 1 | **`NestFactory.create` recebe `abortOnError: false`** | Manter o padrão do Nest e desistir do teste que prova que ambiente inválido não abre a porta | No padrão, erro de construção do container vira `process.abort()`, que não é interceptável e mata o worker do Jest com `SIGABRT` — o requisito `RF-04` ficaria verificável só pelo schema isolado, nunca pelo caminho que os testes de integração usam. A auditoria mediu as cinco combinações de falha com e sem a opção: em todas, código de saída 1, processo morto e porta nunca aberta. Não há regressão de disponibilidade; o que muda é que a falha passa a ser capturável, que é o que `D36` fixa |
| D38 | fase 1 | **Os dois achados de portão que a validação cega levantou vão para o roadmap, e nenhum vira fase deste item** — o universo cego do `gates_runner.sh` amplia o item `026`, e o `typecheck` que não lê arquivo de teste vira o item `029` | Corrigir os dois nesta branch, já que o segundo custa um `tsconfig` de teste e o primeiro uma linha no dispatcher | Nenhum dos dois é do assunto deste item, que é endurecimento de segurança, e os dois são de portão — mexer no dispatcher de portões dentro de uma fase que os portões estão medindo é trocar a régua durante a medição. `026` já era a casa da classe "o dispatcher sai verde sobre um universo que não conseguiu montar": ele descrevia a forma latente, do modo por diff, e passa a descrever também a forma atual e medida, do arquivo não rastreado. `029` nasce ao lado de `024`, que é a mesma família — portão que não cobre o que diz cobrir |
| D39 | fase 1 | **O critério estrutural que sonda `app.use(helmet(` como literal fica como está, e a fragilidade vira observação de processo** | Reescrever o critério agora como padrão `ast-grep`, que é imune a formatação | O critério já foi julgado, com evidência: o validador cego mediu a relação por `ast-grep` e a deu por cumprida, porque o `prettier` quebrou a chamada em duas linhas e o literal deixou de existir sem que nada de real mudasse. Reescrever um critério depois do veredicto faria o veredicto validar outra coisa. O que sobra é a lição, e ela é de quem escreve critério, não de quem implementa: critério `estrutural` cujo sonda é um literal dependente de formatação reprova no próximo `--write` do formatador. Fica registrado aqui para a retrospectiva da corrida levar à skill que escreve plano |
| D40 | fase 2 | **A política do hotsite é a mesma em `next dev` e no build de produção; o console afogado do desenvolvimento vira o item de roadmap `031`** | Emitir em desenvolvimento uma política relaxada em `script-src` e `style-src`, como a documentação do Next sugere | Medido no navegador: em produção o hotsite responde com zero erro de console e hidrata; em desenvolvimento aparecem 35, um do `eval()` que o React usa só para depurar e 34 de estilo embutido do overlay `next-devtools` — nenhum é do produto. Relaxar por ambiente exigiria escrever em `middleware.ts` exatamente as duas strings que o critério `comando` da fase manda o `grep` não encontrar, e RF-24.2 e RF-24.3 são ubíquos: proíbem sem distinguir modo. Mudar isso é mudar documento aprovado, e o custo hoje recai sobre uma frente que ninguém está desenvolvendo |
| D41 | fase 2 | **`apps/site/src/middleware.ts` fica com a convenção que o Next 16 deprecou, e a migração para `proxy` vira o item de roadmap `030`** | Aceitar o codemod do próprio Next e renomear agora | RF-08.1 nomeia `middleware.ts`, e o critério estrutural da fase mede esse caminho. Renomear no meio da execução trocaria por baixo o arquivo que o portão verifica, sem passar pelo documento aprovado. A convenção antiga funciona nesta série e o build só avisa; a troca é mecânica e cabe num item próprio, com a reconciliação de RF-08.1 junto |
| D42 | fase 2 | **`exige_nonces_distintos` imprime o que mediu na saída padrão antes de reprovar pela de erro** | Mandar tudo para a saída de erro, como `_reprova` de `medir.sh` faz | O critério `comando` da fase lê a saída do comando para achar o valor repetido, e quem o executa não é obrigado a redirecionar a saída de erro. Portão que reprova calado não se distingue de ferramenta que quebrou — e aqui o valor repetido é a única informação que torna a reprovação acionável |
| D43 | fase 2 | **O passo do CI que verifica a política reconstrói o hotsite, em vez de reaproveitar o artefato do passo `Build` anterior** | Fazer o script assumir `.next/` já construído e só subir o servidor | O script é a mesma peça que se roda à mão, onde não há passo anterior nenhum; assumir artefato que talvez não exista é a segunda forma da tabela de portões que mentem — mede o que sobrou de outra execução e aprova sem ter construído. O build do hotsite leva menos de um segundo neste tamanho |
| D44 | fase 2 | **Dez dos onze achados da auditoria de segurança são corrigidos nesta fase; o décimo primeiro e as duas suspeitas viram os itens de roadmap `032`, `033` e `034`** | Abrir item para todos, ou corrigir todos aqui | Os dez cabem no arquivo que a fase já toca e fecham a classe de defeito que este repositório persegue — portão que aprova sem ter medido. Os três que saem exigem outra coisa: a página de erro global precisa de uma página de produto nova (conteúdo do hotsite, item `015`), o registro do portão precisa que o dispatcher aprenda a cobrar portão que não é por arquivo, e o par de isolamento entre origens muda o conjunto constante que o PRD declara — reconciliação de documento aprovado, não implementação |
| D45 | fase 2 | **A asserção de forma do nonce nasce como `exige_nonce_imprevisivel`, função separada** | Acrescentar a verificação de forma dentro de `exige_nonces_distintos`, que já compara os dois valores | O critério `comando` aprovado exercita `exige_nonces_distintos abc123 def456` e espera código zero: `abc123` não tem forma de base64 de 16 bytes, então a asserção embutida reprovaria o próprio critério. Separadas, cada uma responde uma pergunta — *repetiu?* e *é imprevisível?* — e as duas são chamadas no fluxo do portão |
| D46 | fase 2 | **O portão compara a política inteira com uma constante canônica, normalizando só o nonce** | Manter as nove buscas de substring, uma por diretiva | A auditoria reproduziu: `object-src 'none'` está contido em `object-src 'none' *`, e `script-src 'self' 'nonce-X'` está contido em `script-src 'self' 'nonce-X' https: *`. Nove buscas aprovavam 9 de 9 numa política onde o nonce era decoração e qualquer origem carregava script. Custo: acrescentar diretiva passa a exigir editar a constante, que é exatamente onde a decisão deve ser tomada conscientemente |
| D47 | fase 2 | **O servidor do portão grava o próprio identificador num arquivo e faz `exec`; o portão exige a porta livre antes de subir** | Confiar em `$!` depois do `setsid`, como estava | Reproduzido pela auditoria nos dois lados: com controle de trabalho ligado o `setsid` bifurca, `$!` morre, o `trap` não derruba nada e a porta fica ocupada; e um servidor já de pé na 3001 respondia todas as perguntas do portão, que aprovava um build que ninguém tinha servido. Exigir a porta livre é o que dá ao portão o direito de dizer de quem é a resposta que ele mede |
| D48 | fase 2 | **`poweredByHeader: false` entra nesta fase, embora nenhum `RF-nn` o peça para o hotsite** | Deixar como está e abrir item, já que a spec só proíbe `X-Powered-By` na API | É remoção de superfície entregue de graça, num arquivo que a fase já reescreve, e a frente irmã do **mesmo item** já o proíbe por `RF-09.3`. Assimetria entre duas frentes do mesmo endurecimento não é decisão, é esquecimento. Diferente de `034`, isto não acrescenta política nova a lugar nenhum |
| D49 | fase 2 | **A porta 3001 vira constante do portão, e deixa de ser variável de ambiente** | Passar a porta também ao servidor, mantendo a variável | O script `start` de `apps/site/package.json` fixa `-p 3001`. Configurar só o lado da medição fazia o portão medir uma porta onde o servidor não estava — e cair direto no defeito que `D47` acabou de fechar. Uma porta configurável de verdade exige o `package.json` junto, e nada hoje pede isso |
| D50 | fase 2 | **Os três apontamentos da validação cega viram registro, e nenhum vira correção nesta fase** | Corrigir o aviso de convenção deprecada e tirar a linha de `.gitignore` do diff | O primeiro já era o item `030`, aberto antes da validação; o terceiro — `style-src 'self'` numa frente que ainda não tem folha de estilo — não é verificável hoje e foi acoplado ao item `015-hotsite`, que traz a primeira. A linha `.playwright-mcp/` fica: ela nasceu de a validação de campo desta fase rodar num navegador de verdade, e tirá-la faria a próxima sessão sujar a árvore com o mesmo diretório |
| D51 | fase 2 | **O vazamento do prompt do orquestrador pela lista de processos vira o item de roadmap `035`, e não uma correção de madrugada** | Mudar agora como `scripts/loop/` passa o prompt ao processo filho | O próprio validador registrou que `ps aux` lhe entregou a linha de comando do orquestrador, com as normas da sessão dentro. A validação se sustenta — os critérios foram a régua, e a evidência de cada um está no veredicto —, mas a cegueira é o mecanismo do portão, e mecanismo que depende da boa vontade de quem ele mede não é mecanismo. Consertar isso é mexer no motor que despacha esta e todas as sessões seguintes, no meio de uma delas: é a mudança com maior raio de impacto e menor urgência do dia |
| D52 | fase 2 | **O problema que o `check` levanta sobre a fase 5 de `001` fica registrado como observação de processo, e nada é feito para calá-lo** | Revalidar a fase 5 de um item já encerrado, ou reescrever `validated_trees` para a árvore de hoje | `001-esqueleto-do-monorepo` está `done`, e o `check` acusa que `apps/api` e `apps/site` mudaram depois do veredicto da fase 5 — mudaram mesmo: foram as fases 1 e 2 **deste** item que as mudaram. A própria norma do harness diz que fase encerrada é história e não se cobra; o predicado é que não distingue "fase corrente de item vivo" de "última fase de item fechado". Revalidar seria gastar um validador contra o passado, e reescrever a árvore gravada seria apagar o rastro do que foi julgado. O conserto é no `state.py`, que mora fora deste repositório — vai para a retrospectiva da corrida, com `D39` |
| D53 | fase 3 | **O passo de CI que a etapa 3.4 pede é um script versionado, `apps/web/scripts/verificar-politica.sh`, e não um bloco de `grep` embutido no `run:`** | Escrever as asserções inline no YAML, que é o que o texto da etapa literalmente descreve | Asserção embutida em YAML não roda à mão, não tem teste que prove que ela morde, e repete a primeira armadilha da tabela de portões — `grep` que sai vazio tanto quando não achou quanto quando não conseguiu procurar. A fase 2 abriu o precedente com `apps/site/scripts/verificar-politica.sh`; o script chama `medir.sh`, e é ele que a seção *Como testar à mão* do PR entrega ao dono. O passo do CI continua sendo um passo `run:`, sem `uses:` novo, e a contagem de RNF-01 segue 27 |
| D54 | fase 3 | **O portão mede também os dois servidores do Vite, e não só o artefato de build que a etapa 3.4 nomeia** | Medir só `apps/web/dist/index.html`, deixando RF-07.1, RF-07.2, RF-07.3 e RF-10.2 provados uma única vez, na validação desta fase | A justificativa escrita na própria etapa 3.4 é que sem asserção o requisito «vale só no dia da fase e deixa de valer no primeiro PR que mexer no `index.html` ou no plugin». Os quatro cabeçalhos constantes e a ausência da meta em `vite dev` não são observáveis em arquivo: só existem em resposta HTTP, e o critério estrutural que os cobre é leitura de código, que aprova um `headers` que o Vite ignora. Custo: dois servidores efêmeros e cerca de quarenta segundos no job `qualidade`, que já constrói o artefato de que o `preview` precisa |

## Aprovações registradas em modo autônomo

Cada linha aqui é um `state.sh approve --por autonomo` ou um
`diverge-set --por autonomo` que o dono **não** deu.

| Estágio | Documento | O que foi aprovado | Quando |
|---|---|---|---|
| discovery | `00-discovery.md` | O estágio não tem aprovação: sai por `stage --stage prd`. As decisões D1–D12 acima são o que ele leva junto | 2026-09-03 |
| prd | `01-prd.md` | Aprovado sem você: `state.sh approve --stage prd --por autonomo`, `sha a9180bfa438303cf06c69c127a8d561cecc658d5`. O que ele fecha é o escopo dos cinco assuntos, o não-escopo de sete linhas — com os cabeçalhos que dependem de host e o `npm` do Dependabot lá dentro — e as métricas. É este documento que a spec em EARS traduz em `RF-nn` | 2026-09-03 |
| prd (reaprovação) | `01-prd.md` | Reaprovado sem você depois da reconciliação de `D-001`: `sha c1322f99d883caa0ae45a063604acae9a976946e`. O `sha` anterior amarrava um texto que a divergência tornou falso — a subseção dos cabeçalhos do hotsite e o não-escopo do `apps/web` foram reescritos no presente, e a reaprovação amarra o "sim" ao conteúdo que está no PR | 2026-09-03 |
| spec | `02-spec.md` | Aprovada sem você: `state.sh approve --stage spec --por autonomo`, `sha b499ec3c367b083dc0d87565c72397ed66142a30`. Vinte e cinco requisitos `RF-01`…`RF-25` em 68 frases EARS, quinze delas de comportamento indesejado. É deste documento que o `03-plan.md` tira os critérios de aceite tipados, um por `RF-nn` | 2026-09-03 |
| divergência | `04-divergencias/D-001.md` | Ratificada sem você na opção **(a)**: `diverge-set --id D-001 --status APROVADA --por autonomo`, depois `RECONCILIADA`. **O PR permanece `blocked-on-D-001` até você ratificar com `--por humano`** | 2026-09-03 |

| plan | `03-plan.md` | Aprovado sem você: `state.sh approve --stage plan --por autonomo`, `sha 42b11ae92acc86ed0c1935d3ed123649e02d40dc`. Cinco fases, uma por frente, com os critérios de aceite tipados que o validador cego de cada fase vai executar. A auditoria de cobertura rodou **antes** da aprovação, como a norma manda: reprovou na primeira passada com sete achados — dois requisitos provados só pela metade fácil, um adjetivo escondido num Given-When-Then, dois critérios sem requisito que os pedisse e dois comandos que não decidiam sozinhos —, e na segunda passada sobrou um, o código de saída de um `tee` mascarando o do build, que corrigi à mão. É este documento que a execução consome, fase por fase | 2026-09-03 |
| divergência | `04-divergencias/D-002.md` | Ratificada sem você na opção **(a)**: o padrão de origem ganha uma segunda peneira em vez de ser substituído. **O PR da Fase 1 permanece `blocked-on-D-002` até você ratificar com `--por humano`** | 2026-09-03 |
| divergência | `04-divergencias/D-003.md` | Ratificada sem você na opção **(a)**: cada cenário de teste reavalia o módulo com `jest.isolateModules`, porque `ConfigModule.forRoot` lê o ambiente uma vez por processo. **PR `blocked-on-D-003`** | 2026-09-03 |
| divergência | `04-divergencias/D-004.md` | Ratificada sem você na opção **(a)**: a fixture de chave privada do critério da Fase 4 passa a ser gerada em execução, em vez de entrar na allowlist. **PR `blocked-on-D-004`, aplicado pela Fase 4** | 2026-09-03 |

| divergência | `04-divergencias/D-005.md` | Ratificada sem você na opção **(a)**: os quatro endurecimentos que a auditoria mediu entram nesta fase em vez de virar item futuro. **PR `blocked-on-D-005`** | 2026-09-03 |
| divergência | `04-divergencias/D-006.md` | Ratificada sem você na opção **(a)**: a política de recurso entre origens volta ao padrão `same-origin`, corrigindo um ponto em que a recomendação de `D-005` errou. **PR `blocked-on-D-006`** | 2026-09-03 |


## O que ficou para o dono

- **A allowlist com que o `.gitleaks.toml` nasce (`D27`).** Duas entradas:
  `.env.example`, cujos valores são exemplo de máquina local publicados desde o
  primeiro commit, e a linha de `POSTGRES_PASSWORD` de `ci-nestjs.yml`, que é
  `${{ github.run_id }}-${{ github.run_attempt }}` — credencial descartável de um
  banco que só o runner efêmero alcança. **Allowlist é permissão concedida**, e
  esta é a única deste item. Cada entrada nasce com o motivo na linha acima, para
  quem revisar poder julgar se ela ainda vale.
- **O plano tem cinco fases, e o discovery decidiu quatro (`D22`).** Não mudei o
  escopo nem o roadmap: os assuntos continuam os quatro de `D10`. O que se
  quebrou em três foi o assunto do navegador, porque ele tocaria `apps/api`,
  `apps/site` e `apps/web` num PR só, e a norma do plano diz que fase de três
  stacks é três fases. Custo: dois PRs a mais na pilha. Ganho: três PRs que dá
  para revisar, e três frentes paralelizáveis por worktree.

- **A quebra do item, se ele discordar de D11.** O item tem cinco assuntos e
  quatro fases; a régua do INVEST passa, mas por margem. Quebrar é decisão dele,
  porque mexe no roadmap.
- **O robô de atualização de ações (D5).** É a única decisão deste discovery que
  põe algo novo agindo sobre o repositório de fora. Reversível apagando
  `.github/dependabot.yml`.
- **Os cabeçalhos que só o host de produção entrega** — `frame-ancestors` como
  cabeçalho de resposta e HSTS para o artefato `dist/` de `apps/web`. Estão nas
  *Pendências de produto abertas* do roadmap, dependentes da escolha de deploy,
  que é dele. `X-Frame-Options` saiu dessa lista por `D18`: ele já vale nos dois
  servidores do Vite que existem hoje.
- **`state.sh check` vai continuar acusando a fase 5 de `001` para sempre, e
  isso não é um defeito desta fase.** O item está `done`; o que mudou as árvores
  de `apps/api` e `apps/site` depois do veredicto dele foram as fases 1 e 2 deste
  item. Um problema permanente na saída do `check` é pior que nenhum, porque
  ensina a ignorá-la — mas o conserto é no `state.py`, fora deste repositório.
  Ver `D52`.

- **A validação cega desta fase leu o prompt do orquestrador por acidente
  (`ps aux`), e disse isso sozinha.** O veredicto continua de pé — cada critério
  tem comando e saída real no arquivo, e os critérios 4 a 8 foram medidos com
  `curl` próprio contra servidores que o validador subiu. Mas a cegueira é o que
  dá valor ao portão, e hoje ela depende de o agente não olhar. O conserto é o
  item `035`, e mexe no motor que despacha todas as sessões.

- **Seis itens novos no roadmap saíram da fase 2, e nenhum deles mudou o
  escopo deste item.** `030` migra o hotsite para a convenção que o Next 16
  passou a prescrever no lugar de `middleware`; `031` tira o console de
  `next dev` de baixo da política de produção; `032` é o único com risco de
  quebra em produção — a página de erro global do hotsite é pré-renderizada sem
  nonce, e seus dois `<script>` seriam bloqueados no dia em que ela aparecer;
  `033` põe no comando canônico dos portões os que medem resposta em vez de
  arquivo; `034` fecha a assimetria de isolamento entre origens entre a API e o
  hotsite; `035` tira o prompt do orquestrador do alcance do validador cego. Os
  três do meio vieram da auditoria de segurança, e o último da validação.

- **A divergência `D-001` — a política de conteúdo do hotsite sai de
  `middleware.ts` com nonce, e não de `headers()`.** Ratifiquei na opção (a) e
  reconciliei os documentos, mas a ratificação é autônoma: o PR fica
  `blocked-on-D-001` até ele confirmar. O que ele está confirmando é o custo —
  as rotas do hotsite deixam de ser prerenderizadas estaticamente —, e a
  alternativa que recusei foi `'unsafe-inline'` no `script-src`, que preservaria
  o prerender e esvaziaria a política.
