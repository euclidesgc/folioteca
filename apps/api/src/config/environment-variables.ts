export type EnvironmentVariables = {
  NODE_ENV: string;
  PORT: number;
  DATABASE_URL: string;
  WEB_ORIGIN: string;
  API_URL: string;
  BETTER_AUTH_SECRET: string;
  SMTP_HOST: string;
  SMTP_PORT: number;
  SMTP_USER: string;
  SMTP_PASSWORD: string;
  MAIL_FROM: string;
};
