packages:
  - "apps/*"
  - "packages/*"

# Nenhuma dependência roda script de instalação, e a recusa é nomeada. Na linha
# 11 do pnpm, script de build ignorado é erro duro em vez de aviso: a decisão
# tem de ser de alguém, não da omissão. `@scarf/scarf` é telemetria e não entra
# de jeito nenhum; `@parcel/watcher` e `unrs-resolver` são bindings nativos com
# caminho de reserva em JavaScript, e nada no projeto depende do ganho de
# desempenho deles. O script do `msw` instala o worker de service worker do
# navegador, e a web usa só `msw/node`.
allowBuilds:
  "@parcel/watcher": false
  "@scarf/scarf": false
  unrs-resolver: false
  msw: false

# `js-yaml` 4.2.0 entra por `@hey-api/openapi-ts` e carrega dois avisos de
# negação de serviço por análise quadrática. O gerador só lê `apps/api/openapi.json`,
# que é nosso e é JSON, então não há entrada hostil hoje — e é justamente por
# não haver que a correção é barata agora e cara no dia em que o contrato de um
# terceiro entrar no gerador.
overrides:
  js-yaml: ">=4.3.2"

# Versão publicada há menos de sete dias não entra na resolução. As campanhas
# recentes de publicação maliciosa em npm foram detectadas e despublicadas
# dentro dessa janela, e é dela que a espera tira o número: 10080 minutos. Mais
# que isso começa a atrasar correção de segurança legítima, que também chega por
# versão nova. Quem for tentado a baixar o valor precisa encontrar o argumento
# aqui, junto do número — e `scripts/gates/quarentena.sh` compara a leitura de
# `pnpm config get minimumReleaseAge` com ele, porque `undefined` não distingue
# "não configurei" de "troquei um caractere".
minimumReleaseAge: 10080

# A espera atrasa também a correção de segurança, que chega por versão nova — e
# no primeiro dia ela atrasou uma: a resolução sob a espera fixou `qs@6.15.3`,
# com GHSA-4mjr-xmp4-gh2g e GHSA-x5fp-wj9c-mxmx, no lugar de `qs@6.16.0`, que as
# corrige e tinha cinco dias. `qs` lê a query de toda requisição que chega ao
# `express`, e é caminho de produção da API.
#
# Isenção é a política morrendo por dentro quando não tem dono nem prazo, então
# esta tem os dois, e os dois são medidos: `scripts/gates/quarentena.sh` compara
# esta lista com uma constante própria e reprova qualquer nome a mais, e reprova
# também quando o vencimento chega.
#
# O vencimento é 2026-09-06, e o dia importa. `qs@6.16.0` foi publicada em
# 2026-08-29T23:50:15Z e completa os sete dias em 2026-09-05T23:50:15Z — quase o
# fim daquele dia. Até esse instante, `pnpm install` REPROVA o lockfile inteiro
# sem esta isenção, e não apenas ao reconstruí-lo: a verificação de política é
# feita contra as entradas já existentes, e devolve
# ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION. Medido no CI, em cinco jobs de uma vez.
#
# Uma data de vencimento igual ao DIA em que a versão sai da quarentena abre uma
# janela de vinte e quatro horas em que os dois lados reprovam: o portão porque a
# isenção venceu à meia-noite, e o `install` porque a versão ainda não completou a
# espera. A data certa é o dia SEGUINTE ao instante da liberação. Foi por isso
# que ela mudou de 05 para 06 — não para adiar o vermelho, que é o antipadrão que
# o prazo existe para impedir, mas porque a data anterior não era executável.
minimumReleaseAgeExclude:
  - qs
