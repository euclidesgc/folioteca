{
  "version": "0.2.0",
  "configurations": [
    {
      "// hml": "Código da branch rodando na máquina contra a API e o banco de homologação. VITE_API_URL da task vence o valor do .env da raiz — o Vite dá precedência ao process.env sobre os arquivos .env —, então nada sobe aqui além do Vite.",
      "name": "hml ▶ API e banco de homologação",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/apps/web",
      "preLaunchTask": "web hml (bg)",
      "postDebugTask": "para o web",
      "presentation": { "group": "1-aplicacao", "order": 2 }
    },
    {
      "// api (local)": "Metade servidor do composto 'local'. Fica oculta na lista porque não se roda sozinha: quem escolhe o ambiente é o composto.",
      "name": "api (local)",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}/apps/api",
      "preLaunchTask": "banco (up)",
      "runtimeExecutable": "${env:HOME}/.local/share/pnpm/pnpm",
      "runtimeArgs": ["run", "dev"],
      "console": "integratedTerminal",
      "skipFiles": ["<node_internals>/**"],
      "presentation": { "hidden": true }
    },
    {
      "// web (local)": "Metade navegador do composto 'local'. O preLaunchTask sobe o Vite e só devolve quando /health responder, para o navegador não abrir antes de a API existir.",
      "name": "web (local)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/apps/web",
      "preLaunchTask": "local: Vite e espera a API",
      "postDebugTask": "para o web",
      "presentation": { "hidden": true }
    }
  ],
  "compounds": [
    {
      "// local": "Banco em contêiner, API em watch e Vite, tudo na máquina, com breakpoint no navegador e no servidor. O banco fica de pé ao terminar; para derrubá-lo, a task 'banco (down)'.",
      "name": "local ▶ API e banco na máquina",
      "configurations": ["api (local)", "web (local)"],
      "stopAll": true,
      "presentation": { "group": "1-aplicacao", "order": 1 }
    }
  ]
}
