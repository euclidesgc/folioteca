# PLAN 007 — trash

Branch: `feature/007-trash` (sai de `feature/006-favorites`, que é a base real; toda comparação de "arquivo intocado" é contra ela, nunca contra `develop`).

Fonte: `docs/features/007-trash/spec.md` (as siglas D1…D15 são as decisões técnicas da SPEC e R1…R11 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Hocuspocus 4.7, Vite 8, Vitest com os projetos `api` e `web`, Playwright.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho que o próprio Vitest imprime sobre o ambiente jsdom é da ferramenta e **não conta**. O aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck final de cada fase roda com o cache do `tsc` limpo: `pnpm exec tsc -b --clean` e conferir que não sobrou nenhum `*.tsbuildinfo` fora de `node_modules` antes de `pnpm typecheck`.

## Regras que valem para todas as tarefas

- Uma única dependência nova, só em `apps/web`: `@radix-ui/react-alert-dialog`. Nomes de API de biblioteca (Prisma, Hocuspocus, Radix, TanStack Query, MSW, BlockNote, Playwright) vêm dos tipos do pacote instalado; os critérios medem comportamento e estrutura essencial.
- Todo componente devolve `React.JSX.Element` (nunca o `JSX` global); `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx` (inclusive gatilho, "Cancelar" e confirmar dos diálogos); navegação interna é `<Link>`/`<NavLink>`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case. Textos de tela em pt_BR; código, comentários e nomes de teste em en_US.
- Banco: a migration desta fatia é **escrita à mão** e aplicada por `prisma migrate deploy` (o `global-setup` dos testes da API já faz isso). Ninguém — agente, teste ou critério — roda os subcomandos `diff`, `reset` ou `dev` do `prisma migrate`. Conferência permitida: `pnpm --filter api exec prisma validate`. A limpeza entre testes é `resetDatabase(prisma)`.
- Nenhum literal com cara de senha, token ou hash em arquivo versionado. Sessões de teste vêm de `createPersonWithSession` (`apps/api/test/create-person.ts`) ou dos ajudantes já existentes; valor inválido é `randomUUID()`.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado. Não contam arquivos só de tipos, arquivos com 0 de 0 linhas instrumentáveis, nem os já excluídos pelo `vitest.config.ts` da raiz (`**/types/**`, `**/generated/**`, `**/src/testing/**`, `**/test/**`, `**/*.config.*`, `document-editor.tsx`, `editor-schema.ts`, `editor-dictionary.ts`). Nenhuma exclusão nova.
- **Toda** espera logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes unitários/integração da web pela constante `LAZY_TIMEOUT` do arquivo (`findBy…(…, {}, LAZY_TIMEOUT)`, `waitFor(…, LAZY_TIMEOUT)`); no e2e por constante declarada no topo do spec. Nunca `setTimeout`/sleep fixo nem `waitForTimeout`.
- Toda decisão de acesso é do servidor. Na web, a única ramificação por `accessLevel` é `document.accessLevel === 'owner'`, num único lugar (`document-view.tsx`).
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa.
- Nenhuma tarefa acrescenta escopo: sem expiração automática da lixeira, sem "esvaziar lixeira", sem seleção múltipla, sem lixeira na barra lateral, sem trocar os botões vermelhos de erro existentes pela variante `destructive`.

Desvios em relação à tabela de arquivos da SPEC, sem mudar escopo:

1. `docs/design.md` sai da fase 3 e entra na fase 2 (T2.2, T2.4 e T2.6): a skill `interface-design` manda a receita nova entrar na mesma entrega da tela que a usa.
2. Na fase 1, se o `pnpm typecheck` acusar objeto de teste da web tipado como `Document` ou `DocumentSummary` sem `trashedAt`, acrescenta-se `trashedAt: null` naquele objeto, e nada mais (os testes da web são reescritos na fase 2).

## Fase 1 — API: mover para a lixeira, restaurar, apagar definitivamente e listar a lixeira, sempre pelo caminho único de acesso, com a colaboração recusando gravação

Caminhos relativos à raiz do repositório.

- [x] T1.1 — Contrato primeiro: três operações novas, escopo `trash`, 409 no `PATCH` e `trashedAt` nos corpos
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script); `apps/web/src/testing/mocks/db.ts` (alterar); `apps/web/src/testing/mocks/handlers/documents.ts` (alterar)
  - O que fazer (D4, D5, D6):
    - `openapi.yaml`: `POST /documents/{documentId}/trash` (`operationId: trashDocument`) e `POST /documents/{documentId}/restore` (`operationId: restoreDocument`): sem corpo de requisição; respostas `200` com `DocumentResponse`, `401`, `403` e `404`. `DELETE /documents/{documentId}` (`operationId: deleteDocument`): `204` sem conteúdo, `401`, `403`, `404` e `409`. O `PATCH /documents/{documentId}` ganha a resposta `409`. O parâmetro `scope` de `GET /documents` passa a `enum: [mine, favorites, trash]`. Os schemas `Document` e `DocumentSummary` ganham `trashedAt` (`type: [string, 'null']` ou a forma equivalente que o arquivo já usa para nulo, `format: date-time`), listado em `required` nos dois, com descrição em pt_BR ("quando o documento foi movido para a lixeira; nulo se não está nela"). Reaproveitar os componentes de resposta de erro já existentes; se não houver um de 409, criar um no mesmo formato dos outros.
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`. O `.d.ts` nunca é editado à mão.
    - `db.ts` e `handlers/documents.ts` (**só** o necessário para o typecheck da raiz fechar; o resto do mock é da fase 2): `MockDocument` ganha `trashedAt: string | null`, nascendo `null` em todo lugar que cria documento simulado; os resumos devolvidos por `GET /documents` levam `trashedAt: null`.
  - Skills: api-mocking
  - Complexidade: baixa

- [x] T1.2 — Coluna `Document.trashedAt`: modelo, índice e migration à mão
  - Arquivos: `apps/api/prisma/schema.prisma` (alterar); `apps/api/prisma/migrations/0006_document_trashed_at/migration.sql` (criar)
  - O que fazer (D1): em `model Document`, `trashedAt DateTime?` e `@@index([ownerId, trashedAt(sort: Desc)])`. Migration **à mão**, no formato das anteriores, com exatamente: `ALTER TABLE "Document" ADD COLUMN "trashedAt" TIMESTAMP(3);` e `CREATE INDEX "Document_ownerId_trashedAt_idx" ON "Document"("ownerId", "trashedAt" DESC);`. As migrations `0001`–`0005` e `apps/api/test/reset-database.ts` não são tocados (nenhuma tabela nova). Consequência aceita, a registrar em comentário curto no schema: `trash` e `restore` avançam `updatedAt` (`@updatedAt`).
  - Skills: security
  - Complexidade: baixa

- [x] T1.3 — Decisão de acesso: lixeira nas duas portas de lista e a terceira pergunta `canWrite`
  - Arquivos: `apps/api/src/access/access.service.ts` (alterar)
  - O que fazer (D2, D3):
    - Ajudante **privado** `findDecision(documentId)`: valida o formato do id (o `UUID_PATTERN` que já existe; malformado → sem decisão) e lê **uma** vez `document.findFirst({ where: { id }, select: { ownerId: true, trashedAt: true } })`. `resolveAccess` e `canWrite` saem dele.
    - `resolveAccess(personId, documentId): Promise<AccessLevel>` — mesma assinatura. Proprietário continua `'owner'` **mesmo na lixeira** (precisa ver, restaurar e apagar). Documento na lixeira para qualquer outra pessoa → `'none'`: hoje já é verdade, mas o ramo fica **escrito e comentado**, antes do ponto onde os níveis de compartilhamento vão entrar.
    - Novo `canWrite(personId: string, documentId: string): Promise<boolean>` = `canEdit(nível) && trashedAt === null`; id malformado, inexistente ou alheio → `false`. É a resposta a "esta pessoa pode gravar título ou conteúdo agora?".
    - `readableDocumentsWhere(personId)` → `{ ownerId: personId, trashedAt: null }`: toda lista existente e futura exclui a lixeira sem mudar uma linha.
    - Nova porta `trashedDocumentsWhere(personId: string): Prisma.DocumentWhereInput` → `{ ownerId: personId, trashedAt: { not: null } }`. É a **única** expressão de leitura do código que enxerga `trashedAt` não nulo; só do proprietário.
    - Atualizar o comentário da classe (três portas de lista/decisão). `apps/api/src/access/access-level.ts` não muda (nenhum nível novo).
  - Skills: security
  - Complexidade: alta

- [x] T1.4 — Leitura: `trashedAt` nas respostas, lista da lixeira e o proprietário abrindo o documento que está nela
  - Arquivos: `apps/api/src/documents/documents.schema.ts` (alterar); `apps/api/src/documents/documents.service.ts` (alterar); `apps/api/src/documents/favorites.service.ts` (alterar); `apps/api/src/documents/documents.controller.ts` (alterar)
  - O que fazer (D2, D4, D6):
    - `documents.schema.ts`: `scope: z.enum(['mine', 'favorites', 'trash'])`, mantendo a mensagem de erro de `scope`.
    - `toDocument` converte `trashedAt` (`Date | null`) em ISO ou `null` — sem a conversão vazaria um `Date`. `create` devolve `trashedAt: null`.
    - `listMine` e `FavoritesService.list` devolvem `trashedAt: null` **literal** em cada resumo (a porta garante; não selecionar a coluna).
    - Novo `listTrash(personId: string): Promise<DocumentSummary[]>`: `document.findMany({ where: this.access.trashedDocumentsWhere(personId), orderBy: [{ trashedAt: 'desc' }, { id: 'desc' }], take: 100, select: { id, title, updatedAt, trashedAt } })`, mapeado com as duas datas em ISO.
    - `get` passa a ler com `AND: [{ id }, { OR: [readableDocumentsWhere(personId), trashedDocumentsWhere(personId)] }]` (o resto igual, inclusive o `include` de `favorites`).
    - `DocumentsController.getDocuments` despacha `trash` → `this.documents.listTrash(person.id)`; `mine` e `favorites` como hoje; escopo inválido continua 400 com o erro no campo `scope`.
  - Skills: security
  - Complexidade: média

- [x] T1.5 — Gravação: `trash`, `restore`, `delete` e o 409 do `rename`, com as rotas
  - Arquivos: `apps/api/src/documents/documents.service.ts` (alterar); `apps/api/src/documents/documents.controller.ts` (alterar)
  - O que fazer (D5, D6, R9). Tudo em `documents.service.ts` (o teste estrutural exige gravação de `Document` ali). As três operações começam por `resolveAccess`; **qualquer nível diferente de `'owner'`** → `throw documentNotFound()` (mensagem "Documento não encontrado.", a mesma para inexistente, alheio e id malformado; quando existirem `edit`/`view`, continuam recebendo 404).
    - `trash(personId: string, documentId: string): Promise<Document>` — `document.updateMany({ where: { id: documentId, trashedAt: null }, data: { trashedAt: new Date() } })` (repetir **não** renova a data); devolve `this.get(personId, documentId)`.
    - `restore(personId: string, documentId: string): Promise<Document>` — `updateMany({ where: { id: documentId, trashedAt: { not: null } }, data: { trashedAt: null } })`; devolve `this.get(…)`. Conteúdo e linhas de `Favorite` nunca são tocados. `trashedAt` em `where` de **gravação** é permitido.
    - `delete(personId: string, documentId: string): Promise<void>` — `document.deleteMany({ where: { id: documentId, trashedAt: { not: null } } })`; zero linhas → `ConflictException('Mova o documento para a lixeira antes de apagá-lo definitivamente.')`. `DocumentContent` e `Favorite` somem pelo `onDelete: Cascade` que já existe. Repetir responde 404 (o documento não existe mais).
    - `rename` ganha um passo. Ordem fixa: CSRF (403) → sessão (401) → `resolveAccess` (`'none'` → 404) → `canEdit` (403) → `canWrite` falso → **409** `ConflictException('Este documento está na lixeira. Restaure-o para editar.')` → corpo (400) → gravação. O 409 vem **antes** da validação do corpo.
    - Controller: `@Post(':documentId/trash')` e `@Post(':documentId/restore')`, ambos `@HttpCode(200)`, devolvendo `{ data }` no mesmo envelope das outras rotas de documento; `@Delete(':documentId')` com `@HttpCode(204)` e `Promise<void>`. O id chega cru (malformado é 404, nunca 400). Nenhum guard novo: CSRF e `SessionGuard` vêm da estrutura existente. Conferir que `@Delete(':documentId')` não captura `DELETE :documentId/favorite`.
  - Skills: security
  - Complexidade: alta

- [x] T1.6 — Colaboração: gravação condicional, conexão somente leitura e fechamento ao mover ou apagar
  - Arquivos: `apps/api/src/documents/documents.service.ts` (alterar); `apps/api/src/collab/collab.service.ts` (alterar)
  - O que fazer (D7, R10), três peças:
    1. `saveContent(documentId, state): Promise<boolean>` — dentro da transação, **primeiro** `tx.document.updateMany({ where: { id: documentId, trashedAt: null }, data: { updatedAt: new Date() } })`; `count === 0` (na lixeira ou apagado) → **não** faz o `upsert` do conteúdo, registra o aviso de descarte (`logger.warn`, no formato do que já existe) e devolve `false`; caso contrário faz o `upsert` e devolve `true`. O tratamento de `isMissingDocumentError` que já existe devolve `false`.
    2. `onConnect`: depois de `resolveAccess` (`'none'` → recusa, como hoje), `connectionConfig.readOnly = !(await this.access.canWrite(personId, documentName))`. O proprietário de documento na lixeira conecta e lê; o que escrever é descartado pelo Hocuspocus. `onStoreDocument` só transmite `STORED_MESSAGE` quando `saveContent` devolveu `true`.
    3. `DocumentsService.onDocumentClosed(listener: (documentId: string) => void): void` (lista de ouvintes em memória) e a chamada dos ouvintes **depois** de `trash` e de `delete` bem-sucedidos (não em `restore`, não em falha, não em 404/409). `CollabService` registra no construtor `this.documents.onDocumentClosed((id) => this.hocuspocus.closeConnections(id))`. Motivo, em comentário curto: sem fechar, o documento Yjs em memória guardaria as edições pós-lixeira e as gravaria ao restaurar; fechando, ele descarrega (a gravação do descarregamento é recusada pela peça 1) e a web reconecta sozinha como somente leitura.
    - `DocumentsModule` **não** importa `CollabModule` (seria circular); `documents.module.ts`, `attach-collab.ts` e `upgrade-gate.ts` não mudam. Atualizar o comentário "Limite conhecido" da classe `CollabService`.
  - Skills: security
  - Complexidade: alta

- [x] T1.7 — Testes da fase 1
  - Arquivos: `apps/api/src/documents/__tests__/trash.integration.test.ts` (criar); `apps/api/src/collab/__tests__/collab.integration.test.ts` (alterar); `apps/api/src/access/__tests__/access.integration.test.ts` (alterar); `apps/api/src/access/__tests__/document-access-boundary.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.contract.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.service.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.schema.test.ts` (alterar); `apps/api/src/documents/__tests__/favorites.service.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.integration.test.ts` (alterar); `apps/api/src/documents/__tests__/favorites.integration.test.ts` (alterar)
  - O que fazer: integração contra o Postgres real, sem mock do Prisma; `resetDatabase(prisma)` em `beforeEach`. Pessoa A como nos testes de documentos já existentes; pessoa B por `createPersonWithSession`. Documentos nascem por `POST /api/documents` com o cookie da dona. Nenhuma espera fixa: na colaboração, só os ajudantes de espera por condição que o arquivo já tem e o debounce de teste. Os casos existentes ficam, salvo o dito abaixo.
    - `trash.integration.test.ts`:
      - `POST trash answers 200 with trashedAt, hides the document from mine and favorites, lists it in trash and keeps it readable by the owner` (favoritado antes; `GET /api/documents/{id}` do dono traz `trashedAt`);
      - `a second POST trash answers 200 with the same trashedAt`;
      - `POST restore answers 200 with trashedAt null and brings the document back to mine and favorites with the same favorite row` (mesmo `createdAt` da linha de `Favorite`);
      - `restore keeps the stored Yjs state byte for byte` (estado inserido direto em `DocumentContent` antes de mover);
      - `a second POST restore still answers 200`;
      - `lists the trash from the most recently trashed to the oldest` (três documentos; `trashedAt` ajustado direto no banco, sem depender do relógio);
      - `another person gets 404 on trash, restore and DELETE, identical to unknown and malformed ids, and no row changes` (documento de A dentro **e** fora da lixeira; corpos comparados entre si com `toEqual`; mensagem "Documento não encontrado."; `trashedAt` e `document.count()` iguais antes e depois);
      - `the trash list of another person is empty and GET of a trashed document of someone else answers 404`;
      - `DELETE outside the trash answers 409 with the message and deletes nothing`;
      - `DELETE in the trash answers 204 and removes the document, its content and every favorite` (favorito de A pela API e de B inserido direto no banco);
      - `a second DELETE answers 404`;
      - `PATCH in the trash answers 409 with the message and keeps the title`;
      - `PATCH with an invalid body in the trash answers 409, not 400`;
      - `PATCH after restore answers 200`;
      - `answers 403 without the X-Requested-With header` (as três operações);
      - `answers 401 without a session cookie` (as três operações);
      - `answers 400 with the scope error for an unknown scope`.
    - `collab.integration.test.ts` (acrescentar): `closes the open connection when the document goes to the trash and stores nothing written afterwards`; `a new connection to a trashed document syncs the content, discards writes and gets no stored message`; `after restore the text written in the trash does not come back and a new connection stores normally`.
    - `access.integration.test.ts`: o cenário ganha um documento na lixeira de cada dono; acrescentar `the owner of a trashed document still resolves to owner`; `another person resolves to none for a trashed document`; `canWrite is true outside the trash and false in the trash, for another person and for a malformed id`; `readableDocumentsWhere excludes trashed documents and trashedDocumentsWhere includes only the trashed documents of the owner`. O caso existente de concordância das portas é **reescrito** como `the doors agree: resolveAccess is not none exactly when the document is in the readable or in the trashed list, never in both`.
    - `document-access-boundary.test.ts` (acrescentar; as sete regras e os casos existentes ficam): **regra 8**, função `checkTrashGate`, no mesmo formato das outras (lê os arquivos de `apps/api/src` do disco, fora `__tests__/`; a falha aponta arquivo e linha): (1) `trashedDocumentsWhere(` só é **chamado** em `src/documents/documents.service.ts` (a definição em `src/access/access.service.ts` não conta como chamada); (2) em todo o `src/`, uma chamada de leitura (`findMany`, `findFirst`, `count`, `aggregate`, `groupBy`, em `.document.` ou `.favorite.`) só pode conter o texto `trashedAt` se também contiver `trashedDocumentsWhere(`; (3) o corpo de `readableDocumentsWhere` em `access.service.ts` contém `trashedAt: null`. Casos: `only documents.service.ts calls trashedDocumentsWhere`; `no document or favorite read mentions trashedAt without trashedDocumentsWhere`; `readableDocumentsWhere keeps trashedAt null`; `rule 8 flags a sample offender and accepts a sample compliant snippet`.
    - `documents.contract.test.ts` (acrescentar): `POST trash answers the documented 200`; `POST restore answers the documented 200`; `DELETE answers 204 in the trash and the documented 409 outside it`; `PATCH in the trash answers the documented 409`; `GET documents with scope trash matches DocumentsResponse`; `every document and summary body carries trashedAt`.
    - `documents.service.test.ts` (acrescentar; registros simulados ganham `trashedAt`): `toDocument turns trashedAt into an ISO string or null`; `listTrash filters by trashedDocumentsWhere, orders by trashedAt desc then id desc and takes 100`; `listMine reports trashedAt null`; `get reads through the readable or the trashed documents of the person`; `rename throws 409 before parsing the body when canWrite is false and writes nothing`; `trash, restore and delete throw document not found when access is not owner and write nothing`; `delete throws 409 when no row was deleted`; `saveContent returns false without upsert when no document row was updated`; `saveContent returns true after storing`; `closed listeners run after a successful trash and delete, never on restore or failure`.
    - `documents.schema.test.ts`: acrescentar `accepts the trash scope`; o caso de rejeição existente passa a `rejects a scope outside mine, favorites and trash`.
    - `favorites.service.test.ts` (acrescentar): `list reports trashedAt null in every summary`.
    - `documents.integration.test.ts` e `favorites.integration.test.ts`: tipos e corpos esperados ganham `trashedAt: null`; em `documents.integration.test.ts` acrescentar `a new document is not in the trash`.
  - Skills: unit-testing, integration-testing, security
  - Complexidade: alta

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` passam **sem erro nem aviso** (sem aviso de peer no install; sem aviso de chunk grande no build; o aviso de desempenho do Vitest sobre jsdom não conta). A saída de `pnpm test` não tem `console.error` nem `console.warn` vazando, nem aviso do Nest, do Prisma ou do Hocuspocus. `git diff feature/006-favorites... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json` não mostra dependência nova.
- [x] CA1.2 — `packages/api-contract/openapi.yaml` descreve `POST /documents/{documentId}/trash` (`trashDocument`) e `POST /documents/{documentId}/restore` (`restoreDocument`), sem corpo de requisição, com `200` (`DocumentResponse`), `401`, `403` e `404`; `DELETE /documents/{documentId}` (`deleteDocument`) com `204`, `401`, `403`, `404` e `409`; `409` no `PATCH /documents/{documentId}`; `scope` aceitando `mine`, `favorites` e `trash`; `trashedAt` (data-hora ou nulo) **obrigatório** em `Document` e em `DocumentSummary`. `packages/api-contract/src/generated/openapi.d.ts` reflete o YAML (o teste de tipos gerados do pacote passa).
- [x] CA1.3 — `apps/api/prisma/schema.prisma`: `Document` tem `trashedAt DateTime?` e índice por `(ownerId, trashedAt desc)`. Existe **uma** migration nova, em SQL escrito à mão, que acrescenta a coluna `"trashedAt" TIMESTAMP(3)` (anulável, sem default) e cria o índice em `("ownerId", "trashedAt" DESC)`. `pnpm --filter api exec prisma validate` passa. `git diff --stat feature/006-favorites... -- apps/api/prisma/migrations` lista só o arquivo novo, e `git diff --stat feature/006-favorites... -- apps/api/test/reset-database.ts` é vazio.
- [x] CA1.4 — `apps/api/src/access/access.service.ts` tem `resolveAccess(personId: string, documentId: string): Promise<AccessLevel>`, `canWrite(personId: string, documentId: string): Promise<boolean>`, `readableDocumentsWhere(personId: string): Prisma.DocumentWhereInput` e `trashedDocumentsWhere(personId: string): Prisma.DocumentWhereInput`. Lendo o código: `resolveAccess` e `canWrite` saem de um mesmo ajudante privado que lê `ownerId` e `trashedAt` numa consulta só; o proprietário de documento na lixeira resolve `'owner'`; há um ramo escrito e comentado que dá `'none'` a quem não é proprietário de documento na lixeira; `canWrite` é falso na lixeira; `readableDocumentsWhere` devolve `ownerId` da pessoa **e** `trashedAt: null`; `trashedDocumentsWhere` devolve `ownerId` da pessoa **e** `trashedAt` não nulo. `git diff --stat feature/006-favorites... -- apps/api/src/access/access-level.ts` é vazio.
- [x] CA1.5 — `apps/api/src/documents/documents.service.ts` tem `listTrash(personId: string): Promise<DocumentSummary[]>` (filtra por `trashedDocumentsWhere`, ordena por `trashedAt` desc com desempate por `id` desc, limita a 100, devolve `trashedAt` em ISO), `trash(personId: string, documentId: string): Promise<Document>`, `restore(personId: string, documentId: string): Promise<Document>`, `delete(personId: string, documentId: string): Promise<void>`, `onDocumentClosed(listener: (documentId: string) => void): void` e `saveContent(documentId: string, state: Uint8Array): Promise<boolean>`. Lendo o código: as três operações chamam `resolveAccess` **antes** de gravar e lançam `documentNotFound()` para qualquer nível diferente de `'owner'`; `trash` só grava onde `trashedAt` é nulo (repetir não renova a data) e `restore` só onde não é nulo; `delete` só apaga o que está na lixeira e, com zero linhas, lança 409 com o texto "Mova o documento para a lixeira antes de apagá-lo definitivamente."; `rename` lança 409 "Este documento está na lixeira. Restaure-o para editar." depois da checagem de permissão e **antes** de validar o corpo; `get` lê pelo `OR` das duas portas; `toDocument` devolve `trashedAt` como string ISO ou `null`; `saveContent` primeiro atualiza o documento só se `trashedAt` for nulo e, sem linha atualizada, não grava o conteúdo e devolve `false`; os ouvintes são chamados só depois de `trash` e `delete` bem-sucedidos. O construtor continua com `PrismaService` e `AccessService`.
- [x] CA1.6 — `apps/api/src/documents/documents.controller.ts` tem `POST :documentId/trash` e `POST :documentId/restore` respondendo `200` com o documento no envelope `data`, e `DELETE :documentId` respondendo `204` sem corpo, sob o mesmo `SessionGuard` da classe e sem validação de formato do id; `getDocuments` despacha `trash` para `DocumentsService.listTrash`. `apps/api/src/documents/documents.schema.ts` aceita exatamente `mine`, `favorites` e `trash`. `apps/api/src/documents/favorites.service.ts` devolve `trashedAt: null` em cada resumo. A rota `DELETE :documentId/favorite` continua funcionando (os testes de favoritos da fatia anterior passam).
- [x] CA1.7 — `apps/api/src/collab/collab.service.ts`: ao conectar, a conexão é somente leitura quando `canWrite` é falso (e continua recusada quando o acesso é `'none'`); a mensagem `stored` só é transmitida quando `saveContent` devolveu `true`; o construtor registra em `DocumentsService.onDocumentClosed` um ouvinte que fecha as conexões **daquele** documento no Hocuspocus. `DocumentsModule` não importa `CollabModule`: `git diff --stat feature/006-favorites... -- apps/api/src/documents/documents.module.ts apps/api/src/collab/attach-collab.ts apps/api/src/collab/upgrade-gate.ts` é vazio.
- [x] CA1.8 — Fronteira da lixeira: `rg -n "trashedDocumentsWhere\(" apps/api/src --glob '!**/__tests__/**'` só acha `apps/api/src/access/access.service.ts` (definição) e `apps/api/src/documents/documents.service.ts`. `document-access-boundary.test.ts` tem a função `checkTrashGate`, aplicada ao código real lido do disco e a uma amostra infratora × amostra correta, com as três checagens descritas em T1.7.
- [x] CA1.9 — Existem e passam, pelos nomes listados em T1.7: `trash.integration.test.ts` (17 casos, entre eles `a second POST trash answers 200 with the same trashedAt`, `restore keeps the stored Yjs state byte for byte`, `another person gets 404 on trash, restore and DELETE, identical to unknown and malformed ids, and no row changes`, `DELETE in the trash answers 204 and removes the document, its content and every favorite`, `PATCH with an invalid body in the trash answers 409, not 400` e os de 403, 401 e 400), os 3 novos de `collab.integration.test.ts`, os 4 novos e o reescrito `the doors agree: …` de `access.integration.test.ts`, os 4 novos de `document-access-boundary.test.ts`, os 6 novos de `documents.contract.test.ts`, os 10 novos de `documents.service.test.ts`, `accepts the trash scope` e `rejects a scope outside mine, favorites and trash` em `documents.schema.test.ts`, o novo de `favorites.service.test.ts` e `a new document is not in the trash` em `documents.integration.test.ts`. A integração usa o Postgres real (`rg -n "vi\.mock\(.*prisma" apps/api/src/documents/__tests__/trash.integration.test.ts` é vazio). Os testes das fatias anteriores continuam passando. Conferir com `npx vitest run --project api --reporter=verbose`.
- [x] CA1.10 — Nenhuma espera fixa nem credencial literal nos testes novos: `rg -n "setTimeout\(|sleep\(" apps/api/src/documents/__tests__/trash.integration.test.ts` é vazio; os três casos novos de `collab.integration.test.ts` esperam por condição (ajudantes já existentes no arquivo), sem espera fixa nova; `rg -n "folioteca_session=[A-Za-z0-9]" apps/api/src/documents/__tests__ apps/api/src/collab/__tests__ apps/api/src/access/__tests__` é vazio.
- [x] CA1.11 — O relatório de cobertura de `pnpm test` lista com ≥ 80% de linhas cada: `apps/api/src/access/access.service.ts`, `apps/api/src/documents/documents.service.ts`, `documents/documents.controller.ts`, `documents/documents.schema.ts`, `documents/favorites.service.ts` e `apps/api/src/collab/collab.service.ts`; arquivo ausente do relatório conta como reprovado (salvo 0 de 0 linhas). `git diff feature/006-favorites... -- vitest.config.ts` é vazio.
- [x] CA1.12 — Web só o mínimo: em `apps/web/src/testing/mocks/db.ts`, `MockDocument` tem `trashedAt: string | null`, e os resumos de `GET /documents` em `handlers/documents.ts` levam `trashedAt`. `git diff --stat feature/006-favorites... -- apps/web/src/features apps/web/src/app apps/web/src/components apps/web/src/types/api.ts apps/web/package.json` só mostra, no máximo, arquivos dentro de `__tests__/` (o desvio 2 do topo do plano).

## Fase 2 — Web: "Mover para a lixeira" com confirmação, documento somente leitura na lixeira, página "Lixeira" com restaurar e apagar definitivamente

Caminhos relativos a `apps/web/`, salvo os que começam por `docs/` ou `pnpm-lock.yaml`.

- [x] T2.1 — API simulada: lixeira no banco em memória, as três operações, o escopo novo e a chave de desenvolvimento
  - Arquivos: `src/testing/mocks/db.ts` (alterar); `src/testing/mocks/handlers/documents.ts` (alterar); `src/testing/mocks/index.ts` (alterar); `src/testing/mocks/utils.ts` (alterar)
  - O que fazer (D15):
    - `handlers/documents.ts`: `GET /documents` com `scope=mine` e `scope=favorites` **filtra** os documentos com `trashedAt` não nulo; `scope=trash` devolve só os que estão na lixeira, por `trashedAt` desc, limite 100, com `trashedAt` no resumo; escopo fora dos três continua 400 com o erro de `scope`. Handlers novos `http.post('…/documents/:documentId/trash')`, `http.post('…/documents/:documentId/restore')` e `http.delete('…/documents/:documentId')`, com o mesmo começo dos existentes (`networkDelay`, `devOverride('documents')`, sem cookie → 401 "Sessão não encontrada.", documento inexistente → 404 "Documento não encontrado."): `trash` grava `trashedAt` só se for nulo (repetir não renova) e responde 200 com o corpo do documento; `restore` zera e responde 200 com o corpo; `DELETE` fora da lixeira → 409 "Mova o documento para a lixeira antes de apagá-lo definitivamente.", na lixeira → 204 sem corpo, apagando o documento **e** os favoritos dele. `PATCH` em documento na lixeira → 409 "Este documento está na lixeira. Restaure-o para editar.". Os corpos de documento levam o `trashedAt` real. O handler `DELETE …/:documentId/favorite` continua funcionando.
    - `db.ts`: `export const seedSampleTrash = (): void` — move dois documentos de exemplo para a lixeira, com `trashedAt` decrescentes; sem documentos, não faz nada.
    - `mocks/index.ts`: chave de `localStorage` `mock-trash`; valor `sample` chama `seedSampleTrash()` **depois** do seed dos documentos e dos favoritos. `utils.ts`: documentar a chave nova no mesmo bloco de comentário das outras.
  - Skills: api-mocking
  - Complexidade: média

- [x] T2.2 — `ConfirmationDialog` compartilhado sobre o AlertDialog do Radix e a variante `destructive` do `Button`
  - Arquivos: `package.json` (alterar); `pnpm-lock.yaml` da raiz (alterar, só pelo `pnpm`); `src/components/ui/confirmation-dialog/confirmation-dialog.tsx` (criar); `src/components/ui/button/button.tsx` (alterar); `docs/design.md` (alterar)
  - O que fazer (D9, R11):
    - Instalar `@radix-ui/react-alert-dialog` em `apps/web` (`pnpm --filter web add @radix-ui/react-alert-dialog`). Conferir que o pacote aceita `react@^19` e que o `pnpm install` não imprime aviso; **se imprimir, parar e reportar** em vez de contornar.
    - `ConfirmationDialog`, **controlado**, sem domínio, sem API, sem store, devolvendo `React.JSX.Element`. Props: `open: boolean`; `onOpenChange: (open: boolean) => void`; `trigger: React.ReactNode` (vai no gatilho do primitivo, em modo `asChild`); `title: string` (título do primitivo = nome acessível); `description: React.ReactNode` (descrição do primitivo = descrição acessível); `confirmButton: React.ReactNode` (slot); `cancelLabel?: string` (padrão "Cancelar"; no cancelar do primitivo, em modo `asChild`, com `Button variant="secondary"`).
    - O confirmar é um `Button` comum no slot, **não** a ação do primitivo (que fecharia no clique): o diálogo fica aberto enquanto a mutação está pendente e continua aberto se falhar; quem fecha é o chamador, por `onOpenChange(false)`. Registrar em comentário curto.
    - Tudo o que é comportamento vem do primitivo, nada reimplementado: `role="alertdialog"`, não fecha com clique fora, foco inicial em "Cancelar" (a opção segura), foco preso, `Esc` fecha, foco devolvido ao gatilho.
    - Estrutura: fundo escurecido cobrindo a tela; caixa centralizada, largura limitada e com respiro lateral em 360px; título; descrição em tom suave com quebra de palavra longa; rodapé alinhado à direita, que quebra linha, com "Cancelar" **antes** do confirmar.
    - `button.tsx`: variante `destructive` = base do botão + `bg-red-600 text-white hover:bg-red-700 focus-visible:outline-red-600`. `primary`, `secondary`, `ghost` e o padrão não mudam. Os botões de erro existentes, que repetem essas classes por `className`, **não** são trocados.
    - `docs/design.md`, tabela "Padrões acrescentados pelas entregas", Fatia = 007: "Botão destrutivo (`destructive`)" — base do botão + `bg-red-600 text-white hover:bg-red-700 focus-visible:outline-red-600`; "Diálogo de confirmação" — fundo `fixed inset-0 z-50 bg-black/50`; caixa `fixed left-1/2 top-1/2 z-50 w-[calc(100%-2rem)] max-w-md -translate-x-1/2 -translate-y-1/2 rounded-md bg-white p-6 shadow-lg`; título `text-lg font-semibold text-gray-900`; descrição `mt-2 text-sm text-gray-600 break-words`; rodapé `mt-6 flex flex-wrap justify-end gap-2` com "Cancelar" (secundário) antes do confirmar.
  - Skills: security, ui-components, interface-design
  - Complexidade: média

- [x] T2.3 — Chamadas de API: escopo `trash` e as três mutações
  - Arquivos: `src/features/documents/api/get-documents.ts` (alterar); `src/features/documents/api/trash-document.ts` (criar); `src/features/documents/api/restore-document.ts` (criar); `src/features/documents/api/delete-document.ts` (criar); `src/features/documents/components/sidebar-documents.tsx` (alterar)
  - O que fazer (D8):
    - `get-documents.ts`: `DocumentsScope = 'mine' | 'favorites' | 'trash'` e a lista de escopos que `invalidateDocumentLists` percorre ganha `'trash'` — a função passa a cobrir a lixeira **sem mudar de assinatura**.
    - `trash-document.ts`: `trashDocument({ documentId }: { documentId: string }): Promise<DocumentResponse>` (`api.post(`/documents/${documentId}/trash`)`) e `useTrashDocument({ mutationConfig }: { mutationConfig?: MutationConfig<typeof trashDocument> } = {})`. `restore-document.ts`: `restoreDocument` / `useRestoreDocument`, idem com `/restore`. Nos dois, o `onSuccess` interno grava a resposta no cache do documento (`setQueryData` na chave de `getDocumentQueryOptions(documentId)`), chama `invalidateDocumentLists(queryClient)` e **depois** repassa o `onSuccess` de quem chamou.
    - `delete-document.ts`: `deleteDocument({ documentId }): Promise<void>` (`api.delete(`/documents/${documentId}`)`) e `useDeleteDocument({ mutationConfig })`; o `onSuccess` interno remove a consulta do documento do cache (`removeQueries`), chama `invalidateDocumentLists` e repassa o `onSuccess`.
    - Sem atualização otimista (ações confirmadas e raras; o botão mostra o pendente). Sem `try/catch` e sem notificação de erro própria: a falha é avisada pelo interceptor do cliente HTTP que já existe; a notificação de **sucesso** é de quem chama.
    - `sidebar-documents.tsx`: a prop vira `scope?: Exclude<DocumentsScope, 'trash'>` e o mapa de textos é tipado por esse mesmo tipo; nenhum texto novo, nenhum comportamento novo (a barra lateral não lista a lixeira).
  - Skills: api-requests, error-handling
  - Complexidade: média

- [x] T2.4 — Componentes de ação: `TrashDocumentButton` e `TrashedDocumentActions`
  - Arquivos: `src/features/documents/components/trash-document-button.tsx` (criar); `src/features/documents/components/trashed-document-actions.tsx` (criar); `docs/design.md` (alterar)
  - O que fazer (D10, R1, R4, R8, R11):
    - `TrashDocumentButton({ document }: { document: Pick<Document, 'id' | 'title'> }): React.JSX.Element` — gatilho: `Button variant="ghost"` com ícone de lixeira (`<svg>` inline no arquivo, `aria-hidden="true"`, `focusable="false"`, tamanho da receita, traço `currentColor`) + texto visível "Mover para a lixeira" (o nome acessível é o texto; sem `aria-label`). Estado `open` local. Usa `ConfirmationDialog`:
      - título "Mover para a lixeira?"; descrição "“{título}” sai das suas listas e dos favoritos. Você pode restaurar o documento depois, pela Lixeira." (aspas tipográficas; `{título}` é o título do documento); botões "Cancelar" e "Mover para a lixeira" (`Button variant="primary"` com `isLoading`; texto "Movendo…" enquanto pendente);
      - sucesso: fecha o diálogo e notifica, por `useNotifications`, "Documento movido para a lixeira";
      - falha: o diálogo **continua aberto**; a notificação é a do interceptor.
    - `TrashedDocumentActions({ document, onDeleted }: { document: Pick<Document, 'id' | 'title'>; onDeleted?: () => void }): React.JSX.Element` — usado na página do documento **e** em cada item da lista da lixeira, uma marcação só:
      - "Restaurar" (`Button variant="secondary"`, **sem** confirmação — é a ação reversível; pendente: "Restaurando…"); sucesso: notificação "Documento restaurado";
      - "Apagar definitivamente" (`Button variant="ghost"` no tom vermelho da receita "Ação destrutiva discreta"), gatilho do segundo `ConfirmationDialog`: título "Apagar definitivamente?"; descrição "“{título}” e todo o seu conteúdo serão apagados para sempre. Esta ação não tem volta."; botões "Cancelar" e "Apagar definitivamente" (`Button variant="destructive"` com `isLoading`; pendente: "Apagando…"); sucesso: fecha o diálogo, notifica "Documento apagado definitivamente" e chama `onDeleted?.()`; falha: diálogo continua aberto.
      - contêiner dos dois botões pela receita "Ações do item de lista" (quebra linha, alinha à direita, largura total em tela estreita).
    - Clique duplo (`component-robustness` §9, sem desvio): `disabled`/`isLoading` enquanto `isPending` **e** retorno antecipado no manipulador. Em `TrashedDocumentActions`, enquanto **qualquer uma** das duas mutações está pendente, os dois botões ficam desabilitados.
    - `docs/design.md`, Fatia = 007: "Ação destrutiva discreta" — botão `ghost` com `text-red-700 hover:bg-red-50 focus-visible:outline-red-600` (gatilho de "Apagar definitivamente"); "Ícone de lixeira" — `<svg aria-hidden="true" focusable="false" className="size-5">`, traço `currentColor`; "Ações do item de lista" — `<div className="flex w-full flex-wrap justify-end gap-2 sm:w-auto">`; o item da lista ganha `flex-wrap`.
  - Skills: interface-design, component-robustness, error-handling
  - Complexidade: alta

- [x] T2.5 — Página do documento: botão de mover só para o proprietário e o modo lixeira
  - Arquivos: `src/features/documents/components/document-view.tsx` (alterar); `src/features/documents/components/document-editor.tsx` (alterar)
  - O que fazer (D11, R1, R3, R4, R11). **Só** `LoadedDocument` muda; carregando, não encontrado e erro ficam como estão (quem não é dono de documento na lixeira vê "Documento não encontrado", porque a API responde 404). `const isTrashed = document.trashedAt !== null`.
    - Fora da lixeira: como hoje, e a linha de ações passa a `[Mover para a lixeira] [estrela]`, à direita, com espaço entre os dois; `TrashDocumentButton` **só** quando `document.accessLevel === 'owner'` (dado que vem do servidor; comentário curto dizendo que o servidor aplica a mesma regra).
    - Na lixeira, de cima para baixo: **aviso** → `h1` **visível** com o título (receita "Título de página", com quebra de palavra longa; deixa de ser `sr-only`) → editor somente leitura. **Sem** linha de ações, **sem** estrela, **sem** `DocumentTitleForm`, **sem** `SaveIndicator`. O aviso usa a receita "Aviso informativo" como `<div>` focável por programa (`tabIndex={-1}`), com o texto "Este documento está na lixeira desde {data e hora}. Restaure-o para voltar a editar." (`formatDateTime(document.trashedAt)`, de `@/utils/format-date-time`) e, abaixo, `<TrashedDocumentActions document={document} onDeleted={…} />`.
    - `onDeleted` navega para `paths.trash.getHref()` com `useNavigate`.
    - Foco: quando o documento **passa a estar** na lixeira nesta tela (o gatilho do diálogo some), um efeito leva o foco ao aviso. Documento que já abre na lixeira **não** rouba o foco. Ao restaurar, nenhuma gestão extra.
    - A sessão de colaboração continua sendo aberta na lixeira (o conteúdo só chega pelo Yjs; o servidor a marca somente leitura).
    - `document-editor.tsx`: prop `editable?: boolean` (padrão `true`), repassada ao componente de visualização do BlockNote (`editable`); `document-view.tsx` passa `editable={!isTrashed}`.
    - `document-title-form.tsx`, `favorite-button.tsx` e `hooks/use-document-collaboration.ts` não mudam.
  - Skills: interface-design, authorization, component-robustness
  - Complexidade: alta

- [x] T2.6 — Lista por escopo com a lixeira e a página "Lixeira"
  - Arquivos: `src/features/documents/components/documents-list.tsx` (alterar); `src/app/routes/app/trash.tsx` (alterar)
  - O que fazer (D12, R5, R6). O mapa interno por escopo de `DocumentsList` ganha a entrada `trash` e passa a carregar também **qual data mostrar** e **se o item tem ações**. Marcação, estados e classes **não** são duplicados; os textos de `mine` e `favorites` não mudam.

    | Estado (`scope="trash"`) | O que aparece |
    |---|---|
    | Carregando (`role="status"`) | "Carregando lixeira…" |
    | Vazio | "A lixeira está vazia. Os documentos que você excluir aparecem aqui." |
    | Erro (`role="alert"`) | "Não foi possível carregar a lixeira." + botão vermelho "Tentar novamente" |
    | Com dados | receita "Lista": título como link para o documento (truncado, com `title`); `<time dateTime={trashedAt}>` com "Na lixeira desde {data e hora}" em tom suave; `<TrashedDocumentActions document={document} />` por item (sem `onDeleted`: ao apagar pela lista, a pessoa continua na Lixeira); na ordem da API |

    - Nos outros escopos o `<time>` continua mostrando `updatedAt` sem prefixo e o item não tem ações. O `<li>` ganha quebra de linha (`flex-wrap`) para caber em 360px sem rolagem horizontal.
    - `trash.tsx`: mantém `ContentLayout` com `title="Lixeira"` e o apoio "Documentos excluídos ficam aqui até serem restaurados ou apagados de vez.", e troca o parágrafo fixo por `<DocumentsList scope="trash" />`. `router.tsx`, `paths.ts` e `app-layout.tsx` não mudam (rota e link "Lixeira" já existem).
  - Skills: interface-design, component-robustness, routing
  - Complexidade: média

- [x] T2.7 — Testes da fase 2
  - Arquivos: `src/components/ui/confirmation-dialog/__tests__/confirmation-dialog.test.tsx` (criar); `src/features/documents/api/__tests__/trash-document.test.tsx` (criar); `src/features/documents/api/__tests__/restore-document.test.tsx` (criar); `src/features/documents/api/__tests__/delete-document.test.tsx` (criar); `src/features/documents/components/__tests__/trash-document-button.test.tsx` (criar); `src/features/documents/components/__tests__/trashed-document-actions.test.tsx` (criar); `src/app/routes/app/__tests__/trash.test.tsx` (criar); `src/app/routes/app/__tests__/empty-areas.test.tsx` (alterar); `src/features/documents/components/__tests__/document-view.test.tsx` (alterar); `src/features/documents/components/__tests__/documents-list.test.tsx` (alterar); `src/features/documents/components/__tests__/sidebar-documents.test.tsx` (alterar); `src/features/documents/api/__tests__/get-documents.test.tsx` (alterar); `src/components/ui/button/__tests__/button.test.tsx` (alterar); demais testes existentes com corpo simulado ou objeto tipado como `Document`/`DocumentSummary` (alterar, só para levar `trashedAt`)
  - O que fazer: MSW + Testing Library, com o banco simulado semeado pelos ajudantes existentes. Teclado por `userEvent`. Toda espera logo após mudança de rota ou chunk `lazy` usa a constante `LAZY_TIMEOUT` do arquivo (declarar `const LAZY_TIMEOUT = { timeout: 5000 }` onde não existir; em `document-view.test.tsx` pode derivar do `TIMEOUT` que já existe); nenhuma espera fixa. Nenhum erro nem aviso no console (inclusive `act` e avisos do Radix sobre título/descrição). Os casos existentes ficam, salvo o dito abaixo.
    - `confirmation-dialog.test.tsx`: `opens from the trigger as an alertdialog named by the title and described by the description`; `opens with focus on Cancelar`; `Escape closes and returns focus to the trigger`; `Tab never leaves the dialog`; `Cancelar closes through onOpenChange`; `renders the confirm slot after Cancelar and clicking it does not close the dialog`; `uses a custom cancelLabel`.
    - `button.test.tsx` (acrescentar): `renders the destructive variant`.
    - `trash-document.test.tsx`: `sends POST to the trash endpoint and returns the document`; `writes the returned document into the document cache`; `invalidates the mine, favorites and trash lists`; `forwards onSuccess to the caller`; `rejects on a 500 and leaves the cache alone`.
    - `restore-document.test.tsx`: `sends POST to the restore endpoint and returns the document`; `writes the returned document into the document cache`; `invalidates the mine, favorites and trash lists`; `forwards onSuccess to the caller`; `rejects on a 500 and leaves the cache alone`.
    - `delete-document.test.tsx`: `sends DELETE to the document endpoint`; `removes the document query from the cache`; `invalidates the mine, favorites and trash lists`; `forwards onSuccess to the caller`; `rejects on a 409 and keeps the document query`.
    - `get-documents.test.tsx`: acrescentar `uses its own key for the trash scope`; o caso existente de `invalidateDocumentLists` passa a `invalidateDocumentLists invalidates the three lists and leaves the open document alone`.
    - `trash-document-button.test.tsx`: `shows Mover para a lixeira with an icon hidden from assistive technology`; `opens the dialog with the title, the quoted document title and both buttons`; `Cancelar closes without any request`; `confirming moves the document, closes the dialog and notifies Documento movido para a lixeira`; `shows Movendo… and disables the confirm button while pending` (handler com `delay('infinite')`); `a second click while pending sends a single request`; `a failure keeps the dialog open and shows the interceptor notification`.
    - `trashed-document-actions.test.tsx`: `shows Restaurar and Apagar definitivamente`; `Restaurar restores without a dialog and notifies Documento restaurado`; `shows Restaurando… and disables both buttons while restoring`; `a second click on Restaurar while pending sends a single request`; `Apagar definitivamente opens the dialog with Esta ação não tem volta.`; `the confirm button is the destructive variant`; `confirming deletes, notifies Documento apagado definitivamente and calls onDeleted`; `shows Apagando… and a second click while pending sends a single request`; `a failed delete keeps the dialog open and shows the interceptor notification`; `works without onDeleted`.
    - `document-view.test.tsx` (acrescentar; o dublê de `document-editor` passa a expor a prop `editable` recebida): `shows Mover para a lixeira before the favorite button for the owner`; `hides Mover para a lixeira when accessLevel is not owner`; `trashed document shows the notice with the date and the two actions`; `trashed document shows a visible h1 and no title field, favorite button or save indicator`; `mounts the editor with editable false in the trash and true outside it`; `trashed document still opens the collaboration session`; `moves focus to the trash notice after moving the document`; `a document that opens already trashed does not steal focus`; `Restaurar brings back the title field and the action row`; `navigates to /trash after deleting permanently` (espera com `LAZY_TIMEOUT`).
    - `documents-list.test.tsx` (acrescentar): `trash scope requests scope=trash`; `trash scope shows Carregando lixeira…`; `trash scope shows the empty text`; `trash scope shows the error and retries`; `trash scope lists in the API order with link and Na lixeira desde with the trashedAt date`; `trash scope shows Restaurar and Apagar definitivamente per item and the other scopes show none`; `restoring an item removes it from the trash list`; `trash scope truncates a 200 character title and keeps it in title`.
    - `sidebar-documents.test.tsx` (acrescentar): `still renders the mine and favorites sections after the scope type was narrowed`.
    - `trash.test.tsx` (integração, rotas reais, esperas com `LAZY_TIMEOUT`): `shows a single h1 Lixeira and the support text`; `lists the trashed documents`; `shows the empty state`; `shows the error and retries`; `marks the Lixeira link as current`; `deleting from the list keeps the person on /trash and empties the list`.
    - `empty-areas.test.tsx`: "Lixeira" sai do conjunto de áreas vazias (o vazio agora vem da API); as demais áreas ficam.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: alta

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` passam **sem erro nem aviso** (sem aviso de peer no install). A saída de `pnpm test` não tem `Warning:`, `act(`, `console.error` nem `console.warn` (o aviso de desempenho do Vitest sobre jsdom não conta). A saída de `pnpm build` **não** tem o aviso de chunk grande (`larger than`), e `rg -n "chunkSizeWarningLimit" apps/web --glob '!node_modules/**'` é vazio. `git diff feature/006-favorites... -- apps/web/package.json` mostra **só** a entrada `@radix-ui/react-alert-dialog`; `git diff feature/006-favorites... -- package.json apps/api/package.json` não mostra dependência nova.
- [x] CA2.2 — `apps/web/src/components/ui/confirmation-dialog/confirmation-dialog.tsx` exporta `ConfirmationDialog`, que devolve `React.JSX.Element` e recebe `open: boolean`, `onOpenChange: (open: boolean) => void`, `trigger: React.ReactNode`, `title: string`, `description: React.ReactNode`, `confirmButton: React.ReactNode` e `cancelLabel?: string` (padrão "Cancelar"). Lendo o código: é construído sobre o AlertDialog de `@radix-ui/react-alert-dialog` (título e descrição do primitivo, gatilho e cancelar compostos com o filho); o cancelar é um `Button` `variant="secondary"` e vem **antes** do slot de confirmar; o slot de confirmar **não** está embrulhado na ação do primitivo que fecha no clique; foco, `Esc` e retorno de foco não são reimplementados à mão (sem `addEventListener('keydown'` nem `.focus()` no arquivo); o arquivo não importa de `@/features`, `@/app` nem de store ou cliente HTTP. `apps/web/src/components/ui/button/button.tsx` tem a variante `destructive` com `bg-red-600 text-white hover:bg-red-700 focus-visible:outline-red-600`, e `primary`, `secondary` e `ghost` não mudaram.
- [x] CA2.3 — `apps/web/src/features/documents/api/`: `get-documents.ts` exporta `DocumentsScope` como `'mine' | 'favorites' | 'trash'` e `invalidateDocumentLists(queryClient)` (mesma assinatura) invalida os três escopos e não a chave de um documento por id. `trash-document.ts` exporta `trashDocument({ documentId }): Promise<DocumentResponse>` (envia `POST` para `/documents/{documentId}/trash`) e `useTrashDocument({ mutationConfig })`; `restore-document.ts` exporta `restoreDocument` (`POST …/restore`) e `useRestoreDocument`; `delete-document.ts` exporta `deleteDocument({ documentId }): Promise<void>` (`DELETE /documents/{documentId}`) e `useDeleteDocument`. Lendo o código: em sucesso, `trash` e `restore` gravam a resposta no cache do documento e invalidam as listas; `delete` remove a consulta do documento do cache e invalida as listas; os três repassam o `onSuccess` de quem chamou; nenhum tem atualização otimista, `try/catch` nem notificação própria. `sidebar-documents.tsx` tipa a prop como `Exclude<DocumentsScope, 'trash'>` e não ganhou texto de lixeira.
- [x] CA2.4 — `apps/web/src/features/documents/components/trash-document-button.tsx` exporta `TrashDocumentButton({ document })`, devolve `React.JSX.Element`, usa `ConfirmationDialog` e tem os textos literais "Mover para a lixeira" (gatilho `Button` `variant="ghost"`, com `<svg>` inline `aria-hidden="true"` e `focusable="false"`, sem `aria-label`), "Mover para a lixeira?", "sai das suas listas e dos favoritos. Você pode restaurar o documento depois, pela Lixeira." (precedido do título do documento entre aspas tipográficas), "Movendo…" e a notificação "Documento movido para a lixeira". O confirmar é `Button` `variant="primary"`, desabilitado/carregando enquanto pendente, e o manipulador retorna cedo enquanto pendente; o diálogo só é fechado no sucesso.
- [x] CA2.5 — `apps/web/src/features/documents/components/trashed-document-actions.tsx` exporta `TrashedDocumentActions({ document, onDeleted })` (`onDeleted?: () => void`), devolve `React.JSX.Element` e tem os textos literais "Restaurar", "Restaurando…", "Apagar definitivamente", "Apagar definitivamente?", "e todo o seu conteúdo serão apagados para sempre. Esta ação não tem volta.", "Apagando…", e as notificações "Documento restaurado" e "Documento apagado definitivamente". "Restaurar" é `Button` `variant="secondary"` e **não** abre diálogo; o gatilho de apagar é `Button` `variant="ghost"` em tom vermelho (`text-red-700`); o confirmar do diálogo é `Button` `variant="destructive"`. Enquanto qualquer uma das duas mutações está pendente os dois botões ficam desabilitados, e os manipuladores retornam cedo; `onDeleted` é chamado só no sucesso do apagar.
- [x] CA2.6 — `document-view.tsx`: fora da lixeira, a ordem é `<h1 className="sr-only">` → linha de ações com `TrashDocumentButton` **antes** de `FavoriteButton` → `DocumentTitleForm` → `SaveIndicator` → editor; `TrashDocumentButton` só é renderizado quando `document.accessLevel === 'owner'`, e `rg -n "accessLevel" apps/web/src/features apps/web/src/app --glob '!**/__tests__/**'` só acha `document-view.tsx`. Com `document.trashedAt !== null`, a ordem é aviso → `<h1>` visível com o título → editor, sem linha de ações, sem `FavoriteButton`, sem `DocumentTitleForm` e sem `SaveIndicator`; o aviso é um `<div>` com `tabIndex={-1}`, o texto "Este documento está na lixeira desde … . Restaure-o para voltar a editar." (data por `formatDateTime`) e `TrashedDocumentActions` com `onDeleted` que navega para `paths.trash.getHref()`; um efeito leva o foco ao aviso quando o documento passa a estar na lixeira nesta tela; o editor recebe `editable={false}` na lixeira. `document-editor.tsx` aceita `editable?: boolean` com padrão `true` e o repassa ao componente de visualização do editor. Os estados carregando, não encontrado e erro mantêm os textos "Carregando documento…", "Documento não encontrado", "Este documento não existe ou você não tem acesso a ele.", "Ir para Meus documentos" e "Não foi possível carregar o documento.".
- [x] CA2.7 — `documents-list.tsx` guarda num mapa por escopo os textos, a data a mostrar e se o item tem ações, e continua com **uma** marcação por estado (um só `role="status"`, um só `role="alert"`, uma só lista). Textos literais da lixeira: "Carregando lixeira…", "A lixeira está vazia. Os documentos que você excluir aparecem aqui.", "Não foi possível carregar a lixeira.", "Tentar novamente" e o prefixo "Na lixeira desde" dentro do `<time>` cujo `dateTime` é `trashedAt`. Só o escopo `trash` renderiza `TrashedDocumentActions` no item. Os textos de `mine` e `favorites` não mudaram. `apps/web/src/app/routes/app/trash.tsx` mostra `ContentLayout` com o título "Lixeira", o apoio "Documentos excluídos ficam aqui até serem restaurados ou apagados de vez." e `<DocumentsList scope="trash" />`, sem texto fixo de vazio.
- [x] CA2.8 — API simulada: `apps/web/src/testing/mocks/handlers/documents.ts` tem handlers `POST …/documents/:documentId/trash`, `POST …/documents/:documentId/restore` e `DELETE …/documents/:documentId` (401 sem cookie, 404 sem documento, 200 com o documento nos dois primeiros, 204 no terceiro, 409 com a mensagem "Mova o documento para a lixeira antes de apagá-lo definitivamente." fora da lixeira, favoritos do documento apagados junto); `PATCH` responde 409 "Este documento está na lixeira. Restaure-o para editar." na lixeira; `GET /documents` esconde a lixeira em `mine` e `favorites` e responde a `scope=trash` por `trashedAt` desc. `db.ts` exporta `seedSampleTrash`; `mocks/index.ts` lê a chave `mock-trash`, e `mocks/utils.ts` a documenta.
- [x] CA2.9 — Piso de acabamento, conferível lendo o código: a página "Lixeira" usa o contêiner e o título de página do `docs/design.md` e tem um único `<h1>`; a página do documento tem um único `<h1>` nos dois modos, e o visível do modo lixeira usa as classes de título de página com quebra de palavra longa; os quatro estados da lista ocupam o mesmo lugar, com `role="status"` no carregando e `role="alert"` no erro; o erro tem borda e fundo de destaque e `Button` vermelho; o vazio é em tom suave; cada item separa o título (truncado, com `title`) da data (tom suave) e das ações, e o `<li>` quebra linha (sem largura fixa em px); o aviso da lixeira usa as classes da receita "Aviso informativo" e tem espaço vertical até os botões e até o `<h1>`; o diálogo usa as classes da receita "Diálogo de confirmação". **Todo botão nosso é `Button`**: `rg -n "<button" apps/web/src/features apps/web/src/app apps/web/src/components/ui/confirmation-dialog --glob '!**/__tests__/**'` é vazio. `rg -n "style=\{\{|!important|forwardRef|: JSX\.Element" apps/web/src --glob '!**/__tests__/**'` não acha nada. Navegação interna só por `<Link>`/`<NavLink>`/`useNavigate`. `docs/design.md` tem, com Fatia 007, as receitas "Botão destrutivo (`destructive`)", "Ação destrutiva discreta", "Diálogo de confirmação", "Ações do item de lista" e "Ícone de lixeira".
- [x] CA2.10 — Existem e passam, pelos nomes listados em T2.7: `confirmation-dialog.test.tsx` (7, entre eles `opens with focus on Cancelar`, `Escape closes and returns focus to the trigger` e `Tab never leaves the dialog`), `trash-document.test.tsx` (5), `restore-document.test.tsx` (5), `delete-document.test.tsx` (5), `trash-document-button.test.tsx` (7, entre eles `a second click while pending sends a single request` e `a failure keeps the dialog open and shows the interceptor notification`), `trashed-document-actions.test.tsx` (10, entre eles `Restaurar restores without a dialog and notifies Documento restaurado` e `a failed delete keeps the dialog open and shows the interceptor notification`), os 10 novos de `document-view.test.tsx` (entre eles `hides Mover para a lixeira when accessLevel is not owner`, `mounts the editor with editable false in the trash and true outside it`, `moves focus to the trash notice after moving the document` e `navigates to /trash after deleting permanently`), os 8 novos de `documents-list.test.tsx`, o novo de `sidebar-documents.test.tsx`, `trash.test.tsx` (6), `uses its own key for the trash scope` e `invalidateDocumentLists invalidates the three lists and leaves the open document alone` em `get-documents.test.tsx` e `renders the destructive variant` em `button.test.tsx`. `empty-areas.test.tsx` não cobre mais "Lixeira". Os testes das fatias anteriores continuam passando. Conferir com `npx vitest run --project web --reporter=verbose`.
- [x] CA2.11 — Esperas: `trash.test.tsx` declara a constante `LAZY_TIMEOUT` e a passa em **toda** espera logo após renderizar ou mudar de rota; em `document-view.test.tsx`, o caso `navigates to /trash after deleting permanently` e toda espera nova pelo editor passam timeout explícito por constante; `rg -n "new Promise\(.*setTimeout|sleep\(" apps/web/src/features/documents apps/web/src/components/ui/confirmation-dialog apps/web/src/app/routes/app/__tests__` é vazio.
- [x] CA2.12 — O relatório de cobertura de `pnpm test` lista com ≥ 80% de linhas cada: `apps/web/src/components/ui/confirmation-dialog/confirmation-dialog.tsx`, `apps/web/src/components/ui/button/button.tsx`, `apps/web/src/features/documents/api/get-documents.ts`, `api/trash-document.ts`, `api/restore-document.ts`, `api/delete-document.ts`, `components/trash-document-button.tsx`, `components/trashed-document-actions.tsx`, `components/document-view.tsx`, `components/documents-list.tsx`, `components/sidebar-documents.tsx` e `apps/web/src/app/routes/app/trash.tsx`; arquivo ausente do relatório conta como reprovado, salvo 0 de 0 linhas (`src/testing/**` e `document-editor.tsx` já são excluídos e não contam). `git diff feature/006-favorites... -- vitest.config.ts` é vazio.
- [x] CA2.13 — Fronteiras e intocados: nenhum arquivo de `apps/web/src/components`, `lib`, `config`, `types` ou `utils` importa de `@/features` ou `@/app`; `features/documents` não importa de outra feature, de `@/app` nem de `@/testing`; não há barrel novo. `git diff --stat feature/006-favorites... -- apps/web/src/components/layouts/app-layout.tsx apps/web/src/config/paths.ts apps/web/src/app/router.tsx apps/web/src/types/api.ts apps/web/src/features/documents/api/update-favorite.ts apps/web/src/features/documents/api/update-document.ts apps/web/src/features/documents/api/get-document.ts apps/web/src/features/documents/api/create-document.ts apps/web/src/features/documents/hooks/use-document-collaboration.ts apps/web/src/features/documents/components/document-title-form.tsx apps/web/src/features/documents/components/favorite-button.tsx` é vazio.

## Fase 3 — Jornada "mandar um documento para a lixeira e decidir o destino dele" provada de ponta a ponta e documentada

- [x] T3.1 — Documentação: a entrega `trash` na arquitetura
  - Arquivos: `docs/architecture.md` (alterar)
  - O que fazer, em pt_BR, sem reescrever o resto:
    - §3 (decisão de acesso), parágrafo novo "Entrega `trash` (fatia 007)": lixeira é a coluna `Document.trashedAt`; **duas portas de lista** — `readableDocumentsWhere` exclui a lixeira (toda lista existente e futura) e `trashedDocumentsWhere` é a única leitura que a enxerga, só do proprietário; terceira pergunta `canWrite` (pode editar **e** não está na lixeira), usada por renomear e pela colaboração; mover, restaurar e apagar começam por `resolveAccess` e respondem o 404 único "Documento não encontrado." a quem não é proprietário; `POST …/trash` e `POST …/restore` idempotentes (repetir não renova a data), devolvendo o documento; `PATCH` na lixeira → 409, antes de validar o corpo; `DELETE` fora da lixeira → 409, na lixeira → 204 com `DocumentContent` e `Favorite` sumindo por cascade; regra 8 do teste estrutural `document-access-boundary.test.ts` (só `documents.service.ts` chama `trashedDocumentsWhere`, leitura nenhuma filtra `trashedAt` por fora, `readableDocumentsWhere` mantém `trashedAt: null`); novo enunciado da concordância das portas (acesso diferente de `none` ⇔ o documento está na lista legível **ou** na da lixeira, nunca nas duas).
    - §5 (editor e colaboração): a gravação do conteúdo é condicional (só com `trashedAt` nulo, na mesma transação; senão nada entra no banco e não há `stored`); ao mover ou apagar, o servidor fecha as conexões daquele documento por um ouvinte em memória (`DocumentsService.onDocumentClosed`), sem dependência circular entre os módulos; quem reconecta a documento na lixeira entra somente leitura; limite conhecido: edição feita na última janela de debounce antes de mover pode ser descartada, e o ouvinte vale para um processo só. Atualizar a frase "Limite conhecido" que já existe na seção.
    - O status da fatia no roadmap e o registro das dívidas **não** são desta tarefa (são feitos pela orquestração no encerramento).
  - Skills: —
  - Complexidade: baixa

- [x] T3.2 — Testes da fase 3 (e2e da jornada da lixeira)
  - Arquivos: `apps/web/e2e/tests/trash.spec.ts` (criar)
  - O que fazer: contra a API simulada; localizar por papel e texto em pt_BR. Estado inicial por `page.addInitScript` gravando `mock-installation` = `signed-in` no `localStorage`. Depois de criar o documento, **navegar só por links** (o banco simulado vive na página): nenhum `page.goto` nem `page.reload` entre a criação e o fim do caso. Constantes no topo do arquivo, como em `favorites.spec.ts`: `EDITOR_TIMEOUT = 20_000` (região do editor) e `ROUTE_TIMEOUT` (objeto `{ timeout }`); **toda** espera logo após mudança de rota (`toHaveURL`, primeiro `toBeVisible` da tela nova) e toda espera pela região do editor passam uma delas; nenhum `waitForTimeout`. Se algum passo usar tecla de movimento seguida de tecla de edição dentro de um campo ou do editor, espera-se a seleção colapsar (condição com timeout) antes da tecla de edição; a jornada não precisa escrever no editor.
    - Teclado: os botões "Adicionar aos favoritos", "Mover para a lixeira", "Restaurar", "Apagar definitivamente" e os botões dos diálogos são alcançados por `Tab`/`Shift+Tab`, conferidos com `toBeFocused` e acionados com `Enter` ou `Space`; **nenhum** `.click()` nem `.focus()` nesses localizadores. O ponto de partida do foco pode ser um vizinho (campo "Título", link do item na lista) focado diretamente; links de navegação podem ser clicados.
    - Um caso, `moves a document to the trash from the keyboard, restores it from Lixeira, then deletes it permanently`:
      1. abre `/`, cria o documento pelo botão "Novo documento", espera a URL do documento e a região "Conteúdo do documento", renomeia pelo campo "Título" (`fill` + `Enter`) e favorita pelo teclado; o título aparece nos `<nav>` "Documentos favoritos" e "Meus documentos recentes";
      2. leva o foco a "Mover para a lixeira" e aciona com `Enter`: aparece o `alertdialog` de nome "Mover para a lixeira?"; o foco está em "Cancelar" (`toBeFocused`); roda `expectNoSeriousA11yViolations(page)` com o diálogo aberto; `Tab` até o confirmar "Mover para a lixeira" e `Enter`;
      3. o aviso "Este documento está na lixeira desde" fica visível; o `h1` com o título é visível; não há caixa de texto "Título"; o título **some** dos dois `<nav>` da barra lateral;
      4. vai a "Lixeira" pelo link da "Navegação principal" (URL `/trash`), vê o `h1` "Lixeira" e o item com o link do título, o `<time>` e o texto "Na lixeira desde"; roda `expectNoSeriousA11yViolations(page)`;
      5. "Restaurar" pelo teclado: o item some e aparece "A lixeira está vazia. Os documentos que você excluir aparecem aqui."; o título **volta** aos dois `<nav>`;
      6. abre o documento pelo link da barra lateral, espera a região do editor, move de novo para a lixeira pelo teclado; a partir do aviso (que recebeu o foco), `Tab` até "Apagar definitivamente" e `Enter`: `alertdialog` "Apagar definitivamente?" com o texto "Esta ação não tem volta." e foco em "Cancelar"; roda o axe; `Tab` até o confirmar e `Enter`;
      7. cai em `/trash` com o estado vazio; o título não está em nenhum `<nav>` da barra lateral.
    - axe na página do documento usa a **mesma** exceção já existente (`disableRulesWithin` com `.bn-container` e `aria-input-field-name`); nenhuma regra é desativada para a página inteira. `apps/web/e2e/a11y.ts` e os outros specs não são tocados.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [x] CA3.1 — `pnpm test:e2e` na raiz passa (o Playwright sobe o próprio servidor; não precisa de API nem de Postgres), e continuam passando **sem erro nem aviso**, com `docker compose up -d` rodando e o cache do `tsc` limpo (nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta e não pode aparecer).
- [x] CA3.2 — `apps/web/e2e/tests/trash.spec.ts` contém o caso `moves a document to the trash from the keyboard, restores it from Lixeira, then deletes it permanently`, e `pnpm --filter web exec playwright test --list` o lista junto com os casos já existentes de `open-app.spec.ts`, `install.spec.ts`, `login-logout.spec.ts`, `create-document.spec.ts`, `block-editor.spec.ts` e `favorites.spec.ts` (nenhum removido nem renomeado). Não há `test.skip`, `test.only` nem `waitForTimeout` em `apps/web/e2e`.
- [x] CA3.3 — Lendo o caso: os botões "Mover para a lixeira", "Restaurar", "Apagar definitivamente" e os dois confirmar dos diálogos são acionados só pelo teclado (foco conferido com `toBeFocused`; teclas `Enter`/`Space`; nenhum `.click()` nem `.focus()` nesses localizadores); com cada um dos dois diálogos aberto, confere o papel `alertdialog` pelo nome ("Mover para a lixeira?" e "Apagar definitivamente?"), o foco em "Cancelar" e chama `expectNoSeriousA11yViolations`; confere o aviso "Este documento está na lixeira desde", o `h1` visível e a ausência da caixa de texto "Título"; confere que o título some das navegações "Documentos favoritos" e "Meus documentos recentes" depois de mover e volta às duas depois de restaurar; na página `/trash` confere o item com link, `<time>` e "Na lixeira desde", chama `expectNoSeriousA11yViolations`, e no fim confere a URL `/trash` com o texto "A lixeira está vazia. Os documentos que você excluir aparecem aqui.". Entre a criação do documento e o fim do caso não há `page.goto` nem `page.reload`.
- [x] CA3.4 — Esperas do e2e: o arquivo declara no topo as constantes de timeout (uma para a região do editor, com `20_000`, e uma para mudança de rota); **toda** asserção logo após mudança de rota (`toHaveURL` e a primeira visibilidade da tela nova) e toda espera pela região "Conteúdo do documento" passam uma delas; não há espera fixa. Se houver tecla de movimento seguida de tecla de edição, há entre elas uma espera por condição (seleção colapsada) com timeout.
- [x] CA3.5 — Acessibilidade sem atalho novo: `git diff --stat feature/006-favorites... -- apps/web/e2e/a11y.ts apps/web/e2e/mock-credentials.ts apps/web/e2e/tests/install.spec.ts apps/web/e2e/tests/login-logout.spec.ts apps/web/e2e/tests/open-app.spec.ts apps/web/e2e/tests/create-document.spec.ts apps/web/e2e/tests/block-editor.spec.ts apps/web/e2e/tests/favorites.spec.ts` é vazio; em `trash.spec.ts`, qualquer desativação de regra do axe é restrita ao seletor `.bn-container`. Os arquivos de `apps/web/e2e/**` passam no `pnpm lint` e no `pnpm typecheck`. Nenhuma senha, token ou hash literal em `apps/web/e2e/tests/trash.spec.ts`.
- [x] CA3.6 — `docs/architecture.md`: a §3 tem o parágrafo "Entrega `trash` (fatia 007)" dizendo: `trashedAt`; as duas portas de lista (`readableDocumentsWhere` exclui, `trashedDocumentsWhere` é a única que enxerga, só do proprietário); `canWrite`; 404 único para quem não é proprietário em mover, restaurar e apagar; `trash`/`restore` idempotentes; 409 do `PATCH` na lixeira e do `DELETE` fora dela; cascade ao apagar; regra 8 do teste estrutural; o novo enunciado da concordância das portas. A §5 diz: gravação de conteúdo condicional a `trashedAt` nulo; fechamento das conexões do documento ao mover ou apagar, por ouvinte em memória; conexão somente leitura na lixeira; o limite do debounce e o de processo único; a frase "Limite conhecido" foi atualizada.
- [x] CA3.7 — A fatia está utilizável de ponta a ponta sem navegador aberto por pessoa: existem e passam `apps/api/src/documents/__tests__/trash.integration.test.ts` (`npx vitest run --project api`), `apps/web/src/app/routes/app/__tests__/trash.test.tsx` com o caso `lists the trashed documents` (`npx vitest run --project web`) e a jornada e2e de `trash.spec.ts` (`pnpm test:e2e`).

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
