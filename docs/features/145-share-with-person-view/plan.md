# PLAN 145 — share-with-person-view

Branch: `feature/145-share-with-person-view`, que sai de `develop`.

Fonte: `docs/features/145-share-with-person-view/spec.md` (D1…D11 são as decisões técnicas da SPEC e R1…R12 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

O proprietário de um documento abre "Compartilhar", procura uma pessoa da instância (nome ou e-mail, a partir de 2 letras) e dá a ela acesso de leitura; a pessoa abre o endereço do documento e lê, sem editar, sem renomear e sem lixeira. Fora desta fatia: lista de quem tem acesso (147), nível "Pode editar" (148), trocar/tirar nível (146), pessoa inativa (não existe no modelo).

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; banco de teste `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`.

**Critérios sobre o disco.** Os critérios são conferidos **antes** do commit da fase, sobre os arquivos em disco (commitados ou não). Nenhum critério depende de `git diff` contra a base. Onde um critério prova **ausência**, o padrão exige código (não casa com comentário) ou o critério manda ler o arquivo. O commit de cada fase é feito pela orquestração depois da revisão; nenhum agente de fase commita.

**Desvio registrado em relação à SPEC.** D7 diz "botão `disabled` com 'Compartilhando…'". Como o botão tem o foco durante o envio, o plano usa `aria-disabled="true"` (mais a trava `isSharingRef`) em vez do `disabled` nativo, que derruba o foco para o `body`. O PR cita este desvio.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **Uma migration só, escrita à mão** (`apps/api/prisma/migrations/0015_document_share/migration.sql`). Os únicos subcomandos de Prisma permitidos são `prisma validate`, `prisma generate`, `prisma migrate status` e `prisma migrate deploy`, sempre contra `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test`; **nunca** `migrate diff`, `migrate dev` ou `migrate reset`. As migrations `0001…0014` não mudam.
- **Nenhum teste remove, desativa ou recria restrição do banco** (`DROP CONSTRAINT`, `ALTER TABLE … DISABLE`, `session_replication_role`).
- O arquivo `packages/api-contract/src/generated/openapi.d.ts` só muda por `pnpm --filter @folioteca/api-contract generate`; nunca editado à mão.
- **Sem Prettier reformatando arquivo existente**: só as linhas necessárias mudam; nada de `prettier --write` em arquivo inteiro.
- Todo componente devolve `React.JSX.Element` (ou `React.JSX.Element | null`), nunca o `JSX` global; `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case; **nenhum import entre features**. Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Controle que precisa manter o foco durante o envio usa `aria-disabled`, nunca o `disabled` nativo.
- Nenhum `eslint-disable` sem `-- <motivo>` na própria linha; nenhuma regra desativada para fazer teste passar; nenhum `any` sem comentário.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado, medida **só** por `coverage/coverage-summary.json`, gerado **na raiz** com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`, **sem `--project`** e **sem filtro de caminho**. Não contam arquivos só de tipos nem os já excluídos pelo `vitest.config.ts` da raiz. Nenhuma exclusão nova.
- **Toda** espera logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes da web pela constante `LAZY_TIMEOUT` declarada no arquivo; no e2e por `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo do spec. Nunca `setTimeout`/sleep fixo nos testes nem `waitForTimeout`.
- Testes de API de integração e contrato rodam contra o Postgres real (sem mock do Prisma); só os `*.service.test.ts` usam Prisma falso. "Teste passa" é medido pelo **exit code** (zero), nunca pela saída `--verbose`.
- **Escopo por sessão**: `organizationId` e `personId` de quem pede vêm sempre de `@CurrentPerson()`, nunca do corpo. Escopo de outra organização é provado chamando o **serviço real** com `randomUUID()` como `organizationId` (`Organization.singleton` impede uma segunda organização no banco).
- Assinaturas públicas de `AccessService` (`resolveAccess`, `canWrite`, `readableDocumentsWhere`, `trashedDocumentsWhere`) não mudam; só o corpo.
- Intocados de propósito: `apps/api/src/collab/collab.service.ts`, `apps/api/src/access/access-level.ts`, `apps/api/src/people/people.controller.ts` (o de `AdminGuard`), `apps/web/src/hooks/use-people-search.ts`, `apps/api/src/documents/favorites.service.ts`, `trashedDocumentsWhere`, `apps/web/src/components/ui/**`, migrations `0001…0014`.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa.

## Fase 1 — API: tabela `DocumentShare`, `PUT /documents/{documentId}/shares/{personId}`, `GET /people/search` e acesso `view` na decisão

Caminhos relativos à raiz do repositório. Ao fim da fase, pela API, o dono compartilha e a outra pessoa lê o documento (`GET` 200 com `accessLevel: 'view'`), sem conseguir alterar.

- [x] T1.1 — Contrato primeiro e banco: rotas novas no OpenAPI e migration 0015 à mão
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script); `apps/api/prisma/migrations/0015_document_share/migration.sql` (criar); `apps/api/prisma/schema.prisma` (alterar)
  - O que fazer (D1, D2, R2, R3, R5):
    - `openapi.yaml`: caminho `/documents/{documentId}/shares/{personId}` com `put`, `operationId: shareDocument`, tag `documents`, parâmetros de caminho `documentId` e `personId`, `requestBody` obrigatório `$ref` `ShareDocumentInput`, respostas **200** `DocumentShareResponse` e **400**, **401**, **403**, **404**, **409** com `Error`; `description` em pt_BR (dá acesso de leitura; repetir devolve o mesmo 200).
    - Caminho `/people/search` com `get`, `operationId: searchPeopleToShare`, tag `people`, parâmetro de query `q` (string), respostas **200** `PeopleResponse` (reaproveitado) e **401** `Error`; **sem 403**. `description`: qualquer pessoa com sessão; menos de 2 caracteres devolve lista vazia; no máximo 10; exclui quem pede.
    - Schemas novos: `ShareDocumentInput` (`type: object`, `required: [level]`, `additionalProperties: false`, `level: { type: string, enum: [view] }`); `DocumentShare` (`required: [personId, name, email, level]`, `level` com `enum: [view]`); `DocumentShareResponse` (`required: [data]`, `data: $ref DocumentShare`).
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`.
    - `migration.sql`, à mão, com comentários em pt_BR: `CREATE TYPE "ShareLevel" AS ENUM ('VIEW', 'EDIT');`; `CREATE TABLE "DocumentShare" ("documentId" TEXT NOT NULL, "personId" TEXT NOT NULL, "level" "ShareLevel" NOT NULL, "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "DocumentShare_pkey" PRIMARY KEY ("documentId", "personId"));`; `CREATE INDEX "DocumentShare_personId_idx" ON "DocumentShare"("personId");`; FKs `"DocumentShare_documentId_fkey"` → `"Document"("id")` e `"DocumentShare_personId_fkey"` → `"Person"("id")`, ambas `ON DELETE CASCADE ON UPDATE CASCADE`.
    - `schema.prisma`: `enum ShareLevel { VIEW EDIT }` (comentário `///`: `EDIT` reservado para a fatia 148); `model DocumentShare` com `documentId`, `personId`, `level ShareLevel`, `createdAt DateTime @default(now())`, relações com `onDelete: Cascade, onUpdate: Cascade`, `@@id([documentId, personId])`, `@@index([personId])`; `Document.shares DocumentShare[]` e `Person.documentShares DocumentShare[]`.
    - Conferência: `pnpm --filter api exec prisma validate`, `prisma generate`, e `migrate deploy` + `migrate status` no banco de teste. Se `validate` apontar diferença que exigiria outro SQL, o agente para e relata.
  - Skills: security
  - Complexidade: média

- [x] T1.2 — `AccessService` lê o compartilhamento direto na mesma consulta
  - Arquivos: `apps/api/src/access/access.service.ts` (alterar)
  - O que fazer (D3, R7–R10):
    - `findDecision(documentId, personId)` (privado) passa a receber a pessoa e seleciona, na **mesma** `findUnique`/`findFirst`, `shares: { where: { personId }, select: { level: true } }`; `DocumentDecision` ganha `shareLevel: 'view' | 'edit' | null` (`VIEW → 'view'`, `EDIT → 'edit'`). `resolveAccess` e `canWrite` passam a pessoa. Uma ida ao banco por decisão.
    - `levelOf`: dono → `'owner'`; lixeira → `'none'` (inalterado e **antes** do compartilhamento); `shareLevel` → esse nível; senão `'none'`. `canWrite` não muda de corpo.
    - `readableDocumentsWhere(personId)` → `{ trashedAt: null, OR: [{ ownerId: personId }, { shares: { some: { personId } } }] }`, mantendo o literal `trashedAt: null`. `trashedDocumentsWhere` intocada.
  - Skills: authorization
  - Complexidade: alta

- [x] T1.3 — `SharesService.share` e a rota `PUT` no `DocumentsController`
  - Arquivos: `apps/api/src/documents/documents.schema.ts` (alterar); `apps/api/src/documents/shares.service.ts` (criar); `apps/api/src/documents/documents.controller.ts` (alterar); `apps/api/src/documents/documents.module.ts` (alterar)
  - O que fazer (D1, D4, R1, R5, R6, R9):
    - `documents.schema.ts`: `shareDocumentSchema = z.strictObject({ level: z.literal('view', { error: 'Escolha o nível de acesso.' }) }, { error: 'Campo não permitido.' })`.
    - `shares.service.ts`: `@Injectable() SharesService` com `share(requester: PersonWithOrganization, documentId: string, personId: string, body: unknown): Promise<DocumentShareResponse>`, nesta ordem fixa: (1) `resolveAccess(requester.id, documentId)` `'none'` → 404 `documentNotFound()`; (2) `!== 'owner'` → `ForbiddenException('Só o proprietário pode compartilhar este documento.')`; (3) `!canWrite` → 409 com a mensagem existente de documento na lixeira (a mesma de `rename`); (4) `parseBody(shareDocumentSchema, body)` → 400; (5) `personId === requester.id` → `BadRequestException('Você já é o proprietário deste documento.')`; (6) `!isUuid(personId)` ou `prisma.person.findFirst({ where: { id: personId, organizationId: requester.organizationId }, select: { id: true, name: true, email: true } })` ausente → `BadRequestException('Pessoa não encontrada nesta instância.')`; (7) `prisma.documentShare.upsert({ where: { documentId_personId: { documentId, personId } }, create: { documentId, personId, level: 'VIEW' }, update: { level: 'VIEW' } })` → `{ data: { personId, name, email, level: 'view' } }`.
    - `documents.controller.ts`: `@Put(':documentId/shares/:personId')` com `@HttpCode(200)` se o padrão do arquivo exigir, `@CurrentPerson() person`, `@Param('documentId') documentId: string`, `@Param('personId') personId: string`, `@Body() body: unknown`, chamando `shares.share(person, documentId, personId, body)`. Guards do controller intocados (só na classe).
    - `documents.module.ts`: provê `SharesService`.
  - Skills: authorization, security
  - Complexidade: média

- [x] T1.4 — Busca de pessoas para compartilhar em controller próprio, só com `SessionGuard`
  - Arquivos: `apps/api/src/people/people.service.ts` (alterar); `apps/api/src/people/people-search.controller.ts` (criar); `apps/api/src/people/people.module.ts` (alterar)
  - O que fazer (D5, R2, R3):
    - `people.service.ts`: exporta `PEOPLE_SHARE_SEARCH_MIN_LENGTH = 2`; método `searchToShare(organizationId: string, requesterId: string, term: string | undefined): Promise<PeopleResponse>`: `term?.trim()` com menos de 2 caracteres → `{ data: [], hasMore: false }` **sem** tocar no banco; senão o mesmo `findMany` de `search` (nome ou e-mail `contains` com `mode: 'insensitive'`, `organizationId`, mesma ordenação), com `NOT: { id: requesterId }`, teto `PEOPLE_SEARCH_LIMIT` (10, pedindo 11 para calcular `hasMore`, como `search`), `select` só `id`, `name`, `email`.
    - `people-search.controller.ts`: `@Controller('people/search')`, `@UseGuards(SessionGuard)` **na classe** (sem `AdminGuard`), `@Get()` com `@CurrentPerson() person` e `@Query('q') q: string | undefined` → `people.searchToShare(person.organizationId, person.id, q)`.
    - `people.module.ts`: registra `PeopleSearchController` **antes** de `PeopleController` na lista de `controllers` (para `people/search` não ser capturado por uma rota com parâmetro).
  - Skills: security
  - Complexidade: média

- [x] T1.5 — Testes da fase 1
  - Arquivos: `apps/api/src/access/__tests__/access-level.test.ts` (alterar); `apps/api/src/access/__tests__/access.integration.test.ts` (alterar); `apps/api/src/access/__tests__/document-access-boundary.test.ts` (alterar); `apps/api/src/documents/__tests__/shares.service.test.ts` (criar); `apps/api/src/documents/__tests__/shares.integration.test.ts` (criar); `apps/api/src/documents/__tests__/documents.integration.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.schema.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.contract.test.ts` (alterar); `apps/api/src/collab/__tests__/collab.integration.test.ts` (alterar); `apps/api/src/people/__tests__/people.service.test.ts` (alterar); `apps/api/src/people/__tests__/people.integration.test.ts` (alterar); `apps/api/src/people/__tests__/people.contract.test.ts` (alterar)
  - O que fazer (D9, D11): integração e contrato contra o Postgres real com `resetDatabase(prisma)` em `beforeEach` e os ajudantes existentes; `apps/api/test/**` não muda. Casos preexistentes cujo Prisma falso muda por T1.2 são ajustados **sem trocar o nome**. Nenhuma regra existente do teste de fronteira muda.
    - `access-level.test.ts` (ou onde `levelOf` já é testado, no mesmo arquivo): `levelOf returns view for a view share`; `levelOf returns none for a shared document in the trash`; `levelOf returns owner for the owner even with a share row`.
    - `access.integration.test.ts`: `resolveAccess returns view for a person with a view share`; `canWrite is false for a view share`; `resolveAccess returns none without a share`; `a shared document in the trash is none and view again after restore`; `readableDocumentsWhere includes the shared document and excludes it in the trash`; `deleting the share row makes the next resolveAccess none`.
    - `document-access-boundary.test.ts`: regra 9 com `rule 9 flags documentShare outside access and shares service` (exemplo infrator) e `rule 9 accepts documentShare in access service and shares service` (exemplo conforme), mais a verificação sobre o código real: só `apps/api/src/access/access.service.ts` e `apps/api/src/documents/shares.service.ts` contêm `.documentShare.` ou `"DocumentShare"`; `shares.service.ts` contém `resolveAccess(`; `documentShare.findUnique` só em `access.service.ts`.
    - `shares.service.test.ts` (Prisma falso): `share answers 404 when the requester has no access`; `share answers 403 to a view person`; `share answers 409 for the owner on a trashed document`; `share checks access before the body`; `share answers 400 for an invalid body`; `share answers 400 when sharing with oneself`; `share answers 400 for a malformed person id without querying`; `share upserts VIEW and returns the person from the database`.
    - `shares.integration.test.ts` (HTTP): `PUT share answers 200 and creates one row`; `PUT share repeated answers 200 and keeps one row`; `PUT share with oneself answers 400 with Você já é o proprietário deste documento.`; `PUT share with an unknown person answers 400 with Pessoa não encontrada nesta instância.` (`randomUUID()`); `PUT share with a malformed person id answers 400`; `PUT share on a document without access answers 404`; `PUT share by a view person answers 403 with Só o proprietário pode compartilhar este documento.`; `PUT share rejects an empty body with 400`; `PUT share rejects level edit with 400`; `PUT share rejects extra fields with 400`; `PUT share on a trashed document answers 409`; `PUT share answers 401 without session`; `share with another organization id answers 400` (serviço real, `{ ...requester, organizationId: randomUUID() }`, com pessoa existente).
    - `documents.integration.test.ts`: `GET a shared document answers 200 with accessLevel view`; `PATCH a shared document by a view person answers 403`; `POST trash on a shared document by a view person answers 404`; `GET a shared document by a third person answers the same 404 as a missing one`; `my documents of the view person does not list the shared document`.
    - `documents.schema.test.ts`: `shareDocumentSchema accepts level view`; `shareDocumentSchema rejects a missing level with Escolha o nível de acesso.`; `shareDocumentSchema rejects level edit`; `shareDocumentSchema rejects extra fields with Campo não permitido.`.
    - `documents.contract.test.ts`: `PUT documents shares answers the documented 200`, `… 400`, `… 403`, `… 404`, `… 409`, chamando exatamente `/documents/{id}/shares/{personId}`.
    - `collab.integration.test.ts`: `a view person connects read only and an update is not stored`.
    - `people.service.test.ts`: `searchToShare returns empty without querying for a 1 character term`; `searchToShare excludes the requester and caps at 10`.
    - `people.integration.test.ts`: `GET people search answers 200 to a non-admin member`; `GET people search excludes the requester`; `GET people search with 1 character returns empty`; `GET people search caps at 10 with hasMore`; `GET people search ignores case in name and email`; `GET people search answers 401 without session`; `searchToShare with another organization id returns empty` (serviço real, `randomUUID()`).
    - `people.contract.test.ts`: `GET people search answers the documented 200`, `… 401`, chamando exatamente `/people/search`.
  - Skills: unit-testing, integration-testing
  - Complexidade: média

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules`): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e sem aviso.
- [x] CA1.2 — `packages/api-contract/openapi.yaml` tem `/documents/{documentId}/shares/{personId}:` com `put`, `operationId: shareDocument`, parâmetros `documentId` e `personId`, `requestBody` `$ref` `ShareDocumentInput` e respostas exatamente `200` (`DocumentShareResponse`), `400`, `401`, `403`, `404`, `409`; e `/people/search:` com `get`, `operationId: searchPeopleToShare`, parâmetro `q` e respostas exatamente `200` (`PeopleResponse`) e `401`. `ShareDocumentInput` tem `required: [level]`, `additionalProperties: false` e `enum: [view]`. `rg -n "shareDocument|searchPeopleToShare|ShareDocumentInput|DocumentShareResponse" packages/api-contract/src/generated/openapi.d.ts` encontra os quatro.
- [x] CA1.3 — Identidade de caminho: `apps/api/src/documents/documents.controller.ts` tem `@Put(':documentId/shares/:personId')` com `@Param('documentId')` e `@Param('personId')` chamando `share(person, documentId, personId, body)`, e continua com `@Controller('documents')`. `apps/api/src/people/people-search.controller.ts` tem `@Controller('people/search')`, `@Get()`, `@Query('q')` e exatamente um `@UseGuards(SessionGuard)`, acima da classe; `rg -n "AdminGuard" apps/api/src/people/people-search.controller.ts` é vazio. `apps/api/src/people/people.module.ts` lista `PeopleSearchController` antes de `PeopleController`.
- [x] CA1.4 — Existe `apps/api/prisma/migrations/0015_document_share/migration.sql` com `CREATE TYPE "ShareLevel" AS ENUM ('VIEW', 'EDIT')`, `CREATE TABLE "DocumentShare"`, `PRIMARY KEY ("documentId", "personId")`, `CREATE INDEX "DocumentShare_personId_idx"` e duas FKs com `ON DELETE CASCADE ON UPDATE CASCADE`; `ls apps/api/prisma/migrations` lista só `0001…0015` e `migration_lock.toml`. `apps/api/prisma/schema.prisma` tem `enum ShareLevel`, `model DocumentShare` com `@@id([documentId, personId])` e `@@index([personId])`. `pnpm --filter api exec prisma validate` sai com 0 e `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test pnpm --filter api exec prisma migrate status` reporta o banco em dia.
- [x] CA1.5 — `apps/api/src/access/access.service.ts`, lido: `findDecision` recebe `personId` e seleciona `shares: { where: { personId } … }` na mesma consulta do documento; em `levelOf` o teste de lixeira vem antes do de `shareLevel`; `readableDocumentsWhere` contém `trashedAt: null` e `shares: { some: { personId } }`. As assinaturas públicas `resolveAccess`, `canWrite`, `readableDocumentsWhere` e `trashedDocumentsWhere` são as mesmas de antes (mesmos parâmetros e tipos de retorno).
- [x] CA1.6 — `apps/api/src/documents/documents.schema.ts` exporta `shareDocumentSchema` (`z.strictObject`, `z.literal('view'`) com `Escolha o nível de acesso.` e `Campo não permitido.`. `apps/api/src/documents/shares.service.ts` exporta `SharesService` com `share(requester: PersonWithOrganization, documentId: string, personId: string, body: unknown)`, contém `Só o proprietário pode compartilhar este documento.`, `Você já é o proprietário deste documento.`, `Pessoa não encontrada nesta instância.`, `resolveAccess(`, `organizationId: requester.organizationId` e `documentShare.upsert`; lido, a ordem é 404 → 403 → 409 → corpo → si mesmo → pessoa → upsert. `rg -n "^\s*[^/*].*'EDIT'" apps/api/src/documents/shares.service.ts` é vazio.
- [x] CA1.7 — `apps/api/src/people/people.service.ts` exporta `PEOPLE_SHARE_SEARCH_MIN_LENGTH = 2` e `searchToShare(organizationId: string, requesterId: string, term: string | undefined)` com `NOT: { id: requesterId }` e `mode: 'insensitive'`. `apps/api/src/people/people.controller.ts` continua com `AdminGuard` na classe.
- [x] CA1.8 — `pnpm exec vitest run --project api` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/api/src/access/__tests__ apps/api/src/documents/__tests__ apps/api/src/collab/__tests__ apps/api/src/people/__tests__`, todos os casos nomeados em T1.5 com os nomes literais (`access-level.test.ts`: 3; `access.integration.test.ts`: 6; `document-access-boundary.test.ts`: 2 novos; `shares.service.test.ts`: 8; `shares.integration.test.ts`: 13; `documents.integration.test.ts`: 5; `documents.schema.test.ts`: 4; `documents.contract.test.ts`: 5; `collab.integration.test.ts`: 1; `people.service.test.ts`: 2; `people.integration.test.ts`: 7; `people.contract.test.ts`: 2). `rg -n "vi\.mock\(.*prisma" apps/api/src/documents/__tests__/shares.integration.test.ts apps/api/src/access/__tests__/access.integration.test.ts apps/api/src/people/__tests__/people.integration.test.ts` é vazio.
- [x] CA1.9 — Lendo os testes: `share with another organization id answers 400` e `searchToShare with another organization id returns empty` chamam o serviço real (instância ligada ao Prisma de teste) com `randomUUID()` como `organizationId`, sobre pessoas que existem na organização da instalação; `PUT share repeated answers 200 and keeps one row` conta as linhas de `DocumentShare` (1) depois da segunda chamada; `deleting the share row makes the next resolveAccess none` apaga a linha e chama `resolveAccess` de novo; `a view person connects read only and an update is not stored` confere o conteúdo gravado inalterado. `rg -n "DROP CONSTRAINT|DISABLE TRIGGER|session_replication_role" apps/api/src apps/api/test` é vazio.
- [x] CA1.10 — `rg -n "setTimeout\(|sleep\(" apps/api/src/documents apps/api/src/people apps/api/src/access` é vazio; `rg -n "eslint-disable" apps/api/src | rg -v -- "--"` é vazio.
- [x] CA1.11 — Cobertura ≥ 80% de linhas para `apps/api/src/access/access.service.ts`, `apps/api/src/documents/shares.service.ts`, `apps/api/src/documents/documents.schema.ts`, `apps/api/src/documents/documents.controller.ts`, `apps/api/src/people/people.service.ts` e `apps/api/src/people/people-search.controller.ts`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 2 — Web: diálogo "Compartilhar" para o dono e a página do documento em somente leitura

Caminhos relativos à raiz do repositório. A ordem importa: API simulada e chamadas antes da tela. Ao fim da fase, no app com API simulada, o dono compartilha pelo diálogo e quem tem `view` vê a página em somente leitura.

- [x] T2.1 — API simulada com compartilhamentos, busca e a semente `shared-view`
  - Arquivos: `apps/web/src/testing/mocks/db.ts` (alterar); `apps/web/src/testing/mocks/index.ts` (alterar); `apps/web/src/testing/mocks/handlers/documents.ts` (alterar); `apps/web/src/testing/mocks/handlers/people.ts` (alterar)
  - O que fazer (D10):
    - `db.ts`: tipo `MockDocumentShare = { documentId: string; personId: string; level: 'view' }`, `shares: MockDocumentShare[]` no estado (zerado pelo reset existente); `shareDocument(documentId: string, personId: string)` (upsert, uma linha por par); `searchPeopleToShare(term: string, requesterId: string)` sobre `allPeople()` (termo `trim()` com menos de 2 → `[]`; nome ou e-mail sem diferença de maiúsculas; sem quem pede; máximo 10 com `hasMore`); `seedSharedReadOnlyDocument()` cria um documento de outra pessoa com `accessLevel: 'view'` para a sessão simulada e devolve seu id.
    - `index.ts`: chave de desenvolvimento `mock-documents=shared-view` chama `seedSharedReadOnlyDocument()`.
    - `handlers/documents.ts`: `http.put(\`${env.API_URL}/documents/:documentId/shares/:personId\`)` com o preâmbulo de sempre do arquivo (`networkDelay()`, `devOverride('documents')`, 401) e, na ordem de D4: 404 "Documento não encontrado.", 403 "Só o proprietário pode compartilhar este documento.", 409 (lixeira), 400 corpo (`level !== 'view'` ou campo extra), 400 "Você já é o proprietário deste documento.", 400 "Pessoa não encontrada nesta instância.", 200 `{ data: { personId, name, email, level: 'view' } }`.
    - `handlers/people.ts`: `http.get(\`${env.API_URL}/people/search\`)` com `networkDelay()`, 401 sem sessão e 200 `{ data, hasMore }`; sem 403.
  - Skills: api-mocking
  - Complexidade: média

- [x] T2.2 — Chamadas da web: busca para compartilhar e a mutação `useShareDocument`
  - Arquivos: `apps/web/src/features/documents/api/search-people-to-share.ts` (criar); `apps/web/src/features/documents/api/share-document.ts` (criar)
  - O que fazer (D6):
    - `search-people-to-share.ts`: `searchPeopleToShare(term: string): Promise<PeopleResponse>` → `api.get('/people/search', { params: { q: term } })`; `useSearchPeopleToShare(term: string)` com `queryKey: ['people', 'share-search', term]`, `enabled: term.trim().length >= 2`, `placeholderData: keepPreviousData`, `staleTime: 30_000`. Não importa de `@/hooks/use-people-search`.
    - `share-document.ts`: `shareDocument({ documentId, personId }: { documentId: string; personId: string }): Promise<DocumentShareResponse>` → `api.put(\`/documents/${documentId}/shares/${personId}\`, { level: 'view' }, { silentError: true })`; `useShareDocument({ mutationConfig })`. Nada a invalidar.
  - Skills: api-requests, error-handling
  - Complexidade: baixa

- [x] T2.3 — Diálogo "Compartilhar" e a página do documento em somente leitura
  - Arquivos: `apps/web/src/features/documents/components/share-document-dialog.tsx` (criar); `apps/web/src/features/documents/components/document-view.tsx` (alterar)
  - O que fazer (D7, D8, Interface, R1, R2, R4, R6, R8, R11, R12). Aparência pelo `docs/design.md` e pelo piso de `interface-design`, com as receitas "Linha de ações do documento", "Botão secundário", "Diálogo de formulário", "Campo de formulário", "Resultados de busca", "Selo de status", "Alerta dentro de formulário", "Carregando", "Erro" e a receita **nova** "Selo de somente leitura" (receita "Selo de status" com o par `bg-gray-100 text-gray-800`, à esquerda da linha de ações com `mr-auto`) — registrada no `docs/design.md` na fase 3:
    - `share-document-dialog.tsx`: `ShareDocumentDialog({ documentId, documentTitle }: { documentId: string; documentTitle: string }): React.JSX.Element`. Gatilho: `Button` secundário **"Compartilhar"** (altura ≥ 40px). Diálogo (`Dialog` de `components/ui/dialog/dialog.tsx`), de cima para baixo: título **"Compartilhar documento"**; descrição **"Quem você escolher poderá ler “{título}”, sem alterar nada."**; campo com `<label>` **"Buscar pessoa"** e dica **"Nome ou e-mail, com pelo menos 2 letras."** (ligada por `aria-describedby`); área de resultados; mensagem de sucesso em `aria-live="polite"`; alerta de erro; rodapé com `Button` secundário **"Fechar"** (`DialogClose`).
    - Estado local: `term`, `deferredTerm` (debounce de 300 ms num `useEffect` com `setTimeout` e limpeza, como `promote-admin-search.tsx`), `selected: PersonSummary | null`. Nada em Zustand nem na URL. Ao reabrir, começa limpo.
    - Área de resultados (mesmo lugar): menos de 2 caracteres → **"Digite pelo menos 2 letras para buscar."** (tom suave); carregando → `role="status"` **"Buscando…"**; vazio → **"Nenhuma pessoa encontrada."** (tom suave, `role="status"`); erro da busca → `role="alert"` **"Não foi possível buscar pessoas."** com `Button` **"Tentar de novo"** (`refetch`); com dados → contagem em `role="status"` **"1 resultado."** / **"{n} resultados."** e lista com nome (principal, `truncate`) e e-mail (secundário, `text-gray-600`, `break-words`) à esquerda e `Button` `ghost` **"Selecionar"** à direita, `aria-label` **"Selecionar {nome}"**.
    - Pessoa escolhida (substitui a lista): nome e e-mail; selo **"Pode ver"**; frase **"Esta pessoa poderá ler o documento."**; `Button` secundário **"Trocar pessoa"** (volta à busca com o termo preservado) e `Button` principal **"Compartilhar"**, que vira **"Compartilhando…"** durante o envio, com `aria-disabled="true"` (sem `disabled` nativo) e a ref `isSharingRef` impedindo segundo envio.
    - Sucesso: **"Documento compartilhado com {nome}."** (nome da **resposta**), `term` e `selected` limpos, foco no campo de busca; o diálogo continua aberto. Erro: `role="alert"` com a mensagem do servidor em 400/403/409, ou **"Não foi possível compartilhar o documento. Tente de novo."**; `selected` mantido.
    - Fechar por "Fechar", `Esc` ou clique fora; o foco volta ao gatilho (padrão do Radix). A 360px: caixa `w-[calc(100%-2rem)]`, linha de resultado e rodapé com `flex-wrap`.
    - `document-view.tsx`: monta `ShareDocumentDialog` só com `accessLevel === 'owner'` e fora da lixeira, na linha de ações, **antes** de "Mover para a lixeira". `const isReadOnly = document.accessLevel === 'view'`; com `isReadOnly` e fora da lixeira: `<h1>` visível com o título (mesmo estilo do ramo da lixeira, `text-2xl font-bold break-words`) no lugar do `DocumentTitleForm`; selo **"Somente leitura"** à esquerda da linha de ações; sem `SaveIndicator`; `editable={!isTrashed && !isReadOnly}`; `FavoriteButton` continua.
  - Skills: interface-design, client-state, component-robustness, authorization
  - Complexidade: alta

- [x] T2.4 — Testes da fase 2 e os e2e antigos
  - Arquivos: `apps/web/src/features/documents/api/__tests__/search-people-to-share.test.tsx` (criar); `apps/web/src/features/documents/api/__tests__/share-document.test.tsx` (criar); `apps/web/src/features/documents/components/__tests__/share-document-dialog.test.tsx` (criar); `apps/web/src/features/documents/components/__tests__/document-view.test.tsx` (alterar)
  - O que fazer (D11): banco falso pelos ajudantes; `userEvent`; localizar por papel e nome acessível em pt_BR; `LAZY_TIMEOUT` em cada arquivo que espera após rota ou `lazy`; o debounce é esperado com `findBy…`/`waitFor` (ou timers falsos do Vitest), nunca espera fixa; sem `console.error`/`console.warn`/aviso de `act(...)`. Casos preexistentes não são renomeados.
    - `search-people-to-share.test.tsx`: `useSearchPeopleToShare does not request with 1 character`; `useSearchPeopleToShare requests people search with q from 2 characters`.
    - `share-document.test.tsx`: `shareDocument sends PUT to the document share path with level view`; `useShareDocument returns the shared person from the response`.
    - `share-document-dialog.test.tsx`: `asks for at least 2 letters before searching`; `shows Buscando while the search loads`; `shows Nenhuma pessoa encontrada for an empty result`; `lists the people with the result count`; `shows the search error with Tentar de novo`; `selecting a person shows Pode ver and the Compartilhar button`; `Trocar pessoa goes back to the results keeping the term`; `sharing announces success with the name from the response and clears the search`; `a 400 shows the server message and keeps the selected person`; `a 500 shows the generic share error`; `a double click sends only one request`; `the share button keeps focus while sending`; `Escape closes the dialog and returns focus to the trigger`.
    - `document-view.test.tsx`: `the owner sees the Compartilhar button`; `the owner of a trashed document has no Compartilhar button`; `a view person sees Somente leitura and a read only editor`; `a view person sees the title as a heading without the title field`; `a view person has no Compartilhar nor trash button`.
    - Rodar `pnpm test:e2e` com **todos** os e2e existentes; se algum quebrar, o conserto é no código de produção. Conserto em spec antigo só se a mudança de UI for intencional (ex.: um novo botão "Compartilhar" que torna ambígua uma busca por nome) e registrado como desvio no fim deste plano.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` saem com 0 e sem aviso; a saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)`. `pnpm test:e2e` (todos os e2e existentes) sai com 0.
- [x] CA2.2 — `apps/web/src/features/documents/api/search-people-to-share.ts` exporta `searchPeopleToShare` (chama `api.get('/people/search'` com `params: { q: term }`) e `useSearchPeopleToShare`, com `['people', 'share-search', term]`, `enabled` a partir de 2 caracteres e `keepPreviousData`. `apps/web/src/features/documents/api/share-document.ts` exporta `shareDocument` (chama `api.put` em `` `/documents/${documentId}/shares/${personId}` `` com `{ level: 'view' }` e `silentError: true`) e `useShareDocument`. `rg -n "use-people-search" apps/web/src/features/documents` é vazio; `rg -n "from '@/features/(?!documents)" -P apps/web/src/features/documents` é vazio.
- [x] CA2.3 — `apps/web/src/features/documents/components/share-document-dialog.tsx` exporta `ShareDocumentDialog` com retorno `React.JSX.Element` e contém literalmente `Compartilhar documento`, `Quem você escolher poderá ler`, `sem alterar nada.`, `Buscar pessoa`, `Nome ou e-mail, com pelo menos 2 letras.`, `Digite pelo menos 2 letras para buscar.`, `Buscando…`, `Nenhuma pessoa encontrada.`, `resultado.`, `resultados.`, `Não foi possível buscar pessoas.`, `Tentar de novo`, `Selecionar`, `Pode ver`, `Esta pessoa poderá ler o documento.`, `Trocar pessoa`, `Compartilhando…`, `Documento compartilhado com`, `Não foi possível compartilhar o documento. Tente de novo.` e `Fechar`; tem `aria-live="polite"`, `role="status"`, `role="alert"`, `aria-disabled` e uma ref `isSharingRef`. `rg -n -P "^\s*[^/*].*(?<![-\w])disabled=\{" apps/web/src/features/documents/components/share-document-dialog.tsx` é vazio.
- [x] CA2.4 — `apps/web/src/features/documents/components/document-view.tsx` contém `isReadOnly`, `Somente leitura`, `ShareDocumentDialog` (renderizado só sob `accessLevel === 'owner'` e fora da lixeira, antes do botão da lixeira) e `editable={!isTrashed && !isReadOnly}` (ou expressão equivalente com os dois termos); no ramo `isReadOnly`, um `<h1>` com `break-words` e nenhum `DocumentTitleForm` nem `SaveIndicator`.
- [x] CA2.5 — Acabamento (piso de `interface-design`, lendo o código): o diálogo usa `DialogTitle` e `DialogDescription`; o campo tem `<label>` e a dica ligada por `aria-describedby`; cada resultado separa nome (`truncate`, em flex com `min-w-0`) de e-mail (`text-gray-600`, `break-words`) com o botão à direita; o erro da busca e o de envio usam fundo ou borda de destaque; "Pode ver" e "Somente leitura" são selos (receita "Selo de status"; o de somente leitura com `bg-gray-100 text-gray-800` e `mr-auto`); a caixa tem `w-[calc(100%-2rem)]`; resultado e rodapé com `flex-wrap`. `rg -n "<button|<a href|style=\{\{|!important|forwardRef|: JSX\." apps/web/src/features/documents/components/share-document-dialog.tsx apps/web/src/features/documents/components/document-view.tsx` é vazio; nenhuma largura fixa em px nesses arquivos.
- [x] CA2.6 — API simulada: `apps/web/src/testing/mocks/db.ts` exporta `shareDocument(documentId: string, personId: string)`, `searchPeopleToShare(term: string, requesterId: string)` e `seedSharedReadOnlyDocument()`, e tem o tipo `MockDocumentShare`; `apps/web/src/testing/mocks/index.ts` contém `shared-view`; `apps/web/src/testing/mocks/handlers/documents.ts` tem `http.put` em `${env.API_URL}/documents/:documentId/shares/:personId` com `networkDelay()`, `devOverride('documents')` e as respostas 400, 401, 403, 404, 409 e 200; `apps/web/src/testing/mocks/handlers/people.ts` tem `http.get` em `${env.API_URL}/people/search`.
- [x] CA2.7 — `pnpm exec vitest run --project web` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/web/src/features/documents`, os 22 casos nomeados em T2.4 com os nomes literais (`search-people-to-share.test.tsx`: 2; `share-document.test.tsx`: 2; `share-document-dialog.test.tsx`: 13; `document-view.test.tsx`: 5 novos).
- [x] CA2.8 — Lendo os testes: `a double click sends only one request` conta uma única chamada ao `PUT`; `the share button keeps focus while sending` assere `toHaveFocus()` no botão enquanto mostra "Compartilhando…"; `sharing announces success with the name from the response and clears the search` usa na resposta simulada um nome diferente do da linha da busca e assere o nome da resposta, o campo vazio e o foco nele; `a 400 shows the server message and keeps the selected person` assere o texto do servidor em `role="alert"` e "Pode ver" ainda visível; `Escape closes the dialog and returns focus to the trigger` tem `toHaveFocus()` no botão "Compartilhar"; `a view person sees Somente leitura and a read only editor` assere `data-editable="false"`.
- [x] CA2.9 — `rg -n "sleep\(|waitForTimeout" apps/web/src/features/documents` é vazio e `setTimeout(` só aparece em `share-document-dialog.tsx` (o debounce); `rg -n "eslint-disable" apps/web/src | rg -v -- "--"` é vazio.
- [x] CA2.10 — Cobertura ≥ 80% de linhas para `apps/web/src/features/documents/api/search-people-to-share.ts`, `apps/web/src/features/documents/api/share-document.ts`, `apps/web/src/features/documents/components/share-document-dialog.tsx` e `apps/web/src/features/documents/components/document-view.tsx`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 3 — e2e da jornada e documentação: a fatia utilizável de ponta a ponta

- [x] T3.1 — Documentação: receita nova e arquitetura
  - Arquivos: `docs/design.md` (alterar); `docs/architecture.md` (alterar)
  - O que fazer, em pt_BR, só acrescentando linhas:
    - `docs/design.md`, em "Padrões acrescentados pelas entregas": receita **"Selo de somente leitura"** (fatia 145): receita "Selo de status" com `bg-gray-100 text-gray-800`, à esquerda da "Linha de ações do documento" com `mr-auto`, com as classes usadas em `document-view.tsx`.
    - `docs/architecture.md` §"access": parágrafo **"Entrega `share-with-person-view` (fatia 145)"**: compartilhamento direto implementado na tabela `DocumentShare` (migration 0015 escrita à mão, enum `ShareLevel` com `EDIT` reservado); ordem dono → lixeira → compartilhamento → `none`, lida na mesma consulta a cada pedido (revogação imediata); `readableDocumentsWhere` inclui o compartilhado; regra 9 da fronteira (só `access.service.ts` e `shares.service.ts` tocam `DocumentShare`); `PUT /documents/{documentId}/shares/{personId}` só do dono; `GET /people/search` sem `AdminGuard`, em controller próprio.
  - Skills: interface-design
  - Complexidade: baixa

- [x] T3.2 — Testes da fase 3 (e2e: compartilhar só pelo teclado e abrir em somente leitura)
  - Arquivos: `apps/web/e2e/tests/share-with-person-view.spec.ts` (criar)
  - O que fazer (D11): API simulada, estado inicial por `page.addInitScript`, como os specs existentes. `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo, usado em toda espera após mudança de rota; `toBeFocused()` antes de cada `Enter`/`Space`; nenhum `waitForTimeout`; nenhuma regra do axe desativada; outros specs e `apps/web/e2e/a11y.ts` intocados.
    - `the owner shares a document with a person using only the keyboard`: abre o documento, alcança "Compartilhar" por `Tab`, abre o diálogo "Compartilhar documento", `expectNoSeriousA11yViolations(page)` com o diálogo aberto, digita 1 caractere e vê "Digite pelo menos 2 letras para buscar.", digita o nome, alcança "Selecionar {nome}", vê "Pode ver", aciona "Compartilhar" e vê "Documento compartilhado com {nome}.".
    - `a person with view access opens the document read only`: segundo contexto do Playwright com `mock-documents=shared-view`; abre o endereço do documento (`heading` nível 1 com o título, `ROUTE_TIMEOUT`), vê "Somente leitura", digitar no editor não muda o texto, e não há botão "Compartilhar" nem "Mover para a lixeira".
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [x] CA3.1 — `pnpm test:e2e` na raiz sai com 0 (os e2e antigos e os novos), e `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` continuam saindo com 0 e sem aviso, com o cache do `tsc` limpo.
- [x] CA3.2 — `apps/web/e2e/tests/share-with-person-view.spec.ts` contém os casos `the owner shares a document with a person using only the keyboard` e `a person with view access opens the document read only`, e `pnpm --filter web exec playwright test --list` os lista. `rg -n "test\.skip|test\.only|waitForTimeout|disableRules" apps/web/e2e/tests/share-with-person-view.spec.ts` é vazio.
- [x] CA3.3 — Lendo o spec: `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo, passado em toda asserção após mudança de rota; `toBeFocused()` antes de cada tecla de ação; `expectNoSeriousA11yViolations(page)` com o diálogo aberto; o primeiro caso assere "Digite pelo menos 2 letras para buscar.", "Pode ver" e "Documento compartilhado com"; o segundo usa um novo contexto com `shared-view`, assere "Somente leitura", o texto do editor inalterado depois de digitar e a ausência de "Compartilhar" e "Mover para a lixeira".
- [x] CA3.4 — `docs/design.md` tem a receita "Selo de somente leitura" com `bg-gray-100 text-gray-800`; `docs/architecture.md` tem o parágrafo "Entrega `share-with-person-view` (fatia 145)" citando `DocumentShare`, `PUT /documents/{documentId}/shares/{personId}`, `GET /people/search` e a regra 9.
- [x] CA3.5 — A fatia está utilizável de ponta a ponta: `pnpm exec vitest run --project api` (0) inclui `PUT share answers 200 and creates one row` e `GET a shared document answers 200 with accessLevel view`; `pnpm exec vitest run --project web` (0) inclui `sharing announces success with the name from the response and clears the search` e `a view person sees Somente leitura and a read only editor`; `pnpm test:e2e` (0) inclui os dois casos de `share-with-person-view.spec.ts`.

## DoD da entrega

- [x] DoD1 — Todas as tarefas e critérios do plano marcados
- [x] DoD2 — Suíte de testes inteira passa
- [x] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [x] DoD4 — Tipos de todos os `tsconfig` sem erros
- [x] DoD5 — Console dos testes sem erro nem aviso
- [x] DoD6 — `build` passa
- [x] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [x] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [x] DoD9 — Nenhuma worktree ou branch temporária sobrando
