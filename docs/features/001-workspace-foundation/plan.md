# PLAN 001 — workspace-foundation

Branch: `feature/001-workspace-foundation`

Fonte: `docs/features/001-workspace-foundation/spec.md` (as siglas D1…D12 abaixo são as decisões técnicas da SPEC; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict. Portas: web 5173, API 3000, Postgres 5433, web do e2e 5174.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local). Os comandos de verificação são sempre rodados na raiz: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`.

Desvio único em relação à tabela de arquivos da SPEC: a alteração de `docs/design.md` sai da fase 3 e entra na fase 2 (T2.5), porque a skill `interface-design` manda a receita nova entrar na mesma tarefa da tela que a usa, e os critérios visuais da fase 2 apontam para essas receitas.

## Fase 1 — Monorepo, contrato e API de health verdes nos quatro comandos

- [x] T1.1 — Raiz do monorepo: workspace, lint, tipos e teste únicos na raiz
  - Arquivos: `package.json` (criar); `pnpm-workspace.yaml` (criar); `.nvmrc` (criar); `tsconfig.base.json` (criar); `tsconfig.json` (criar); `tsconfig.node.json` (criar); `eslint.config.js` (criar); `vitest.config.ts` (criar); `.gitignore` (alterar); `README.md` (criar)
  - O que fazer:
    - `package.json`: `private: true`, `packageManager` `pnpm@11.x`, `engines.node: ">=24"`, `type: "module"`. Scripts literais: `dev` = `pnpm --parallel --filter "./apps/*" dev`; `lint` = `eslint . --max-warnings 0`; `typecheck` = `tsc -b --noEmit`; `test` = `vitest run --coverage`; `test:e2e` = `pnpm --filter web test:e2e`; `build` = `pnpm -r build`. Dev-deps da raiz: `eslint`, `typescript-eslint`, `eslint-plugin-react-hooks`, `eslint-plugin-jsx-a11y`, `eslint-plugin-import` (com resolver de TypeScript), `eslint-plugin-check-file`, `typescript` 5.9, `vitest`, `@vitest/coverage-v8`.
    - `pnpm-workspace.yaml`: `packages: ["apps/*", "packages/*"]`; `onlyBuiltDependencies`: `prisma`, `@prisma/client`, `@prisma/engines`, `esbuild`, `@swc/core`, `msw`.
    - `.nvmrc`: `24`.
    - `tsconfig.base.json`: `strict`, `noUncheckedIndexedAccess`, `moduleResolution: "bundler"`, `composite: true`, `skipLibCheck`. Cada projeto composto grava `tsBuildInfoFile` em `node_modules/.tmp/`.
    - `tsconfig.json`: só `files: []` e `references`. **Nesta fase**: `packages/api-contract`, `apps/api`, `./tsconfig.node.json`. (A web entra na fase 2.)
    - `tsconfig.node.json`: projeto composto que cobre `vitest.config.ts` e `eslint.config.js` da raiz.
    - `eslint.config.js`: flat config único. `typescript-eslint` com tipos (`projectService`), `react-hooks` e `jsx-a11y` (recomendado) para `apps/web/**`, `import/no-restricted-paths` **só** para `apps/web/src` com as zonas: `features/connection` não é importada por outra feature; `features/**` não importa de `app/**`; compartilhado (`components`, `hooks`, `lib`, `types`, `utils`, `config`) não importa de `features/**` nem de `app/**`. `check-file` exigindo kebab-case em arquivos e pastas. Ignora `**/dist`, `**/coverage`, `**/generated/**`, `apps/web/public/mockServiceWorker.js`, `node_modules`. Proibido `any` (`@typescript-eslint/no-explicit-any: error`).
    - `vitest.config.ts`: `test.projects` — **nesta fase** só `apps/api`. Cobertura `provider: 'v8'`, `thresholds.lines: 80`, `exclude` com `**/main.ts`, `**/main.tsx`, `**/src/testing/**`, `**/generated/**`, `**/*.d.ts`, arquivos de configuração e `**/test/**`.
    - `.gitignore`: acrescentar `node_modules`, `dist`, `coverage`, `.env`, `apps/web/e2e/test-results/`, `apps/web/e2e/report/`.
    - `README.md` em pt_BR: pré-requisitos (Node 24, pnpm 11, Docker), `docker compose up -d`, `pnpm install`, copiar os `.env.example`, `pnpm dev`, e os comandos de verificação; registrar literalmente que `pnpm test` (e `npx vitest run` na raiz) exige `docker compose up -d` antes.
  - Skills: project-structure, unit-testing
  - Complexidade: alta

- [x] T1.2 — Contrato OpenAPI e pacote de tipos gerados
  - Arquivos: `packages/api-contract/package.json` (criar); `packages/api-contract/tsconfig.json` (criar); `packages/api-contract/openapi.yaml` (criar); `packages/api-contract/src/generated/openapi.d.ts` (criar, gerado e versionado)
  - O que fazer: pacote `@folioteca/api-contract`, `private`, só de tipos: `exports["."].types` → `./src/generated/openapi.d.ts`; script `generate` = `openapi-typescript openapi.yaml -o src/generated/openapi.d.ts`; script `build` ausente ou no-op (o `pnpm -r build` não pode falhar). `tsconfig.json` composto estendendo a base e incluindo `src/generated`. `openapi.yaml` OpenAPI 3.1: `servers: [{ url: /api }]`; `GET /health` com `200` → `HealthResponse` e `503` → `Error`; schemas: `Health` = `{ status: "ok" (enum), database: "up" (enum) }`, ambos obrigatórios; `HealthResponse` = `{ data: Health }`, `data` obrigatório; `Error` = `{ message: string }`, só `message` obrigatório, `additionalProperties: true`. Rodar `pnpm --filter @folioteca/api-contract generate` e versionar a saída sem editar à mão.
  - Skills: —
  - Complexidade: baixa

- [x] T1.3 — Banco local e esqueleto da API (NestJS + SWC + Prisma 6)
  - Arquivos: `docker-compose.yml` (criar); `docker/postgres/init.sql` (criar); `apps/api/package.json` (criar); `apps/api/tsconfig.json` (criar); `apps/api/tsconfig.build.json` (criar); `apps/api/nest-cli.json` (criar); `apps/api/.swcrc` (criar); `apps/api/vitest.config.ts` (criar); `apps/api/.env.example` (criar); `apps/api/prisma/schema.prisma` (criar); `apps/api/prisma/migrations/0001_init/migration.sql` (criar); `apps/api/prisma/migrations/migration_lock.toml` (criar); `apps/api/test/global-setup.ts` (criar)
  - O que fazer:
    - `docker-compose.yml`: serviço com imagem `pgvector/pgvector:pg16`, porta `127.0.0.1:5433:5432`, `POSTGRES_USER=folioteca`, `POSTGRES_DB=folioteca`, `POSTGRES_HOST_AUTH_METHOD=trust`, volume nomeado e `./docker/postgres/init.sql` montado em `/docker-entrypoint-initdb.d/`. **Nenhuma senha** em arquivo versionado (URL com senha dispara o GitGuardian). `init.sql`: `CREATE DATABASE folioteca_test;`.
    - `apps/api/package.json`: nome `api`; NestJS 11 (`@nestjs/common`, `@nestjs/core`, `@nestjs/platform-express`, `reflect-metadata`, `rxjs`), `@prisma/client` 6.x, `zod`; dev: `@nestjs/cli`, `@nestjs/testing`, `@swc/core`, `@swc/cli`, `prisma` 6.x, `supertest` + tipos, `@apidevtools/swagger-parser`, `ajv`, `ajv-formats`, `openapi-typescript`, `unplugin-swc`, `@folioteca/api-contract` (`workspace:*`). Scripts: `dev` = `nest start --watch -b swc`; `build` = `nest build -b swc`; `prisma:migrate` = `prisma migrate dev`; `prisma:generate` = `prisma generate`; `postinstall` = `prisma generate`.
    - `tsconfig.json`: estende a base, `experimentalDecorators`, `emitDecoratorMetadata`, inclui `src`, `test`, `vitest.config.ts`. `tsconfig.build.json`: exclui `**/__tests__/**`, `test`, `**/*.test.ts`. `nest-cli.json`: `compilerOptions.builder: "swc"`. `.swcrc` único: parser TypeScript com `decorators: true`, `transform.legacyDecorator: true`, `transform.decoratorMetadata: true`.
    - `vitest.config.ts`: `environment: 'node'`, plugin `unplugin-swc` (lendo o `.swcrc`), `globalSetup: './test/global-setup.ts'`, `fileParallelism: false`, `include` com `src/**/*.test.ts` e `test/**/*.test.ts`.
    - `.env.example`: `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca` e `PORT=3000`.
    - `schema.prisma`: só `datasource db` (`postgresql`, `env("DATABASE_URL")`) e `generator client` (`prisma-client-js`); nenhum modelo, nenhum `previewFeatures`. `migration.sql` escrito à mão: `CREATE EXTENSION IF NOT EXISTS vector;`. `migration_lock.toml`: `provider = "postgresql"`.
    - `test/global-setup.ts`: se `process.env.DATABASE_URL` estiver ausente, define `postgresql://folioteca@localhost:5433/folioteca_test`; roda `prisma migrate deploy` (via `execSync`, `cwd` em `apps/api`) antes dos testes.
  - Skills: security, unit-testing
  - Complexidade: média

- [x] T1.4 — API: ambiente validado, app com prefixo `/api`, Prisma e `GET /health`
  - Arquivos: `apps/api/src/config/env.ts` (criar); `apps/api/src/create-app.ts` (criar); `apps/api/src/main.ts` (criar); `apps/api/src/app.module.ts` (criar); `apps/api/src/prisma/prisma.module.ts` (criar); `apps/api/src/prisma/prisma.service.ts` (criar); `apps/api/src/health/health.module.ts` (criar); `apps/api/src/health/health.controller.ts` (criar)
  - O que fazer:
    - `env.ts`: schema zod com `DATABASE_URL` (string não vazia) e `PORT` (coerção para número, default `3000`); exporta `env`; variável inválida lança na importação, com mensagem que nomeia a variável.
    - `create-app.ts`: `export async function createApp(overrides?): Promise<INestApplication>` — cria o app a partir de `AppModule`, aplica `app.setGlobalPrefix('api')` e devolve **sem** `listen`. É a mesma função usada por `main.ts` e pelos testes; precisa permitir substituir o `PrismaService` nos testes (aceitar um módulo já compilado pelo `Test.createTestingModule` ou expor `configureApp(app)` que aplica o prefixo; escolha uma e use nos dois lugares).
    - `main.ts`: `createApp()` e `listen(env.PORT)`.
    - `AppModule` importa `PrismaModule` e `HealthModule`. `PrismaModule` é `@Global()` e exporta `PrismaService`, que estende `PrismaClient` com `onModuleInit` (`$connect`) e `onModuleDestroy` (`$disconnect`).
    - `HealthController` (`@Controller('health')`, `@Get()`): executa `` this.prisma.$queryRaw`SELECT 1` ``; sucesso → `{ data: { status: 'ok', database: 'up' } }` tipado pelo `HealthResponse` de `@folioteca/api-contract`; qualquer falha → `throw new ServiceUnavailableException('Banco de dados indisponível.')` (HTTP 503, corpo padrão do Nest com `message`).
  - Skills: security
  - Complexidade: média

- [x] T1.5 — Testes da fase 1
  - Arquivos: `apps/api/test/contract.ts` (criar); `apps/api/test/generated-types.test.ts` (criar); `apps/api/src/health/__tests__/health.integration.test.ts` (criar); `apps/api/src/health/__tests__/health.contract.test.ts` (criar); `apps/api/src/config/__tests__/env.test.ts` (criar)
  - O que fazer:
    - `contract.ts`: `export async function expectMatchesContract(args: { path: string; method: 'get' | 'post' | 'put' | 'patch' | 'delete'; status: number; body: unknown }): Promise<void>` — carrega e valida `packages/api-contract/openapi.yaml` com `@apidevtools/swagger-parser` (dereferenciado, em cache), acha o schema de `paths[path][method].responses[status].content['application/json'].schema` e valida `body` com `ajv` (modo 2020/3.1) + `ajv-formats`; falha com os erros do ajv na mensagem; falha também se caminho, método ou status não existirem no contrato.
    - Testes HTTP com `supertest` sobre o app de `create-app.ts` (prefixo `/api` de verdade). O 503 substitui o `PrismaService` por um cujo `$queryRaw` rejeita.
    - `generated-types.test.ts`: `generated types match the openapi contract` (regenera em memória com a API de `openapi-typescript` e compara com o arquivo versionado).
    - `health.integration.test.ts`: `GET /api/health returns 200 with status ok and database up`; `GET /health without the api prefix returns 404`; `the vector extension is installed in the database` (consulta `pg_extension`); `GET /api/health returns 503 with the unavailable message when the database query rejects`.
    - `health.contract.test.ts`: `200 response matches the HealthResponse schema`; `503 response matches the Error schema`; `expectMatchesContract rejects a body that violates the schema`; `expectMatchesContract rejects a status that is not in the contract`.
    - `env.test.ts`: `parses a valid environment and defaults PORT to 3000`; `throws naming DATABASE_URL when it is missing`.
  - Skills: unit-testing, integration-testing
  - Complexidade: média

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d` rodando, na raiz passam sem erro nem aviso: `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`; e também os comandos crus `npx eslint .`, `npx tsc -b` e `npx vitest run`.
- [x] CA1.2 — O `package.json` da raiz tem os seis scripts com o valor literal: `dev` = `pnpm --parallel --filter "./apps/*" dev`, `lint` = `eslint . --max-warnings 0`, `typecheck` = `tsc -b --noEmit`, `test` = `vitest run --coverage`, `test:e2e` = `pnpm --filter web test:e2e`, `build` = `pnpm -r build`; `engines.node` é `>=24` e `packageManager` começa com `pnpm@11`. `.nvmrc` contém `24`.
- [x] CA1.3 — `tsconfig.json` da raiz tem `files: []` e `references` para `packages/api-contract`, `apps/api` e `tsconfig.node.json`; `tsconfig.base.json` tem `strict: true` e `noUncheckedIndexedAccess: true`. O `vitest.config.ts` da raiz usa `test.projects`, cobertura `v8` e `thresholds.lines: 80`.
- [x] CA1.4 — `eslint.config.js` é o único config de lint do repositório, carrega `jsx-a11y`, `react-hooks`, `import/no-restricted-paths` restrito a `apps/web/src` e `check-file` em kebab-case. Não existe nenhum comentário `eslint-disable` no repositório.
- [x] CA1.5 — `docker-compose.yml` usa `pgvector/pgvector:pg16`, publica `127.0.0.1:5433:5432` e `POSTGRES_HOST_AUTH_METHOD=trust`; `docker/postgres/init.sql` cria `folioteca_test`. `rg -n "postgresql://[^@/]+:[^@/]+@" .` (fora de `node_modules`) não acha nada: nenhuma URL com senha versionada. `apps/api/.env.example` tem `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca` e `PORT=3000`.
- [x] CA1.6 — `packages/api-contract/openapi.yaml` é OpenAPI 3.1 com servidor `/api`, `GET /health` (200 `HealthResponse`, 503 `Error`) e os schemas `Health`, `HealthResponse`, `Error`. `packages/api-contract/package.json` chama-se `@folioteca/api-contract`, tem `exports["."].types` apontando para `./src/generated/openapi.d.ts` e o script `generate`. O arquivo gerado está versionado (`git ls-files packages/api-contract/src/generated/openapi.d.ts` o lista).
- [x] CA1.7 — `apps/api/prisma/schema.prisma` não tem nenhum `model` nem `previewFeatures`; `apps/api/prisma/migrations/0001_init/migration.sql` contém `CREATE EXTENSION IF NOT EXISTS vector;`. `prisma` e `@prisma/client` estão na faixa 6.x.
- [x] CA1.8 — Existe `createApp` em `apps/api/src/create-app.ts`, que aplica o prefixo global `api` e não chama `listen`; `apps/api/src/main.ts` usa essa função e sobe em `env.PORT`. `HealthController` responde `{ data: { status: 'ok', database: 'up' } }` e, na falha do `SELECT 1`, lança `ServiceUnavailableException` com a mensagem literal "Banco de dados indisponível.".
- [x] CA1.9 — `apps/api/.swcrc` tem `legacyDecorator` e `decoratorMetadata` ligados; `apps/api/nest-cli.json` usa o builder `swc`; `apps/api/vitest.config.ts` usa `unplugin-swc`, `globalSetup` e `fileParallelism: false`.
- [x] CA1.10 — Existe `expectMatchesContract({ path, method, status, body })` em `apps/api/test/contract.ts`, retornando `Promise<void>`, implementado com `@apidevtools/swagger-parser` e `ajv`.
- [x] CA1.11 — Existem e passam, pelos nomes: em `apps/api/test/generated-types.test.ts`, `generated types match the openapi contract`; em `apps/api/src/health/__tests__/health.integration.test.ts`, `GET /api/health returns 200 with status ok and database up`, `GET /health without the api prefix returns 404`, `the vector extension is installed in the database`, `GET /api/health returns 503 with the unavailable message when the database query rejects`; em `apps/api/src/health/__tests__/health.contract.test.ts`, `200 response matches the HealthResponse schema`, `503 response matches the Error schema`, `expectMatchesContract rejects a body that violates the schema`, `expectMatchesContract rejects a status that is not in the contract`; em `apps/api/src/config/__tests__/env.test.ts`, `parses a valid environment and defaults PORT to 3000` e `throws naming DATABASE_URL when it is missing`. Conferir com `npx vitest run --reporter=verbose`.
- [x] CA1.12 — O relatório de `pnpm test` mostra ≥ 80% de linhas em cada arquivo de `apps/api/src` (exceto `main.ts`).
- [x] CA1.13 — `README.md` está em pt_BR, lista `docker compose up -d`, `pnpm install`, `pnpm dev` e os comandos de verificação, e avisa que os testes exigem o Postgres no ar.

## Fase 2 — App web navegável: moldura, cinco páginas, 404 e indicador de conexão

Caminhos relativos a `apps/web/`, salvo os que começam por `docs/` ou estão marcados como "raiz".

- [x] T2.1 — Esqueleto da web (Vite, Tailwind 4, tipos, Vitest) e registro na raiz
  - Arquivos: `package.json` (criar); `index.html` (criar); `vite.config.ts` (criar); `vitest.config.ts` (criar); `tsconfig.json` (criar); `tsconfig.app.json` (criar); `tsconfig.node.json` (criar); `.env.example` (criar); `src/index.css` (criar); `src/vite-env.d.ts` (criar); raiz `tsconfig.json` (alterar); raiz `vitest.config.ts` (alterar)
  - O que fazer:
    - `package.json`: nome `web`, `type: "module"`; deps: `react` 19, `react-dom` 19, `react-router` 7, `@tanstack/react-query` 5, `axios`, `zod`, `react-error-boundary`, `@folioteca/api-contract` (`workspace:*`); dev: `vite`, `@vitejs/plugin-react`, `tailwindcss` 4, `@tailwindcss/vite`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`, `msw`, `@playwright/test`, `@axe-core/playwright`, tipos de React. Scripts: `dev` = `vite`, `build` = `vite build`, `test:e2e` = `playwright test`. Campo `msw.workerDirectory` = `public`.
    - `index.html`: `<html lang="pt-BR">`, `<title>Folioteca</title>`, `<meta name="viewport" …>`, `<div id="root">`, script de `/src/main.tsx`.
    - `vite.config.ts`: plugins React e Tailwind, alias `@` → `./src`, `server.port: 5173`, `server.proxy['/api']` → `http://localhost:3000`. Sem proxy de `/collab`.
    - `vitest.config.ts`: `environment: 'jsdom'`, `setupFiles: ['./src/testing/setup-tests.ts']`, alias `@`, `exclude` com `e2e/**` e `node_modules`.
    - `tsconfig.json`: só `references` para `tsconfig.app.json` e `tsconfig.node.json`. `tsconfig.app.json`: composto, `jsx: react-jsx`, `lib` com DOM, `paths` `@/*` → `src/*`, `include: ["src"]`, tipos de `vite/client` e `@testing-library/jest-dom`. `tsconfig.node.json`: composto, `include` (por padrão de glob, não `files`, para não quebrar antes da fase 3) com `vite.config.ts`, `vitest.config.ts`, `playwright.config.ts`, `e2e`.
    - `.env.example`: `VITE_APP_API_URL=/api` e `VITE_APP_ENABLE_API_MOCKING=false`. Nenhum segredo em `VITE_*`.
    - `src/index.css`: só `@import "tailwindcss";`. `src/vite-env.d.ts`: referência a `vite/client` e `ImportMetaEnv` com `VITE_APP_API_URL?: string` e `VITE_APP_ENABLE_API_MOCKING?: string`.
    - Raiz: `tsconfig.json` ganha as referências `apps/web/tsconfig.app.json` e `apps/web/tsconfig.node.json`; `vitest.config.ts` ganha `apps/web` em `test.projects`.
  - Skills: api-client, component-testing, e2e-testing, security, interface-design
  - Complexidade: média

- [x] T2.2 — Compartilhado: ambiente, caminhos, cliente HTTP, React Query, erros e tipos
  - Arquivos: `src/config/env.ts` (criar); `src/config/paths.ts` (criar); `src/lib/api-client.ts` (criar); `src/lib/react-query.ts` (criar); `src/lib/errors.ts` (criar); `src/lib/report-error.ts` (criar); `src/types/api.ts` (criar)
  - O que fazer:
    - `env.ts`: zod sobre as variáveis `VITE_APP_*` sem o prefixo; exporta `env` com `API_URL: string` (default `/api`) e `ENABLE_API_MOCKING: boolean` (`'true'` → `true`; ausente ou outro valor → `false`).
    - `paths.ts`: `export const paths` com `home` (`/`), `favorites` (`/favorites`), `myDocuments` (`/my-documents`), `spaces` (`/spaces`), `trash` (`/trash`); cada um `{ path: string; getHref: () => string }`.
    - `errors.ts`: `class UnauthenticatedError extends Error` e o guard `isUnauthenticatedError(error: unknown): error is UnauthenticatedError`. `report-error.ts`: `reportError(error: unknown, context?: Record<string, unknown>): void`, ponto único de reporte (usa `console.error`; nada de `console.log`).
    - `api-client.ts`: `export const api` — instância axios única com `baseURL: env.API_URL`, `withCredentials: true`, header `Accept: application/json`; interceptor de resposta devolve `response.data`; 401 rejeita com `UnauthenticatedError` **sem redirecionar**; todo erro termina em `Promise.reject`. **Sem** bloco de notificação (o polling do health notificaria a cada 3 s; notificações entram na fatia 002).
    - `react-query.ts`: `queryConfig` (defaults: `retry: false`, `refetchOnWindowFocus: false`, `staleTime` de 1 min) e os tipos utilitários `ApiFnReturnType`, `QueryConfig`, `MutationConfig` do modelo da skill.
    - `types/api.ts`: `Health` e `HealthResponse` derivados de `components['schemas']` de `@folioteca/api-contract` (import só de tipo).
  - Skills: api-client, error-handling, routing, api-requests
  - Complexidade: baixa

- [x] T2.3 — API simulada (MSW) e utilitários de teste
  - Arquivos: `public/mockServiceWorker.js` (criar, gerado por `npx msw init public/ --save`); `src/testing/mocks/utils.ts` (criar); `src/testing/mocks/handlers/health.ts` (criar); `src/testing/mocks/handlers/index.ts` (criar); `src/testing/mocks/server.ts` (criar); `src/testing/mocks/browser.ts` (criar); `src/testing/mocks/index.ts` (criar); `src/testing/setup-tests.ts` (criar); `src/testing/test-utils.tsx` (criar)
  - O que fazer: `utils.ts` com `networkDelay(): Promise<void>` (sem atraso em teste) e `devOverride(resource)` lendo do `localStorage` as chaves `mock-error` (valor `health` → handler responde 500) e `mock-delay` (valor `infinite` → nunca responde). `handlers/health.ts`: `http.get` em `${env.API_URL}/health`, começa por `await networkDelay()`, aplica `devOverride('health')` e responde `200` com corpo tipado por `HealthResponse` (`{ data: { status: 'ok', database: 'up' } }`). `handlers/index.ts` exporta `handlers`; `server.ts` (`setupServer`), `browser.ts` (`setupWorker`); `index.ts` exporta `enableMocking(): Promise<void>`, que só importa e inicia o worker quando `env.ENABLE_API_MOCKING` é `true` (`onUnhandledRequest: 'bypass'`). Sem `db` de mock: não há escrita. `setup-tests.ts`: jest-dom, `server.listen({ onUnhandledRequest: 'error' })`, `resetHandlers` e `cleanup` a cada teste, `close` no fim. `test-utils.tsx`: `renderApp(ui, { url?, path? })` com `QueryClientProvider` novo por teste (usando `queryConfig`) e `createMemoryRouter`; reexporta `screen`, `waitFor`, `within` e `userEvent`.
  - Skills: api-mocking, component-testing
  - Complexidade: média

- [x] T2.4 — Feature `connection`: consulta de saúde com polling e indicador
  - Arquivos: `src/features/connection/api/get-health.ts` (criar); `src/features/connection/components/connection-indicator.tsx` (criar)
  - O que fazer:
    - `get-health.ts`: `getHealth(): Promise<HealthResponse>` (`api.get('/health')`); `getHealthQueryOptions()` com `queryKey: ['health']`, `refetchInterval: (query) => query.state.status === 'error' ? 3_000 : 10_000`, `refetchOnWindowFocus: true`, `refetchOnReconnect: true`, `staleTime: 0`, `networkMode: 'always'` (offline falha na hora em vez de ficar pausada) e `retry: false`; `useHealth({ queryConfig }?)`. Sem store, sem `setInterval`, sem estado copiado.
    - `ConnectionIndicator` deriva tudo do estado da query. Carregando (`isPending`): parágrafo com `role="status"` e selo cinza com o texto "Conectando…". Com dados: parágrafo com `role="status"` e selo verde "Conectado". Erro (`isError`): bloco com `role="alert"`, selo vermelho "Sem conexão" e, abaixo, em tom suave, "Tentando reconectar…". **Sem** botão "Tentar de novo": a reconexão é automática. Selos pela receita "Selo de status" do `docs/design.md` (o par vermelho é a receita nova "Selo de erro").
  - Skills: api-requests, client-state, interface-design, error-handling
  - Complexidade: média

- [x] T2.5 — Moldura do app, layout de conteúdo, tela de erro da raiz e receitas novas
  - Arquivos: `src/components/layouts/app-layout.tsx` (criar); `src/components/layouts/content-layout.tsx` (criar); `src/components/errors/main-error-fallback.tsx` (criar); `docs/design.md` (alterar)
  - O que fazer:
    - `AppLayout({ sidebarFooter, children }: { sidebarFooter: React.ReactNode; children: React.ReactNode }): React.JSX.Element`. Não importa nada de `features/` nem de `app/`. Estrutura, de cima para baixo: (1) link "Pular para o conteúdo", primeiro elemento focável, visível só com foco, apontando para `#main-content`; (2) barra de topo, só abaixo de `md`: "Folioteca" à esquerda e, à direita, botão secundário `type="button"` com texto visível "Abrir menu" / "Fechar menu", `aria-expanded` refletindo o estado e `aria-controls` com o `id` do painel; (3) barra lateral `<aside>`: topo com "Folioteca" como `<Link>` para `paths.home.getHref()` em `font-semibold`; `<nav aria-label="Navegação principal">` com lista de quatro `NavLink` montados a partir de `paths`, nesta ordem e com estes textos: "Favoritos", "Meus documentos", "Espaços", "Lixeira" — o ativo recebe `aria-current="page"` (padrão do `NavLink`) e fundo de destaque; rodapé com `sidebarFooter`; (4) área de conteúdo com `children`.
    - Comportamento: de `md` para cima, duas colunas com a barra lateral sempre visível. Abaixo de `md`, a navegação + rodapé formam um painel recolhível **no fluxo** (entre a barra de topo e o conteúdo, empurrando o conteúdo; sem overlay, sem portal, sem foco preso, sem biblioteca). Estado em `useState` dentro do componente. Recolhido: o painel fica fora da árvore acessível abaixo de `md` (`hidden`) e continua visível de `md` para cima. Fecha quando `location.pathname` muda e quando `Esc` é pressionado — e o `Esc` devolve o foco ao botão do menu. Ordem de foco igual à visual; `focus-visible` em tudo que é clicável; sem ícones. Sem rolagem horizontal em 360 px.
    - `ContentLayout({ title, description, children }: { title: string; description: string; children?: React.ReactNode })`: `<main id="main-content">` com o contêiner de página, um único `<h1>` (título de página), o texto de apoio e `children`.
    - `MainErrorFallback`: página inteira no contêiner de página, com `role="alert"`; `h1` "Algo deu errado"; texto "Não foi possível abrir a página. Recarregue para tentar de novo."; botão principal `type="button"` "Recarregar" que chama `window.location.reload()`.
    - `docs/design.md`, seção "Padrões acrescentados pelas entregas", uma linha por padrão, com a coluna da fatia = `001`: **Moldura do app** — `<div className="min-h-screen md:flex">`; conteúdo `min-w-0 flex-1`. **Barra lateral** — `<aside className="border-b border-gray-200 bg-gray-50 md:flex md:w-64 md:shrink-0 md:flex-col md:border-b-0 md:border-r">`; blocos internos com `p-4`. **Barra de topo (tela estreita)** — `flex items-center justify-between gap-4 p-4 md:hidden`. **Item da barra lateral** — `block rounded-md px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-100 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600`; ativo: `bg-gray-200 text-gray-900`. **Link de pular conteúdo** — `sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-50 focus:rounded-md focus:bg-white focus:px-4 focus:py-2 focus:text-sm focus:font-medium focus:text-blue-600 focus:outline-2 focus:outline-blue-600`. **Selo de erro** — par `bg-red-100 text-red-800` para a receita "Selo de status". O componente usa exatamente essas classes.
  - Skills: interface-design, ui-components, client-state, component-robustness, error-handling
  - Complexidade: média

- [x] T2.6 — App, roteador e páginas (início, quatro áreas vazias, 404, erro de área)
  - Arquivos: `src/main.tsx` (criar); `src/app/index.tsx` (criar); `src/app/provider.tsx` (criar); `src/app/router.tsx` (criar); `src/app/routes/app/root.tsx` (criar); `src/app/routes/app/home.tsx` (criar); `src/app/routes/app/favorites.tsx` (criar); `src/app/routes/app/my-documents.tsx` (criar); `src/app/routes/app/spaces.tsx` (criar); `src/app/routes/app/trash.tsx` (criar); `src/app/routes/not-found.tsx` (criar)
  - O que fazer:
    - `main.tsx`: importa `index.css`, `await enableMocking()` e só então renderiza `<App />` em `StrictMode`. `app/index.tsx`: `App` = `AppProvider` + `AppRouter`. `provider.tsx`: `AppProvider` com `ErrorBoundary` de `react-error-boundary` (`FallbackComponent={MainErrorFallback}`, `onError` → `reportError`) e `QueryClientProvider` com o cliente criado em `useState` a partir de `queryConfig`.
    - `router.tsx`: exporta `createAppRouter()`/`AppRouter` com `createBrowserRouter`; rota-pai `paths.home.path` com `root.tsx` **importado direto** (elemento + `ErrorBoundary`), filhas com `lazy` (`home` como índice, `favorites`, `my-documents`, `spaces`, `trash`); `path: '*'` → `not-found.tsx`, **última do array e fora do layout**. Exportar também a lista de rotas para os testes montarem um roteador em memória.
    - `root.tsx`: `AppLayout` com `sidebarFooter={<ConnectionIndicator />}` e `<Outlet />` como conteúdo. `ErrorBoundary` da área (usa `useRouteError` + `reportError`): dentro do contêiner de página, `role="alert"`, `h1` "Algo deu errado", texto "Não foi possível abrir esta página. Tente de novo em instantes." e `<Link>` "Voltar para o início" para `paths.home.getHref()`.
    - `home.tsx` (`/`), com `ContentLayout`: `h1` "Boas-vindas à Folioteca"; apoio "Aqui você escreve, organiza e compartilha os documentos da sua organização."; `h2` "Como a Folioteca se organiza" (receita de subtítulo); lista (receita "Lista") de quatro itens, cada um com o nome da área como `<Link>` (receita "Link de navegação", destino por `paths`) e, abaixo, a descrição em tom secundário: "Favoritos" — "Os documentos que você marca para ter sempre à mão."; "Meus documentos" — "O que você cria, visível só para você até compartilhar."; "Espaços" — "Documentos reunidos por equipe ou por assunto."; "Lixeira" — "Documentos excluídos, até serem restaurados ou apagados de vez.".
    - Áreas vazias, cada uma com `ContentLayout` + um parágrafo na receita "Vazio" (não leem API; só o estado vazio existe):
      - `/favorites`: `h1` "Favoritos"; apoio "Os documentos que você marca como favoritos ficam à mão aqui."; vazio "Nenhum favorito ainda. Quando você marcar um documento como favorito, ele aparece aqui."
      - `/my-documents`: `h1` "Meus documentos"; apoio "Os documentos que você cria ficam aqui, visíveis só para você até serem compartilhados."; vazio "Nenhum documento ainda. Os documentos que você criar aparecem aqui."
      - `/spaces`: `h1` "Espaços"; apoio "Espaços reúnem os documentos de uma equipe ou de um assunto."; vazio "Nenhum espaço ainda. Os espaços de que você participa aparecem aqui."
      - `/trash`: `h1` "Lixeira"; apoio "Documentos excluídos ficam aqui até serem restaurados ou apagados de vez."; vazio "A lixeira está vazia. Os documentos que você excluir aparecem aqui."
    - `not-found.tsx`: página inteira, fora da moldura, no contêiner de página com `id="main-content"`: `h1` "Página não encontrada"; apoio "O endereço que você abriu não existe ou foi movido."; `<Link replace>` "Voltar para o início" para `paths.home.getHref()`.
    - Navegação interna sempre com `<Link>`/`NavLink` do roteador, nunca `<a href>` (exceto o link de pular conteúdo, que é âncora da própria página). Componentes retornam `React.JSX.Element`.
  - Skills: routing, error-handling, api-mocking, interface-design
  - Complexidade: média

- [x] T2.7 — Testes da fase 2
  - Arquivos: `src/components/layouts/__tests__/app-layout.test.tsx` (criar); `src/features/connection/components/__tests__/connection-indicator.test.tsx` (criar); `src/app/routes/__tests__/not-found.test.tsx` (criar); `src/app/routes/app/__tests__/home.test.tsx` (criar); `src/app/routes/app/__tests__/empty-areas.test.tsx` (criar); `src/config/__tests__/env.test.ts` (criar); `src/lib/__tests__/api-client.test.ts` (criar); `src/components/errors/__tests__/main-error-fallback.test.tsx` (criar)
  - O que fazer: interações por `userEvent`; consultas por papel e texto em pt_BR; API só pelo MSW de `src/testing`. Nenhum erro nem aviso no console dos testes (inclusive `act`).
    - `app-layout.test.tsx`: `renders the four navigation links in order inside the main navigation landmark`; `marks the link of the current route with aria-current page`; `renders the sidebarFooter slot`; `skip link is the first focusable element and targets main-content`; `menu button starts collapsed with aria-expanded false and the label Abrir menu`; `clicking the menu button expands the panel and changes the label to Fechar menu`; `menu button toggles with the keyboard using Enter and Space`; `Escape collapses the panel and returns focus to the menu button`; `collapses the panel after navigating to another route`; `aria-controls of the menu button matches the panel id`.
    - `connection-indicator.test.tsx`: `shows Conectando… with role status while the first request is pending`; `shows Conectado with role status when health responds 200`; `shows Sem conexão and Tentando reconectar… with role alert when health responds 503`; `shows Sem conexão when the request fails with a network error`; `goes back to Conectado on its own after a failure` (handler de erro com `once: true` + timers falsos avançando 3 s); `does not render a retry button`.
    - `not-found.test.tsx` (integração, roteador em memória com as rotas reais): `unknown address renders Página não encontrada as the only h1`; `shows the support text`; `renders outside the app layout without the main navigation`; `Voltar para o início link leads to the home page`.
    - `home.test.tsx`: `renders Boas-vindas à Folioteca as the only h1 with the support text`; `renders the Como a Folioteca se organiza heading as h2`; `lists the four areas with their descriptions`; `each area link navigates to its page`; `renders inside the app layout with the connection indicator`.
    - `empty-areas.test.tsx` (`it.each` sobre as quatro rotas): `%s renders its title as the only h1`; `%s renders its support text`; `%s renders its empty state text`; `%s marks its sidebar link as the current page`.
    - `env.test.ts`: `defaults API_URL to /api and ENABLE_API_MOCKING to false`; `parses the string true as the boolean true`; `treats any other value as false`.
    - `api-client.test.ts`: `returns the response body instead of the axios response`; `rejects with UnauthenticatedError on 401 without redirecting`; `rejects with the original error on 500`; `isUnauthenticatedError narrows only UnauthenticatedError`.
    - `main-error-fallback.test.tsx`: `renders Algo deu errado, the support text and the Recarregar button`; `Recarregar reloads the page`.
  - Skills: component-testing, integration-testing, api-mocking, unit-testing
  - Complexidade: média

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d` rodando, passam na raiz sem erro nem aviso: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` (gera `apps/web/dist/index.html`). O `tsconfig.json` da raiz referencia `apps/web/tsconfig.app.json` e `apps/web/tsconfig.node.json`; o `vitest.config.ts` da raiz lista `apps/api` e `apps/web` em `test.projects`.
- [x] CA2.2 — `apps/web/index.html` tem `<html lang="pt-BR">` e `<title>Folioteca</title>`. `apps/web/src/index.css` contém só `@import "tailwindcss";`. `apps/web/vite.config.ts` usa a porta 5173, o alias `@` e faz proxy só de `/api` para `http://localhost:3000`.
- [x] CA2.3 — `apps/web/src/config/env.ts` exporta `env` com `API_URL` (default `/api`) e `ENABLE_API_MOCKING` (boolean, default `false`). `apps/web/.env.example` tem só `VITE_APP_API_URL=/api` e `VITE_APP_ENABLE_API_MOCKING=false`. `apps/web/src/config/paths.ts` exporta `paths` com `home`, `favorites`, `myDocuments`, `spaces`, `trash`, cada um com `path` e `getHref(): string`, nos caminhos `/`, `/favorites`, `/my-documents`, `/spaces`, `/trash`.
- [x] CA2.4 — `apps/web/src/lib/api-client.ts` exporta uma única instância axios `api` com `baseURL: env.API_URL` e `withCredentials: true`; devolve `response.data`; 401 rejeita com `UnauthenticatedError` (de `src/lib/errors.ts`) e não toca em `window.location`; não importa nenhum módulo de notificação. `rg -n "localStorage" apps/web/src --glob '!**/testing/**'` não acha nada.
- [x] CA2.5 — `apps/web/src/types/api.ts` exporta `Health` e `HealthResponse` derivados de `@folioteca/api-contract`, sem redeclarar campos à mão.
- [x] CA2.6 — Em `apps/web/src/features/connection/api/get-health.ts` existem `getHealth(): Promise<HealthResponse>`, `getHealthQueryOptions()` e `useHealth`, com `refetchInterval` de `3_000` em erro e `10_000` caso contrário, `refetchOnWindowFocus: true`, `refetchOnReconnect: true`, `staleTime: 0`, `networkMode: 'always'` e `retry: false`. Não há `setInterval`, Zustand nem `useState` na feature `connection`.
- [x] CA2.7 — `ConnectionIndicator` mostra: `role="status"` com "Conectando…"; `role="status"` com "Conectado"; `role="alert"` com "Sem conexão" e "Tentando reconectar…". Cada texto de estado está num selo com as classes de "Selo de status" do `docs/design.md` (cinza `bg-gray-100 text-gray-700`, verde `bg-green-100 text-green-800`, vermelho `bg-red-100 text-red-800`). Não há `<button>` no componente.
- [x] CA2.8 — `AppLayout` em `apps/web/src/components/layouts/app-layout.tsx` tem a prop `sidebarFooter: React.ReactNode`, não importa de `@/features` nem de `@/app`, monta os quatro `NavLink` a partir de `paths` dentro de `<nav aria-label="Navegação principal">` com os textos "Favoritos", "Meus documentos", "Espaços", "Lixeira", e tem o link "Pular para o conteúdo" para `#main-content` como primeiro elemento focável. O botão do menu é `<button type="button">` com as classes de botão secundário do `docs/design.md`, `aria-expanded`, `aria-controls` e os textos "Abrir menu" / "Fechar menu". Não há `@radix-ui` nem biblioteca de ícones em `apps/web/package.json`.
- [x] CA2.9 — `docs/design.md` tem, em "Padrões acrescentados pelas entregas", as seis receitas da fatia 001 (Moldura do app, Barra lateral, Barra de topo (tela estreita), Item da barra lateral, Link de pular conteúdo, Selo de erro), e `app-layout.tsx` usa exatamente essas classes: barra de topo com `md:hidden`, `<aside>` com `md:w-64 md:shrink-0`, conteúdo com `min-w-0 flex-1`, item ativo com `bg-gray-200 text-gray-900`, todo item com `focus-visible:outline-2`.
- [x] CA2.10 — Piso de acabamento, conferível lendo o código: toda página (`home.tsx`, as quatro áreas, `not-found.tsx`, `MainErrorFallback`, o `ErrorBoundary` de `root.tsx`) fica dentro do contêiner `mx-auto max-w-2xl p-8`, tem **um único** `<h1>` com `text-2xl font-bold` e texto de apoio com `mt-2 text-gray-600`; o `<h2>` de `home.tsx` usa `mt-8 text-lg font-semibold`; a lista de `home.tsx` usa `mt-6 divide-y divide-gray-200` com a descrição em `text-gray-600`; o vazio das quatro áreas usa a receita "Vazio" (`border-dashed`, `text-center text-gray-600`); o botão "Recarregar" usa as classes de botão principal; links usam a receita "Link de navegação". `rg -n "style=\{\{|!important|<a href=\"/" apps/web/src` não acha nada, e não há largura ou altura fixa em px em contêiner de conteúdo.
- [x] CA2.11 — `apps/web/src/app/router.tsx` registra as filhas com `lazy`, importa `root.tsx` direto, e `path: '*'` é a última entrada do array e está fora da rota do layout. `root.tsx` passa `<ConnectionIndicator />` em `sidebarFooter` e exporta `ErrorBoundary` com "Algo deu errado", "Não foi possível abrir esta página. Tente de novo em instantes." e o link "Voltar para o início". `not-found.tsx` usa `<Link replace>` para `paths.home.getHref()`.
- [x] CA2.12 — Os textos literais das páginas conferem, caractere a caractere, com os da tarefa T2.6 (h1, apoio, vazio das quatro áreas; h1, apoio, h2 e as quatro descrições do início; h1, apoio e link da 404).
- [x] CA2.13 — O handler de `apps/web/src/testing/mocks/handlers/health.ts` intercepta `${env.API_URL}/health`, começa por `await networkDelay()`, tem o corpo tipado por `HealthResponse` e respeita as chaves `mock-error` (`health`) e `mock-delay` (`infinite`). `main.tsx` chama `enableMocking()` antes de renderizar, e o worker só é importado quando `env.ENABLE_API_MOCKING` é `true`. `apps/web/public/mockServiceWorker.js` está versionado.
- [x] CA2.14 — Existem e passam, pelos nomes listados em T2.7, os testes de `app-layout.test.tsx` (10 casos, incluindo `Escape collapses the panel and returns focus to the menu button` e `collapses the panel after navigating to another route`), `connection-indicator.test.tsx` (6 casos, incluindo `goes back to Conectado on its own after a failure`), `not-found.test.tsx` (4), `home.test.tsx` (5), `empty-areas.test.tsx` (4 casos × 4 rotas), `env.test.ts` (3), `api-client.test.ts` (4) e `main-error-fallback.test.tsx` (2). Conferir com `npx vitest run --project web --reporter=verbose`.
- [x] CA2.15 — O relatório de `pnpm test` mostra ≥ 80% de linhas em cada arquivo de `apps/web/src` (fora `main.tsx` e `src/testing/**`), e a saída dos testes não tem `Warning:`, `act(` nem `console.error`.
- [x] CA2.16 — Nenhum arquivo de `apps/web/src/components`, `lib`, `config` ou `types` importa de `@/features` ou `@/app`; nenhum arquivo de `features/connection` importa de `@/app`. Não há `index.ts` de barrel fora de `src/testing/mocks`. Nenhum `forwardRef` e nenhum tipo `JSX.Element` global (`rg -n "forwardRef|: JSX\.Element" apps/web/src` vazio).

## Fase 3 — Jornada "abrir o app" provada de ponta a ponta, com acessibilidade medida

- [x] T3.1 — Configuração do Playwright contra a API simulada, em porta própria
  - Arquivos: `apps/web/playwright.config.ts` (criar); `README.md` (alterar)
  - O que fazer: a partir do modelo da skill `e2e-testing`: `testDir: './e2e/tests'`, `outputDir: './e2e/test-results'`, relatório HTML em `./e2e/report` (`open: 'never'`), `use.baseURL: 'http://localhost:5174'`, `locale: 'pt-BR'`, `trace: 'on-first-retry'`; `webServer` com `command: 'pnpm dev --port 5174 --strictPort'`, `url: 'http://localhost:5174'`, `env: { VITE_APP_ENABLE_API_MOCKING: 'true' }`, `reuseExistingServer: !process.env.CI`. Um único projeto, `chromium`. **Sem** projeto `setup` e **sem** `storageState` (não há login). Não usar a 5173: um `pnpm dev` aberto sem mock seria reaproveitado. Conferir que o arquivo e `e2e/` estão cobertos por `apps/web/tsconfig.node.json`, pelo lint e excluídos do Vitest da web (feito em T2.1; ajustar se faltar). Instalar o navegador com `pnpm --filter web exec playwright install chromium` e registrar esse comando e o `pnpm test:e2e` no `README.md`.
  - Skills: e2e-testing
  - Complexidade: baixa

- [x] T3.2 — Arquitetura atualizada com as decisões da fundação
  - Arquivos: `docs/architecture.md` (alterar)
  - O que fazer: em pt_BR, sem reescrever o resto. §1 (stack): registrar Prisma 6 (gerador `prisma-client-js`, sem driver adapter; migrar para o 7 é tarefa isolada) e NestJS compilado com SWC no dev, no build e no Vitest (esbuild não emite `design:paramtypes`). §7 (testes): e2e roda na porta 5174 contra a API simulada por MSW, sem API nem Postgres; a API real é provada pelos testes de integração e de contrato, que exigem `docker compose up -d`. §8 (ambiente local/segurança): Postgres local em `trust` preso a `127.0.0.1:5433`, sem senha versionada (motivo: falso positivo do GitGuardian); proxy do Vite só de `/api`, e o de `/collab` entra com a fatia 005.
  - Skills: —
  - Complexidade: baixa

- [x] T3.3 — Testes da fase 3 (e2e da jornada "abrir o app")
  - Arquivos: `apps/web/e2e/tests/open-app.spec.ts` (criar)
  - O que fazer: localizar tudo por papel e texto em pt_BR. Helper local `expectNoSeriousA11yViolations(page)` com `AxeBuilder` de `@axe-core/playwright`, tags `wcag2a`, `wcag2aa`, `wcag21a`, `wcag21aa`, reprovando qualquer violação de impacto `critical` ou `serious`. Casos:
    - `opens the app with the sidebar, the welcome page and the Conectado indicator` — `/`: título do documento "Folioteca", `h1` "Boas-vindas à Folioteca", navegação "Navegação principal" com os quatro links, selo "Conectado"; axe.
    - `navigates through the four areas using only the keyboard` — só `Tab`/`Enter`: o primeiro `Tab` foca "Pular para o conteúdo"; percorre "Favoritos", "Meus documentos", "Espaços", "Lixeira", conferindo em cada uma a URL, o `h1`, o texto de vazio e `aria-current="page"`; axe numa área vazia.
    - `unknown address shows the not found page and goes back to the start` — `/nao-existe`: `h1` "Página não encontrada", sem a navegação principal; "Voltar para o início" leva a `/`; axe na 404.
    - `on a 360px screen the menu opens, closes with Escape and has no horizontal scroll` — viewport 360×740: botão "Abrir menu" com `aria-expanded="false"`; ao abrir vira "Fechar menu" e a navegação fica visível; axe com o menu aberto; `Esc` recolhe e o foco volta ao botão; `document.documentElement.scrollWidth <= clientWidth`; navegar por um link recolhe o painel.
    - `shows Sem conexão when the API fails and goes back to Conectado on its own` — `localStorage` `mock-error=health` antes de carregar: "Sem conexão" e "Tentando reconectar…"; remove a chave e espera "Conectado" sem recarregar nem clicar.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [x] CA3.1 — `pnpm test:e2e` na raiz passa (o Playwright sobe o próprio servidor; não precisa de API nem de Postgres), e continuam passando `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build`.
- [x] CA3.2 — `apps/web/playwright.config.ts` tem `webServer.command` = `pnpm dev --port 5174 --strictPort`, `VITE_APP_ENABLE_API_MOCKING` = `'true'` no `env` do `webServer`, `baseURL` `http://localhost:5174`, um único projeto `chromium`, nenhum projeto `setup` e nenhum `storageState`.
- [x] CA3.3 — `apps/web/e2e/tests/open-app.spec.ts` contém, pelos nomes, os cinco casos de T3.3, e `pnpm --filter web exec playwright test --list` os lista. O arquivo usa `AxeBuilder` com as tags `wcag2a`, `wcag2aa`, `wcag21a`, `wcag21aa`, chama a verificação em quatro paradas (início, área vazia, 404, 360 px com o menu aberto) e reprova em impacto `critical` ou `serious`. Não há `test.skip`, `test.only` nem `disableRules`.
- [x] CA3.4 — O caso de teclado não usa `click()` para navegar entre as áreas (só `keyboard.press`), e o caso de 360 px mede a ausência de rolagem horizontal comparando `scrollWidth` com `clientWidth`.
- [x] CA3.5 — `apps/web/e2e/**` e `playwright.config.ts` passam no `pnpm lint` e no `pnpm typecheck` (estão no `include` de `apps/web/tsconfig.node.json`) e não são coletados pelo Vitest (`npx vitest run --project web` não lista `open-app.spec.ts`). `apps/web/e2e/test-results/` e `apps/web/e2e/report/` estão no `.gitignore` e fora do `git status`.
- [x] CA3.6 — `docs/architecture.md` registra: Prisma 6 e SWC (§1); e2e na 5174 com API simulada (§7); Postgres local em `trust` no loopback e proxy de `/collab` só a partir da fatia 005 (§8). O `README.md` lista `pnpm --filter web exec playwright install chromium` e `pnpm test:e2e`.
- [x] CA3.7 — A fatia está utilizável de ponta a ponta: com `docker compose up -d` e `pnpm dev`, a web em 5173 consulta `/api/health` pelo proxy até a API em 3000 — comprovado, sem navegador aberto por pessoa, pela soma de CA1.11 (API real), CA2.14 (web com API simulada pelo mesmo contrato tipado) e CA3.1.

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
