# PLAN 088 — invitations-list

Branch: `feature/088-invitations-list`, que **sai de `feature/086-invitations-accept`** (entrega empilhada). **Toda** comparação de diff e de "arquivo intocado" deste plano é contra `feature/086-invitations-accept` — nunca contra `develop`, nunca contra `main`. Onde um critério diz `<base>`, leia `feature/086-invitations-accept`.

Fonte: `docs/features/088-invitations-list/spec.md` (D1…D7 são as decisões técnicas da SPEC e R1…R10 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; o banco de teste é `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom é da ferramenta e **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **Esta fatia não tem migration e não muda `schema.prisma`** (D3): a consulta usa o índice `@@index([organizationId, createdAt(sort: Desc)])` da migration `0009`. Ninguém — agente, teste ou critério — roda `prisma migrate diff`, `prisma migrate dev` nem `prisma migrate reset`; os únicos subcomandos permitidos são `pnpm --filter api exec prisma validate`, `pnpm --filter api exec prisma migrate status` e `pnpm --filter api exec prisma migrate deploy`. **Se o implementador concluir que precisa de migration, ele para a tarefa e relata** — não escreve migration nem roda `diff`/`dev`/`reset` por conta própria.
- Todo componente devolve `React.JSX.Element` (nunca o `JSX` global); `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case. Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Nenhum `eslint-disable` sem `-- <motivo>`. Nenhum segredo em variável `VITE_*`; nenhum token de convite em `localStorage`, `sessionStorage` ou `console`; nenhum literal com cara de senha, token ou hash em arquivo versionado (o GitGuardian reprova o push).
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado. Não contam arquivos só de tipos, arquivos com 0 de 0 linhas instrumentáveis, nem os já excluídos pelo `vitest.config.ts` da raiz (`**/types/**`, `**/generated/**`, `**/src/testing/**`, `**/test/**`, `**/*.config.*`). Nenhuma exclusão nova.
- **Toda** espera **dos casos novos** logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes da web pela constante `LAZY_TIMEOUT` declarada no arquivo (`findBy…(…, {}, LAZY_TIMEOUT)`, `waitFor(…, LAZY_TIMEOUT)`); no e2e pela constante `ROUTE_TIMEOUT` declarada no topo do spec. Nunca `setTimeout`/sleep fixo nem `waitForTimeout`. **Casos preexistentes não são alterados.**
- Testes de API são de integração contra o Postgres real (padrão `apps/api/test`); "teste passa" é medido pelo **exit code** de `npx vitest run --project api` / `--project web` (zero), nunca pela leitura da saída `--verbose`.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa. Cada fase termina com **um** commit Conventional Commits em inglês (`feat:`, `test:`, `docs:`), sem `--no-verify`.
- Nenhuma tarefa acrescenta escopo: sem revogar, sem reenviar, sem e-mail, sem coluna `revokedAt`, sem paginação, busca, filtro ou ordenação escolhida pela pessoa, sem mostrar convite aceito, vencido ou revogado.

Desvio em relação à tabela de arquivos da SPEC, sem mudar escopo: `docs/design.md` (receita "Pares rótulo–valor no item de lista") sai da fase 3 e entra na **fase 2** (T2.4) — a skill `interface-design` manda a receita nova entrar na mesma entrega da tela que a usa. `docs/roadmap.md` (item 088 concluído; dívidas que virarem item) **não** é tarefa nem critério de fase: a orquestração o registra no encerramento.

## Fase 1 — API: contrato `GET /invitations`, `list` no serviço e a rota que nasce protegida

Caminhos relativos à raiz do repositório.

- [x] T1.1 — Contrato primeiro: a operação `listInvitations` e o schema `Invitation` sem token
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D1, R1, R5, R9):
    - No caminho **`/invitations` que já existe** (sem prefixo `/api`, como todos os caminhos do arquivo), acrescentar a operação `get` ao lado do `post` da 085: `operationId: listInvitations`, tag `invitations`, **sem parâmetro nenhum** (sem paginação, sem filtro, sem `status`).
    - Resposta **200**: `InvitationsResponse` = `{ data: Invitation[] }`, com o schema **novo**:
      ```yaml
      Invitation:
        type: object
        properties:
          id: { type: string }
          email: { type: string, format: email }
          createdAt: { type: string, format: date-time }
          expiresAt: { type: string, format: date-time }
        required: [id, email, createdAt, expiresAt]
      ```
      `Invitation` e `CreatedInvitation` (da 085) seguem **dois schemas distintos**, sem `allOf` entre eles: um campo `token` opcional num schema compartilhado faria o tipo da web mentir sobre quando o token existe.
    - A `description` da operação, em pt_BR, registra as regras observáveis: só a administração; devolve **apenas** convites pendentes (nem aceitos, nem vencidos); ordem do mais recente para o mais antigo; o token **nunca** sai aqui, porque o servidor guarda só o `sha256`.
    - Erros: **401** e **403** com o schema `Error` já existente. **Não** existe 400 (não há entrada) nem 404 (lista vazia é `{ data: [] }` com 200).
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`; o `.d.ts` nunca é editado à mão. `apps/api/test/generated-types.test.ts` e `apps/web/src/types/api.ts` não mudam.
  - Skills: security
  - Complexidade: baixa

- [x] T1.2 — `InvitationsService.list` (o filtro de "pendente" e o `select` explícito) e o `@Get()` que herda os guards
  - Arquivos: `apps/api/src/invitations/invitations.service.ts` (alterar); `apps/api/src/invitations/invitations.controller.ts` (alterar)
  - O que fazer (D2, D3, R1, R3, R4, R5, R9):
    - `invitations.service.ts`, método novo `list(organizationId: string): Promise<Invitation[]>`:
      ```ts
      const rows = await this.prisma.invitation.findMany({
        where: {
          organizationId,
          acceptedAt: null,
          expiresAt: { gt: new Date() },
        },
        orderBy: { createdAt: 'desc' },
        select: { id: true, email: true, createdAt: true, expiresAt: true },
      });
      ```
      O `select` é explícito e **não** inclui `tokenHash` nem `invitedById`: é a segunda tranca de R5 (a primeira é o schema de D1) e vale mesmo se alguém acrescentar campo ao contrato sem pensar. As datas saem em `toISOString()`, como `create` já faz.
    - `expiresAt: { gt: new Date() }` e **não** um `WHERE` com `now()` no banco: o Prisma manda o instante como parâmetro, o índice continua utilizável e o teste consegue inserir um convite vencido sem mexer no relógio do Postgres.
    - Comentário cruzado: "pendente" existe em dois lugares — `findPending` (em memória, sobre **uma** linha já carregada, para não dar pista de qual recusa ocorreu) e `list` (no `where`, porque a lista precisa que o banco filtre). Cada um aponta para o outro e registra que a 087 acrescenta `revokedAt` **nos dois**.
    - **Nenhuma migration e nenhuma mudança em `schema.prisma`**: a ordem `createdAt DESC` por organização é exatamente o índice da `0009`. Se o implementador achar que precisa de índice novo, **para e relata** em vez de criar migration.
    - `invitations.controller.ts`: `@Get()` no `InvitationsController`, que **já tem** `@UseGuards(SessionGuard, AdminGuard)` na classe — **nenhuma linha de guard é escrita nesta fatia**, e é essa a garantia de que R9 não depende de alguém lembrar. `@CurrentPerson() person` dá o `organizationId`; retorno `{ data }` no formato `InvitationsResponse`.
    - Sem `Logger` e sem `console.` no módulo fora de testes.
  - Skills: authorization, security
  - Complexidade: média

- [x] T1.3 — Testes da fase 1
  - Arquivos: `apps/api/src/invitations/__tests__/invitations.service.test.ts` (alterar); `apps/api/src/invitations/__tests__/invitations.integration.test.ts` (alterar); `apps/api/src/invitations/__tests__/invitations.contract.test.ts` (alterar)
  - O que fazer (D7): integração e contrato contra o Postgres real, sem mock do Prisma, `resetDatabase(prisma)` em `beforeEach`, instalação e sessões pelos ajudantes já usados nos testes de `invitations`; ajudante local `getInvitations(cookie?)` mandando `X-Requested-With: XMLHttpRequest`. `apps/api/test/**` **não muda**. Convite aceito e convite vencido são gravados direto pelo Prisma (ou aceitos pela rota pública), nunca mexendo no relógio. Nenhuma espera fixa; nenhum literal com cara de senha ou token. **Casos preexistentes não são renomeados nem alterados.**
    - `invitations.service.test.ts` (Prisma falso), casos novos: `list filters by organization, unaccepted and not expired`; `list orders by createdAt desc`; `list selects no tokenHash`; `list returns the dates as ISO strings`.
    - `invitations.integration.test.ts`, casos novos: `an admin sees only the pending invitations`; `an accepted invitation is not listed`; `an expired invitation is not listed`; `the invitations come from the newest to the oldest`; `with no pending invitation the list answers 200 with an empty data array`; `a non-admin answers 403`; `an anonymous request answers 401`; `the list body never contains the invitation token` — cria um convite pelo `POST /invitations`, guarda o `token` do 201 e afirma que `JSON.stringify(body)` **não contém** o token **nem** o `sha256` dele; `the list items expose exactly id, email, createdAt and expiresAt` — `Object.keys(body.data[0])` ordenado é exatamente `['createdAt', 'email', 'expiresAt', 'id']`.
    - `invitations.contract.test.ts`, casos novos: `GET invitations answers the documented 200`; `GET invitations answers the documented 401`; `GET invitations answers the documented 403`.
    - Fecha a fase com um commit, por exemplo `feat(api): list the pending invitations of the organization`.
  - Skills: unit-testing, integration-testing, security
  - Complexidade: média

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (sem aviso de peer no install; sem aviso de chunk grande no build; o aviso de desempenho do Vitest sobre jsdom não conta). A saída de `pnpm test` não tem `console.error` nem `console.warn` vazando, nem aviso do Nest ou do Prisma. `git diff feature/086-invitations-accept... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA1.2 — `packages/api-contract/openapi.yaml` tem, no caminho `/invitations` (sem prefixo `/api`, o mesmo que já tinha o `post`), a operação `get` com `operationId: listInvitations`, tag `invitations` e **nenhum** `parameters`. A resposta `200` referencia `InvitationsResponse`, cujo `data` é um array de `Invitation`; o schema `Invitation` tem **exatamente** as propriedades `id`, `email`, `createdAt` e `expiresAt`, todas em `required`, e **não** tem `token` nem `tokenHash` (lendo o bloco do schema). `Invitation` não usa `allOf` com `CreatedInvitation`. Existem as respostas `401` e `403` com o schema `Error` e **não** existem `400` nem `404` nessa operação. `rg -n "listInvitations" packages/api-contract/src/generated/openapi.d.ts` encontra a operação; `apps/api/test/generated-types.test.ts` passa sem ter sido alterado; `git diff feature/086-invitations-accept... -- apps/web/src/types/api.ts` é vazio.
- [x] CA1.3 — Nenhuma migration e nenhum `schema.prisma` nesta fatia: `git diff --stat feature/086-invitations-accept... -- apps/api/prisma` é vazio (as migrations `0001`–`0010` e o schema seguem idênticos). `pnpm --filter api exec prisma validate` passa e, com `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test`, `pnpm --filter api exec prisma migrate status` reporta nenhuma migration pendente. Nos arquivos **alterados por esta branch** não há chamada a `prisma migrate diff`, `dev` ou `reset`: `git diff feature/086-invitations-accept... -- apps/api apps/web packages | rg -n "migrate (diff|dev|reset)"` é vazio (`docs/` fica fora porque a documentação cita esses comandos justamente para proibi-los).
- [x] CA1.4 — `apps/api/src/invitations/invitations.service.ts` tem `list(organizationId: string)` chamando `this.prisma.invitation.findMany` com `where` contendo `organizationId`, `acceptedAt: null` e `expiresAt: { gt: <Date> }`, `orderBy: { createdAt: 'desc' }` e um `select` explícito com exatamente `id`, `email`, `createdAt` e `expiresAt`. O `select` **não** menciona `tokenHash` nem `invitedById` (lendo o método). Não há `WHERE` com `now()` do banco nem `$queryRaw` neste método. Há comentário em `list` e em `findPending` apontando um para o outro e dizendo que a 087 acrescenta `revokedAt` nos dois. `rg -n "Logger|console\." apps/api/src/invitations --glob '!**/__tests__/**'` é vazio.
- [x] CA1.5 — `apps/api/src/invitations/invitations.controller.ts` continua com `@UseGuards(SessionGuard, AdminGuard)` **na classe** e ganha **um** método com `@Get()` (sem argumento de caminho), que usa `@CurrentPerson()` para obter o `organizationId` e devolve `{ data }`. `git diff feature/086-invitations-accept... -- apps/api/src/invitations/invitations.controller.ts` **não** acrescenta nenhuma linha com `@UseGuards` (a rota nasce protegida por herança). `apps/api/src/invitations/public-invitations.controller.ts` não ganha rota nenhuma.
- [x] CA1.6 — Intocados nesta fase: `git diff --stat feature/086-invitations-accept... -- apps/api/prisma apps/api/src/auth apps/api/src/common apps/api/src/invitations/invitations.schema.ts apps/api/src/invitations/public-invitations.controller.ts apps/api/src/invitations/invitations.module.ts apps/api/src/app.module.ts apps/api/src/access apps/api/src/documents apps/api/src/org-units apps/api/src/installation apps/api/test apps/web vitest.config.ts eslint.config.js` é vazio.
- [x] CA1.7 — `npx vitest run --project api` termina com exit code 0 e existem, pelos nomes de T1.3: os 4 casos novos de `invitations.service.test.ts` (`list filters by organization, unaccepted and not expired`, `list orders by createdAt desc`, `list selects no tokenHash`, `list returns the dates as ISO strings`); os 9 casos novos de `invitations.integration.test.ts` (entre eles `an accepted invitation is not listed`, `an expired invitation is not listed`, `the invitations come from the newest to the oldest`, `a non-admin answers 403`, `an anonymous request answers 401`, `the list body never contains the invitation token` e `the list items expose exactly id, email, createdAt and expiresAt`); e os 3 casos novos de `invitations.contract.test.ts`. Conferir a presença dos nomes com `rg -n "^\s*(it|test)\(" apps/api/src/invitations/__tests__`. A integração usa o Postgres real: `rg -n "vi\.mock\(.*prisma" apps/api/src/invitations/__tests__/invitations.integration.test.ts apps/api/src/invitations/__tests__/invitations.contract.test.ts` é vazio. Todos os casos preexistentes desses três arquivos continuam listados e não foram renomeados. Nenhum `.only(` nem `.skip(`.
- [x] CA1.8 — Lendo `the list body never contains the invitation token` em `invitations.integration.test.ts`: o teste cria um convite pelo `POST /invitations`, guarda o `token` da resposta 201 e afirma, sobre o corpo da lista, que `JSON.stringify(body)` não contém esse token **nem** o `sha256` dele. Lendo `the list items expose exactly id, email, createdAt and expiresAt`: há uma asserção de igualdade sobre `Object.keys(body.data[0])` (ordenado) com exatamente `['createdAt', 'email', 'expiresAt', 'id']`. O 403 afirma a mensagem "Apenas a administração pode fazer isso." e o 401 afirma "Sessão não encontrada.".
- [x] CA1.9 — `rg -n "setTimeout\(|sleep\(" apps/api/src/invitations` é vazio; nenhum arquivo de `apps/api/src/invitations/**` contém senha literal nem cadeia de 20+ caracteres aleatórios servindo de token (as senhas vêm dos ajudantes/constantes de teste já existentes; os tokens vêm do corpo da resposta).
- [x] CA1.10 — Cobertura ≥ 80% de linhas para `apps/api/src/invitations/invitations.service.ts` e `apps/api/src/invitations/invitations.controller.ts`. A medição vale por `coverage/coverage-summary.json`, gerado na raiz com `npx vitest run --coverage --coverage.reporter=json-summary --coverage.reporter=text` **sem `--project`** (com filtro de projeto o `include` da raiz não casa e o relatório sai vazio): o reporter `text` do v8 **omite arquivos com 100%**, então ausência da tabela de texto não reprova — reprova só quem aparecer no `coverage-summary.json` com `lines.pct < 80` ou não aparecer em lugar nenhum.
- [x] CA1.11 — `git log feature/086-invitations-accept..HEAD --format=%s` mostra exatamente um commit desta fase, em inglês, no formato Conventional Commits (prefixo `feat`, `fix`, `test`, `docs`, `chore` ou `refactor` seguido de `:` ou `(escopo):`).

## Fase 2 — Web: a consulta, a lista na página "Convites", a invalidação ao criar e a API simulada

Caminhos relativos à raiz do repositório.

- [x] T2.1 — A consulta `GET /invitations` e a invalidação depois de criar
  - Arquivos: `apps/web/src/features/invitations/api/get-invitations.ts` (criar); `apps/web/src/features/invitations/api/create-invitation.ts` (alterar)
  - O que fazer (D4, R6):
    - `get-invitations.ts`, no molde exato de `features/org-units/api/get-org-units.ts`: `getInvitations(): Promise<InvitationsResponse>` chamando `api.get('/invitations')`; `getInvitationsQueryOptions()` com `queryKey: ['invitations']`; `useInvitations({ queryConfig })`. **Sem** `silentError`: uma falha de carga é notificada pelo interceptor global, como em toda lista do app.
    - Os tipos `Invitation` e `InvitationsResponse` são aliasados **no próprio arquivo** (`components['schemas'][…]` do contrato gerado), como `create-invitation.ts` já faz com `CreatedInvitation`. `apps/web/src/types/api.ts` continua intocado.
    - `create-invitation.ts`: `useCreateInvitation` ganha, no `onSuccess`, `queryClient.invalidateQueries({ queryKey: getInvitationsQueryOptions().queryKey })` **antes** de chamar o `mutationConfig?.onSuccess` de quem usa o hook. O comentário que reservava o lugar ("Slice 088 adds the invalidateQueries together with the GET /invitations") é **substituído** por um que explica R6: recarregar é o único jeito de a lista bater com o banco, porque o servidor pode ter substituído um convite pendente do mesmo e-mail. O `silentError` do `post` continua (o 409 segue respondido no campo).
    - Nada de `setQueryData` para a lista e nada de `refetchInterval`.
  - Skills: api-requests, client-state
  - Complexidade: baixa

- [x] T2.2 — A API simulada: `GET /invitations` com o mesmo filtro e a mesma ordem do servidor
  - Arquivos: `apps/web/src/testing/mocks/handlers/invitations.ts` (alterar); `apps/web/src/testing/mocks/utils.ts` (alterar)
  - O que fazer (D6, R3, R4, R5, R9):
    - `handlers/invitations.ts`: handler novo `http.get(\`${env.API_URL}/invitations\`)`, colocado **antes** dos handlers de `/invitations/:token` no array (o MSW casa na ordem; não há colisão, mas a ordem deixa a leitura óbvia). Mesmo preâmbulo do `post` que já está lá: `networkDelay()`, `devOverride('invitations')`, 401 sem cookie ou sem instalação, 403 para não-admin.
    - O corpo repete **as mesmas duas regras** do servidor sobre `getDb().invitations`: `acceptedAt === null` e `new Date(expiresAt).getTime() > Date.now()`; depois ordem **decrescente** por `createdAt` usando uma cópia (`[...invitations].sort(…)`) — nunca `sort` no array do estado.
    - O mapeamento monta `{ id, email, createdAt, expiresAt }` **campo a campo**: `token` e `acceptedAt` **não** são espalhados com `...`, para o banco falso não vazar o token nem por descuido.
    - Um predicado interno `isPending(invitation: MockInvitation): boolean` com as duas checagens passa a ser usado pelo handler da lista **e** pela `findAvailableInvitation` que já existe (ela continua devolvendo `undefined` para "recusado", que é o contrato do 404 público, e **não** é reaproveitada como filtro da lista).
    - `db.ts` **não muda**: `MockInvitation` já tem `acceptedAt`, `addInvitation` já substitui por e-mail e `acceptInvitation` já marca `acceptedAt`. `seedSampleInvitations` **não** ganha convite vencido nem aceito (isso mudaria o estado que as jornadas da 086 esperam); casos de borda são semeados pelo próprio teste, com handler próprio.
    - `utils.ts`: **só** o comentário das chaves de desenvolvimento — `mock-invitations=sample` agora também popula a lista da página; `mock-error=invitations` já dá o 500 do estado de erro e `mock-role=member` já dá o 403.
  - Skills: api-mocking
  - Complexidade: média

- [x] T2.3 — O componente `InvitationsList` e a seção nova na página "Convites"
  - Arquivos: `apps/web/src/features/invitations/components/invitations-list.tsx` (criar); `apps/web/src/app/routes/app/admin/invitations.tsx` (alterar)
  - O que fazer (D5, R1, R2, R5, R7, R8, R10). A aparência segue `docs/design.md` e o piso da skill `interface-design`; nenhuma classe é ditada por esta tarefa, exceto a receita nova descrita em T2.4.
    - `invitations-list.tsx`, com a mesma divisão de `OrgUnitsTree`: o componente exportado `InvitationsList` cuida dos quatro estados da consulta (`useInvitations`) e um `LoadedInvitationsList` interno só recebe `Invitation[]`.
    - Estados, copiados de `OrgUnitsTree`, sem inventar variação:
      - carregando (`isPending || (isError && isFetching)`, porque o retry volta a "pending" no TanStack Query v5): receita "Carregando", `role="status"`, texto "Carregando convites…";
      - vazio: receita "Vazio", texto "Nenhum convite pendente.";
      - erro: receita "Erro", `role="alert"`, texto "Não foi possível carregar os convites." e um botão "Tentar novamente" que chama `refetch`;
      - com dados: a lista.
    - A lista é `<ul aria-label="Convites pendentes">` na receita "Lista", **não** `<table>`. Cada `<li>` tem `key` do `id` do convite e mostra: à esquerda o e-mail convidado, num bloco que quebra palavra longa (e-mail não tem espaço para quebrar sozinho; nunca `truncate`, porque o e-mail é a única coisa que identifica a linha); à direita (abaixo, a 360px) um `<dl>` com dois pares rótulo–valor, `<dt>` "Criado em" e `<dt>` "Expira em", cada `<dd>` com a data. Em leitura linear o `<dt>` vem antes do `<dd>`, então o par sai completo sem modo de navegação por tabela.
    - Datas por `formatDateTime` de `@/utils/format-date-time.ts` (já existe, `Intl.DateTimeFormat('pt-BR', { dateStyle: 'short', timeStyle: 'short' })`, e já trata data inválida devolvendo string vazia). **Nenhum utilitário novo de data e nenhum tempo relativo.** Cada data vai num `<time dateTime={invitation.createdAt}>` com o ISO cru no atributo e o texto em pt_BR visível.
    - A ordem na tela é **exatamente** a que a API mandou: o componente não reordena nada.
    - **Nada de link nem de token na tela** (R5): o componente não recebe token, não importa `buildInvitationLink` e não renderiza `<a>` nenhum. Nenhum selo de status (todas as linhas são pendentes por definição; o selo volta na 087).
    - `invitations.tsx`: abaixo do bloco do link do convite criado (e **nunca** antes dele, para não mudar nenhuma contagem de `Tab` dos e2e da 085), acrescentar: `<h2>` "Convites pendentes" (receita "Subtítulo"); texto de apoio "O link de cada convite aparece uma única vez, quando ele é criado, e não pode ser mostrado de novo. Para gerar um link novo, convide o mesmo e-mail outra vez."; e `<InvitationsList />`. Nada acima do bloco do link muda.
  - Skills: interface-design, component-robustness, error-handling
  - Complexidade: média

- [x] T2.4 — A receita "Pares rótulo–valor no item de lista" no `docs/design.md`
  - Arquivos: `docs/design.md` (alterar)
  - O que fazer (Interface da SPEC): na tabela "Padrões acrescentados pelas entregas", linha com Fatia = 088 e Padrão = "Pares rótulo–valor no item de lista": dentro do `<li>` da receita "Lista", um `<dl className="flex shrink-0 flex-wrap gap-x-4 gap-y-1">`; cada par num `<div>`; `<dt className="text-xs text-gray-600">` com o rótulo visível e `<dd>` com a receita "Data em lista"; o `<li>` ganha `flex-wrap` e o bloco principal `min-w-0 break-words`. Nenhuma outra receita é reescrita.
  - Skills: interface-design
  - Complexidade: baixa

- [x] T2.5 — Testes da fase 2
  - Arquivos: `apps/web/src/features/invitations/api/__tests__/get-invitations.test.tsx` (criar); `apps/web/src/features/invitations/api/__tests__/create-invitation.test.tsx` (alterar); `apps/web/src/features/invitations/components/__tests__/invitations-list.test.tsx` (criar); `apps/web/src/app/routes/app/admin/__tests__/invitations.test.tsx` (alterar)
  - O que fazer (D7): banco falso pelos ajudantes já usados (`seedInstalled(...)`); interação por `userEvent`; localizar por papel e nome acessível em pt_BR; senhas vindas das constantes já existentes (`MOCK_PASSWORD` e afins), **nunca** literais. `LAZY_TIMEOUT` declarada em `invitations-list.test.tsx` e em `invitations.test.tsx` e passada em **toda** espera dos casos novos logo após rota ou chunk `lazy`. Fuso fixado no teste para as datas serem determinísticas. Nenhuma espera fixa; nenhum `console.error`/`console.warn`/aviso de `act(...)` vazando. `src/testing/setup-tests.ts` **não** muda; **casos preexistentes não são alterados nem renomeados**.
    - `get-invitations.test.tsx`: `sends GET to /invitations and returns data`; `an empty list resolves with an empty data array`; `a failure rejects with the global notification` (sem `silentError`).
    - `create-invitation.test.tsx`, caso novo: `a successful POST invalidates the invitations query key` (espiando `invalidateQueries` do `QueryClient` do teste e conferindo a chave `['invitations']`).
    - `invitations-list.test.tsx`: `shows the loading state with role status`; `shows the error state with role alert and retries on Tentar novamente` (handler contador: 2 chamadas); `shows the empty state`; `shows the invited e-mail with Criado em and Expira em in pt-BR`; `keeps the order the API sent`; `renders no link and no token-like text`; `an eighty character e-mail wraps instead of overflowing`.
    - `invitations.test.tsx` (rota), casos novos: `an admin sees the two seeded invitations, newest first`; `creating an invitation puts the new one on top without a reload`; `a pending invitation for the same e-mail is not duplicated`; `a non-admin is sent home without requesting GET /invitations`.
    - Fecha a fase com um commit, por exemplo `feat(web): list the pending invitations on the admin page`.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta). A saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)` vazando. `git diff feature/086-invitations-accept... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA2.2 — `apps/web/src/features/invitations/api/get-invitations.ts` existe (antes do commit, `git status --porcelain -uall apps/web/src/features/invitations/api/get-invitations.ts` lista o arquivo; depois, `git diff --stat feature/086-invitations-accept... -- apps/web/src/features/invitations/api/get-invitations.ts` lista o arquivo) e exporta `getInvitations()` chamando `api.get('/invitations')`, `getInvitationsQueryOptions()` com `queryKey: ['invitations']` e `useInvitations`. O arquivo **não** passa `silentError` e aliasa `Invitation` e `InvitationsResponse` a partir do contrato gerado, no próprio arquivo. `git diff feature/086-invitations-accept... -- apps/web/src/types/api.ts` é vazio.
- [x] CA2.3 — `apps/web/src/features/invitations/api/create-invitation.ts` chama `queryClient.invalidateQueries({ queryKey: getInvitationsQueryOptions().queryKey })` dentro do `onSuccess`, **antes** da chamada a `mutationConfig?.onSuccess`, e o comentário que anunciava a fatia 088 foi substituído por um que explica por que a lista é recarregada em vez de escrita no cache. `rg -n "setQueryData|refetchInterval" apps/web/src/features/invitations/api/create-invitation.ts apps/web/src/features/invitations/api/get-invitations.ts` é vazio. O `silentError` do `post` continua presente no arquivo.
- [x] CA2.4 — `apps/web/src/features/invitations/components/invitations-list.tsx` existe e exporta `InvitationsList`, que devolve `React.JSX.Element` e usa `useInvitations`. Lendo o arquivo: o estado de carregando tem `role="status"` e o texto "Carregando convites…"; o vazio tem "Nenhum convite pendente."; o erro tem `role="alert"`, o texto "Não foi possível carregar os convites." e um botão "Tentar novamente" que chama `refetch`; a condição do carregando é `isPending || (isError && isFetching)`. Com dados, há um `<ul aria-label="Convites pendentes">`, cada `<li>` com `key` do `id`, um `<dl>` com `<dt>` "Criado em" e `<dt>` "Expira em", e as datas passando por `formatDateTime` de `@/utils/format-date-time` dentro de `<time dateTime={…}>` com o ISO cru. O componente **não** ordena (`rg -n "\.sort\(|reverse\(" apps/web/src/features/invitations/components/invitations-list.tsx` é vazio) e **não** renderiza link nem token (`rg -n "<a |buildInvitationLink|token" apps/web/src/features/invitations/components/invitations-list.tsx` é vazio). Não existe `<table>` no arquivo.
- [x] CA2.5 — `apps/web/src/app/routes/app/admin/invitations.tsx` tem, **depois** do bloco do link do convite criado, um `<h2>` "Convites pendentes", o texto "O link de cada convite aparece uma única vez, quando ele é criado, e não pode ser mostrado de novo. Para gerar um link novo, convide o mesmo e-mail outra vez." e `<InvitationsList />`. `git diff feature/086-invitations-accept... -- apps/web/src/app/routes/app/admin/invitations.tsx` **não** altera nenhuma linha acima do bloco do link (só acrescenta abaixo dele, mais o import). A página continua com **um único** `<h1>` ("Convites").
- [x] CA2.6 — Acabamento (piso de `interface-design`, verificável lendo o código): o `<h2>` usa as classes de "Subtítulo (`h2`)" do `docs/design.md` e o texto de apoio as de "Texto de apoio"; os quatro estados ocupam o mesmo lugar do conteúdo e cada um é estilizado; o estado de erro tem borda ou fundo de destaque; o botão "Tentar novamente" é o componente `Button` de `@/components/ui/button/button` com `type` explícito (`rg -n "<button" apps/web/src/features/invitations/components/invitations-list.tsx apps/web/src/app/routes/app/admin/invitations.tsx` é vazio); cada `<li>` separa a informação principal (e-mail) das datas, com o `<li>` em `flex-wrap` e o bloco do e-mail em `min-w-0 break-words` (nunca `truncate`), para funcionar a 360px sem rolagem horizontal; os rótulos das datas ficam em tom mais suave que o e-mail. `rg -n "forwardRef|style=\{\{|: JSX\.|!important" apps/web/src/features/invitations apps/web/src/app/routes/app/admin/invitations.tsx` é vazio e todo componente declara retorno `React.JSX.Element`.
- [x] CA2.7 — `docs/design.md` tem, em "Padrões acrescentados pelas entregas", a linha da fatia 088 "Pares rótulo–valor no item de lista" com `<dl className="flex shrink-0 flex-wrap gap-x-4 gap-y-1">`, `<dt className="text-xs text-gray-600">` e a menção de que o `<li>` ganha `flex-wrap` e o bloco principal `min-w-0 break-words`; `invitations-list.tsx` usa essa mesma receita. Nenhuma outra linha da tabela foi reescrita.
- [x] CA2.8 — API simulada: `apps/web/src/testing/mocks/handlers/invitations.ts` tem um `http.get` em `${env.API_URL}/invitations` declarado **antes** dos handlers de `/invitations/:token` no array, com `networkDelay()`, `devOverride('invitations')`, 401 sem cookie ou sem instalação e 403 para não-admin. O corpo filtra por `acceptedAt === null` **e** `expiresAt` no futuro, ordena por `createdAt` decrescente sobre uma **cópia** do array (`[...` antes do `.sort(`) e monta cada item campo a campo com `id`, `email`, `createdAt` e `expiresAt` — não há espalhamento (`...invitation`) na montagem da resposta. Existe o predicado `isPending` no arquivo, usado pelo handler da lista **e** por `findAvailableInvitation`. `git diff feature/086-invitations-accept... -- apps/web/src/testing/mocks/db.ts` é vazio; `git diff feature/086-invitations-accept... -- apps/web/src/testing/mocks/utils.ts` só tem linhas iniciadas por `//` além do contexto.
- [x] CA2.9 — Intocados nesta fase (base é o commit da fase 1, porque `packages` e `apps/api` mudaram legitimamente lá): `git diff --stat <commit da fase 1> -- apps/web/src/lib apps/web/src/types apps/web/src/components apps/web/src/config apps/web/src/utils apps/web/src/features/auth apps/web/src/features/connection apps/web/src/features/installation apps/web/src/features/documents apps/web/src/features/org-units apps/web/src/testing/setup-tests.ts apps/web/src/testing/mocks/db.ts apps/web/src/features/invitations/components/create-invitation-form.tsx apps/web/src/features/invitations/components/invitation-link.tsx apps/web/src/features/invitations/components/accept-invitation-form.tsx apps/web/src/features/invitations/api/get-invitation.ts apps/web/src/features/invitations/api/accept-invitation.ts apps/web/src/features/invitations/utils apps/web/e2e vitest.config.ts eslint.config.js packages apps/api` é vazio. A feature não importa de outra feature nem de `app/`: `rg -n "@/features/(?!invitations)|@/app/" -P apps/web/src/features/invitations --glob '!**/__tests__/**'` é vazio.
- [x] CA2.10 — `npx vitest run --project web` termina com exit code 0 e existem, pelos nomes de T2.5: os 3 casos de `get-invitations.test.tsx`; o caso novo `a successful POST invalidates the invitations query key` em `create-invitation.test.tsx`; os 7 de `invitations-list.test.tsx` (entre eles `shows the error state with role alert and retries on Tentar novamente`, `keeps the order the API sent`, `renders no link and no token-like text` e `an eighty character e-mail wraps instead of overflowing`); e os 4 casos novos de `app/routes/app/admin/__tests__/invitations.test.tsx` (entre eles `creating an invitation puts the new one on top without a reload`, `a pending invitation for the same e-mail is not duplicated` e `a non-admin is sent home without requesting GET /invitations`). Conferir os nomes com `rg -n "^\s*(it|test)\(" apps/web/src/features/invitations apps/web/src/app/routes/app/admin/__tests__/invitations.test.tsx`. Os casos preexistentes de `create-invitation.test.tsx`, `create-invitation-form.test.tsx`, `invitation-link.test.tsx` e `invitations.test.tsx` continuam listados e não foram renomeados. Nenhum `.only(` nem `.skip(`.
- [x] CA2.11 — Esperas: `invitations-list.test.tsx` e `invitations.test.tsx` declaram `LAZY_TIMEOUT` e a passam em **toda** espera dos casos novos desta fase logo após abrir ou trocar de rota (`findBy…` e `waitFor`); `rg -n "setTimeout\(|sleep\(" apps/web/src/features/invitations apps/web/src/app/routes/app/admin/__tests__/invitations.test.tsx` é vazio. Nenhum arquivo de teste desta fase contém senha literal: a senha usada vem de uma constante de teste já existente (`rg -n "MOCK_PASSWORD|VALID_PASSWORD" apps/web/src/app/routes/app/admin/__tests__/invitations.test.tsx` encontra o uso da constante, se o arquivo precisar de senha). Nenhum `eslint-disable` sem motivo: `rg -n "eslint-disable" apps/web/src apps/api/src | rg -v -- "--"` é vazio.
- [x] CA2.12 — Cobertura ≥ 80% de linhas para `apps/web/src/features/invitations/api/get-invitations.ts`, `apps/web/src/features/invitations/api/create-invitation.ts`, `apps/web/src/features/invitations/components/invitations-list.tsx` e `apps/web/src/app/routes/app/admin/invitations.tsx`. A medição vale por `coverage/coverage-summary.json`, gerado na raiz com `npx vitest run --coverage --coverage.reporter=json-summary --coverage.reporter=text` **sem `--project`** (com filtro de projeto o `include` da raiz não casa e o relatório sai vazio): o reporter `text` do v8 omite arquivos com 100%, então ausência da tabela de texto não reprova — reprova só quem aparecer no `coverage-summary.json` com `lines.pct < 80` ou não aparecer em lugar nenhum.
- [x] CA2.13 — `git log feature/086-invitations-accept..HEAD --format=%s` mostra exatamente dois commits (um por fase), ambos em inglês e no formato Conventional Commits.

## Fase 3 — e2e da jornada da administração e documentação: a fatia utilizável de ponta a ponta

Caminhos relativos à raiz do repositório.

- [x] T3.1 — Documentação: a entrega `invitations-list` na arquitetura
  - Arquivos: `docs/architecture.md` (alterar)
  - O que fazer (D1, D2, D3), em pt_BR, sem reescrever o resto: na §6, parágrafo novo **"Entrega `invitations-list` (fatia 088)"** dizendo: `GET /invitations` entra no `InvitationsController` e **herda** `SessionGuard` + `AdminGuard` da classe, sem nenhuma linha de guard nova; "pendente" é `acceptedAt IS NULL` **e** `expiresAt > agora`, decidido no banco (o relógio do navegador é do usuário), com ordem `createdAt DESC` atendida pelo índice da `0009` — **nenhuma migration nova nesta fatia**; o schema `Invitation` **não tem** `token`, porque o servidor guarda só o `sha256` e um campo opcional faria o tipo mentir, por isso `Invitation` e `CreatedInvitation` são schemas distintos; o `select` do Prisma é explícito e não traz `tokenHash`, como segunda tranca; a mesma definição de "pendente" existe **duas vezes** (`findPending`, em memória, e `list`, no `where`) e a 087 precisa acrescentar `revokedAt` nas duas.
  - Skills: —
  - Complexidade: baixa

- [x] T3.2 — Testes da fase 3 (e2e: a administração vê quem está pendente)
  - Arquivos: `apps/web/e2e/tests/invitations-list.spec.ts` (criar)
  - O que fazer (D7): contra a API simulada; localizar por papel e nome acessível em pt_BR. Estado inicial por `page.addInitScript` gravando as chaves `mock-installation=signed-in` e `mock-invitations=sample`. No topo, `const ROUTE_TIMEOUT = { timeout: 10_000 }`; **toda** espera logo após mudança de rota (`toHaveURL`, primeira visibilidade da tela nova) o recebe; nenhum `waitForTimeout`. Depois de cada `Tab`/seta e antes do `Enter`, `await expect(locator).toBeFocused()`. Nenhum literal com cara de senha ou token no arquivo. `apps/web/e2e/a11y.ts` e os outros specs não são tocados; nenhuma regra do axe é desativada.
    - Caso `an admin opens Convites and sees who is still pending`: entra na página pela barra lateral ("Administração" → "Convites"), espera a lista, confere o e-mail do convite semeado e os rótulos "Criado em" e "Expira em" com data e hora visíveis, e roda `expectNoSeriousA11yViolations(page)` com a lista na tela.
    - Caso `a new invitation shows up on top without a reload`: no mesmo estado, cria um convite com um e-mail novo pelo formulário da página e confere, **sem recarregar**, que o novo é o **primeiro** item da lista e que o total de itens aumentou em um.
    - Os e2e da 064, 065, 066, 085 e 086 **não** mudam: a lista entra depois do bloco do link, então nenhuma contagem de `Tab` existente muda. Se algum deles quebrar, o conserto é no código de produção, não no spec antigo.
    - Fecha a fase com um commit, por exemplo `test(e2e): cover the pending invitations list and document the slice`.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [x] CA3.1 — `pnpm test:e2e` na raiz termina com exit code 0 (o Playwright sobe o próprio servidor; não precisa de API nem de Postgres), e continuam terminando com exit code 0 e **sem aviso**, com `docker compose up -d` rodando e o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta).
- [x] CA3.2 — `apps/web/e2e/tests/invitations-list.spec.ts` contém os casos `an admin opens Convites and sees who is still pending` e `a new invitation shows up on top without a reload`, e `pnpm --filter web exec playwright test --list` os lista junto com todos os casos já existentes de `invitations-create.spec.ts`, `invitations-accept.spec.ts`, `org-units-view.spec.ts`, `org-units-create-rename.spec.ts` e `org-units-delete.spec.ts` (nenhum removido nem renomeado). `rg -n "test\.skip|test\.only|waitForTimeout" apps/web/e2e` é vazio.
- [x] CA3.3 — Lendo o primeiro caso: a página é alcançada **navegando pela barra lateral** ("Administração" → "Convites"), não por URL direta; o e-mail do convite semeado é conferido na tela, junto com os rótulos "Criado em" e "Expira em"; `expectNoSeriousA11yViolations` é chamado com a lista já visível. Lendo o segundo: um convite é criado pelo formulário da própria página e há asserção de que o e-mail novo é o **primeiro** item da lista e de que a contagem de itens aumentou em um, **sem** nenhum `page.reload()` no caso.
- [x] CA3.4 — Esperas e segredos do e2e: o spec declara `ROUTE_TIMEOUT = { timeout: 10_000 }` no topo e **toda** asserção logo após mudança de rota (`toHaveURL` e a primeira visibilidade da tela nova) o recebe; entre cada `Tab`/seta e o `Enter` seguinte há `toBeFocused()`; não há espera fixa; não há valor literal de senha nem de token no arquivo.
- [x] CA3.5 — `git status --porcelain -uall -- apps/web/e2e` (antes do commit) ou `git diff --stat feature/086-invitations-accept... -- apps/web/e2e` (depois) lista **só** `apps/web/e2e/tests/invitations-list.spec.ts`; o spec não desativa regra do axe (`rg -n "disableRules" apps/web/e2e/tests/invitations-list.spec.ts` é vazio); os arquivos de `apps/web/e2e/**` passam no `pnpm lint` e no `pnpm typecheck`.
- [x] CA3.6 — `docs/architecture.md` tem, na §6, o parágrafo "Entrega `invitations-list` (fatia 088)" dizendo: `GET /invitations` no `InvitationsController`, herdando `SessionGuard` + `AdminGuard` da classe; "pendente" é `acceptedAt IS NULL` e `expiresAt > agora`, decidido no banco, com ordem `createdAt DESC` pelo índice da `0009` e **nenhuma migration nova**; o schema `Invitation` sem `token`, distinto de `CreatedInvitation`; o `select` explícito sem `tokenHash`; e o aviso de que a definição de "pendente" existe em dois lugares (`findPending` e `list`) e a 087 precisa tocar os dois.
- [x] CA3.7 — A fatia está utilizável de ponta a ponta sem navegador aberto por pessoa: `npx vitest run --project api` (exit code 0) inclui `an admin sees only the pending invitations`, `an accepted invitation is not listed`, `an expired invitation is not listed`, `the list body never contains the invitation token`, `a non-admin answers 403` e `an anonymous request answers 401` em `apps/api/src/invitations/__tests__/invitations.integration.test.ts`; `npx vitest run --project web` (exit code 0) inclui `creating an invitation puts the new one on top without a reload` em `apps/web/src/app/routes/app/admin/__tests__/invitations.test.tsx`; e `pnpm test:e2e` (exit code 0) inclui as duas jornadas de `invitations-list.spec.ts`.
- [x] CA3.8 — `git log feature/086-invitations-accept..HEAD --format=%s` mostra exatamente três commits (um por fase), todos em inglês e no formato Conventional Commits; `git diff --stat feature/086-invitations-accept... -- docs/roadmap.md` pode ser não vazio só pelo registro da orquestração.

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
