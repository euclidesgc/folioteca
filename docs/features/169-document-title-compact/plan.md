# PLAN 169 — document-title-compact

Branch: `feature/169-document-title-compact`

Fonte: `docs/features/169-document-title-compact/spec.md` (D1…D8 são as decisões técnicas da SPEC e R1…R10 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

O título do documento sai de baixo da barra e vira um campo compacto na própria "Linha de ações do documento": título à esquerda, ações ("Compartilhar", "Mover para a lixeira", favoritos) à direita. Enter ou sair do campo salva; Esc devolve o nome anterior; vazio volta ao nome anterior sem pedir nada ao servidor; falha mantém o texto digitado. Quem só pode ver (e documento na lixeira) vê um `<h1>` de texto no mesmo lugar. Documento novo nasce como "documento-sem-titulo-N", com N o menor inteiro livre entre os documentos do dono (a lixeira conta), calculado sob um lock consultivo por dono, para que duas criações simultâneas recebam números diferentes. Documentos antigos "Sem título" não mudam.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; banco de teste `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`.

**Critérios sobre o disco.** Os critérios são conferidos **antes** do commit da fase, sobre os arquivos em disco (commitados ou não). Nenhum critério depende de `git diff` contra a base. Critério de ausência (`rg` vazio) vale só para os arquivos criados ou alterados na própria fase, listados no próprio critério, e usa padrão que não casa com comentário nem com atributo parecido (ex.: `disabled` casa dentro de `aria-disabled`; por isso usa `rg -P` com lookbehind). O commit de cada fase é feito pela orquestração depois da revisão; nenhum agente de fase commita. Se `pnpm test` ou `pnpm test:e2e` falhar num teste **antigo** e sem relação com a fase, o comando pode ser repetido **uma** vez; a segunda execução vale.

**Índice e migration.** A SPEC deixou em aberto se há índice em `Document.ownerId`. Conferido no `apps/api/prisma/schema.prisma`: `model Document` já tem `@@index([ownerId, updatedAt(sort: Desc)])` e `@@index([ownerId, trashedAt(sort: Desc)])`, ambos com `ownerId` na frente, que servem à leitura `where ownerId = … and title like 'documento-sem-titulo-%'`. **Não há migration nesta fatia.**

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **Sem migration**: `apps/api/prisma/schema.prisma` e `apps/api/prisma/migrations/**` não mudam. Nenhum subcomando de Prisma além de `prisma generate`; **nunca** `migrate diff`, `migrate dev` ou `migrate reset`. Nenhum update em dados existentes (R9).
- **Nenhum teste remove, desativa ou recria restrição do banco** (`DROP CONSTRAINT`, `ALTER TABLE … DISABLE`, `session_replication_role`).
- SQL cru só por `tx.$executeRaw` com template tagged (parâmetro), nunca `$executeRawUnsafe` nem concatenação de string.
- O arquivo `packages/api-contract/src/generated/openapi.d.ts` só muda por `pnpm --filter @folioteca/api-contract generate`; nunca editado à mão.
- **Sem Prettier reformatando arquivo existente**: só as linhas necessárias mudam.
- Todo componente devolve `React.JSX.Element` (ou `React.JSX.Element | null`), nunca o `JSX` global; `ref` é prop comum (sem `forwardRef`); todo botão nosso é o componente `Button` de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case; nenhum import entre features. Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Nenhum `disabled` nativo no campo do título: durante o envio, `aria-disabled="true"` + `readOnly` (D7).
- Nenhum `eslint-disable` sem `-- <motivo>` na própria linha; nenhuma regra desativada para fazer teste passar; nenhum `any` sem comentário.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado, medida **só** por `coverage/coverage-summary.json`, gerado **na raiz** com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`, **sem `--project`** e **sem filtro de caminho**. Não contam arquivos só de tipos nem os já excluídos pelo `vitest.config.ts` da raiz. Nenhuma exclusão nova.
- Nos e2e: API simulada, estado inicial por `page.addInitScript`; `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo do spec, e a **primeira** asserção após cada mudança de rota usa `ROUTE_TIMEOUT` (uma seguinte com timeout maior é aceita); `toBeFocused()` antes de cada `Enter`/`Escape`/`Tab`/`Space`; `expectNoSeriousA11yViolations(page)`; nunca `setTimeout`/sleep fixo nem `waitForTimeout`.
- Testes de API de integração e contrato rodam contra o Postgres real (sem mock do Prisma). "Teste passa" é medido pelo **exit code** (zero), nunca pela saída `--verbose`.
- Testes existentes afetados por esta fatia são ajustados **sem trocar o nome** e registrados na seção "Desvios" no fim do plano (DVn).
- Intocados de propósito: `apps/api/prisma/**`, `apps/api/src/access/**`, `apps/web/src/components/ui/**`, `apps/web/src/lib/api-client.ts`, `apps/web/e2e/a11y.ts`, e todo spec e2e fora dos listados em T3.2.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa.

## Fase 1 — API: documento novo nasce "documento-sem-titulo-N"

Caminhos relativos à raiz do repositório. Ao fim da fase, `POST /documents` devolve `title: "documento-sem-titulo-1"` na primeira criação do dono, o menor número livre nas seguintes, e duas criações simultâneas do mesmo dono dão certo com números diferentes.

- [x] T1.1 — Função pura do nome padrão e o contrato
  - Arquivos: `apps/api/src/documents/default-title.ts` (criar); `apps/api/src/documents/documents.schema.ts` (alterar); `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D1, R3, R6, R9):
    - `default-title.ts`: exporta `DEFAULT_TITLE_PREFIX = 'documento-sem-titulo-'` e `nextDefaultTitle(titles: string[]): string`. Só ocupa número o título que casa exatamente `^documento-sem-titulo-([1-9]\d*)$` ("documento-sem-titulo-01", "-0", "documento-sem-titulo-2 cópia" e "Sem título" não ocupam). Devolve `DEFAULT_TITLE_PREFIX + n`, com `n` o menor inteiro ≥ 1 não ocupado.
    - `documents.schema.ts`: só o comentário de `DEFAULT_DOCUMENT_TITLE` muda, passando a dizer que é o título gravado quando o título é apagado ao renomear (não mais o da criação). O valor e o `updateDocumentSchema` não mudam (D4).
    - `openapi.yaml`: em `createDocument`, `summary`/`description` em pt_BR dizem que o documento nasce com o nome "documento-sem-titulo-N", N o menor inteiro livre entre os documentos do dono, inclusive os da lixeira; `Document.title` ganha `description` (tipo continua `string`, sem `nullable`). Regenerar com `pnpm --filter @folioteca/api-contract generate`.
  - Skills: —
  - Complexidade: baixa

- [x] T1.2 — `DocumentsService.create` calcula o nome sob lock consultivo por dono
  - Arquivos: `apps/api/src/documents/documents.service.ts` (alterar)
  - O que fazer (D1, D2, R3):
    - Dentro da transação já existente de `create`, a **primeira** instrução é `await tx.$executeRaw\`SELECT pg_advisory_xact_lock(hashtextextended('document-default-title:' || ${person.id}, 0))\`` (template tagged; `ownerId` como parâmetro).
    - Depois de decidir o espaço, `tx.document.findMany({ where: { ownerId: person.id, title: { startsWith: DEFAULT_TITLE_PREFIX } }, select: { title: true } })`, **sem** filtro de `trashedAt`; o título do `create` é `nextDefaultTitle(titles.map((d) => d.title))`.
    - Docstring de `create` explica o lock: a segunda criação do mesmo dono espera o commit da primeira; em READ COMMITTED a leitura seguinte já vê o título gravado; o lock é liberado no fim da transação, inclusive em erro; donos diferentes não se bloqueiam.
  - Skills: security
  - Complexidade: alta

- [x] T1.3 — Testes da fase 1
  - Arquivos: `apps/api/src/documents/__tests__/default-title.test.ts` (criar); `apps/api/src/documents/__tests__/documents.integration.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.contract.test.ts` (alterar, só se algum exemplo citar o título da criação)
  - O que fazer: integração e contrato contra o Postgres real com `resetDatabase(prisma)` em `beforeEach` e os ajudantes existentes; casos preexistentes que esperavam "Sem título" na criação são ajustados sem renomear (DV1).
    - `default-title.test.ts` (`unit-testing`): `nextDefaultTitle returns documento-sem-titulo-1 for an empty list`; `nextDefaultTitle returns the next number after a sequence`; `nextDefaultTitle fills the smallest gap`; `nextDefaultTitle ignores titles with leading zeros`; `nextDefaultTitle ignores documento-sem-titulo-0`; `nextDefaultTitle ignores titles with a suffix`; `nextDefaultTitle ignores Sem título`.
    - `documents.integration.test.ts` (HTTP, salvo onde indicado): `POST documents names the first document documento-sem-titulo-1`; `POST documents names the second document documento-sem-titulo-2`; `POST documents counts trashed documents when picking the number`; `POST documents reuses the smallest free number after a rename`; `POST documents numbering of another owner does not interfere`; `POST documents keeps existing Sem título documents untouched`; `create twice at the same time for the same owner gives distinct numbers` — **serviço real** (instância ligada ao Prisma de teste, sem mock), `Promise.all([service.create(person, input), service.create(person, input)])`: as duas resolvem, e os títulos são exatamente `{"documento-sem-titulo-1", "documento-sem-titulo-2"}` em qualquer ordem; `POST documents twice in parallel answers 201 with distinct numbers` (o mesmo pela HTTP, dois `POST` em `Promise.all`).
  - Skills: unit-testing, integration-testing
  - Complexidade: média

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules`): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e sem aviso.
- [x] CA1.2 — `apps/api/src/documents/default-title.ts` exporta `DEFAULT_TITLE_PREFIX` com valor `'documento-sem-titulo-'` e `nextDefaultTitle(titles: string[]): string`; contém a expressão `[1-9]\d*` ancorada com `^` e `$`.
- [x] CA1.3 — `apps/api/src/documents/documents.service.ts`, lido em `create`: dentro da mesma transação, a primeira chamada a `tx` é `tx.$executeRaw` com template tagged contendo `pg_advisory_xact_lock(hashtextextended('document-default-title:'`; depois há `tx.document.findMany` com `ownerId` e `startsWith: DEFAULT_TITLE_PREFIX`, sem `trashedAt` no `where`; o título criado vem de `nextDefaultTitle(`. `rg -n -P "^\s*[^/*].*\\\$(executeRawUnsafe|queryRawUnsafe)" apps/api/src/documents/documents.service.ts` é vazio.
- [x] CA1.4 — `packages/api-contract/openapi.yaml` cita `documento-sem-titulo-` na operação `createDocument` e `Document.title` tem `description`, continua `type: string` e sem `nullable`. `apps/api/src/documents/documents.schema.ts` mantém `DEFAULT_DOCUMENT_TITLE` com o mesmo valor de antes.
- [x] CA1.5 — Banco intocado: `ls apps/api/prisma/migrations` lista as mesmas pastas de antes da fase; `apps/api/prisma/schema.prisma` inalterado; `rg -n "DROP CONSTRAINT|DISABLE TRIGGER|session_replication_role|migrate (diff|dev|reset)" apps/api/src/documents/__tests__/default-title.test.ts apps/api/src/documents/__tests__/documents.integration.test.ts apps/api/src/documents/__tests__/documents.contract.test.ts` é vazio.
- [x] CA1.6 — `pnpm exec vitest run --project api` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/api/src/documents/__tests__/default-title.test.ts apps/api/src/documents/__tests__/documents.integration.test.ts`, os 15 casos nomeados em T1.3 com os nomes literais (`default-title.test.ts`: 7; `documents.integration.test.ts`: 8). `rg -n "vi\.mock\(.*prisma" apps/api/src/documents/__tests__/documents.integration.test.ts` é vazio.
- [x] CA1.7 — Lendo os testes: `create twice at the same time for the same owner gives distinct numbers` chama o método `create` do `DocumentsService` real (ligado ao Prisma de teste) duas vezes dentro de um `Promise.all` e assere o conjunto `documento-sem-titulo-1`/`documento-sem-titulo-2`; `POST documents counts trashed documents when picking the number` manda o `-1` para a lixeira antes de criar e espera `-2`; `POST documents keeps existing Sem título documents untouched` assere que o título "Sem título" preexistente continua igual depois da criação.
- [x] CA1.8 — Cobertura ≥ 80% de linhas para `apps/api/src/documents/default-title.ts` e `apps/api/src/documents/documents.service.ts`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 2 — Web: título compacto na barra do documento

Caminhos relativos à raiz do repositório. A ordem importa: API simulada e mutations antes da tela. Ao fim da fase, no app com API simulada, o documento novo aparece como "documento-sem-titulo-N", o título é editado na barra e a barra lateral reflete o nome novo.

- [x] T2.1 — Invalidação aguardada nas mutations de documento
  - Arquivos: `apps/web/src/features/documents/api/get-documents.ts` (alterar); `apps/web/src/features/documents/api/update-document.ts` (alterar); `apps/web/src/features/documents/api/create-document.ts` (alterar); `apps/web/src/features/documents/api/trash-document.ts`, `apps/web/src/features/documents/api/restore-document.ts`, `apps/web/src/features/documents/api/delete-document.ts`, `apps/web/src/features/documents/hooks/use-document-collaboration.ts` (alterar, uma linha cada)
  - O que fazer (D6, R7): `invalidateDocumentLists` passa a devolver `Promise<void>` (`Promise.all` das invalidações que já faz). `useUpdateDocument`: `onSuccess` assíncrono faz `setQueryData` do documento, `await invalidateDocumentLists(...)` e só então chama o `onSuccess` de quem chama; comentário do schema do cliente atualizado (vazio é tratado no formulário). `useCreateDocument`, `useTrashDocument`, `useRestoreDocument` e `useDeleteDocument`: `await invalidateDocumentLists(...)` antes do `onSuccess` de quem chama. `use-document-collaboration.ts` não é mutation: `void invalidateDocumentLists(...)`.
  - Skills: api-requests
  - Complexidade: média

- [x] T2.2 — API simulada cria "documento-sem-titulo-N"
  - Arquivos: `apps/web/src/testing/mocks/handlers/documents.ts` (alterar); `apps/web/src/testing/mocks/db.ts` (alterar)
  - O que fazer (D1, R3, R9): o handler de `POST ${env.API_URL}/documents` nomeia o documento com a mesma regra da API (menor inteiro livre a partir de 1 entre os documentos do dono do banco falso, lixeira conta, só casa `^documento-sem-titulo-([1-9]\d*)$`), com a regra implementada no `db.ts` (a feature não é importada pelos mocks). A fábrica de documento novo que representa criação deixa de usar "Sem título" fixo; seeds antigos com "Sem título" ficam como estão.
  - Skills: api-mocking
  - Complexidade: baixa

- [x] T2.3 — Campo compacto do título e barra do documento reorganizada
  - Arquivos: `apps/web/src/features/documents/components/document-title-form.tsx` (alterar); `apps/web/src/features/documents/components/document-view.tsx` (alterar); `docs/design.md` (alterar)
  - O que fazer (D3, D4, D5, D7, D8, R1, R2, R4, R5, R6, R8, R10). Aparência pelo `docs/design.md` e pelo piso de `interface-design`:
    - `document-title-form.tsx`: sai o `Input` com rótulo visível "Título" e sai o alerta inline "Não foi possível salvar o título. Tente de novo.". Fica um `<label>` visualmente oculto **"Título do documento"** ligado ao campo, um campo que parece texto (borda transparente; borda cinza no hover e no foco; anel de foco azul; texto em peso semibold; altura de 40px; largura total; corta com reticências), `maxLength={200}` e validação Zod `.max(200, 'O título pode ter no máximo 200 caracteres.')`, com a mensagem num elemento visualmente oculto ligado por `aria-describedby`. Comportamento: Enter envia (submit nativo); sair do campo chama `requestSubmit` (como hoje); nada é enviado se o valor aparado for igual ao nome atual. `Escape` no `onKeyDown`: `preventDefault` e `form.reset({ title: document.title })`, com o foco ficando no campo. Aparado vazio no `onSubmit`: `form.reset` para o nome atual, sem requisição e sem mensagem. Enquanto `isPending`: `aria-disabled="true"`, `readOnly`, cursor de espera, novos envios ignorados, foco mantido; **nunca `disabled`**. Na falha: o texto digitado fica (sem `reset`), o campo ganha `aria-invalid="true"` (borda vermelha) até a próxima tentativa; a mensagem vem da "Notificação" que o interceptor de `@/lib/api-client` já dispara (sem alteração ali).
    - `document-view.tsx`: a barra ("Linha de ações do documento") é uma linha única que quebra: à esquerda o bloco do título (cresce, com `min-w-0` e base mínima), à direita as ações "Compartilhar", "Mover para a lixeira" (só dono) e "Adicionar aos favoritos"/"Remover dos favoritos", empurradas para a direita e que descem inteiras para a linha de baixo a 360px. Quem pode editar: `<h1>` visualmente oculto com o título (mantido) + `DocumentTitleForm` na barra. Acesso `view`: no lugar do campo, um `<h1>` de texto (versão texto da receita, `truncate`, atributo `title` com o nome completo) seguido do selo "Somente leitura"; sem o `<h1>` oculto (um só `<h1>`). Lixeira: aviso âmbar inalterado ("Este documento está na lixeira desde {data}. Restaure-o para voltar a editar." + "Restaurar" / "Apagar definitivamente"), com o foco nele como hoje; abaixo, a barra com o `<h1>` de texto, como no acesso `view`. O título grande abaixo da barra sai. Carregando, não encontrado e erro da página inalterados ("Carregando documento…", "Documento não encontrado", "Não foi possível carregar o documento." + "Tentar novamente"). "Indicador de salvamento" e área do editor inalterados.
    - `docs/design.md`, só acrescentando/ajustando linhas: em "Padrões acrescentados pelas entregas", a receita nova **"Título compacto do documento"** (fatia 169) — o "título editável na barra": campo `h-10 w-full min-w-0 truncate rounded-md border border-transparent bg-transparent px-2 text-base font-semibold text-gray-900 hover:border-gray-300 focus-visible:border-gray-300 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600 aria-disabled:cursor-wait aria-[invalid=true]:border-red-500`, rótulo `sr-only` "Título do documento", versão texto `<h1>` `min-w-0 truncate px-2 text-base font-semibold text-gray-900` com `title`, e o comportamento (Enter/sair salva, Esc desfaz, vazio volta, falha mantém o texto, `aria-disabled` no envio). "Linha de ações do documento" atualizada (título à esquerda, ações à direita, `flex-wrap`); "Selo de somente leitura" passa a ficar depois do título; "Título editável" (fatia 004) marcada como substituída pela receita nova; nota da "Área do editor" sobre alinhar com o campo do título ajustada.
  - Skills: forms, interface-design, component-robustness, authorization
  - Complexidade: média

- [x] T2.4 — Testes da fase 2
  - Arquivos: `apps/web/src/features/documents/components/__tests__/document-title-form.test.tsx` (alterar); `apps/web/src/features/documents/components/__tests__/document-view.test.tsx` (alterar); `apps/web/src/features/documents/api/__tests__/update-document.test.tsx`, `apps/web/src/features/documents/api/__tests__/create-document.test.tsx`, `apps/web/src/features/documents/api/__tests__/get-documents.test.tsx` (alterar); `apps/web/src/features/documents/components/__tests__/new-document-button.test.tsx`, `apps/web/src/app/routes/app/__tests__/my-documents.test.tsx`, `apps/web/src/app/routes/app/__tests__/space.test.tsx`, `apps/web/src/app/routes/app/__tests__/document.test.tsx` (alterar)
  - O que fazer: banco falso pelos ajudantes; `userEvent`; localizar por papel e nome acessível em pt_BR (campo por `getByRole('textbox', { name: 'Título do documento' })`); esperas com `findBy…`/`waitFor`, nunca espera fixa; sem `console.error`/`console.warn`/aviso de `act(...)`. Casos preexistentes que procuravam o rótulo "Título", o alerta inline ou "Sem título" na criação são ajustados sem renomear (DV2).
    - `document-title-form.test.tsx` (`component-testing`): `labels the field Título do documento`; `Enter saves the new title once`; `Tab out saves the new title`; `Escape restores the previous title and keeps focus`; `leaving without changes sends nothing`; `an empty title restores the previous one without a request`; `a failed save keeps the typed text and sets aria-invalid`; `while saving the field is aria-disabled and never disabled`; `blocks titles longer than 200 characters`.
    - `document-view.test.tsx` (`component-testing`): `an editor sees the title field in the actions row`; `a viewer sees the title as a heading and Somente leitura`; `a trashed document shows the title as a heading below the trash notice`; `renders a single h1 for each access level`; `no longer renders the title below the actions row`.
    - `update-document.test.tsx` (`unit-testing`): `useUpdateDocument awaits list invalidation before the caller onSuccess`. `create-document.test.tsx`: `useCreateDocument returns documento-sem-titulo-1 for the first document`; `useCreateDocument awaits list invalidation before the caller onSuccess`. `get-documents.test.tsx`: `invalidateDocumentLists resolves after the list queries are invalidated`.
    - Integração (`integration-testing`): `new-document-button.test.tsx`: `creating a document opens it named documento-sem-titulo-1`; `document.test.tsx`: `renaming in the title field updates the sidebar`; `my-documents.test.tsx`: `a new document appears as documento-sem-titulo-1`; `space.test.tsx`: `a new document in the space appears as documento-sem-titulo-1`.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` saem com 0 e sem aviso; a saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)`.
- [x] CA2.2 — `apps/web/src/features/documents/api/get-documents.ts` declara `invalidateDocumentLists` com retorno `Promise<void>` e usa `Promise.all`; `update-document.ts`, `create-document.ts`, `trash-document.ts`, `restore-document.ts` e `delete-document.ts` (em `apps/web/src/features/documents/api/`) contêm `await invalidateDocumentLists(` antes da chamada ao `onSuccess` do `mutationConfig`; `apps/web/src/features/documents/hooks/use-document-collaboration.ts` contém `void invalidateDocumentLists(`.
- [x] CA2.3 — `apps/web/src/features/documents/components/document-title-form.tsx` contém literalmente `Título do documento`, `sr-only`, `aria-describedby`, `aria-invalid`, `aria-disabled`, `readOnly`, `maxLength={200}`, `O título pode ter no máximo 200 caracteres.`, `'Escape'`, `requestSubmit` e `form.reset`; não contém `Não foi possível salvar o título`. `rg -n -P "(?<![-\w])disabled\b" apps/web/src/features/documents/components/document-title-form.tsx` é vazio (o lookbehind não casa `aria-disabled`); `rg -n -P "label=\"Título\"|>Título<" apps/web/src/features/documents/components/document-title-form.tsx apps/web/src/features/documents/components/document-view.tsx` é vazio.
- [x] CA2.4 — Acabamento (piso de `interface-design`, lendo o código): o campo de `document-title-form.tsx` usa as classes da receita "Título compacto do documento" do `docs/design.md` (`h-10`, `border-transparent`, `hover:border-gray-300`, `focus-visible:outline-blue-600`, `aria-[invalid=true]:border-red-500`, `truncate`, `min-w-0`); em `document-view.tsx` a barra tem `flex-wrap`, o bloco do título tem `min-w-0` e `grow`, o grupo de ações tem `ml-auto`, e o `<h1>` de texto (acesso `view` e lixeira) usa as classes da versão texto da receita com atributo `title`; o selo "Somente leitura" vem depois desse `<h1>`. `rg -n -P "<button|<a href|style=\{\{|!important|forwardRef|: JSX\." apps/web/src/features/documents/components/document-title-form.tsx apps/web/src/features/documents/components/document-view.tsx` é vazio.
- [x] CA2.5 — `docs/design.md` tem, em "Padrões acrescentados pelas entregas", a receita "Título compacto do documento" citando `Título do documento`, `aria-disabled` e `aria-[invalid=true]:border-red-500`; a receita "Título editável" diz que foi substituída por ela; "Linha de ações do documento" cita o título à esquerda e as ações à direita.
- [x] CA2.6 — API simulada: `apps/web/src/testing/mocks/db.ts` contém `documento-sem-titulo-` e a expressão `[1-9]\d*`; o handler de criação em `apps/web/src/testing/mocks/handlers/documents.ts` usa essa regra; `rg -n -P "from '@/features/" apps/web/src/testing/mocks/db.ts apps/web/src/testing/mocks/handlers/documents.ts` não traz import novo desta fase.
- [x] CA2.7 — `pnpm exec vitest run --project web` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/web/src/features/documents apps/web/src/app/routes/app/__tests__`, os 22 casos nomeados em T2.4 com os nomes literais (`document-title-form.test.tsx`: 9; `document-view.test.tsx`: 5; `update-document.test.tsx`: 1; `create-document.test.tsx`: 2; `get-documents.test.tsx`: 1; `new-document-button.test.tsx`, `document.test.tsx`, `my-documents.test.tsx`, `space.test.tsx`: 1 cada).
- [x] CA2.8 — Lendo os testes: `Escape restores the previous title and keeps focus` assere o valor anterior e `toHaveFocus()` no campo; `an empty title restores the previous one without a request` e `leaving without changes sends nothing` asserem zero `PATCH`; `a failed save keeps the typed text and sets aria-invalid` força erro no `PATCH` e assere o texto digitado e `aria-invalid="true"`; `while saving the field is aria-disabled and never disabled` assere `aria-disabled="true"` e `not.toBeDisabled()`; `renders a single h1 for each access level` conta um `heading` de nível 1 nos três casos; os dois testes de "awaits list invalidation" asserem que a invalidação resolveu antes do `onSuccess`; `renaming in the title field updates the sidebar` assere o nome novo na barra lateral.
- [x] CA2.9 — `rg -n "sleep\(|waitForTimeout" apps/web/src/features/documents/components/__tests__/document-title-form.test.tsx apps/web/src/features/documents/components/__tests__/document-view.test.tsx apps/web/src/app/routes/app/__tests__/document.test.tsx` é vazio; `rg -n "eslint-disable" apps/web/src/features/documents apps/web/src/testing/mocks | rg -v -- "--"` é vazio. Os desvios que tenham ocorrido estão na seção "Desvios", com arquivo e nome do caso.
- [x] CA2.10 — Cobertura ≥ 80% de linhas para `apps/web/src/features/documents/components/document-title-form.tsx`, `apps/web/src/features/documents/components/document-view.tsx`, `apps/web/src/features/documents/api/get-documents.ts`, `update-document.ts`, `create-document.ts`, `trash-document.ts`, `restore-document.ts` e `delete-document.ts` (em `apps/web/src/features/documents/api/`), lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 3 — e2e da jornada e documentação: a fatia utilizável de ponta a ponta

- [x] T3.1 — Status da fatia no roadmap
  - Arquivos: `docs/roadmap.md` (alterar; ou o índice de fatias do projeto, se for outro arquivo)
  - O que fazer: marcar a 169 como em revisão, no formato das outras linhas, e registrar as dívidas da SPEC (`PATCH` com título vazio grava "Sem título"; documentos antigos "Sem título"; varredura de mensagens de erro duplicadas).
  - Skills: —
  - Complexidade: baixa

- [x] T3.2 — Testes da fase 3 (e2e: criar, renomear na barra, desfazer com Esc)
  - Arquivos: `apps/web/e2e/tests/create-document.spec.ts` (alterar); `apps/web/e2e/tests/free-space-documents.spec.ts`, `apps/web/e2e/tests/favorites.spec.ts`, `apps/web/e2e/tests/unit-space-documents.spec.ts`, `apps/web/e2e/tests/trash.spec.ts`, `apps/web/e2e/tests/block-editor.spec.ts` (alterar, só localizadores)
  - O que fazer: API simulada, estado inicial por `page.addInitScript`, como os specs existentes; `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo; `toBeFocused()` antes de cada tecla; nenhum `waitForTimeout`; nenhuma regra do axe desativada; `apps/web/e2e/a11y.ts` intocado.
    - `create-document.spec.ts`, caso novo `the owner creates a document and renames it in the actions row`: cria o documento, vê o campo "Título do documento" com valor casando `documento-sem-titulo-\d+` e o mesmo nome na barra lateral; digita um nome novo, `toBeFocused()`, Enter, e vê o nome novo no campo e na barra lateral; digita outro texto, `toBeFocused()`, Escape, e o campo volta ao nome salvo; `expectNoSeriousA11yViolations(page)` na página do documento.
    - Os cinco specs antigos procuram o campo pelo rótulo "Título" e/ou esperam "Sem título" na criação: trocar só o localizador para o nome "Título do documento" e o valor para a regex `documento-sem-titulo-\d+` (o banco do e2e acumula documentos), sem renomear casos (DV3). Nenhum outro spec muda.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [x] CA3.1 — `pnpm test:e2e` na raiz sai com 0 (a suíte inteira), e `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` continuam saindo com 0 e sem aviso, com o cache do `tsc` limpo.
- [x] CA3.2 — `apps/web/e2e/tests/create-document.spec.ts` contém o caso `the owner creates a document and renames it in the actions row`, e `pnpm --filter web exec playwright test --list` o lista. `rg -n "test\.skip|test\.only|waitForTimeout" apps/web/e2e/tests/create-document.spec.ts apps/web/e2e/tests/free-space-documents.spec.ts apps/web/e2e/tests/favorites.spec.ts apps/web/e2e/tests/unit-space-documents.spec.ts apps/web/e2e/tests/trash.spec.ts apps/web/e2e/tests/block-editor.spec.ts` é vazio. `rg -n "disableRules" apps/web/e2e/tests/create-document.spec.ts apps/web/e2e/tests/free-space-documents.spec.ts apps/web/e2e/tests/favorites.spec.ts apps/web/e2e/tests/unit-space-documents.spec.ts apps/web/e2e/tests/trash.spec.ts apps/web/e2e/tests/block-editor.spec.ts` só lista as exclusões que já existiam (as de `create-document.spec.ts` no caso antigo, `trash.spec.ts` e `block-editor.spec.ts`) e uma no caso novo, `disableRulesWithin` com `selector: '.bn-container'` e `rules: ['aria-input-field-name']` e nada além (violação conhecida do BlockNote, item 053 do roadmap; DV9).
- [x] CA3.3 — Lendo o spec novo: `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo; a primeira asserção após cada mudança de rota usa `ROUTE_TIMEOUT`; `toBeFocused()` antes de cada `Enter` e `Escape`; `expectNoSeriousA11yViolations(page)` chamado; o campo é localizado pelo nome "Título do documento" e o valor inicial pela regex `documento-sem-titulo-\d+`; o caso assere o nome novo na barra lateral e o nome salvo de volta após o Escape.
- [x] CA3.4 — `rg -n -P "getByLabel\(['\"]Título['\"]|name: ['\"]Título['\"]" apps/web/e2e/tests` é vazio; os casos dos cinco specs antigos mantêm os nomes de antes (conferido pelos DV3 registrados).
- [x] CA3.5 — `docs/roadmap.md` (ou o índice de fatias) mostra a 169 com o status novo.
- [x] CA3.6 — A fatia está utilizável de ponta a ponta: `pnpm exec vitest run --project api` (0) inclui `create twice at the same time for the same owner gives distinct numbers` e `POST documents names the first document documento-sem-titulo-1`; `pnpm exec vitest run --project web` (0) inclui `Escape restores the previous title and keeps focus` e `renaming in the title field updates the sidebar`; `pnpm test:e2e` (0) inclui `the owner creates a document and renames it in the actions row`.

## Desvios

Preenchido pelos agentes de fase quando um teste existente precisar de ajuste (sem renomear). Previstos:

- DV1 — `apps/api/src/documents/__tests__/documents.integration.test.ts` e `documents.contract.test.ts`: casos que esperavam "Sem título" na criação passam a esperar `documento-sem-titulo-N`.
- DV2 — testes web que procuravam o rótulo "Título", o alerta inline ou "Sem título" na criação (T2.4).
- DV3 — `apps/web/e2e/tests/free-space-documents.spec.ts`, `favorites.spec.ts`, `unit-space-documents.spec.ts`, `trash.spec.ts`, `block-editor.spec.ts`: localizador "Título" → "Título do documento" e valor pela regex.
- DV4 — `apps/api/src/access/__tests__/document-access-boundary.test.ts` (regra 2, caso `every document read in the documents module goes through readableDocumentsWhere`): exceção nomeada `isDefaultTitleRead` para a única leitura `tx.document.findMany` de `create` em `documents.service.ts` que numera `documento-sem-titulo-N`. Não se criou porta nova no módulo `access` porque uma porta "do dono, lixeira incluída" ficaria disponível a qualquer leitura de `documents/` e deixaria passar lista com lixeira sem mencionar `trashedAt` (enfraquece a regra 8). A exceção vale só nesse arquivo e só para a chamada com `ownerId: person.id`, `title: { startsWith: DEFAULT_TITLE_PREFIX }` e `select: { title: true }`: a leitura não devolve documento a ninguém, só calcula o próximo número, e é restrita ao `ownerId` de quem cria. O caso de amostra da regra 2 ganhou a chamada aceita e a mesma chamada fora do arquivo reprovada; nenhum caso renomeado.
- DV5 — quatro casos antigos adaptados ao comportamento novo (DV2) ficaram com nomes que já não descreviam o que verificam e foram renomeados: em `apps/web/src/features/documents/components/__tests__/document-title-form.test.tsx`, `an empty title comes back as Sem título` → `an empty title keeps the previous name after Enter`; `a failed save keeps the typed text and shows the alert` → `a failed save keeps the typed text and marks the field invalid`; `the alert clears on the next save` → `aria-invalid clears on the next successful save`; em `document-view.test.tsx`, `shows the favorite button before the title field when the document is loaded` → `shows the title field first and the favorite button to its right when the document is loaded`. Nenhum critério citava os nomes antigos.
- DV6 — CA2.7 dizia 23 casos, mas a lista do próprio critério soma 22 (9 + 5 + 1 + 2 + 1 + 4); o número do CA2.7 foi corrigido para 22.
- DV7 — o T2.2 listava `apps/web/src/testing/mocks/handlers/documents.ts` para alterar, mas a regra do nome padrão entrou só por `createDocumentIn` em `apps/web/src/testing/mocks/db.ts`, que o handler já chamava; o handler ficou intacto. E o caso novo em `apps/web/src/app/routes/app/__tests__/space.test.tsx` liga `env.ENABLE_API_MOCKING` só dentro do teste e o restaura no fim, para a página do documento não abrir WebSocket real.
- DV8 — a fase 2 mudou a ordem de Tab na barra de ações do documento para título → Compartilhar → Mover para a lixeira → Adicionar aos favoritos → editor. Quatro casos e2e antigos que chegavam aos botões ou ao editor pelo teclado a partir do título passaram a usar a ordem nova, com `toBeFocused()` em cada parada antes da tecla de ação, sem renomear nem pular: em `apps/web/e2e/tests/block-editor.spec.ts`, `writes with the keyboard only, sees Salvo and finds the text again after leaving and coming back` e `shows the Portuguese slash menu without table or media blocks` (quatro Tabs do título até o editor, em vez de um); em `favorites.spec.ts`, o caso `marks a document as favorite from the keyboard, ...` (três Tabs do título até o favorito, em vez de um Shift+Tab, nas duas passagens); em `trash.spec.ts`, o caso `moves a document to the trash from the keyboard, ...` (três Tabs até o favorito e, na reabertura, dois Tabs até a lixeira, em vez de Shift+Tab).
- DV9 — o caso novo `the owner creates a document and renames it in the actions row` em `apps/web/e2e/tests/create-document.spec.ts` chama `expectNoSeriousA11yViolations` com `disableRulesWithin` restrito a `.bn-container` e só a `aria-input-field-name` (violação conhecida do BlockNote, item 053 do roadmap), como o caso antigo do mesmo arquivo. O texto do CA3.2 foi ajustado para admitir exatamente essa exclusão e nada além.

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
