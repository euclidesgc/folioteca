# PLAN 135 — free-space-members

Branch: `feature/135-free-space-members`, empilhada sobre a 134 (`free-space-invite`: tabela `SpaceMember`, `SpacesService.addMember`, `reach: 'member'`, `useAddSpaceMember` e `AddSpaceMemberDialog` já existem).

Fonte: `docs/features/135-free-space-members/spec.md` (D1…D7 são as decisões técnicas da SPEC e R1…R14 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

O dono e os membros de um espaço livre veem, na página do espaço, a seção "Pessoas neste espaço" (dono primeiro, com selo "dono"; quem vê com selo "você"); o dono remove membros, um de cada vez, com confirmação. Quem foi removido perde o acesso na hora. Fora desta fatia: papéis de membro (142), documentos no espaço livre, sair do espaço por conta própria.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; banco de teste `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. Se a suíte inteira (`pnpm test` ou `pnpm test:e2e`) falhar num teste **antigo** e alheio a esta fatia, o critério permite repetir o comando **uma** vez; a segunda execução tem de sair com 0.

**Critérios sobre o disco.** Os critérios são conferidos **antes** do commit da fase, sobre os arquivos em disco (commitados ou não). Nenhum critério depende de `git diff` contra a base. Onde um critério prova **ausência**, o padrão fica restrito aos arquivos criados ou alterados na fase e exige código (não casa com comentário nem com atributo parecido); onde isso não é possível, o critério exige a presença do que é certo ou manda ler o arquivo. O commit de cada fase é feito pela orquestração depois da revisão; nenhum agente de fase commita.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **Nenhuma migration**: `apps/api/prisma/schema.prisma` e `apps/api/prisma/migrations/**` não mudam. Os únicos subcomandos de Prisma permitidos são `prisma validate`, `prisma generate`, `prisma migrate status` e `prisma migrate deploy`, sempre contra `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test`; **nunca** `migrate diff`, `migrate dev` ou `migrate reset`.
- **Nenhum teste remove, desativa ou recria restrição do banco** (`DROP CONSTRAINT`, `ALTER TABLE … DISABLE`, `session_replication_role`).
- O arquivo `packages/api-contract/src/generated/openapi.d.ts` só muda por `pnpm --filter @folioteca/api-contract generate`; nunca editado à mão.
- **Sem Prettier reformatando arquivo existente**: só as linhas necessárias mudam; nada de `prettier --write` em arquivo inteiro.
- Todo componente devolve `React.JSX.Element` (ou `React.JSX.Element | null`), nunca o `JSX` global; `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case; **nenhum import entre features** (`features/spaces` não importa de `features/org-units` nem de outra feature; `ConfirmationDialog` e `Notifications` vêm de `@/components/ui`). Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Botão de confirmar que precisa manter o foco durante o envio usa `aria-disabled`, **nunca** o `disabled` nativo. Ação destrutiva fica por último (na linha e no rodapé do diálogo).
- Nenhum `eslint-disable` sem `-- <motivo>` na própria linha; nenhuma regra desativada para fazer teste passar; nenhum `any` sem comentário.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado, medida **só** por `coverage/coverage-summary.json`, gerado **na raiz** com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`, **sem `--project`** e **sem filtro de caminho**. Não contam arquivos só de tipos nem os já excluídos pelo `vitest.config.ts` da raiz. Nenhuma exclusão nova.
- **Toda** espera logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes da web pela constante `LAZY_TIMEOUT` declarada no arquivo; no e2e por `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo do spec. Nunca `setTimeout`/sleep fixo nos testes nem `waitForTimeout`.
- Testes de API de integração e contrato rodam contra o Postgres real (sem mock do Prisma). "Teste passa" é medido pelo **exit code** (zero), nunca pela saída `--verbose`.
- **Escopo por sessão**: `organizationId` e `personId` de quem pede vêm sempre de `@CurrentPerson()`. Escopo de outra organização é provado chamando o **serviço real** com `randomUUID()` como `organizationId` (`Organization.singleton` impede uma segunda organização no banco).
- **Casos existentes não são renomeados.** Os testes da 128 (`unit-space-members`) que comparam o corpo de `GET /spaces/{spaceId}/members` por igualdade estrita são ajustados para incluir `role: 'assigned'`, mantendo o nome; isso é o desvio esperado DV1 no fim deste plano.
- Intocados de propósito: `apps/web/src/components/ui/**`, `apps/web/src/hooks/**`, `apps/web/src/features/org-units/**` (inclusive `unit-people-list.tsx`), `apps/web/src/features/documents/**`, `apps/web/e2e/a11y.ts`.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa.

## Fase 1 — API: listar membros do espaço livre e remover membro

Caminhos relativos à raiz do repositório. Ao fim da fase, pela API, dono e membros de um espaço livre recebem a lista em `GET /spaces/{spaceId}/members` (dono primeiro, com `role`), e o dono remove um membro com `DELETE /spaces/{spaceId}/members/{personId}` (204, idempotente); o removido passa a receber 404 no espaço.

- [ ] T1.1 — Contrato: campo `role`, `GET members` para FREE e o DELETE
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D1, D2):
    - Schema `SpaceMember`: campo novo **obrigatório** `role`, `type: string`, `enum: [owner, member, assigned]`, com descrição em pt_BR (`owner` = dono do espaço livre; `member` = membro do espaço livre; `assigned` = lotado na unidade). `isCurrentPerson` fica.
    - `get` de `/spaces/{spaceId}/members`: a `description` passa a cobrir o espaço livre (dono e membros veem; dono primeiro, depois quem pede se for membro, depois por nome); 404 também para espaço livre que quem pede não alcança.
    - Chave existente `/spaces/{spaceId}/members/{personId}` ganha `delete`, `operationId: removeSpaceMember`, tag `spaces`, mesmos parâmetros de caminho, **sem** `requestBody`; respostas **204** (sem corpo), **400**, **401**, **403** e **404** com `Error`. `description` em pt_BR: só o dono do espaço livre remove; idempotente (pessoa que não é membro também dá 204); 400 "O dono não pode ser removido."; 403 "Só o dono do espaço pode adicionar pessoas." (a mesma `OWNER_ONLY_MESSAGE` do PUT) para membro; 404 "Espaço não encontrado." para inexistente, id malformado, espaço de unidade ou espaço que quem pede não alcança.
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`.
  - Skills: api-requests
  - Complexidade: baixa

- [ ] T1.2 — `SpacesService` e `SpacesController`: `listMembers` no FREE, `role` e `removeMember`
  - Arquivos: `apps/api/src/spaces/spaces.service.ts` (alterar); `apps/api/src/spaces/spaces.controller.ts` (alterar)
  - O que fazer (D1, D2, R2, R7, R8, R10, R12):
    - `listMembers`: ramifica pelo tipo do espaço. UNIT: comportamento atual, cada item ganha `role: 'assigned'`. FREE: `space.findFirst({ where: { id: spaceId, type: 'FREE', organizationId, OR: [{ ownerId: personId }, { members: { some: { personId } } }] }, include: { owner: …, members: { include: { person: … } } } })` (o mesmo filtro de `getDetail`); ausente → `null` (404 no controller, como hoje). Resposta: o dono com `role: 'owner'` e os membros com `role: 'member'`, cada um com `isCurrentPerson`.
    - `compareMembers` estendido: `role === 'owner'` antes de tudo, depois `isCurrentPerson`, depois nome pt-BR, e-mail, id. No UNIT (sem `owner`) a ordem não muda.
    - Constante nova de mensagem `O dono não pode ser removido.`.
    - `removeMember(requester: { organizationId: string; id: string }, spaceId: string, personId: string): Promise<void>`, nesta ordem: `!isUuid(spaceId)` → `spaceNotFound()`; `space.findFirst` com `type: 'FREE'`, `organizationId` e o `OR` dono/membro de `addMember` ausente → `spaceNotFound()`; `ownerId !== requester.id` → `ForbiddenException` com `OWNER_ONLY_MESSAGE`; `personId === ownerId` → `BadRequestException` com `O dono não pode ser removido.`; `!isUuid(personId)` → retorna sem consulta; senão `spaceMember.deleteMany({ where: { spaceId, personId } })`.
    - `spaces.controller.ts`: `@Delete(':spaceId/members/:personId')` com `@HttpCode(204)`, `@CurrentPerson() person`, `@Param('spaceId') spaceId: string`, `@Param('personId') personId: string`, delegando a `removeMember`. Identidade conferida à mão: `@Controller('spaces')` + `':spaceId/members/:personId'` ↔ YAML `/spaces/{spaceId}/members/{personId}`.
  - Skills: security, authorization
  - Complexidade: alta

- [ ] T1.3 — Testes da fase 1
  - Arquivos: `apps/api/src/spaces/__tests__/spaces.integration.test.ts` (alterar); `apps/api/src/spaces/__tests__/spaces.service.test.ts` (alterar); `apps/api/src/spaces/__tests__/spaces.contract.test.ts` (alterar)
  - O que fazer (D7): Postgres real, `resetDatabase(prisma)` em `beforeEach`, ajudantes existentes; casos existentes não são renomeados (os da 128 só ganham `role: 'assigned'` no esperado, DV1); `apps/api/test/**` não muda. O caso existente `GET space members still answers 404 for a FREE space to its member` (da 134) contradiz esta fatia: é **substituído** pelo caso novo `GET space members lists a FREE space to its member` e registrado como desvio DV2.
    - `spaces.integration.test.ts`, DELETE: `DELETE space member answers 204 and removes the row`; `DELETE space member repeated answers 204`; `DELETE space member answers 204 for a person who is not a member`; `DELETE space member answers 204 for a malformed person id`; `DELETE space member answers 400 when removing the owner`; `DELETE space member answers 400 when the owner removes himself`; `DELETE space member answers 403 to a member`; `DELETE space member answers 404 for a malformed space id`; `DELETE space member answers 404 for a random space id`; `DELETE space member answers 404 for a FREE space of another person`; `DELETE space member answers 404 for a UNIT space` (quem pede está lotado diretamente na unidade; asserir `Espaço não encontrado.`); `DELETE space member answers 401 without session`.
    - `spaces.integration.test.ts`, leitura: `GET space members lists a FREE space to its owner with the owner first` (ordem e `role`, `isCurrentPerson` só no dono); `GET space members lists a FREE space to its member` (dono, depois quem pede com `isCurrentPerson: true`, depois os outros por nome); `GET space members answers 404 for a FREE space to a person who is not a member`; `GET space members answers 404 to a removed member`; `GET space answers 404 to a removed member`; `GET space members of a UNIT space answers role assigned`.
    - `spaces.service.test.ts`: `compareMembers puts the owner before the current person`; `listMembers with another organization id returns null for a FREE space`; `removeMember with another organization id throws not found` (serviço real ligado ao Prisma de teste, `randomUUID()` como `organizationId`, sobre espaço livre com membro existente; a linha de `SpaceMember` continua lá).
    - `spaces.contract.test.ts`: `DELETE space member answers the documented 204`; `DELETE space member answers the documented 400`; `DELETE space member answers the documented 401`; `DELETE space member answers the documented 403`; `DELETE space member answers the documented 404`; `GET space members answers the documented 200 for a FREE space`. Os casos do DELETE validam contra a chave literal do YAML `/spaces/{spaceId}/members/{personId}` e o último contra `/spaces/{spaceId}/members`.
  - Skills: unit-testing, integration-testing
  - Complexidade: média

### Critérios de aceite da fase 1

- [ ] CA1.1 — Com `docker compose up -d`, na raiz: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e sem aviso.
- [ ] CA1.2 — Sem migration: `rg -n "SpaceMember" apps/api/prisma/schema.prisma` mostra o `model SpaceMember` sem campo `role`; nenhuma pasta nova em `apps/api/prisma/migrations/` além de `0016_space_member` como a mais recente (`ls apps/api/prisma/migrations`). `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test pnpm --filter api exec prisma migrate status` diz que o banco está em dia.
- [ ] CA1.3 — `packages/api-contract/openapi.yaml`: o schema `SpaceMember` tem `role` em `required` com `enum` exatamente `owner`, `member`, `assigned`; a chave `/spaces/{spaceId}/members/{personId}:` tem `put` (inalterado) e `delete` com `operationId: removeSpaceMember`, tag `spaces`, sem `requestBody` e respostas exatamente `204`, `400`, `401`, `403`, `404`; a descrição do `delete` contém `O dono não pode ser removido.`. `rg -n "removeSpaceMember" packages/api-contract/src/generated/openapi.d.ts` encontra a operação.
- [ ] CA1.4 — Identidade de caminho conferida à mão: `apps/api/src/spaces/spaces.controller.ts` continua com `@Controller('spaces')` e tem `@Delete(':spaceId/members/:personId')` com `@HttpCode(204)`, `@Param('spaceId')` e `@Param('personId')`, formando `/spaces/{spaceId}/members/{personId}`; `@Put(':spaceId/members/:personId')` e `@Get(':spaceId/members')` seguem presentes.
- [ ] CA1.5 — `apps/api/src/spaces/spaces.service.ts` tem `removeMember(` e, lido, segue a ordem `isUuid(spaceId)` → `spaceNotFound()` → busca com `type: 'FREE'`, `organizationId` e `members: { some: … }` → 403 `OWNER_ONLY_MESSAGE` → 400 dono → `isUuid(personId)` retornando sem consulta → `spaceMember.deleteMany`; contém literalmente `O dono não pode ser removido.`. `listMembers`, lido, trata `type: 'FREE'` com o filtro dono/membro e produz `role: 'owner'`, `role: 'member'` e, no UNIT, `role: 'assigned'`; `compareMembers` compara `role === 'owner'` antes de `isCurrentPerson`.
- [ ] CA1.6 — `pnpm exec vitest run --project api` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/api/src/spaces/__tests__`, todos os casos nomeados em T1.3 com os nomes literais (`spaces.integration.test.ts`: 18 novos; `spaces.service.test.ts`: 3 novos; `spaces.contract.test.ts`: 6 novos). `rg -n "vi\.mock\(.*prisma" apps/api/src/spaces/__tests__/spaces.integration.test.ts` é vazio; `rg -n "DROP CONSTRAINT|DISABLE TRIGGER|session_replication_role" apps/api/src/spaces/__tests__` é vazio. Todo caso da 128 alterado aparece em DV1 com o mesmo nome; a substituição do caso da 134 aparece em DV2.
- [ ] CA1.7 — Lendo os testes: `DELETE space member answers 404 for a UNIT space` usa um espaço de unidade que quem pede alcança (lotação direta) e assere 404 com `Espaço não encontrado.`; `DELETE space member repeated answers 204` faz dois DELETEs e assere 204 nos dois; `DELETE space member answers 204 and removes the row` assere `spaceMember.count` = 0 depois; `removeMember with another organization id throws not found` e `listMembers with another organization id returns null for a FREE space` instanciam o serviço real com o Prisma de teste e passam `randomUUID()` como `organizationId`; os casos `DELETE space member answers the documented …` citam literalmente `/spaces/{spaceId}/members/{personId}` e `GET space members answers the documented 200 for a FREE space` cita `/spaces/{spaceId}/members`.
- [ ] CA1.8 — Cobertura ≥ 80% de linhas para `apps/api/src/spaces/spaces.service.ts` e `apps/api/src/spaces/spaces.controller.ts`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 2 — Web: a seção "Pessoas neste espaço" e a remoção pelo dono

Caminhos relativos à raiz do repositório. A ordem importa: API simulada e chamadas antes da tela. Ao fim da fase, no app com API simulada, dono e membro veem a lista na página do espaço livre, o dono remove membros com confirmação, e a lista se atualiza depois de adicionar ou remover.

- [ ] T2.1 — API simulada espelha o servidor
  - Arquivos: `apps/web/src/testing/mocks/db.ts` (alterar); `apps/web/src/testing/mocks/handlers/spaces.ts` (alterar)
  - O que fazer (D6):
    - `db.ts`: `listSpaceMembers` passa a cobrir o FREE (dono com `role: 'owner'` + `spaceMembers` com `role: 'member'`, `isCurrentPerson`, ordem de T1.2; `null` para quem não é dono nem membro) e acrescenta `role: 'assigned'` no UNIT; exporta `removeSpaceMember(requesterId: string, spaceId: string, personId: string)` com a ordem de T1.2 (404 → 403 → 400 dono → remove se existir) devolvendo resultado discriminado com status e mensagem. `addSpaceMember` inalterado.
    - `handlers/spaces.ts`: `http.delete(\`${env.API_URL}/spaces/:spaceId/members/:personId\`)` com o preâmbulo de sempre (`networkDelay()`, `devOverride('spaces')`, 401); 404 pelo mesmo `spaceNotFound()`; 403/400 com `{ message }` literal de T1.2; 204 sem corpo.
  - Skills: api-mocking
  - Complexidade: média

- [ ] T2.2 — Chamadas: `useRemoveSpaceMember` e a invalidação ao adicionar
  - Arquivos: `apps/web/src/features/spaces/api/remove-space-member.ts` (criar); `apps/web/src/features/spaces/api/add-space-member.ts` (alterar)
  - O que fazer (D5, R6, R11):
    - `remove-space-member.ts`: `removeSpaceMember({ spaceId, personId }: { spaceId: string; personId: string }): Promise<void>` → `api.delete(\`/spaces/${spaceId}/members/${personId}\`, { silentError: true })`; `useRemoveSpaceMember({ spaceId }: { spaceId: string })` com `useMutation`, `onSuccess` que faz `await queryClient.invalidateQueries({ queryKey: ['space-members', spaceId] })` antes do `onSuccess` do chamador. Sem atualização otimista.
    - `add-space-member.ts`: `useAddSpaceMember` passa a invalidar `['space-members', spaceId]` no próprio `onSuccess`, com `await` antes do `onSuccess` do chamador, como faz `useUpdateOrgUnitSpace` e as irmãs; o `spaceId` vem da variável da mutação. Continua **sem** invalidar `['spaces']`. `add-space-member-dialog.tsx` não muda.
  - Skills: api-requests
  - Complexidade: baixa

- [ ] T2.3 — `SpaceMembers` com `spaceType` e `canRemove`, e `SpaceView` no espaço livre
  - Arquivos: `apps/web/src/features/spaces/components/space-members.tsx` (alterar); `apps/web/src/features/spaces/components/space-view.tsx` (alterar)
  - O que fazer (D3, D4, Interface, R1–R5, R9, R12, R13). Aparência pelo `docs/design.md` e pelo piso de `interface-design`; receitas usadas: Lista, Selo de status, Ação destrutiva com confirmação num item de lista, Diálogo de confirmação, Notificação; nenhuma receita nova. Estrutura de foco como `apps/web/src/features/org-units/components/unit-people-list.tsx`, **sem importar dele**:
    - `space-members.tsx`: `SpaceMembers({ spaceId, spaceType, canRemove }: { spaceId: string; spaceType: 'unit' | 'free'; canRemove: boolean }): React.JSX.Element`; textos por tipo num objeto local.
      - UNIT, sem mudança: `<h2>` "Pessoas nesta unidade", "Carregando as pessoas desta unidade…", "Não foi possível carregar as pessoas desta unidade.", "Tentar novamente", "Ninguém está lotado diretamente nesta unidade."; `role: 'assigned'` não muda a tela; nunca botão de remover.
      - FREE: `<h2>` **"Pessoas neste espaço"**, `<ul aria-label="Pessoas neste espaço">` dentro de região `aria-live="polite"`. Carregando (`role="status"`): **"Carregando as pessoas deste espaço…"**. Erro (`role="alert"`, receita de erro): **"Não foi possível carregar as pessoas deste espaço."** + botão **"Tentar de novo"**. Sem texto de vazio (o dono sempre vem). Cada linha: nome e e-mail; selo **"dono"** quando `role === 'owner'`; selo **"você"** só quando `isCurrentPerson && role !== 'owner'`. A `<li>` do dono tem `tabIndex={-1}`.
      - Remoção num subcomponente no mesmo arquivo, montado só quando `canRemove`: em cada linha com `role === 'member'`, como último item da linha, botão ícone (`Button`) com `aria-label` **"Remover {nome}"**. Um único `ConfirmationDialog` de `@/components/ui` para a lista, sem gatilho próprio: título **"Remover {nome} do espaço?"**, descrição **"A pessoa perde o acesso na hora."**, **"Cancelar"** e, por último, **"Remover"** / **"Removendo…"** durante o envio, com `aria-disabled` (nunca `disabled`) e trava por `ref` contra duplo clique. Ao abrir, guarda o id do membro da linha de cima (ou `null`).
      - Sucesso (inclusive 204 de pessoa que já não era membro): fecha o diálogo, notificação **"{nome} foi removida do espaço."**; `onCloseAutoFocus` faz `preventDefault` e foca o botão "Remover" do membro de cima ou, se `null`, a `<li>` do dono, depois da lista refeita. Cancelar, Esc ou erro: foco volta ao botão que abriu.
      - Erro: diálogo continua aberto, botão volta a aceitar clique, notificação **"Não foi possível remover {nome}. Tente de novo."**; a pessoa continua na lista.
    - `space-view.tsx`: no ramo `free`, abaixo do nome, da descrição e do aviso de documentos, renderiza `<SpaceMembers spaceId={…} spaceType="free" canRemove={reach === 'owner'} />` para dono e membro; no ramo de unidade, `spaceType="unit"` e `canRemove={false}`. O resto sem mudança.
  - Skills: interface-design, error-handling, component-robustness, authorization
  - Complexidade: alta

- [ ] T2.4 — Testes da fase 2 e os e2e antigos
  - Arquivos: `apps/web/src/features/spaces/api/__tests__/remove-space-member.test.tsx` (criar); `apps/web/src/features/spaces/api/__tests__/add-space-member.test.tsx` (alterar); `apps/web/src/features/spaces/components/__tests__/space-members.test.tsx` (alterar); `apps/web/src/features/spaces/components/__tests__/space-view.test.tsx` (alterar); `apps/web/src/features/spaces/components/__tests__/add-space-member-dialog.test.tsx` (alterar)
  - O que fazer (D7): banco falso pelos ajudantes; `userEvent`; localizar por papel e nome acessível em pt_BR; `LAZY_TIMEOUT` em cada arquivo que espera após rota ou `lazy`; sem `console.error`/`console.warn`/aviso de `act(...)`. Casos existentes não são renomeados; os que comparam membros por igualdade estrita ganham `role` (DV1). Os casos UNIT existentes de `space-members.test.tsx` passam `spaceType="unit"` e `canRemove={false}`.
    - `remove-space-member.test.tsx`: `removeSpaceMember sends DELETE to the space member path`; `useRemoveSpaceMember invalidates the space members query`; `useRemoveSpaceMember exposes the server message on 400`.
    - `add-space-member.test.tsx`: `useAddSpaceMember invalidates the space members query` (o caso existente `useAddSpaceMember does not invalidate the spaces query` continua verde).
    - `space-members.test.tsx`: `a FREE space lists the owner first with the dono badge`; `a FREE space marks the current member with the você badge`; `the owner does not get the você badge`; `a FREE space shows the loading status`; `a FREE space shows the error with Tentar de novo`; `shows Remover only on member rows when canRemove`; `shows no Remover button without canRemove`; `cancelling the confirmation keeps the person and returns focus`; `removing a member announces it and removes the row`; `after removing focus goes to the Remover button of the row above`; `after removing the first member focus goes to the owner row`; `removing a person who already left succeeds without error`; `a failed removal keeps the dialog open and the person listed`; `the confirm button uses aria-disabled while sending and sends once on double click`; `a UNIT space keeps its texts and has no Remover button`.
    - `space-view.test.tsx`: `a FREE space shows Pessoas neste espaço with Remover to its owner`; `a FREE space shows Pessoas neste espaço without Remover to a member`.
    - `add-space-member-dialog.test.tsx`: `the people list shows the person after adding` (renderiza o diálogo junto de `SpaceMembers` do mesmo espaço).
    - Rodar `pnpm test:e2e` com **todos** os e2e existentes. A página do espaço livre ganha uma lista nova: specs antigos de espaço livre que contam listas ou itens podem mudar. Conserto em spec antigo só por essa mudança intencional, registrado como desvio no fim deste plano; qualquer outra quebra se conserta no código de produção.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [ ] CA2.1 — Com `docker compose up -d`, na raiz: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` saem com 0 e sem aviso; a saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)`. `pnpm test:e2e` (todos os e2e existentes) sai com 0; todo spec antigo alterado aparece como desvio no fim deste plano, com o motivo.
- [ ] CA2.2 — `apps/web/src/features/spaces/api/remove-space-member.ts` exporta `removeSpaceMember` e `useRemoveSpaceMember`; `removeSpaceMember` chama `api.delete` em `` `/spaces/${spaceId}/members/${personId}` `` com `silentError: true`; o arquivo contém `invalidateQueries` com `['space-members', spaceId]`. `apps/web/src/features/spaces/api/add-space-member.ts` contém `invalidateQueries` com `['space-members'` e, lido, faz `await` dessa invalidação dentro do `onSuccess` do hook; o arquivo não invalida `['spaces']`.
- [ ] CA2.3 — `apps/web/src/features/spaces/components/space-members.tsx` exporta `SpaceMembers` com retorno `React.JSX.Element` e props `spaceId: string`, `spaceType: 'unit' | 'free'` e `canRemove: boolean`; contém literalmente `Pessoas neste espaço`, `Carregando as pessoas deste espaço…`, `Não foi possível carregar as pessoas deste espaço.`, `Tentar de novo`, `dono`, `você`, `Remover `, ` do espaço?`, `A pessoa perde o acesso na hora.`, `Cancelar`, `Removendo…`, ` foi removida do espaço.`, `Não foi possível remover `, `. Tente de novo.`, `aria-live="polite"`, `role="status"`, `role="alert"`, `aria-disabled`, `tabIndex={-1}`, `onCloseAutoFocus`, `ConfirmationDialog` e `useRemoveSpaceMember`; e continua contendo `Pessoas nesta unidade`, `Carregando as pessoas desta unidade…`, `Não foi possível carregar as pessoas desta unidade.`, `Tentar novamente` e `Ninguém está lotado diretamente nesta unidade.`. `rg -nP "(?<![-\w])disabled=\{" apps/web/src/features/spaces/components/space-members.tsx` é vazio.
- [ ] CA2.4 — `apps/web/src/features/spaces/components/space-view.tsx` contém `spaceType="free"`, `spaceType="unit"` e `canRemove={reach === 'owner'}`, e, lido, renderiza `SpaceMembers` no ramo livre para dono e membro. `rg -nP "from '@/features/(?!spaces/)" apps/web/src/features/spaces` é vazio.
- [ ] CA2.5 — Acabamento (piso de `interface-design`, lendo o código de `space-members.tsx`): o `<h2>` usa as mesmas classes de subtítulo já usadas no ramo de unidade; cada linha separa nome/e-mail (esquerda, com `min-w-0` ou `truncate`) de selos e ação; "dono" e "você" usam a forma de selo do `docs/design.md`; o botão "Remover {nome}" é `Button` com `type` explícito e `aria-label`, e é o último elemento da linha; no diálogo, "Remover" vem depois de "Cancelar"; o erro tem fundo ou borda de destaque. `rg -n "<button|<a href|style=\{\{|!important|forwardRef|: JSX\." apps/web/src/features/spaces/components/space-members.tsx apps/web/src/features/spaces/components/space-view.tsx` é vazio; nenhuma largura fixa em px nesses arquivos.
- [ ] CA2.6 — API simulada: `apps/web/src/testing/mocks/db.ts` exporta `removeSpaceMember(requesterId: string, spaceId: string, personId: string)` e `listSpaceMembers` produz `role: 'owner'`, `role: 'member'` e `role: 'assigned'`; `apps/web/src/testing/mocks/handlers/spaces.ts` tem `http.delete` em `${env.API_URL}/spaces/:spaceId/members/:personId` com `networkDelay()`, `devOverride('spaces')`, 401, 404 `Espaço não encontrado.`, 403, 400 `O dono não pode ser removido.` e 204.
- [ ] CA2.7 — `pnpm exec vitest run --project web` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/web/src/features/spaces`, os 22 casos nomeados em T2.4 com os nomes literais (`remove-space-member.test.tsx`: 3; `add-space-member.test.tsx`: 1 novo; `space-members.test.tsx`: 15 novos; `space-view.test.tsx`: 2 novos; `add-space-member-dialog.test.tsx`: 1 novo).
- [ ] CA2.8 — Lendo os testes: `after removing focus goes to the Remover button of the row above` assere o foco no botão "Remover {nome}" do membro de cima; `after removing the first member focus goes to the owner row` assere o foco na `<li>` do dono; `a failed removal keeps the dialog open and the person listed` usa 500 e assere a notificação `Não foi possível remover` e a pessoa ainda na lista; `the confirm button uses aria-disabled while sending and sends once on double click` assere `aria-disabled="true"` e um único pedido ao handler DELETE; `removing a member announces it and removes the row` assere ` foi removida do espaço.`; `a FREE space shows Pessoas neste espaço without Remover to a member` assere a ausência de botão com nome começando por "Remover"; `the people list shows the person after adding` assere a pessoa na lista "Pessoas neste espaço" depois de "Adicionar".
- [ ] CA2.9 — `rg -n "sleep\(|waitForTimeout|setTimeout\(" apps/web/src/features/spaces/api/remove-space-member.ts apps/web/src/features/spaces/components/space-members.tsx apps/web/src/features/spaces/components/space-view.tsx apps/web/src/features/spaces/api/__tests__/remove-space-member.test.tsx apps/web/src/features/spaces/components/__tests__/space-members.test.tsx` é vazio; `rg -n "eslint-disable" apps/web/src | rg -v -- "--"` é vazio.
- [ ] CA2.10 — Cobertura ≥ 80% de linhas para `apps/web/src/features/spaces/api/remove-space-member.ts`, `apps/web/src/features/spaces/api/add-space-member.ts`, `apps/web/src/features/spaces/components/space-members.tsx` e `apps/web/src/features/spaces/components/space-view.tsx`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 3 — e2e da jornada e documentação: a fatia utilizável de ponta a ponta

- [ ] T3.1 — Documentação da arquitetura
  - Arquivos: `docs/architecture.md` (alterar)
  - O que fazer, em pt_BR: §6 (espaços) ganha o parágrafo **"Entrega `free-space-members` (fatia 135)"**: `GET /spaces/{spaceId}/members` serve também o espaço livre (dono e membros; dono primeiro); `SpaceMember.role` (`owner`, `member`, `assigned`); `DELETE /spaces/{spaceId}/members/{personId}` idempotente, só o dono, ordem 401 → 404 → 403 → 400 → 204; o removido recebe 404 e perde o espaço da barra lateral; a lista é refeita após adicionar ou remover (invalidação de `['space-members', spaceId]`).
  - Skills: —
  - Complexidade: baixa

- [ ] T3.2 — Testes da fase 3 (e2e: dono remove pelo teclado, removido perde o acesso)
  - Arquivos: `apps/web/e2e/tests/free-space-members.spec.ts` (criar)
  - O que fazer (D7): API simulada com semente por `page.addInitScript`, segundo contexto como em `apps/web/e2e/tests/free-space-invite.spec.ts`. `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo; `toBeFocused()` antes de cada `Enter`/`Space`; nenhum `waitForTimeout`; nenhuma regra do axe desativada; outros specs e `apps/web/e2e/a11y.ts` intocados.
    - `the owner sees the people of a free space and removes a member using only the keyboard`: abre o espaço livre, vê "Pessoas neste espaço" com o selo "dono", foca "Remover {nome}", confirma em "Remover", vê "{nome} foi removida do espaço." e a linha some, `expectNoSeriousA11yViolations(page)`.
    - `the removed person no longer sees the space`: segundo contexto como a pessoa removida; abre a URL do espaço e vê `Espaço não encontrado.`; o espaço não está na seção "Espaços" da barra lateral; `expectNoSeriousA11yViolations(page)`.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [ ] CA3.1 — `pnpm test:e2e` na raiz sai com 0 (os e2e antigos e os novos), e `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` continuam saindo com 0 e sem aviso.
- [ ] CA3.2 — `apps/web/e2e/tests/free-space-members.spec.ts` contém os casos `the owner sees the people of a free space and removes a member using only the keyboard` e `the removed person no longer sees the space`, e `pnpm --filter web exec playwright test --list` os lista. `rg -n "test\.skip|test\.only|waitForTimeout|disableRules" apps/web/e2e/tests/free-space-members.spec.ts` é vazio.
- [ ] CA3.3 — Lendo o spec: `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo; a primeira asserção após mudança de rota usa `ROUTE_TIMEOUT`; asserção seguinte com timeout maior é aceita; `toBeFocused()` antes de cada tecla de ação; `expectNoSeriousA11yViolations(page)` nos dois casos; o primeiro assere `Pessoas neste espaço` e ` foi removida do espaço.`; o segundo usa um segundo contexto de navegador e assere `Espaço não encontrado.` e a ausência do espaço na barra lateral.
- [ ] CA3.4 — `docs/architecture.md` tem o parágrafo "Entrega `free-space-members` (fatia 135)" citando `DELETE /spaces/{spaceId}/members/{personId}`, `role` e `['space-members', spaceId]`.
- [ ] CA3.5 — A fatia está utilizável de ponta a ponta: `pnpm exec vitest run --project api` (0) inclui `DELETE space member answers 204 and removes the row` e `GET space members lists a FREE space to its member`; `pnpm exec vitest run --project web` (0) inclui `removing a member announces it and removes the row`; `pnpm test:e2e` (0) inclui os dois casos de `free-space-members.spec.ts`.

## Desvios esperados

- DV1 — Testes da 128 (`unit-space-members`) em `apps/api/src/spaces/__tests__/*` e `apps/web/src/features/spaces/**/__tests__/*` que comparam membros por igualdade estrita ganham `role: 'assigned'` no esperado, **sem renomear** o caso. Motivo: `role` é campo obrigatório novo de `SpaceMember` (D2). O agente lista aqui cada caso alterado.
- DV2 — O caso `GET space members still answers 404 for a FREE space to its member` (134) é substituído por `GET space members lists a FREE space to its member`. Motivo: esta fatia abre a lista do espaço livre aos membros (R1).

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
