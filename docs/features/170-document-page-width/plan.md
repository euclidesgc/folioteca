# PLAN 170 — document-page-width

Branch: `feature/170-document-page-width`

Fonte: `docs/features/170-document-page-width/spec.md` (D1…D6 são as decisões técnicas da SPEC e R1…R10 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

O documento passa a ser uma folha branca sobre um fundo cinza claro, e a pessoa escolhe a largura da folha num menu "Largura da página" na barra do documento: Pequena (`max-w-2xl`, a de hoje), Média (`max-w-4xl`, padrão), Grande (`max-w-6xl`) ou Completa (`max-w-none`). A escolha é da pessoa (coluna `Person.documentPageWidth`), vale para todos os documentos, sessões e aparelhos, é lida em `GET /auth/me` e gravada em `PATCH /auth/me/preferences`. No web a folha muda na hora (otimista, sem desfazer em caso de falha). Abaixo de `sm` a folha ocupa a largura toda, sem rolagem horizontal.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; banco de teste `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`.

**Critérios sobre o disco.** Os critérios são conferidos **antes** do commit da fase, sobre os arquivos em disco (commitados ou não). Nenhum critério depende de `git diff` contra a base. Critério de ausência (`rg` vazio) vale só para os arquivos criados ou alterados na própria fase, listados no próprio critério, e usa padrão que não casa com comentário nem com atributo parecido (ex.: `disabled` casa dentro de `aria-disabled`; por isso usa `rg -P` com lookbehind). O commit de cada fase é feito pela orquestração depois da revisão; nenhum agente de fase commita. Se `pnpm test` ou `pnpm test:e2e` falhar num teste **antigo** e sem relação com a fase, o comando pode ser repetido **uma** vez; a segunda execução vale.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda (nada de `@radix-ui/react-dropdown-menu`, D4).
- **Migration escrita à mão**: só a pasta nova `apps/api/prisma/migrations/0020_person_document_page_width/`. Nenhum subcomando de Prisma além de `prisma generate` e da aplicação das migrations já usada pelos testes; **nunca** `prisma migrate diff`, `migrate dev` ou `migrate reset`. Nenhuma migration antiga muda.
- **Nenhum teste remove, desativa ou recria restrição do banco** (`DROP CONSTRAINT`, `ALTER TABLE … DISABLE`, `session_replication_role`).
- O contrato é **OpenAPI 3.1**: valor fechado por `enum`; **nunca** `nullable`. O arquivo `packages/api-contract/src/generated/openapi.d.ts` só muda por `pnpm --filter @folioteca/api-contract generate`; nunca editado à mão.
- **Sem `localStorage`** (nem `sessionStorage`) para a preferência; o store Zustand não é persistido (D1, D3).
- **Sem Prettier reformatando arquivo existente**: só as linhas necessárias mudam.
- Todo componente devolve `React.JSX.Element` (ou `React.JSX.Element | null`), nunca o `JSX` global; `ref` é prop comum (sem `forwardRef`); todo botão nosso é o componente `Button` de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case; nenhum import entre features. Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Nenhum `disabled` nativo nos rádios do menu: durante o envio, `aria-disabled="true"` e a troca ignorada no `onChange`, com o foco preservado (D4).
- Nenhum `style={{…}}` para aparência; as larguras são classes Tailwind escritas por extenso (D5).
- Nenhum `eslint-disable` sem `-- <motivo>` na própria linha; nenhuma regra desativada para fazer teste passar; nenhum `any` sem comentário.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado, medida **só** por `coverage/coverage-summary.json`, gerado **na raiz** com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`, **sem `--project`** e **sem filtro de caminho**. Não contam arquivos só de tipos nem os já excluídos pelo `vitest.config.ts` da raiz. Nenhuma exclusão nova.
- Nos e2e: API simulada, estado inicial por `page.addInitScript`; `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo do spec, e a **primeira** asserção após cada mudança de rota usa `ROUTE_TIMEOUT`; `toBeFocused()` antes de cada `Enter`/`Escape`/`Tab`/`Space`/seta; `expectNoSeriousA11yViolations(page)`; nunca `setTimeout`/sleep fixo nem `waitForTimeout`; nunca `test.skip`/`test.only`.
- Testes de API de integração e contrato rodam contra o Postgres real (sem mock do Prisma). "Teste passa" é medido pelo **exit code** (zero), nunca pela saída `--verbose`.
- Testes existentes afetados por esta fatia são ajustados **sem trocar o nome** e registrados na seção "Desvios" no fim do plano (DVn).
- Intocados de propósito: `apps/api/src/access/**`, `apps/web/src/components/ui/**`, `apps/web/src/lib/api-client.ts`, `apps/web/src/lib/auth.tsx`, `apps/web/e2e/a11y.ts`, `apps/web/src/components/layouts/content-layout.tsx` (dívida registrada na SPEC), e todo spec e2e fora dos listados em T3.2.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa.

## Fase 1 — API: preferência de largura da página na pessoa

Caminhos relativos à raiz do repositório. Ao fim da fase, `GET /auth/me` devolve `person.documentPageWidth` (`"medium"` para quem nunca escolheu) e `PATCH /auth/me/preferences` grava a escolha e devolve o usuário atual.

- [x] T1.1 — Coluna no banco e contrato
  - Arquivos: `apps/api/prisma/schema.prisma` (alterar); `apps/api/prisma/migrations/0020_person_document_page_width/migration.sql` (criar); `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D1, D2, R3, R4, R7):
    - `schema.prisma`: `enum DocumentPageWidth { SMALL MEDIUM LARGE FULL }` e, no `model Person`, `documentPageWidth DocumentPageWidth @default(MEDIUM)`.
    - `migration.sql`, escrito à mão com comentário em pt_BR no topo, no estilo da `0019`: `CREATE TYPE "DocumentPageWidth" AS ENUM ('SMALL', 'MEDIUM', 'LARGE', 'FULL');` e `ALTER TABLE "Person" ADD COLUMN "documentPageWidth" "DocumentPageWidth" NOT NULL DEFAULT 'MEDIUM';`. Toda pessoa existente passa a ter Média. Depois, `prisma generate`.
    - `openapi.yaml`: schema `DocumentPageWidth` (`type: string`, `enum: [small, medium, large, full]`, `description` em pt_BR); `CurrentUser.person.documentPageWidth` com `$ref` a ele e na lista `required`; schema `UpdatePreferencesBody` (`documentPageWidth` obrigatório); path `/auth/me/preferences` com `patch`, `operationId: updateCurrentUserPreferences`, respostas `200` (`CurrentUserResponse`), `400` (`Error`, exemplo "Escolha uma largura de página válida.") e `401` (`Error`). Sem `nullable`. Regenerar com `pnpm --filter @folioteca/api-contract generate`.
  - Skills: —
  - Complexidade: média

- [x] T1.2 — Endpoint `PATCH /auth/me/preferences` e o campo em todo `CurrentUser`
  - Arquivos: `apps/api/src/auth/update-preferences.schema.ts` (criar); `apps/api/src/auth/session.service.ts` (alterar); `apps/api/src/auth/auth.service.ts` (alterar); `apps/api/src/auth/auth.controller.ts` (alterar)
  - O que fazer (D2):
    - `update-preferences.schema.ts`, no padrão de `login.schema.ts`: zod `{ documentPageWidth: enum ['small','medium','large','full'] }`, obrigatório, com a mensagem "Escolha uma largura de página válida." para valor inválido ou ausente; exporta o schema e o tipo do corpo.
    - `session.service.ts`: `toCurrentUser` inclui `person.documentPageWidth` convertido para minúsculas (`SMALL`→`small` etc.). Login, instalação, aceite de convite e `/auth/me` já passam por ele; não mudam.
    - `auth.service.ts`: `updatePreferences(personId: string, body: UpdatePreferencesBody)` faz `prisma.person.update` convertendo para o enum do Prisma, com `include: { organization: true }`, e devolve `toCurrentUser(...)`.
    - `auth.controller.ts` (já `@Controller('auth')`): método com `@Patch('me/preferences')`, `@UseGuards(SessionGuard)`, pessoa por `@CurrentPerson()` e corpo por `@Body()` validado pelo schema; sem `@Param`. Resposta 400 com a mensagem do schema; 401 pelo guard.
  - Skills: security
  - Complexidade: média

- [x] T1.3 — Testes da fase 1
  - Arquivos: `apps/api/src/auth/__tests__/update-preferences.integration.test.ts` (criar); `apps/api/src/auth/__tests__/auth.contract.test.ts` (alterar)
  - O que fazer: contra o Postgres real com `resetDatabase(prisma)` em `beforeEach` e os ajudantes existentes de sessão; casos preexistentes que comparavam `CurrentUser` inteiro ganham o campo sem renomear (DV1).
    - `update-preferences.integration.test.ts` (`integration-testing`): `GET auth me returns medium for a new person`; `PATCH auth me preferences saves large and returns the current user`; `PATCH auth me preferences is returned by the next GET auth me`; `PATCH auth me preferences accepts each of small medium large full`; `PATCH auth me preferences rejects an unknown width with 400`; `PATCH auth me preferences rejects a missing width with 400`; `PATCH auth me preferences without a session answers 401`; `PATCH auth me preferences changes only the current person`.
    - `auth.contract.test.ts`: `updateCurrentUserPreferences path and method match the controller`; `CurrentUser requires documentPageWidth as an enum without nullable`.
  - Skills: integration-testing, unit-testing
  - Complexidade: média

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules`): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e sem aviso.
- [x] CA1.2 — `apps/api/prisma/schema.prisma` tem `enum DocumentPageWidth` com `SMALL`, `MEDIUM`, `LARGE`, `FULL` e, em `model Person`, `documentPageWidth DocumentPageWidth @default(MEDIUM)`. Existe `apps/api/prisma/migrations/0020_person_document_page_width/migration.sql` com `CREATE TYPE "DocumentPageWidth" AS ENUM` e `ADD COLUMN "documentPageWidth"` com `NOT NULL DEFAULT 'MEDIUM'`, começando por um comentário `--` em pt_BR. `ls apps/api/prisma/migrations` mostra as pastas de antes mais só a `0020_person_document_page_width`. `rg -n "migrate (diff|dev|reset)|DROP CONSTRAINT|DISABLE TRIGGER|session_replication_role" apps/api/src/auth/__tests__/update-preferences.integration.test.ts apps/api/src/auth/__tests__/auth.contract.test.ts apps/api/prisma/migrations/0020_person_document_page_width/migration.sql` é vazio.
- [x] CA1.3 — `packages/api-contract/openapi.yaml`: schema `DocumentPageWidth` com `type: string` e `enum` `small`, `medium`, `large`, `full`; `CurrentUser.person` lista `documentPageWidth` em `required`; existe `UpdatePreferencesBody` com `documentPageWidth` obrigatório; o path `/auth/me/preferences` tem `patch` com `operationId: updateCurrentUserPreferences` e respostas `200`, `400`, `401`. `rg -n "nullable" packages/api-contract/openapi.yaml` não traz linha desta fase (nenhuma em `DocumentPageWidth`, `UpdatePreferencesBody` ou no path novo). `packages/api-contract/src/generated/openapi.d.ts` contém `updateCurrentUserPreferences` e `"/auth/me/preferences"`.
- [x] CA1.4 — Identidade de caminho: `apps/api/src/auth/auth.controller.ts` é `@Controller('auth')` e tem `@Patch('me/preferences')` com `@UseGuards(SessionGuard)` no mesmo método, que recebe `@CurrentPerson()` e `@Body()` e nenhum `@Param`; `auth` + `me/preferences` = `/auth/me/preferences`, igual ao YAML.
- [x] CA1.5 — `apps/api/src/auth/update-preferences.schema.ts` contém `Escolha uma largura de página válida.` e os quatro valores `small`, `medium`, `large`, `full`; `apps/api/src/auth/auth.service.ts` declara `updatePreferences(personId: string, body: UpdatePreferencesBody)` (ou tipo de corpo equivalente importado do schema) e usa `include: { organization: true }`; `apps/api/src/auth/session.service.ts`, em `toCurrentUser`, produz `documentPageWidth` em minúsculas.
- [x] CA1.6 — `pnpm exec vitest run --project api` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/api/src/auth/__tests__/update-preferences.integration.test.ts apps/api/src/auth/__tests__/auth.contract.test.ts`, os 10 casos nomeados em T1.3 com os nomes literais (`update-preferences.integration.test.ts`: 8; `auth.contract.test.ts`: 2). `rg -n "vi\.mock\(.*prisma" apps/api/src/auth/__tests__/update-preferences.integration.test.ts` é vazio.
- [x] CA1.7 — Lendo os testes: `PATCH auth me preferences is returned by the next GET auth me` faz o `PATCH` e depois um `GET /auth/me` na mesma sessão e assere o valor novo; `PATCH auth me preferences changes only the current person` assere que outra pessoa continua `medium`; os dois casos 400 asserem a mensagem `Escolha uma largura de página válida.`; `updateCurrentUserPreferences path and method match the controller` compara o caminho e o método do YAML com o `@Controller` e o `@Patch` do controller.
- [x] CA1.8 — Cobertura ≥ 80% de linhas para `apps/api/src/auth/update-preferences.schema.ts`, `apps/api/src/auth/session.service.ts`, `apps/api/src/auth/auth.service.ts` e `apps/api/src/auth/auth.controller.ts`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 2 — Web: folha do documento e menu "Largura da página"

Caminhos relativos à raiz do repositório. A ordem importa: tipos e API simulada, depois utilitário, store e mutação, e só então a tela. Ao fim da fase, no app com API simulada, o documento aparece como folha branca em Média sobre fundo cinza, e o menu troca a largura na hora.

- [x] T2.1 — Tipos e API simulada da preferência
  - Arquivos: `apps/web/src/types/api.ts` (alterar); `apps/web/src/testing/mocks/db.ts` (alterar); `apps/web/src/testing/mocks/handlers/auth.ts` (alterar); `apps/web/src/testing/mocks/handlers/installation.ts`, `apps/web/src/testing/mocks/handlers/invitations.ts` (alterar)
  - O que fazer (D2): `types/api.ts` exporta `DocumentPageWidth` e `UpdatePreferencesBody` do contrato gerado. `db.ts`: a pessoa falsa ganha `documentPageWidth` (padrão `'medium'`) e a função `updatePersonPreferences(personId, body)`. `handlers/auth.ts`: os corpos de `/auth/me` e do login levam o campo; handler novo `PATCH ${env.API_URL}/auth/me/preferences` que responde 401 sem sessão, 400 `{ message: 'Escolha uma largura de página válida.' }` para valor fora dos quatro e 200 com o usuário atual. `installation.ts` e `invitations.ts`: os corpos com `CurrentUser` levam o campo.
  - Skills: api-mocking
  - Complexidade: baixa

- [x] T2.2 — Larguras, store da sessão e mutação otimista
  - Arquivos: `apps/web/src/features/documents/utils/page-width.ts` (criar); `apps/web/src/features/documents/stores/page-width-store.ts` (criar); `apps/web/src/features/documents/api/update-page-width.ts` (criar)
  - O que fazer (D3, D5, R3, R6):
    - `page-width.ts`: `PAGE_WIDTHS` na ordem `small`, `medium`, `large`, `full`; rótulos "Pequena", "Média", "Grande", "Completa"; classes `max-w-2xl`, `max-w-4xl`, `max-w-6xl`, `max-w-none`, escritas por extenso; funções `pageWidthLabel(width: DocumentPageWidth): string` e `pageWidthClass(width: DocumentPageWidth): string`.
    - `page-width-store.ts`: store Zustand em memória, **sem `persist`**, com `sessionChoice: { personId: string; width: DocumentPageWidth } | null` e `setSessionChoice`; `usePageWidth(): DocumentPageWidth` devolve `sessionChoice.width` quando `sessionChoice.personId === user.person.id` (de `useUser()`, `@/lib/auth`), senão `user.person.documentPageWidth`.
    - `update-page-width.ts`: fetcher `updatePageWidth(body: UpdatePreferencesBody)` com `PATCH /auth/me/preferences` pelo `api` de `@/lib/api-client`; `useUpdatePageWidth({ mutationConfig })`: `onMutate` grava no store (a folha muda na hora); `onSuccess` faz `await queryClient.invalidateQueries(getUserQueryOptions())` **antes** do `onSuccess` do chamador; `onError` **não desfaz** (a notificação é a do interceptor, como em `update-favorite.ts`).
  - Skills: client-state, api-requests, error-handling, unit-testing
  - Complexidade: média

- [x] T2.3 — Menu "Largura da página", folha do documento e nova ordem das ações
  - Arquivos: `apps/web/src/features/documents/components/page-width-menu.tsx` (criar); `apps/web/src/features/documents/components/document-view.tsx` (alterar)
  - O que fazer (D4, D5, D6, R1, R2, R5, R8, R9, R10). Aparência pelo `docs/design.md` e pelo piso de `interface-design`; as receitas novas entram no `docs/design.md` na fase 3:
    - `page-width-menu.tsx` (`PageWidthMenu`): contêiner relativo com um `Button` secundário "Largura da página" com `aria-expanded` e `aria-controls` apontando para o painel. Aberto, um painel abaixo do botão, alinhado à direita dele, com a receita "Escolha entre opções (rádios)": `fieldset` com `legend` "Largura da página" visualmente oculta e quatro rádios nativos de mesmo `name`, "Pequena", "Média", "Grande", "Completa"; abaixo, uma frase `aria-live` "A largura vale para todos os seus documentos." seguida de " Salvando…" enquanto envia. Comportamento: ao abrir, o foco vai para o rádio **marcado**; setas trocam a opção (nativo) e cada mudança chama `useUpdatePageWidth` (a largura muda na hora); o painel continua aberto após a escolha; Esc, clique fora ou o próprio botão fecham e **devolvem o foco ao botão**; Tab para fora do painel fecha sem roubar o foco. O marcado é derivado (`isPending ? variables.documentPageWidth : usePageWidth()`), nunca copiado para estado. Durante o envio: rádios com `aria-disabled="true"` e troca ignorada no `onChange`, foco mantido; **nunca `disabled`**. Falha: sem alerta dentro do menu (a notificação é do interceptor); sucesso: sem notificação.
    - `document-view.tsx`, em `LoadedDocument`: o `<main id="main-content">` deixa de ser a moldura estreita e vira o fundo cinza claro ("Fundo do documento"); dentro, a folha `<div data-page-width={width}>` ("Folha do documento": branca, borda sutil, sombra leve, centralizada, sem altura fixa, sem borda lateral e sem canto abaixo de `sm`) com a classe de `pageWidthClass(usePageWidth())`. Dentro da folha, na ordem: aviso da lixeira (inalterado), "Linha de ações do documento", indicador de salvamento, editor ("Carregando editor…" dentro da folha). Ações fora da lixeira, na ordem: `PageWidthMenu` · `ShareDocumentDialog` (dono) · `FavoriteButton` · `TrashDocumentButton` (dono, **última**). Na lixeira o bloco de ações existe com **só** `PageWidthMenu`. Quem só vê também tem o menu. Carregando/erro/não encontrado continuam na moldura atual, com os textos "Carregando documento…", "Não foi possível carregar o documento.", "Tentar novamente", "Documento não encontrado".
  - Skills: interface-design, component-robustness
  - Complexidade: alta

- [x] T2.4 — Testes da fase 2
  - Arquivos: `apps/web/src/features/documents/utils/__tests__/page-width.test.ts` (criar); `apps/web/src/features/documents/stores/__tests__/page-width-store.test.ts` (criar); `apps/web/src/features/documents/api/__tests__/update-page-width.test.tsx` (criar); `apps/web/src/features/documents/components/__tests__/page-width-menu.test.tsx` (criar); `apps/web/src/features/documents/components/__tests__/document-view.test.tsx` (alterar)
  - O que fazer: banco falso pelos ajudantes; `userEvent`; localizar por papel e nome acessível em pt_BR (botão por `getByRole('button', { name: 'Largura da página' })`, rádios por `getByRole('radio', { name: 'Média' })` etc.); esperas com `findBy…`/`waitFor`, nunca espera fixa; o store é zerado entre os testes; sem `console.error`/`console.warn`/aviso de `act(...)`. Casos antigos de `document-view.test.tsx` que dependiam da estrela ser a última ação ou da moldura `max-w-2xl` são ajustados sem renomear (DV2).
    - `page-width.test.ts` (`unit-testing`): `lists the widths in order small medium large full`; `maps each width to its pt_BR label`; `maps each width to its max-width class`.
    - `page-width-store.test.ts` (`unit-testing`): `usePageWidth returns the server value when there is no session choice`; `usePageWidth returns the session choice for the same person`; `usePageWidth ignores a session choice from another person`.
    - `update-page-width.test.tsx` (`unit-testing`): `useUpdatePageWidth sets the session choice before the request resolves`; `useUpdatePageWidth awaits the user invalidation before the caller onSuccess`; `useUpdatePageWidth keeps the chosen width after a failed request`.
    - `page-width-menu.test.tsx` (`component-testing`): `the button exposes aria-expanded and aria-controls`; `opening the menu focuses the checked radio`; `arrow keys change the width right away`; `clicking an option changes the width and keeps the menu open`; `Escape closes the menu and returns focus to the button`; `clicking outside closes the menu`; `while saving the radios are aria-disabled and never disabled`; `a failed save keeps the new width and shows the notification`.
    - `document-view.test.tsx` (`integration-testing`): `the sheet uses the medium width by default`; `a viewer also sees Largura da página`; `a trashed document shows only Largura da página in the actions`; `the actions are ordered width share favorite trash`; `choosing Grande widens the sheet without reloading`.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` saem com 0 e sem aviso; a saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)`.
- [x] CA2.2 — `apps/web/src/types/api.ts` exporta `DocumentPageWidth` e `UpdatePreferencesBody`. `apps/web/src/testing/mocks/db.ts` contém `documentPageWidth` e `updatePersonPreferences`; `apps/web/src/testing/mocks/handlers/auth.ts` contém `/auth/me/preferences`, `http.patch` e `Escolha uma largura de página válida.`; `installation.ts` e `invitations.ts` (em `apps/web/src/testing/mocks/handlers/`) contêm `documentPageWidth`.
- [x] CA2.3 — `apps/web/src/features/documents/utils/page-width.ts` contém `max-w-2xl`, `max-w-4xl`, `max-w-6xl`, `max-w-none`, `Pequena`, `Média`, `Grande`, `Completa` e exporta `PAGE_WIDTHS`, `pageWidthLabel` e `pageWidthClass`. `apps/web/src/features/documents/stores/page-width-store.ts` exporta `usePageWidth` e contém `sessionChoice` e `personId`. `apps/web/src/features/documents/api/update-page-width.ts` exporta `updatePageWidth` e `useUpdatePageWidth`, contém `/auth/me/preferences`, `onMutate` e `await queryClient.invalidateQueries(` antes da chamada ao `onSuccess` do `mutationConfig`, e não tem `onError` que restaure o valor anterior.
- [x] CA2.4 — Sem armazenamento local: `rg -n -P "(?<![\w.])(localStorage|sessionStorage|persist)\b" apps/web/src/features/documents/stores/page-width-store.ts apps/web/src/features/documents/api/update-page-width.ts apps/web/src/features/documents/components/page-width-menu.tsx apps/web/src/features/documents/components/document-view.tsx` não casa código (só comentário, se houver, e nenhum import de `zustand/middleware`).
- [x] CA2.5 — `apps/web/src/features/documents/components/page-width-menu.tsx` contém literalmente `Largura da página`, `aria-expanded`, `aria-controls`, `aria-disabled`, `aria-live`, `A largura vale para todos os seus documentos.`, ` Salvando…`, `type="radio"`, `fieldset`, `legend`, `sr-only`, `'Escape'`, e devolve o foco ao botão por `ref` (`.focus()`); o foco inicial vai para o rádio marcado. `rg -n -P "(?<![-\w])disabled\b" apps/web/src/features/documents/components/page-width-menu.tsx` é vazio (o lookbehind não casa `aria-disabled`); `rg -n "useState\(.*(width|Width)" apps/web/src/features/documents/components/page-width-menu.tsx` é vazio (marcado derivado, nunca copiado).
- [x] CA2.6 — Acabamento (piso de `interface-design`, lendo o código): em `document-view.tsx`, o `<main id="main-content">` de `LoadedDocument` tem `bg-gray-100`, `px-0` e `sm:px-4`; a folha tem `data-page-width`, `mx-auto`, `w-full`, `bg-white`, `border-gray-200`, `shadow-sm`, `sm:rounded-md`, `sm:border-x` e a classe vinda de `pageWidthClass(`; `max-w-2xl` só aparece nos estados carregando/erro/não encontrado. No menu, o botão é o `Button` com `variant="secondary"` e o painel tem `absolute`, `right-0`, `top-full`, `z-20`, `max-w-[calc(100vw-2rem)]`, `rounded-md`, `border`, `bg-white` e `shadow-lg`. A ordem no JSX de `document-view.tsx` é `PageWidthMenu`, `ShareDocumentDialog`, `FavoriteButton`, `TrashDocumentButton`, e o ramo da lixeira renderiza `PageWidthMenu`. `rg -n -P "<button|<a href|style=\{\{|!important|forwardRef|: JSX\." apps/web/src/features/documents/components/page-width-menu.tsx apps/web/src/features/documents/components/document-view.tsx` é vazio.
- [x] CA2.7 — `pnpm exec vitest run --project web` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/web/src/features/documents`, os 22 casos nomeados em T2.4 com os nomes literais (`page-width.test.ts`: 3; `page-width-store.test.ts`: 3; `update-page-width.test.tsx`: 3; `page-width-menu.test.tsx`: 8; `document-view.test.tsx`: 5).
- [x] CA2.8 — Lendo os testes: `opening the menu focuses the checked radio` assere `toHaveFocus()` no rádio "Média"; `Escape closes the menu and returns focus to the button` assere `aria-expanded="false"` e `toHaveFocus()` no botão "Largura da página"; `while saving the radios are aria-disabled and never disabled` assere `aria-disabled="true"` e `not.toBeDisabled()`; `a failed save keeps the new width and shows the notification` força erro no `PATCH` e assere a largura nova mantida e a notificação; `usePageWidth ignores a session choice from another person` grava escolha com outro `personId` e assere o valor do servidor; `useUpdatePageWidth awaits the user invalidation before the caller onSuccess` assere a ordem; `the sheet uses the medium width by default` assere `data-page-width="medium"`; `the actions are ordered width share favorite trash` assere a ordem dos quatro botões.
- [x] CA2.9 — `rg -n "sleep\(|waitForTimeout" apps/web/src/features/documents/components/__tests__/page-width-menu.test.tsx apps/web/src/features/documents/components/__tests__/document-view.test.tsx apps/web/src/features/documents/api/__tests__/update-page-width.test.tsx` é vazio; `rg -n "eslint-disable" apps/web/src/features/documents apps/web/src/testing/mocks | rg -v -- "--"` é vazio. Os desvios que tenham ocorrido estão na seção "Desvios", com arquivo e nome do caso.
- [x] CA2.10 — Cobertura ≥ 80% de linhas para `apps/web/src/features/documents/utils/page-width.ts`, `apps/web/src/features/documents/stores/page-width-store.ts`, `apps/web/src/features/documents/api/update-page-width.ts`, `apps/web/src/features/documents/components/page-width-menu.tsx` e `apps/web/src/features/documents/components/document-view.tsx`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 3 — e2e da jornada e `docs/design.md`: a fatia utilizável de ponta a ponta

- [x] T3.1 — Receitas no `docs/design.md`
  - Arquivos: `docs/design.md` (alterar)
  - O que fazer, só acrescentando/ajustando linhas em "Padrões acrescentados pelas entregas" (fatia 170): receita **"Fundo do documento"** (`<main id="main-content">` com `min-h-full bg-gray-100 px-0 py-4 sm:px-4 sm:py-8 md:px-6`); receita **"Folha do documento"** (`mx-auto w-full border-y border-gray-200 bg-white p-4 shadow-sm sm:rounded-md sm:border-x sm:p-8` + largura: Pequena `max-w-2xl`, Média `max-w-4xl`, Grande `max-w-6xl`, Completa `max-w-none`; sem altura fixa; linha de ações, aviso da lixeira e indicador de salvamento dentro dela); receita **"Menu de rádios"** (a SPEC chama de "Menu de escolha"; ainda não existe): contêiner `relative`, botão secundário com `aria-expanded`/`aria-controls`, painel `absolute right-0 top-full z-20 mt-2 w-64 max-w-[calc(100vw-2rem)] rounded-md border border-gray-200 bg-white p-3 shadow-lg` com a receita "Escolha entre opções (rádios)" dentro, foco no marcado ao abrir, Esc/clique fora fecha e devolve o foco ao botão, `aria-disabled` durante o envio; e a **"Linha de ações do documento"** atualizada (ordem Largura da página · Compartilhar · estrela · Mover para a lixeira; na lixeira só "Largura da página").
  - Skills: interface-design
  - Complexidade: baixa

- [x] T3.2 — Testes da fase 3 (e2e: escolher a largura, recarregar, outro documento, 360px, acessibilidade)
  - Arquivos: `apps/web/e2e/tests/document-page-width.spec.ts` (criar); `apps/web/e2e/tests/block-editor.spec.ts`, `apps/web/e2e/tests/favorites.spec.ts`, `apps/web/e2e/tests/trash.spec.ts` (alterar, só se quebrarem pela nova ordem de Tab)
  - O que fazer: API simulada, estado inicial por `page.addInitScript`, como os specs existentes; `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo; `toBeFocused()` antes de cada tecla; nenhum `waitForTimeout`, `test.skip` ou `test.only`; `apps/web/e2e/a11y.ts` intocado.
    - `document-page-width.spec.ts`:
      - `the owner picks Grande and it sticks on another document and back on the first`: abre um documento, vê `data-page-width="medium"`; pelo teclado chega ao botão "Largura da página" (`toBeFocused()`), Enter; o rádio "Média" está focado; seta para baixo (`toBeFocused()` antes) marca "Grande" e a folha passa a `data-page-width="large"`; Escape (`toBeFocused()` no rádio antes) fecha e o botão fica focado; abre outro documento e vê `large`; volta ao primeiro e vê `large`.
      - `the menu has no serious accessibility violations when open`: com o menu aberto, `expectNoSeriousA11yViolations(page)`. Se o editor estiver na tela, a única exclusão aceita é `disableRulesWithin` com `selector: '.bn-container'` e `rules: ['aria-input-field-name']` (violação conhecida do BlockNote, item 053 do roadmap); nada além.
      - `the sheet fits a 360px screen without horizontal scroll`: viewport 360px de largura, abre o documento e assere `document.documentElement.scrollWidth <= window.innerWidth`.
    - Ordem de Tab na barra muda de título → Compartilhar → Mover para a lixeira → estrela → editor para título → **Largura da página** → Compartilhar → **estrela** → **Mover para a lixeira** → editor. Specs antigos que navegam na barra pelo teclado e podem quebrar: `block-editor.spec.ts` (`writes with the keyboard only, sees Salvo and finds the text again after leaving and coming back`, `shows the Portuguese slash menu without table or media blocks`: Tabs do título até o editor), `favorites.spec.ts` (`marks a document as favorite from the keyboard, ...`: Tabs do título até a estrela), `trash.spec.ts` (`moves a document to the trash from the keyboard, ...`: Tabs até a estrela e até a lixeira). Se quebrarem, ajustar só a sequência de teclas para a ordem nova, com `toBeFocused()` em cada parada, sem renomear nem pular (DV3). Nenhum outro spec muda.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [x] CA3.1 — `pnpm test:e2e` na raiz sai com 0 (a suíte inteira), e `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` continuam saindo com 0 e sem aviso, com o cache do `tsc` limpo.
- [x] CA3.2 — `apps/web/e2e/tests/document-page-width.spec.ts` contém os casos `the owner picks Grande and it sticks on another document and back on the first`, `the menu has no serious accessibility violations when open` e `the sheet fits a 360px screen without horizontal scroll`, e `pnpm --filter web exec playwright test --list` os lista. `rg -n "test\.skip|test\.only|waitForTimeout" apps/web/e2e/tests/document-page-width.spec.ts apps/web/e2e/tests/block-editor.spec.ts apps/web/e2e/tests/favorites.spec.ts apps/web/e2e/tests/trash.spec.ts` é vazio. Em `document-page-width.spec.ts`, `rg -n "disableRules" ` só traz, se houver, `disableRulesWithin` com `selector: '.bn-container'` e `rules: ['aria-input-field-name']`.
- [x] CA3.3 — Lendo o spec novo: `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo; a primeira asserção após cada mudança de rota (abrir, outro documento, volta ao primeiro — DV6) usa `ROUTE_TIMEOUT`; `toBeFocused()` antes de cada `Enter`, `Escape`, `Tab` e seta; o rádio "Média" é asserido focado ao abrir; após o Escape o botão "Largura da página" é asserido focado; `expectNoSeriousA11yViolations(page)` é chamado com o menu aberto (depois de asserir `aria-expanded="true"`); a folha é conferida por `data-page-width`; o caso de 360px usa `setViewportSize` (ou `test.use`) com largura 360 e compara `scrollWidth` com `innerWidth`; nenhuma largura guardada no navegador: `rg -n "localStorage" apps/web/src --glob '!**/testing/**'` é vazio, e no spec novo `localStorage` só aparece no bloco de estado inicial (`page.addInitScript` do `beforeEach`, com as chaves `mock-*`), como nos specs existentes (DV7).
- [x] CA3.4 — `docs/design.md`, em "Padrões acrescentados pelas entregas", tem as receitas "Fundo do documento", "Folha do documento" (com `max-w-2xl`, `max-w-4xl`, `max-w-6xl`, `max-w-none` e `sm:border-x`) e "Menu de rádios" (com `aria-expanded`, `aria-controls`, `max-w-[calc(100vw-2rem)]` e a devolução do foco ao botão); a "Linha de ações do documento" cita "Largura da página" e diz que na lixeira só ele aparece.
- [x] CA3.5 — Os casos dos specs antigos ajustados mantêm os nomes de antes (conferido pelos DV3 registrados); nenhum spec fora de `document-page-width.spec.ts`, `block-editor.spec.ts`, `favorites.spec.ts` e `trash.spec.ts` mudou nesta fase.
- [x] CA3.6 — A fatia está utilizável de ponta a ponta: `pnpm exec vitest run --project api` (0) inclui `PATCH auth me preferences is returned by the next GET auth me`; `pnpm exec vitest run --project web` (0) inclui `opening the menu focuses the checked radio` e `choosing Grande widens the sheet without reloading`; `pnpm test:e2e` (0) inclui `the owner picks Grande and it sticks on another document and back on the first`.

## Desvios

Preenchido pelos agentes de fase quando um teste existente precisar de ajuste (sem renomear). Previstos:

- DV1 — `apps/api/src/auth/__tests__/` (e outros testes de API que comparem `CurrentUser` inteiro): passam a esperar `documentPageWidth: 'medium'`.
- DV2 — `apps/web/src/features/documents/components/__tests__/document-view.test.tsx`: casos que dependiam da estrela como última ação ou da moldura `max-w-2xl` no documento carregado.
- DV3 — `apps/web/e2e/tests/block-editor.spec.ts`, `favorites.spec.ts`, `trash.spec.ts`: sequência de Tab ajustada à ordem nova da barra. Fase 3: nos dois casos de `block-editor.spec.ts` (`writes with the keyboard only, sees Salvo and finds the text again after leaving and coming back`, `shows the Portuguese slash menu without table or media blocks`) o caminho do título ao editor passou a título → Largura da página → Compartilhar → Adicionar aos favoritos → Mover para a lixeira → editor; em `favorites.spec.ts` (`marks a document as favorite from the keyboard, ...`), nas duas idas do título à estrela, Mover para a lixeira saiu e Largura da página entrou antes de Compartilhar; em `trash.spec.ts` (`moves a document to the trash from the keyboard, ...`), a ida à estrela ganhou a parada em Largura da página e perdeu a da lixeira, a volta estrela → lixeira trocou `Shift+Tab` por `Tab` (com a estrela asserida focada antes), e a segunda ida à lixeira passou por Largura da página e pela estrela. Só teclas e localizadores mudaram, com `toBeFocused()` em cada parada; nenhum caso renomeado nem pulado.
- DV4 — Fase 1, para o `pnpm typecheck` ficar verde com `CurrentUser.person.documentPageWidth` obrigatório: os corpos de `CurrentUser` em `apps/web/src/testing/mocks/handlers/auth.ts` (GET `/auth/me` e login), `installation.ts` e `invitations.ts` passam a levar `documentPageWidth: 'medium'` fixo, e os usuários de teste de `sidebar-admin.test.tsx`, `login-form.test.tsx`, `lib/__tests__/auth.test.tsx` e `lib/__tests__/authorization.test.tsx` ganham o mesmo campo. Nenhum comportamento nem nome de teste mudou; a pessoa falsa com o campo e o handler de `PATCH /auth/me/preferences` continuam na T2.1. Na API, o DV1 atingiu `auth.integration.test.ts`, `login-logout.integration.test.ts` e `installation/__tests__/installation.integration.test.ts`.
- DV5 — Fase 2, `apps/web/src/features/documents/components/__tests__/document-view.test.tsx`: o caso `shows Mover para a lixeira before the favorite button for the owner` quebrou porque a barra nova põe a lixeira por último (regra do produto: ação destrutiva sempre por último). A asserção passou a exigir a lixeira depois da estrela, e o caso foi **renomeado** para `shows Mover para a lixeira after the favorite button for the owner`, porque o nome antigo afirmava o contrário do que o teste verifica (exceção à regra de ajustar sem renomear, pedida na orientação da T2.4). Os demais casos antigos do arquivo passaram sem ajuste; o `beforeEach` passou a zerar o store da largura.

- DV6 — Fase 3, `apps/web/e2e/tests/document-page-width.spec.ts`: a API simulada guarda tudo só na memória da página e volta ao estado inicial a cada recarga, sem mecanismo de persistência entre recargas para nenhuma escrita; a recarga do caso da largura foi trocada por abrir outro documento e voltar ao primeiro (a persistência no servidor está provada pelos testes de API da fase 1, como `PATCH auth me preferences is returned by the next GET auth me`). Como o nome antigo deixou de ser verdadeiro, o caso `the owner picks Grande and it sticks after reload and on another document` passou a se chamar `the owner picks Grande and it sticks on another document and back on the first`; os nomes citados em CA3.2 e CA3.6 valem com o nome novo. O mock não mudou.
- DV7 — Fase 3, CA3.3: a busca por `localStorage` no spec novo conflitava com o estado inicial dos specs (`page.addInitScript` grava as chaves `mock-*` no `localStorage`, único jeito de semear a API simulada). O critério foi reescrito para proibir `localStorage` onde guardaria a largura (código do app fora de `src/testing`) e aceitá-lo no spec só no bloco de estado inicial.

## DoD da entrega

- [x] DoD1 — Todas as tarefas e critérios do plano marcados
- [x] DoD2 — Suíte de testes inteira passa
- [x] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [x] DoD4 — Tipos de todos os `tsconfig` sem erros
- [x] DoD5 — Console dos testes sem erro nem aviso
- [x] DoD6 — `build` passa
- [x] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [x] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [x] DoD9 — Nenhuma worktree ou branch temporária sobrando
