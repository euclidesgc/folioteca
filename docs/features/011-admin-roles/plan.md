# PLAN 011 — admins-list

Branch: `feature/011-admin-roles`, que **sai de `feature/108-unit-assignments-remove`** (entrega empilhada). **Toda** comparação de diff e de "arquivo intocado" deste plano é contra `feature/108-unit-assignments-remove` — nunca contra `develop`, nunca contra `main`. Onde um critério diz `<base>`, leia `feature/108-unit-assignments-remove`.

Fonte: `docs/features/011-admin-roles/spec.md` (D1…D9 são as decisões técnicas da SPEC e R1…R9 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; o banco de teste é `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom é da ferramenta e **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **Nenhuma migration é esperada** (D3): `Person.isAdmin Boolean @default(false)` já existe e é a única coluna que a fatia lê; `apps/api/prisma/**` fica intocado, `schema.prisma` inclusive, e **nenhum índice novo é criado**. Se, na implementação, alguém concluir que precisa de migration, o agente **para e relata o achado ao dono**, sem escrever SQL: significaria que a premissa de "sempre poucas pessoas administram" do PRD não vale. Os únicos subcomandos de Prisma permitidos são `pnpm --filter api exec prisma validate`, `pnpm --filter api exec prisma migrate status` e `pnpm --filter api exec prisma migrate deploy`, sempre contra `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test`; **nunca** `migrate diff`, `migrate dev` ou `migrate reset`.
- Todo componente devolve `React.JSX.Element` (nunca o `JSX` global); `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case. Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Nenhum `eslint-disable` sem `-- <motivo>`. Nenhum segredo em variável `VITE_*`; nenhum token em `localStorage`; nenhum literal com cara de senha, token ou hash em arquivo versionado.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado. Não contam arquivos só de tipos, arquivos com 0 de 0 linhas instrumentáveis, nem os já excluídos pelo `vitest.config.ts` da raiz (`**/types/**`, `**/generated/**`, `**/src/testing/**`, `**/test/**`, `**/*.config.*`). Nenhuma exclusão nova.
- **Toda** espera **dos casos novos** logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes da web pela constante `LAZY_TIMEOUT` declarada no arquivo (`findBy…(…, {}, LAZY_TIMEOUT)`, `waitFor(…, LAZY_TIMEOUT)`); no e2e pela constante `ROUTE_TIMEOUT` declarada no topo do spec. Nunca `setTimeout`/sleep fixo nem `waitForTimeout`. **Casos preexistentes não são alterados nem renomeados.**
- Testes de API são de integração contra o Postgres real (padrão `apps/api/test`, sem mock do Prisma); "teste passa" é medido pelo **exit code** de `npx vitest run --project api` / `--project web` (zero), nunca pela leitura da saída `--verbose`.
- **Só a administração vê a lista.** Os guards ficam **na classe** do controller, o `organizationId` vem sempre de `@CurrentPerson()` (nunca da rota nem da query), e o corpo traz **apenas** `id`, `name` e `email` por `select` explícito — `Person` tem `passwordHash`.
- **Esta fatia não toca `apps/web/src/lib/auth.tsx`** (D8): o `staleTime: Infinity` de `getUserQueryOptions` é assunto da fatia **114**. Nada muda papel aqui, então não há o que o front perceba.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa. Cada fase termina com **um** commit Conventional Commits em inglês (`feat:`, `test:`, `docs:`), sem `--no-verify`.
- Nenhuma tarefa acrescenta escopo: sem promover, sem rebaixar, sem paginação, sem busca, sem filtro na rota, sem `count` no corpo, sem coluna de histórico, sem semente que promova pessoas de exemplo, sem componente novo em `src/components/ui/`.

Desvio em relação à tabela de arquivos da SPEC, sem mudar escopo: `docs/roadmap.md` **não** é tarefa nem critério de fase — a orquestração o registra no encerramento (item 011 concluído com o escopo reduzido, dívidas novas). O `docs/architecture.md` fica na fase 3, junto com o e2e.

## Fase 1 — API: contrato `GET /admins` e o módulo `admin-roles`

Caminhos relativos à raiz do repositório.

- [x] T1.1 — Contrato primeiro: a operação `getAdmins`
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D1, R1, R4):
    - Caminho **novo** `/admins` (sem prefixo `/api`, como todos os caminhos do arquivo), declarado **depois** de `/people`, com a operação `get`: `operationId: getAdmins`, **tag nova `admins`**, **sem parâmetros** (nem de rota, nem de query) e **sem** `requestBody`.
    - Respostas: **200** com o schema `AdminsResponse`; **401** e **403** com o schema `Error`, que já existe. **Não** há `400`, `404` nem `409`.
    - Schemas novos: `AdminPerson`, objeto com **exatamente** as propriedades `id`, `name` e `email` (`type: string`), as três `required`; e `AdminsResponse`, objeto com a propriedade `data` (array de `AdminPerson`), `required`. **Nenhum `count`** no envelope: a contagem é `data.length` na tela, e duas fontes para o mesmo número podem discordar.
    - A `description` da operação, em pt_BR, registra as regras observáveis: só a administração; lista **todas** as pessoas com `isAdmin` da organização de quem chama, sem paginação e sem limite; ordem alfabética por nome com colador pt-BR; nenhuma pessoa de outra organização; ordem de falha CSRF → 401 → 403 (`GET` não passa pelo `CsrfGuard`, que só cobre método de escrita).
    - **Identidade de caminho conferida no YAML inteiro** antes de escrever: `/admins` é um primeiro segmento inédito — nenhum caminho declarado hoje (`/health`, `/installation`, `/auth/*`, `/documents*`, `/org-units*`, `/people`, `/invitations*`) começa por ele. Nenhum caminho existente é renomeado e nenhum parâmetro muda de nome.
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`; o `.d.ts` nunca é editado à mão. `apps/api/test/generated-types.test.ts` e `apps/web/src/types/api.ts` não mudam.
  - Skills: security
  - Complexidade: baixa

- [x] T1.2 — O módulo `admin-roles`: serviço com `select` explícito e colador pt-BR, controller com os guards na classe
  - Arquivos: `apps/api/src/admin-roles/admin-roles.service.ts` (criar); `apps/api/src/admin-roles/admin-roles.controller.ts` (criar); `apps/api/src/admin-roles/admin-roles.module.ts` (criar); `apps/api/src/app.module.ts` (alterar)
  - O que fazer (D2, R1, R2, R4):
    - `admin-roles.service.ts`: constante de módulo `const adminFields = { id: true, name: true, email: true } as const;` com o comentário "Campos que descrevem uma pessoa administradora para quem chama a API."; constante `const collator = new Intl.Collator('pt-BR', { sensitivity: 'base' });` com o comentário explicando que ordenar no banco dependeria da collation do Postgres de cada instância ("Álvaro" viria depois de "Zilda" sob `C`), e que é a mesma decisão, pela mesma razão, de `unit-assignments.service.ts`. Método `list(organizationId: string): Promise<AdminsResponse>` que chama `this.prisma.person.findMany({ where: { organizationId, isAdmin: true }, select: adminFields })` — **sem `orderBy`, sem `take`, sem cursor** — e devolve `{ data: [...admins].sort((a, b) => collator.compare(a.name, b.name) || a.id.localeCompare(b.id)) }`.
    - **`select` explícito, nunca a linha inteira**: `Person` tem `passwordHash`, e o `select` é a barreira. **Nenhum `isUuid` e nenhum 404**: não há id na rota. **Nenhum parâmetro de query**: um `limit` ou um `q` aqui seriam um botão para pedir coisa que a fatia não promete. O desempate por `id` faz a ordem ser total, para que dois homônimos não troquem de lugar entre duas cargas.
    - `admin-roles.controller.ts`: `@Controller('admins')` com `@UseGuards(SessionGuard, AdminGuard)` **na classe** e **nenhum** `@UseGuards` por método (o Nest soma os de método aos de classe, então um decorador vazio não limparia nada) — é essa a garantia de que a regra não dependa de alguém lembrar quando a fatia 114 acrescentar o `PUT`. Um método `@Get()` que obtém o `organizationId` de `@CurrentPerson() person` e devolve `Promise<AdminsResponse>`.
    - `admin-roles.module.ts`: módulo com o controller e o serviço, no molde do `unit-assignments.module.ts`; registrado em `apps/api/src/app.module.ts` como `AdminRolesModule`. **Não** é um método a mais em `PeopleController`: aquele controller é a busca com limite duro, e as fatias 114/115 vão pendurar aqui as escritas de papel.
    - Sem `Logger` e sem `console.` no módulo fora de testes.
  - Skills: authorization, security, project-structure
  - Complexidade: média

- [x] T1.3 — Testes da fase 1
  - Arquivos: `apps/api/src/admin-roles/__tests__/admin-roles.service.test.ts` (criar); `apps/api/src/admin-roles/__tests__/admin-roles.integration.test.ts` (criar); `apps/api/src/admin-roles/__tests__/admin-roles.contract.test.ts` (criar)
  - O que fazer (D9): integração e contrato contra o Postgres real, sem mock do Prisma, `resetDatabase(prisma)` em `beforeEach`, instalação e sessões pelos ajudantes já usados nos testes de `unit-assignments` e `people`. `apps/api/test/**` **não muda**. Nenhuma espera fixa; nenhum literal com cara de senha (as senhas vêm das constantes de teste já existentes).
    - `admin-roles.service.test.ts` (Prisma falso), casos: `list filters by the organization and by isAdmin only` (o `where` é **exatamente** `{ organizationId, isAdmin: true }`); `list selects only id, name and email` (o `select` é `{ id: true, name: true, email: true }` e nada mais); `list sorts in the service, not in the database` (`findMany` é chamado **sem** `orderBy`, e a saída vem ordenada); `namesakes keep a stable order by id`; `an empty database answers an empty data array` (`{ data: [] }`, sem estourar).
    - `admin-roles.integration.test.ts`, casos: `a fresh installation answers 200 with the single installed admin`; `only the admins of the organization come back, sorted with the pt-BR collator` (semear "Álvaro", "Beatriz" e "Zilda" administradoras mais membros; o corpo traz **só** as três, **nesta ordem**, com "Álvaro" **primeiro** — é a asserção que falharia com `ORDER BY name` sob collation `C`); `each item has exactly the id, name and email keys` (`expect(Object.keys(item).sort()).toEqual(['email', 'id', 'name'])` para cada item, nenhum item com `passwordHash`, `isAdmin` ou `createdAt`, e o corpo inteiro serializado **não contém** o hash da senha semeada); `an admin of another organization never shows up`; `a non-admin answers 403` (mensagem "Apenas a administração pode fazer isso."); `an anonymous request answers 401` (mensagem "Sessão não encontrada."); `the role is re-read on every request` — a **mesma** sessão que recebeu 200 recebe **403** depois de um `update` direto em `Person.isAdmin` pelo Prisma.
    - `admin-roles.contract.test.ts`, casos: `GET admins answers the documented 200`; `GET admins answers the documented 401`; `GET admins answers the documented 403` — via `expectMatchesContract`, que falha quando caminho, método ou status não existem no documento.
    - Fecha a fase com um commit, por exemplo `feat(api): list the admins of the organization`.
  - Skills: unit-testing, integration-testing, security
  - Complexidade: média

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (sem aviso de peer no install; sem aviso de chunk grande no build; o aviso de desempenho do Vitest sobre jsdom não conta). A saída de `pnpm test` não tem `console.error` nem `console.warn` vazando, nem aviso do Nest ou do Prisma. `git diff feature/108-unit-assignments-remove... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA1.2 — `packages/api-contract/openapi.yaml` tem o caminho `/admins`, declarado **depois** de `/people`, com a operação `get`, `operationId: getAdmins`, tag `admins`, **nenhum** `parameters` e **nenhum** `requestBody`. As respostas são exatamente `200` (com `AdminsResponse`), `401` e `403` (as duas com o schema `Error`); **não** há `400`, `404` nem `409` nessa operação. Existem os schemas `AdminPerson`, com **exatamente** as propriedades `id`, `name` e `email`, as três em `required` (nenhuma outra propriedade, em especial nada de `isAdmin`), e `AdminsResponse`, com `data` como array de `AdminPerson` e **sem** propriedade `count`. A `description` da operação menciona, em pt_BR, que só a administração obtém a lista, que ela vem inteira (sem paginação e sem limite), ordenada alfabeticamente com colador pt-BR, e que nenhuma pessoa de outra organização aparece. Nenhum caminho preexistente foi renomeado e nenhum parâmetro de caminho mudou de nome (`git diff feature/108-unit-assignments-remove... -- packages/api-contract/openapi.yaml` só acrescenta linhas).
- [x] CA1.3 — Tipos regenerados pelo script, nunca à mão: `rg -n "getAdmins" packages/api-contract/src/generated/openapi.d.ts` encontra a operação e `rg -n "AdminsResponse" packages/api-contract/src/generated/openapi.d.ts` encontra o schema; `apps/api/test/generated-types.test.ts` passa sem ter sido alterado; `git diff feature/108-unit-assignments-remove... -- apps/web/src/types/api.ts apps/api/test` é vazio.
- [x] CA1.4 — Nenhuma migration nova e nenhum comando proibido de Prisma: `git diff --stat feature/108-unit-assignments-remove... -- apps/api/prisma` é vazio (nem `schema.prisma`, nem diretório novo em `migrations/`), e `git status --porcelain -uall apps/api/prisma` não lista nenhum arquivo. `git diff feature/108-unit-assignments-remove... -- apps/api apps/web packages | rg -n "migrate (diff|dev|reset)"` é vazio (`docs/` fica fora porque a documentação cita esses comandos justamente para proibi-los). Nenhum índice novo: `git diff feature/108-unit-assignments-remove... -- apps/api | rg -n "@@index|CREATE INDEX"` é vazio. Conferência de rotina: `pnpm --filter api exec prisma validate` termina com exit code 0 e `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test pnpm --filter api exec prisma migrate status` reporta nenhuma migration pendente. Se o implementador tiver concluído que precisa de migration, a fase **não** é entregue: o achado volta ao dono.
- [x] CA1.5 — `apps/api/src/admin-roles/admin-roles.service.ts` existe (antes do commit, `git status --porcelain -uall apps/api/src/admin-roles` lista os arquivos; depois, `git diff --stat feature/108-unit-assignments-remove... -- apps/api/src/admin-roles` lista os arquivos) e tem `list(organizationId: string): Promise<AdminsResponse>` que chama `this.prisma.person.findMany` com `where` **exatamente** `{ organizationId, isAdmin: true }` e `select` **exatamente** `{ id: true, name: true, email: true }`, **sem** `orderBy`, `take`, `skip` nem `cursor`, e devolve `{ data: … }` ordenado por `Intl.Collator('pt-BR', { sensitivity: 'base' })` com desempate por `a.id.localeCompare(b.id)`. `rg -n "orderBy|take:|cursor|passwordHash|isUuid" apps/api/src/admin-roles/admin-roles.service.ts` é vazio. `rg -n "Logger|console\." apps/api/src/admin-roles --glob '!**/__tests__/**'` é vazio.
- [x] CA1.6 — `apps/api/src/admin-roles/admin-roles.controller.ts` tem `@Controller('admins')` e `@UseGuards(SessionGuard, AdminGuard)` **na classe**, nessa ordem de guards, e **nenhum** `@UseGuards` por método (`rg -n "@UseGuards" apps/api/src/admin-roles/admin-roles.controller.ts` devolve exatamente uma ocorrência, acima da declaração da classe). O único método tem `@Get()` sem argumento de caminho, obtém o `organizationId` de `@CurrentPerson()` e devolve `Promise<AdminsResponse>`; não há `@Param`, `@Query` nem `@Body` no arquivo. `apps/api/src/admin-roles/admin-roles.module.ts` declara o controller e o serviço, e `apps/api/src/app.module.ts` importa `AdminRolesModule` (`rg -n "AdminRolesModule" apps/api/src/app.module.ts` encontra duas ocorrências: o import e a lista de `imports`).
- [x] CA1.7 — Intocados nesta fase: `git diff --stat feature/108-unit-assignments-remove... -- apps/api/prisma apps/api/src/auth apps/api/src/common apps/api/src/access apps/api/src/documents apps/api/src/people apps/api/src/invitations apps/api/src/installation apps/api/src/org-units apps/api/src/unit-assignments apps/api/test apps/web vitest.config.ts eslint.config.js docs/design.md docs/architecture.md` é vazio. (`docs/roadmap.md` e `docs/features/011-admin-roles/plan.md` ficam **fora** de qualquer lista de intocados deste plano: a orquestração edita os dois.)
- [x] CA1.8 — `npx vitest run --project api` termina com exit code 0 e existem, pelos nomes de T1.3: os casos de `apps/api/src/admin-roles/__tests__/admin-roles.service.test.ts`, entre eles `list filters by the organization and by isAdmin only`, `list selects only id, name and email`, `list sorts in the service, not in the database`, `namesakes keep a stable order by id` e `an empty database answers an empty data array`; os casos de `admin-roles.integration.test.ts`, entre eles `a fresh installation answers 200 with the single installed admin`, `only the admins of the organization come back, sorted with the pt-BR collator`, `each item has exactly the id, name and email keys`, `an admin of another organization never shows up`, `a non-admin answers 403`, `an anonymous request answers 401` e `the role is re-read on every request`; e os casos de `admin-roles.contract.test.ts`, entre eles `GET admins answers the documented 200`, `GET admins answers the documented 401` e `GET admins answers the documented 403`. Conferir a presença dos nomes com `rg -n "^\s*(it|test)(\.each)?\(" apps/api/src/admin-roles/__tests__`. A integração usa o Postgres real: `rg -n "vi\.mock\(.*prisma" apps/api/src/admin-roles/__tests__/admin-roles.integration.test.ts apps/api/src/admin-roles/__tests__/admin-roles.contract.test.ts` é vazio. Nenhum `.only(` nem `.skip(`.
- [x] CA1.9 — Lendo `only the admins of the organization come back, sorted with the pt-BR collator`: há três administradoras semeadas com nomes acentuados ("Álvaro", "Beatriz", "Zilda") mais pelo menos um membro; a administradora de outra organização **não** cabe neste teste (o check `Organization_singleton_check` da migration 0002 impede uma segunda organização no banco) e o filtro por organização é provado num teste próprio, chamando o serviço com outro `organizationId`; a asserção é sobre a **lista inteira na ordem**, com "Álvaro" no índice 0. Lendo `each item has exactly the id, name and email keys`: há `expect(Object.keys(item).sort())` comparado a `['email', 'id', 'name']` para cada item, asserção de ausência de `passwordHash`, `isAdmin` e `createdAt`, e uma asserção de que o corpo serializado (`JSON.stringify`) **não contém** o hash da senha semeada. Lendo `the role is re-read on every request`: a mesma variável de cookie de sessão é usada nas duas requisições, com um `prisma.person.update({ data: { isAdmin: false } })` entre elas, e a segunda afirma **403**.
- [x] CA1.10 — `rg -n "setTimeout\(|sleep\(" apps/api/src/admin-roles` é vazio; `rg -n "eslint-disable" apps/api/src apps/web/src | rg -v -- "--"` é vazio; nenhum arquivo de `apps/api/src/admin-roles/**` contém senha literal (as senhas vêm dos ajudantes/constantes de teste já existentes).
- [x] CA1.11 — Cobertura ≥ 80% de linhas para `apps/api/src/admin-roles/admin-roles.service.ts`, `apps/api/src/admin-roles/admin-roles.controller.ts` e `apps/api/src/admin-roles/admin-roles.module.ts`. A medição vale por `coverage/coverage-summary.json`, gerado na raiz com `npx vitest run --coverage --coverage.reporter=json-summary --coverage.reporter=text` **sem `--project`** e **sem filtro de caminho** (com filtro de projeto o `include` da raiz não casa e o relatório sai vazio): o reporter `text` do v8 **omite arquivos com 100%**, então ausência da tabela de texto não reprova — reprova só quem aparecer no `coverage-summary.json` com `lines.pct < 80`. Arquivo com 0 de 0 linhas instrumentáveis (caso possível do módulo) não reprova.
- [x] CA1.12 — `git log feature/108-unit-assignments-remove..HEAD --format=%s` mostra exatamente um commit desta fase, em inglês, no formato Conventional Commits (prefixo `feat`, `fix`, `test`, `docs`, `chore` ou `refactor` seguido de `:` ou `(escopo):`).

## Fase 2 — Web: a consulta, a rota protegida, o item da barra lateral, a lista e a API simulada

Caminhos relativos à raiz do repositório.

- [x] T2.1 — A consulta `GET /admins` e a zona da feature nova no lint
  - Arquivos: `apps/web/src/features/admin-roles/api/get-admins.ts` (criar); `eslint.config.js` (alterar)
  - O que fazer (D4, R1):
    - No molde de `features/unit-assignments/api/get-unit-people.ts`: `getAdmins(): Promise<AdminsResponse>` chamando `api.get<AdminsResponse, AdminsResponse>('/admins')` (os dois argumentos de tipo são de propósito, porque a resposta tem envelope e o `AxiosResponse<T>` padrão do axios só carrega `data`); `getAdminsQueryOptions()` com `queryKey: ['admins']`; `useAdmins()`.
    - **Chave `['admins']`, curta e sem prefixo de pessoa**: é a lista inteira de um recurso só, e é essa a chave que as fatias 114 e 115 vão invalidar. **Sem `staleTime` próprio e sem `silentError`**: é uma leitura comum do app, notificada pelo interceptor como todas as outras. Nenhuma mutação, nenhuma invalidação, nenhum `refetchInterval`.
    - `eslint.config.js`: a feature nova ganha a zona `./apps/web/src/features/admin-roles` em `import/no-restricted-paths`, no mesmo molde das features já listadas — senão ela nasce desprotegida contra import de outra feature.
  - Skills: api-requests, client-state, project-structure
  - Complexidade: baixa

- [x] T2.2 — A API simulada: `listAdmins` no banco falso e o handler `GET /admins`
  - Arquivos: `apps/web/src/testing/mocks/db.ts` (alterar); `apps/web/src/testing/mocks/handlers/admin-roles.ts` (criar); `apps/web/src/testing/mocks/handlers/index.ts` (alterar); `apps/web/src/testing/mocks/utils.ts` (alterar)
  - O que fazer (D7):
    - `db.ts`: função nova `listAdmins(): MockPerson[]`, que filtra `allPeople()` por `isAdmin` e ordena com o **mesmo** colador `Intl.Collator('pt-BR', { sensitivity: 'base' })` e o **mesmo** desempate por `id` do servidor — o banco falso repete a regra do real, senão a ordem provada no teste não é a ordem entregue.
    - `handlers/admin-roles.ts` (arquivo novo), exportando `adminRolesHandlers`: `http.get(\`${env.API_URL}/admins\`)` com o mesmo preâmbulo dos outros handlers — `networkDelay()`, `devOverride('admins')`, 401 sem cookie ou sem instalação, 403 quando `!installation.person.isAdmin` — e o corpo `{ data: listAdmins().map(({ id, name, email }) => ({ id, name, email })) }`. O `map` explícito é a barreira: sem ele o `isAdmin` do banco falso vazaria para um corpo que o contrato não tem. Nenhuma função de resposta nova: as que existem no diretório bastam.
    - `handlers/index.ts`: soma `adminRolesHandlers` **depois** de `peopleHandlers`.
    - `utils.ts`: **só** o comentário das chaves de desenvolvimento — registrar `mock-error=admins`, que com `mock-installation=signed-in` a página de administradores mostra **uma** pessoa (o estado real de qualquer instalação nova) e que `mock-role=member` mostra o 403/redirecionamento.
    - **Nenhuma semente que promova pessoas de exemplo**: o jeito honesto de ter três administradores no app é a fatia 114; os testes que precisam de vários semeiam pelo próprio `seedDb`.
  - Skills: api-mocking
  - Complexidade: média

- [x] T2.3 — A lista com os quatro estados, a contagem em texto e o selo "você"
  - Arquivos: `apps/web/src/features/admin-roles/components/admins-list.tsx` (criar)
  - O que fazer (D6, R2, R3, R6, R7). A aparência segue `docs/design.md` e o piso da skill `interface-design`; **nenhuma classe é ditada por esta tarefa** e `docs/design.md` **não muda** (todas as receitas usadas já existem: "Lista", "Selo de status" no par cinza, "Ações do item de lista", "Carregando", "Vazio", "Erro", "Botão destrutivo", "Texto de apoio").
    - Exporta `AdminsList`, que recebe **a query** como prop (`query={adminsQuery}`), no molde de `UnitPeopleList`: a rota decide o que consultar, o componente decide o que desenhar.
    - Os **quatro estados** ficam no mesmo lugar do conteúdo, dentro de um `<div aria-live="polite">`, como em `unit-people-list.tsx` — é ele que anuncia a lista quando ela chega. O carregando repete a condição que a fatia 088 documentou (`isPending || (isError && isFetching)`), senão um "Tentar novamente" volta para o alerta em vez de voltar para o carregando.
    - Textos literais, em pt_BR:
      - carregando: **"Carregando administradores…"**, com `role="status"`;
      - vazio: **"Ninguém administra esta instância. Isso não deveria acontecer: para voltar a ter uma administração, é preciso marcar alguém direto no banco de dados."**;
      - erro: **"Não foi possível carregar os administradores."** com `role="alert"` e um botão **"Tentar novamente"**.
    - Com dados, acima da lista, a contagem **em texto**: com uma pessoa, **"Só uma pessoa administra esta instância."**; com duas ou mais, **"{n} pessoas administram esta instância."**, com `n` vindo de `data.length` (não há `count` no corpo). Depois, uma `<ul>` com `aria-label="Administradores"`; cada `<li>` traz o nome à esquerda (bloco que trunca, com `title` igual ao nome, porque nome longo trunca), o e-mail em seguida em tom mais suave (endereço **quebra**, nunca trunca) e, **só na linha de quem está usando o app**, um selo cinza com a palavra **você** à direita, na receita "Ações do item de lista" — a 360px o selo desce para a direita, sem rolagem horizontal.
    - A marcação "você" compara `person.id` com `useUser().data?.person.id`, **nunca** por e-mail nem por nome. `useUser` é lido **dentro** deste componente (é infraestrutura compartilhada de `lib/`, direção permitida) e não passado pela rota.
    - A ordem na tela é exatamente a que a API mandou: **nada é reordenado aqui**.
    - **Nenhum `Button` de ação, nenhum `ConfirmationDialog`, nenhuma mutação, nenhum diálogo** (R3): o único elemento focável da página, além da barra lateral, é o "Tentar novamente" do estado de erro. **Nenhum `<h2>`**: o cabeçalho é da rota.
    - Lista com uma pessoa só é o caso normal hoje e **não** recebe tratamento especial: a mesma lista, com uma linha, e a contagem no singular. Nada de aviso "você é a única administração" — essa frase é da recusa da fatia 115.
  - Skills: interface-design, component-robustness
  - Complexidade: média

- [x] T2.4 — A rota protegida, o caminho e o item da barra lateral
  - Arquivos: `apps/web/src/app/routes/app/admin/admins.tsx` (criar); `apps/web/src/config/paths.ts` (alterar); `apps/web/src/app/router.tsx` (alterar); `apps/web/src/components/layouts/sidebar-admin.tsx` (alterar)
  - O que fazer (D5, R1, R3, R4, R5). Aparência pelo `docs/design.md` e pelo piso de `interface-design`; nenhuma classe é ditada aqui e `docs/design.md` **não muda**.
    - `config/paths.ts`: `admin.admins = { path: '/admin/admins', getHref: () => '/admin/admins' }`, **depois** de `invitations`. Segmento em inglês (`admins`), como toda URL do projeto; o rótulo em pt_BR é só da tela.
    - `app/router.tsx`: entrada `lazy: () => import('@/app/routes/app/admin/admins')` **depois** da de `invitations`, dentro do mesmo `Root`, com `paths.admin.admins.path.slice(1)` — igual às outras rotas de admin.
    - `components/layouts/sidebar-admin.tsx`: um `<li>` com `NavLink` para `paths.admin.admins.getHref()`, rótulo **"Administradores"**, **terceiro** da lista, depois de "Estrutura" e "Convites", na receita "Item da barra lateral", com o estado ativo que o `NavLink` já aplica. Nada mais muda no arquivo: o `Authorization` que embrulha a área inteira já esconde o item de quem não é administração.
    - `app/routes/app/admin/admins.tsx`: `Component` embrulhado em `<Authorization allowedRoles={[ROLES.ADMIN]} forbiddenFallback={<Navigate to={paths.home.getHref()} replace />}>`, com a consulta (`useAdmins`) **dentro** do componente interno — é o desenho de `invitations.tsx` e de `org-unit-people.tsx`, e o que garante que a requisição **nunca sai** para quem não é administração.
    - A página usa `ContentLayout` com `<h1>` **"Administradores"** e texto de apoio **"Quem administra esta instância hoje."**. Abaixo, nesta ordem: (1) um **aviso informativo** (receita 004, âmbar) com o texto **"Administrar a instância não dá acesso a documento: ninguém vê um documento por ser administração. O acesso chega com os espaços de unidade e o compartilhamento."**; (2) um texto de apoio **"Esta página é só de leitura. Promover alguém a administração e tirar o papel de quem não deve mais tê-lo ainda não é possível por aqui — por enquanto, isso só acontece direto no banco de dados."**; (3) `<AdminsList query={…} />`.
    - **Não há `<h2>`**: o `<h1>` já se chama "Administradores", e um subtítulo com o mesmo nome seria eco para quem usa leitor de tela.
    - Nenhuma rota-mãe `/admin` é criada: isso mudaria as três rotas de admin que já existem e é refatoração no meio de uma fatia de produto.
  - Skills: routing, authorization, interface-design
  - Complexidade: média

- [x] T2.5 — Testes da fase 2
  - Arquivos: `apps/web/src/features/admin-roles/api/__tests__/get-admins.test.tsx` (criar); `apps/web/src/features/admin-roles/components/__tests__/admins-list.test.tsx` (criar); `apps/web/src/app/routes/app/admin/__tests__/admins.test.tsx` (criar)
  - O que fazer (D9): banco falso pelos ajudantes já usados (`seedInstalled({ isAdmin })`, `seedDb`); interação por `userEvent`; localizar por papel e nome acessível em pt_BR; senhas vindas das constantes já existentes, **nunca** literais. `LAZY_TIMEOUT` declarada em cada arquivo e passada em **toda** espera dos casos novos logo após rota ou chunk `lazy`. Nenhuma espera fixa; nenhum `console.error`/`console.warn`/aviso de `act(...)` vazando. `src/testing/setup-tests.ts` **não** muda.
    - `get-admins.test.tsx`: `sends GET to /admins and returns the envelope`; `the query key is admins`; `a failure notifies through the interceptor` (não há `silentError`).
    - `admins-list.test.tsx`: `shows the loading state with role status`; `shows the empty state when nobody administers the instance`; `shows the error state with an alert and a retry button`; `Tentar novamente goes back to loading and then shows the list`; `a single admin gets the singular count`; `three admins get the plural count`; `the você badge shows once, on the row of the session person` (semear **duas pessoas com o mesmo nome** para provar que a comparação é por `id`, não por nome); `the loaded list has no button at all` (`queryAllByRole('button')` vazio — é a prova de que a página é só de leitura).
    - `admins.test.tsx` (rota): `an admin opens /admin/admins and sees the heading, both notices and the list`; `a member is sent home and no request goes to /admins` (handler contador, asserção de **zero** chamadas); `the Administradores sidebar item shows for an admin and not for a member`.
    - Fecha a fase com um commit, por exemplo `feat(web): list the admins of the instance`.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta). A saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)` vazando. `git diff feature/108-unit-assignments-remove... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA2.2 — `apps/web/src/features/admin-roles/api/get-admins.ts` existe (antes do commit, `git status --porcelain -uall apps/web/src/features/admin-roles` lista os arquivos; depois, `git diff --stat feature/108-unit-assignments-remove... -- apps/web/src/features/admin-roles` lista os arquivos) e exporta `getAdmins(): Promise<AdminsResponse>` chamando `api.get<AdminsResponse, AdminsResponse>('/admins')`, `getAdminsQueryOptions()` com `queryKey: ['admins']` e `useAdmins()`. `rg -n "staleTime|silentError|setQueryData|refetchInterval|onMutate|useMutation" apps/web/src/features/admin-roles/api/get-admins.ts` é vazio. Em `eslint.config.js`, `rg -n "features/admin-roles" eslint.config.js` encontra a zona da feature dentro da configuração de `import/no-restricted-paths`.
- [x] CA2.3 — `apps/web/src/features/admin-roles/components/admins-list.tsx` exporta `AdminsList`, que recebe a query por prop (`query`), declara retorno `React.JSX.Element` e embrulha os quatro estados num elemento com `aria-live="polite"`. Os textos literais presentes no arquivo são exatamente: `Carregando administradores…` (no bloco com `role="status"`); `Ninguém administra esta instância. Isso não deveria acontecer: para voltar a ter uma administração, é preciso marcar alguém direto no banco de dados.`; `Não foi possível carregar os administradores.` (no bloco com `role="alert"`) e `Tentar novamente`; `Só uma pessoa administra esta instância.` e a forma plural com o número interpolado seguida de ` pessoas administram esta instância.`. A condição do carregando é `isPending || (isError && isFetching)`. Existe `<ul` com `aria-label="Administradores"`.
- [x] CA2.4 — Só de leitura e "você" por `id`, lendo `admins-list.tsx`: o arquivo chama `useUser` de `@/lib/auth` e compara `person.id` com o `id` da sessão (`rg -n "\.email" apps/web/src/features/admin-roles/components/admins-list.tsx` não encontra nenhuma comparação de e-mail — o e-mail só aparece sendo renderizado). O selo traz a palavra `você` como **texto** dentro do elemento, nunca só cor ou ícone. `rg -n "ConfirmationDialog|useMutation|<button|onClick=\{" apps/web/src/features/admin-roles/components/admins-list.tsx` encontra, no máximo, o `onClick` do "Tentar novamente" do estado de erro e nada mais — nenhum `ConfirmationDialog`, nenhum `useMutation`, nenhum `<button` cru. Não há `<h2` no arquivo. Nada reordena os itens recebidos: `rg -n "\.sort\(" apps/web/src/features/admin-roles/components/admins-list.tsx` é vazio.
- [x] CA2.5 — `apps/web/src/app/routes/app/admin/admins.tsx` exporta um `Component` embrulhado em `<Authorization allowedRoles={[ROLES.ADMIN]} forbiddenFallback={<Navigate to={paths.home.getHref()} replace />}>`, com a chamada a `useAdmins()` **dentro** do componente interno (nunca no componente que renderiza o `Authorization`). A página usa `ContentLayout` com **um único** `<h1>` "Administradores" e o texto de apoio `Quem administra esta instância hoje.`; contém, literalmente, `Administrar a instância não dá acesso a documento: ninguém vê um documento por ser administração. O acesso chega com os espaços de unidade e o compartilhamento.` e `Esta página é só de leitura. Promover alguém a administração e tirar o papel de quem não deve mais tê-lo ainda não é possível por aqui — por enquanto, isso só acontece direto no banco de dados.`; e renderiza `<AdminsList query={…} />`. Não há `<h2` no arquivo.
- [x] CA2.6 — Rota, caminho e barra lateral: `apps/web/src/config/paths.ts` tem `admins: { path: '/admin/admins', getHref: () => '/admin/admins' }` dentro de `admin`, declarado **depois** de `invitations`; `apps/web/src/app/router.tsx` tem uma entrada com `paths.admin.admins.path.slice(1)` e `lazy: () => import('@/app/routes/app/admin/admins')`, declarada **depois** da entrada de `invitations` e dentro do mesmo `Root`; `apps/web/src/components/layouts/sidebar-admin.tsx` tem um `<li>` com `NavLink` para `paths.admin.admins.getHref()` e rótulo `Administradores`, sendo o **terceiro** `<li>` da lista, depois de "Estrutura" e "Convites". `git diff feature/108-unit-assignments-remove... -- apps/web/src/components/layouts/sidebar-admin.tsx` só acrescenta esse item (nenhuma linha de `Authorization` nem dos itens existentes é alterada), e `rg -n "<a href" apps/web/src/components/layouts/sidebar-admin.tsx` é vazio.
- [x] CA2.7 — Acabamento (piso de `interface-design`, verificável lendo o código): o conteúdo da rota fica dentro do contêiner de página do `ContentLayout`; o `<h1>` usa as classes de título de página do `docs/design.md` (as mesmas de `invitations.tsx` e `org-unit-people.tsx`); há espaço vertical entre título, avisos e lista. Na lista, cada `<li>` tem respiro interno e separação (divisória ou espaçamento), o nome fica em bloco com `min-w-0` e trunca com `title`, o e-mail quebra (`break-words`) em tom mais suave, e o selo "você" é um selo (fundo/pílula cinza da receita "Selo de status"), alinhado à direita, que desce a 360px sem rolagem horizontal. O estado de erro tem borda ou fundo de destaque e o botão é o `Button` compartilhado de `@/components/ui/button/button` (com `type` explícito, `hover`, `focus-visible` e `disabled` vindos dele). `rg -n "forwardRef|style=\{\{|: JSX\.|!important" apps/web/src/features/admin-roles apps/web/src/app/routes/app/admin/admins.tsx` é vazio e todo componente declara retorno `React.JSX.Element`. `git diff --stat feature/108-unit-assignments-remove... -- docs/design.md` é vazio (nenhuma receita nova) e `git diff --stat feature/108-unit-assignments-remove... -- apps/web/src/components/ui` é vazio (nenhum componente compartilhado novo).
- [x] CA2.8 — API simulada: `apps/web/src/testing/mocks/db.ts` exporta `listAdmins(): MockPerson[]`, que filtra `allPeople()` por `isAdmin` e ordena com `Intl.Collator('pt-BR', { sensitivity: 'base' })` e desempate por `id` — o **mesmo** par de regras do serviço. `apps/web/src/testing/mocks/handlers/admin-roles.ts` exporta `adminRolesHandlers` com `http.get` em `${env.API_URL}/admins`, com `networkDelay()`, `devOverride('admins')`, 401 sem cookie ou sem instalação, 403 quando a pessoa não é administração, e corpo `{ data: listAdmins().map(({ id, name, email }) => ({ id, name, email })) }` — o `map` explícito está lá, e `isAdmin` não aparece no corpo. `apps/web/src/testing/mocks/handlers/index.ts` soma `adminRolesHandlers` depois de `peopleHandlers`. `git diff feature/108-unit-assignments-remove... -- apps/web/src/testing/mocks/utils.ts` só tem linhas iniciadas por `//` além do contexto, e elas mencionam `mock-error=admins`, `mock-installation=signed-in` e `mock-role=member`. Nenhuma semente nova promove pessoas de exemplo: `git diff feature/108-unit-assignments-remove... -- apps/web/src/testing/mocks/db.ts` não altera nenhuma função de semente existente nem cria pessoa com `isAdmin: true` além da instalação.
- [x] CA2.9 — **`apps/web/src/lib/auth.tsx` não é tocado nesta fatia**: `git diff feature/108-unit-assignments-remove... -- apps/web/src/lib/auth.tsx` é **vazio**, e `rg -n "staleTime" apps/web/src/lib/auth.tsx` continua mostrando `Infinity` (o ajuste é da fatia 114).
- [x] CA2.10 — Intocados nesta fase (base é o commit da fase 1, porque `packages` e `apps/api` mudaram legitimamente lá): `git diff --stat <commit da fase 1> -- apps/web/src/lib apps/web/src/types apps/web/src/components/ui apps/web/src/utils apps/web/src/features/auth apps/web/src/features/connection apps/web/src/features/installation apps/web/src/features/documents apps/web/src/features/org-units apps/web/src/features/invitations apps/web/src/features/unit-assignments apps/web/src/testing/setup-tests.ts apps/web/e2e vitest.config.ts packages apps/api docs` é vazio. (`docs/roadmap.md` e `docs/features/011-admin-roles/plan.md` são editados pela orquestração; se aparecerem, não reprovam — qualquer outro caminho de `docs/` reprova.) A feature não importa de outra feature nem de `app/`: `rg -n "@/features/(?!admin-roles)|@/app/" -P apps/web/src/features/admin-roles --glob '!**/__tests__/**'` é vazio.
- [x] CA2.11 — `npx vitest run --project web` termina com exit code 0 e existem, pelos nomes de T2.5: os casos de `apps/web/src/features/admin-roles/api/__tests__/get-admins.test.tsx`, entre eles `sends GET to /admins and returns the envelope`, `the query key is admins` e `a failure notifies through the interceptor`; os casos de `apps/web/src/features/admin-roles/components/__tests__/admins-list.test.tsx`, entre eles `shows the loading state with role status`, `shows the empty state when nobody administers the instance`, `shows the error state with an alert and a retry button`, `Tentar novamente goes back to loading and then shows the list`, `a single admin gets the singular count`, `three admins get the plural count`, `the você badge shows once, on the row of the session person` e `the loaded list has no button at all`; e os casos de `apps/web/src/app/routes/app/admin/__tests__/admins.test.tsx`, entre eles `an admin opens /admin/admins and sees the heading, both notices and the list`, `a member is sent home and no request goes to /admins` e `the Administradores sidebar item shows for an admin and not for a member`. Conferir os nomes com `rg -n "^\s*(it|test)\(" apps/web/src/features/admin-roles apps/web/src/app/routes/app/admin/__tests__/admins.test.tsx`. Nenhum `.only(` nem `.skip(`. Todos os casos preexistentes de `apps/web/src/app/routes/app/admin/__tests__` continuam listados e não foram renomeados.
- [x] CA2.12 — Lendo `the você badge shows once, on the row of the session person`: há **duas** pessoas semeadas com o **mesmo nome** e ids diferentes, e a asserção é `getAllByText('você')` com comprimento **1**, na linha da pessoa da sessão. Lendo `the loaded list has no button at all`: há `queryAllByRole('button')` com asserção de vazio, com a lista já carregada. Lendo `a member is sent home and no request goes to /admins`: há um contador de chamadas no handler de `/admins` e asserção de que ele vale **0**, além da asserção de que a tela do início está visível. Lendo `a single admin gets the singular count`: a asserção é sobre o texto `Só uma pessoa administra esta instância.`.
- [x] CA2.13 — Esperas: `get-admins.test.tsx`, `admins-list.test.tsx` e `admins.test.tsx` declaram `LAZY_TIMEOUT` e a passam em **toda** espera dos casos novos logo após abrir ou trocar de rota (`findBy…` e `waitFor`). `rg -n "setTimeout\(|sleep\(" apps/web/src/features/admin-roles apps/web/src/app/routes/app/admin/__tests__/admins.test.tsx` é vazio. Nenhum arquivo de teste desta fase contém senha literal. `rg -n "eslint-disable" apps/web/src apps/api/src | rg -v -- "--"` é vazio.
- [x] CA2.14 — Cobertura ≥ 80% de linhas para `apps/web/src/features/admin-roles/api/get-admins.ts`, `apps/web/src/features/admin-roles/components/admins-list.tsx` e `apps/web/src/app/routes/app/admin/admins.tsx`. A medição vale por `coverage/coverage-summary.json`, gerado na raiz com `npx vitest run --coverage --coverage.reporter=json-summary --coverage.reporter=text` **sem `--project`** e **sem filtro de caminho**: o reporter `text` do v8 omite arquivos com 100%, então ausência da tabela de texto não reprova — reprova só quem aparecer no `coverage-summary.json` com `lines.pct < 80`.
- [x] CA2.15 — `git log feature/108-unit-assignments-remove..HEAD --format=%s` mostra exatamente dois commits (um por fase), ambos em inglês e no formato Conventional Commits.

## Fase 3 — e2e da jornada e documentação: a fatia utilizável de ponta a ponta

Caminhos relativos à raiz do repositório.

- [x] T3.1 — Documentação: a entrega na arquitetura
  - Arquivos: `docs/architecture.md` (alterar)
  - O que fazer, em pt_BR, na §6, parágrafo novo **"Entrega `admins-list` (fatia 011)"**, sem reescrever o resto:
    - `GET /admins` é recurso próprio com tag própria — é dele que as fatias 114 e 115 penduram `PUT`/`DELETE /admins/{personId}`; guards `SessionGuard` + `AdminGuard` **na classe** do `AdminRolesController`, e `organizationId` sempre da sessão, nunca da rota nem da query;
    - corpo com `id`, `name` e `email` e nada mais, garantido por `select` explícito (`Person` tem `passwordHash`);
    - lista **inteira**, sem paginação e sem limite, ordenada **em memória** com colador pt-BR e desempate por `id`, pela mesma razão da fatia 010: a collation do Postgres varia por instância e sob `C` os acentos iriam para o fim;
    - **sem `count` no corpo** — a contagem é `data.length` na tela, porque duas fontes para o mesmo número podem discordar;
    - identidade de caminho conferida contra o YAML inteiro: `/admins` é primeiro segmento inédito, e por isso não foi escolhido `/people/admins`, que dependeria da ordem de declaração no Nest para não casar o literal com um futuro `/people/{personId}`;
    - **nenhuma migration** — `Person.isAdmin` já existe e nenhum índice foi criado;
    - `isAdmin` relido a cada pedido já basta para o `AdminGuard` (promoção e rebaixamento valem na requisição seguinte), enquanto o front **não** percebe mudança de papel sem recarregar (`staleTime: Infinity` em `getUserQueryOptions`), o que é assunto da fatia **114**.
  - Skills: —
  - Complexidade: baixa

- [x] T3.2 — Testes da fase 3 (e2e: a administração vê quem administra, pelo teclado)
  - Arquivos: `apps/web/e2e/tests/admins-list.spec.ts` (criar)
  - O que fazer (D9): contra a API simulada; localizar por papel e nome acessível em pt_BR. Estado inicial por `page.addInitScript` gravando `mock-installation=signed-in` (e `mock-role=member` no segundo caso). No topo, `const ROUTE_TIMEOUT = { timeout: 10_000 }`; **toda** espera logo após mudança de rota (`toHaveURL`, primeira visibilidade da tela nova) o recebe; nenhum `waitForTimeout`. Depois de cada `Tab`/seta e antes do `Enter`, `await expect(locator).toBeFocused()`. Nenhum literal com cara de senha ou token. `apps/web/e2e/a11y.ts` e os outros specs não são tocados; nenhuma regra do axe é desativada.
    - Caso `an admin reaches the admins page from the sidebar and sees itself marked`: chega à página **pelo item "Administradores" da barra lateral** (não pelo endereço), alcançando o item **pelo teclado** com `toBeFocused()` antes do `Enter`; vê o `<h1>` "Administradores", o próprio nome com o selo "você" e a contagem no singular ("Só uma pessoa administra esta instância."); `expectNoSeriousA11yViolations(page)` com a página carregada.
    - Caso `a member has no admins item and is sent home from the direct address`: com `mock-role=member`, o item "Administradores" **não existe** na barra lateral, e abrir `/admin/admins` direto termina no início (asserção de `toHaveURL` com `ROUTE_TIMEOUT`).
    - Os e2e da 010, 064, 065, 066, 085, 086, 087, 088 e 108 **não** mudam. Se algum deles quebrar, o conserto é no código de produção, não no spec antigo.
    - Fecha a fase com um commit, por exemplo `test(e2e): cover the admins list page`.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [x] CA3.1 — `pnpm test:e2e` na raiz termina com exit code 0 (o Playwright sobe o próprio servidor; não precisa de API nem de Postgres), e continuam terminando com exit code 0 e **sem aviso**, com `docker compose up -d` rodando e o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta).
- [x] CA3.2 — `apps/web/e2e/tests/admins-list.spec.ts` contém os casos `an admin reaches the admins page from the sidebar and sees itself marked` e `a member has no admins item and is sent home from the direct address`, e `pnpm --filter web exec playwright test --list` os lista junto com todos os casos já existentes de `unit-assignments.spec.ts`, `unit-assignments-remove.spec.ts`, `org-units-view.spec.ts`, `org-units-create-rename.spec.ts`, `org-units-delete.spec.ts`, `invitations-create.spec.ts`, `invitations-accept.spec.ts`, `invitations-list.spec.ts` e `invitations-revoke.spec.ts` (nenhum removido nem renomeado). `rg -n "test\.skip|test\.only|waitForTimeout" apps/web/e2e` é vazio.
- [x] CA3.3 — Lendo o primeiro caso: a página é alcançada **navegando pela interface** (item "Administradores" da barra lateral), não por URL direta; o item é alcançado por teclado, com `toBeFocused()` antes do `Enter`; há asserção do `<h1>` "Administradores", do selo "você" na linha do próprio nome e do texto `Só uma pessoa administra esta instância.`; `expectNoSeriousA11yViolations` é chamado com a página carregada. Lendo o segundo caso: com `mock-role=member`, há asserção de que o item "Administradores" **não está** na barra lateral e de que `/admin/admins` termina no endereço do início.
- [x] CA3.4 — Esperas e segredos do e2e: o spec declara `ROUTE_TIMEOUT = { timeout: 10_000 }` no topo e **toda** asserção logo após mudança de rota (`toHaveURL` e a primeira visibilidade da tela nova) o recebe; entre cada `Tab`/seta e o `Enter` seguinte há `toBeFocused()`; não há espera fixa; não há valor literal de senha nem de token no arquivo.
- [x] CA3.5 — `git status --porcelain -uall -- apps/web/e2e` (antes do commit) ou `git diff --stat feature/108-unit-assignments-remove... -- apps/web/e2e` (depois) lista **só** `apps/web/e2e/tests/admins-list.spec.ts`; o spec não desativa regra do axe (`rg -n "disableRules" apps/web/e2e/tests/admins-list.spec.ts` é vazio); os arquivos de `apps/web/e2e/**` passam no `pnpm lint` e no `pnpm typecheck`.
- [x] CA3.6 — `docs/architecture.md` tem, na §6, o parágrafo "Entrega `admins-list` (fatia 011)" dizendo: `GET /admins` como recurso próprio com tag própria, base para `PUT`/`DELETE /admins/{personId}` das fatias 114 e 115; `SessionGuard` + `AdminGuard` **na classe** e `organizationId` sempre da sessão; corpo com `id`, `name` e `email` e nada mais, por `select` explícito por causa do `passwordHash`; lista inteira sem paginação, ordenada em memória com colador pt-BR e desempate por `id`, pela mesma razão da 010; **sem `count` no corpo**; a conferência de identidade de caminho contra o YAML inteiro e a recusa de `/people/admins`; **nenhuma migration** e nenhum índice novo; e o registro de que `isAdmin` é relido a cada pedido, enquanto o front só percebe mudança de papel ao recarregar (assunto da 114). Nenhuma outra seção do arquivo foi reescrita.
- [x] CA3.7 — Intocados nesta fase: `git diff --stat <commit da fase 2> -- apps/api packages apps/web/src` é vazio (a fase 3 só acrescenta o spec de e2e e edita `docs/architecture.md`). `git diff feature/108-unit-assignments-remove... -- apps/web/src/lib/auth.tsx` continua **vazio**. (`docs/roadmap.md` e `docs/features/011-admin-roles/plan.md` são editados pela orquestração e ficam fora desta lista.)
- [x] CA3.8 — A fatia está utilizável de ponta a ponta sem navegador aberto por pessoa: `npx vitest run --project api` (exit code 0) inclui `a fresh installation answers 200 with the single installed admin`, `only the admins of the organization come back, sorted with the pt-BR collator`, `each item has exactly the id, name and email keys`, `a non-admin answers 403`, `an anonymous request answers 401` e `the role is re-read on every request` em `apps/api/src/admin-roles/__tests__/admin-roles.integration.test.ts`; `npx vitest run --project web` (exit code 0) inclui `an admin opens /admin/admins and sees the heading, both notices and the list` e `a member is sent home and no request goes to /admins` em `apps/web/src/app/routes/app/admin/__tests__/admins.test.tsx`; e `pnpm test:e2e` (exit code 0) inclui os dois casos de `apps/web/e2e/tests/admins-list.spec.ts`.
- [x] CA3.9 — `git log feature/108-unit-assignments-remove..HEAD --format=%s` mostra exatamente três commits (um por fase), todos em inglês e no formato Conventional Commits; `git diff --stat feature/108-unit-assignments-remove... -- docs/roadmap.md docs/features/011-admin-roles/plan.md` pode ser não vazio só pelo registro da orquestração.

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
