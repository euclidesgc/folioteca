# PLAN 147 — share-access-list

Branch: `feature/147-share-access-list`, empilhada sobre a 145 (`feature/145-share-with-person-view`).

Fonte: `docs/features/147-share-access-list/spec.md` (D1…D7 são as decisões técnicas da SPEC e R1…R11 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

O proprietário abre o diálogo "Compartilhar documento" e vê, abaixo do seletor de pessoa, a seção "Quem tem acesso": ele primeiro (selos "dono" e "você") e depois cada pessoa com acesso direto ("Pode ver"/"Pode editar"), em ordem alfabética pt-BR. No rodapé, "Copiar link" copia o endereço do documento ou, sem área de transferência, mostra o endereço num campo somente leitura já selecionado. Quem tem acesso mas não é o proprietário recebe recusa (403) ao pedir a lista; quem não tem acesso recebe "não encontrado" (404). Fora desta fatia: gravar "Pode editar" (148), trocar/tirar nível (146).

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; banco de teste `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`.

**Critérios sobre o disco.** Os critérios são conferidos **antes** do commit da fase, sobre os arquivos em disco (commitados ou não). Nenhum critério depende de `git diff` contra a base. Critério de ausência (`rg` vazio) vale só para arquivos criados ou alterados na própria fase e usa padrão que não casa com comentário. O commit de cada fase é feito pela orquestração depois da revisão; nenhum agente de fase commita. Se `pnpm test` ou `pnpm test:e2e` falhar num teste **antigo** e sem relação com a fase, o comando pode ser repetido **uma** vez; a segunda execução vale.

**Nota sobre o PRD.** O R8 do PRD está sendo corrigido para "quem tem acesso mas não é o proprietário recebe recusa (403); sem acesso, não encontrado (404)". Este plano segue a SPEC (ordem 404 → 403, a mesma do `PUT`).

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **Sem migration**: `apps/api/prisma/schema.prisma` e `apps/api/prisma/migrations/**` não mudam. Nenhum subcomando de Prisma além de `prisma generate`; **nunca** `migrate diff`, `migrate dev` ou `migrate reset`.
- **Nenhum teste remove, desativa ou recria restrição do banco** (`DROP CONSTRAINT`, `ALTER TABLE … DISABLE`, `session_replication_role`).
- O arquivo `packages/api-contract/src/generated/openapi.d.ts` só muda por `pnpm --filter @folioteca/api-contract generate`; nunca editado à mão.
- **Sem Prettier reformatando arquivo existente**: só as linhas necessárias mudam; nada de `prettier --write` em arquivo inteiro.
- `DocumentShare` (`.documentShare.` ou `"DocumentShare"`) só aparece em `apps/api/src/access/access.service.ts` e `apps/api/src/documents/shares.service.ts` (regras 9–11 de `apps/api/src/access/__tests__/document-access-boundary.test.ts`, que **não muda**).
- Todo componente devolve `React.JSX.Element` (ou `React.JSX.Element | null`), nunca o `JSX` global; `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case; **nenhum import entre features** (a marcação de `features/spaces/components/space-members.tsx` e de `features/invitations/components/invitation-link.tsx` é **copiada**, não importada). Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Nenhum `eslint-disable` sem `-- <motivo>` na própria linha; nenhuma regra desativada para fazer teste passar; nenhum `any` sem comentário.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado, medida **só** por `coverage/coverage-summary.json`, gerado **na raiz** com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`, **sem `--project`** e **sem filtro de caminho**. Não contam arquivos só de tipos nem os já excluídos pelo `vitest.config.ts` da raiz. Nenhuma exclusão nova.
- Nos e2e, `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo do spec: a **primeira** asserção após cada mudança de rota usa `ROUTE_TIMEOUT`; uma asserção seguinte com timeout maior é aceita. `toBeFocused()` antes de cada `Enter`/`Space`. Nunca `setTimeout`/sleep fixo nos testes nem `waitForTimeout`.
- Testes de API de integração e contrato rodam contra o Postgres real (sem mock do Prisma); só os `*.service.test.ts` usam Prisma falso. "Teste passa" é medido pelo **exit code** (zero), nunca pela saída `--verbose`.
- **Escopo por sessão**: quem pede vem sempre de `@CurrentPerson()`. Escopo de outra organização é provado chamando o **serviço real** com `{ ...requester, organizationId: randomUUID() }` (`Organization.singleton` impede uma segunda organização no banco).
- Testes existentes afetados por esta fatia são ajustados **sem trocar o nome** e registrados na seção "Desvios" no fim do plano (DVn).
- Intocados de propósito: `apps/api/src/access/access.service.ts`, `apps/api/src/access/__tests__/document-access-boundary.test.ts`, `apps/api/prisma/**`, `apps/web/src/features/spaces/components/space-members.tsx`, `apps/web/src/features/invitations/components/invitation-link.tsx`, `apps/web/src/components/ui/**`, `apps/web/e2e/a11y.ts`.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa.

## Fase 1 — API: `GET /documents/{documentId}/shares` só para o proprietário

Caminhos relativos à raiz do repositório. Ao fim da fase, pela API, o dono recebe a lista (ele primeiro, depois as pessoas em ordem pt-BR); quem tem acesso sem ser dono recebe 403; quem não tem acesso, 404.

- [x] T1.1 — Contrato primeiro: rota de leitura da lista no OpenAPI
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D1, R1, R2, R8):
    - No caminho já existente `/documents/{documentId}/shares/{personId}` nada muda. Acrescentar o caminho `/documents/{documentId}/shares` com `get`, `operationId: listDocumentShares`, tag `documents`, parâmetro de caminho `documentId` (uuid), respostas **200** `$ref` `DocumentAccessListResponse` e **401**, **403**, **404** com `Error`; `description` em pt_BR (só o proprietário; o dono vem primeiro, depois as pessoas em ordem alfabética; documento na lixeira continua legível para o dono).
    - Schemas novos: `DocumentAccessEntry` (`type: object`, `required: [personId, name, email, level, isCurrentPerson]`, `personId: { type: string, format: uuid }`, `name: string`, `email: string`, `level: { type: string, enum: [owner, view, edit] }`, `isCurrentPerson: boolean`); `DocumentAccessListResponse` (`required: [data]`, `data: { type: array, items: $ref DocumentAccessEntry }`). O schema `DocumentShare` do `PUT` fica **inalterado**.
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`.
  - Skills: security
  - Complexidade: baixa

- [x] T1.2 — `SharesService.list` e a rota `GET` no `DocumentsController`
  - Arquivos: `apps/api/src/documents/shares.service.ts` (alterar); `apps/api/src/documents/documents.controller.ts` (alterar)
  - O que fazer (D1, D2, R2, R8, R9):
    - `shares.service.ts`: método `list(requester: PersonWithOrganization, documentId: string): Promise<DocumentAccessListResponse>` (tipo vindo do contrato gerado, como o `share` faz com `DocumentShareResponse`), nesta ordem fixa: (1) `resolveAccess(requester.id, documentId)` `'none'` → 404 `documentNotFound()` (a mesma função/resposta do `share`); (2) `!== 'owner'` → `ForbiddenException('Só o proprietário pode ver quem tem acesso a este documento.')`; (3) **sem** `canWrite`: a lixeira não bloqueia o dono; (4) `prisma.documentShare.findMany({ where: { documentId }, select: { level: true, person: { select: { id: true, name: true, email: true } } } })`, **sem** `orderBy`; ordena em memória com `ptBrCollator.compare` (de `apps/api/src/common/pt-br-collator.ts`) por nome, depois e-mail, depois id; (5) devolve `{ data: [ownerEntry, ...shareEntries] }`, com `ownerEntry = { personId: requester.id, name: requester.name, email: requester.email, level: 'owner', isCurrentPerson: true }` e cada share `{ personId, name, email, level, isCurrentPerson: false }` com `VIEW` → `'view'` e `EDIT` → `'edit'`.
    - Não busca o dono por `document.owner`: o `requester` já é o dono provado no passo 2.
    - `documents.controller.ts`: `@Get(':documentId/shares')` com `@CurrentPerson() person` e `@Param('documentId') documentId: string`, chamando `shares.list(person, documentId)`. Guards do controller intocados (só na classe).
  - Skills: authorization, security
  - Complexidade: média

- [x] T1.3 — Testes da fase 1
  - Arquivos: `apps/api/src/documents/__tests__/shares.integration.test.ts` (alterar); `apps/api/src/documents/__tests__/shares.service.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.contract.test.ts` (alterar)
  - O que fazer (D7): integração e contrato contra o Postgres real com `resetDatabase(prisma)` em `beforeEach` e os ajudantes existentes; `apps/api/test/**` não muda. Casos preexistentes não são renomeados.
    - `shares.integration.test.ts` (HTTP, salvo onde indicado): `GET shares lists the owner first and people in pt-BR order` (pessoas "Bruno" e "Álvaro": a ordem é dono, "Álvaro", "Bruno"; a linha do dono tem `level: 'owner'` e `isCurrentPerson: true`, as outras `isCurrentPerson: false` e `level: 'view'`); `GET shares breaks name ties by email` (duas pessoas com o mesmo nome, e-mails `b…` e `a…`: vem primeiro a do e-mail `a…`); `GET shares includes a person right after sharing` (`PUT` e depois `GET`: a pessoa aparece uma vez só); `GET shares on a document without access answers the same 404 as a missing one` (mesmo status e mesmo corpo de um id `randomUUID()` inexistente); `GET shares by a view person answers 403 with Só o proprietário pode ver quem tem acesso a este documento.`; `GET shares on a trashed document answers 200 to the owner`; `GET shares answers 401 without session`; `list with another organization id does not leak shares` (serviço real, `list({ ...requester, organizationId: randomUUID() }, documentId)`: não devolve nenhuma linha de share de outra organização; o documento continua do dono, então o esperado é a linha do dono e as shares do próprio documento, sem pessoa alheia — o teste assere que todo `personId` devolvido pertence ao documento).
    - `shares.service.test.ts` (Prisma falso): `list answers 404 when the requester has no access`; `list answers 403 to a view person`; `list checks access before reading shares` (no 404 e no 403, `documentShare.findMany` não é chamado); `list does not check canWrite`; `list maps VIEW to view and EDIT to edit`; `list puts the owner first with isCurrentPerson true`.
    - `documents.contract.test.ts`: `GET documents shares answers the documented 200`, `GET documents shares answers the documented 401`, `GET documents shares answers the documented 403`, `GET documents shares answers the documented 404`, chamando exatamente `/documents/${id}/shares` e validando contra a chave literal `/documents/{documentId}/shares` do YAML, método `get`.
  - Skills: unit-testing, integration-testing
  - Complexidade: média

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules`): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e sem aviso.
- [x] CA1.2 — `packages/api-contract/openapi.yaml` tem a chave `/documents/{documentId}/shares:` com `get`, `operationId: listDocumentShares`, tag `documents`, parâmetro `documentId` e respostas exatamente `200` (`DocumentAccessListResponse`), `401`, `403`, `404`; `DocumentAccessEntry` com `required` contendo `personId`, `name`, `email`, `level` e `isCurrentPerson`, e `level` com `enum: [owner, view, edit]`; `DocumentAccessListResponse` com `required: [data]` e `data` como array de `DocumentAccessEntry`. `rg -n "listDocumentShares|DocumentAccessEntry|DocumentAccessListResponse" packages/api-contract/src/generated/openapi.d.ts` encontra os três. O schema `DocumentShare` continua com `enum: [view]`.
- [x] CA1.3 — Identidade de caminho: `apps/api/src/documents/documents.controller.ts` tem `@Get(':documentId/shares')` com `@Param('documentId')` chamando `list(person, documentId)`, e continua com `@Controller('documents')`.
- [x] CA1.4 — `apps/api/src/documents/shares.service.ts` tem `list(requester: PersonWithOrganization, documentId: string)`, contém literalmente `Só o proprietário pode ver quem tem acesso a este documento.`, `documentShare.findMany`, `ptBrCollator`, `isCurrentPerson: true` e `level: 'owner'`; lido, em `list` a ordem é `resolveAccess` (404) → dono (403) → `findMany`, sem chamada a `canWrite` dentro de `list` e sem `orderBy` no `findMany` de `list`.
- [x] CA1.5 — Fronteira e banco: `rg -l "\.documentShare\.|\"DocumentShare\"" apps/api/src` lista só `apps/api/src/access/access.service.ts`, `apps/api/src/documents/shares.service.ts` e arquivos sob `__tests__`; `ls apps/api/prisma/migrations` lista as mesmas pastas de antes da fase (a última é `0019_space_member_level`); `rg -n "DROP CONSTRAINT|DISABLE TRIGGER|session_replication_role" apps/api/src/documents/__tests__` é vazio.
- [x] CA1.6 — `pnpm exec vitest run --project api` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/api/src/documents/__tests__`, os 18 casos nomeados em T1.3 com os nomes literais (`shares.integration.test.ts`: 8; `shares.service.test.ts`: 6; `documents.contract.test.ts`: 4). `rg -n "vi\.mock\(.*prisma" apps/api/src/documents/__tests__/shares.integration.test.ts apps/api/src/documents/__tests__/documents.contract.test.ts` é vazio.
- [x] CA1.7 — Lendo os testes: `GET shares lists the owner first and people in pt-BR order` usa uma pessoa com nome começando por "Á" e assere que ela vem antes de "Bruno"; `list with another organization id does not leak shares` chama o serviço real (instância ligada ao Prisma de teste) com `randomUUID()` como `organizationId`; `GET shares includes a person right after sharing` assere uma única ocorrência do `personId`; os testes de contrato citam a chave literal `/documents/{documentId}/shares`.
- [x] CA1.8 — Cobertura ≥ 80% de linhas para `apps/api/src/documents/shares.service.ts` e `apps/api/src/documents/documents.controller.ts`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 2 — Web: seção "Quem tem acesso" e "Copiar link" no diálogo

Caminhos relativos à raiz do repositório. A ordem importa: API simulada e chamadas antes da tela. Ao fim da fase, no app com API simulada, o dono abre "Compartilhar", vê quem tem acesso, compartilha e vê a pessoa entrar na lista, e copia o link.

- [x] T2.1 — API simulada e chamadas: leitura da lista e invalidação após compartilhar
  - Arquivos: `apps/web/src/testing/mocks/db.ts` (alterar); `apps/web/src/testing/mocks/handlers/documents.ts` (alterar); `apps/web/src/features/documents/api/get-document-shares.ts` (criar); `apps/web/src/features/documents/api/share-document.ts` (alterar)
  - O que fazer (D3, D6, R2, R4, R5, R8):
    - `db.ts`: `listDocumentShares(documentId: string)` devolve o dono do documento primeiro (`level: 'owner'`, `isCurrentPerson: true` — o dono é a sessão simulada nos documentos do dono) e depois as shares do documento com nome e e-mail da pessoa, `level` da share e `isCurrentPerson: false`, ordenadas com `localeCompare(…, 'pt-BR')` por nome, depois e-mail, depois id.
    - `handlers/documents.ts`: `http.get(\`${env.API_URL}/documents/:documentId/shares\`)` com o preâmbulo de sempre do arquivo (`networkDelay()`, `devOverride('documents')`, 401), e na ordem: 404 "Documento não encontrado." (sem acesso), 403 "Só o proprietário pode ver quem tem acesso a este documento." (tem acesso, não é dono), 200 `{ data: listDocumentShares(documentId) }`. Os textos das respostas ficam no handler (ou onde o arquivo já centraliza as mensagens de erro; o critério aponta o arquivo em que o texto de fato estiver).
    - `get-document-shares.ts`: `getDocumentShares(documentId: string): Promise<DocumentAccessListResponse>` → `api.get(\`/documents/${documentId}/shares\`, { silentError: true })`; `getDocumentSharesQueryOptions(documentId: string)` com `queryKey: ['document-shares', documentId]`; `useDocumentShares({ documentId, enabled }: { documentId: string; enabled: boolean })`.
    - `share-document.ts`: `useShareDocument` passa a usar `useQueryClient()` e, no `onSuccess`, faz `await queryClient.invalidateQueries({ queryKey: ['document-shares', documentId] })` (com o `documentId` das variáveis da mutação) **antes** de chamar o `onSuccess` do `mutationConfig`. `shareDocument` não muda.
  - Skills: api-requests, api-mocking
  - Complexidade: média

- [x] T2.2 — Diálogo: seção "Quem tem acesso" e "Copiar link"
  - Arquivos: `apps/web/src/features/documents/components/share-document-dialog.tsx` (alterar)
  - O que fazer (D4, D5, Interface, R1–R4, R6, R7, R10, R11). Aparência pelo `docs/design.md` e pelo piso de `interface-design`, com as receitas "Diálogo de formulário", "Lista", "Selo de status", "Botão secundário", "Carregando", "Erro" e "Campo de formulário"; a receita nova "Copiar link" é registrada no `docs/design.md` na fase 3:
    - Ordem, de cima para baixo: título e descrição (inalterados); seletor de pessoa (inalterado); **seção "Quem tem acesso"**; mensagens do compartilhar (inalteradas); área do link copiado ou do campo de fallback; rodapé com `Button` secundário **"Copiar link"** e depois **"Fechar"**, com `flex-wrap` a 360px.
    - Seção: `<h3>` **"Quem tem acesso"** com o estilo de subtítulo do `design.md`. Consulta por `useDocumentShares({ documentId, enabled: open })` (só com o diálogo aberto), independente da mutação: o seletor e "Compartilhar" funcionam com a lista carregando ou com erro.
    - Estados no mesmo lugar: carregando → `role="status"` **"Carregando quem tem acesso…"**; erro → `role="alert"` **"Não foi possível carregar quem tem acesso."** com `Button` **"Tentar de novo"** que chama `refetch`; com dados → `<ul>` com a marcação e as classes de `features/spaces/components/space-members.tsx` (copiadas): nome (principal, `truncate`, em flex com `min-w-0`) e e-mail (secundário, tom suave) à esquerda, selos à direita — na linha do dono **"dono"** e **"você"**; nas outras **"Pode ver"** (`view`) ou **"Pode editar"** (`edit`). Sem estado vazio (a linha do dono sempre existe). A lista fica dentro de uma região `aria-live="polite"`.
    - "Copiar link": URL `\`${window.location.origin}${paths.document.getHref(documentId)}\``. Estado local `copyState: 'idle' | 'copied' | 'manual'`, voltando a `'idle'` ao reabrir. Se `navigator.clipboard?.writeText` existir e resolver → `'copied'`: **"Link copiado"** num `<p aria-live="polite">` em tom suave. Se não existir ou rejeitar → `'manual'`: `<label>` **"Endereço do documento"** ligado a um `<input readOnly>` com a URL, selecionado por uma `ref` num efeito quando aparece. Mesmo padrão de `features/invitations/components/invitation-link.tsx` (copiado, não importado). Sem `document.execCommand`.
  - Skills: interface-design, error-handling, component-robustness
  - Complexidade: média

- [x] T2.3 — Testes da fase 2 e os e2e antigos
  - Arquivos: `apps/web/src/features/documents/api/__tests__/get-document-shares.test.tsx` (criar); `apps/web/src/features/documents/api/__tests__/share-document.test.tsx` (alterar); `apps/web/src/features/documents/components/__tests__/share-document-dialog.test.tsx` (alterar)
  - O que fazer (D7): banco falso pelos ajudantes; `userEvent`; localizar por papel e nome acessível em pt_BR; esperas com `findBy…`/`waitFor`, nunca espera fixa; clipboard simulado com `vi.spyOn`/`Object.defineProperty` em `navigator.clipboard` restaurado ao fim do caso; sem `console.error`/`console.warn`/aviso de `act(...)`. Casos preexistentes não são renomeados; os que ficarem ambíguos pela nova seção (ex.: `getByText('Pode ver')` com o selo da pessoa escolhida e o da lista) são ajustados restringindo a busca (`within`) e registrados como desvio (DV1).
    - `get-document-shares.test.tsx`: `getDocumentShares requests the document shares path`; `useDocumentShares returns the owner first and the shared people`; `useDocumentShares does not request when enabled is false`.
    - `share-document.test.tsx`: `useShareDocument invalidates document shares before the caller onSuccess` (espiona `invalidateQueries` com `queryKey: ['document-shares', documentId]` e assere que ela terminou antes do `onSuccess` passado em `mutationConfig`).
    - `share-document-dialog.test.tsx`: `shows Quem tem acesso with the owner badges dono and você`; `lists shared people with Pode ver and Pode editar in server order`; `shows Carregando quem tem acesso while the list loads`; `shows the list error with Tentar de novo and retries`; `sharing still works when the list fails`; `a shared person appears in the list once after sharing`; `Copiar link writes the document URL and announces Link copiado`; `without clipboard shows the selected Endereço do documento field`; `a rejected clipboard write shows the selected Endereço do documento field`; `reopening the dialog clears the copy state`.
    - Rodar `pnpm test:e2e` com **todos** os e2e existentes; se algum quebrar, o conserto é no código de produção. Conserto em spec antigo só se a mudança de UI for intencional (ex.: "Pode ver" passa a aparecer também na lista em `apps/web/e2e/tests/share-with-person-view.spec.ts`), sem renomear o caso, e registrado como desvio (DV2).
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` saem com 0 e sem aviso; a saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)`. `pnpm test:e2e` (a suíte inteira) sai com 0.
- [x] CA2.2 — `apps/web/src/features/documents/api/get-document-shares.ts` exporta `getDocumentShares` (chama `api.get` em `` `/documents/${documentId}/shares` `` com `silentError: true`), `getDocumentSharesQueryOptions` (com `['document-shares', documentId]`) e `useDocumentShares` (recebe `{ documentId, enabled }`). `apps/web/src/features/documents/api/share-document.ts` contém `useQueryClient`, `await queryClient.invalidateQueries` com `['document-shares'` e a chamada ao `onSuccess` do `mutationConfig` depois dela. `rg -n -P "from '@/features/(?!documents)" apps/web/src/features/documents/components/share-document-dialog.tsx apps/web/src/features/documents/api/get-document-shares.ts apps/web/src/features/documents/api/share-document.ts` é vazio.
- [x] CA2.3 — `apps/web/src/features/documents/components/share-document-dialog.tsx` continua exportando `ShareDocumentDialog` com retorno `React.JSX.Element` e contém literalmente `Quem tem acesso`, `Carregando quem tem acesso…`, `Não foi possível carregar quem tem acesso.`, `Tentar de novo`, `dono`, `você`, `Pode ver`, `Pode editar`, `Copiar link`, `Link copiado`, `Endereço do documento`, `navigator.clipboard`, `paths.document.getHref(`, `window.location.origin`, `readOnly`, `useDocumentShares` e `refetch`; tem `<h3`, `<ul`, `role="status"`, `role="alert"` e `aria-live="polite"`; o tipo `'idle' | 'copied' | 'manual'` (em qualquer ordem) aparece no arquivo. `rg -n -P "^\s*[^/*].*execCommand\(" apps/web/src/features/documents/components/share-document-dialog.tsx` é vazio.
- [x] CA2.4 — Acabamento (piso de `interface-design`, lendo o código de `share-document-dialog.tsx`): o `<h3>` usa as classes de subtítulo do `docs/design.md`; cada linha da lista separa nome (`truncate`, em flex com `min-w-0`) de e-mail (tom suave) e os selos ficam à direita com as classes da receita "Selo de status"; o erro da lista tem fundo ou borda de destaque e o "Tentar de novo" é `Button`; "Copiar link" é `Button` secundário e o rodapé tem `flex-wrap`; o campo "Endereço do documento" tem `<label>` ligado ao `<input>` (`htmlFor`/`id`). `rg -n -P "<button|<a href|style=\{\{|!important|forwardRef|: JSX\." apps/web/src/features/documents/components/share-document-dialog.tsx` é vazio.
- [x] CA2.5 — API simulada: `apps/web/src/testing/mocks/db.ts` exporta `listDocumentShares(documentId: string)` com `localeCompare` e `'pt-BR'`; `apps/web/src/testing/mocks/handlers/documents.ts` tem `http.get` em `` `${env.API_URL}/documents/:documentId/shares` `` com `networkDelay()`, `devOverride('documents')` e as respostas 401, 403, 404 e 200; o texto `Só o proprietário pode ver quem tem acesso a este documento.` está no arquivo onde a resposta 403 é montada (`handlers/documents.ts` ou `db.ts`; `rg -n "Só o proprietário pode ver quem tem acesso" apps/web/src/testing/mocks` encontra uma ocorrência).
- [x] CA2.6 — `pnpm exec vitest run --project web` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/web/src/features/documents`, os 14 casos nomeados em T2.3 com os nomes literais (`get-document-shares.test.tsx`: 3; `share-document.test.tsx`: 1 novo; `share-document-dialog.test.tsx`: 10 novos); os casos da 145 nesses arquivos continuam com os mesmos nomes (`shareDocument sends PUT to the document share path with level view`, `useShareDocument returns the shared person from the response`, `selecting a person shows Pode ver and the Compartilhar button`).
- [x] CA2.7 — Lendo os testes: `useShareDocument invalidates document shares before the caller onSuccess` assere a ordem (invalidação resolvida antes do `onSuccess`); `a shared person appears in the list once after sharing` conta uma única linha com o nome da pessoa na lista; `sharing still works when the list fails` força erro no `GET …/shares` e assere "Documento compartilhado com"; `without clipboard shows the selected Endereço do documento field` e `a rejected clipboard write shows the selected Endereço do documento field` asserem o valor do campo terminando com o caminho do documento e a seleção inteira (`selectionStart` 0 e `selectionEnd` igual ao tamanho); `useDocumentShares does not request when enabled is false` assere zero pedidos ao `GET`.
- [x] CA2.8 — `rg -n "sleep\(|waitForTimeout" apps/web/src/features/documents/api/__tests__/get-document-shares.test.tsx apps/web/src/features/documents/api/__tests__/share-document.test.tsx apps/web/src/features/documents/components/__tests__/share-document-dialog.test.tsx` é vazio; `rg -n "eslint-disable" apps/web/src/features/documents apps/web/src/testing/mocks | rg -v -- "--"` é vazio. Os desvios DV1/DV2 que tenham ocorrido estão escritos na seção "Desvios" deste plano, com o arquivo e o nome do caso.
- [x] CA2.9 — Cobertura ≥ 80% de linhas para `apps/web/src/features/documents/api/get-document-shares.ts`, `apps/web/src/features/documents/api/share-document.ts` e `apps/web/src/features/documents/components/share-document-dialog.tsx`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 3 — e2e da jornada e documentação: a fatia utilizável de ponta a ponta

- [x] T3.1 — Documentação: receita "Copiar link"
  - Arquivos: `docs/design.md` (alterar)
  - O que fazer, em pt_BR, só acrescentando linhas em "Padrões acrescentados pelas entregas": receita **"Copiar link"** (fatia 147): `Button` secundário "Copiar link"; sucesso em `<p aria-live="polite">` com texto suave; fallback com `<label>` + `<input readOnly>` da receita "Campo de formulário", texto selecionado; com as classes usadas em `share-document-dialog.tsx`.
  - Skills: interface-design
  - Complexidade: baixa

- [x] T3.2 — Testes da fase 3 (e2e: o dono vê quem tem acesso e copia o link)
  - Arquivos: `apps/web/e2e/tests/share-access-list.spec.ts` (criar)
  - O que fazer (D7): API simulada, estado inicial por `page.addInitScript`, como os specs existentes; `test.use({ permissions: ['clipboard-write'] })` no topo, como `apps/web/e2e/tests/invitations-create.spec.ts`. `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo: a primeira asserção após cada mudança de rota o usa; `toBeFocused()` antes de cada `Enter`/`Space`; nenhum `waitForTimeout`; nenhuma regra do axe desativada; outros specs e `apps/web/e2e/a11y.ts` intocados.
    - `the owner sees who has access and the shared person joins the list`: abre o documento, aciona "Compartilhar", vê o diálogo "Compartilhar documento" e a seção "Quem tem acesso" com "dono" e "você", `expectNoSeriousA11yViolations(page)` com o diálogo aberto, busca e seleciona uma pessoa, aciona "Compartilhar", e a pessoa aparece na lista com "Pode ver".
    - `the owner copies the document link`: abre o diálogo, aciona "Copiar link" e vê "Link copiado"; se a área de transferência falhar no headless, assere em vez disso o campo "Endereço do documento" com valor terminando no caminho do documento (a asserção aceita uma das duas saídas e nomeia qual ocorreu).
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [x] CA3.1 — `pnpm test:e2e` na raiz sai com 0 (a suíte inteira), e `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` continuam saindo com 0 e sem aviso, com o cache do `tsc` limpo.
- [x] CA3.2 — `apps/web/e2e/tests/share-access-list.spec.ts` contém os casos `the owner sees who has access and the shared person joins the list` e `the owner copies the document link`, e `pnpm --filter web exec playwright test --list` os lista. `rg -n "test\.skip|test\.only|waitForTimeout|disableRules" apps/web/e2e/tests/share-access-list.spec.ts` é vazio.
- [x] CA3.3 — Lendo o spec: `test.use({ permissions: ['clipboard-write'] })` e `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo; a primeira asserção após cada mudança de rota usa `ROUTE_TIMEOUT` (uma seguinte com timeout maior é aceita); `toBeFocused()` antes de cada tecla de ação; `expectNoSeriousA11yViolations(page)` com o diálogo aberto; o primeiro caso assere "Quem tem acesso", "dono", "você" e "Pode ver" na linha da pessoa compartilhada; o segundo assere "Link copiado" ou o campo "Endereço do documento".
- [x] CA3.4 — `docs/design.md` tem a receita "Copiar link" em "Padrões acrescentados pelas entregas", citando `aria-live="polite"` e `readOnly`.
- [x] CA3.5 — A fatia está utilizável de ponta a ponta: `pnpm exec vitest run --project api` (0) inclui `GET shares lists the owner first and people in pt-BR order` e `GET shares by a view person answers 403 with Só o proprietário pode ver quem tem acesso a este documento.`; `pnpm exec vitest run --project web` (0) inclui `a shared person appears in the list once after sharing` e `Copiar link writes the document URL and announces Link copiado`; `pnpm test:e2e` (0) inclui os dois casos de `share-access-list.spec.ts`.

## Desvios

Preenchido pelos agentes de fase quando um teste existente precisar de ajuste (sem renomear). Previstos:

- DV1 — `apps/web/src/features/documents/components/__tests__/share-document-dialog.test.tsx`: Os casos da 145 em share-document-dialog.test.tsx passaram a usar `DOCUMENT_ID = 'document-1'` (documento do dono já presente na API simulada) em vez de `document-to-share`, porque com a lista nova o documento inexistente respondia 404 e criava um segundo alerta; nenhuma asserção nem nome de caso mudou.
- DV2 — `apps/web/e2e/tests/share-with-person-view.spec.ts`: "Pode ver" passa a aparecer também na lista; ajuste restringindo o localizador, sem renomear o caso.
- `apps/web/src/features/documents/api/__tests__/share-document.test.tsx` (`useShareDocument returns the shared person from the response`): não deve precisar de ajuste (invalidar uma chave sem consulta ativa não pede nada); se precisar, registrar aqui como DV3.

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
