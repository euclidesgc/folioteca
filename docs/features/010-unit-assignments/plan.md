# PLAN 010 — unit-assignments

Branch: `feature/010-unit-assignments`, que **sai de `feature/087-invitations-revoke`** (entrega empilhada). **Toda** comparação de diff e de "arquivo intocado" deste plano é contra `feature/087-invitations-revoke` — nunca contra `develop`, nunca contra `main`. Onde um critério diz `<base>`, leia `feature/087-invitations-revoke`.

Fonte: `docs/features/010-unit-assignments/spec.md` (D1…D10 são as decisões técnicas da SPEC e R1…R13 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; o banco de teste é `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom é da ferramenta e **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **A migration `0012` é escrita à mão**, como todas as deste projeto. Ninguém — agente, teste ou critério — roda `prisma migrate diff`, `prisma migrate dev` nem `prisma migrate reset` (as migrations `0007`, `0009`, `0010` e `0011` têm índices por expressão e parciais invisíveis ao `schema.prisma`, e qualquer `diff` proporia apagá-los). Os únicos subcomandos permitidos são `pnpm --filter api exec prisma validate`, `pnpm --filter api exec prisma migrate status` e `pnpm --filter api exec prisma migrate deploy`, sempre contra `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test`.
- Todo componente devolve `React.JSX.Element` (nunca o `JSX` global); `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; navegação interna é sempre `<Link>` do roteador, nunca `<a href>`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case. Textos de tela em pt_BR; código, comentários, caminhos de URL e de API e nomes de teste em en_US.
- Nenhum `eslint-disable` sem `-- <motivo>`. Nenhum segredo em variável `VITE_*`; nenhum dado pessoal (nome, e-mail) em log; nenhum `Logger` nem `console.` fora de testes nos módulos novos; nenhum literal com cara de senha, token ou hash em arquivo versionado (o GitGuardian reprova o push).
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado. Não contam arquivos só de tipos, arquivos com 0 de 0 linhas instrumentáveis, nem os já excluídos pelo `vitest.config.ts` da raiz (`**/types/**`, `**/generated/**`, `**/src/testing/**`, `**/test/**`, `**/*.config.*`). Nenhuma exclusão nova.
- **Toda** espera **dos casos novos** logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes da web pela constante `LAZY_TIMEOUT` declarada no arquivo (`findBy…(…, {}, LAZY_TIMEOUT)`, `waitFor(…, LAZY_TIMEOUT)`); no e2e pela constante `ROUTE_TIMEOUT` declarada no topo do spec. Nunca `setTimeout`/sleep fixo nem `waitForTimeout`. **Casos preexistentes não são alterados nem renomeados.**
- Testes de API são de integração contra o Postgres real (padrão `apps/api/test`); "teste passa" é medido pelo **exit code** de `npx vitest run --project api` / `--project web` (zero), nunca pela leitura da saída `--verbose`.
- **Lotação não dá acesso a documento nenhum.** Nenhum arquivo de `apps/api/src/access/**` nem de `apps/api/src/documents/**` é alterado, e o teste estrutural `apps/api/src/access/__tests__/document-access-boundary.test.ts` continua passando **sem uma linha alterada** — ele varre `apps/api/src/` e já alcança os módulos novos.
- O 404 de unidade é **opaco** (inexistente, de outra organização e id malformado respondem o mesmo corpo, "Unidade não encontrada."). O 404 de pessoa é "Pessoa não encontrada." e nunca converge com o de unidade.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa. Cada fase termina com **um** commit Conventional Commits em inglês (`feat:`, `test:`, `docs:`), sem `--no-verify`.
- Nenhuma tarefa acrescenta escopo: **nenhuma remoção de lotação** (é a fatia 108), nenhum `ConfirmationDialog`, nenhum convite ou criação de pessoa, nenhuma "unidade principal", nenhum histórico, nenhuma visão a partir da pessoa, nenhuma paginação da lista de lotados.

Desvio em relação à tabela de arquivos da SPEC, sem mudar escopo: as fases 1 e 2 da SPEC (banco/contrato e rotas) são executadas como **uma** fase, porque a migration sem o serviço não é verificável de ponta a ponta e o agente é o mesmo. `docs/roadmap.md` (010 concluída, 108 na fila, dívidas novas) **não** é tarefa nem critério de fase: a orquestração o registra no encerramento.

## Fase 1 — API: contrato, migration `0012`, o módulo `unit-assignments`, a busca `/people` e a quarta recusa da 066

Caminhos relativos à raiz do repositório.

- [x] T1.1 — Contrato primeiro: as três operações e os cinco schemas
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D2, R1, R3, R4, R5, R9, R10):
    - Caminhos **sem** o prefixo `/api` (o `/api` é do proxy do Vite e do `env.API_URL`) e em inglês.
    - `GET /org-units/{orgUnitId}/people`: `operationId: getOrgUnitPeople`, tag `org-units`, parâmetro de rota `orgUnitId` (`in: path`, `required: true`, `schema: { type: string }`). Respostas: **200** com `AssignedPeopleResponse`; **401**, **403** e **404** com o schema `Error`, que já existe.
    - `POST /org-units/{orgUnitId}/people`: `operationId: assignPersonToOrgUnit`, tag `org-units`, `requestBody` **obrigatório** com `AssignPersonRequest` (`{ personId: string }`, `additionalProperties: false`). Respostas: **201** com `AssignedPersonResponse`; **400**, **401**, **403**, **404** e **409** com `Error`.
    - `GET /people`: `operationId: searchPeople`, tag `people`, parâmetro de consulta `q` (`in: query`, **não** obrigatório, `schema: { type: string }`). Respostas: **200** com `PeopleResponse`; **401** e **403** com `Error`. **Sem** `400` e **sem** `404`. **Nenhum** parâmetro de limite.
    - Schemas novos: `AssignedPerson` (`id`, `name`, `email`, os três obrigatórios); `OrgUnitSummary` (`id`, `name`, os dois obrigatórios); `AssignedPeopleResponse` (`data: AssignedPerson[]` e `orgUnit: OrgUnitSummary`, ambos obrigatórios); `AssignedPersonResponse` (`data: AssignedPerson`, obrigatório); `AssignPersonRequest`; `PeopleResponse` (`data: PersonSummary[]` e `hasMore: boolean`, ambos obrigatórios) com `PersonSummary` (`id`, `name`, `email`).
    - As `description` das operações, em pt_BR, registram as regras observáveis: só a administração; a unidade vem no mesmo envelope para a página ter o nome numa requisição só; unidade inexistente, de outra organização e com id malformado respondem o **mesmo** 404; pessoa inexistente, de outra organização e com id malformado respondem "Pessoa não encontrada."; lotação repetida responde 409; a busca tem limite **duro** de 10 e `q` em branco devolve lista vazia; a ordem de falha do `POST` é CSRF → 401 → 403 → 404 de unidade → 400 de corpo → 404 de pessoa → 409.
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`; o `.d.ts` nunca é editado à mão. `apps/api/test/generated-types.test.ts` e `apps/web/src/types/api.ts` não mudam.
  - Skills: security
  - Complexidade: média

- [x] T1.2 — Migration `0012`, escrita à mão: a tabela `OrgUnitAssignment` com PK composta
  - Arquivos: `apps/api/prisma/migrations/0012_org_unit_assignment/migration.sql` (criar); `apps/api/prisma/schema.prisma` (alterar)
  - O que fazer (D1, R5, R6, R11):
    - `migration.sql`, escrita à mão, com comentários em pt_BR explicando a PK composta e o `RESTRICT`: `CREATE TABLE "OrgUnitAssignment"` com `"orgUnitId" TEXT NOT NULL`, `"personId" TEXT NOT NULL`, `"createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP` e `CONSTRAINT "OrgUnitAssignment_pkey" PRIMARY KEY ("orgUnitId", "personId")`; `CREATE INDEX "OrgUnitAssignment_personId_idx" ON "OrgUnitAssignment"("personId")`; e as duas chaves estrangeiras `OrgUnitAssignment_orgUnitId_fkey` → `"OrgUnit"("id")` e `OrgUnitAssignment_personId_fkey` → `"Person"("id")`, ambas `ON DELETE RESTRICT ON UPDATE CASCADE`. **Sem** coluna `id` e **sem** `@@unique` separado: a linha *é* o par, e o índice da PK já é o de leitura "quem está nesta unidade"; `@@index([personId])` é o caminho contrário, "em que unidades esta pessoa está".
    - `schema.prisma`: o modelo `OrgUnitAssignment` (`orgUnitId`, `personId`, `createdAt @default(now())`, relações `orgUnit` e `person` com `onDelete: Restrict`, `@@id([orgUnitId, personId])`, `@@index([personId])`) e o lado inverso `assignments OrgUnitAssignment[]` em `OrgUnit` e em `Person`. Comentário `///` do modelo dizendo que a PK composta é a única garantia de "não se lota duas vezes na mesma unidade" (não há consulta prévia em lugar nenhum) e que **esta linha não dá acesso a documento**.
    - Conferir com `pnpm --filter api exec prisma validate` e `pnpm --filter api exec prisma migrate status`; aplicar no banco de teste com `pnpm --filter api exec prisma migrate deploy`. **Nenhum** `migrate diff`, `dev` ou `reset`. As migrations `0001`–`0011` não são tocadas.
  - Skills: security
  - Complexidade: média

- [x] T1.3 — O módulo `unit-assignments`: `list`, `assign`, o 409 vindo do banco e os guards na classe
  - Arquivos: `apps/api/src/unit-assignments/unit-assignments.schema.ts` (criar); `apps/api/src/unit-assignments/unit-assignments.service.ts` (criar); `apps/api/src/unit-assignments/unit-assignments.controller.ts` (criar); `apps/api/src/unit-assignments/unit-assignments.module.ts` (criar)
  - O que fazer (D3, D4, D5, R1, R5, R6, R8, R9, R10):
    - `unit-assignments.schema.ts`: `assignPersonSchema = z.object({ personId: z.string().min(1, 'Informe a pessoa.') }).strict()` — `.strict()` para campo desconhecido virar 400, como o `PATCH` de unidades.
    - `unit-assignments.service.ts`, `list(organizationId: string, orgUnitId: string)`: `isUuid` → `orgUnit.findFirst({ where: { id, organizationId } })` → `orgUnitNotFound()` (a função que **já existe** em `org-units`, única origem de 404 de unidade) se não achar; depois `orgUnitAssignment.findMany({ where: { orgUnitId }, select: { person: { select: { id: true, name: true, email: true } } } })`. Ordenação **em memória** com `new Intl.Collator('pt-BR', { sensitivity: 'base' })`, desempate por `id` — ordenar no banco dependeria da collation da instância. O colador é **copiado** para o arquivo novo, três linhas; serviço não importa serviço. Devolve `{ data, orgUnit: { id, name } }`.
    - `assign(organizationId, orgUnitId, body)`, **nesta ordem**: unidade primeiro (404 opaco), `parseBody(assignPersonSchema, body)` depois (400), `person.findFirst({ where: { id, organizationId } })` em seguida (404 `PERSON_NOT_FOUND_MESSAGE = 'Pessoa não encontrada.'`), e então `orgUnitAssignment.create` dentro de `try/catch`, com `P2002` virando `ConflictException(ALREADY_ASSIGNED_MESSAGE)`, com `ALREADY_ASSIGNED_MESSAGE = 'Esta pessoa já está lotada nesta unidade.'`. **Sem** consulta prévia de duplicidade: o 409 vem do banco, e é isso que fecha a corrida de dois envios simultâneos. A pessoa **não** passa por `isUuid`: `findFirst` com id malformado simplesmente não acha, e o resultado é o mesmo 404. Devolve `{ data: { id, name, email } }`.
    - Nada no serviço olha `parentId`: a raiz aceita lotação como qualquer unidade.
    - `unit-assignments.controller.ts`: `@Controller('org-units/:orgUnitId/people')` com `@UseGuards(SessionGuard, AdminGuard)` **na classe** e **nenhum** `@UseGuards` por método (o Nest **soma** guards de método aos de classe; decorador vazio não limpa nada). `@Get()` e `@Post()` + `@HttpCode(201)`, ambos com `@CurrentPerson() person` dando o `organizationId` — o `organizationId` **nunca** vem da rota. Nenhuma rota pública nesta fatia, logo nenhum controller separado.
    - `unit-assignments.module.ts`: módulo com o controller e o serviço, no molde de `org-units.module.ts`.
  - Skills: authorization, security
  - Complexidade: alta

- [x] T1.4 — O módulo `people`: busca com limite duro, termo em branco e `hasMore`
  - Arquivos: `apps/api/src/people/people.service.ts` (criar); `apps/api/src/people/people.controller.ts` (criar); `apps/api/src/people/people.module.ts` (criar); `apps/api/src/app.module.ts` (alterar)
  - O que fazer (D3, D4, R3, R4, R9):
    - `people.service.ts`: `PEOPLE_SEARCH_LIMIT = 10` como constante do servidor — **não** há parâmetro de limite na rota, porque ele seria um botão para pedir a instância inteira. `search(organizationId: string, q?: string)`: se `q?.trim()` for vazio, devolve `{ data: [], hasMore: false }` **sem tocar o banco** (o campo nasce vazio e volta a ficar vazio; um 400 transformaria o estado normal da tela em erro). Senão, `person.findMany({ where: { organizationId, OR: [{ name: { contains: term, mode: 'insensitive' } }, { email: { contains: term, mode: 'insensitive' } }] }, take: PEOPLE_SEARCH_LIMIT + 1, orderBy: { name: 'asc' }, select: { id: true, name: true, email: true } })`, recorta os 10 primeiros e põe `hasMore: true` se veio o 11º.
    - `people.controller.ts`: `@Controller('people')` com `@UseGuards(SessionGuard, AdminGuard)` **na classe**, `@Get()` com `@Query('q')` e `@CurrentPerson()`. Módulo próprio, e não uma rota dentro de `unit-assignments`: `/people` é outro recurso, com outra tag, e é dele que as fatias 011 e 012 vão pendurar rotas.
    - `app.module.ts`: os dois módulos novos (`UnitAssignmentsModule` e `PeopleModule`) entram na lista de `imports`. Nenhuma outra linha do arquivo muda.
    - Nenhum `Logger` e nenhum `console.` nos dois módulos novos: nome e e-mail não vão para log.
  - Skills: authorization, security
  - Complexidade: média

- [x] T1.5 — A quarta recusa de `OrgUnitsService.remove`: unidade com gente lotada
  - Arquivos: `apps/api/src/org-units/org-units.service.ts` (alterar)
  - O que fazer (D1, D4): com `RESTRICT` nos dois lados, apagar uma unidade que ainda tem gente lotada passaria a chegar como `P2003` e viraria o 409 **errado** que já existe ("A unidade mudou enquanto era apagada. Recarregue a estrutura e tente de novo."), que manda recarregar e não resolve nada. `remove` ganha uma **quarta** condição, lida na **mesma consulta única** da transação (`_count: { select: { assignments: true } }`), com mensagem própria `HAS_PEOPLE_MESSAGE = 'Ainda há pessoas lotadas nesta unidade. Tire a lotação delas antes de apagar a unidade.'`, na ordem **raiz → filhas → documentos → pessoas**. O `P2003` continua sendo a rede da corrida. `org-units.controller.ts` e `org-units.schema.ts` **não** são tocados.
  - Skills: error-handling
  - Complexidade: média

- [x] T1.6 — Testes da fase 1
  - Arquivos: `apps/api/src/unit-assignments/__tests__/unit-assignments.service.test.ts` (criar); `apps/api/src/unit-assignments/__tests__/unit-assignments.integration.test.ts` (criar); `apps/api/src/unit-assignments/__tests__/unit-assignments.contract.test.ts` (criar); `apps/api/src/people/__tests__/people.service.test.ts` (criar); `apps/api/src/people/__tests__/people.integration.test.ts` (criar); `apps/api/src/people/__tests__/people.contract.test.ts` (criar); `apps/api/src/org-units/__tests__/org-units.integration.test.ts` (alterar)
  - O que fazer (D8, D9): integração e contrato contra o **Postgres real**, sem mock do Prisma, `resetDatabase(prisma)` em `beforeEach`, instalação e sessões por `createPersonWithSession` de `apps/api/test/create-person.ts` e pelos ajudantes de `apps/api/test/http.ts`; contrato por `expectMatchesContract` de `apps/api/test/contract.ts`. `apps/api/test/**` **não muda**. Segunda organização criada direto pelo Prisma. Nenhuma espera fixa; nenhum literal com cara de senha ou token. **Casos preexistentes não são renomeados nem alterados.**
    - `unit-assignments.service.test.ts` (Prisma falso): `assign checks the org unit before parsing the body`; `assign checks the body before looking up the person`; `assign turns P2002 into a conflict`; `assign never queries for an existing assignment` (espião: nenhum `findFirst`/`findUnique` sobre `orgUnitAssignment`); `list sorts with the pt-BR collator and never orders in the database` (o `findMany` não recebe `orderBy`).
    - `people.service.test.ts` (Prisma falso): `a blank term never touches the database` (asserção sobre o espião); `the search filters by organization with case-insensitive contains`; `the search takes the limit plus one`; `ten results answer hasMore false`; `eleven results answer ten items and hasMore true`.
    - `unit-assignments.integration.test.ts`: `an admin assigns a person and gets 201 with the person`; `the unit list shows the assigned person with the unit name`; `assigning the same person twice answers 409 and keeps one row`; `two simultaneous assignments produce one 201, one 409 and one row` (dois `POST` disparados sem `await` entre eles, resolvidos com `Promise.all`, e consulta direta pelo Prisma contando as linhas); `the same person can be assigned to two units` e `assigning in the second unit does not touch the first`; `the list is sorted by the pt-BR collator` (semeando "Álvaro", "ana", "Zilda"); `it.each` com `an unknown org unit`, `an org unit of another organization` e `a malformed org unit id` → os três com o **mesmo** status e o **mesmo** corpo comparados entre si com `toEqual`, no `GET` **e** no `POST`; `it.each` com `an unknown person`, `a person of another organization` e `a malformed person id` → os três com o mesmo corpo `Pessoa não encontrada.`, mais `the person not found body differs from the org unit not found body` (asserção explícita de que as duas mensagens não convergem); `a body without personId answers 400 on the personId field`, `an empty personId answers 400 on the personId field`, `an unknown field answers 400 on that field`; `a non-admin answers 403 on all three routes` e `an anonymous request answers 401 on all three routes`, e em ambos **a tabela continua vazia** depois das tentativas (consulta direta pelo Prisma); `the root unit accepts an assignment`; e a fronteira de acesso (D8): `an assignment grants no document access` — uma segunda pessoa **sem** documento nenhum tem o corpo de `GET /documents` guardado **antes** de ser lotada, é lotada na unidade que tem um documento no espaço dela, e o **mesmo** `GET /documents` é conferido depois com `toEqual` entre os dois corpos; mais `a document of the unit space stays 404 after the assignment` (`GET /documents/{id}` responde 404 para ela depois da lotação).
    - `people.integration.test.ts`: `a missing q answers an empty list`; `an empty q answers an empty list`; `a blank q answers an empty list` (os três com `data: []` e `hasMore: false`); `the search matches by name`; `the search matches by email`; `the search ignores case`; `eleven matches answer ten people and hasMore true`; `exactly ten matches answer hasMore false`; `a person of another organization never appears`; `a non-admin answers 403`; `an anonymous request answers 401`.
    - `unit-assignments.contract.test.ts` e `people.contract.test.ts`: cada resposta documentada de cada operação passando por `expectMatchesContract` — `GET org unit people answers the documented 200/401/403/404`; `POST assign answers the documented 201/400/401/403/404/409`; `GET people answers the documented 200/401/403`.
    - `org-units.integration.test.ts`, casos **novos**: `deleting a unit with assigned people answers 409` com a mensagem literal "Ainda há pessoas lotadas nesta unidade. Tire a lotação delas antes de apagar a unidade."; `the unit is still listed after the refused delete`; `the refusals keep the order root, children, documents, people`. Os casos de raiz, filhas e documentos **continuam exatamente iguais**.
    - Fecha a fase com um commit, por exemplo `feat(api): assign people to an org unit and search people`.
  - Skills: unit-testing, integration-testing, security
  - Complexidade: alta

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (sem aviso de peer no install; sem aviso de chunk grande no build; o aviso de desempenho do Vitest sobre jsdom não conta). A saída de `pnpm test` não tem `console.error` nem `console.warn` vazando, nem aviso do Nest ou do Prisma. `git diff feature/087-invitations-revoke... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA1.2 — `packages/api-contract/openapi.yaml` tem os caminhos `/org-units/{orgUnitId}/people` (com `get` e `post`) e `/people` (com `get`), **sem** prefixo `/api`. O `get` do primeiro tem `operationId: getOrgUnitPeople`, tag `org-units`, um parâmetro `orgUnitId` em `path` com `required: true` e `schema.type: string`, e respostas `200`, `401`, `403`, `404` (as três últimas referenciando `Error`). O `post` tem `operationId: assignPersonToOrgUnit`, `requestBody` com `required: true` referenciando `AssignPersonRequest`, e respostas `201`, `400`, `401`, `403`, `404`, `409`. O `get /people` tem `operationId: searchPeople`, tag `people`, um parâmetro `q` em `query` **sem** `required: true`, e respostas exatamente `200`, `401` e `403` — **não** há `400` nem `404` nessa operação, e **não** existe nenhum parâmetro de limite (`rg -n "limit" packages/api-contract/openapi.yaml` não encontra nenhum parâmetro novo nessas três operações).
- [x] CA1.3 — Os schemas `AssignedPerson` (`id`, `name`, `email` em `required`), `OrgUnitSummary` (`id`, `name` em `required`), `AssignedPeopleResponse` (`data` e `orgUnit` em `required`), `AssignedPersonResponse` (`data` em `required`), `AssignPersonRequest` (`personId` em `required`, `additionalProperties: false`), `PeopleResponse` (`data` e `hasMore` em `required`) e `PersonSummary` (`id`, `name`, `email`) existem em `packages/api-contract/openapi.yaml`. `rg -n "getOrgUnitPeople|assignPersonToOrgUnit|searchPeople" packages/api-contract/src/generated/openapi.d.ts` encontra as três operações; `apps/api/test/generated-types.test.ts` passa sem ter sido alterado; `git diff feature/087-invitations-revoke... -- apps/web/src/types/api.ts` é vazio.
- [x] CA1.4 — A migration existe e foi escrita à mão: antes do commit, `git status --porcelain -uall apps/api/prisma/migrations/0012_org_unit_assignment/migration.sql` lista o arquivo; depois do commit, `git diff --stat feature/087-invitations-revoke... -- apps/api/prisma/migrations/0012_org_unit_assignment/migration.sql` lista o arquivo. Lendo o SQL: `CREATE TABLE "OrgUnitAssignment"` com as colunas `"orgUnitId"`, `"personId"` e `"createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP`, **sem** coluna `"id"`; `CONSTRAINT "OrgUnitAssignment_pkey" PRIMARY KEY ("orgUnitId", "personId")`; `CREATE INDEX "OrgUnitAssignment_personId_idx"`; e duas `ADD CONSTRAINT … FOREIGN KEY` com `ON DELETE RESTRICT ON UPDATE CASCADE`, uma para `"OrgUnit"("id")` e outra para `"Person"("id")`. `git diff --stat feature/087-invitations-revoke... -- apps/api/prisma/migrations` lista **só** o diretório `0012_org_unit_assignment` (as migrations `0001`–`0011` seguem idênticas).
- [x] CA1.5 — Prova **no banco** de que a unicidade do par existe. Com `docker compose up -d`, `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test pnpm --filter api exec prisma migrate deploy` termina com exit code 0, `pnpm --filter api exec prisma validate` passa e `DATABASE_URL=… pnpm --filter api exec prisma migrate status` reporta nenhuma migration pendente. Em seguida, `psql postgresql://folioteca@localhost:5433/folioteca_test -At -c "select indexdef from pg_indexes where tablename = 'OrgUnitAssignment' order by indexname"` imprime **duas** linhas: uma com `UNIQUE` sobre `("orgUnitId", "personId")` (o índice da chave primária) e outra, não única, sobre `("personId")`. E `psql postgresql://folioteca@localhost:5433/folioteca_test -At -c "select conname, confdeltype from pg_constraint where conrelid = '\"OrgUnitAssignment\"'::regclass and contype = 'f' order by conname"` imprime duas linhas, ambas com `confdeltype` igual a `r` (RESTRICT).
- [x] CA1.6 — Nenhum `prisma migrate diff/dev/reset` entrou no código: `git diff feature/087-invitations-revoke... -- apps/api apps/web packages | rg -n "migrate (diff|dev|reset)"` é vazio (`docs/` fica fora porque a documentação cita esses comandos justamente para proibi-los).
- [x] CA1.7 — `apps/api/src/unit-assignments/unit-assignments.service.ts` tem `list(organizationId, orgUnitId)` e `assign(organizationId, orgUnitId, body)`. Lendo `assign`: a unidade é resolvida **antes** de `parseBody`, e `parseBody` vem **antes** da consulta de `person` (as três chamadas aparecem nessa ordem no corpo do método). O `create` de `orgUnitAssignment` está dentro de um `try/catch` que converte o código `P2002` em `ConflictException` com a mensagem literal `Esta pessoa já está lotada nesta unidade.`, e **não** há nenhum `findFirst`/`findUnique`/`count` sobre `orgUnitAssignment` no arquivo. O 404 de pessoa usa a mensagem literal `Pessoa não encontrada.` e o 404 de unidade vem de `orgUnitNotFound()`, importado de `org-units` — nenhuma mensagem nova de unidade é escrita aqui. `list` usa `new Intl.Collator('pt-BR', { sensitivity: 'base' })` declarado **neste** arquivo e o `findMany` de `orgUnitAssignment` **não** recebe `orderBy`. O arquivo não importa nenhum outro serviço de domínio (só `PrismaService`, como todos os serviços do projeto).
- [x] CA1.8 — `apps/api/src/unit-assignments/unit-assignments.controller.ts` tem `@Controller('org-units/:orgUnitId/people')` e `@UseGuards(SessionGuard, AdminGuard)` **na classe**, com um `@Get()` e um `@Post()` + `@HttpCode(201)`. `apps/api/src/people/people.controller.ts` tem `@Controller('people')` e `@UseGuards(SessionGuard, AdminGuard)` **na classe**, com um `@Get()` e `@Query('q')`. Em nenhum dos dois arquivos existe `@UseGuards` dentro de um método (`rg -n "@UseGuards" apps/api/src/unit-assignments apps/api/src/people` mostra exatamente duas ocorrências, ambas imediatamente acima de uma declaração de classe). O `organizationId` vem de `@CurrentPerson()` nos três handlers e **nunca** de `@Param`.
- [x] CA1.9 — `apps/api/src/people/people.service.ts` declara `PEOPLE_SEARCH_LIMIT = 10`, usa `take: PEOPLE_SEARCH_LIMIT + 1` no `findMany`, filtra por `organizationId` e por `OR` de `name`/`email` com `mode: 'insensitive'` e `contains`, e devolve `{ data: [], hasMore: false }` **antes** de qualquer chamada ao Prisma quando o termo aparado é vazio (o `return` antecipado está acima do `findMany`, lendo o código). Nenhum parâmetro de limite chega ao serviço vindo do controller. `rg -n "Logger|console\." apps/api/src/unit-assignments apps/api/src/people --glob '!**/__tests__/**'` é vazio.
- [x] CA1.10 — `apps/api/src/app.module.ts` importa os dois módulos novos e `apps/api/src/org-units/org-units.service.ts` tem a constante com o valor literal `Ainda há pessoas lotadas nesta unidade. Tire a lotação delas antes de apagar a unidade.`, lida na mesma consulta de `remove` por `_count: { select: { assignments: true } }`, recusando **depois** de raiz, filhas e documentos. `git diff --stat feature/087-invitations-revoke... -- apps/api/src/org-units/org-units.controller.ts apps/api/src/org-units/org-units.schema.ts` é vazio.
- [x] CA1.11 — Fronteira de acesso: `git diff --stat feature/087-invitations-revoke... -- apps/api/src/access apps/api/src/documents apps/api/src/collab` é vazio (em especial `apps/api/src/access/__tests__/document-access-boundary.test.ts` não foi alterado), e `npx vitest run --project api` inclui esse arquivo passando. `rg -n "prisma\.(document|favorite|documentContent)" apps/api/src/unit-assignments apps/api/src/people` é vazio fora de `__tests__/` (os testes de integração chamam as rotas `GET /documents` por HTTP, não o Prisma).
- [x] CA1.12 — Intocados nesta fase: `git diff --stat feature/087-invitations-revoke... -- apps/api/src/auth apps/api/src/common apps/api/src/invitations apps/api/src/installation apps/api/test apps/web vitest.config.ts eslint.config.js docs -- ':!docs/roadmap.md' ':!docs/features/010-unit-assignments/plan.md'` é vazio (roadmap e plano são editados pela orquestração: status da fatia e correção de redação de critério).
- [x] CA1.13 — `npx vitest run --project api` termina com exit code 0 e existem, pelos nomes de T1.6: os casos de `unit-assignments.service.test.ts`; os 5 de `people.service.test.ts`; os casos de `unit-assignments.integration.test.ts` (entre eles `an admin assigns a person and gets 201 with the person`, `assigning the same person twice answers 409 and keeps one row`, `two simultaneous assignments produce one 201, one 409 and one row`, `the same person can be assigned to two units`, `the list is sorted by the pt-BR collator`, `the person not found body differs from the org unit not found body`, `a non-admin answers 403 on all three routes`, `an anonymous request answers 401 on all three routes`, `the root unit accepts an assignment`, `an assignment grants no document access`, `a document of the unit space stays 404 after the assignment`, mais os dois `it.each` de 404); os 11 de `people.integration.test.ts`; os casos dos dois arquivos de contrato; e os 3 novos de `org-units.integration.test.ts` (`deleting a unit with assigned people answers 409`, `the unit is still listed after the refused delete`, `the refusals keep the order root, children, documents, people`). Conferir a presença dos nomes com `rg -n "^\s*(it|test)(\.each)?\(" apps/api/src/unit-assignments/__tests__ apps/api/src/people/__tests__ apps/api/src/org-units/__tests__`. A integração usa o Postgres real: `rg -n "vi\.mock\(.*prisma" apps/api/src/unit-assignments/__tests__/unit-assignments.integration.test.ts apps/api/src/people/__tests__/people.integration.test.ts` é vazio. Todos os casos preexistentes de `org-units.integration.test.ts` continuam listados e não foram renomeados. Nenhum `.only(` nem `.skip(`.
- [x] CA1.14 — Lendo `two simultaneous assignments produce one 201, one 409 and one row`: os dois `POST` são disparados **sem** `await` entre eles e resolvidos por um `Promise.all`, e há asserção de exatamente um status 201, um 409 e uma contagem `1` obtida por consulta direta pelo Prisma na tabela `orgUnitAssignment`. Lendo os `it.each` de 404 de unidade: os três corpos são guardados em variáveis e comparados **entre si** com `toEqual`, além da igualdade dos status — não basta comparar cada um com o literal. Lendo `an assignment grants no document access`: o corpo de `GET /documents` é guardado antes e depois da lotação e há um `toEqual` **entre os dois corpos**. Lendo `exactly ten matches answer hasMore false` em `people.integration.test.ts`: há asserção de `hasMore` igual a `false` com exatamente 10 pessoas casando o termo.
- [x] CA1.15 — `rg -n "setTimeout\(|sleep\(" apps/api/src/unit-assignments apps/api/src/people` é vazio; nenhum arquivo dessas pastas contém senha literal (as senhas vêm dos ajudantes/constantes de teste já existentes). `rg -n "eslint-disable" apps/api/src | rg -v -- "--"` é vazio.
- [x] CA1.16 — Cobertura ≥ 80% de linhas para `apps/api/src/unit-assignments/unit-assignments.service.ts`, `apps/api/src/unit-assignments/unit-assignments.controller.ts`, `apps/api/src/unit-assignments/unit-assignments.schema.ts`, `apps/api/src/people/people.service.ts`, `apps/api/src/people/people.controller.ts` e `apps/api/src/org-units/org-units.service.ts`. A medição vale por `coverage/coverage-summary.json`, gerado na raiz com `npx vitest run --coverage --coverage.reporter=json-summary --coverage.reporter=text` **sem `--project`** (com filtro de projeto o `include` da raiz não casa e o relatório sai vazio): o reporter `text` do v8 **omite arquivos com 100%**, então ausência da tabela de texto não reprova — reprova só quem aparecer no `coverage-summary.json` com `lines.pct < 80` ou não aparecer em lugar nenhum.
- [x] CA1.17 — `git log feature/087-invitations-revoke..HEAD --format=%s` mostra exatamente um commit desta fase, em inglês, no formato Conventional Commits (prefixo `feat`, `fix`, `test`, `docs`, `chore` ou `refactor` seguido de `:` ou `(escopo):`).

## Fase 2 — Web: a rota da unidade, a ação na árvore, a lista, a busca e a API simulada

Caminhos relativos à raiz do repositório.

- [x] T2.1 — As três chamadas de API e a zona de import da feature nova
  - Arquivos: `apps/web/src/features/unit-assignments/api/get-unit-people.ts` (criar); `apps/web/src/features/unit-assignments/api/assign-person.ts` (criar); `apps/web/src/features/unit-assignments/api/search-people.ts` (criar); `eslint.config.js` (alterar)
  - O que fazer (D6, R4, R5, R7):
    - `get-unit-people.ts`: `getUnitPeopleQueryOptions(orgUnitId: string)` com `queryKey: ['org-units', orgUnitId, 'people']` — a chave começa pela unidade porque tudo que as fatias 108 e 012 vão invalidar é "o que se sabe desta unidade" — chamando `api.get(\`/org-units/${orgUnitId}/people\`)` e devolvendo `{ data, orgUnit }`.
    - `assign-person.ts`: `assignPerson({ orgUnitId, personId })` chamando `api.post(\`/org-units/${orgUnitId}/people\`, { personId }, { silentError: true })`; `useAssignPerson({ orgUnitId, mutationConfig })` com `await queryClient.invalidateQueries({ queryKey: getUnitPeopleQueryOptions(orgUnitId).queryKey })` dentro do `onSuccess`, **antes** de chamar o `onSuccess` de quem usa o hook (molde de `useCreateOrgUnit`): a mutação fica `isPending` até a lista nova chegar, então a pessoa **já está** na lista quando a notificação aparece. `silentError` porque o 409 e o 404 de pessoa têm resposta melhor na própria tela do que numa notificação genérica.
    - `search-people.ts`: `getPeopleSearchQueryOptions(term: string)` com `queryKey: ['people', 'search', term]`, `enabled: term.trim().length > 0`, `placeholderData: keepPreviousData` (a lista não pisca entre teclas) e `staleTime` de 30 s, chamando `api.get('/people', …)` com `q`. **Sem** `silentError`: falha de busca é notificada pelo interceptor como qualquer outra leitura. Nenhum temporizador nesta camada.
    - `eslint.config.js`: a zona `import/no-restricted-paths` da feature `unit-assignments`, no mesmo molde das outras features (obrigatório em feature nova). Nenhuma outra regra muda.
  - Skills: api-requests, client-state, project-structure
  - Complexidade: média

- [x] T2.2 — A API simulada: lotações, pessoas e as duas famílias de handler
  - Arquivos: `apps/web/src/testing/mocks/db.ts` (alterar); `apps/web/src/testing/mocks/handlers/unit-assignments.ts` (criar); `apps/web/src/testing/mocks/handlers/people.ts` (criar); `apps/web/src/testing/mocks/handlers/index.ts` (alterar); `apps/web/src/testing/mocks/utils.ts` (alterar)
  - O que fazer (D9), com as **mesmas** regras do servidor:
    - `db.ts`: `assignments: { orgUnitId: string; personId: string }[]`; `allPeople()` (a pessoa instalada mais as criadas por convite); `addAssignment(orgUnitId: string, personId: string): 'created' | 'duplicate'`; `seedSamplePeople()` semeando 12 pessoas com nomes em pt_BR, para `hasMore` aparecer de verdade no navegador. As escritas vão no objeto que já está no array, nunca substituindo o array (a razão está escrita em `touchDocumentUpdatedAt`: o handler lê antes do `await` e escreve depois).
    - `handlers/unit-assignments.ts`: `GET` e `POST` de `${env.API_URL}/org-units/:orgUnitId/people`, com o mesmo preâmbulo dos outros handlers (`networkDelay()`, `devOverride`, 401 sem cookie ou sem instalação, 403 para não-admin) e as mesmas mensagens literais: `Unidade não encontrada.` (404), `Pessoa não encontrada.` (404), `Esta pessoa já está lotada nesta unidade.` (409). O `GET` devolve `{ data, orgUnit }` ordenado pelo colador `pt-BR`; o `POST` devolve 201 com `{ data: { id, name, email } }`.
    - `handlers/people.ts`: `GET ${env.API_URL}/people` com o **mesmo limite 10**, o mesmo `hasMore` (lendo 11 e devolvendo 10) e o mesmo "termo vazio, ausente ou em branco não devolve nada" (`{ data: [], hasMore: false }`).
    - `handlers/index.ts`: os dois handlers novos entram na lista. `utils.ts`: as chaves de desenvolvimento das duas famílias novas são anotadas.
  - Skills: api-mocking
  - Complexidade: média

- [x] T2.3 — A ação "Pessoas" na árvore e a rota `/admin/structure/:orgUnitId/people`
  - Arquivos: `apps/web/src/config/paths.ts` (alterar); `apps/web/src/app/router.tsx` (alterar); `apps/web/src/app/routes/app/admin/org-unit-people.tsx` (criar); `apps/web/src/features/org-units/components/org-units-tree.tsx` (alterar)
  - O que fazer (D7, R1, R8, R9, R11). A aparência segue `docs/design.md` e o piso da skill `interface-design`; nenhuma classe é ditada por esta tarefa além das receitas citadas.
    - `paths.ts`: `admin.orgUnitPeople = { path: '/admin/structure/:orgUnitId/people', getHref: (orgUnitId: string) => \`/admin/structure/${orgUnitId}/people\` }`. **Debaixo de `/admin/structure`** porque é de lá que se chega e para lá que se volta; uma rota irmã esconderia essa relação da URL. `router.tsx` ganha a entrada `lazy`, no mesmo molde das outras.
    - `org-units-tree.tsx`: em `renderActions`, uma ação **"Pessoas"** como **primeira** da linha, antes de "Criar unidade filha". É um `<Link>` (navegação, nunca `<button>` com `navigate`) com `className={buttonVariants({ variant: 'ghost', size: 'icon' })}`, `tabIndex={tabIndex}`, `aria-label` **e** `title` iguais a `Pessoas de {nome}`, e um ícone `UsersIcon` local (duas silhuetas, traço `currentColor`, sem preenchimento, `aria-hidden="true"`, `focusable="false"`), copiado no próprio arquivo como os outros três ícones já são. Renderizada para **todo** nó, **inclusive a raiz** — sem a condição de `parentId` que o botão "Apagar" tem. Nada mais da árvore muda.
    - `org-unit-people.tsx`: `Authorization` com `ROLES.ADMIN` e `Navigate` para a home, igual a `structure.tsx`; `useParams` para o `orgUnitId`; a consulta `getUnitPeopleQueryOptions`; e a composição de `ContentLayout` com `title={orgUnit.name}`. Enquanto carrega, o `<h1>` é **"Pessoas da unidade"** — o nome ainda não chegou, e um título vazio faria o `<h1>` pular de valor na frente de quem usa leitor de tela. É a rota que junta os dois componentes (busca e lista). Blocos, nesta ordem, com os textos literais: `<h1>` (nome da unidade); texto de apoio **"As pessoas lotadas nesta unidade."**; aviso (receita "Aviso informativo") **"Lotação ainda não dá acesso a documento: ninguém passa a ver nada por estar lotado aqui. O acesso chega com os espaços de unidade e o compartilhamento com unidade."**; link de volta **"Voltar para a estrutura"** (receita "Link de navegação"), logo abaixo do aviso; `<h2>` **"Lotar alguém"** com a busca; `<h2>` **"Pessoas lotadas"** com a lista. Quando a unidade responde 404, a página inteira é substituída por um `role="alert"` com **"Unidade não encontrada."** e o link "Voltar para a estrutura".
  - Skills: routing, authorization, interface-design
  - Complexidade: média

- [x] T2.4 — A lista de lotados e a busca com marcação, foco e envio duplo
  - Arquivos: `apps/web/src/features/unit-assignments/components/unit-people-list.tsx` (criar); `apps/web/src/features/unit-assignments/components/assign-person-search.tsx` (criar)
  - O que fazer (D6, D7, R2, R5, R7, R12, R13). A aparência segue `docs/design.md` e o piso da skill `interface-design`; nenhuma classe é ditada por esta tarefa além das receitas citadas.
    - `unit-people-list.tsx`: recebe a consulta já resolvida e desenha os **quatro** estados, cada um no mesmo lugar do conteúdo — carregando com `role="status"` e **"Carregando as pessoas lotadas…"**; vazio com **"Ninguém está lotado nesta unidade ainda. Use a busca acima para lotar a primeira pessoa."**; erro com `role="alert"`, **"Não foi possível carregar as pessoas lotadas."** e um `Button` **"Tentar novamente"**; com dados, a receita "Lista", `<ul aria-live="polite">` para a chegada de uma pessoa nova ser anunciada, cada `<li>` com o nome (`min-w-0 truncate`) e o e-mail em tom mais suave (`break-words`). A ordem vem do servidor e **não** é reordenada na tela.
    - `assign-person-search.tsx`: `<label>` **"Buscar pessoa por nome ou e-mail"** + `<input type="search">` (receita "Campo de formulário"), dica **"Digite ao menos uma letra. São mostrados até 10 resultados."**; um `<p role="status">` com a contagem — **"{n} resultados."**, **"1 resultado."** no singular, **"Buscando…"** enquanto busca, **"Ninguém encontrado com esse termo."** sem resultado, nada abaixo do campo com o campo vazio, **"Há mais resultados do que os 10 mostrados. Refine a busca."** quando `hasMore`, e erro com `role="alert"`, **"Não foi possível buscar pessoas."** e um `Button` **"Tentar novamente"**. Cada resultado é um `<li>` com nome e e-mail à esquerda e, à direita, um `Button` `variant="secondary"` **"Lotar"** com `aria-label` `Lotar {nome}`, virando **"Lotando…"** enquanto pendente. Quem já está na lista da unidade vem no lugar do botão com o selo **"Já lotado"** (receita "Selo de status", par cinza) e **nenhum** botão — a marcação compara com a lista de lotados que já está na tela, sem pedir nada ao servidor.
    - **Não é `combobox`**: campo + lista de botões, navegável por `Tab` desde a primeira linha, sem ARIA de listbox — a ação aqui **grava**, não seleciona um texto.
    - O `debounce` de 250 ms mora **neste componente**, num estado `deferredTerm` atualizado por `setTimeout` com limpeza no `useEffect` — nunca na camada de API.
    - Envio duplo barrado por `ref` (`isAssigningRef`), **não** por `mutation.isPending`: `isPending` só vira verdadeiro no próximo render, e dois `Enter` no mesmo lote de eventos passariam os dois.
    - Depois do sucesso: campo limpo, foco de volta no campo (por `ref`, prop comum) e notificação com título **"Pessoa lotada"** e mensagem **"{nome} agora está lotado em {unidade}."**. O foco vai para o campo **depois** de a lista nova ter chegado, porque a invalidação é esperada.
    - 409 e 404 de pessoa **não** viram notificação genérica (`silentError`): viram uma mensagem `role="alert"` abaixo do campo com a mensagem do servidor, e uma releitura — o 409 recarrega a lista de lotados, o 404 refaz a busca. Os dois significam "a tela está velha".
  - Skills: interface-design, forms, component-robustness, error-handling
  - Complexidade: alta

- [x] T2.5 — Testes da fase 2
  - Arquivos: `apps/web/src/features/unit-assignments/api/__tests__/get-unit-people.test.tsx` (criar); `apps/web/src/features/unit-assignments/api/__tests__/assign-person.test.tsx` (criar); `apps/web/src/features/unit-assignments/api/__tests__/search-people.test.tsx` (criar); `apps/web/src/features/unit-assignments/components/__tests__/unit-people-list.test.tsx` (criar); `apps/web/src/features/unit-assignments/components/__tests__/assign-person-search.test.tsx` (criar); `apps/web/src/app/routes/app/admin/__tests__/org-unit-people.test.tsx` (criar); `apps/web/src/features/org-units/components/__tests__/org-units-tree.test.tsx` (alterar)
  - O que fazer (D9): banco falso pelos ajudantes já usados (`seedInstalled(...)`, `seedSampleOrgUnits`, `seedSamplePeople`); interação por `userEvent`; localizar por papel e nome acessível em pt_BR; senhas vindas das constantes já existentes, **nunca** literais. `LAZY_TIMEOUT` declarada em cada arquivo e passada em **toda** espera dos casos novos logo após rota ou chunk `lazy`. Nenhuma espera fixa; nenhum `console.error`/`console.warn`/aviso de `act(...)` vazando. `src/testing/setup-tests.ts` **não** muda; **casos preexistentes não são alterados nem renomeados**.
    - `get-unit-people.test.tsx`: `requests the people of the given org unit`; `uses the org unit scoped query key`; `a 404 rejects with the not found error`.
    - `assign-person.test.tsx`: `sends POST to /org-units/:orgUnitId/people with the personId`; `a successful assignment awaits the invalidation of the unit people key` (espiando `invalidateQueries`); `errors are silent for the global notifier`.
    - `search-people.test.tsx`: `sends the term as q`; `an empty term sends no request` (`enabled` falso, contador de chamadas em 0); `a blank term sends no request`; `previous results are kept between terms`.
    - `unit-people-list.test.tsx`: `shows the loading state`; `shows the empty state`; `shows the error state with a retry button`; `shows the people in the order given by the server`; `a long e-mail does not break the row`.
    - `assign-person-search.test.tsx`: `typing a term shows the results`; `an empty term asks for nothing`; `someone already assigned shows the Já lotado badge and no button`; `clicking Lotar assigns, adds the person to the list and notifies`; `after a successful assignment the field is cleared and focused`; `two Enter presses send a single request` (handler contador); `a 409 shows the server message in an alert and refetches the list`; `a 404 of the person shows the server message in an alert and refetches the search`; `hasMore shows the refine message`; `the no access notice is on the screen`.
    - `org-unit-people.test.tsx` (rota): `an admin opens the route by URL and sees the unit name in the h1`; `while loading the h1 reads Pessoas da unidade`; `an unknown unit shows the not found alert with a link back to the structure`; `a non-admin is sent home with no request` (contador de chamadas em 0).
    - `org-units-tree.test.tsx`, casos **novos**: `every node has a Pessoas action`; `the root node also has the Pessoas action`; `the Pessoas action links to the unit people route`.
    - Fecha a fase com um commit, por exemplo `feat(web): assign people to an org unit from its page`.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: alta

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta). A saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)` vazando. `git diff feature/087-invitations-revoke... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA2.2 — Os três arquivos de API existem (antes do commit, `git status --porcelain -uall apps/web/src/features/unit-assignments/api` lista os três; depois, `git diff --stat feature/087-invitations-revoke... -- apps/web/src/features/unit-assignments/api` lista os três). `get-unit-people.ts` exporta `getUnitPeopleQueryOptions(orgUnitId)` com `queryKey: ['org-units', orgUnitId, 'people']`. `assign-person.ts` exporta `assignPerson` chamando `api.post` no caminho `/org-units/${orgUnitId}/people` com `{ personId }` e `{ silentError: true }`, e `useAssignPerson`, cujo `onSuccess` tem `await queryClient.invalidateQueries({ queryKey: getUnitPeopleQueryOptions(orgUnitId).queryKey })` **antes** da chamada a `mutationConfig?.onSuccess` (o `await` está lá, lendo o código). `search-people.ts` exporta `getPeopleSearchQueryOptions(term)` com `queryKey: ['people', 'search', term]`, `enabled` derivado de `term.trim().length > 0`, `placeholderData: keepPreviousData` e `staleTime`, e **não** passa `silentError`. `rg -n "setTimeout\(|setInterval\(" apps/web/src/features/unit-assignments/api` é vazio (o debounce não mora na camada de API). `rg -n "setQueryData|onMutate|refetchInterval" apps/web/src/features/unit-assignments/api` é vazio.
- [x] CA2.3 — `apps/web/src/features/org-units/components/org-units-tree.tsx`: `renderActions` renderiza, como **primeira** ação da linha, um `<Link>` (não um `<button>`, não um `<a href>`) com `className={buttonVariants({ variant: 'ghost', size: 'icon' })}`, `tabIndex={tabIndex}` e `aria-label` **e** `title` iguais a `Pessoas de ${node.name}`, apontando para `paths.admin.orgUnitPeople.getHref(node.id)`. Essa ação **não** está dentro de nenhuma condição de `parentId` (lendo o JSX, ela é irmã incondicional das outras, ao contrário do botão "Apagar"). O ícone é um `<svg>` local com `aria-hidden="true"` e `focusable="false"`, sem preenchimento. `git diff feature/087-invitations-revoke... -- apps/web/src/features/org-units/components/org-units-tree.tsx` não remove nem renomeia nenhuma das três ações que já existiam.
- [x] CA2.4 — `apps/web/src/config/paths.ts` tem `admin.orgUnitPeople` com `path: '/admin/structure/:orgUnitId/people'` e um `getHref(orgUnitId)` que devolve `/admin/structure/${orgUnitId}/people`; `apps/web/src/app/router.tsx` tem a entrada dessa rota com carregamento `lazy`, no mesmo molde das outras. `apps/web/src/app/routes/app/admin/org-unit-people.tsx` envolve o conteúdo em `Authorization` com `ROLES.ADMIN` e `Navigate` para a home, lê o `orgUnitId` de `useParams` e passa o nome da unidade como título do `ContentLayout`.
- [x] CA2.5 — Textos literais da página, presentes em `org-unit-people.tsx`: o título de carregamento `Pessoas da unidade`; o texto de apoio `As pessoas lotadas nesta unidade.`; o aviso `Lotação ainda não dá acesso a documento: ninguém passa a ver nada por estar lotado aqui. O acesso chega com os espaços de unidade e o compartilhamento com unidade.`; o link `Voltar para a estrutura`; os dois `<h2>` `Lotar alguém` e `Pessoas lotadas`; e, no estado de 404 da unidade, um `role="alert"` com `Unidade não encontrada.` e o link de volta. A página tem **um único** `<h1>`.
- [x] CA2.6 — Textos literais dos componentes: `unit-people-list.tsx` contém `Carregando as pessoas lotadas…` (com `role="status"`), `Ninguém está lotado nesta unidade ainda. Use a busca acima para lotar a primeira pessoa.`, `Não foi possível carregar as pessoas lotadas.` (com `role="alert"`) e `Tentar novamente`. `assign-person-search.tsx` contém `Buscar pessoa por nome ou e-mail`, `Digite ao menos uma letra. São mostrados até 10 resultados.`, `Buscando…`, `Ninguém encontrado com esse termo.`, `Há mais resultados do que os 10 mostrados. Refine a busca.`, `Não foi possível buscar pessoas.`, `Tentar novamente`, `Lotar`, `Lotando…`, `Já lotado`, `Pessoa lotada` e a mensagem de sucesso terminando em `agora está lotado em` + o nome da unidade. A contagem de resultados tem singular (`1 resultado.`) e plural (`{n} resultados.`) distintos.
- [x] CA2.7 — Acabamento (piso de `interface-design`, verificável lendo o código): a lista de lotados é um `<ul>` com `aria-live="polite"`, cada `<li>` separando o nome (à esquerda, `min-w-0` com `truncate`) do e-mail (tom mais suave, `break-words`), e a ordem renderizada é exatamente a recebida do servidor (nenhum `.sort(` em `unit-people-list.tsx`). No resultado da busca, o estado "já lotado" é um **selo** e não texto solto, e no lugar dele **não** há botão. O campo de busca tem `<label>` associado. `rg -n "<button|<a href|forwardRef|style=\{\{|: JSX\.|!important" apps/web/src/features/unit-assignments apps/web/src/app/routes/app/admin/org-unit-people.tsx` é vazio (todo botão é o `Button` compartilhado, toda navegação é `<Link>`), e todo componente desses arquivos declara retorno `React.JSX.Element`. Nenhuma largura ou altura fixa em px em contêiner de conteúdo.
- [x] CA2.8 — Robustez de `assign-person-search.tsx`: o debounce de 250 ms está **neste** arquivo, num `useEffect` com `setTimeout` e função de limpeza; a guarda de envio duplo é um `useRef` (`isAssigningRef`) consultado **antes** de disparar a mutação, e **não** `mutation.isPending`; o foco do campo volta por um `ref` (prop comum, sem `forwardRef`) no `onSuccess`, depois da invalidação esperada; o 409 e o 404 de pessoa renderizam a mensagem **vinda do servidor** num `role="alert"` abaixo do campo e disparam uma releitura (`refetch`/`invalidateQueries`), e nenhum literal de mensagem de erro do servidor está escrito no componente. Não há `role="combobox"` nem `aria-activedescendant` no arquivo.
- [x] CA2.9 — API simulada: `apps/web/src/testing/mocks/db.ts` tem o array `assignments` e exporta `allPeople`, `addAssignment(orgUnitId, personId)` — devolvendo `'created'` ou `'duplicate'` — e `seedSamplePeople`, que semeia **12** pessoas. As escritas mutam o objeto/array existente: `rg -n "assignments = " apps/web/src/testing/mocks/db.ts` não mostra substituição do array dentro de `addAssignment`. Existem `apps/web/src/testing/mocks/handlers/unit-assignments.ts` (com `http.get` e `http.post` em `${env.API_URL}/org-units/:orgUnitId/people`) e `apps/web/src/testing/mocks/handlers/people.ts` (com `http.get` em `${env.API_URL}/people`), ambos com `networkDelay()`, `devOverride`, 401 sem cookie ou sem instalação e 403 para não-admin, e ambos registrados em `handlers/index.ts`. O limite do handler de busca é **10** com leitura de 11 para o `hasMore`, e termo vazio, ausente ou em branco devolve `{ data: [], hasMore: false }`. As mensagens literais `Unidade não encontrada.`, `Pessoa não encontrada.` e `Esta pessoa já está lotada nesta unidade.` aparecem em `handlers/unit-assignments.ts`, iguais às do servidor.
- [x] CA2.10 — `eslint.config.js` tem a zona `import/no-restricted-paths` da feature `unit-assignments`, no mesmo molde das outras. A feature não importa de outra feature nem de `app/`: `rg -n "@/features/(?!unit-assignments)|@/app/" -P apps/web/src/features/unit-assignments --glob '!**/__tests__/**'` é vazio.
- [x] CA2.11 — Intocados nesta fase (base é o commit da fase 1, porque `apps/api` e `packages` mudaram legitimamente lá): `git diff --stat <commit da fase 1> -- apps/api packages apps/web/src/lib apps/web/src/types apps/web/src/components apps/web/src/utils apps/web/src/features/auth apps/web/src/features/connection apps/web/src/features/installation apps/web/src/features/documents apps/web/src/features/invitations apps/web/src/testing/setup-tests.ts apps/web/e2e vitest.config.ts docs` é vazio (exceto `docs/features/010-unit-assignments/plan.md`, editado só pela orquestração). `git diff --stat <commit da fase 1> -- apps/web/src/features/org-units` lista **só** `components/org-units-tree.tsx` e `components/__tests__/org-units-tree.test.tsx`. Nenhum `ConfirmationDialog` aparece na fatia: `rg -n "ConfirmationDialog" apps/web/src/features/unit-assignments apps/web/src/app/routes/app/admin/org-unit-people.tsx` é vazio.
- [x] CA2.12 — `npx vitest run --project web` termina com exit code 0 e existem, pelos nomes de T2.5: os 3 casos de `get-unit-people.test.tsx`; os 3 de `assign-person.test.tsx`; os 4 de `search-people.test.tsx`; os 5 de `unit-people-list.test.tsx`; os 10 de `assign-person-search.test.tsx` (entre eles `someone already assigned shows the Já lotado badge and no button`, `after a successful assignment the field is cleared and focused`, `two Enter presses send a single request`, `a 409 shows the server message in an alert and refetches the list`, `a 404 of the person shows the server message in an alert and refetches the search`); os 4 de `org-unit-people.test.tsx`; e os 3 novos de `org-units-tree.test.tsx`. Conferir os nomes com `rg -n "^\s*(it|test)\(" apps/web/src/features/unit-assignments apps/web/src/app/routes/app/admin/__tests__/org-unit-people.test.tsx apps/web/src/features/org-units/components/__tests__/org-units-tree.test.tsx`. Os casos preexistentes de `org-units-tree.test.tsx` continuam listados e não foram renomeados. Nenhum `.only(` nem `.skip(`.
- [x] CA2.13 — Lendo `two Enter presses send a single request`: há um contador de chamadas no handler e uma asserção de que ele vale **1** depois de dois `Enter` seguidos no botão "Lotar". Lendo `after a successful assignment the field is cleared and focused`: há asserção de valor vazio no campo **e** `toHaveFocus()` sobre ele, depois de a pessoa já aparecer na lista. Lendo `an empty term sends no request` e `a blank term sends no request`: há contador de chamadas com asserção de **0**. Lendo `a non-admin is sent home with no request`: há contador de chamadas com asserção de **0** e asserção da URL da home.
- [x] CA2.14 — Esperas: cada arquivo de teste desta fase declara `LAZY_TIMEOUT` e a passa em **toda** espera dos casos novos logo após abrir ou trocar de rota (`findBy…` e `waitFor`); os casos preexistentes de `org-units-tree.test.tsx` não são renomeados nem têm asserção alterada ou removida; a única alteração admitida é um `await user.tab()` a mais nos casos que caminham pela linha por teclado, consequência inevitável de a ação "Pessoas" ser a primeira da linha (CA2.3), e ela vem com comentário dizendo por quê. `rg -n "setTimeout\(|sleep\(" apps/web/src/features/unit-assignments/**/__tests__ apps/web/src/app/routes/app/admin/__tests__/org-unit-people.test.tsx` é vazio. Nenhum arquivo de teste desta fase contém senha literal. `rg -n "eslint-disable" apps/web/src apps/api/src | rg -v -- "--"` é vazio.
- [x] CA2.15 — Cobertura ≥ 80% de linhas para `apps/web/src/features/unit-assignments/api/get-unit-people.ts`, `.../api/assign-person.ts`, `.../api/search-people.ts`, `.../components/unit-people-list.tsx`, `.../components/assign-person-search.tsx`, `apps/web/src/app/routes/app/admin/org-unit-people.tsx` e `apps/web/src/features/org-units/components/org-units-tree.tsx`. A medição vale por `coverage/coverage-summary.json`, gerado na raiz com `npx vitest run --coverage --coverage.reporter=json-summary --coverage.reporter=text` **sem `--project`**: o reporter `text` do v8 omite arquivos com 100%, então ausência da tabela de texto não reprova — reprova só quem aparecer no `coverage-summary.json` com `lines.pct < 80` ou não aparecer em lugar nenhum.
- [x] CA2.16 — `git log feature/087-invitations-revoke..HEAD --format=%s` mostra exatamente dois commits (um por fase), ambos em inglês e no formato Conventional Commits.

## Fase 3 — e2e da jornada de lotação e documentação: a fatia utilizável de ponta a ponta

Caminhos relativos à raiz do repositório.

- [ ] T3.1 — Documentação: as receitas novas e a entrega na arquitetura
  - Arquivos: `docs/design.md` (alterar); `docs/architecture.md` (alterar)
  - O que fazer (D10), em pt_BR, sem reescrever o resto:
    - `docs/design.md`, na tabela "Padrões acrescentados pelas entregas", três linhas com Fatia = 010: **"Link com aparência de botão só com ícone"** (`<Link className={buttonVariants({ variant: 'ghost', size: 'icon' })}>` com `aria-label` e `title`; usado quando a ação **navega** — botão de verdade nunca navega, e `<a href>` nunca é navegação interna); **"Ícone de pessoas"** (`<svg aria-hidden="true" focusable="false" viewBox="0 0 24 24" className="size-4">`, traço `currentColor`, sem preenchimento: duas silhuetas); **"Resultados de busca"** (`<p role="status" className="mt-2 text-sm text-gray-600">` com a contagem, e abaixo a receita "Lista" com `mt-2`; item com nome e e-mail à esquerda e, à direita, a ação ou o selo de estado). E a receita **"Ações do nó da árvore" (065) é atualizada**: passa de "até três botões" para "até quatro ações (pessoas · criar filha · renomear · apagar, nessa ordem)" e passa a admitir `<Link>` com `buttonVariants` além de `Button`. Nenhuma outra receita é reescrita.
    - `docs/architecture.md`, na §4, parágrafo novo **"Entrega `unit-assignments` (fatia 010)"**: a tabela `OrgUnitAssignment` com PK composta `(orgUnitId, personId)` como única garantia de "uma pessoa não se lota duas vezes na mesma unidade", sem consulta prévia — o 409 "Esta pessoa já está lotada nesta unidade." nasce do `P2002`, como o 409 de nome da 065; `RESTRICT` nos dois lados pela mesma regra da `0008`, com a consequência de que apagar unidade passa a ter uma **quarta** recusa e de que apagar pessoa, quando existir, vai **ter** de decidir o que fazer com as lotações; migration `0012` escrita à mão; o contrato com `{ data, orgUnit }` numa requisição só e **por que** (um 404, nenhuma dependência da árvore inteira); `GET /people` com limite **duro** de 10 sem parâmetro, `q` em branco devolvendo lista vazia e `hasMore` por `take + 1`; os dois 404 com mensagens diferentes e a razão (o de unidade é opaco entre organizações, o de pessoa não é); e, **em negrito**, que **lotação não dá acesso a documento** e que o teste estrutural de fronteira (§3) cobre os módulos novos sem uma linha nova.
  - Skills: interface-design
  - Complexidade: baixa

- [ ] T3.2 — Testes da fase 3 (e2e: a administração lota alguém numa unidade)
  - Arquivos: `apps/web/e2e/tests/unit-assignments.spec.ts` (criar)
  - O que fazer (D9): contra a API simulada; localizar por papel e nome acessível em pt_BR. Estado inicial por `page.addInitScript` gravando as chaves `mock-installation=signed-in` e `mock-org-units=sample`. No topo, `const ROUTE_TIMEOUT = { timeout: 10_000 }`; **toda** espera logo após mudança de rota (`toHaveURL`, primeira visibilidade da tela nova) o recebe; nenhum `waitForTimeout`. Depois de cada `Tab`/seta e antes do `Enter`, `await expect(locator).toBeFocused()`. Nenhum literal com cara de senha ou token no arquivo. `apps/web/e2e/a11y.ts` e os outros specs não são tocados; nenhuma regra do axe é desativada.
    - Caso `an admin assigns a person to an org unit from the keyboard`: entra na página "Estrutura" pela barra lateral ("Administração" → "Estrutura"), chega à ação "Pessoas" de uma unidade **pelo teclado**, cai na página da unidade, busca por parte de um nome, lota, vê a notificação "Pessoa lotada" e a lista com a pessoa.
    - Caso `an already assigned person shows the Já lotado badge with no button`: busca a mesma pessoa de novo, encontra-a marcada "Já lotado" e sem botão; apaga o termo e a lista de resultados some.
    - `expectNoSeriousA11yViolations(page)` é chamado **duas vezes**: na página com a lista vazia e com a lista preenchida e os resultados de busca na tela.
    - Os e2e da 064, 065, 066 e 085–088 **não** mudam. Se algum deles quebrar, o conserto é no código de produção, não no spec antigo.
    - Fecha a fase com um commit, por exemplo `test(e2e): cover assigning people to an org unit and document the slice`.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [ ] CA3.1 — `pnpm test:e2e` na raiz termina com exit code 0 (o Playwright sobe o próprio servidor; não precisa de API nem de Postgres), e continuam terminando com exit code 0 e **sem aviso**, com `docker compose up -d` rodando e o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta).
- [ ] CA3.2 — `apps/web/e2e/tests/unit-assignments.spec.ts` contém os casos `an admin assigns a person to an org unit from the keyboard` e `an already assigned person shows the Já lotado badge with no button`, e `pnpm --filter web exec playwright test --list` os lista junto com todos os casos já existentes de `org-units-view.spec.ts`, `org-units-create-rename.spec.ts`, `org-units-delete.spec.ts`, `invitations-create.spec.ts`, `invitations-accept.spec.ts`, `invitations-list.spec.ts` e `invitations-revoke.spec.ts` (nenhum removido nem renomeado). `rg -n "test\.skip|test\.only|waitForTimeout" apps/web/e2e` é vazio.
- [ ] CA3.3 — Lendo o primeiro caso: a página de estrutura é alcançada **navegando pela barra lateral** ("Administração" → "Estrutura"), não por URL direta; a ação "Pessoas" é alcançada por teclado, com `toBeFocused()` antes do `Enter`; há asserção da notificação "Pessoa lotada" e de que a pessoa **está** na lista de lotados. Lendo o segundo caso: há asserção do selo "Já lotado" no resultado da busca e de que **não existe** botão "Lotar" naquele item, mais a asserção de que os resultados somem quando o termo é apagado. `expectNoSeriousA11yViolations` aparece **duas vezes** no arquivo — uma com a lista vazia e outra com lista e resultados na tela.
- [ ] CA3.4 — Esperas e segredos do e2e: o spec declara `ROUTE_TIMEOUT = { timeout: 10_000 }` no topo e **toda** asserção logo após mudança de rota (`toHaveURL` e a primeira visibilidade da tela nova) o recebe; entre cada `Tab`/seta e o `Enter` seguinte há `toBeFocused()`; não há espera fixa; não há valor literal de senha nem de token no arquivo.
- [ ] CA3.5 — `git status --porcelain -uall -- apps/web/e2e` (antes do commit) ou `git diff --stat feature/087-invitations-revoke... -- apps/web/e2e` (depois) lista **só** `apps/web/e2e/tests/unit-assignments.spec.ts`; o spec não desativa regra do axe (`rg -n "disableRules" apps/web/e2e/tests/unit-assignments.spec.ts` é vazio); os arquivos de `apps/web/e2e/**` passam no `pnpm lint` e no `pnpm typecheck`.
- [ ] CA3.6 — `docs/design.md` tem, em "Padrões acrescentados pelas entregas", as três linhas da fatia 010 — "Link com aparência de botão só com ícone", "Ícone de pessoas" e "Resultados de busca" — com o conteúdo descrito em T3.1; e a receita "Ações do nó da árvore" (065) agora diz "até quatro ações (pessoas · criar filha · renomear · apagar, nessa ordem)" e admite `<Link>` com `buttonVariants` além de `Button`. Nenhuma outra linha da tabela foi reescrita.
- [ ] CA3.7 — `docs/architecture.md` tem, na §4, o parágrafo "Entrega `unit-assignments` (fatia 010)" dizendo: PK composta `(orgUnitId, personId)` como única garantia, com o 409 nascendo do `P2002` e sem consulta prévia; `RESTRICT` nos dois lados e a quarta recusa ao apagar unidade; migration `0012` escrita à mão; `{ data, orgUnit }` numa requisição só e a razão; `GET /people` com limite duro de 10, `q` em branco devolvendo lista vazia e `hasMore` por `take + 1`; os dois 404 com mensagens diferentes e a razão; e, em negrito, que lotação **não dá acesso a documento**, coberto pelo teste estrutural de fronteira sem uma linha nova.
- [ ] CA3.8 — A fatia está utilizável de ponta a ponta sem navegador aberto por pessoa: `npx vitest run --project api` (exit code 0) inclui `an admin assigns a person and gets 201 with the person`, `two simultaneous assignments produce one 201, one 409 and one row`, `the root unit accepts an assignment`, `an assignment grants no document access`, `a non-admin answers 403 on all three routes` e `an anonymous request answers 401 on all three routes` em `apps/api/src/unit-assignments/__tests__/unit-assignments.integration.test.ts`; `npx vitest run --project web` (exit code 0) inclui `an admin opens the route by URL and sees the unit name in the h1` em `apps/web/src/app/routes/app/admin/__tests__/org-unit-people.test.tsx` e `clicking Lotar assigns, adds the person to the list and notifies` em `apps/web/src/features/unit-assignments/components/__tests__/assign-person-search.test.tsx`; e `pnpm test:e2e` (exit code 0) inclui as duas jornadas de `unit-assignments.spec.ts`.
- [ ] CA3.9 — `git log feature/087-invitations-revoke..HEAD --format=%s` mostra exatamente três commits (um por fase), todos em inglês e no formato Conventional Commits; `git diff --stat feature/087-invitations-revoke... -- docs/roadmap.md` pode ser não vazio só pelo registro da orquestração.

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
