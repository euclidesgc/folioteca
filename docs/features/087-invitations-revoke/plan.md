# PLAN 087 — invitations-revoke

Branch: `feature/087-invitations-revoke`, que **sai de `feature/088-invitations-list`** (entrega empilhada). **Toda** comparação de diff e de "arquivo intocado" deste plano é contra `feature/088-invitations-list` — nunca contra `develop`, nunca contra `main`. Onde um critério diz `<base>`, leia `feature/088-invitations-list`.

Fonte: `docs/features/087-invitations-revoke/spec.md` (D1…D7 são as decisões técnicas da SPEC e R1…R12 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; o banco de teste é `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom é da ferramenta e **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **A migration `0011` é escrita à mão**, como todas as deste projeto. Ninguém — agente, teste ou critério — roda `prisma migrate diff`, `prisma migrate dev` nem `prisma migrate reset` (o índice por expressão `lower("email")` não existe no `schema.prisma`, e qualquer `diff` proporia apagá-lo). Os únicos subcomandos permitidos são `pnpm --filter api exec prisma validate`, `pnpm --filter api exec prisma migrate status` e `pnpm --filter api exec prisma migrate deploy`, sempre contra `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test`.
- Todo componente devolve `React.JSX.Element` (nunca o `JSX` global); `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case. Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Nenhum `eslint-disable` sem `-- <motivo>`. Nenhum segredo em variável `VITE_*`; nenhum token de convite em `localStorage`, `sessionStorage` ou `console`; nenhum literal com cara de senha, token ou hash em arquivo versionado (o GitGuardian reprova o push).
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado. Não contam arquivos só de tipos, arquivos com 0 de 0 linhas instrumentáveis, nem os já excluídos pelo `vitest.config.ts` da raiz (`**/types/**`, `**/generated/**`, `**/src/testing/**`, `**/test/**`, `**/*.config.*`). Nenhuma exclusão nova.
- **Toda** espera **dos casos novos** logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes da web pela constante `LAZY_TIMEOUT` declarada no arquivo (`findBy…(…, {}, LAZY_TIMEOUT)`, `waitFor(…, LAZY_TIMEOUT)`); no e2e pela constante `ROUTE_TIMEOUT` declarada no topo do spec. Nunca `setTimeout`/sleep fixo nem `waitForTimeout`. **Casos preexistentes não são alterados nem renomeados.**
- Testes de API são de integração contra o Postgres real (padrão `apps/api/test`); "teste passa" é medido pelo **exit code** de `npx vitest run --project api` / `--project web` (zero), nunca pela leitura da saída `--verbose`.
- **Nada na resposta do servidor nem na tela diz "revogado"**: inexistente, de outra organização, aceito, vencido e já revogado respondem o **mesmo** 404, com o **mesmo** corpo (`INVITATION_UNAVAILABLE_MESSAGE = 'Convite não encontrado.'`).
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa. Cada fase termina com **um** commit Conventional Commits em inglês (`feat:`, `test:`, `docs:`), sem `--no-verify`.
- Nenhuma tarefa acrescenta escopo: sem reenvio, sem e-mail, sem desfazer, sem revogar em lote, sem mostrar convite revogado/aceito/vencido, sem filtro por situação, sem coluna `revokedById`, sem rotina de limpeza.

Desvio em relação à tabela de arquivos da SPEC, sem mudar escopo: `docs/design.md` (receita "Ícone de revogação" e a generalização do verbo na receita 066) fica na **fase 3** junto com o e2e e a arquitetura, porque a receita é uma linha de tabela e o piso visual da fase 2 já é cobrado pelos critérios de CA2. `docs/roadmap.md` (item 087 concluído; dívidas 091 e 099 encerradas; dívidas novas) **não** é tarefa nem critério de fase: a orquestração o registra no encerramento.

## Fase 1 — API: contrato `POST /invitations/{invitationId}/revoke`, migration `0011`, `pendingInvitationWhere` e `revoke`

Caminhos relativos à raiz do repositório.

- [x] T1.1 — Contrato primeiro: a operação `revokeInvitation`
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D1, R5, R6, R9):
    - Caminho **novo** `/invitations/{invitationId}/revoke` (sem prefixo `/api`, como todos os caminhos do arquivo — o `/api` é do proxy do Vite e do `env.API_URL`; o último segmento é literal, então não colide com `/invitations/{token}` da 086, e o documento continua válido), com a operação `post`, **sem** `requestBody`: `operationId: revokeInvitation`, tag `invitations`, parâmetro de rota `invitationId` (`in: path`, `required: true`, `schema: { type: string }`).
    - Respostas: **204** sem corpo; **401**, **403** e **404** com o schema `Error`, que já existe. **Não** existe `400` (não há corpo) nem `409`.
    - A `description` da operação, em pt_BR, registra as regras observáveis: só a administração; marca o convite como revogado **sem apagar a linha**; o link para de funcionar na hora; convite inexistente, de outra organização, já aceito, vencido ou já revogado respondem o **mesmo** 404; ordem de falha CSRF → `401` → `403` → `404`.
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`; o `.d.ts` nunca é editado à mão. `apps/api/test/generated-types.test.ts` e `apps/web/src/types/api.ts` não mudam.
    - O caminho `/invitations/{token}` que já existe **não** é tocado: a operação nova não é acomodada nele (o parâmetro passaria a se chamar `token` e o contrato mentiria sobre o que a web envia).
  - Skills: security
  - Complexidade: baixa

- [x] T1.2 — Migration `0011`, escrita à mão: a coluna `revokedAt` e o índice parcial com dois predicados
  - Arquivos: `apps/api/prisma/migrations/0011_invitation_revoked_at/migration.sql` (criar); `apps/api/prisma/schema.prisma` (alterar)
  - O que fazer (D3, R8):
    - `migration.sql`, exatamente com este efeito, escrito à mão e com o comentário explicando por que o predicado muda:
      ```sql
      ALTER TABLE "Invitation" ADD COLUMN "revokedAt" TIMESTAMP(3);

      DROP INDEX "Invitation_lower_email_key";

      CREATE UNIQUE INDEX "Invitation_lower_email_key" ON "Invitation"(lower("email"))
          WHERE "acceptedAt" IS NULL AND "revokedAt" IS NULL;
      ```
      Sem `DEFAULT`, sem `NOT NULL`: a coluna nasce `NULL` em todas as linhas existentes, o conjunto coberto pelo predicado novo é exatamente o mesmo de antes, e a criação do índice único não tem como falhar por duplicata.
    - `schema.prisma`: `revokedAt DateTime?` no modelo `Invitation` e o comentário `///` do modelo atualizado com o predicado vigente do índice — o índice por expressão é **invisível** ao schema, então o comentário é o único lugar onde ele fica escrito. **Nenhum** `@@index` novo. As migrations `0001`–`0010` não são tocadas.
    - Conferir com `pnpm --filter api exec prisma validate` e `pnpm --filter api exec prisma migrate status`; aplicar no banco de teste com `pnpm --filter api exec prisma migrate deploy`. **Nenhum** `migrate diff`, `dev` ou `reset`.
  - Skills: security
  - Complexidade: média

- [x] T1.3 — `pendingInvitationWhere` como única definição de "pendente", `revoke` no serviço e o `@Post(':invitationId/revoke')` que herda os guards
  - Arquivos: `apps/api/src/invitations/invitations.service.ts` (alterar); `apps/api/src/invitations/invitations.controller.ts` (alterar)
  - O que fazer (D2, R5, R6, R7, R8, R9):
    - Em `invitations.service.ts`, **uma** definição exportada de "pendente":
      ```ts
      /** A única definição de "convite pendente" do servidor. */
      export const pendingInvitationWhere = (now: Date = new Date()) =>
        ({ acceptedAt: null, revokedAt: null, expiresAt: { gt: now } }) satisfies
          Prisma.InvitationWhereInput;
      ```
    - `findPending` troca o `findUnique({ where: { tokenHash } })` por `findFirst({ where: { tokenHash, ...pendingInvitationWhere() } })` e perde as checagens em memória `isValid` e `isUnused`. O `timingSafeEqual` sobre os dois digests **continua**, depois da consulta.
    - `list` troca as duas condições literais por `...pendingInvitationWhere()`.
    - `create` troca `deleteMany({ where: { email } })` por `deleteMany({ where: { email, ...pendingInvitationWhere() } })`: sem isso, convidar de novo o mesmo e-mail **apagaria** a linha revogada (e a aceita).
    - O comentário cruzado que a 088 deixou em `findPending` e em `list` (anunciando que a 087 acrescentaria `revokedAt` nos dois) é **apagado**: não sobra segunda cópia da regra.
    - Método novo `revoke(organizationId: string, invitationId: string): Promise<void>`:
      ```ts
      const { count } = await this.prisma.invitation.updateMany({
        where: { id: invitationId, organizationId, ...pendingInvitationWhere() },
        data: { revokedAt: new Date() },
      });

      if (count === 0) {
        throw new DomainNotFoundException(INVITATION_UNAVAILABLE_MESSAGE);
      }
      ```
      `updateMany` e **não** `findFirst` + `update`: o `where` e a escrita viram uma instrução só, então duas abas revogando ao mesmo tempo não conseguem os dois `count = 1`. Nenhum `Person`, `Space` ou `Session` é tocado: a operação é um `UPDATE` de uma coluna.
    - `invitations.controller.ts`: `@Post(':invitationId/revoke')` + `@HttpCode(204)` no `InvitationsController`, que **já tem** `@UseGuards(SessionGuard, AdminGuard)` na classe — **nenhuma linha de guard é escrita nesta fatia**. O `organizationId` vem de `@CurrentPerson()`, nunca da rota; retorno `Promise<void>`. **Nunca** no `PublicInvitationsController`, que não tem guard.
    - Sem `Logger` e sem `console.` no módulo fora de testes.
  - Skills: authorization, security
  - Complexidade: alta

- [x] T1.4 — Testes da fase 1
  - Arquivos: `apps/api/src/invitations/__tests__/invitations.service.test.ts` (alterar); `apps/api/src/invitations/__tests__/invitations.integration.test.ts` (alterar); `apps/api/src/invitations/__tests__/invitations.contract.test.ts` (alterar)
  - O que fazer (D7): integração e contrato contra o Postgres real, sem mock do Prisma, `resetDatabase(prisma)` em `beforeEach`, instalação e sessões pelos ajudantes já usados nos testes de `invitations`; ajudante local `revokeInvitation(id, cookie?)` mandando `X-Requested-With: XMLHttpRequest`. `apps/api/test/**` **não muda**. Convite aceito e convite vencido são gravados direto pelo Prisma (ou aceitos pela rota pública), nunca mexendo no relógio. Nenhuma espera fixa; nenhum literal com cara de senha ou token. **Casos preexistentes não são renomeados nem alterados.**
    - `invitations.service.test.ts` (Prisma falso), casos novos: `revoke updates only the pending invitation of the organization` (o `where` do `updateMany` tem `id`, `organizationId`, `acceptedAt: null`, `revokedAt: null` e `expiresAt: { gt: <Date> }`); `revoke writes only revokedAt` (o `data` é **só** `{ revokedAt }`); `revoke with no row rejects with the generic not found` (`count: 0` → `DomainNotFoundException` com `INVITATION_UNAVAILABLE_MESSAGE`); `list, findPending and create share the same pending filter` (asserção sobre os argumentos capturados, provando o mesmo objeto nos três).
    - `invitations.integration.test.ts`, casos novos: `revoking a pending invitation answers 204 with no body`; `a revoked invitation disappears from the list`; `the revoked link answers exactly what an unknown link answers` — guarda o corpo do `GET /invitations/{token}` de um token que nunca existiu e o corpo do mesmo `GET` com o token revogado e afirma `toEqual` **entre os dois corpos** (não contra o literal da mensagem), além do mesmo status; `accepting a revoked token answers the same not found`; `revoking twice answers the same not found` (mesmo corpo do primeiro item); `it.each` com `an accepted invitation`, `an expired invitation`, `an unknown id` e `an invitation of another organization` → os quatro com o **mesmo** status e o **mesmo** corpo; `a non-admin answers 403` (mensagem "Apenas a administração pode fazer isso.") e `an anonymous request answers 401` (mensagem "Sessão não encontrada."), e nos dois o convite **continua pendente** depois da tentativa; `the same e-mail can be invited again after a revoke` — convida `ana@exemplo.com.br`, revoga, convida o mesmo e-mail → **201**, a lista mostra **um** convite, e o convite revogado **continua na tabela** com `revokedAt` preenchido (consulta direta pelo Prisma); `revoking keeps the session of who already accepted` (R7: `Person` e `Session` intactos).
    - `invitations.contract.test.ts`, casos novos: `POST revoke invitation answers the documented 401`; `POST revoke invitation answers the documented 403`; `POST revoke invitation answers the documented 404`; `POST revoke invitation answers 204 with an empty body` (status e corpo vazio; não há schema para validar).
    - Fecha a fase com um commit, por exemplo `feat(api): revoke a pending invitation`.
  - Skills: unit-testing, integration-testing, security
  - Complexidade: alta

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (sem aviso de peer no install; sem aviso de chunk grande no build; o aviso de desempenho do Vitest sobre jsdom não conta). A saída de `pnpm test` não tem `console.error` nem `console.warn` vazando, nem aviso do Nest ou do Prisma. `git diff feature/088-invitations-list... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA1.2 — `packages/api-contract/openapi.yaml` tem o caminho `/invitations/{invitationId}/revoke` (sem prefixo `/api`) com a operação `post` e **sem** `requestBody`, `operationId: revokeInvitation`, tag `invitations` e **um** parâmetro `invitationId` em `path`, `required: true`, `schema.type: string`. As respostas são exatamente `204` (sem `content`), `401`, `403` e `404`, as três últimas referenciando o schema `Error`; **não** há `400` nem `409` nessa operação. A `description` menciona, em pt_BR, que só a administração revoga, que a linha não é apagada, que o link para de funcionar e que inexistente, de outra organização, aceito, vencido e já revogado respondem o mesmo 404. O caminho `/invitations/{token}` continua existindo e não foi renomeado. `rg -n "revokeInvitation" packages/api-contract/src/generated/openapi.d.ts` encontra a operação; `apps/api/test/generated-types.test.ts` passa sem ter sido alterado; `git diff feature/088-invitations-list... -- apps/web/src/types/api.ts` é vazio.
- [x] CA1.3 — A migration existe e foi escrita à mão: antes do commit, `git status --porcelain -uall apps/api/prisma/migrations/0011_invitation_revoked_at/migration.sql` lista o arquivo; depois do commit, `git diff --stat feature/088-invitations-list... -- apps/api/prisma/migrations/0011_invitation_revoked_at/migration.sql` lista o arquivo. Lendo o SQL: um `ALTER TABLE "Invitation" ADD COLUMN "revokedAt" TIMESTAMP(3);` **sem** `DEFAULT` e **sem** `NOT NULL`; um `DROP INDEX "Invitation_lower_email_key";`; e um `CREATE UNIQUE INDEX "Invitation_lower_email_key" ON "Invitation"(lower("email"))` com `WHERE "acceptedAt" IS NULL AND "revokedAt" IS NULL`. `git diff --stat feature/088-invitations-list... -- apps/api/prisma/migrations` lista **só** o diretório `0011_invitation_revoked_at` (as migrations `0001`–`0010` seguem idênticas).
- [x] CA1.4 — Prova no banco de que o índice ficou com os **dois** predicados. Com `docker compose up -d`, `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test pnpm --filter api exec prisma migrate deploy` termina com exit code 0, `pnpm --filter api exec prisma validate` passa e `DATABASE_URL=… pnpm --filter api exec prisma migrate status` reporta nenhuma migration pendente. Em seguida, `psql postgresql://folioteca@localhost:5433/folioteca_test -At -c "select indexdef from pg_indexes where indexname = 'Invitation_lower_email_key'"` imprime **uma** linha contendo `UNIQUE`, `lower(email)`, `acceptedAt IS NULL` **e** `revokedAt IS NULL` (os dois predicados na mesma definição). Não existe segundo índice único sobre `lower(email)`: a mesma consulta sem o filtro de nome (`where indexdef ilike '%lower(email)%'`) devolve exatamente uma linha.
- [x] CA1.5 — Nenhum `prisma migrate diff/dev/reset` entrou no código: `git diff feature/088-invitations-list... -- apps/api apps/web packages | rg -n "migrate (diff|dev|reset)"` é vazio (`docs/` fica fora porque a documentação cita esses comandos justamente para proibi-los).
- [x] CA1.6 — `apps/api/src/invitations/invitations.service.ts` exporta **uma** função `pendingInvitationWhere(now: Date = new Date())` devolvendo um objeto com `acceptedAt: null`, `revokedAt: null` e `expiresAt: { gt: now }`. Ela é usada por `findPending`, por `list`, pelo `deleteMany` de `create` e pelo `revoke` novo (`rg -n "pendingInvitationWhere" apps/api/src/invitations/invitations.service.ts` mostra a definição e pelo menos quatro usos). `findPending` usa `findFirst` com `tokenHash` **e** o espalhamento de `pendingInvitationWhere()`, não tem mais checagem em memória de validade ou de uso, e **continua** chamando `timingSafeEqual` depois da consulta. Não sobrou nenhuma condição literal `acceptedAt: null` ou `expiresAt: { gt:` fora da definição única (`rg -n "acceptedAt: null" apps/api/src/invitations/invitations.service.ts` mostra apenas a linha de `pendingInvitationWhere`). O comentário cruzado deixado pela fatia 088 em `findPending` e em `list` não está mais no arquivo.
- [x] CA1.7 — `revoke(organizationId: string, invitationId: string): Promise<void>` existe em `invitations.service.ts`, chama `this.prisma.invitation.updateMany` com `where` contendo `id`, `organizationId` e o espalhamento de `pendingInvitationWhere()`, `data` **só** com `{ revokedAt: … }`, e lança `DomainNotFoundException(INVITATION_UNAVAILABLE_MESSAGE)` quando `count === 0`. Não há `findFirst`/`findUnique` antes da escrita dentro de `revoke`, não há `delete`/`deleteMany` no método, e ele não menciona `person`, `space` nem `session` (`rg -n "deleteMany|prisma\.(person|space|session)" ` dentro do corpo de `revoke` não encontra nada). `rg -n "Logger|console\." apps/api/src/invitations --glob '!**/__tests__/**'` é vazio.
- [x] CA1.8 — `apps/api/src/invitations/invitations.controller.ts` continua com `@UseGuards(SessionGuard, AdminGuard)` **na classe** e ganha **um** método com `@Post(':invitationId/revoke')` e `@HttpCode(204)`, que obtém o `organizationId` de `@CurrentPerson()` e devolve `Promise<void>`. `git diff feature/088-invitations-list... -- apps/api/src/invitations/invitations.controller.ts` **não** acrescenta nenhuma linha com `@UseGuards`. `git diff --stat feature/088-invitations-list... -- apps/api/src/invitations/public-invitations.controller.ts` é vazio (o link revogado para de abrir sem uma linha nova ali).
- [x] CA1.9 — Intocados nesta fase: `git diff --stat feature/088-invitations-list... -- apps/api/src/auth apps/api/src/common apps/api/src/invitations/invitations.schema.ts apps/api/src/invitations/public-invitations.controller.ts apps/api/src/invitations/invitations.module.ts apps/api/src/app.module.ts apps/api/src/access apps/api/src/documents apps/api/src/org-units apps/api/src/installation apps/api/test apps/web vitest.config.ts eslint.config.js` é vazio.
- [x] CA1.10 — `npx vitest run --project api` termina com exit code 0 e existem, pelos nomes de T1.4: os 4 casos novos de `invitations.service.test.ts` (`revoke updates only the pending invitation of the organization`, `revoke writes only revokedAt`, `revoke with no row rejects with the generic not found`, `list, findPending and create share the same pending filter`); os casos novos de `invitations.integration.test.ts` (entre eles `revoking a pending invitation answers 204 with no body`, `a revoked invitation disappears from the list`, `the revoked link answers exactly what an unknown link answers`, `accepting a revoked token answers the same not found`, `revoking twice answers the same not found`, `a non-admin answers 403`, `an anonymous request answers 401`, `the same e-mail can be invited again after a revoke`, `revoking keeps the session of who already accepted`, mais o `it.each` com os três estados alcançáveis por HTTP mais o de outra organização, provado no serviço); e os 4 casos novos de `invitations.contract.test.ts`. Conferir a presença dos nomes com `rg -n "^\s*(it|test)(\.each)?\(" apps/api/src/invitations/__tests__`. A integração usa o Postgres real: `rg -n "vi\.mock\(.*prisma" apps/api/src/invitations/__tests__/invitations.integration.test.ts apps/api/src/invitations/__tests__/invitations.contract.test.ts` é vazio. Todos os casos preexistentes dos três arquivos continuam listados e não foram renomeados. Nenhum `.only(` nem `.skip(`.
- [x] CA1.11 — Lendo `the revoked link answers exactly what an unknown link answers` em `invitations.integration.test.ts`: o teste guarda os **dois corpos** (token inexistente e token revogado) em variáveis e há uma asserção de igualdade **entre elas** (`expect(revokedBody).toEqual(unknownBody)`), além da igualdade dos status — não basta comparar cada um com o literal `'Convite não encontrado.'`. Lendo o `it.each` dos três estados alcançáveis por HTTP (aceito, vencido, id inexistente): os três são comparados com o **mesmo** corpo e o **mesmo** status (404); "outra organização" não é alcançável pela rota (a organização é única no banco) e é provado num teste próprio, chamando o serviço e afirmando a mesma mensagem, com a linha do convite intacta. Lendo `the same e-mail can be invited again after a revoke`: há asserção de `201` no segundo convite, de que a lista tem **um** item, e uma consulta direta pelo Prisma provando que a linha revogada **continua** na tabela com `revokedAt` preenchido.
- [x] CA1.12 — Nada diz "revogado" ao cliente: `git diff feature/088-invitations-list... -- packages/api-contract/openapi.yaml apps/api/src/invitations/invitations.service.ts apps/api/src/invitations/invitations.controller.ts` não acrescenta nenhuma mensagem de erro nova (a única mensagem de recusa continua sendo a constante `INVITATION_UNAVAILABLE_MESSAGE`, com o valor `'Convite não encontrado.'`, que não foi alterada). Nenhum corpo de resposta do `POST …/revoke` de sucesso: o 204 não tem `content` no contrato nem `res.json` no controller.
- [x] CA1.13 — `rg -n "setTimeout\(|sleep\(" apps/api/src/invitations` é vazio; nenhum arquivo de `apps/api/src/invitations/**` contém senha literal nem cadeia de 20+ caracteres aleatórios servindo de token (as senhas vêm dos ajudantes/constantes de teste já existentes; os tokens vêm do corpo da resposta). `rg -n "eslint-disable" apps/api/src | rg -v -- "--"` é vazio.
- [x] CA1.14 — Cobertura ≥ 80% de linhas para `apps/api/src/invitations/invitations.service.ts` e `apps/api/src/invitations/invitations.controller.ts`. A medição vale por `coverage/coverage-summary.json`, gerado na raiz com `npx vitest run --coverage --coverage.reporter=json-summary --coverage.reporter=text` **sem `--project`** (com filtro de projeto o `include` da raiz não casa e o relatório sai vazio): o reporter `text` do v8 **omite arquivos com 100%**, então ausência da tabela de texto não reprova — reprova só quem aparecer no `coverage-summary.json` com `lines.pct < 80` ou não aparecer em lugar nenhum.
- [x] CA1.15 — `git log feature/088-invitations-list..HEAD --format=%s` mostra exatamente um commit desta fase, em inglês, no formato Conventional Commits (prefixo `feat`, `fix`, `test`, `docs`, `chore` ou `refactor` seguido de `:` ou `(escopo):`).

## Fase 2 — Web: a mutação, a ação na lista, o diálogo, o foco e a API simulada

Caminhos relativos à raiz do repositório.

- [x] T2.1 — A mutação `POST /invitations/{id}/revoke` com a invalidação esperada
  - Arquivos: `apps/web/src/features/invitations/api/revoke-invitation.ts` (criar)
  - O que fazer (D4, R4, R6):
    - No molde exato de `features/org-units/api/delete-org-unit.ts`: `revokeInvitation({ invitationId }: { invitationId: string }): Promise<void>` chamando `api.post(\`/invitations/${invitationId}/revoke\`)`; `useRevokeInvitation({ mutationConfig })` com `invalidateQueries({ queryKey: getInvitationsQueryOptions().queryKey })` **esperado com `await`** dentro do `onSuccess`, **antes** de chamar o `onSuccess` de quem usa o hook — assim a mutação continua `isPending` até a lista nova chegar, e quem fecha o diálogo já encontra a lista sem a linha.
    - **Sem `silentError`**: o 404 é notificado pelo interceptor global com a mensagem do servidor, que é justamente a que não diz o motivo. Nada de atualização otimista, nada de `setQueryData`, nada de `refetchInterval`.
  - Skills: api-requests, client-state
  - Complexidade: baixa

- [x] T2.2 — A API simulada: `revokedAt` no banco falso, o predicado `isPending` e o handler `POST …/revoke`
  - Arquivos: `apps/web/src/testing/mocks/db.ts` (alterar); `apps/web/src/testing/mocks/handlers/invitations.ts` (alterar); `apps/web/src/testing/mocks/utils.ts` (alterar)
  - O que fazer (D6, R5, R6, R8, R9):
    - `db.ts`: `MockInvitation` ganha `revokedAt: string | null` (nulo em `addInvitation`). Função nova `revokeInvitation(id: string): boolean`, que **marca** `revokedAt` no objeto que já está no array — nunca `splice`, nunca substituindo o array (mesma razão de `touchDocumentUpdatedAt`: o handler lê antes do `await` e escreve depois). Devolve `false` para id desconhecido ou convite não pendente, que é o 404. `addInvitation` passa a remover **só** o convite pendente do mesmo e-mail, espelhando o `deleteMany` estreitado do servidor.
    - `handlers/invitations.ts`: o predicado `isPending` que já está lá ganha `invitation.revokedAt === null`. Como `findAvailableInvitation` usa `isPending`, o link revogado deixa de abrir nos dois handlers públicos, com o mesmo `invitationUnavailable()`, **sem uma linha nova neles**.
    - Handler novo `http.post(\`${env.API_URL}/invitations/:invitationId\`)`, declarado **depois** do `post` de `/invitations` e **antes** dos handlers de `/invitations/:token`. Mesmo preâmbulo dos outros: `networkDelay()`, `devOverride('invitations')`, 401 sem cookie ou sem instalação, 403 para não-admin. Depois: `revokeInvitation(id)` → `new HttpResponse(null, { status: 204 })` ou `invitationUnavailable()`.
    - `utils.ts`: **só** o comentário das chaves de desenvolvimento — `mock-invitations=sample` agora também dá o que revogar. `seedSampleInvitations` **não** ganha convite revogado (a lista não mostra revogados; o caso de borda é semeado pelo próprio teste, com handler próprio).
  - Skills: api-mocking
  - Complexidade: média

- [x] T2.3 — A ação na lista, o diálogo único e a regra de foco
  - Arquivos: `apps/web/src/features/invitations/components/invitations-list.tsx` (alterar); `apps/web/src/app/routes/app/admin/invitations.tsx` (alterar)
  - O que fazer (D5, R1, R2, R3, R4, R6, R10, R11). A aparência segue `docs/design.md` e o piso da skill `interface-design`; nenhuma classe é ditada por esta tarefa.
    - **O cabeçalho da seção muda de arquivo**: o `<h2>` "Convites pendentes" e o texto de apoio saem de `app/routes/app/admin/invitations.tsx` e entram no componente, dentro de um `<section>`, acima dos três estados alcançáveis por HTTP mais o de outra organização, provado no serviço. Motivo: o destino de foco de "a lista ficou vazia" é esse cabeçalho, e o componente não tem como focar um elemento que mora na rota. A tela fica **idêntica** (mesmas classes, mesma ordem); a rota passa a renderizar só `<InvitationsList />`, sem mudar nada acima do bloco do link do convite criado.
    - Cada `<li>` ganha, à direita, a coluna de ações da receita "Ações do item de lista" com **um** `Button` `variant="ghost"` + `size="icon"`, `type="button"`, `aria-label` e `title` **"Revogar {e-mail}"**, com um ícone de revogação local (círculo com barra diagonal, `aria-hidden="true"`, `focusable="false"`, traço `currentColor`, sem preenchimento) — **nunca** a lixeira, que no app significa remoção definitiva. Seis linhas de SVG no próprio arquivo, como `OrgUnitsTree` já faz com o `TrashIcon`.
    - `LoadedInvitationsList` passa a segurar o estado da revogação, no mesmo desenho de `OrgUnitsTree`: **um** `ConfirmationDialog` para a lista inteira, **sem `trigger`**; `revoking: { id, email, neighbourId } | null` guardando uma **cópia** do que o diálogo precisa (depois do sucesso o convite não está mais na lista, e a descrição ainda mostra o e-mail enquanto a caixa fecha); `isRevokeOpen` separado do `revoking`, porque o fechamento é animado; guarda de clique duplo em `handleConfirmRevoke` (`if (mutation.isPending) return;`).
    - Textos literais do diálogo, em pt_BR: título **"Revogar convite?"**; descrição **"O convite de “{e-mail}” deixa de valer agora, e o link enviado para de funcionar. Não é possível desfazer nem reenviar: para este e-mail voltar a ter um convite válido, convide de novo no formulário acima, o que gera um link novo."**; botão de confirmar **"Revogar"** (destrutivo), **"Revogando…"** enquanto pendente; cancelar **"Cancelar"** (padrão do componente).
    - Notificação de sucesso: título **"Convite revogado"**, mensagem **"O convite de {e-mail} não vale mais."**. No 404 (`NotFoundError`), o diálogo **fecha**, a lista recarrega e a notificação de erro é a do interceptor; **nenhum texto da tela menciona revogação como motivo de recusa**. Qualquer outra falha **mantém** o diálogo aberto, com a notificação do interceptor explicando.
    - **Destino do foco, regra fechada**, em `onCloseAutoFocus`: (1) revogação bem-sucedida → o botão "Revogar" do item **imediatamente acima** do que sumiu; (2) se o que sumiu era o primeiro, o botão do item que **passou a ser** o primeiro; (3) se não sobrou item nenhum, o `<h2>` "Convites pendentes", que ganha `tabIndex={-1}` (e só assim: não entra na ordem de `Tab`); (4) cancelar, `Escape` ou recusa seguida de cancelar → o botão que abriu o diálogo, enquanto ele ainda está lá (`opener?.isConnected`). O vizinho é escolhido **na abertura**, sobre a lista que está na tela, e guardado em `neighbourId`. Os botões são alcançados por um `useRef(new Map<string, HTMLButtonElement>())` preenchido pelo `ref` de cada item; se o id guardado não estiver mais no mapa, o foco cai na regra 3. Tudo em `ref`, nunca em estado.
  - Skills: interface-design, component-robustness, error-handling
  - Complexidade: alta

- [x] T2.4 — Testes da fase 2
  - Arquivos: `apps/web/src/features/invitations/api/__tests__/revoke-invitation.test.tsx` (criar); `apps/web/src/features/invitations/components/__tests__/invitations-list.test.tsx` (alterar); `apps/web/src/app/routes/app/admin/__tests__/invitations.test.tsx` (alterar)
  - O que fazer (D7): banco falso pelos ajudantes já usados (`seedInstalled(...)`); interação por `userEvent`; localizar por papel e nome acessível em pt_BR; senhas vindas das constantes já existentes, **nunca** literais. `LAZY_TIMEOUT` declarada em cada arquivo e passada em **toda** espera dos casos novos logo após rota ou chunk `lazy`. Fuso fixado no teste para as datas serem determinísticas. Nenhuma espera fixa; nenhum `console.error`/`console.warn`/aviso de `act(...)` vazando. `src/testing/setup-tests.ts` **não** muda; **casos preexistentes não são alterados nem renomeados**.
    - `revoke-invitation.test.tsx`: `sends POST to /invitations/:id/revoke`; `a successful revoke invalidates the invitations query key` (espiando `invalidateQueries` e conferindo a chave `['invitations']`); `a failure rejects with the global notification` (sem `silentError`).
    - `invitations-list.test.tsx`, casos novos: `each item has a Revogar button named after the e-mail`; `clicking Revogar opens the dialog with the e-mail in the description`; `Cancelar calls no API and returns the focus to the button`; `confirming revokes, removes the row and notifies`; `revoking the second of two focuses the button above`; `revoking the first of two focuses the new first button`; `revoking the only invitation focuses the heading`; `a 404 closes the dialog, refetches and notifies`; `a 500 keeps the dialog open`; `two Enter presses send a single request` (handler contador).
    - `invitations.test.tsx` (rota), casos novos: `an admin revokes one invitation and still sees the other`; `the Convites pendentes heading is still on the page`.
    - Fecha a fase com um commit, por exemplo `feat(web): revoke a pending invitation from the list`.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta). A saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)` vazando. `git diff feature/088-invitations-list... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA2.2 — `apps/web/src/features/invitations/api/revoke-invitation.ts` existe (antes do commit, `git status --porcelain -uall apps/web/src/features/invitations/api/revoke-invitation.ts` lista o arquivo; depois, `git diff --stat feature/088-invitations-list... -- apps/web/src/features/invitations/api/revoke-invitation.ts` lista o arquivo) e exporta `revokeInvitation({ invitationId })` chamando `api.post` num caminho `/invitations/${invitationId}` e `useRevokeInvitation`. Dentro do `onSuccess` do hook há `await queryClient.invalidateQueries({ queryKey: getInvitationsQueryOptions().queryKey })` **antes** da chamada a `mutationConfig?.onSuccess` (o `await` está lá, lendo o código). O arquivo **não** passa `silentError`; `rg -n "setQueryData|refetchInterval|onMutate" apps/web/src/features/invitations/api/revoke-invitation.ts` é vazio.
- [x] CA2.3 — `apps/web/src/features/invitations/components/invitations-list.tsx`: cada item da lista renderiza um `Button` de `@/components/ui/button/button` com `variant="ghost"`, `size="icon"`, `type="button"` e `aria-label` **e** `title` iguais a `Revogar ${invitation.email}`. O ícone é um `<svg>` local com `aria-hidden="true"` e `focusable="false"`, sem preenchimento, e **não** é uma lixeira: `rg -n "TrashIcon|lixeira" apps/web/src/features/invitations/components/invitations-list.tsx` é vazio. `rg -n "<button" apps/web/src/features/invitations/components/invitations-list.tsx` é vazio (todo botão é o `Button` compartilhado).
- [x] CA2.4 — Diálogo, com os textos literais: o arquivo usa **um** `ConfirmationDialog` para a lista inteira, **sem** prop `trigger` (`rg -c "<ConfirmationDialog" apps/web/src/features/invitations/components/invitations-list.tsx` devolve 1 e não há `trigger=` nessa chamada). Os textos presentes no arquivo são exatamente: `Revogar convite?`; a descrição começando por `O convite de` e contendo `deixa de valer agora, e o link enviado para de funcionar.`, `Não é possível desfazer nem reenviar` e `convide de novo no formulário acima, o que gera um link novo.`, com o e-mail interpolado; o rótulo de confirmar `Revogar` e o de pendente `Revogando…`. A notificação de sucesso tem título `Convite revogado` e mensagem `O convite de ${email} não vale mais.`. **Nenhum texto de tela diz que um convite foi "revogado" como motivo de recusa**: a única mensagem de erro exibida vem do interceptor global (não há literal de erro escrito no componente).
- [x] CA2.5 — Estado e foco, lendo `invitations-list.tsx`: existe um estado `revoking` guardando `id`, `email` e `neighbourId` (uma cópia, não uma referência ao item da lista) e um `isRevokeOpen` separado dele; `handleConfirmRevoke` começa com uma guarda de clique duplo baseada em `isPending` da mutação; há um `useRef` com um `Map<string, HTMLButtonElement>` preenchido pelo `ref` de cada item (prop comum, sem `forwardRef`); `onCloseAutoFocus` implementa as quatro regras — vizinho de cima, novo primeiro, o `<h2>` (que tem `tabIndex={-1}`) quando a lista esvazia, e o botão que abriu o diálogo quando a interação foi cancelada, com checagem de `isConnected`. O `neighbourId` é calculado no manipulador que **abre** o diálogo, não depois do sucesso. Só o sucesso e o 404 fecham o diálogo; qualquer outra falha o mantém aberto (lendo o tratamento de erro).
- [x] CA2.6 — `apps/web/src/app/routes/app/admin/invitations.tsx`: o `<h2>` "Convites pendentes" e o texto de apoio "O link de cada convite aparece uma única vez, quando ele é criado, e não pode ser mostrado de novo. Para gerar um link novo, convide o mesmo e-mail outra vez." **saíram** da rota e estão agora em `invitations-list.tsx`, dentro de um `<section>`, acima dos três estados alcançáveis por HTTP mais o de outra organização, provado no serviço (`rg -n "Convites pendentes" apps/web/src/app/routes/app/admin/invitations.tsx` não encontra o `<h2>`; `rg -n "Convites pendentes" apps/web/src/features/invitations/components/invitations-list.tsx` encontra). `git diff feature/088-invitations-list... -- apps/web/src/app/routes/app/admin/invitations.tsx` não altera nenhuma linha acima do bloco do link do convite criado, e a página continua com **um único** `<h1>` ("Convites").
- [x] CA2.7 — Acabamento (piso de `interface-design`, verificável lendo o código): os três estados alcançáveis por HTTP mais o de outra organização, provado no serviço da consulta continuam existindo, cada um estilizado e no mesmo lugar do conteúdo — carregando com `role="status"` e "Carregando convites…", vazio com "Nenhum convite pendente.", erro com `role="alert"`, "Não foi possível carregar os convites." e um `Button` "Tentar novamente"; o `<li>` continua em `flex-wrap` com o bloco do e-mail em `min-w-0 break-words` (nunca `truncate`) e a coluna de ações alinhada à direita, funcionando a 360px sem rolagem horizontal; o botão de revogar tem alvo de clique de 40px (`size-10` da receita "Botão só com ícone"), com `hover`, `focus-visible` e `disabled` vindos do `Button`. `rg -n "forwardRef|style=\{\{|: JSX\.|!important" apps/web/src/features/invitations apps/web/src/app/routes/app/admin/invitations.tsx` é vazio e todo componente declara retorno `React.JSX.Element`.
- [x] CA2.8 — API simulada: `apps/web/src/testing/mocks/db.ts` tem `revokedAt: string | null` em `MockInvitation` (nulo em `addInvitation`) e exporta `revokeInvitation(id: string): boolean`, que **atribui** `revokedAt` ao objeto existente e devolve `false` para id desconhecido ou convite não pendente — `rg -n "splice|invitations = " apps/web/src/testing/mocks/db.ts` não mostra nenhuma remoção nem substituição do array de convites dentro de `revokeInvitation`. `addInvitation` remove **só** o convite pendente do mesmo e-mail. Em `apps/web/src/testing/mocks/handlers/invitations.ts`, o predicado `isPending` inclui `revokedAt === null` e continua sendo usado por `findAvailableInvitation` (por isso o link revogado deixa de abrir sem linha nova nos handlers públicos); existe um `http.post` em `${env.API_URL}/invitations/:invitationId` declarado **depois** do `http.post` de `/invitations` e **antes** dos handlers de `/invitations/:token`, com `networkDelay()`, `devOverride('invitations')`, 401 sem cookie ou sem instalação, 403 para não-admin, e respondendo `new HttpResponse(null, { status: 204 })` ou `invitationUnavailable()`. `git diff feature/088-invitations-list... -- apps/web/src/testing/mocks/utils.ts` só tem linhas iniciadas por `//` além do contexto. `seedSampleInvitations` não semeia convite revogado.
- [x] CA2.9 — Intocados nesta fase (base é o commit da fase 1, porque `packages` e `apps/api` mudaram legitimamente lá): `git diff --stat <commit da fase 1> -- apps/web/src/lib apps/web/src/types apps/web/src/components apps/web/src/config apps/web/src/utils apps/web/src/features/auth apps/web/src/features/connection apps/web/src/features/installation apps/web/src/features/documents apps/web/src/features/org-units apps/web/src/testing/setup-tests.ts apps/web/src/features/invitations/api/get-invitations.ts apps/web/src/features/invitations/api/create-invitation.ts apps/web/src/features/invitations/api/get-invitation.ts apps/web/src/features/invitations/api/accept-invitation.ts apps/web/src/features/invitations/components/create-invitation-form.tsx apps/web/src/features/invitations/components/invitation-link.tsx apps/web/src/features/invitations/components/accept-invitation-form.tsx apps/web/src/features/invitations/utils apps/web/e2e vitest.config.ts eslint.config.js packages apps/api docs --  ':!docs/features/087-invitations-revoke/plan.md'` é vazio (o próprio plano é editado pela orquestração quando um critério precisa de correção de redação). A feature não importa de outra feature nem de `app/`: `rg -n "@/features/(?!invitations)|@/app/" -P apps/web/src/features/invitations --glob '!**/__tests__/**'` é vazio.
- [x] CA2.10 — `npx vitest run --project web` termina com exit code 0 e existem, pelos nomes de T2.4: os 3 casos de `revoke-invitation.test.tsx`; os 10 casos novos de `invitations-list.test.tsx` (entre eles `Cancelar calls no API and returns the focus to the button`, `revoking the second of two focuses the button above`, `revoking the first of two focuses the new first button`, `revoking the only invitation focuses the heading`, `a 404 closes the dialog, refetches and notifies`, `a 500 keeps the dialog open` e `two Enter presses send a single request`); e os 2 casos novos de `app/routes/app/admin/__tests__/invitations.test.tsx` (`an admin revokes one invitation and still sees the other`, `the Convites pendentes heading is still on the page`). Conferir os nomes com `rg -n "^\s*(it|test)\(" apps/web/src/features/invitations apps/web/src/app/routes/app/admin/__tests__/invitations.test.tsx`. Os casos preexistentes de `invitations-list.test.tsx`, `invitations.test.tsx`, `get-invitations.test.tsx`, `create-invitation.test.tsx`, `create-invitation-form.test.tsx` e `invitation-link.test.tsx` continuam listados e não foram renomeados. Nenhum `.only(` nem `.skip(`.
- [x] CA2.11 — Lendo `two Enter presses send a single request`: há um contador de chamadas no handler e uma asserção de que ele vale **1** depois de dois `Enter` seguidos no botão de confirmar. Lendo os três casos de foco: cada um termina com uma asserção `toHaveFocus()` sobre o elemento esperado (o botão do item de cima, o botão do novo primeiro item e o `<h2>` "Convites pendentes", respectivamente). Lendo `a 500 keeps the dialog open`: há asserção de que o diálogo **continua** na tela depois da falha.
- [x] CA2.12 — Esperas: `revoke-invitation.test.tsx`, `invitations-list.test.tsx` e `invitations.test.tsx` declaram `LAZY_TIMEOUT` e a passam em **toda** espera **dos casos novos** logo após abrir ou trocar de rota (`findBy…` e `waitFor`); os casos preexistentes não foram alterados. `rg -n "setTimeout\(|sleep\(" apps/web/src/features/invitations apps/web/src/app/routes/app/admin/__tests__/invitations.test.tsx` é vazio. Nenhum arquivo de teste desta fase contém senha literal (a senha, quando necessária, vem de uma constante de teste já existente). `rg -n "eslint-disable" apps/web/src apps/api/src | rg -v -- "--"` é vazio.
- [x] CA2.13 — Cobertura ≥ 80% de linhas para `apps/web/src/features/invitations/api/revoke-invitation.ts`, `apps/web/src/features/invitations/components/invitations-list.tsx` e `apps/web/src/app/routes/app/admin/invitations.tsx`. A medição vale por `coverage/coverage-summary.json`, gerado na raiz com `npx vitest run --coverage --coverage.reporter=json-summary --coverage.reporter=text` **sem `--project`**: o reporter `text` do v8 omite arquivos com 100%, então ausência da tabela de texto não reprova — reprova só quem aparecer no `coverage-summary.json` com `lines.pct < 80` ou não aparecer em lugar nenhum.
- [x] CA2.14 — `git log feature/088-invitations-list..HEAD --format=%s` mostra exatamente dois commits (um por fase), ambos em inglês e no formato Conventional Commits.

## Fase 3 — e2e da jornada de revogação e documentação: a fatia utilizável de ponta a ponta

Caminhos relativos à raiz do repositório.

- [x] T3.1 — Documentação: a receita do ícone, a entrega na arquitetura
  - Arquivos: `docs/design.md` (alterar); `docs/architecture.md` (alterar)
  - O que fazer, em pt_BR, sem reescrever o resto:
    - `docs/design.md`, na tabela "Padrões acrescentados pelas entregas", linha com Fatia = 087 e Padrão = **"Ícone de revogação"**: `<svg aria-hidden="true" focusable="false" viewBox="0 0 24 24" className="size-4">`, traço `currentColor`, sem preenchimento, desenho de círculo + barra diagonal; usado em ação que **invalida** algo sem apagar — nunca o "Ícone de lixeira", que significa remoção definitiva. E uma linha da receita "Ação destrutiva com confirmação num item de lista ou árvore" (066) é **generalizada**: o `aria-label` do gatilho é "{verbo} {o que identifica o item}" ("Apagar {nome}", "Revogar {e-mail}") e o botão de confirmar leva o mesmo verbo ("Apagar"/"Apagando…", "Revogar"/"Revogando…"). Nenhuma outra receita é reescrita.
    - `docs/architecture.md`, na §6, parágrafo novo **"Entrega `invitations-revoke` (fatia 087)"**: `POST /invitations/{invitationId}/revoke` entra no `InvitationsController` e **herda** os guards da classe; **um** 404 com `INVITATION_UNAVAILABLE_MESSAGE` para inexistente, de outra organização, aceito, vencido e já revogado, porque qualquer código distinto transformaria a rota num oráculo; `pendingInvitationWhere` passa a ser a **única** definição de "pendente" no servidor (`findPending`, `list`, `create`, `revoke`), encerrando a nota deixada pela 088; `revoke` é um `updateMany` atômico que marca `revokedAt` e **nunca** apaga a linha; a migration `0011` acrescenta a coluna e troca o índice parcial para `WHERE "acceptedAt" IS NULL AND "revokedAt" IS NULL` (as linhas existentes nascem com `revokedAt` nulo e não mudam de lado); e a nota sobre os dois caminhos `/invitations/{…}` do contrato, com o conserto pronto caso entre um validador.
  - Skills: interface-design
  - Complexidade: baixa

- [x] T3.2 — Testes da fase 3 (e2e: a administração corta um convite e o link morre)
  - Arquivos: `apps/web/e2e/tests/invitations-revoke.spec.ts` (criar)
  - O que fazer (D7): contra a API simulada; localizar por papel e nome acessível em pt_BR. Estado inicial por `page.addInitScript` gravando as chaves `mock-installation=signed-in` e `mock-invitations=sample`. No topo, `const ROUTE_TIMEOUT = { timeout: 10_000 }`; **toda** espera logo após mudança de rota (`toHaveURL`, primeira visibilidade da tela nova) o recebe; nenhum `waitForTimeout`. Depois de cada `Tab`/seta e antes do `Enter`, `await expect(locator).toBeFocused()`. Nenhum literal com cara de senha ou token no arquivo. `apps/web/e2e/a11y.ts` e os outros specs não são tocados; nenhuma regra do axe é desativada.
    - Caso `an admin revokes an invitation from the keyboard`: entra na página "Convites" pela barra lateral ("Administração" → "Convites"), espera a lista, chega ao botão "Revogar {e-mail}" **pelo teclado**, confirma no diálogo, vê a notificação "Convite revogado" e a lista **sem** aquele convite. `expectNoSeriousA11yViolations(page)` é chamado **duas vezes**: com a lista e a ação na tela, e com o diálogo aberto.
    - Caso `the revoked link falls into the same generic error as an unknown link`: em seguida, abre o link daquele convite e cai na tela de erro genérica da 086, conferindo que o texto é **o mesmo** de um link inventado (o teste visita os dois e compara o texto visível).
    - Os e2e da 064, 065, 066, 085, 086 e 088 **não** mudam. Se algum deles quebrar, o conserto é no código de produção, não no spec antigo.
    - Fecha a fase com um commit, por exemplo `test(e2e): cover revoking a pending invitation and document the slice`.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [x] CA3.1 — `pnpm test:e2e` na raiz termina com exit code 0 (o Playwright sobe o próprio servidor; não precisa de API nem de Postgres), e continuam terminando com exit code 0 e **sem aviso**, com `docker compose up -d` rodando e o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta).
- [x] CA3.2 — `apps/web/e2e/tests/invitations-revoke.spec.ts` contém os casos `an admin revokes an invitation from the keyboard` e `the revoked link falls into the same generic error as an unknown link`, e `pnpm --filter web exec playwright test --list` os lista junto com todos os casos já existentes de `invitations-create.spec.ts`, `invitations-accept.spec.ts`, `invitations-list.spec.ts`, `org-units-view.spec.ts`, `org-units-create-rename.spec.ts` e `org-units-delete.spec.ts` (nenhum removido nem renomeado). `rg -n "test\.skip|test\.only|waitForTimeout" apps/web/e2e` é vazio.
- [x] CA3.3 — Lendo o primeiro caso: a página é alcançada **navegando pela barra lateral** ("Administração" → "Convites"), não por URL direta; o botão "Revogar {e-mail}" é alcançado por teclado, com `toBeFocused()` antes do `Enter`; há asserção da notificação "Convite revogado" e de que o convite **não está mais** na lista; `expectNoSeriousA11yViolations` aparece **duas vezes** no caso — uma com a lista visível e outra com o diálogo aberto. Lendo o segundo caso: o link do convite revogado e um link inventado são abertos, e há asserção de que o **texto visível da tela de erro é o mesmo** nos dois (comparação entre os dois textos, não só contra um literal).
- [x] CA3.4 — Esperas e segredos do e2e: o spec declara `ROUTE_TIMEOUT = { timeout: 10_000 }` no topo e **toda** asserção logo após mudança de rota (`toHaveURL` e a primeira visibilidade da tela nova) o recebe; entre cada `Tab`/seta e o `Enter` seguinte há `toBeFocused()`; não há espera fixa; não há valor literal de senha nem de token no arquivo.
- [x] CA3.5 — `git status --porcelain -uall -- apps/web/e2e` (antes do commit) ou `git diff --stat feature/088-invitations-list... -- apps/web/e2e` (depois) lista **só** `apps/web/e2e/tests/invitations-revoke.spec.ts`; o spec não desativa regra do axe (`rg -n "disableRules" apps/web/e2e/tests/invitations-revoke.spec.ts` é vazio); os arquivos de `apps/web/e2e/**` passam no `pnpm lint` e no `pnpm typecheck`.
- [x] CA3.6 — `docs/design.md` tem, em "Padrões acrescentados pelas entregas", a linha da fatia 087 "Ícone de revogação" com `aria-hidden="true"`, `focusable="false"`, `viewBox="0 0 24 24"`, `className="size-4"`, traço `currentColor` e o aviso de que nunca se usa a lixeira para isso; e a receita 066 diz que o `aria-label` do gatilho é "{verbo} {o que identifica o item}" e que o botão de confirmar leva o mesmo verbo, citando "Apagar"/"Apagando…" e "Revogar"/"Revogando…". Nenhuma outra linha da tabela foi reescrita.
- [x] CA3.7 — `docs/architecture.md` tem, na §6, o parágrafo "Entrega `invitations-revoke` (fatia 087)" dizendo: `POST /invitations/{invitationId}/revoke` no `InvitationsController`, herdando os guards da classe; **um** 404 com `INVITATION_UNAVAILABLE_MESSAGE` para os cinco casos (inexistente, de outra organização, aceito, vencido, já revogado); `pendingInvitationWhere` como única definição de "pendente", usada por `findPending`, `list`, `create` e `revoke`, encerrando a nota da 088; `revoke` como `updateMany` atômico que marca `revokedAt` sem apagar a linha; a migration `0011` com o índice parcial em `WHERE "acceptedAt" IS NULL AND "revokedAt" IS NULL`; e a nota sobre os dois caminhos `/invitations/{…}` do contrato.
- [x] CA3.8 — A fatia está utilizável de ponta a ponta sem navegador aberto por pessoa: `npx vitest run --project api` (exit code 0) inclui `revoking a pending invitation answers 204 with no body`, `the revoked link answers exactly what an unknown link answers`, `revoking twice answers the same not found`, `the same e-mail can be invited again after a revoke`, `a non-admin answers 403` e `an anonymous request answers 401` em `apps/api/src/invitations/__tests__/invitations.integration.test.ts`; `npx vitest run --project web` (exit code 0) inclui `an admin revokes one invitation and still sees the other` em `apps/web/src/app/routes/app/admin/__tests__/invitations.test.tsx`; e `pnpm test:e2e` (exit code 0) inclui as duas jornadas de `invitations-revoke.spec.ts`.
- [x] CA3.9 — `git log feature/088-invitations-list..HEAD --format=%s` mostra exatamente três commits (um por fase), todos em inglês e no formato Conventional Commits; `git diff --stat feature/088-invitations-list... -- docs/roadmap.md` pode ser não vazio só pelo registro da orquestração.

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
