# PLAN 066 — org-units-delete

Branch: `feature/066-org-units-delete` (sai de `develop`, que é a base real desta entrega; **toda** comparação de diff e de "arquivo intocado" é `git diff develop...`, nunca contra outra branch).

Fonte: `docs/features/066-org-units-delete/spec.md` (as siglas D1…D8 são as decisões técnicas da SPEC e R1…R9 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; o banco de teste é `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom é da ferramenta e **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **A migration desta fatia é escrita à mão** e aplicada por `prisma migrate deploy` (é o que `apps/api/test/global-setup.ts` já roda antes dos testes). Ninguém — agente, teste ou critério — roda `prisma migrate diff`, `prisma migrate dev` nem `prisma migrate reset`. Os únicos subcomandos permitidos são `pnpm --filter api exec prisma validate`, `pnpm --filter api exec prisma migrate status` e `pnpm --filter api exec prisma migrate deploy` (contra `postgresql://folioteca@localhost:5433/folioteca_test`). A limpeza entre testes da API é `resetDatabase(prisma)`.
- Todo componente devolve `React.JSX.Element` (nunca o `JSX` global); `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case. Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Nenhum `eslint-disable` sem `-- <motivo>`. Nenhum literal com cara de senha, token ou hash em arquivo versionado; sessões de teste da API vêm de `createPersonWithSession` (`apps/api/test/create-person.ts`) ou dos ajudantes já usados no arquivo; id inválido é `randomUUID()` ou `'nao-e-uuid'`.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado. Não contam arquivos só de tipos, arquivos com 0 de 0 linhas instrumentáveis, nem os já excluídos pelo `vitest.config.ts` da raiz (`**/types/**`, `**/generated/**`, `**/src/testing/**`, `**/test/**`, `**/*.config.*`). Nenhuma exclusão nova.
- **Toda** espera logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes da web pela constante `LAZY_TIMEOUT` declarada no arquivo (`findBy…(…, {}, LAZY_TIMEOUT)`, `waitFor(…, LAZY_TIMEOUT)`); no e2e pela constante `ROUTE_TIMEOUT` declarada no topo do spec. Nunca `setTimeout`/sleep fixo nem `waitForTimeout`. Tecla de movimento (`Tab`, setas) seguida de tecla de edição (`Enter`, `Espaço`, `Escape`) só depois de esperar (`expect(...).toHaveFocus()` / `toBeFocused()`) a seleção assentar.
- "É administração?" é decidido pelo **servidor**. A rota nova nasce coberta pelo `@UseGuards(SessionGuard, AdminGuard)` que **já está na classe** do controller; nada em `apps/api/src/auth/**` muda.
- Testes de API são de integração contra o Postgres real (padrão `apps/api/test`); "teste passa" é medido pelo **exit code** de `npx vitest run --project api` / `--project web` (zero), nunca pela leitura da saída `--verbose`.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa. Cada fase termina com **um** commit Conventional Commits em inglês (`feat:`, `test:`, `docs:`), sem `--no-verify`.
- Nenhuma tarefa acrescenta escopo: sem apagar em cascata (`?recursive`), sem mover unidade, sem lixeira de unidades, sem `DomainConflictException`, sem `ConflictError` na feature, sem `setQueryData`/atualização otimista, sem extrair o `DialogOpenerKeeper` de `dialog.tsx`, sem componente de ícone compartilhado, sem tabela de `Space` no banco falso, sem chave `mock-*` nova.

Desvios em relação à tabela de arquivos da SPEC, sem mudar escopo: (1) `docs/design.md` sai da fase 3 e entra na fase 2 (T2.3) — a skill `interface-design` manda a receita nova entrar na mesma entrega da tela que a usa; (2) `docs/roadmap.md` (item 008 concluído; dívidas que virarem item) **não** é tarefa nem critério de fase: a orquestração o registra no encerramento.

## Fase 1 — API: `DELETE /org-units/{orgUnitId}` transacional, com as FKs em `RESTRICT` no banco

Caminhos relativos à raiz do repositório.

- [x] T1.1 — Contrato primeiro: a operação `delete` e a migration `0008`
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script); `apps/api/prisma/migrations/0008_org_unit_delete_restrict/migration.sql` (criar); `apps/api/prisma/schema.prisma` (alterar)
  - O que fazer (D1, D2):
    - `openapi.yaml`: dentro do caminho `/org-units/{orgUnitId}` que já existe (ao lado do `patch`), operação `delete` com `operationId: deleteOrgUnit`, tag `org-units`, mesma segurança de sessão, **sem** corpo. Respostas: `204` sem conteúdo; `401`, `403`, `404`, `409` reaproveitando o schema `Error` e os componentes de resposta de erro já existentes. Nenhum schema novo. `description` em pt_BR registrando a ordem observável: CSRF (guard global) → 401 → 403 → 404 (id inexistente ou malformado) → 409 (raiz → filhas → documentos, nessa ordem), e que o espaço `UNIT` da unidade é apagado na mesma operação. Regenerar com `pnpm --filter @folioteca/api-contract generate`; o `.d.ts` nunca é editado à mão. `apps/api/test/generated-types.test.ts` e `apps/web/src/types/api.ts` não mudam.
    - `migration.sql`, **só** com (mais um comentário SQL curto explicando que apagar uma unidade nunca cria raiz órfã nem espaço sem dono):
      - `ALTER TABLE "OrgUnit" DROP CONSTRAINT "OrgUnit_parentId_fkey";`
      - `ALTER TABLE "OrgUnit" ADD CONSTRAINT "OrgUnit_parentId_fkey" FOREIGN KEY ("parentId") REFERENCES "OrgUnit"("id") ON DELETE RESTRICT ON UPDATE CASCADE;`
      - `ALTER TABLE "Space" DROP CONSTRAINT "Space_orgUnitId_fkey";`
      - `ALTER TABLE "Space" ADD CONSTRAINT "Space_orgUnitId_fkey" FOREIGN KEY ("orgUnitId") REFERENCES "OrgUnit"("id") ON DELETE RESTRICT ON UPDATE CASCADE;`
      Os nomes das constraints são os da migration `0002`. `Document.spaceId` já é `RESTRICT` (`0003`): nada muda ali. Migrations `0001`–`0007` intocadas.
    - `schema.prisma`: **duas linhas** mudam — `parent OrgUnit? @relation("OrgUnitToParent", fields: [parentId], references: [id], onDelete: Restrict)` e `orgUnit OrgUnit? @relation(fields: [orgUnitId], references: [id], onDelete: Restrict)` (o nome da relação é o que já está no arquivo; só o `onDelete` muda de `SetNull` para `Restrict`). Nenhum campo, índice ou modelo a mais.
    - Conferir com `pnpm --filter api exec prisma validate` e, com o Postgres de teste no ar, `pnpm --filter api exec prisma migrate deploy` seguido de `pnpm --filter api exec prisma migrate status` (tudo aplicado, nenhuma pendente). **Nunca** `migrate diff/dev/reset`.
  - Skills: —
  - Complexidade: média

- [x] T1.2 — `OrgUnitsService.remove` transacional e `@Delete(':orgUnitId')`
  - Arquivos: `apps/api/src/org-units/org-units.service.ts` (alterar); `apps/api/src/org-units/org-units.controller.ts` (alterar)
  - O que fazer (D3, R2, R3, R4, R5, R7):
    - Constantes exportadas do serviço (para os testes lerem): `ROOT_MESSAGE = 'A unidade raiz não pode ser apagada.'`; `HAS_CHILDREN_MESSAGE = 'Apague ou mova as unidades filhas antes de apagar esta unidade.'`; `HAS_DOCUMENTS_MESSAGE = 'O espaço desta unidade ainda tem documentos, inclusive na lixeira. Trate-os antes de apagar a unidade.'`; `CHANGED_MESSAGE = 'A unidade mudou enquanto era apagada. Recarregue a estrutura e tente de novo.'`.
    - `remove(organizationId: string, orgUnitId: string): Promise<void>`: (1) `isUuid(orgUnitId)` falso → `orgUnitNotFound()` (404 "Unidade não encontrada.", sem ir ao banco). (2) `this.prisma.$transaction(async (tx) => { … })`: **uma** consulta `tx.orgUnit.findFirst({ where: { id: orgUnitId, organizationId }, select: { id: true, parentId: true, _count: { select: { children: true } }, space: { select: { id: true, _count: { select: { documents: true } } } } } })`; `null` → `orgUnitNotFound()`; `parentId === null` → `ConflictException(ROOT_MESSAGE)`; `_count.children > 0` → `ConflictException(HAS_CHILDREN_MESSAGE)`; `space && space._count.documents > 0` → `ConflictException(HAS_DOCUMENTS_MESSAGE)` (a contagem **não filtra `trashedAt`**: lixeira conta); depois `if (space) await tx.space.delete({ where: { id: space.id } })` e então `tx.orgUnit.delete({ where: { id: orgUnitId } })` — o espaço **antes** da unidade; unidade sem espaço é apagada mesmo assim. (3) `catch` em volta da transação: função local `isForeignKeyViolation(error: unknown): boolean` (`Prisma.PrismaClientKnownRequestError` com `code === 'P2003'`) → `ConflictException(CHANGED_MESSAGE)`; qualquer outro erro é **relançado**. Nunca tocar `prisma.document`/`tx.document` (fronteira da tabela `Document`, provada por `apps/api/src/access/__tests__/document-access-boundary.test.ts`, que continua verde sem alteração): a contagem vai pela relação `space._count.documents`.
    - Controller: `@Delete(':orgUnitId')` + `@HttpCode(204)`, id cru (id malformado vira 404 no serviço, como o comentário do topo da classe já diz), organização tirada de `@CurrentPerson()`, devolve `Promise<void>`. O `@UseGuards(SessionGuard, AdminGuard)` da **classe** não muda; nenhum guard por método. `org-units.module.ts`, `org-units.schema.ts`, `org-unit-not-found.ts` e `apps/api/src/common/**` não mudam.
  - Skills: security
  - Complexidade: alta

- [x] T1.3 — Testes da fase 1
  - Arquivos: `apps/api/src/org-units/__tests__/org-units.service.test.ts` (alterar); `apps/api/src/org-units/__tests__/org-units.integration.test.ts` (alterar); `apps/api/src/org-units/__tests__/org-units.contract.test.ts` (alterar)
  - O que fazer (D8): integração e contrato contra o Postgres real, sem mock do Prisma, `resetDatabase(prisma)` em `beforeEach`, instalação e sessões pelos ajudantes já usados nesses arquivos; não-admin por `createPersonWithSession`. `apps/api/test/**` **não muda**. Os casos já existentes ficam como estão. Nenhuma espera fixa. O ajudante novo `deleteOrgUnit(id: string, cookie?: string)` manda `X-Requested-With: XMLHttpRequest` como os demais. Documento no espaço da unidade é criado com `prisma.document.create` direto no `Space` de tipo `UNIT` da unidade (`__tests__` fica fora da varredura do teste estrutural de fronteira).
    - `org-units.service.test.ts` (Prisma falso; casos novos): `remove throws not found for a malformed id without querying`; `remove looks the unit up by id and organization`; `remove throws not found when the unit is not in the organization`; `remove answers conflict with the root message for the root`; `remove answers conflict with the children message when the unit has children`; `remove answers conflict with the documents message when the space has documents`; `remove checks root before children and children before documents`; `remove deletes the space before the unit`; `remove deletes only the unit when it has no space`; `remove turns a foreign key violation into ConflictException with the changed message`; `remove rethrows any other error`.
    - `org-units.integration.test.ts` (casos novos, `DELETE /api/org-units/:id`): `deletes a leaf, answers 204 without a body and removes it from the list`; `deleting a leaf also deletes its UNIT space` (`prisma.orgUnit.findUnique` e `prisma.space.findFirst({ where: { orgUnitId } })` devolvem `null`); `answers 409 with the message for the root`; `answers 409 with the message for a unit with children and keeps it` (a mãe continua no banco); `answers 409 with the message when the space has a document`; `answers 409 when the only document of the space is in the trash` (`trashedAt` preenchido); `a person who is not admin gets 403 on DELETE` ("Apenas a administração pode fazer isso."); `answers 401 without a session cookie on DELETE`; `answers 404 with the message for an unknown id` (`randomUUID()`); `answers 404 with the message for a malformed id` (`'nao-e-uuid'`, nunca 400); `the database refuses to delete a parent with children` (`prisma.orgUnit.delete` direto rejeita com `code === 'P2003'`); `the database refuses to delete a unit that still has its space` (`prisma.orgUnit.delete` direto rejeita com `P2003`); `the database refuses to delete a space that still has a document` (`prisma.space.delete` direto rejeita com `P2003`).
    - `org-units.contract.test.ts` (ajudantes de `apps/api/test/contract.ts`; casos novos): `DELETE org-units answers the documented 204`; `DELETE org-units answers the documented 403`; `DELETE org-units answers the documented 404`; `DELETE org-units answers the documented 409`.
    - "Unidade de outra organização → 404" **não tem integração** (`Organization.singleton` impede a segunda organização); o filtro é provado no unitário `remove looks the unit up by id and organization`.
    - Fecha a fase com um commit, por exemplo `feat(api): delete org unit with restrict foreign keys`.
  - Skills: unit-testing, integration-testing, security
  - Complexidade: alta

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (sem aviso de peer no install; sem aviso de chunk grande no build; o aviso de desempenho do Vitest sobre jsdom não conta). A saída de `pnpm test` não tem `console.error` nem `console.warn` vazando, nem aviso do Nest ou do Prisma. `git diff develop... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA1.2 — `packages/api-contract/openapi.yaml` tem, em `/org-units/{orgUnitId}`, a operação `delete` com `operationId: deleteOrgUnit`, sem `requestBody`, e respostas `204` (sem `content`), `401`, `403`, `404` e `409` (as quatro com o schema `Error` já existente). `rg -n "deleteOrgUnit" packages/api-contract/src/generated/openapi.d.ts` acha a operação; `apps/api/test/generated-types.test.ts` passa sem ter sido alterado; `git diff develop... -- apps/web/src/types/api.ts` é vazio.
- [x] CA1.3 — Existe `apps/api/prisma/migrations/0008_org_unit_delete_restrict/migration.sql` e suas únicas instruções (fora comentários `--`) são quatro `ALTER TABLE`: `DROP CONSTRAINT "OrgUnit_parentId_fkey"`, `ADD CONSTRAINT "OrgUnit_parentId_fkey" … ON DELETE RESTRICT ON UPDATE CASCADE`, `DROP CONSTRAINT "Space_orgUnitId_fkey"` e `ADD CONSTRAINT "Space_orgUnitId_fkey" … ON DELETE RESTRICT ON UPDATE CASCADE`. `git diff develop... -- apps/api/prisma/schema.prisma` muda **só duas linhas**, trocando `onDelete: SetNull` por `onDelete: Restrict` nas relações `OrgUnit.parent` e `Space.orgUnit`. `git diff --stat develop... -- apps/api/prisma/migrations` lista **só** o arquivo novo. `pnpm --filter api exec prisma validate` passa e, com `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test`, `pnpm --filter api exec prisma migrate deploy` seguido de `pnpm --filter api exec prisma migrate status` reporta `0008_org_unit_delete_restrict` aplicada e nenhuma pendente. `rg -n "migrate (diff|dev|reset)" apps package.json --glob "!apps/api/package.json"` é vazio (o script `prisma:migrate` de `apps/api/package.json` já existia em `develop` e fica fora do escopo; `docs/` fica fora porque a documentação cita os comandos para proibi-los).
- [x] CA1.4 — `apps/api/src/org-units/org-units.service.ts` exporta `ROOT_MESSAGE`, `HAS_CHILDREN_MESSAGE`, `HAS_DOCUMENTS_MESSAGE` e `CHANGED_MESSAGE` com os textos literais "A unidade raiz não pode ser apagada.", "Apague ou mova as unidades filhas antes de apagar esta unidade.", "O espaço desta unidade ainda tem documentos, inclusive na lixeira. Trate-os antes de apagar a unidade." e "A unidade mudou enquanto era apagada. Recarregue a estrutura e tente de novo.", e tem `remove(organizationId: string, orgUnitId: string): Promise<void>`. Lendo o código: `isUuid` antes de qualquer consulta; **uma** `findFirst` dentro de `$transaction` filtrando por `id` **e** `organizationId`, com `select` de `parentId`, `_count.children` e `space._count.documents`; ordem raiz → filhas → documentos; `tx.space.delete` **antes** de `tx.orgUnit.delete`; nenhum `where` com `trashedAt`; `P2003` vira `ConflictException(CHANGED_MESSAGE)` e qualquer outro erro é relançado. `rg -n "\.document\." apps/api/src/org-units --glob '!**/__tests__/**'` é vazio; `rg -n "NotFoundException" apps/api/src/org-units --glob '!**/__tests__/**'` só acha `org-unit-not-found.ts`.
- [x] CA1.5 — `apps/api/src/org-units/org-units.controller.ts` tem um método com `@Delete(':orgUnitId')` e `@HttpCode(204)` que devolve `Promise<void>` e usa a organização **da pessoa da sessão**. Os guards continuam só na classe: `rg -n "UseGuards" apps/api/src/org-units/org-units.controller.ts` acha uma única linha além do import, com `SessionGuard, AdminGuard` nessa ordem. `rg -n "isAdmin" apps/api/src/org-units --glob '!**/__tests__/**'` é vazio.
- [x] CA1.6 — Intocados: `git diff --stat develop... -- apps/api/src/auth apps/api/src/access apps/api/src/documents apps/api/src/common apps/api/src/installation apps/api/src/org-units/org-units.schema.ts apps/api/src/org-units/org-unit-not-found.ts apps/api/src/org-units/org-units.module.ts apps/api/src/app.module.ts apps/api/test apps/web vitest.config.ts` é vazio.
- [x] CA1.7 — `npx vitest run --project api` termina com exit code 0 e existem, pelos nomes de T1.3: os 11 casos novos de `org-units.service.test.ts` (entre eles `remove checks root before children and children before documents`, `remove deletes the space before the unit`, `remove deletes only the unit when it has no space` e `remove turns a foreign key violation into ConflictException with the changed message`); os 13 casos novos de `org-units.integration.test.ts` (entre eles `deleting a leaf also deletes its UNIT space`, `answers 409 when the only document of the space is in the trash`, `a person who is not admin gets 403 on DELETE`, `answers 404 with the message for a malformed id`, `the database refuses to delete a parent with children`, `the database refuses to delete a unit that still has its space` e `the database refuses to delete a space that still has a document`); e os 4 casos novos de `org-units.contract.test.ts`. Conferir a presença dos nomes com `rg -n "^\s*(it|test)\(" apps/api/src/org-units/__tests__`. A integração usa o Postgres real: `rg -n "vi\.mock\(.*prisma" apps/api/src/org-units/__tests__/org-units.integration.test.ts apps/api/src/org-units/__tests__/org-units.contract.test.ts` é vazio. Os casos que já existiam em `develop` continuam presentes (o diff desses três arquivos só acrescenta).
- [x] CA1.8 — Nenhuma espera fixa nem credencial literal: `rg -n "setTimeout\(|sleep\(" apps/api/src/org-units/__tests__` é vazio; `rg -n "folioteca_session=[A-Za-z0-9]" apps/api/src/org-units/__tests__` é vazio.
- [x] CA1.9 — O relatório de cobertura de `pnpm test` lista com ≥ 80% de linhas cada: `apps/api/src/org-units/org-units.service.ts` e `apps/api/src/org-units/org-units.controller.ts`; arquivo ausente do relatório conta como reprovado.
- [x] CA1.10 — `git log develop..HEAD --format=%s` mostra exatamente um commit desta fase, em inglês, no formato Conventional Commits (prefixo `feat`, `fix`, `test`, `docs`, `chore` ou `refactor` seguido de `:` ou `(escopo):`).

## Fase 2 — Web: ação "Apagar" na árvore da página "Estrutura", com confirmação, foco na mãe e API simulada

Caminhos relativos a `apps/web/`, salvo os que começam por `docs/` (raiz).

- [x] T2.1 — `ConfirmationDialog` sem gatilho e com `onCloseAutoFocus`
  - Arquivos: `src/components/ui/confirmation-dialog/confirmation-dialog.tsx` (alterar)
  - O que fazer (D5): duas props novas em `ConfirmationDialogProps`, opcionais e sem efeito nos usos existentes (`src/features/documents/components/trash-document-button.tsx` e `trashed-document-actions.tsx` ficam intocados): `trigger?: React.ReactNode` — quando ausente, o `AlertDialogPrimitive.Trigger` **não** é desenhado e o diálogo abre só por `open`/`onOpenChange`; `onCloseAutoFocus?: React.ComponentProps<typeof AlertDialogPrimitive.Content>['onCloseAutoFocus']` — repassado ao `Content`. Comentário (en_US) na prop dizendo que sem gatilho o Radix não tem para onde devolver o foco, logo quem abre sem `trigger` é responsável pelo foco ao fechar. Classes, estrutura e o `Button variant="secondary"` de cancelar não mudam. Sem `forwardRef`, sem `style={{`, `React.JSX.Element`.
  - Skills: ui-components, interface-design
  - Complexidade: baixa

- [x] T2.2 — API simulada: `DELETE /org-units/:orgUnitId`, `removeOrgUnit` e o documento da "Sala Infantil"
  - Arquivos: `src/testing/mocks/db.ts` (alterar); `src/testing/mocks/handlers/org-units.ts` (alterar); `src/testing/mocks/utils.ts` (alterar)
  - O que fazer (D7, R2, R3, R5, R7):
    - `db.ts`: `removeOrgUnit(id: string): void` ao lado de `addOrgUnit`/`renameOrgUnit`, retirando do array **no lugar** com `splice` (regra do comentário de `touchDocumentUpdatedAt`: nunca substituir `state` entre o `await` e a escrita do handler). Comentário curto registrando a convenção: documento de unidade usa `spaceId = \`space-${orgUnitId}\`` (mesmo formato de `space-${person.id}` do espaço pessoal); o `Space` `UNIT` continua sem existir como linha. `seedSampleOrgUnits` passa a acrescentar **um** `MockDocument` com título "Regulamento da Sala Infantil", `spaceId: 'space-org-unit-sala-infantil'`, `authorId`/`ownerId` da pessoa instalada, `trashedAt: null`, `accessLevel: 'owner'` — só quando a semente `sample` roda.
    - `handlers/org-units.ts`: `http.delete(\`${env.API_URL}/org-units/:orgUnitId\`)` com o mesmo preâmbulo do `PATCH` (`networkDelay`, `devOverride('org-units')`, 401 "Sessão não encontrada." sem cookie/instalação, 403 "Apenas a administração pode fazer isso." para não-admin) e as regras do servidor na mesma ordem: id fora do banco falso → 404 "Unidade não encontrada."; `parentId === null` → 409 "A unidade raiz não pode ser apagada."; alguma unidade com `parentId === id` → 409 "Apague ou mova as unidades filhas antes de apagar esta unidade."; algum documento com `spaceId === \`space-${id}\`` (com ou sem `trashedAt`) → 409 "O espaço desta unidade ainda tem documentos, inclusive na lixeira. Trate-os antes de apagar a unidade."; senão `removeOrgUnit(id)` e `new HttpResponse(null, { status: 204 })`. Sem chave de desenvolvimento nova (`mock-org-units=sample` dá folha, mãe com filhas e unidade com documento; `mock-role=member` dá o 403; `mock-error=org-units` dá o 500).
    - `utils.ts`: **só** o comentário das chaves — `mock-org-units=sample` agora inclui um documento no espaço da "Sala Infantil", que aparece nas listas de documentos da pessoa.
    - `handlers/auth.ts`, `installation.ts`, `documents.ts` e `index.ts` não mudam.
  - Skills: api-mocking
  - Complexidade: média

- [x] T2.3 — Mutação `deleteOrgUnit`, ação "Apagar" na árvore com diálogo único e foco na mãe, e a receita no `docs/design.md`
  - Arquivos: `src/features/org-units/api/delete-org-unit.ts` (criar); `src/features/org-units/components/org-units-tree.tsx` (alterar); `docs/design.md` (alterar)
  - O que fazer (D4, D6, R1, R2, R6, R8, R9), seção "Interface" da SPEC copiada aqui. Carregando, erro, o texto de "só a raiz" e a página `src/app/routes/app/admin/structure.tsx` ficam como estão.
    - `delete-org-unit.ts`, espelho de `src/features/documents/api/delete-document.ts`: `deleteOrgUnit({ orgUnitId }: { orgUnitId: string }): Promise<void>` → `api.delete(\`/org-units/${orgUnitId}\`)` **sem** `silentError` (o interceptor de `src/lib/api-client.ts` já mostra a notificação de erro "Algo deu errado" com o `message` do corpo — é o R8); `useDeleteOrgUnit({ mutationConfig }: { mutationConfig?: MutationConfig<typeof deleteOrgUnit> } = {})` com `onSuccess` interno que **espera** `queryClient.invalidateQueries({ queryKey: getOrgUnitsQueryOptions().queryKey })` e **só depois** chama o `onSuccess` de quem usou o hook (padrão de `update-org-unit.ts`). Sem `setQueryData`, sem `try/catch`, sem `isConflictError`.
    - `org-units-tree.tsx` (`LoadedOrgUnitsTree`): `unitsById = useMemo(() => new Map(units.map((unit) => [unit.id, unit])), [units])`. `renderActions` desenha um terceiro `Button variant="ghost" size="icon"` `type="button"`, **depois** de "Renomear" (ordem: criar filha · renomear · apagar), com ícone de lixeira SVG inline `aria-hidden` (mesmo desenho do `TrashIcon` de `src/features/documents/components/trash-document-button.tsx`, copiado — não importar de outra feature), `aria-label` e `title` "Apagar {nome da unidade}", o `tabIndex` recebido, e **só quando** `unitsById.get(node.id)?.parentId !== null` (a raiz nunca tem o botão).
    - Estado: `deletingId: string | null` (`useState`); `deletingUnit` derivado de `units` a cada render; `open = deletingUnit !== null` (se a unidade some da lista, o diálogo fecha por derivação; sem `useEffect` de sincronização). `deleteOpenerRef: useRef<HTMLElement | null>(null)`: no `onClick` de "Apagar", `deleteOpenerRef.current = event.currentTarget; setDeletingId(node.id)`. `focusAfterDeleteRef: useRef<string | null>(null)`.
    - `useDeleteOrgUnit({ mutationConfig: { onSuccess } })`; `handleConfirmDelete`: retorno antecipado se `isPending` (clique/`Enter` duplo faz um pedido só); guarda `parentId` da unidade **antes** de `mutate({ orgUnitId })`. No `onSuccess` (a lista já veio sem a unidade): `focusAfterDeleteRef.current = parentId`, `addNotification({ type: 'success', title: 'Unidade apagada' })`, `setDeletingId(null)`. Na falha o diálogo **fica aberto** (a notificação do interceptor explica), como `TrashDocumentButton`.
    - **Um** `ConfirmationDialog` para a árvore inteira, **sem** `trigger`: título "Apagar unidade?"; descrição "“{nome}” e o espaço de documentos dela serão apagados. Esta ação não pode ser desfeita."; `cancelLabel` padrão "Cancelar"; `confirmButton` = `Button variant="destructive" type="button" isLoading={isPending}` com texto "Apagar" e, enviando, "Apagando…". Foco inicial no "Cancelar" (padrão do Radix). `onOpenChange(false)` → `setDeletingId(null)`.
    - `onCloseAutoFocus`: se `focusAfterDeleteRef.current` → `event.preventDefault()`, `treeRef.current?.focusNode(id)`, limpa a ref (foco no `treeitem` da mãe); senão, se `deleteOpenerRef.current?.isConnected` → `preventDefault()` e `.focus()` nele (cancelar, `Esc` ou 409 seguido de cancelar devolvem o foco ao botão "Apagar …" que abriu).
    - A 360px: quem cede espaço continua sendo o rótulo (`min-w-0 truncate`; ações `shrink-0`, já garantido pelo `Tree`). Se o arquivo passar de ~120 linhas a mais, o corte permitido é mover a ação e o diálogo para `src/features/org-units/components/delete-org-unit-dialog.tsx`, sem mudar o desenho.
    - `docs/design.md`, tabela "Padrões acrescentados pelas entregas", Fatia = 066, linha "Ação destrutiva com confirmação num item de lista ou árvore": botão `ghost` + `size="icon"` com ícone de lixeira e `aria-label` "Apagar {nome}", **último** entre as ações do item; abre "Diálogo de confirmação" (sem gatilho próprio quando o item pode sumir da lista) cujo botão de confirmar é o "Botão destrutivo" com o verbo da ação ("Apagar" / "Apagando…"); a falha mantém o diálogo aberto e o aviso vem pela "Notificação" de erro. A receita "Ações do nó da árvore" (065) passa a citar três botões.
  - Skills: api-requests, error-handling, interface-design, client-state, component-robustness, authorization
  - Complexidade: alta

- [x] T2.4 — Testes da fase 2
  - Arquivos: `src/features/org-units/api/__tests__/delete-org-unit.test.tsx` (criar); `src/components/ui/confirmation-dialog/__tests__/confirmation-dialog.test.tsx` (alterar); `src/features/org-units/components/__tests__/org-units-tree.test.tsx` (alterar); `src/app/routes/app/admin/__tests__/structure.test.tsx` (alterar)
  - O que fazer (D8): banco falso por `seedInstalled({ signedIn: true })` (admin) ou `seedInstalled({ signedIn: true, isAdmin: false })` e `seedSampleOrgUnits()`; interação por `userEvent`; localizar por papel e nome acessível em pt_BR. Os casos antigos de `confirmation-dialog.test.tsx` e `org-units-tree.test.tsx` ficam **intocados**. `LAZY_TIMEOUT` declarada no arquivo e passada em **toda** espera logo após rota ou chunk `lazy` (`structure.test.tsx` já a tem; `org-units-tree.test.tsx` a declara ou a mantém e a usa nas esperas de rede da árvore). Depois de cada `Tab`/seta e antes do `Enter`/`Escape`, esperar `expect(...).toHaveFocus()`. Nenhuma espera fixa; nenhum `console.error`/`console.warn`/aviso de `act(...)` vazando.
    - `delete-org-unit.test.tsx`: `sends DELETE to the unit path`; `invalidates the org-units query`; `calls the caller onSuccess only after the list was refetched` (a lista já sem a unidade); `rejects on 409 and the notifications store receives the server message` (`type: 'error'`, `message` igual ao do handler); `rejects with 403 for a person who is not admin`.
    - `confirmation-dialog.test.tsx` (novos): `without trigger it renders no trigger button and opens by the open prop`; `calls onCloseAutoFocus when closing`; `preventDefault in onCloseAutoFocus keeps the focus where the caller put it`; `existing usages with trigger keep working` (gatilho desenhado e abre ao clicar).
    - `org-units-tree.test.tsx` (novos): `the root has only the create and rename actions` (nenhum botão "Apagar …" na linha da raiz); `a child unit has the delete action after rename`; `deleting from the keyboard removes the unit, notifies and focuses the parent` (item → `Tab` ×3 → `Enter` abre "Apagar unidade?" com o nome na descrição → `Tab` até "Apagar" → `Enter` → unidade fora da árvore, notificação "Unidade apagada", foco no `treeitem` da mãe); `Escape in the delete dialog returns the focus to the opener`; `confirming on a unit with children keeps the dialog open and notifies the server message`; `confirming on a unit whose space has a document keeps the dialog open and notifies the other message` ("Sala Infantil"); `pressing Enter twice on Apagar sends a single request` (handler contador com `delay`).
    - `structure.test.tsx` (novo, `renderApp`): `deleteOrgUnit called directly by a person who is not admin gets 403 from the default handler`.
    - Fecha a fase com um commit, por exemplo `feat(web): delete org unit from the structure tree with confirmation`.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: alta

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta). A saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)` vazando. `git diff develop... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA2.2 — `apps/web/src/components/ui/confirmation-dialog/confirmation-dialog.tsx`: `ConfirmationDialogProps` tem `trigger?: React.ReactNode` (opcional) e `onCloseAutoFocus?` tipado a partir das props do `Content` do `@radix-ui/react-alert-dialog`; lendo o código, o `AlertDialogPrimitive.Trigger` só é desenhado quando `trigger` existe e `onCloseAutoFocus` é repassado ao `Content`; há um comentário na prop sobre a responsabilidade do foco de quem abre sem gatilho. `git diff develop... -- apps/web/src/components/ui/confirmation-dialog/confirmation-dialog.tsx` não altera nenhuma classe CSS. `rg -n "forwardRef|style=\{\{|: JSX\." apps/web/src/components/ui/confirmation-dialog/confirmation-dialog.tsx` é vazio. `git diff --stat develop... -- apps/web/src/features/documents` é vazio.
- [x] CA2.3 — `apps/web/src/features/org-units/api/delete-org-unit.ts` exporta `deleteOrgUnit({ orgUnitId }: { orgUnitId: string }): Promise<void>` (chama `api.delete` com `/org-units/${orgUnitId}` e **sem** `silentError`) e `useDeleteOrgUnit({ mutationConfig })`. Lendo o código: o `onSuccess` interno faz `await queryClient.invalidateQueries` com a chave de `getOrgUnitsQueryOptions()` antes de chamar o `onSuccess` de quem usou o hook. `rg -n "silentError\s*:|setQueryData|isConflictError|response\?*\.status|isAxiosError" apps/web/src/features/org-units/api/delete-org-unit.ts apps/web/src/features/org-units/components/org-units-tree.tsx` é vazio (o nome `silentError` pode aparecer em comentário explicando a ausência; o que se proíbe é a opção em código).
- [x] CA2.4 — `apps/web/src/features/org-units/components/org-units-tree.tsx`: o `renderActions` desenha, **depois** do botão "Renomear {nome}", um `Button variant="ghost" size="icon"` `type="button"` com SVG `aria-hidden`, `aria-label` e `title` "Apagar {nome}", que recebe o `tabIndex` do `renderActions` e está condicionado a `typeof unitsById.get(node.id)?.parentId === 'string'`; há **um** `ConfirmationDialog` para a árvore, sem a prop `trigger`, com título "Apagar unidade?", descrição contendo "e o espaço de documentos dela serão apagados. Esta ação não pode ser desfeita." e o nome entre aspas curvas “ ”, e `confirmButton` = `Button variant="destructive"` com `isLoading` e os textos "Apagar" / "Apagando…"; o estado guarda uma cópia da unidade a apagar (`deleting` com `id`, `name` e `parentId`) mais `isDeleteOpen`, para a descrição manter o nome enquanto o diálogo fecha depois que a unidade sumiu da lista; a confirmação tem retorno antecipado quando a mutação está pendente; o `onCloseAutoFocus` chama `focusNode` do `Tree` com o `parentId` guardado depois do sucesso, e nos demais casos devolve o foco ao elemento guardado em uma `ref` quando `isConnected`; notificação `{ type: 'success', title: 'Unidade apagada' }`. Não há `useEffect` novo (`git diff develop... -- apps/web/src/features/org-units/components/org-units-tree.tsx` não acrescenta `useEffect(`), não há `<button` cru (`rg -n "<button" apps/web/src/features/org-units --glob '!**/__tests__/**'` é vazio) e o arquivo não importa de `@/features/documents` nem de `@/app` (`rg -n "@/features/(?!org-units)|@/app" -P apps/web/src/features/org-units --glob '!**/__tests__/**'` é vazio).
- [x] CA2.5 — API simulada: `apps/web/src/testing/mocks/db.ts` exporta `removeOrgUnit(id: string)` que usa `splice` sobre `state.orgUnits` (não reatribui `state`), tem um comentário com a convenção `space-${orgUnitId}` e `seedSampleOrgUnits` acrescenta um documento com `spaceId: 'space-org-unit-sala-infantil'` e título "Regulamento da Sala Infantil"; `handlers/org-units.ts` tem `http.delete` em `/org-units/:orgUnitId` respondendo, nessa ordem no código, 401 "Sessão não encontrada.", 403 "Apenas a administração pode fazer isso.", 404 "Unidade não encontrada.", 409 "A unidade raiz não pode ser apagada.", 409 "Apague ou mova as unidades filhas antes de apagar esta unidade.", 409 "O espaço desta unidade ainda tem documentos, inclusive na lixeira. Trate-os antes de apagar a unidade." e 204 sem corpo; `utils.ts` só muda em comentário (`git diff develop... -- apps/web/src/testing/mocks/utils.ts` só tem linhas iniciadas por `//` além do contexto). `rg -n "mock-[a-z-]+" apps/web/src/testing/mocks/utils.ts` não lista chave que não existia em `develop`.
- [x] CA2.6 — `docs/design.md` tem, em "Padrões acrescentados pelas entregas", a linha da fatia 066 "Ação destrutiva com confirmação num item de lista ou árvore" (botão `ghost` + `size="icon"`, último entre as ações, "Diálogo de confirmação" sem gatilho quando o item pode sumir, "Botão destrutivo" com "Apagar" / "Apagando…", falha mantém o diálogo aberto e avisa por "Notificação"), e a receita "Ações do nó da árvore" cita três botões; `org-units-tree.tsx` usa exatamente essas variantes do `Button` (`ghost`/`icon` na ação, `destructive` na confirmação).
- [x] CA2.7 — Intocados nesta fase (base é o commit da fase 1, `a860b16`, porque `packages` mudou legitimamente lá): `git diff --stat a860b16 -- apps/web/src/lib apps/web/src/types apps/web/src/components/ui/dialog apps/web/src/components/ui/tree apps/web/src/components/ui/button apps/web/src/components/ui/form apps/web/src/components/ui/notifications apps/web/src/components/layouts apps/web/src/app/router.tsx apps/web/src/app/routes/app/admin/structure.tsx apps/web/src/features/documents apps/web/src/features/org-units/api/get-org-units.ts apps/web/src/features/org-units/api/create-org-unit.ts apps/web/src/features/org-units/api/update-org-unit.ts apps/web/src/features/org-units/components/create-org-unit-form.tsx apps/web/src/features/org-units/components/rename-org-unit-form.tsx apps/web/src/features/org-units/utils apps/web/src/testing/mocks/handlers/auth.ts apps/web/src/testing/mocks/handlers/installation.ts apps/web/src/testing/mocks/handlers/documents.ts apps/web/src/testing/mocks/handlers/index.ts apps/web/e2e eslint.config.js vitest.config.ts packages` é vazio.
- [x] CA2.8 — `npx vitest run --project web` termina com exit code 0 e existem, pelos nomes de T2.4: os 5 casos de `delete-org-unit.test.tsx` (entre eles `calls the caller onSuccess only after the list was refetched` e `rejects on 409 and the notifications store receives the server message`); os 4 novos de `confirmation-dialog.test.tsx` (entre eles `without trigger it renders no trigger button and opens by the open prop` e `preventDefault in onCloseAutoFocus keeps the focus where the caller put it`); os 7 novos de `org-units-tree.test.tsx` (entre eles `the root has only the create and rename actions`, `deleting from the keyboard removes the unit, notifies and focuses the parent`, `Escape in the delete dialog returns the focus to the opener`, `confirming on a unit whose space has a document keeps the dialog open and notifies the other message` e `pressing Enter twice on Apagar sends a single request`); e o caso novo de `structure.test.tsx`. Conferir os nomes com `rg -n "^\s*(it|test)\(" apps/web/src/features/org-units/__tests__ apps/web/src/features/org-units/api/__tests__ apps/web/src/features/org-units/components/__tests__ apps/web/src/components/ui/confirmation-dialog/__tests__ apps/web/src/app/routes/app/admin/__tests__`. Os casos que já existiam em `develop` nesses arquivos estão presentes e sem alteração (o diff só acrescenta). Nenhum `.only(` nem `.skip(`.
- [x] CA2.9 — Esperas: `org-units-tree.test.tsx` e `structure.test.tsx` declaram `LAZY_TIMEOUT` e a passam em toda espera **dos casos novos desta fase** logo após abrir rota, trocar de rota ou aguardar a árvore recarregar (`findBy…` e `waitFor`); casos preexistentes ficam como estão (CA2.8); em cada jornada de teclado, entre `Tab`/seta e o `Enter`/`Escape` seguinte há uma asserção `toHaveFocus()`; `rg -n "setTimeout\(|sleep\(" apps/web/src/app/routes/app/admin/__tests__ apps/web/src/features/org-units apps/web/src/components/ui/confirmation-dialog` é vazio.
- [x] CA2.10 — O relatório de cobertura de `pnpm test` lista com ≥ 80% de linhas cada: `apps/web/src/components/ui/confirmation-dialog/confirmation-dialog.tsx`, `apps/web/src/features/org-units/api/delete-org-unit.ts` e `apps/web/src/features/org-units/components/org-units-tree.tsx` (e `delete-org-unit-dialog.tsx`, se o corte de T2.3 tiver sido usado); o reporter `text` do v8 omite arquivos 100% cobertos, então arquivo ausente da tabela é aprovado desde que `coverage/coverage-summary.json` (gerado com `--coverage.reporter=json-summary`) ou `coverage/index.html` mostre ≥ 80% de linhas para ele.
- [x] CA2.11 — `git log develop..HEAD --format=%s` mostra exatamente dois commits (um por fase), ambos em inglês e no formato Conventional Commits.

## Fase 3 — e2e da jornada de apagar e documentação: a fatia utilizável de ponta a ponta

Caminhos relativos à raiz do repositório.

- [ ] T3.1 — Documentação: a entrega `org-units-delete` na arquitetura
  - Arquivos: `docs/architecture.md` (alterar)
  - O que fazer, em pt_BR, sem reescrever o resto:
    - §4 (árvore de unidades), parágrafo novo "Entrega `org-units-delete` (fatia 066)": as FKs `OrgUnit.parentId` e `Space.orgUnitId` passaram a `ON DELETE RESTRICT` na migration `0008`, escrita à mão, e com `Document.spaceId` (já `RESTRICT`) a ordem obrigatória de apagar é documento → espaço → unidade, recusada pelo banco em qualquer atalho; a regra do serviço (raiz, filhas, documentos inclusive na lixeira) roda numa transação com **uma** consulta; a contagem de documentos vai pela relação do espaço (`space._count.documents`) por causa da fronteira da tabela `Document` (§3); `P2003` vira 409 "A unidade mudou enquanto era apagada. Recarregue a estrutura e tente de novo." (corrida entre contar e apagar); na web, um diálogo de confirmação no nível da árvore (sem gatilho) e o foco vai à mãe pelo `onCloseAutoFocus`.
    - §6 (autenticação): o `DELETE /org-units/{orgUnitId}` herda `SessionGuard` + `AdminGuard` da classe do controller; ordem observável CSRF → 401 → 403 → 404 → 409.
    - O status da fatia no roadmap e o registro das dívidas **não** são desta tarefa (a orquestração registra no encerramento).
  - Skills: —
  - Complexidade: baixa

- [ ] T3.2 — Testes da fase 3 (e2e: apagar pelo teclado, recusa por filhas e raiz sem a ação)
  - Arquivos: `apps/web/e2e/tests/org-units-delete.spec.ts` (criar)
  - O que fazer (D8): contra a API simulada; localizar por papel e nome acessível em pt_BR. Estado inicial por `page.addInitScript` gravando `mock-installation` = `signed-in` e `mock-org-units` = `sample` no `localStorage`. No topo: `const ROUTE_TIMEOUT = { timeout: 10_000 }`; **toda** espera logo após mudança de rota (`toHaveURL`, primeira visibilidade da tela nova) o recebe; nenhum `waitForTimeout`. `goToStructure` copiado do `org-units-create-rename.spec.ts` (não importado entre specs, como os specs existentes fazem). Depois de cada `Tab`/seta e antes do `Enter`/`Escape`, `await expect(locator).toBeFocused()`. `apps/web/e2e/a11y.ts` e os outros specs não são tocados; nenhuma regra do axe é desativada. Nenhum literal com cara de senha.
    - Caso `an admin deletes a leaf unit using only the keyboard`: chega a `/admin/structure` pelo link "Estrutura" acionado por teclado (`focus()` + `Enter`); leva o foco à árvore, desce com as setas até o `treeitem` "Restauro e Conservação" (expandindo "Acervo e Processamento Técnico" pelo teclado se preciso), `Tab` até o botão "Apagar Restauro e Conservação" e `Enter`; o `alertdialog` "Apagar unidade?" abre com a descrição contendo "Restauro e Conservação"; roda `expectNoSeriousA11yViolations(page)` **com o diálogo aberto**; `Tab` até "Apagar" e `Enter`; espera o `treeitem` "Restauro e Conservação" sumir, a notificação "Unidade apagada" aparecer e `toBeFocused()` no `treeitem` "Acervo e Processamento Técnico". Sem `.click()` no caso.
    - Caso `deleting a unit with children is refused and the dialog stays open`: abre "Apagar Acervo e Processamento Técnico", confirma; a notificação mostra "Apague ou mova as unidades filhas antes de apagar esta unidade."; o `alertdialog` continua visível; `Escape` fecha e `toBeFocused()` no botão "Apagar Acervo e Processamento Técnico".
    - Caso `the root has no delete action`: o `treeitem` da raiz tem os botões "Criar unidade filha em …" e "Renomear …" e **nenhum** botão cujo nome começa por "Apagar"; uma unidade filha qualquer tem.
    - Os e2e da 064 e 065 não mudam (o botão novo vem depois de "Renomear", então a contagem de `Tab` deles continua certa) — se algum deles quebrar, o conserto é no código de produção, não no spec antigo.
    - Fecha a fase com um commit, por exemplo `test(e2e): cover deleting an org unit and document the slice`.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [ ] CA3.1 — `pnpm test:e2e` na raiz termina com exit code 0 (o Playwright sobe o próprio servidor; não precisa de API nem de Postgres), e continuam terminando com exit code 0 e **sem aviso**, com `docker compose up -d` rodando e o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta).
- [ ] CA3.2 — `apps/web/e2e/tests/org-units-delete.spec.ts` contém os casos `an admin deletes a leaf unit using only the keyboard`, `deleting a unit with children is refused and the dialog stays open` e `the root has no delete action`, e `pnpm --filter web exec playwright test --list` os lista junto com todos os casos já existentes de `org-units-view.spec.ts` e `org-units-create-rename.spec.ts` (nenhum removido nem renomeado). `rg -n "test\.skip|test\.only|waitForTimeout" apps/web/e2e` é vazio.
- [ ] CA3.3 — Lendo o primeiro caso: não há `.click()`; o botão "Apagar Restauro e Conservação" é alcançado por `Tab` e acionado por `Enter`; chama `expectNoSeriousA11yViolations` com o `alertdialog` "Apagar unidade?" aberto; depois de confirmar, espera o `treeitem` "Restauro e Conservação" sumir, a notificação "Unidade apagada" e `toBeFocused()` no `treeitem` "Acervo e Processamento Técnico". Lendo o segundo: confirma em "Acervo e Processamento Técnico", confere a notificação com "Apague ou mova as unidades filhas antes de apagar esta unidade.", que o `alertdialog` segue visível, e que `Escape` devolve o foco ao botão "Apagar Acervo e Processamento Técnico". Lendo o terceiro: confere que a linha da raiz não tem botão com nome iniciado por "Apagar" e que uma filha tem.
- [ ] CA3.4 — Esperas do e2e: `org-units-delete.spec.ts` declara `ROUTE_TIMEOUT` no topo e **toda** asserção logo após mudança de rota (`toHaveURL` e a primeira visibilidade da tela nova) o recebe; entre cada `Tab`/seta e o `Enter`/`Escape` seguinte há `toBeFocused()`; não há espera fixa.
- [ ] CA3.5 — `git diff --stat develop... -- apps/web/e2e` lista **só** `apps/web/e2e/tests/org-units-delete.spec.ts`; o spec não desativa regra do axe (`rg -n "disableRules|\.disableRules\(" apps/web/e2e/tests/org-units-delete.spec.ts` é vazio); os arquivos de `apps/web/e2e/**` passam no `pnpm lint` e no `pnpm typecheck`. `rg -n "folioteca_session=[A-Za-z0-9]|password" apps/web/e2e/tests/org-units-delete.spec.ts` é vazio.
- [ ] CA3.6 — `docs/architecture.md`: a §4 tem o parágrafo "Entrega `org-units-delete` (fatia 066)" dizendo: FKs `OrgUnit.parentId` e `Space.orgUnitId` em `RESTRICT` pela migration `0008` à mão; ordem obrigatória documento → espaço → unidade; regra do serviço (raiz, filhas, documentos inclusive na lixeira) em uma transação com uma consulta; contagem pela relação do espaço por causa da fronteira de `Document`; `P2003` → 409 de corrida; diálogo único no nível da árvore e foco na mãe pelo `onCloseAutoFocus`. A §6 diz que o `DELETE /org-units/{orgUnitId}` herda `SessionGuard` e `AdminGuard` da classe do controller.
- [ ] CA3.7 — A fatia está utilizável de ponta a ponta sem navegador aberto por pessoa: `npx vitest run --project api` (exit code 0) inclui `deletes a leaf, answers 204 without a body and removes it from the list`, `deleting a leaf also deletes its UNIT space` e `answers 409 when the only document of the space is in the trash` em `apps/api/src/org-units/__tests__/org-units.integration.test.ts`; `npx vitest run --project web` (exit code 0) inclui `deleting from the keyboard removes the unit, notifies and focuses the parent` em `apps/web/src/features/org-units/components/__tests__/org-units-tree.test.tsx`; e `pnpm test:e2e` (exit code 0) inclui as três jornadas de `org-units-delete.spec.ts`.
- [ ] CA3.8 — `git log develop..HEAD --format=%s` mostra exatamente três commits (um por fase), todos em inglês e no formato Conventional Commits; `git diff --stat develop... -- docs/roadmap.md` pode ser não vazio só pelo registro da orquestração.

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
