# PLAN 127 — unit-space-documents

Branch: `feature/127-unit-space-documents`, empilhada sobre a 145 (`share-with-person-view`) e a 140 (`unit-space-inherit-parent`).

Fonte: `docs/features/127-unit-space-documents/spec.md` (D1…D9 são as decisões técnicas da SPEC e R1…R15 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

Quem está lotado **diretamente** numa unidade abre a página do espaço dela, vê a lista de documentos do espaço, cria documentos ali e edita qualquer um deles (nível `edit`); só o dono manda para a lixeira. Quem alcança o espaço só por herança (fatia 140) vê um aviso e nenhum documento. Fora desta fatia: herança dando acesso a documento (152), documentos no espaço livre (136), derrubar a conexão de colaboração aberta ao remover a lotação (dívida).

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; banco de teste `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`. Se a suíte inteira (`pnpm test` ou `pnpm test:e2e`) falhar num teste **antigo** e alheio a esta fatia, o critério permite repetir o comando **uma** vez; a segunda execução tem de sair com 0.

**Critérios sobre o disco.** Os critérios são conferidos **antes** do commit da fase, sobre os arquivos em disco (commitados ou não). Nenhum critério depende de `git diff` contra a base. Onde um critério prova **ausência**, o padrão exige código (não casa com comentário nem com atributo parecido) ou o critério manda ler o arquivo. O commit de cada fase é feito pela orquestração depois da revisão; nenhum agente de fase commita.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **Nenhuma migration nova.** `apps/api/prisma/schema.prisma` muda só no comentário de `OrgUnitAssignment`. Os únicos subcomandos de Prisma permitidos são `prisma validate`, `prisma generate`, `prisma migrate status` e `prisma migrate deploy`, sempre contra `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test`; **nunca** `migrate diff`, `migrate dev` ou `migrate reset`. As migrations `0001…0015` não mudam.
- **Nenhum teste remove, desativa ou recria restrição do banco** (`DROP CONSTRAINT`, `ALTER TABLE … DISABLE`, `session_replication_role`).
- O arquivo `packages/api-contract/src/generated/openapi.d.ts` só muda por `pnpm --filter @folioteca/api-contract generate`; nunca editado à mão.
- **Sem Prettier reformatando arquivo existente**: só as linhas necessárias mudam; nada de `prettier --write` em arquivo inteiro.
- Todo componente devolve `React.JSX.Element` (ou `React.JSX.Element | null`), nunca o `JSX` global; `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case; **nenhum import entre features** (`features/spaces` não importa de `features/documents`; quem junta é a rota). Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Controle que precisa manter o foco durante o envio usa `aria-disabled`, nunca o `disabled` nativo. Em grupo de rádios, o foco vai para o rádio marcado. Ação destrutiva vem por último na linha de ações.
- Nenhum `eslint-disable` sem `-- <motivo>` na própria linha; nenhuma regra desativada para fazer teste passar; nenhum `any` sem comentário.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado, medida **só** por `coverage/coverage-summary.json`, gerado **na raiz** com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`, **sem `--project`** e **sem filtro de caminho**. Não contam arquivos só de tipos nem os já excluídos pelo `vitest.config.ts` da raiz. Nenhuma exclusão nova.
- **Toda** espera logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes da web pela constante `LAZY_TIMEOUT` declarada no arquivo; no e2e por `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo do spec. Nunca `setTimeout`/sleep fixo nos testes nem `waitForTimeout`.
- Testes de API de integração e contrato rodam contra o Postgres real (sem mock do Prisma); só os `*.service.test.ts` usam Prisma falso. "Teste passa" é medido pelo **exit code** (zero), nunca pela saída `--verbose`.
- **Escopo por sessão**: `organizationId` e `personId` de quem pede vêm sempre de `@CurrentPerson()`, nunca do corpo. Escopo de outra organização é provado chamando o **serviço real** com `randomUUID()` como `organizationId` (`Organization.singleton` impede uma segunda organização no banco).
- Assinaturas públicas de `AccessService` (`resolveAccess`, `canWrite`, `readableDocumentsWhere`, `trashedDocumentsWhere`) não mudam; só o corpo. `canWrite` não muda nem de corpo.
- Intocados de propósito: `apps/api/src/collab/collab.service.ts`, `apps/api/src/access/access-level.ts`, `apps/api/src/documents/shares.service.ts`, `apps/api/src/documents/favorites.service.ts`, `trashedDocumentsWhere`, `apps/web/src/features/documents/components/document-view.tsx`, `apps/web/src/app/routes/app/root.tsx`, `apps/web/src/components/ui/**`, migrations `0001…0015`.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa.

## Fase 1 — API: membro direto de espaço de unidade edita, `GET /spaces/{spaceId}/documents` e `POST /documents` com `spaceId`

Caminhos relativos à raiz do repositório. Ao fim da fase, pela API, uma pessoa lotada diretamente cria um documento no espaço da unidade, o colega o vê em `GET /spaces/{spaceId}/documents`, abre com `accessLevel: 'edit'` e renomeia; quem só herda recebe 403.

- [x] T1.1 — Contrato primeiro: lista do espaço e corpo opcional na criação
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D2, D3, R1, R3, R10, R11):
    - Caminho `/spaces/{spaceId}/documents` com `get`, `operationId: listSpaceDocuments`, tag `spaces`, parâmetro de caminho `spaceId` (string, obrigatório), respostas **200** `DocumentsResponse` (reaproveitado) e **401**, **403**, **404** com `Error`; `description` em pt_BR: só quem está lotado diretamente na unidade; alcance só por herança → 403; sem alcance, id malformado ou espaço que não é de unidade → 404 opaco.
    - `POST /documents`: `requestBody` com `required: false` e `$ref` `CreateDocumentInput`; respostas ganham **400** (`ValidationError`) e **404** (`Error`), mantendo as existentes; `summary`: "Cria um documento sem título no espaço pessoal de quem chama ou no espaço de unidade informado".
    - Schema novo `CreateDocumentInput`: `type: object`, `additionalProperties: false`, `properties: { spaceId: { type: string } }`, sem `required`.
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`.
  - Skills: security
  - Complexidade: baixa

- [x] T1.2 — `AccessService`: membro direto de espaço `UNIT` = `edit`, na mesma consulta
  - Arquivos: `apps/api/src/access/access.service.ts` (alterar); `apps/api/prisma/schema.prisma` (alterar, só comentário)
  - O que fazer (D1, R4, R7, R8, R9, R12, R13):
    - `findDecision(documentId, personId)` acrescenta ao **mesmo** `select`: `space: { select: { type: true, orgUnit: { select: { assignments: { where: { personId }, select: { personId: true } } } } } }`. Continua uma ida ao banco. `DocumentDecision` ganha `isUnitMember: boolean` = `space.type === 'UNIT' && (space.orgUnit?.assignments.length ?? 0) > 0`.
    - `levelOf`: dono → `'owner'`; lixeira → `'none'` (antes do membro e da share); depois o **maior** nível entre `shareLevel` e `isUnitMember ? 'edit' : null` (ordem `edit > view`); senão `'none'`. A herança da 140 **não** entra (não chamar `resolveReach`).
    - `readableDocumentsWhere(personId)` → `{ trashedAt: null, OR: [{ ownerId: personId }, { shares: { some: { personId } } }, { space: { type: 'UNIT', orgUnit: { assignments: { some: { personId } } } } }] }`, com o literal `trashedAt: null` preservado.
    - `schema.prisma`: reescrever **só** o comentário de `model OrgUnitAssignment` para dizer que a lotação direta dá acesso de edição aos documentos do espaço da unidade, decidido pela porta `AccessService` (em pt_BR). Nenhum campo, índice ou relação muda.
  - Skills: authorization
  - Complexidade: alta

- [x] T1.3 — `POST /documents` com `spaceId` e a leitura `listInSpace` em `documents/`
  - Arquivos: `apps/api/src/common/space-not-found.ts` (criar); `apps/api/src/documents/documents.schema.ts` (alterar); `apps/api/src/documents/documents.service.ts` (alterar); `apps/api/src/documents/documents.controller.ts` (alterar)
  - O que fazer (D2, D3, R3, R4, R11):
    - `space-not-found.ts`: exporta `spaceNotFound(): NotFoundException` com a mensagem `Espaço não encontrado.`.
    - `documents.schema.ts`: `createDocumentSchema = z.strictObject({ spaceId: z.string().optional() }, { error: 'Campo não permitido.' })`.
    - `documents.service.ts`: `create(person: PersonWithOrganization, body: unknown)`, na ordem: (1) corpo `undefined` vira `{}`; `parseBody(createDocumentSchema, …)` → 400 (campo extra, `spaceId` não string); (2) sem `spaceId` → exatamente o comportamento de hoje (upsert do espaço pessoal); (3) com `spaceId`: `!isUuid(spaceId)` → `spaceNotFound()`; `tx.space.findFirst({ where: { id: spaceId, type: 'UNIT', orgUnit: { organizationId: person.organizationId, assignments: { some: { personId: person.id } } } }, select: { id: true } })` ausente → `spaceNotFound()` (herdado também cai aqui); (4) `document.create` com `ownerId` e `authorId` = `person.id` e o `spaceId`. Método novo `listInSpace(personId: string, spaceId: string): Promise<DocumentsResponse>`: `findMany({ where: { AND: [this.access.readableDocumentsWhere(personId), { spaceId }] }, orderBy: [{ updatedAt: 'desc' }, { id: 'desc' }], take: 100, select: { id: true, title: true, updatedAt: true } })`, mesmo mapeamento de `listMine`. Não filtra por `assignments` (a lotação só entra pela porta).
    - `documents.controller.ts`: o `@Post()` passa a receber `@Body() body: unknown` e chama `create(person, body)`.
  - Skills: authorization, security
  - Complexidade: média

- [x] T1.4 — `GET /spaces/{spaceId}/documents` com `reachOf` e a ordem 401 → 404 → 403 → 200
  - Arquivos: `apps/api/src/spaces/spaces.service.ts` (alterar); `apps/api/src/spaces/spaces.controller.ts` (alterar); `apps/api/src/spaces/spaces.module.ts` (alterar)
  - O que fazer (D2, R1, R10, R11):
    - `spaces.service.ts`: extrair o `findMany` das unidades usado por `list` para um método privado, reusado por `list` e `reachOf` (sem duplicar a consulta). Novo `reachOf(organizationId: string, personId: string, spaceId: string): Promise<'direct' | 'inherited' | 'none'>`: espaço `UNIT` com `orgUnit.organizationId = organizationId` não achado (inclusive `FREE` e `PERSONAL`) → `'none'`; lotação direta da pessoa na unidade → `'direct'`; senão lê as unidades e aplica `resolveReach` → `'inherited'` se a unidade é alcançada por herança, senão `'none'`.
    - `spaces.controller.ts` (`@Controller('spaces')`, `SessionGuard` na classe, intocado): `@Get(':spaceId/documents')` com `@CurrentPerson() person` e `@Param('spaceId') spaceId: string`, nesta ordem: `!isUuid(spaceId)` → `spaceNotFound()`; `reachOf(person.organizationId, person.id, spaceId)` `'none'` → `spaceNotFound()`; `'inherited'` → `ForbiddenException('Os documentos deste espaço estão disponíveis para quem está lotado diretamente na unidade.')`; `'direct'` → `documents.listInSpace(person.id, spaceId)`.
    - `spaces.module.ts`: `imports` ganha `DocumentsModule` (que já exporta `DocumentsService`; `DocumentsModule` não importa `SpacesModule`).
  - Skills: authorization, security
  - Complexidade: média

- [x] T1.5 — Testes da fase 1
  - Arquivos: `apps/api/src/access/__tests__/access-level.test.ts` (alterar); `apps/api/src/access/__tests__/access.integration.test.ts` (alterar); `apps/api/src/access/__tests__/document-access-boundary.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.integration.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.schema.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.contract.test.ts` (alterar); `apps/api/src/spaces/__tests__/spaces.integration.test.ts` (alterar); `apps/api/src/spaces/__tests__/spaces.contract.test.ts` (alterar); `apps/api/src/collab/__tests__/collab.integration.test.ts` (alterar)
  - O que fazer (D4, D5, D9): integração e contrato contra o Postgres real com `resetDatabase(prisma)` em `beforeEach` e os ajudantes existentes; `apps/api/test/**` não muda. Casos preexistentes cujo Prisma falso muda por T1.2 são ajustados **sem trocar o nome**. Nenhuma regra existente do teste de fronteira afrouxa.
    - `access-level.test.ts` (onde `levelOf` já é testado): `levelOf returns edit for a direct unit member`; `levelOf returns none for a unit member on a trashed document`; `levelOf returns the higher level between a view share and unit membership`; `levelOf ignores membership outside a UNIT space`.
    - `access.integration.test.ts`: `resolveAccess returns edit for a direct member of the unit space`; `canWrite is true for a direct unit member`; `removing the assignment makes the next resolveAccess none`; `a person assigned only to the parent unit gets none on an inheriting child space`; `a trashed unit space document is none for the member and owner for the owner, edit again after restore`; `the owner removed from the unit still gets owner`; `a view share plus unit membership resolves to edit`; `readableDocumentsWhere includes the unit space document and excludes it in the trash`.
    - `document-access-boundary.test.ts`: regra 10 com `rule 10 flags a Document read filtered by assignments outside access service` (exemplo infrator) e `rule 10 accepts assignments in the access service` (exemplo conforme), mais a verificação sobre o código real: fora de `apps/api/src/access/access.service.ts`, nenhuma leitura de `Document` (`findMany`, `findFirst`, `findUnique`, `count`) filtra por `assignments`.
    - `documents.schema.test.ts`: `createDocumentSchema accepts an empty object`; `createDocumentSchema accepts a spaceId string`; `createDocumentSchema rejects a non string spaceId`; `createDocumentSchema rejects extra fields with Campo não permitido.`.
    - `documents.integration.test.ts`: `POST documents without body still creates in the personal space`; `POST documents with a direct unit spaceId answers 201 in that space owned by the caller`; `a document created in the unit space appears in my documents of the creator and not of the colleague`; `POST documents with an inherited unit spaceId answers 404 with Espaço não encontrado.`; `POST documents with a unit spaceId without assignment answers 404`; `POST documents with a random spaceId answers 404`; `POST documents with a malformed spaceId answers 404`; `POST documents with an extra field answers 400`; `a colleague GETs the unit space document with accessLevel edit`; `a colleague renames the unit space document with 200`; `a colleague gets 404 on trash, restore and delete of the unit space document`; `create with another organization id answers 404` (serviço real, `{ ...person, organizationId: randomUUID() }`, com espaço de unidade existente e lotação direta).
    - `documents.contract.test.ts`: `POST documents with spaceId answers the documented 201`, `… 400`, `… 404`, chamando exatamente `/documents`.
    - `spaces.integration.test.ts`: `GET space documents answers 200 to a direct member ordered by updatedAt desc`; `GET space documents omits trashed documents`; `GET space documents answers 403 with the direct assignment message to an inherited member`; `GET space documents answers 404 without assignment`; `GET space documents answers 404 to an unassigned admin`; `GET space documents answers 404 for a random id`; `GET space documents answers 404 for a malformed id`; `GET space documents answers 404 for a FREE space`; `GET space documents answers 401 without session`; `reachOf with another organization id returns none` (serviço real, `randomUUID()`). Os 404 comparam corpo idêntico (`Espaço não encontrado.`).
    - `spaces.contract.test.ts`: `GET space documents answers the documented 200`, `… 401`, `… 403`, `… 404`, chamando exatamente `/spaces/{id}/documents`.
    - `collab.integration.test.ts`: `a direct unit member connects and an update is stored`; `after removing the assignment the next connection is refused`.
  - Skills: unit-testing, integration-testing
  - Complexidade: média

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules`): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e sem aviso.
- [x] CA1.2 — `packages/api-contract/openapi.yaml` tem `/spaces/{spaceId}/documents:` com `get`, `operationId: listSpaceDocuments`, tag `spaces`, parâmetro de caminho `spaceId` e respostas exatamente `200` (`DocumentsResponse`), `401`, `403`, `404`; `POST` de `/documents` tem `requestBody` com `required: false` e `$ref` `CreateDocumentInput`, e respostas que incluem `400` (`ValidationError`) e `404`; `CreateDocumentInput` tem `additionalProperties: false` e só a propriedade `spaceId` (`type: string`), sem `required`. `rg -n "listSpaceDocuments|CreateDocumentInput" packages/api-contract/src/generated/openapi.d.ts` encontra os dois.
- [x] CA1.3 — Identidade de caminho conferida à mão: `apps/api/src/spaces/spaces.controller.ts` continua com `@Controller('spaces')` e tem `@Get(':spaceId/documents')` com `@Param('spaceId') spaceId: string`, o que forma `/spaces/{spaceId}/documents` do YAML; lido, a ordem é `isUuid` → `reachOf` `'none'` → `spaceNotFound()` → `'inherited'` → `ForbiddenException` com `Os documentos deste espaço estão disponíveis para quem está lotado diretamente na unidade.` → `listInSpace`. `apps/api/src/documents/documents.controller.ts` tem `@Body() body: unknown` no `@Post()` e continua com `@Controller('documents')`. `apps/api/src/spaces/spaces.module.ts` importa `DocumentsModule`; `rg -n "SpacesModule" apps/api/src/documents/documents.module.ts` é vazio.
- [x] CA1.4 — `apps/api/src/access/access.service.ts`, lido: `findDecision` seleciona `space` com `type` e `orgUnit.assignments` filtrado por `personId` na mesma consulta do documento; `DocumentDecision` tem `isUnitMember: boolean`; em `levelOf` o dono vem primeiro, a lixeira antes do membro e da share, e o membro vale `'edit'` só com `type === 'UNIT'`; `readableDocumentsWhere` contém `trashedAt: null`, `shares: { some: { personId } }` e `assignments: { some: { personId } }` dentro de `space`. `rg -n "resolveReach" apps/api/src/access` é vazio. As assinaturas públicas `resolveAccess`, `canWrite`, `readableDocumentsWhere` e `trashedDocumentsWhere` são as mesmas de antes.
- [x] CA1.5 — `ls apps/api/prisma/migrations` lista só `0001…0015` e `migration_lock.toml`; em `apps/api/prisma/schema.prisma` o comentário de `OrgUnitAssignment` diz que a lotação direta dá acesso de edição aos documentos do espaço da unidade pela `AccessService`, e a frase `Lotação não dá acesso a documento` não aparece no arquivo. `pnpm --filter api exec prisma validate` sai com 0 e `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test pnpm --filter api exec prisma migrate status` reporta o banco em dia.
- [x] CA1.6 — `apps/api/src/common/space-not-found.ts` exporta `spaceNotFound` com `Espaço não encontrado.`; `rg -n "Espaço não encontrado\." apps/api/src --glob '!**/__tests__/**'` só casa nesse arquivo. `apps/api/src/documents/documents.schema.ts` exporta `createDocumentSchema` (`z.strictObject`, `spaceId: z.string().optional()`, `Campo não permitido.`). `apps/api/src/documents/documents.service.ts` tem `create(person: PersonWithOrganization, body: unknown)` com `type: 'UNIT'`, `organizationId: person.organizationId`, `assignments: { some: { personId: person.id } }` e `spaceNotFound()`, e `listInSpace(personId: string, spaceId: string)` com `readableDocumentsWhere(personId)`, `{ spaceId }`, `take: 100` e `orderBy` `updatedAt: 'desc'` seguido de `id: 'desc'`. `apps/api/src/spaces/spaces.service.ts` tem `reachOf(organizationId: string, personId: string, spaceId: string): Promise<'direct' | 'inherited' | 'none'>` e, lido, `list` e `reachOf` chamam o mesmo método privado para ler as unidades.
- [x] CA1.7 — `pnpm exec vitest run --project api` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/api/src/access/__tests__ apps/api/src/documents/__tests__ apps/api/src/spaces/__tests__ apps/api/src/collab/__tests__`, todos os casos nomeados em T1.5 com os nomes literais (`access-level.test.ts`: 4; `access.integration.test.ts`: 8; `document-access-boundary.test.ts`: 2 novos; `documents.schema.test.ts`: 4; `documents.integration.test.ts`: 12; `documents.contract.test.ts`: 3; `spaces.integration.test.ts`: 10; `spaces.contract.test.ts`: 4; `collab.integration.test.ts`: 2). `rg -n "vi\.mock\(.*prisma" apps/api/src/access/__tests__/access.integration.test.ts apps/api/src/documents/__tests__/documents.integration.test.ts apps/api/src/spaces/__tests__/spaces.integration.test.ts` é vazio.
- [x] CA1.8 — Lendo os testes: `create with another organization id answers 404` e `reachOf with another organization id returns none` chamam o serviço real (instância ligada ao Prisma de teste) com `randomUUID()` como `organizationId`, sobre espaço de unidade e lotação direta que existem; `removing the assignment makes the next resolveAccess none` apaga a linha de `OrgUnitAssignment` e chama `resolveAccess` de novo; `a person assigned only to the parent unit gets none on an inheriting child space` usa unidade filha com `inheritsParent` verdadeiro; `after removing the assignment the next connection is refused` abre nova conexão depois de apagar a lotação; os casos 404 de `spaces.integration.test.ts` comparam o corpo com o do id aleatório. `rg -n "DROP CONSTRAINT|DISABLE TRIGGER|session_replication_role" apps/api/src apps/api/test` é vazio.
- [x] CA1.9 — `rg -n "setTimeout\(|sleep\(" apps/api/src/documents apps/api/src/spaces apps/api/src/access` é vazio; `rg -n "eslint-disable" apps/api/src | rg -v -- "--"` é vazio.
- [x] CA1.10 — Cobertura ≥ 80% de linhas para `apps/api/src/access/access.service.ts`, `apps/api/src/common/space-not-found.ts`, `apps/api/src/documents/documents.schema.ts`, `apps/api/src/documents/documents.service.ts`, `apps/api/src/documents/documents.controller.ts`, `apps/api/src/spaces/spaces.service.ts` e `apps/api/src/spaces/spaces.controller.ts`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 2 — Web: a lista de documentos na página do espaço de unidade, com "Novo documento"

Caminhos relativos à raiz do repositório. A ordem importa: API simulada e chamadas antes da tela. Ao fim da fase, no app com API simulada, o lotado direto vê a lista do espaço, cria um documento e o abre; quem só herda vê o aviso.

- [x] T2.1 — API simulada: lista do espaço, criação com `spaceId` e a semente `unit-space`
  - Arquivos: `apps/web/src/testing/mocks/db.ts` (alterar); `apps/web/src/testing/mocks/handlers/documents.ts` (alterar); `apps/web/src/testing/mocks/handlers/spaces.ts` (alterar); `apps/web/src/testing/mocks/index.ts` (alterar); `apps/web/src/testing/mocks/utils.ts` (alterar)
  - O que fazer (D8):
    - `db.ts`: `createDocumentIn(personId: string, spaceId: string)` (reaproveitado pelo handler); `spaceReachOf(personId: string, spaceId: string): 'direct' | 'inherited' | 'none'` com a mesma regra de `listSpacesOf`; `listSpaceDocuments(spaceId: string)` (fora da lixeira, `updatedAt` desc); `seedUnitSpaceDocuments()` cria um documento de colega (`accessLevel: 'edit'`) no espaço da unidade de lotação direta da amostra `mock-org-units=sample`.
    - `handlers/documents.ts`: o `POST` de `/documents` lê o corpo opcional; com `spaceId` cujo `spaceReachOf` não é `'direct'` → 404 `Espaço não encontrado.`; senão cria no espaço.
    - `handlers/spaces.ts`: `http.get(\`${env.API_URL}/spaces/:spaceId/documents\`)` com o preâmbulo de sempre (`networkDelay()`, `devOverride('spaces')`, 401) e 404 `Espaço não encontrado.` / 403 `Os documentos deste espaço estão disponíveis para quem está lotado diretamente na unidade.` / 200 `{ data }`.
    - `index.ts`: chave `mock-documents=unit-space` chama `seedUnitSpaceDocuments()`; `utils.ts` documenta a chave junto das outras.
  - Skills: api-mocking
  - Complexidade: média

- [x] T2.2 — Chamadas da web: lista do espaço, criação com `spaceId` e invalidação das listas
  - Arquivos: `apps/web/src/features/documents/api/get-space-documents.ts` (criar); `apps/web/src/features/documents/api/get-documents.ts` (alterar); `apps/web/src/features/documents/api/create-document.ts` (alterar)
  - O que fazer (D6, R3, R7):
    - `get-space-documents.ts`: `getSpaceDocuments(spaceId: string): Promise<DocumentsResponse>` → `api.get(\`/spaces/${spaceId}/documents\`, { silentError: true })`; `getSpaceDocumentsQueryOptions(spaceId)` com `queryKey: ['space-documents', spaceId]`; `useSpaceDocuments({ spaceId }: { spaceId: string })`.
    - `get-documents.ts`: `invalidateDocumentLists` também invalida o prefixo `['space-documents']` (comentário curto em en_US: prefixo próprio para não pegar o documento aberto).
    - `create-document.ts`: `createDocument(input?: { spaceId?: string })` → `api.post('/documents', input)` (sem corpo quando ausente); `onSuccess` passa a chamar `invalidateDocumentLists` no lugar da invalidação só de `mine`.
  - Skills: api-requests, error-handling
  - Complexidade: baixa

- [x] T2.3 — `DocumentsListStates`, `NewDocumentButton` com `spaceId` e `SpaceDocuments`
  - Arquivos: `apps/web/src/features/documents/components/documents-list.tsx` (alterar); `apps/web/src/features/documents/components/new-document-button.tsx` (alterar); `apps/web/src/features/documents/components/space-documents.tsx` (criar)
  - O que fazer (D6, D7, Interface, R1, R2, R3, R10, R14, R15). Aparência pelo `docs/design.md` e pelo piso de `interface-design`, com as receitas "Lista", "Data em lista", "Link de navegação", "Botão principal", "Carregando", "Vazio", "Erro", "Aviso informativo" e a receita **nova** "Ação de página acima da lista" (linha `mt-6` com o botão principal à direita, a lista abaixo mantendo o próprio `mt-6`) — registrada no `docs/design.md` na fase 3:
    - `documents-list.tsx`: refatoração **sem mudar comportamento nem textos**: os quatro estados e o `<ul>` passam para `DocumentsListStates({ query, texts })`, exportado do mesmo arquivo (`texts` com os textos de carregando, vazio e erro); `DocumentsList` continua chamando `useDocuments` e o usa. Retorno `React.JSX.Element`.
    - `new-document-button.tsx`: props opcionais `spaceId?: string` e `className?: string` (padrão `'w-full'`, o de hoje); com `spaceId`, a mutation envia `{ spaceId }`. Mesmo texto "Novo documento", mesmo "Criando…" durante o envio, mesma navegação ao documento criado; falha continua na notificação do cliente HTTP. `root.tsx` não muda.
    - `space-documents.tsx`: `SpaceDocuments({ spaceId }: { spaceId: string }): React.JSX.Element` com `useSpaceDocuments`. Utilitário local `isForbidden(error)` = `isAxiosError(error) && error.response?.status === 403`. Estados, no mesmo lugar: 403 → "Aviso informativo" com **"Os documentos deste espaço estão disponíveis para quem está lotado diretamente na unidade."**, sem botão e sem lista; carregando → `role="status"` **"Carregando documentos do espaço…"**, sem botão; erro (não 403) → `role="alert"` **"Não foi possível carregar os documentos do espaço."** com `Button` **"Tentar novamente"** (`refetch`), sem botão de criar; 200 → linha "Ação de página acima da lista" com `NewDocumentButton spaceId={spaceId}` (sem `w-full`) e, abaixo, `DocumentsListStates` com o vazio **"Nenhum documento neste espaço ainda. Crie o primeiro em “Novo documento”."** ou a lista (título-link `truncate` à esquerda, data à direita, como em "Meus documentos"). O botão de criar só aparece com resposta 200.
  - Skills: interface-design, error-handling, authorization, component-robustness
  - Complexidade: média

- [x] T2.4 — `SpaceView` recebe o conteúdo do espaço de unidade e a rota compõe
  - Arquivos: `apps/web/src/features/spaces/components/space-view.tsx` (alterar); `apps/web/src/app/routes/app/space.tsx` (alterar)
  - O que fazer (D7, R1, R10):
    - `space-view.tsx`: prop nova `unitContent: ReactNode`, renderizada no lugar do aviso **só** quando o espaço achado é `unit`; o espaço `free` mantém o aviso de hoje ("Os documentos deste espaço ainda não chegaram…"). O texto `SPACE_TEXTS.unit.empty` sai. Título (nome da unidade) e "O espaço de documentos da sua unidade." ficam. Não importa de `@/features/documents`.
    - `space.tsx`: passa `unitContent={<SpaceDocuments spaceId={spaceId} />}` a `SpaceView`.
  - Skills: interface-design, project-structure, routing
  - Complexidade: baixa

- [x] T2.5 — Testes da fase 2 e os e2e antigos
  - Arquivos: `apps/web/src/features/documents/api/__tests__/get-space-documents.test.tsx` (criar); `apps/web/src/features/documents/api/__tests__/create-document.test.tsx` (alterar); `apps/web/src/features/documents/components/__tests__/space-documents.test.tsx` (criar); `apps/web/src/features/documents/components/__tests__/new-document-button.test.tsx` (alterar); `apps/web/src/features/spaces/components/__tests__/space-view.test.tsx` (alterar); `apps/web/src/app/routes/app/__tests__/space.test.tsx` (alterar)
  - O que fazer (D9): banco falso pelos ajudantes; `userEvent`; localizar por papel e nome acessível em pt_BR; `LAZY_TIMEOUT` em cada arquivo que espera após rota ou `lazy`; sem `console.error`/`console.warn`/aviso de `act(...)`. Casos preexistentes não são renomeados; `documents-list.test.tsx` continua verde **sem mudar nenhuma asserção**.
    - `get-space-documents.test.tsx`: `getSpaceDocuments requests the space documents path`; `useSpaceDocuments uses the space-documents query key`.
    - `create-document.test.tsx`: `createDocument sends spaceId in the body`; `createDocument without input sends no body`; `useCreateDocument invalidates the space documents list`.
    - `space-documents.test.tsx`: `shows the loading status without the new document button`; `shows the empty message with the new document button`; `lists the space documents with links`; `shows the error with Tentar novamente and refetches`; `a 403 shows the direct assignment notice without button nor list`; `creating a document navigates to it`.
    - `new-document-button.test.tsx`: `keeps w-full by default`; `sends the spaceId when given`.
    - `space-view.test.tsx`: `renders unitContent for a unit space`; `a free space keeps the notice and ignores unitContent`.
    - `space.test.tsx`: `the unit space page shows the space documents list`; `the inherited unit space page shows the direct assignment notice`.
    - Rodar `pnpm test:e2e` com **todos** os e2e existentes; se algum quebrar, o conserto é no código de produção. Conserto em spec antigo só se a mudança de UI for intencional (ex.: um segundo botão "Novo documento" na página do espaço torna ambígua uma busca por nome, ou o aviso antigo do espaço de unidade sumiu) e registrado como desvio no fim deste plano.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` saem com 0 e sem aviso; a saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)`. `pnpm test:e2e` (todos os e2e existentes) sai com 0.
- [x] CA2.2 — `apps/web/src/features/documents/api/get-space-documents.ts` exporta `getSpaceDocuments` (chama `api.get` em `` `/spaces/${spaceId}/documents` `` com `silentError: true`) e `useSpaceDocuments`, com `['space-documents', spaceId]`. `apps/web/src/features/documents/api/get-documents.ts`, lido, invalida `['space-documents']` em `invalidateDocumentLists`. `apps/web/src/features/documents/api/create-document.ts` tem `createDocument(input?: { spaceId?: string })` chamando `api.post('/documents', input)` e `invalidateDocumentLists` no `onSuccess`.
- [x] CA2.3 — `apps/web/src/features/documents/components/space-documents.tsx` exporta `SpaceDocuments` com retorno `React.JSX.Element` e contém literalmente `Carregando documentos do espaço…`, `Nenhum documento neste espaço ainda. Crie o primeiro em “Novo documento”.`, `Não foi possível carregar os documentos do espaço.`, `Tentar novamente`, `Os documentos deste espaço estão disponíveis para quem está lotado diretamente na unidade.`, `role="status"`, `role="alert"`, `status === 403`, `NewDocumentButton` e `DocumentsListStates`; lido, `NewDocumentButton` só é renderizado com a consulta em sucesso.
- [x] CA2.4 — `apps/web/src/features/documents/components/documents-list.tsx` exporta `DocumentsListStates` e `DocumentsList`, e `DocumentsList` usa `useDocuments` e `DocumentsListStates`. `apps/web/src/features/documents/components/new-document-button.tsx` aceita `spaceId?: string` e `className?: string` com padrão `'w-full'`, e contém `Novo documento` e `Criando…`. `apps/web/src/features/spaces/components/space-view.tsx` tem a prop `unitContent: ReactNode`; `rg -n "features/documents" apps/web/src/features/spaces` é vazio; `rg -n "from '@/features/(?!documents)" -P apps/web/src/features/documents` é vazio. `apps/web/src/app/routes/app/space.tsx` renderiza `SpaceDocuments` passado em `unitContent`.
- [x] CA2.5 — Acabamento (piso de `interface-design`, lendo o código): em `space-documents.tsx` a linha do botão usa a receita "Ação de página acima da lista" (`mt-6 flex justify-end`); o erro tem fundo ou borda de destaque e o botão "Tentar novamente" é `Button`; o aviso de lotação direta usa a receita "Aviso informativo" do `docs/design.md`; cada item da lista é `<Link>` com `truncate` e a data à direita em tom secundário (herdado de `DocumentsListStates`). `rg -n "<button|<a href|style=\{\{|!important|forwardRef|: JSX\." apps/web/src/features/documents/components/space-documents.tsx apps/web/src/features/documents/components/documents-list.tsx apps/web/src/features/documents/components/new-document-button.tsx apps/web/src/features/spaces/components/space-view.tsx` é vazio; nenhuma largura fixa em px nesses arquivos.
- [x] CA2.6 — API simulada: `apps/web/src/testing/mocks/db.ts` exporta `createDocumentIn(personId: string, spaceId: string)`, `spaceReachOf(personId: string, spaceId: string)`, `listSpaceDocuments(spaceId: string)` e `seedUnitSpaceDocuments()`; `apps/web/src/testing/mocks/handlers/spaces.ts` tem `http.get` em `${env.API_URL}/spaces/:spaceId/documents` com `networkDelay()`, `devOverride('spaces')` e as respostas 401, 403, 404 e 200; `apps/web/src/testing/mocks/handlers/documents.ts` responde 404 `Espaço não encontrado.` no `POST` com `spaceId` não direto; `apps/web/src/testing/mocks/index.ts` e `apps/web/src/testing/mocks/utils.ts` contêm `unit-space`.
- [x] CA2.7 — `pnpm exec vitest run --project web` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/web/src/features/documents apps/web/src/features/spaces apps/web/src/app/routes/app/__tests__`, os 17 casos nomeados em T2.5 com os nomes literais (`get-space-documents.test.tsx`: 2; `create-document.test.tsx`: 3 novos; `space-documents.test.tsx`: 6; `new-document-button.test.tsx`: 2 novos; `space-view.test.tsx`: 2 novos; `space.test.tsx`: 2 novos).
- [x] CA2.8 — Lendo os testes: `a 403 shows the direct assignment notice without button nor list` assere o texto do aviso e `queryByRole('button', { name: 'Novo documento' })` nulo e nenhuma `list`; `shows the loading status without the new document button` assere `role="status"` e ausência do botão; `shows the error with Tentar novamente and refetches` conta uma segunda chamada ao `GET` após o clique; `creating a document navigates to it` confere o `POST` com `spaceId` no corpo e a rota do documento; `a free space keeps the notice and ignores unitContent` assere o aviso antigo e a ausência do conteúdo passado.
- [x] CA2.9 — `rg -n "sleep\(|waitForTimeout|setTimeout\(" apps/web/src/features/documents/api/get-space-documents.ts apps/web/src/features/documents/api/get-documents.ts apps/web/src/features/documents/api/create-document.ts apps/web/src/features/documents/components/documents-list.tsx apps/web/src/features/documents/components/new-document-button.tsx apps/web/src/features/documents/components/space-documents.tsx apps/web/src/features/spaces/components/space-view.tsx apps/web/src/app/routes/app/space.tsx apps/web/src/features/documents/api/__tests__/get-space-documents.test.tsx apps/web/src/features/documents/api/__tests__/create-document.test.tsx apps/web/src/features/documents/components/__tests__/documents-list.test.tsx apps/web/src/features/documents/components/__tests__/new-document-button.test.tsx apps/web/src/features/documents/components/__tests__/space-documents.test.tsx apps/web/src/features/spaces/components/__tests__/space-view.test.tsx apps/web/src/app/routes/app/__tests__/space.test.tsx` é vazio; `rg -n "eslint-disable" apps/web/src | rg -v -- "--"` é vazio.
- [x] CA2.10 — Cobertura ≥ 80% de linhas para `apps/web/src/features/documents/api/get-space-documents.ts`, `apps/web/src/features/documents/api/get-documents.ts`, `apps/web/src/features/documents/api/create-document.ts`, `apps/web/src/features/documents/components/documents-list.tsx`, `apps/web/src/features/documents/components/new-document-button.tsx`, `apps/web/src/features/documents/components/space-documents.tsx`, `apps/web/src/features/spaces/components/space-view.tsx` e `apps/web/src/app/routes/app/space.tsx`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 3 — e2e da jornada e documentação: a fatia utilizável de ponta a ponta

- [ ] T3.1 — Documentação: receita nova e arquitetura
  - Arquivos: `docs/design.md` (alterar); `docs/architecture.md` (alterar)
  - O que fazer, em pt_BR:
    - `docs/design.md`, em "Padrões acrescentados pelas entregas": receita **"Ação de página acima da lista"** (fatia 127): `<div className="mt-6 flex justify-end">` com o botão principal; a lista abaixo mantém o próprio `mt-6`.
    - `docs/architecture.md` §3 "access": parágrafo **"Entrega `unit-space-documents` (fatia 127)"**: membro direto de espaço `UNIT` = `edit`, lido na mesma consulta a cada pedido; ordem dono → lixeira → maior(compartilhamento, membro) → `none`; herança não dá acesso (152); `readableDocumentsWhere` inclui o espaço da unidade; regra 10 da fronteira (só `access.service.ts` filtra `Document` por `assignments`). Reescrever a frase "Lotação não dá acesso a documento" (§unit-assignments) para dizer que a lotação direta dá acesso pela porta. §spaces: `GET /spaces/{spaceId}/documents` com a ordem 401 → 404 → 403 → 200 e `reachOf`; `POST /documents` com `spaceId` opcional.
  - Skills: interface-design
  - Complexidade: baixa

- [ ] T3.2 — Testes da fase 3 (e2e: o lotado cria no espaço, o herdado vê o aviso, só pelo teclado)
  - Arquivos: `apps/web/e2e/tests/unit-space-documents.spec.ts` (criar)
  - O que fazer (D9): API simulada, estado inicial por `page.addInitScript` com `mock-org-units=sample`, como os specs existentes. `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo, usado em toda espera após mudança de rota; `toBeFocused()` antes de cada `Enter`/`Space`; nenhum `waitForTimeout`; nenhuma regra do axe desativada; outros specs e `apps/web/e2e/a11y.ts` intocados.
    - `a direct member creates a document in the unit space using only the keyboard`: abre o espaço da unidade de lotação direta, vê "Nenhum documento neste espaço ainda. Crie o primeiro em “Novo documento”.", `expectNoSeriousA11yViolations(page)`, alcança "Novo documento" por `Tab`, aciona, chega ao documento (`ROUTE_TIMEOUT`), volta ao espaço e vê o documento no topo da lista.
    - `an inherited member sees the direct assignment notice`: abre o espaço de unidade alcançado só por herança na amostra, vê "Os documentos deste espaço estão disponíveis para quem está lotado diretamente na unidade.", não há botão "Novo documento" na área principal da página, `expectNoSeriousA11yViolations(page)`.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [ ] CA3.1 — `pnpm test:e2e` na raiz sai com 0 (os e2e antigos e os novos), e `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` continuam saindo com 0 e sem aviso, com o cache do `tsc` limpo.
- [ ] CA3.2 — `apps/web/e2e/tests/unit-space-documents.spec.ts` contém os casos `a direct member creates a document in the unit space using only the keyboard` e `an inherited member sees the direct assignment notice`, e `pnpm --filter web exec playwright test --list` os lista. `rg -n "test\.skip|test\.only|waitForTimeout|disableRules" apps/web/e2e/tests/unit-space-documents.spec.ts` é vazio.
- [ ] CA3.3 — Lendo o spec: `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo, passado em toda asserção após mudança de rota; `toBeFocused()` antes de cada tecla de ação; `expectNoSeriousA11yViolations(page)` nos dois casos; o primeiro assere o texto do vazio e o título do documento criado como primeiro item da lista ao voltar; o segundo assere o aviso de lotação direta e a ausência de "Novo documento" dentro do `main`.
- [ ] CA3.4 — `docs/design.md` tem a receita "Ação de página acima da lista" com `mt-6 flex justify-end`; `docs/architecture.md` tem o parágrafo "Entrega `unit-space-documents` (fatia 127)" citando `GET /spaces/{spaceId}/documents`, `reachOf`, `isUnitMember` ou "membro direto" e a regra 10, e a frase `Lotação não dá acesso a documento` não aparece mais no arquivo.
- [ ] CA3.5 — A fatia está utilizável de ponta a ponta: `pnpm exec vitest run --project api` (0) inclui `POST documents with a direct unit spaceId answers 201 in that space owned by the caller`, `a colleague renames the unit space document with 200` e `GET space documents answers 403 with the direct assignment message to an inherited member`; `pnpm exec vitest run --project web` (0) inclui `creating a document navigates to it` e `the unit space page shows the space documents list`; `pnpm test:e2e` (0) inclui os dois casos de `unit-space-documents.spec.ts`.

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
