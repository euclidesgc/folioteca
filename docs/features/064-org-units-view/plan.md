# PLAN 064 — org-units-view

Branch: `feature/064-org-units-view` (sai de `feature/007-trash`, que é a base real; toda comparação de "arquivo intocado" é contra ela, nunca contra `develop`).

Fonte: `docs/features/064-org-units-view/spec.md` (as siglas D1…D10 são as decisões técnicas da SPEC e R1…R10 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho que o próprio Vitest imprime sobre o ambiente jsdom é da ferramenta e **não conta**. O aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck final de cada fase roda com o cache do `tsc` limpo: `pnpm exec tsc -b --clean` e conferir que não sobrou nenhum `*.tsbuildinfo` fora de `node_modules` antes de `pnpm typecheck`.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`. A árvore é escrita à mão (o Radix não tem árvore). Nomes de API de biblioteca (Prisma, NestJS, TanStack Query, MSW, React Router, Playwright) vêm dos tipos do pacote instalado; os critérios medem comportamento e estrutura essencial.
- **Sem migration e sem mudança de schema**: o modelo `OrgUnit` e a unidade raiz criada na instalação já existem. Ninguém — agente, teste ou critério — roda os subcomandos `diff`, `reset` ou `dev` do `prisma migrate`. A limpeza entre testes da API é `resetDatabase(prisma)`.
- Todo componente devolve `React.JSX.Element` (nunca o `JSX` global); `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; navegação interna é `<Link>`/`<NavLink>`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case. Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Nenhum literal com cara de senha, token ou hash em arquivo versionado. Sessões de teste da API vêm de `createPersonWithSession` (`apps/api/test/create-person.ts`) ou dos ajudantes já existentes; valor inválido é `randomUUID()`.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado. Não contam arquivos só de tipos, arquivos com 0 de 0 linhas instrumentáveis, nem os já excluídos pelo `vitest.config.ts` da raiz (`**/types/**`, `**/generated/**`, `**/src/testing/**`, `**/test/**`, `**/*.config.*`). Nenhuma exclusão nova.
- **Toda** espera logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes unitários/integração da web pela constante `LAZY_TIMEOUT` declarada no arquivo (`findBy…(…, {}, LAZY_TIMEOUT)`, `waitFor(…, LAZY_TIMEOUT)`); no e2e pela constante `ROUTE_TIMEOUT` declarada no topo do spec. Nunca `setTimeout`/sleep fixo nem `waitForTimeout`.
- "É administração?" é decidido pelo **servidor**, lendo a pessoa da sessão no banco. Na web, o único lugar que lê `person.isAdmin` para decidir acesso é `apps/web/src/lib/authorization.tsx`.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa.
- Nenhuma tarefa acrescenta escopo: sem criar, renomear ou apagar unidade; sem seleção de nó, busca por digitação, tecla `*` ou slot de ações no `Tree`; sem tabela `POLICIES`; sem rota pai `admin`; sem trocar os botões vermelhos de erro antigos pela variante `destructive`.

Desvio em relação à tabela de arquivos da SPEC, sem mudar escopo: `docs/design.md` sai da fase 3 e entra na fase 2 (T2.3) — a skill `interface-design` manda a receita nova entrar na mesma entrega da tela que a usa.

## Fase 1 — API: `GET /org-units` devolve a árvore plana da organização, só para a administração

Caminhos relativos à raiz do repositório.

- [ ] T1.1 — Contrato primeiro: `GET /org-units` e os schemas `OrgUnit` e `OrgUnitsResponse`
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D2): tag nova `org-units`. Caminho `/org-units` com `get` (`operationId: getOrgUnits`), no mesmo prefixo e com a mesma segurança de sessão dos demais caminhos, sem parâmetros e sem paginação. Respostas: `200` com `OrgUnitsResponse`; `401` e `403` reaproveitando os componentes de resposta de erro que já existem (schema `Error`). Schemas: `OrgUnit` = objeto com `id` (string), `parentId` (string ou nulo, na mesma forma que o arquivo já usa para nulo) e `name` (string), **os três em `required`**, com descrição em pt_BR (`parentId`: "unidade pai; nulo na raiz da organização"); `OrgUnitsResponse` = `{ data: OrgUnit[] }`, `data` obrigatório. Descrição da operação em pt_BR: lista plana de todas as unidades da organização, ordenada por nome, só para a administração. Regenerar com `pnpm --filter @folioteca/api-contract generate`; o `.d.ts` nunca é editado à mão.
  - Skills: —
  - Complexidade: baixa

- [ ] T1.2 — `AdminGuard`: só a administração passa, decidido pela pessoa da sessão
  - Arquivos: `apps/api/src/auth/admin.guard.ts` (criar)
  - O que fazer (D1, R3): classe `AdminGuard`, `@Injectable()`, implementa `CanActivate`, **sem dependências no construtor**. `canActivate(context: ExecutionContext): boolean` lê `request.person` (tipo `RequestWithPerson`, que já existe em `auth/`): ausente → `throw new Error('AdminGuard exige o SessionGuard antes dele na rota.')` (erro de programação, no mesmo espírito do decorator `CurrentPerson`); `person.isAdmin !== true` → `throw new ForbiddenException('Apenas a administração pode fazer isso.')`; senão `true`. Nunca lê papel do corpo, da query ou de cabeçalho do pedido. Comentário curto (en_US): é usado **depois** do `SessionGuard` (`@UseGuards(SessionGuard, AdminGuard)`); `isAdmin` vem da pessoa que o `SessionService.findValid` relê do banco a cada requisição, então rebaixar alguém vale no pedido seguinte; ordem observável CSRF → sessão (401) → administração (403). `session.guard.ts`, `session.service.ts`, `current-person.decorator.ts` e `auth.module.ts` **não mudam** (o guard não precisa ser provedor do módulo: não injeta nada).
  - Skills: security
  - Complexidade: alta

- [ ] T1.3 — Módulo `org-units` só com a leitura, registrado no app
  - Arquivos: `apps/api/src/org-units/org-units.service.ts` (criar); `apps/api/src/org-units/org-units.controller.ts` (criar); `apps/api/src/org-units/org-units.module.ts` (criar); `apps/api/src/app.module.ts` (alterar)
  - O que fazer (D2, D3, R4):
    - `OrgUnitsService`, construtor com `PrismaService`: `list(organizationId: string): Promise<OrgUnit[]>` (tipo `OrgUnit` tirado do contrato gerado, do mesmo jeito que `documents.service.ts` tira os dele). Lê `orgUnit.findMany({ where: { organizationId }, select: { id: true, parentId: true, name: true } })` — **sem** `orderBy` — e ordena **em memória** com um `Intl.Collator('pt-BR', { sensitivity: 'base' })` por `name`, com desempate determinístico por `id`. Comentário curto: ordenar no banco dependeria da collation do Postgres de cada instância ("Área" iria depois de "Zeladoria" com `C`). O `select` já tem a forma do contrato; não há função de conversão.
    - `OrgUnitsController`: `@Controller('org-units')`, `@UseGuards(SessionGuard, AdminGuard)` **na classe**, nessa ordem; `@Get()` recebe a pessoa por `@CurrentPerson()` e devolve `{ data: await this.orgUnits.list(person.organizationId) }` (`Promise<OrgUnitsResponse>`). Nenhum parâmetro lido do pedido.
    - `OrgUnitsModule`: importa `AuthModule` (de onde vem o `SessionService` que o `SessionGuard` injeta — mesmo arranjo do `DocumentsModule`) e o que o `DocumentsModule` já importa para ter `PrismaService`; declara o controller e o serviço. `app.module.ts` registra `OrgUnitsModule`.
    - Conferir que `apps/api/src/access/__tests__/document-access-boundary.test.ts` continua verde **sem alteração** (o módulo novo só toca `OrgUnit`, nunca `Document` nem `Favorite`).
  - Skills: security
  - Complexidade: média

- [ ] T1.4 — Testes da fase 1
  - Arquivos: `apps/api/src/auth/__tests__/admin.guard.test.ts` (criar); `apps/api/src/org-units/__tests__/org-units.service.test.ts` (criar); `apps/api/src/org-units/__tests__/org-units.integration.test.ts` (criar); `apps/api/src/org-units/__tests__/org-units.contract.test.ts` (criar)
  - O que fazer: integração e contrato contra o Postgres real, sem mock do Prisma, `resetDatabase(prisma)` em `beforeEach`, instalação e sessão da administração pelos mesmos ajudantes dos testes de documentos já existentes; pessoa não-admin por `createPersonWithSession` com `isAdmin` omitido. `apps/api/test/create-person.ts` e `apps/api/test/reset-database.ts` **não mudam**. Nenhuma espera fixa.
    - `admin.guard.test.ts` (contexto de execução falso, sem Nest): `lets an admin person through`; `throws ForbiddenException with the message for a person who is not admin`; `throws a plain Error when the request has no person`.
    - `org-units.service.test.ts` (Prisma falso): `reads only the units of the given organization selecting id, parentId and name`; `orders by name with the pt-BR collator ignoring accents and case` (entrada "Zeladoria", "Área Técnica", "acervo" → saída "acervo", "Área Técnica", "Zeladoria"); `breaks ties between equal names by id`; `returns an empty list when the organization has no units`.
    - `org-units.integration.test.ts` (`GET /api/org-units`): `an admin right after installation gets 200 with the single root unit` (um item, `parentId: null`, `name` igual ao nome da organização); `returns every unit flat with its parentId in pt-BR collator order` (filhas e netas criadas **direto pelo Prisma** no teste, fora de ordem e com acento: "Zeladoria", "Área Técnica", "acervo"; confere quantidade, `parentId` de cada uma e a ordem); `a person who is not admin gets 403 with the message` (corpo com "Apenas a administração pode fazer isso."); `answers 401 without a session cookie`; `an admin demoted in the database gets 403 on the next request with the same session` (primeiro pedido 200; `person.update({ isAdmin: false })` direto no banco; segundo pedido, mesmo cookie, 403).
    - `org-units.contract.test.ts` (ajudantes de `apps/api/test/contract.ts`): `GET org-units answers the documented 200`; `GET org-units answers the documented 401`; `GET org-units answers the documented 403`.
  - Skills: unit-testing, integration-testing, security
  - Complexidade: média

### Critérios de aceite da fase 1

- [ ] CA1.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` passam **sem erro nem aviso** (sem aviso de peer no install; sem aviso de chunk grande no build; o aviso de desempenho do Vitest sobre jsdom não conta). A saída de `pnpm test` não tem `console.error` nem `console.warn` vazando, nem aviso do Nest ou do Prisma. `git diff feature/007-trash... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [ ] CA1.2 — `packages/api-contract/openapi.yaml` descreve `GET /org-units` (`operationId: getOrgUnits`, tag `org-units`), sem parâmetros, com `200` (`OrgUnitsResponse`), `401` e `403` (schema de erro já existente); `OrgUnit` tem `id` (string), `parentId` (string ou nulo) e `name` (string), os três obrigatórios; `OrgUnitsResponse` tem `data` (lista de `OrgUnit`) obrigatório. `packages/api-contract/src/generated/openapi.d.ts` reflete o YAML (o teste de tipos gerados do pacote passa).
- [ ] CA1.3 — `apps/api/src/auth/admin.guard.ts` exporta `AdminGuard`, `@Injectable()`, com `canActivate(context: ExecutionContext): boolean` e construtor sem dependências. Lendo o código: a decisão sai **só** de `request.person.isAdmin`; `isAdmin` diferente de `true` lança `ForbiddenException` com o texto "Apenas a administração pode fazer isso."; `request.person` ausente lança `Error` com o texto "AdminGuard exige o SessionGuard antes dele na rota."; nada é lido de corpo, query ou cabeçalho.
- [ ] CA1.4 — `apps/api/src/org-units/org-units.service.ts` tem `list(organizationId: string): Promise<OrgUnit[]>`: filtra por `organizationId`, seleciona só `id`, `parentId` e `name`, **não** usa `orderBy` na consulta e ordena em memória com `Intl.Collator` de `pt-BR` e `sensitivity: 'base'` por `name`, com desempate por `id`. `apps/api/src/org-units/org-units.controller.ts` é `@Controller('org-units')` com `SessionGuard` **e** `AdminGuard` na classe, nessa ordem, e um `@Get()` que devolve `{ data }` com a lista da organização **da pessoa da sessão** (nenhum parâmetro do pedido). `OrgUnitsModule` importa `AuthModule` e está registrado em `apps/api/src/app.module.ts`.
- [ ] CA1.5 — Intocados: `git diff --stat feature/007-trash... -- apps/api/prisma apps/api/test/reset-database.ts apps/api/test/create-person.ts apps/api/src/auth/session.guard.ts apps/api/src/auth/session.service.ts apps/api/src/auth/current-person.decorator.ts apps/api/src/auth/auth.module.ts apps/api/src/access apps/api/src/documents apps/api/src/installation apps/web` é vazio. `rg -n "isAdmin" apps/api/src/org-units --glob '!**/__tests__/**'` é vazio (a regra mora no guard, não no módulo).
- [ ] CA1.6 — Existem e passam, pelos nomes listados em T1.4: os 3 casos de `admin.guard.test.ts`, os 4 de `org-units.service.test.ts`, os 5 de `org-units.integration.test.ts` (entre eles `returns every unit flat with its parentId in pt-BR collator order`, `a person who is not admin gets 403 with the message`, `answers 401 without a session cookie` e `an admin demoted in the database gets 403 on the next request with the same session`) e os 3 de `org-units.contract.test.ts`. A integração usa o Postgres real (`rg -n "vi\.mock\(.*prisma" apps/api/src/org-units/__tests__/org-units.integration.test.ts apps/api/src/org-units/__tests__/org-units.contract.test.ts` é vazio). Os testes das fatias anteriores continuam passando. Conferir com `npx vitest run --project api --reporter=verbose`.
- [ ] CA1.7 — Nenhuma espera fixa nem credencial literal nos testes novos: `rg -n "setTimeout\(|sleep\(" apps/api/src/org-units/__tests__ apps/api/src/auth/__tests__/admin.guard.test.ts` é vazio; `rg -n "folioteca_session=[A-Za-z0-9]" apps/api/src/org-units/__tests__ apps/api/src/auth/__tests__/admin.guard.test.ts` é vazio.
- [ ] CA1.8 — O relatório de cobertura de `pnpm test` lista com ≥ 80% de linhas cada: `apps/api/src/auth/admin.guard.ts`, `apps/api/src/org-units/org-units.service.ts` e `apps/api/src/org-units/org-units.controller.ts`; arquivo ausente do relatório conta como reprovado (salvo 0 de 0 linhas, caso de `org-units.module.ts`). `git diff feature/007-trash... -- vitest.config.ts` é vazio.

## Fase 2 — Web: área "Administração" na barra lateral e página "Estrutura" com a árvore acessível, só para a administração

Caminhos relativos a `apps/web/`, salvo os que começam por `docs/` ou `eslint.config.js` (raiz).

- [ ] T2.1 — API simulada e tipos: unidades no banco em memória, `GET /org-units` com 401/403, raiz criada ao instalar e as chaves de desenvolvimento
  - Arquivos: `src/types/api.ts` (alterar); `src/testing/mocks/db.ts` (alterar); `src/testing/mocks/handlers/org-units.ts` (criar); `src/testing/mocks/handlers/index.ts` (alterar); `src/testing/mocks/handlers/installation.ts` (alterar); `src/testing/mocks/index.ts` (alterar); `src/testing/mocks/utils.ts` (alterar)
  - O que fazer (D8, D9, R3):
    - `types/api.ts`: `OrgUnit` e `OrgUnitsResponse`, tirados do contrato gerado do mesmo jeito que os tipos de documento já são.
    - `db.ts`: `export type MockOrgUnit = { id: string; parentId: string | null; name: string }`; `DbState` ganha `orgUnits: MockOrgUnit[]` (inicial `[]`). `seedInstalled` passa a `({ signedIn, isAdmin = true }: { signedIn: boolean; isAdmin?: boolean }): void`: grava `isAdmin` na pessoa e cria a raiz `{ id: 'org-unit-root', parentId: null, name: <nome da organização> }`. `export const seedSampleOrgUnits = (): void` — sem instalação não faz nada; acrescenta, sob a raiz existente, três níveis em pt_BR: "Acervo e Processamento Técnico" (filhas "Catalogação" e "Restauro e Conservação"), "Atendimento ao Público" (filhas "Empréstimos e Devoluções" e "Sala Infantil"), "Área Administrativa" (folha) e uma folha com nome de ~120 caracteres.
    - `handlers/installation.ts`: o `POST /installation` simulado passa a criar a raiz também (mesmo formato; espelha o servidor real).
    - `handlers/org-units.ts`: `export const orgUnitsHandlers`, com `http.get(`${env.API_URL}/org-units`)` no mesmo começo dos handlers existentes — `networkDelay()`, `devOverride('org-units')`, sem instalação ou sem cookie de sessão → 401 "Sessão não encontrada."; `!installation.person.isAdmin` → **403** "Apenas a administração pode fazer isso."; senão 200 com `{ data }` tipado como `OrgUnitsResponse`, ordenado por `Intl.Collator('pt-BR', { sensitivity: 'base' })` por `name`, desempate por `id`. Registrar em `handlers/index.ts`.
    - `mocks/index.ts`: chave `mock-role` — valor `member` faz a instalação semeada nascer com `isAdmin: false` (lida **antes** das chamadas de `seedInstalled`, implementada pelo parâmetro); chave `mock-org-units` — valor `sample` chama `seedSampleOrgUnits()` depois da instalação. `utils.ts`: documentar `mock-role`, `mock-org-units` e o valor `org-units` de `mock-error` no mesmo bloco de comentário das outras chaves.
    - `handlers/auth.ts` e `handlers/documents.ts` **não mudam**.
  - Skills: api-mocking, api-requests
  - Complexidade: média

- [ ] T2.2 — `lib/authorization.tsx`: papel derivado de `person.isAdmin`, hook e componente `Authorization`
  - Arquivos: `src/lib/authorization.tsx` (criar)
  - O que fazer (D4, R1, R2), pelo modelo da skill `authorization`:
    - `export const ROLES = { ADMIN: 'ADMIN', MEMBER: 'MEMBER' } as const`; `export type Role = keyof typeof ROLES` (ou equivalente que dê `'ADMIN' | 'MEMBER'`); `export const getRole = (user: CurrentUser): Role` → `user.person.isAdmin ? ROLES.ADMIN : ROLES.MEMBER` (função pura; `CurrentUser` é o tipo que `GET /auth/me` já devolve — o contrato **não** muda).
    - `export const useAuthorization = ()` → usa o usuário de `@/lib/auth`; sem usuário (fora de `ProtectedRoute`) **lança** `Error`; devolve `{ checkAccess, role, user }`, com `checkAccess({ allowedRoles }: { allowedRoles: Role[] }): boolean`.
    - `export const Authorization` — props `children`, `forbiddenFallback?: React.ReactNode` (padrão `null`) e **ou** `allowedRoles: Role[]` **ou** `policyCheck: boolean` (união discriminada, nunca os dois); devolve `React.JSX.Element`; mostra `children` quando pode, senão o fallback.
    - **Não** criar a tabela `POLICIES` (não há política de recurso nesta fatia); comentário curto dizendo isso. `src/lib/auth.tsx` e `components/layouts/sidebar-identity.tsx` não mudam.
  - Skills: authorization, authentication
  - Complexidade: média

- [ ] T2.3 — `Tree` compartilhado e acessível (padrão "Tree View" do WAI-ARIA APG), sem biblioteca, e a receita "Árvore"
  - Arquivos: `src/components/ui/tree/tree.tsx` (criar); `docs/design.md` (alterar)
  - O que fazer (D7, R5, R6):
    - Exporta `Tree` e `export type TreeNode = { id: string; label: string; children: TreeNode[] }`. Props: `nodes: TreeNode[]`; `expandedIds: ReadonlySet<string>`; `onExpandedChange: (next: ReadonlySet<string>) => void`; `'aria-label': string` (obrigatório); `className?: string`. **Controlado** na expansão; o nó que tem `tabIndex=0` (foco itinerante) é estado **interno**. Sem domínio, sem API, sem store, sem Context.
    - Marcação: lista com `role="tree"` e o `aria-label` recebido → itens com `role="treeitem"`, `aria-level` (1 na raiz), `aria-setsize`, `aria-posinset` e `tabIndex` (`0` em exatamente um, `-1` nos demais). Nó **com filhas** leva `aria-expanded` e, quando expandido, uma lista `role="group"` com as filhas; **folha não leva `aria-expanded`**. O foco fica no item; a linha visível é um bloco interno com seta + rótulo. Seta em `<svg>` inline `aria-hidden="true"` `focusable="false"`, girada quando expandido; folha leva um espaçador da mesma largura. Rótulo truncável com `title` igual ao rótulo (nome longo em nível fundo não cria rolagem horizontal a 360px). Sem `aria-selected`.
    - Teclado, sempre sobre os nós **visíveis** em ordem de leitura (tirados de uma função pura sobre `nodes` + `expandedIds`): `ArrowDown`/`ArrowUp` próximo/anterior, parando nas pontas; `ArrowRight` em nó fechado expande, em nó aberto vai à primeira filha, em folha nada; `ArrowLeft` em nó aberto recolhe, em nó fechado ou folha vai ao pai, na raiz fechada nada; `Home`/`End` primeiro/último visível; `Enter` e `Espaço` alternam a expansão (em folha, nada). Toda tecla **tratada** chama `preventDefault()` (a página não rola); tecla não tratada passa. Clique na linha foca o nó e alterna a expansão (em folha, só foca).
    - Robustez: se o nó com `tabIndex=0` deixar de estar visível (pai recolhido por props, `nodes` recarregado), o `tabIndex=0` cai no ancestral visível mais próximo ou, na falta, no primeiro nó — a árvore nunca fica sem parada de `Tab`. `nodes` vazio renderiza a lista vazia sem erro.
    - Sem `style={{…}}`: o recuo vem do aninhamento do grupo (margem e borda à esquerda), não de `padding-left` calculado por nível. Foco visível na **linha** quando o item está em `focus-visible`; altura mínima da linha ≥ 40px.
    - `docs/design.md`, tabela "Padrões acrescentados pelas entregas", Fatia = 064, padrão "Árvore": `<ul role="tree" className="mt-6">`; grupo de filhas `ml-5 border-l border-gray-200 pl-2`; nó `<li>` com `outline-none`; linha do nó `flex min-h-10 items-center gap-2 rounded-md px-2 text-sm text-gray-900 hover:bg-gray-100 cursor-pointer` e, com o `<li>` em foco visível, contorno `outline-2 outline-offset-2 outline-blue-600` na linha; seta `<svg aria-hidden="true" focusable="false" className="size-4 shrink-0 text-gray-600">`, com `rotate-90` quando expandido; espaçador de folha `size-4 shrink-0`; rótulo `min-w-0 truncate`.
  - Skills: ui-components, interface-design, component-robustness
  - Complexidade: alta

- [ ] T2.4 — Feature `org-units`: chamada de API, montagem da árvore e o componente com os quatro estados
  - Arquivos: `eslint.config.js` (alterar, raiz); `src/features/org-units/api/get-org-units.ts` (criar); `src/features/org-units/utils/build-tree.ts` (criar); `src/features/org-units/components/org-units-tree.tsx` (criar)
  - O que fazer (D8, R4, R5, R7, R8, R9):
    - `eslint.config.js`: zona nova para `./apps/web/src/features/org-units` em `import/no-restricted-paths`, igual às zonas das outras features (não importa de outra feature nem de `app/`).
    - `get-org-units.ts`, no mesmo formato de `features/documents/api/get-documents.ts`: `getOrgUnits(): Promise<OrgUnitsResponse>` (`GET /org-units`), `getOrgUnitsQueryOptions()` com `queryKey: ['org-units']`, `useOrgUnits({ queryConfig }: { queryConfig?: QueryConfig<typeof getOrgUnitsQueryOptions> } = {})`. Sem `try/catch` e sem notificação própria: a falha é avisada pelo interceptor que já existe.
    - `build-tree.ts`: `buildTree(units: OrgUnit[]): TreeNode[]` — pura, O(n) com `Map`, **preserva a ordem recebida** entre irmãs (a web não reordena), `label` = `name`; unidade cujo `parentId` não está na lista vira raiz (nada some em silêncio); lista vazia → `[]`. `collectExpandableIds(nodes: TreeNode[]): Set<string>` — o id de todo nó com filhas, em qualquer nível.
    - `OrgUnitsTree(): React.JSX.Element`, os quatro estados **no mesmo lugar**:
      - carregando (`role="status"`, receita "Carregando"): "Carregando estrutura…" — também quando `isError && isFetching` (nova tentativa; mesmo tratamento de `features/documents/components/sidebar-documents.tsx`);
      - erro (`role="alert"`, receita "Erro", com borda e fundo de destaque): "Não foi possível carregar a estrutura." + `Button variant="destructive"` `type="button"` "Tentar novamente", que chama `refetch`. Um 403 com a aba aberta cai neste mesmo estado;
      - com dados: `Tree` com `aria-label` "Estrutura de unidades", `buildTree` em `useMemo`, `expandedIds` em `useState` iniciado **uma vez** com `collectExpandableIds` (tudo expandido), dentro de um componente interno que só monta com dados — **sem `useEffect`** para sincronizar;
      - só a raiz (lista com exatamente um item **ou** vazia): a árvore com o único nó, se houver (sem seta), e **abaixo** dela, em receita "Vazio" (tom suave): "Por enquanto só existe a raiz. As unidades filhas serão criadas aqui, abaixo dela.".
  - Skills: api-requests, interface-design, error-handling, client-state, project-structure
  - Complexidade: média

- [ ] T2.5 — Área "Administração" na barra lateral e a rota `/admin/structure`
  - Arquivos: `src/config/paths.ts` (alterar); `src/components/layouts/sidebar-admin.tsx` (criar); `src/app/routes/app/admin/structure.tsx` (criar); `src/app/router.tsx` (alterar); `src/app/routes/app/root.tsx` (alterar)
  - O que fazer (D5, D6, R1, R2, R10):
    - `paths.ts`: `admin: { structure: { path: '/admin/structure', getHref: () => '/admin/structure' } }`, no formato das entradas existentes.
    - `SidebarAdmin(): React.JSX.Element` — `<Authorization allowedRoles={[ROLES.ADMIN]}>` **sem** `forbiddenFallback` (quem não é admin não tem a seção no DOM) envolvendo `<nav aria-label="Administração">` na receita "Seção da barra lateral": cabeçalho `<h2>` "Administração" e lista com um `NavLink` "Estrutura" para `paths.admin.structure.getHref()`, receita "Item da barra lateral", ativo por `isActive`. Mora em `components/layouts/` porque quem usa é o `app` e a área vai listar páginas de várias features; não importa de `features/`.
    - `root.tsx`: `<SidebarAdmin />` entra no fragmento de `sidebarSection`, **depois** de `<SidebarDocuments scope="favorites" />` e `<SidebarDocuments />`. `components/layouts/app-layout.tsx` **não muda**.
    - `structure.tsx`: `export function Component(): React.JSX.Element`, só composição — `<Authorization allowedRoles={[ROLES.ADMIN]} forbiddenFallback={<Navigate to={paths.home.getHref()} replace />}>` envolvendo `ContentLayout` com título "Estrutura" e texto de apoio "As unidades da organização, da raiz até as equipes.", e `<OrgUnitsTree />` **dentro** do `Authorization` (para não-admin a requisição nem é disparada). Sem mensagem de "sem permissão": o não-admin vai para o início. Sem `clientLoader`. Um único `<h1>` (o do `ContentLayout`).
    - `router.tsx`: rota filha de `Root`, `path: paths.admin.structure.path.slice(1)`, com `lazy: () => import(...)` como as rotas existentes, registrada **antes** de `'*'`. A sessão continua protegida pelo `ProtectedRoute` do pai.
  - Skills: routing, authorization, interface-design, performance, project-structure
  - Complexidade: média

- [ ] T2.6 — Testes da fase 2
  - Arquivos: `src/lib/__tests__/authorization.test.tsx` (criar); `src/components/ui/tree/__tests__/tree.test.tsx` (criar); `src/components/layouts/__tests__/sidebar-admin.test.tsx` (criar); `src/features/org-units/utils/__tests__/build-tree.test.ts` (criar); `src/features/org-units/api/__tests__/get-org-units.test.tsx` (criar); `src/features/org-units/components/__tests__/org-units-tree.test.tsx` (criar); `src/app/routes/app/admin/__tests__/structure.test.tsx` (criar); `src/app/__tests__/router.test.tsx` (alterar); testes existentes que afirmem o estado inteiro do banco falso depois de instalar (alterar, só se quebrarem: passam a esperar a raiz em `orgUnits`)
  - O que fazer: banco falso semeado por `seedInstalled({ signedIn: true })` (admin) ou `seedInstalled({ signedIn: true, isAdmin: false })` (não-admin) e `seedSampleOrgUnits()`; interação por `userEvent`; localizar por papel e nome acessível em pt_BR. Todo arquivo que renderiza rota declara `const LAZY_TIMEOUT = { timeout: 5000 }` e passa-o em **toda** espera logo após mudança de rota ou chunk `lazy`. Nenhuma espera fixa. Nenhum `console.error`/`console.warn` vazando (erro esperado é silenciado e conferido dentro do caso).
    - `authorization.test.tsx`: `getRole returns ADMIN for an admin person`; `getRole returns MEMBER for a person who is not admin`; `checkAccess is true only when the role is allowed`; `Authorization renders the children for an allowed role`; `Authorization renders the forbidden fallback for a role that is not allowed`; `Authorization renders nothing without a fallback`; `Authorization follows policyCheck when given`; `useAuthorization throws without a signed-in user`.
    - `tree.test.tsx` (árvore de três níveis montada no arquivo; invólucro de teste que guarda `expandedIds` em estado): `names the tree after aria-label`; `sets treeitem level, setsize and posinset across three levels`; `a leaf has no aria-expanded and a parent has it`; `renders the children group only when expanded`; `keeps a single tabIndex 0`; `ArrowDown moves to the next visible node and stops at the last`; `ArrowUp moves to the previous visible node and stops at the first`; `ArrowDown skips the children of a collapsed node`; `ArrowRight expands a closed node`; `ArrowRight on an open node moves to its first child`; `ArrowRight on a leaf does nothing`; `ArrowLeft collapses an open node`; `ArrowLeft on a child moves to its parent`; `ArrowLeft on a closed root does nothing`; `Home moves to the first visible node`; `End moves to the last visible node`; `Enter toggles expansion`; `Space toggles expansion`; `Enter on a leaf does nothing`; `click focuses the node and toggles expansion`; `falls back to the nearest visible ancestor when the parent is collapsed by props`; `prevents default on handled keys and not on others`; `shows the full label in the title`; `renders an empty tree without nodes`.
    - `sidebar-admin.test.tsx`: `an admin sees the Administração navigation with the Estrutura link` (link com `href` `/admin/structure`); `a person who is not admin does not get the navigation in the DOM`; `marks the Estrutura link as current on its route`.
    - `build-tree.test.ts`: `returns a single root for a single unit`; `nests three levels under the root`; `keeps the received order among siblings`; `turns a unit with an unknown parent into a root`; `returns an empty list for no units`; `collectExpandableIds returns every node with children and no leaf`.
    - `get-org-units.test.tsx`: `getOrgUnits returns the data envelope`; `uses the org-units query key`; `useOrgUnits resolves with the seeded units`; `rejects with 403 for a person who is not admin` (handler padrão).
    - `org-units-tree.test.tsx`: `shows the loading status` (`delay('infinite')`); `shows the root alone with the explanation below it` (nó sem `aria-expanded` + o texto "Por enquanto só existe a raiz. As unidades filhas serão criadas aqui, abaixo dela."); `shows the explanation for an empty list`; `shows the error alert and recovers with Tentar novamente` (500 com `{ once: true }`; depois do clique a árvore aparece); `renders three levels fully expanded in the received order`; `collapses and expands a node by click`; `collapses and expands a node from the keyboard`; `keeps the full 120-character name in the title`.
    - `structure.test.tsx` (com `renderApp`): `an admin opens /admin/structure and sees the heading and the tree` (`h1` "Estrutura", texto de apoio e a árvore "Estrutura de unidades"); `an admin reaches the page from the Administração link in the sidebar`; `a person who is not admin lands on home and no request to org-units is made` (handler espião via `server.use` contando chamadas = 0; sem `h1` "Estrutura"; o conteúdo do início visível); `a person who is not admin does not see Administração in the sidebar`; `the fetcher called directly by a person who is not admin gets 403 from the default handler`.
    - `router.test.tsx`: acrescentar `registers the admin structure route before the catch-all` (ou o caso equivalente no formato que o arquivo já usa para enumerar rotas).
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: alta

### Critérios de aceite da fase 2

- [ ] CA2.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` passam **sem erro nem aviso** (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta e não pode aparecer). A saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)` vazando. `git diff feature/007-trash... -- package.json apps/web/package.json pnpm-lock.yaml` é vazio (nenhuma dependência nova).
- [ ] CA2.2 — `apps/web/src/lib/authorization.tsx` exporta `ROLES` (`ADMIN` e `MEMBER`), o tipo `Role`, `getRole(user: CurrentUser): Role` (admin quando `user.person.isAdmin`), `useAuthorization()` (devolve `{ checkAccess, role, user }` e lança sem usuário) e `Authorization` (aceita `allowedRoles` **ou** `policyCheck`, mais `forbiddenFallback` opcional, e devolve `React.JSX.Element`). Não há tabela `POLICIES`. É o único código de produção da web que lê `isAdmin` para decidir acesso: `rg -n "isAdmin" apps/web/src --glob '!**/__tests__/**' --glob '!**/testing/**'` só acha `apps/web/src/lib/authorization.tsx` e ocorrências que já existiam em `feature/007-trash` (tipos e exibição de identidade).
- [ ] CA2.3 — `apps/web/src/components/ui/tree/tree.tsx` exporta `Tree` e `TreeNode = { id: string; label: string; children: TreeNode[] }`; props `nodes`, `expandedIds: ReadonlySet<string>`, `onExpandedChange: (next: ReadonlySet<string>) => void`, `aria-label` obrigatório e `className`. Lendo o código: a expansão é controlada (nenhum estado interno de expansão); papéis `tree`, `treeitem` e `group`; `aria-level`, `aria-setsize` e `aria-posinset` em todo item; `aria-expanded` só em nó com filhas; um único `tabIndex=0`; trata `ArrowDown`, `ArrowUp`, `ArrowRight`, `ArrowLeft`, `Home`, `End`, `Enter` e espaço com `preventDefault()`; a seta é `<svg>` com `aria-hidden="true"`; o rótulo tem `title` e trunca. O arquivo não importa de `@/features` nem de `@/app`, não usa `style={{`, `forwardRef` nem o `JSX` global: `rg -n "style=\{\{|forwardRef|: JSX\.|@/features|@/app" apps/web/src/components/ui/tree/tree.tsx` é vazio.
- [ ] CA2.4 — `apps/web/src/features/org-units/api/get-org-units.ts` tem `getOrgUnits(): Promise<OrgUnitsResponse>`, `getOrgUnitsQueryOptions()` com chave `['org-units']` e `useOrgUnits({ queryConfig })`. `apps/web/src/features/org-units/utils/build-tree.ts` tem `buildTree(units: OrgUnit[]): TreeNode[]` (pura, não ordena, órfã vira raiz) e `collectExpandableIds(nodes: TreeNode[]): Set<string>`. `apps/web/src/types/api.ts` exporta `OrgUnit` e `OrgUnitsResponse` a partir do contrato. `eslint.config.js` tem a zona de `apps/web/src/features/org-units` em `import/no-restricted-paths`, e `rg -n "@/features/(?!org-units)|@/app" -P apps/web/src/features/org-units --glob '!**/__tests__/**'` é vazio.
- [ ] CA2.5 — `apps/web/src/features/org-units/components/org-units-tree.tsx` (`OrgUnitsTree`) mostra, no mesmo lugar: carregando com `role="status"` e "Carregando estrutura…" (também durante nova tentativa depois de erro); erro com `role="alert"`, borda ou fundo de destaque, "Não foi possível carregar a estrutura." e um `Button` `variant="destructive"` `type="button"` "Tentar novamente" que refaz a consulta; com dados, o `Tree` com `aria-label` "Estrutura de unidades", começando todo expandido; com um item ou nenhum, o texto "Por enquanto só existe a raiz. As unidades filhas serão criadas aqui, abaixo dela." em tom suave, **abaixo** da árvore. O estado de expansão é `useState` local iniciado uma vez, sem `useEffect` de sincronização. Não há `<button` cru: `rg -n "<button" apps/web/src/features/org-units apps/web/src/components/ui/tree apps/web/src/components/layouts/sidebar-admin.tsx apps/web/src/app/routes/app/admin --glob '!**/__tests__/**'` é vazio.
- [ ] CA2.6 — `apps/web/src/config/paths.ts` tem `paths.admin.structure` com `path` `/admin/structure` e `getHref()`. `apps/web/src/components/layouts/sidebar-admin.tsx` (`SidebarAdmin`) renderiza, dentro de `Authorization` com `allowedRoles` só de `ROLES.ADMIN` e **sem** fallback, um `<nav aria-label="Administração">` com cabeçalho "Administração" e um `NavLink` "Estrutura" para `paths.admin.structure.getHref()`, com as classes das receitas "Seção da barra lateral" e "Item da barra lateral" do `docs/design.md` (as mesmas de `sidebar-documents.tsx`), e não importa de `@/features`. Em `apps/web/src/app/routes/app/root.tsx`, `<SidebarAdmin />` vem depois dos dois `SidebarDocuments` no `sidebarSection`.
- [ ] CA2.7 — `apps/web/src/app/routes/app/admin/structure.tsx` exporta `Component`, que só compõe: `Authorization` com `allowedRoles` só de `ROLES.ADMIN` e `forbiddenFallback` igual a `<Navigate>` para `paths.home.getHref()` com `replace`, envolvendo `ContentLayout` (título "Estrutura", apoio "As unidades da organização, da raiz até as equipes.") e `<OrgUnitsTree />` **dentro** do `Authorization`. Há um único `<h1>` na página (o do `ContentLayout`), nenhum `<a href` interno e nenhum `style={{`. `apps/web/src/app/router.tsx` registra a rota como filha de `Root`, carregada por `lazy`, antes da rota `'*'`.
- [ ] CA2.8 — API simulada: `apps/web/src/testing/mocks/db.ts` tem `MockOrgUnit`, `orgUnits` no estado, `seedInstalled({ signedIn, isAdmin })` (padrão admin) criando a raiz com o nome da organização, e `seedSampleOrgUnits()` com três níveis em pt_BR, nomes acentuados e um nome de ~120 caracteres; `handlers/org-units.ts` responde 401 "Sessão não encontrada." sem sessão, 403 "Apenas a administração pode fazer isso." para não-admin e 200 com a lista ordenada pelo colador pt-BR, e está registrado em `handlers/index.ts`; `handlers/installation.ts` cria a raiz ao instalar; `mocks/index.ts` lê `mock-role` (valor `member`) antes de semear a instalação e `mock-org-units` (valor `sample`) depois; as duas chaves estão documentadas no comentário de `mocks/utils.ts`.
- [ ] CA2.9 — `docs/design.md` tem, em "Padrões acrescentados pelas entregas", a linha da fatia 064 com o padrão "Árvore" (lista `role="tree"`, grupo de filhas com margem e borda à esquerda, linha do nó com altura mínima, `hover` e contorno de foco, seta `aria-hidden` que gira, espaçador de folha, rótulo truncado), e `tree.tsx` usa essas classes.
- [ ] CA2.10 — Intocados: `git diff --stat feature/007-trash... -- apps/web/src/components/layouts/app-layout.tsx apps/web/src/components/layouts/sidebar-identity.tsx apps/web/src/components/layouts/content-layout.tsx apps/web/src/lib/auth.tsx apps/web/src/components/ui/button/button.tsx apps/web/src/testing/mocks/handlers/auth.ts apps/web/src/testing/mocks/handlers/documents.ts apps/web/src/features/documents apps/web/package.json` só mostra, no máximo, arquivos dentro de `__tests__/`.
- [ ] CA2.11 — Existem e passam, pelos nomes listados em T2.6: os 8 casos de `authorization.test.tsx`; os 24 de `tree.test.tsx` (um por tecla e por situação, entre eles `ArrowDown skips the children of a collapsed node`, `ArrowLeft on a closed root does nothing`, `falls back to the nearest visible ancestor when the parent is collapsed by props` e `prevents default on handled keys and not on others`); os 3 de `sidebar-admin.test.tsx`; os 6 de `build-tree.test.ts`; os 4 de `get-org-units.test.tsx`; os 8 de `org-units-tree.test.tsx` (entre eles `shows the error alert and recovers with Tentar novamente` e `shows the root alone with the explanation below it`); os 5 de `structure.test.tsx` (entre eles `a person who is not admin lands on home and no request to org-units is made`); e o caso novo de `router.test.tsx`. Os testes das fatias anteriores continuam passando, sem nenhum removido nem pulado. Conferir com `npx vitest run --project web --reporter=verbose`.
- [ ] CA2.12 — Esperas: `structure.test.tsx` declara `LAZY_TIMEOUT` e o passa em toda espera logo após abrir ou trocar de rota (`findBy…` e `waitFor`); `rg -n "setTimeout\(|sleep\(" apps/web/src/app/routes/app/admin/__tests__ apps/web/src/features/org-units apps/web/src/components/ui/tree apps/web/src/lib/__tests__/authorization.test.tsx apps/web/src/components/layouts/__tests__/sidebar-admin.test.tsx` é vazio.
- [ ] CA2.13 — O relatório de cobertura de `pnpm test` lista com ≥ 80% de linhas cada: `apps/web/src/lib/authorization.tsx`, `apps/web/src/components/ui/tree/tree.tsx`, `apps/web/src/components/layouts/sidebar-admin.tsx`, `apps/web/src/features/org-units/api/get-org-units.ts`, `apps/web/src/features/org-units/utils/build-tree.ts`, `apps/web/src/features/org-units/components/org-units-tree.tsx`, `apps/web/src/app/routes/app/admin/structure.tsx`, `apps/web/src/app/routes/app/root.tsx`, `apps/web/src/app/router.tsx` e `apps/web/src/config/paths.ts`; arquivo ausente do relatório conta como reprovado (salvo 0 de 0 linhas). `git diff feature/007-trash... -- vitest.config.ts` é vazio.

## Fase 3 — e2e da jornada pelo teclado e documentação: a fatia utilizável de ponta a ponta

Caminhos relativos à raiz do repositório.

- [ ] T3.1 — Documentação: a entrega `org-units-view` na arquitetura
  - Arquivos: `docs/architecture.md` (alterar)
  - O que fazer, em pt_BR, sem reescrever o resto:
    - §4 (árvore de unidades), parágrafo novo "Entrega `org-units-view` (fatia 064)": `GET /org-units` devolve a **lista plana** inteira da organização, com `parentId` (é a forma do banco, e as próximas entregas devolvem uma unidade solta que a web encaixa no cache); ordenação **em memória** com colador `pt-BR` insensível a acento e caixa, desempate por `id` (não depende da collation do Postgres); a web monta a árvore sem reordenar; só administração nesta entrega; sem paginação (serve para centenas de unidades).
    - §6 (autenticação): `AdminGuard` sempre **depois** do `SessionGuard`; ordem observável CSRF → 401 (sem sessão) → 403 "Apenas a administração pode fazer isso."; `isAdmin` é relido do banco a cada pedido (rebaixar vale no pedido seguinte, sem mexer em sessão); `isAdmin` **não** dá acesso a documento; na web o papel é **derivado** de `person.isAdmin` em `apps/web/src/lib/authorization.tsx` (único lugar), sem campo novo no contrato; não-admin não vê a área "Administração" e, pelo endereço, é levado ao início sem disparar a requisição.
    - O status da fatia no roadmap e o registro das dívidas **não** são desta tarefa (são feitos pela orquestração no encerramento).
  - Skills: —
  - Complexidade: baixa

- [ ] T3.2 — Testes da fase 3 (e2e: administração percorre a árvore pelo teclado; não-admin não entra)
  - Arquivos: `apps/web/e2e/tests/org-units-view.spec.ts` (criar)
  - O que fazer: contra a API simulada; localizar por papel e nome acessível em pt_BR. Estado inicial por `page.addInitScript` gravando chaves no `localStorage`. No topo do arquivo, como em `trash.spec.ts`: `const ROUTE_TIMEOUT = { timeout: 10_000 }`; **toda** espera logo após mudança de rota (`toHaveURL`, primeira visibilidade da tela nova) passa `ROUTE_TIMEOUT`; nenhum `waitForTimeout`. `apps/web/e2e/a11y.ts` e os outros specs não são tocados; nenhuma regra do axe é desativada.
    - Caso `an admin reaches Estrutura and walks the tree from the keyboard` — chaves `mock-installation` = `signed-in` e `mock-org-units` = `sample`:
      1. abre `/`; na navegação "Administração", leva o foco ao link "Estrutura" (`focus()`) e aciona com `Enter` (sem `.click()`); espera a URL `/admin/structure` e o `h1` "Estrutura";
      2. a árvore "Estrutura de unidades" está visível, com a raiz `aria-expanded="true"` e as unidades de exemplo visíveis; `Tab` até a árvore: o foco está no nó raiz (`toBeFocused`);
      3. `ArrowDown` → primeira filha em foco; `ArrowLeft` nela (aberta) → `aria-expanded="false"` e as netas somem; `ArrowRight` → `aria-expanded="true"` de novo; `ArrowRight` outra vez → foco na primeira neta; `ArrowLeft` na neta → foco volta à mãe; `End` → último nó visível em foco; `Home` → raiz em foco. A cada passo confere `toBeFocused()` e, onde couber, `aria-expanded`;
      4. roda `expectNoSeriousA11yViolations(page)` com a árvore aberta;
      5. com a raiz em foco, `ArrowLeft` (ou `Enter`) → raiz `aria-expanded="false"` e nenhuma filha visível; o foco continua na raiz.
    - Caso `a person who is not admin has no Administração area and is sent home from the address` — chaves `mock-installation` = `signed-in` e `mock-role` = `member`: abre `/`; a navegação "Administração" **não existe** (`toHaveCount(0)`); `page.goto('/admin/structure')` (único `goto` direto a rota interna do arquivo: o que se prova é o acesso por endereço) termina com a URL `/` (`toHaveURL` com `ROUTE_TIMEOUT`), sem `h1` "Estrutura"; roda `expectNoSeriousA11yViolations(page)` no início.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [ ] CA3.1 — `pnpm test:e2e` na raiz passa (o Playwright sobe o próprio servidor; não precisa de API nem de Postgres), e continuam passando **sem erro nem aviso**, com `docker compose up -d` rodando e o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta e não pode aparecer).
- [ ] CA3.2 — `apps/web/e2e/tests/org-units-view.spec.ts` contém os casos `an admin reaches Estrutura and walks the tree from the keyboard` e `a person who is not admin has no Administração area and is sent home from the address`, e `pnpm --filter web exec playwright test --list` os lista junto com os casos já existentes dos outros specs (nenhum removido nem renomeado). Não há `test.skip`, `test.only` nem `waitForTimeout` em `apps/web/e2e`.
- [ ] CA3.3 — Lendo o primeiro caso: o link "Estrutura" da navegação "Administração" é acionado por teclado (`focus()` + `Enter`, sem `.click()`); a árvore é alcançada por `Tab` e percorrida **só** por teclas (`ArrowDown`, `ArrowRight`, `ArrowLeft`, `Home`, `End`), sem `.click()` em nó; há asserções `toBeFocused()` depois das teclas e de `aria-expanded` ao recolher e ao expandir; confere que as filhas somem quando a raiz é recolhida; chama `expectNoSeriousA11yViolations` com a árvore aberta. Lendo o segundo caso: confere que a navegação "Administração" não existe, que abrir `/admin/structure` termina em `/` sem o `h1` "Estrutura", e chama `expectNoSeriousA11yViolations`.
- [ ] CA3.4 — Esperas do e2e: o arquivo declara `ROUTE_TIMEOUT` no topo e **toda** asserção logo após mudança de rota (`toHaveURL` e a primeira visibilidade da tela nova) o recebe; não há espera fixa.
- [ ] CA3.5 — Acessibilidade sem atalho novo: `git diff --stat feature/007-trash... -- apps/web/e2e/a11y.ts apps/web/e2e/mock-credentials.ts apps/web/e2e/tests/install.spec.ts apps/web/e2e/tests/login-logout.spec.ts apps/web/e2e/tests/open-app.spec.ts apps/web/e2e/tests/create-document.spec.ts apps/web/e2e/tests/block-editor.spec.ts apps/web/e2e/tests/favorites.spec.ts apps/web/e2e/tests/trash.spec.ts` é vazio; `org-units-view.spec.ts` não desativa nenhuma regra do axe. Os arquivos de `apps/web/e2e/**` passam no `pnpm lint` e no `pnpm typecheck`. Nenhuma senha, token ou hash literal em `apps/web/e2e/tests/org-units-view.spec.ts`.
- [ ] CA3.6 — `docs/architecture.md`: a §4 tem o parágrafo "Entrega `org-units-view` (fatia 064)" dizendo: lista plana com `parentId`; ordenação em memória com colador pt-BR e desempate por `id`; a web não reordena; só administração; sem paginação. A §6 diz: `AdminGuard` depois do `SessionGuard`; 401 antes de 403 e a mensagem "Apenas a administração pode fazer isso."; `isAdmin` relido do banco a cada pedido; `isAdmin` não dá acesso a documento; papel na web derivado de `person.isAdmin` em `lib/authorization.tsx`; não-admin não vê a área e é levado ao início pelo endereço.
- [ ] CA3.7 — A fatia está utilizável de ponta a ponta sem navegador aberto por pessoa: existem e passam `apps/api/src/org-units/__tests__/org-units.integration.test.ts` (`npx vitest run --project api`), `apps/web/src/app/routes/app/admin/__tests__/structure.test.tsx` com o caso `an admin opens /admin/structure and sees the heading and the tree` (`npx vitest run --project web`) e as duas jornadas de `org-units-view.spec.ts` (`pnpm test:e2e`).

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
