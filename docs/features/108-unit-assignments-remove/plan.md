# PLAN 108 — unit-assignments-remove

Branch: `feature/108-unit-assignments-remove`, que **sai de `feature/010-unit-assignments`** (entrega empilhada). **Toda** comparação de diff e de "arquivo intocado" deste plano é contra `feature/010-unit-assignments` — nunca contra `develop`, nunca contra `main`. Onde um critério diz `<base>`, leia `feature/010-unit-assignments`.

Fonte: `docs/features/108-unit-assignments-remove/spec.md` (D1…D7 são as decisões técnicas da SPEC e R1…R14 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; o banco de teste é `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom é da ferramenta e **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **Nenhuma migration nova é esperada** (D3): `apps/api/prisma/**` fica intocado, `schema.prisma` inclusive. Se, na implementação, alguém concluir que precisa de migration, o agente **para e relata o achado ao dono**, sem escrever SQL: significaria que o desenho da fatia 010 não é o que `docs/architecture.md` §4 registra. Os únicos subcomandos de Prisma permitidos são `pnpm --filter api exec prisma validate`, `pnpm --filter api exec prisma migrate status` e `pnpm --filter api exec prisma migrate deploy`, sempre contra `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test`; **nunca** `migrate diff`, `migrate dev` ou `migrate reset`.
- Todo componente devolve `React.JSX.Element` (nunca o `JSX` global); `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case. Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Nenhum `eslint-disable` sem `-- <motivo>`. Nenhum segredo em variável `VITE_*`; nenhum token em `localStorage`; nenhum literal com cara de senha, token ou hash em arquivo versionado.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado. Não contam arquivos só de tipos, arquivos com 0 de 0 linhas instrumentáveis, nem os já excluídos pelo `vitest.config.ts` da raiz (`**/types/**`, `**/generated/**`, `**/src/testing/**`, `**/test/**`, `**/*.config.*`). Nenhuma exclusão nova.
- **Toda** espera **dos casos novos** logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes da web pela constante `LAZY_TIMEOUT` declarada no arquivo (`findBy…(…, {}, LAZY_TIMEOUT)`, `waitFor(…, LAZY_TIMEOUT)`); no e2e pela constante `ROUTE_TIMEOUT` declarada no topo do spec. Nunca `setTimeout`/sleep fixo nem `waitForTimeout`. **Casos preexistentes não são alterados nem renomeados** — com **uma** exceção declarada, que vale como regra desta fatia e resolve o conflito de antemão: em `apps/web/src/app/routes/app/admin/__tests__/org-unit-people.test.tsx`, os casos preexistentes que hoje buscam o `<h2>` "Pessoas lotadas" podem permanecer **exatamente com o mesmo nome e as mesmas asserções**, porque o `<h2>` muda de arquivo mas continua na mesma tela (D5); se algum caso preexistente ainda assim quebrar por causa da mudança de arquivo, **quem cede é o teste preexistente**, que pode ser ajustado no mínimo necessário (nunca renomeado, nunca enfraquecido), e o ajuste é citado no PR.
- Testes de API são de integração contra o Postgres real (padrão `apps/api/test`, sem mock do Prisma); "teste passa" é medido pelo **exit code** de `npx vitest run --project api` / `--project web` (zero), nunca pela leitura da saída `--verbose`.
- **Só a administração remove, e a resposta não conta nada**: pessoa inexistente, de outra organização, com id malformado **ou que já não estava lotada** respondem o **mesmo** 404, com o **mesmo** corpo (`PERSON_NOT_FOUND_MESSAGE = 'Pessoa não encontrada.'`); unidade inexistente, de outra organização ou com id malformado respondem o **mesmo** 404 de `orgUnitNotFound()` ("Unidade não encontrada."). Nada na resposta revela quem existe na instância além do que a lista já mostra.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa. Cada fase termina com **um** commit Conventional Commits em inglês (`feat:`, `test:`, `docs:`), sem `--no-verify`.
- Nenhuma tarefa acrescenta escopo: sem mover de unidade, sem histórico, sem paginação/busca na lista, sem remoção em lote, sem desfazer, sem prévia de acesso, sem mudar `org-units.service.ts`.

Desvio em relação à tabela de arquivos da SPEC, sem mudar escopo: `docs/roadmap.md` **não** é tarefa nem critério de fase — a orquestração o registra no encerramento (item 108 concluído, dívida 109 encerrada, dívidas novas). O `docs/architecture.md` fica na fase 3, junto com o e2e.

## Fase 1 — API: contrato `DELETE /org-units/{orgUnitId}/people/{personId}` e o `remove` do serviço

Caminhos relativos à raiz do repositório.

- [x] T1.1 — Contrato primeiro: a operação `removePersonFromOrgUnit`
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D1, R10, R11):
    - Caminho **novo** `/org-units/{orgUnitId}/people/{personId}`, declarado logo depois de `/org-units/{orgUnitId}/people` (sem prefixo `/api`, como todos os caminhos do arquivo), com a operação `delete` e **sem** `requestBody`: `operationId: removePersonFromOrgUnit`, tag `org-units`, dois parâmetros de rota `orgUnitId` e `personId` (`in: path`, `required: true`, `schema: { type: string }`).
    - Respostas: **204** sem corpo (sem `content`); **401**, **403** e **404** com o schema `Error`, que já existe. **Não** existe `400` (não há corpo a validar) nem `409`.
    - A `description` da operação, em pt_BR, registra as regras observáveis: só a administração; apaga **apenas** a linha de lotação, sem tocar `Person`, `OrgUnit` nem `Space`; a pessoa continua lotada nas outras unidades; unidade inexistente, de outra organização ou com id malformado respondem "Unidade não encontrada."; pessoa inexistente, de outra organização, com id malformado **ou que já não está lotada nesta unidade** respondem "Pessoa não encontrada."; ordem de falha CSRF → 401 → 403 → 404 da unidade → 404 da pessoa.
    - O caminho pai `/org-units/{orgUnitId}/people` **não** é tocado, e nenhum parâmetro existente é renomeado: o nome `orgUnitId` é o mesmo em todos os caminhos.
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`; o `.d.ts` nunca é editado à mão. `apps/api/test/generated-types.test.ts` e `apps/web/src/types/api.ts` não mudam.
  - Skills: security
  - Complexidade: baixa

- [x] T1.2 — `remove` no serviço, num `deleteMany` só, e o `@Delete(':personId')` que herda os guards
  - Arquivos: `apps/api/src/unit-assignments/unit-assignments.service.ts` (alterar); `apps/api/src/unit-assignments/unit-assignments.controller.ts` (alterar)
  - O que fazer (D2, R5, R7, R9, R10, R11):
    - Em `unit-assignments.service.ts`, método novo `remove(organizationId: string, orgUnitId: string, personId: string): Promise<void>`, com comentário explicando que a ordem repete a de `assign` (unidade primeiro, lotação depois) e que não há consulta a `Person`:
      1. `if (!isUuid(orgUnitId)) throw orgUnitNotFound();`
      2. `const orgUnit = await this.prisma.orgUnit.findFirst({ where: { id: orgUnitId, organizationId }, select: { id: true } });` e `if (!orgUnit) throw orgUnitNotFound();`
      3. `const { count } = await this.prisma.orgUnitAssignment.deleteMany({ where: { orgUnitId: orgUnit.id, personId } });`
      4. `if (count === 0) throw new DomainNotFoundException(PERSON_NOT_FOUND_MESSAGE);`
    - **`deleteMany` e não `delete` pela PK composta**: `delete` lança `P2025` quando a linha não existe, e capturar código de erro do Prisma para decidir um 404 é mais frágil; além disso `deleteMany` faz do filtro e da escrita uma instrução só, então duas abas removendo o mesmo par não conseguem os dois `count = 1`.
    - **Nenhuma consulta a `Person`** e nenhum `isUuid` sobre `personId`: a FK garante que só existe lotação para pessoa existente, e `count === 0` cobre num caminho só pessoa inexistente, de outra organização, id malformado e "já não estava lotada". **Nada de 204 idempotente**: distinguir os casos exigiria consulta extra a `Person` e transformaria a rota em oráculo sobre quem existe na instância.
    - Nenhuma constante nova de mensagem: reaproveitar `orgUnitNotFound()` e `PERSON_NOT_FOUND_MESSAGE`, que já existem no módulo.
    - `unit-assignments.controller.ts`: método novo com `@Delete(':personId')` + `@HttpCode(204)` em `UnitAssignmentsController`, que **já tem** `@UseGuards(SessionGuard, AdminGuard)` na classe — **nenhuma linha de guard é escrita nesta fatia**. O `organizationId` vem de `@CurrentPerson()`, nunca da rota; retorno `Promise<void>`.
    - **Nada em `apps/api/src/org-units/**` muda**: a recusa de apagar unidade com gente lotada lê `_count.assignments`, e apagar a linha derruba a contagem sozinha.
    - Sem `Logger` e sem `console.` no módulo fora de testes.
  - Skills: authorization, security
  - Complexidade: alta

- [x] T1.3 — Testes da fase 1
  - Arquivos: `apps/api/src/unit-assignments/__tests__/unit-assignments.service.test.ts` (alterar); `apps/api/src/unit-assignments/__tests__/unit-assignments.integration.test.ts` (alterar); `apps/api/src/unit-assignments/__tests__/unit-assignments.contract.test.ts` (alterar)
  - O que fazer (D7): integração e contrato contra o Postgres real, sem mock do Prisma, `resetDatabase(prisma)` em `beforeEach`, instalação e sessões pelos ajudantes já usados nos testes de `unit-assignments`; ajudante local `removePerson(orgUnitId, personId, cookie?)` mandando `X-Requested-With: XMLHttpRequest`. `apps/api/test/**` **não muda**. Nenhuma espera fixa; nenhum literal com cara de senha. **Casos preexistentes não são renomeados nem alterados.**
    - `unit-assignments.service.test.ts` (Prisma falso), casos novos: `remove deletes only the pair of the unit and the person` (o `where` do `deleteMany` é **só** `{ orgUnitId, personId }`); `remove scopes the unit to the organization of the session` (o `findFirst` da unidade leva `organizationId`); `remove with no deleted row rejects with the person not found message` (`count: 0` → `DomainNotFoundException` com `PERSON_NOT_FOUND_MESSAGE`); `a malformed org unit id never reaches the database` (`findFirst` não é chamado); `a malformed person id reaches the database and gets the same not found` (o `deleteMany` **é** chamado, e o erro é o mesmo 404 da pessoa).
    - `unit-assignments.integration.test.ts`, casos novos:
      - `removing an assigned person answers 204 with no body` — e a pessoa **some** do `GET /org-units/{id}/people`, enquanto quem continua lotado permanece;
      - `removing the same pair twice answers the same not found` — guarda os **dois corpos** e afirma `toEqual` entre eles, além do status 404 e da mensagem "Pessoa não encontrada.";
      - `it.each` `an unknown org unit`, `an org unit of another organization`, `a malformed org unit id` → 404 "Unidade não encontrada.", com os **três corpos comparados entre si**;
      - `it.each` `an unknown person`, `a person of another organization`, `a malformed person id` → 404 "Pessoa não encontrada.", com o **mesmo** corpo do caso "já não estava lotada" (asserção de igualdade entre os corpos — é o que prova que o servidor não é oráculo);
      - `a non-admin answers 403` (mensagem "Apenas a administração pode fazer isso.") e `an anonymous request answers 401` (mensagem "Sessão não encontrada."), e nos dois a lotação **continua** lá depois da tentativa;
      - `a person assigned to two units stays in the other one` — e continua na instância (consulta direta a `Person` pelo Prisma);
      - `an admin removes itself and stays an admin` — o `DELETE` seguinte, na mesma sessão, ainda responde 204;
      - `deleting a unit works again after the last person is removed` — **a prova que fecha a dívida 109**: cria unidade, lota uma pessoa, `DELETE /org-units/{id}` → **409** "Ainda há pessoas lotadas nesta unidade. Tire a lotação delas antes de apagar a unidade."; remove a pessoa pela rota nova; `DELETE /org-units/{id}` de novo → **204**. Os dois `DELETE` no mesmo teste, de propósito.
    - `unit-assignments.contract.test.ts`, casos novos: `DELETE org unit person answers the documented 401`; `DELETE org unit person answers the documented 403`; `DELETE org unit person answers the documented 404`; `DELETE org unit person answers 204 with an empty body` (status e corpo vazio; não há schema para validar).
    - Fecha a fase com um commit, por exemplo `feat(api): remove a person from an org unit`.
  - Skills: unit-testing, integration-testing, security
  - Complexidade: alta

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (sem aviso de peer no install; sem aviso de chunk grande no build; o aviso de desempenho do Vitest sobre jsdom não conta). A saída de `pnpm test` não tem `console.error` nem `console.warn` vazando, nem aviso do Nest ou do Prisma. `git diff feature/010-unit-assignments... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA1.2 — `packages/api-contract/openapi.yaml` tem o caminho `/org-units/{orgUnitId}/people/{personId}` com a operação `delete` e **sem** `requestBody`, `operationId: removePersonFromOrgUnit`, tag `org-units` e **dois** parâmetros em `path` (`orgUnitId` e `personId`), ambos `required: true`, `schema.type: string`. As respostas são exatamente `204` (sem `content`), `401`, `403` e `404`, as três últimas referenciando o schema `Error`; **não** há `400` nem `409` nessa operação. A `description` menciona, em pt_BR, que só a administração remove, que apenas a linha de lotação é apagada, que a pessoa continua nas outras unidades, e os dois 404 com as mensagens "Unidade não encontrada." e "Pessoa não encontrada.", esta última valendo também para quem já não está lotada. O caminho `/org-units/{orgUnitId}/people` continua existindo e não foi renomeado, e nenhum parâmetro de caminho mudou de nome. `rg -n "removePersonFromOrgUnit" packages/api-contract/src/generated/openapi.d.ts` encontra a operação; `apps/api/test/generated-types.test.ts` passa sem ter sido alterado; `git diff feature/010-unit-assignments... -- apps/web/src/types/api.ts` é vazio.
- [x] CA1.3 — Nenhuma migration nova e nenhum comando proibido de Prisma: `git diff --stat feature/010-unit-assignments... -- apps/api/prisma` é vazio (nem `schema.prisma`, nem diretório novo em `migrations/`), e `git status --porcelain -uall apps/api/prisma` não lista nenhum arquivo. `git diff feature/010-unit-assignments... -- apps/api apps/web packages | rg -n "migrate (diff|dev|reset)"` é vazio (`docs/` fica fora porque a documentação cita esses comandos justamente para proibi-los). Conferência de rotina: `pnpm --filter api exec prisma validate` termina com exit code 0 e `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test pnpm --filter api exec prisma migrate status` reporta nenhuma migration pendente.
- [x] CA1.4 — `apps/api/src/unit-assignments/unit-assignments.service.ts` tem `remove(organizationId: string, orgUnitId: string, personId: string): Promise<void>` que, nesta ordem: chama `isUuid(orgUnitId)` e lança `orgUnitNotFound()` se falso; chama `this.prisma.orgUnit.findFirst` com `where` contendo `id` **e** `organizationId`, lançando `orgUnitNotFound()` quando não acha; chama `this.prisma.orgUnitAssignment.deleteMany` com `where` **exatamente** `{ orgUnitId, personId }` (nada além dos dois campos); e lança `DomainNotFoundException(PERSON_NOT_FOUND_MESSAGE)` quando `count === 0`. Dentro de `remove` **não** há `this.prisma.person`, `findUnique`, `findFirst` sobre `person`, `delete(` pela PK composta, nem `isUuid(personId)`. Nenhuma constante de mensagem nova foi criada: `git diff feature/010-unit-assignments... -- apps/api/src/unit-assignments/unit-assignments.service.ts` não acrescenta nenhum literal de mensagem de erro (as únicas continuam sendo `orgUnitNotFound()` e `PERSON_NOT_FOUND_MESSAGE = 'Pessoa não encontrada.'`, inalterada). `rg -n "Logger|console\." apps/api/src/unit-assignments --glob '!**/__tests__/**'` é vazio.
- [x] CA1.5 — `apps/api/src/unit-assignments/unit-assignments.controller.ts` continua com `@UseGuards(SessionGuard, AdminGuard)` **na classe** e ganha **um** método com `@Delete(':personId')` e `@HttpCode(204)`, que obtém o `organizationId` de `@CurrentPerson()`, repassa `orgUnitId` e `personId` da rota ao serviço e devolve `Promise<void>`. `git diff feature/010-unit-assignments... -- apps/api/src/unit-assignments/unit-assignments.controller.ts` **não** acrescenta nenhuma linha com `@UseGuards`, e o 204 não tem `res.json` nem corpo.
- [x] CA1.6 — Intocados nesta fase: `git diff --stat feature/010-unit-assignments... -- apps/api/prisma apps/api/src/unit-assignments/unit-assignments.schema.ts apps/api/src/unit-assignments/unit-assignments.module.ts apps/api/src/org-units apps/api/src/auth apps/api/src/common apps/api/src/access apps/api/src/documents apps/api/src/people apps/api/src/invitations apps/api/src/installation apps/api/src/app.module.ts apps/api/test apps/web vitest.config.ts eslint.config.js` é vazio. (`docs/roadmap.md` e `docs/features/108-unit-assignments-remove/plan.md` ficam **fora** de qualquer lista de intocados deste plano: a orquestração edita os dois.)
- [x] CA1.7 — `npx vitest run --project api` termina com exit code 0 e existem, pelos nomes de T1.3: os 5 casos novos de `unit-assignments.service.test.ts` (`remove deletes only the pair of the unit and the person`, `remove scopes the unit to the organization of the session`, `remove with no deleted row rejects with the person not found message`, `a malformed org unit id never reaches the database`, `a malformed person id reaches the database and gets the same not found`); os casos novos de `unit-assignments.integration.test.ts` (entre eles `removing an assigned person answers 204 with no body`, `removing the same pair twice answers the same not found`, `a non-admin answers 403`, `an anonymous request answers 401`, `a person assigned to two units stays in the other one`, `an admin removes itself and stays an admin`, `deleting a unit works again after the last person is removed`, mais os dois `it.each` dos 404 da unidade e da pessoa); e os 4 casos novos de `unit-assignments.contract.test.ts`. Conferir a presença dos nomes com `rg -n "^\s*(it|test)(\.each)?\(" apps/api/src/unit-assignments/__tests__`. A integração usa o Postgres real: `rg -n "vi\.mock\(.*prisma" apps/api/src/unit-assignments/__tests__/unit-assignments.integration.test.ts apps/api/src/unit-assignments/__tests__/unit-assignments.contract.test.ts` é vazio. Todos os casos preexistentes dos três arquivos continuam listados e não foram renomeados. Nenhum `.only(` nem `.skip(`.
- [x] CA1.8 — Lendo `deleting a unit works again after the last person is removed` em `unit-assignments.integration.test.ts`: o mesmo teste faz **dois** `DELETE /org-units/{id}`, o primeiro com asserção de **409** e da mensagem "Ainda há pessoas lotadas nesta unidade. Tire a lotação delas antes de apagar a unidade.", e o segundo, depois do `DELETE /org-units/{id}/people/{personId}`, com asserção de **204**. `git diff --stat feature/010-unit-assignments... -- apps/api/src/org-units` é vazio: a dívida 109 é fechada por teste, sem uma linha nova em `org-units.service.ts`.
- [x] CA1.9 — Lendo os dois `it.each` e o caso de remover duas vezes: os três corpos dos 404 da **unidade** são comparados entre si (`toEqual` entre variáveis, não só contra o literal); os três corpos dos 404 da **pessoa** são comparados entre si **e** com o corpo do caso "já não estava lotada"; e o caso `removing the same pair twice answers the same not found` guarda os dois corpos em variáveis e afirma igualdade entre eles. Nos casos de 403 e de 401 há asserção de que a lotação **continua** no banco depois da tentativa (consulta direta pelo Prisma ou pelo `GET`).
- [x] CA1.10 — `rg -n "setTimeout\(|sleep\(" apps/api/src/unit-assignments` é vazio; `rg -n "eslint-disable" apps/api/src | rg -v -- "--"` é vazio; nenhum arquivo de `apps/api/src/unit-assignments/**` contém senha literal (as senhas vêm dos ajudantes/constantes de teste já existentes).
- [x] CA1.11 — Cobertura ≥ 80% de linhas para `apps/api/src/unit-assignments/unit-assignments.service.ts` e `apps/api/src/unit-assignments/unit-assignments.controller.ts`. A medição vale por `coverage/coverage-summary.json`, gerado na raiz com `npx vitest run --coverage --coverage.reporter=json-summary --coverage.reporter=text` **sem `--project`** (com filtro de projeto o `include` da raiz não casa e o relatório sai vazio): o reporter `text` do v8 **omite arquivos com 100%**, então ausência da tabela de texto não reprova — reprova só quem aparecer no `coverage-summary.json` com `lines.pct < 80` ou não aparecer em lugar nenhum.
- [x] CA1.12 — `git log feature/010-unit-assignments..HEAD --format=%s` mostra exatamente um commit desta fase, em inglês, no formato Conventional Commits (prefixo `feat`, `fix`, `test`, `docs`, `chore` ou `refactor` seguido de `:` ou `(escopo):`).

## Fase 2 — Web: a mutação, a ação na linha, o diálogo, o foco e a API simulada

Caminhos relativos à raiz do repositório.

- [x] T2.1 — A mutação `DELETE /org-units/{id}/people/{personId}` com a invalidação esperada
  - Arquivos: `apps/web/src/features/unit-assignments/api/remove-assignment.ts` (criar)
  - O que fazer (D4, R4, R6):
    - No molde de `features/unit-assignments/api/assign-person.ts`: `removeAssignment({ orgUnitId, personId }: { orgUnitId: string; personId: string }): Promise<void>` chamando `api.delete(\`/org-units/${orgUnitId}/people/${personId}\`, { silentError: true })`; `useRemoveAssignment({ orgUnitId, mutationConfig })` com `await queryClient.invalidateQueries({ queryKey: getUnitPeopleQueryOptions(orgUnitId).queryKey })` dentro do `onSuccess`, **antes** de chamar o `onSuccess` de quem usa o hook — assim a mutação continua `isPending` até a lista nova chegar, e quem fecha o diálogo já encontra a lista sem a linha. Mesmo comentário explicativo que `useAssignPerson` já tem ao lado.
    - **`silentError: true`**, ao contrário da fatia 087: aqui o 404 **não** deve virar notificação de erro, porque "Pessoa não encontrada." numa tela de remoção soa como acusação de erro de quem clicou, quando o resultado desejado já vale. O componente decide o que dizer (T2.3). Não é preciso capturar o corpo como `assign-person.ts` faz: o `NotFoundError` do cliente HTTP compartilhado basta.
    - Nada de atualização otimista, nada de `setQueryData`, nada de `onMutate`, nada de `refetchInterval`.
  - Skills: api-requests, client-state
  - Complexidade: baixa

- [x] T2.2 — A API simulada: `removeAssignment` no banco falso e o handler `DELETE …/people/:personId`
  - Arquivos: `apps/web/src/testing/mocks/db.ts` (alterar); `apps/web/src/testing/mocks/handlers/unit-assignments.ts` (alterar); `apps/web/src/testing/mocks/utils.ts` (alterar)
  - O que fazer (D6):
    - `db.ts`: função nova `removeAssignment(orgUnitId: string, personId: string): boolean`, que faz `splice` **no array que já está no banco falso**, nunca substituindo o array (mesma razão comentada em `addAssignment`: o handler lê o estado antes do `await` e escreve depois). Devolve `false` quando o par não está lá, que é o 404.
    - `handlers/unit-assignments.ts`: handler novo `http.delete(\`${env.API_URL}/org-units/:orgUnitId/people/:personId\`)`, declarado **depois** do `http.post` de `/org-units/:orgUnitId/people`, com o mesmo preâmbulo dos outros (`networkDelay()`, `devOverride('unit-assignments')`, 401 sem cookie ou sem instalação, 403 para não-admin), a unidade resolvida **antes** de qualquer coisa sobre a pessoa (`unitNotFound()`), e depois `removeAssignment(unit.id, params.personId)` → `new HttpResponse(null, { status: 204 })` ou `personNotFound()`. As três funções de resposta e o `devOverride` já existem no arquivo: **nenhuma constante nova**.
    - `utils.ts`: **só** o comentário das chaves de desenvolvimento — `mock-org-units=sample` agora também dá o que remover.
  - Skills: api-mocking
  - Complexidade: média

- [x] T2.3 — A ação na linha, o diálogo único e a regra de foco
  - Arquivos: `apps/web/src/features/unit-assignments/components/unit-people-list.tsx` (alterar); `apps/web/src/app/routes/app/admin/org-unit-people.tsx` (alterar)
  - O que fazer (D5, R1, R2, R3, R4, R6, R7, R8, R12, R13, R14). A aparência segue `docs/design.md` e o piso da skill `interface-design`; **nenhuma classe é ditada por esta tarefa**, e `docs/design.md` **não muda** (todas as receitas usadas já existem: "Lista", "Ações do item de lista", "Botão discreto (`ghost`)", "Botão só com ícone", "Ícone de lixeira", "Botão destrutivo", "Diálogo de confirmação", "Ação destrutiva com confirmação num item de lista ou árvore", "Notificação", "Vazio").
    - **O cabeçalho da seção muda de arquivo**: o `<h2>` "Pessoas lotadas" sai de `app/routes/app/admin/org-unit-people.tsx` e entra no componente, dentro do `<section>` que hoje está na rota. Motivo: o destino de foco de "a lista ficou vazia" é esse cabeçalho, e o componente não tem como focar um elemento que mora na rota. A tela fica **idêntica** (mesmas classes, mesma ordem); a rota passa a renderizar só `<UnitPeopleList query={…} />`. O `<h2>` ganha `tabIndex={-1}` (foco programático, sem entrar na ordem de `Tab`). O `<section>` de "Lotar alguém", o `AssignPersonSearch` e o aviso de que lotação não dá acesso a documento **não mudam de lugar nem de texto**.
    - Cada `<li>` continua com nome e e-mail e ganha, à direita, a coluna de ações da receita "Ações do item de lista" com **um** `Button` `variant="ghost"` + `size="icon"`, `type="button"`, `aria-label` **e** `title` iguais a **"Remover {nome} desta unidade"**, com um ícone de **lixeira** local (mesmo SVG que `OrgUnitsTree` já usa, `aria-hidden="true"`, `focusable="false"`, traço `currentColor`) — aqui a linha é apagada de verdade, então a lixeira promete exatamente o que acontece. Nada de botão com texto visível "Remover" na linha, nada de esconder a ação até o `hover`.
    - O `UnitPeopleStates` com dados vira um `LoadedUnitPeopleList`, que segura o estado da remoção; **os outros três estados não mudam uma linha**, e o `aria-live="polite"` que embrulha os quatro estados continua onde está.
    - **Um** `ConfirmationDialog` para a lista inteira, **sem `trigger`**; `removing: { personId, personName, neighbourId } | null` guardando uma **cópia** do que o diálogo precisa (depois do sucesso a pessoa não está mais na lista e a descrição ainda mostra o nome enquanto a caixa fecha); `isRemoveOpen` separado do `removing`, porque o fechamento é animado; guarda de clique duplo em `handleConfirmRemove` (`if (mutation.isPending) return;`).
    - O nome da unidade para a descrição sai de `query.data.orgUnit.name` — o componente não ganha prop nova e a rota não passa nada.
    - Textos literais do diálogo, em pt_BR: título **"Remover da unidade?"**; descrição **"{nome} sai de “{unidade}”. A pessoa continua na instância e continua lotada nas outras unidades em que estiver; nada além desta lotação é apagado. Para voltar atrás, basta lotar de novo pela busca acima."**; botão de confirmar **"Remover"** (destrutivo), **"Removendo…"** enquanto pendente; cancelar **"Cancelar"** (padrão do componente).
    - Notificações: sucesso com título **"Pessoa removida"** e mensagem **"{nome} saiu de {unidade}."** (`success`); no 404 (`NotFoundError`), o diálogo **fecha**, a lista recarrega e a notificação é **`info`**, com título **"Lista atualizada"** e mensagem **"A lista foi atualizada: essa pessoa já não estava lotada nesta unidade."** — a mensagem do servidor **não** aparece na tela. Qualquer outra falha **mantém** o diálogo aberto, com a notificação do interceptor explicando.
    - **Destino do foco, regra fechada**, em `onCloseAutoFocus`: (1) remoção bem-sucedida → o botão de remover da linha **imediatamente acima** da que sumiu; (2) se a que sumiu era a primeira, o botão da que **passou a ser** a primeira; (3) se não sobrou linha nenhuma, o `<h2>` "Pessoas lotadas"; (4) cancelar, `Escape` ou falha seguida de cancelar → o botão que abriu o diálogo, enquanto ele ainda está lá (`opener?.isConnected`). O **404 segue a mesma regra do sucesso**. O vizinho é escolhido **na abertura**, sobre a lista que está na tela, e guardado em `neighbourId`; os botões são alcançados por um `useRef(new Map<string, HTMLButtonElement>())` preenchido pelo `ref` de cada linha (prop comum, sem `forwardRef`); se o id guardado não estiver mais no mapa, o foco cai na regra 3. Tudo em `ref`, nunca em estado.
  - Skills: interface-design, component-robustness, error-handling
  - Complexidade: alta

- [x] T2.4 — Testes da fase 2
  - Arquivos: `apps/web/src/features/unit-assignments/api/__tests__/remove-assignment.test.tsx` (criar); `apps/web/src/features/unit-assignments/components/__tests__/unit-people-list.test.tsx` (alterar); `apps/web/src/app/routes/app/admin/__tests__/org-unit-people.test.tsx` (alterar)
  - O que fazer (D7): banco falso pelos ajudantes já usados (`seedInstalled(...)`); interação por `userEvent`; localizar por papel e nome acessível em pt_BR; senhas vindas das constantes já existentes, **nunca** literais. `LAZY_TIMEOUT` declarada em cada arquivo e passada em **toda** espera dos casos novos logo após rota ou chunk `lazy`. Nenhuma espera fixa; nenhum `console.error`/`console.warn`/aviso de `act(...)` vazando. `src/testing/setup-tests.ts` **não** muda; casos preexistentes não são renomeados (ver a exceção declarada nas regras gerais, para o `<h2>` que mudou de arquivo).
    - `remove-assignment.test.tsx`: `sends DELETE to /org-units/:orgUnitId/people/:personId`; `a successful remove invalidates the unit people query key` (espiando `invalidateQueries` e conferindo a chave `['org-units', orgUnitId, 'people']`); `a 404 rejects without a global notification` (prova do `silentError`).
    - `unit-people-list.test.tsx`, casos novos: `each row has a Remover button named after the person`; `clicking Remover opens the dialog with the person and the unit in the description`; `Cancelar calls no API and returns the focus to the button`; `confirming removes the row and notifies with the person name`; `removing the second of two focuses the button above`; `removing the first of two focuses the new first button`; `removing the only person focuses the heading and shows the empty state`; `a 404 closes the dialog, refetches and notifies a neutral message`; `a 500 keeps the dialog open`; `two Enter presses send a single request` (handler contador).
    - `org-unit-people.test.tsx` (rota), casos novos: `an admin removes one person and still sees the other`; `the Pessoas lotadas heading is still on the page`; `the notice that an assignment grants no document access is still on the page`.
    - Fecha a fase com um commit, por exemplo `feat(web): remove a person from an org unit from the list`.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [x] CA2.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` terminam com exit code 0 e **sem aviso** (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta). A saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)` vazando. `git diff feature/010-unit-assignments... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio.
- [x] CA2.2 — `apps/web/src/features/unit-assignments/api/remove-assignment.ts` existe (antes do commit, `git status --porcelain -uall apps/web/src/features/unit-assignments/api/remove-assignment.ts` lista o arquivo; depois, `git diff --stat feature/010-unit-assignments... -- apps/web/src/features/unit-assignments/api/remove-assignment.ts` lista o arquivo) e exporta `removeAssignment({ orgUnitId, personId })` chamando `api.delete` num caminho `/org-units/${orgUnitId}/people/${personId}` com `{ silentError: true }`, e `useRemoveAssignment`. Dentro do `onSuccess` do hook há `await queryClient.invalidateQueries({ queryKey: getUnitPeopleQueryOptions(orgUnitId).queryKey })` **antes** da chamada a `mutationConfig?.onSuccess` (o `await` está lá, lendo o código). `rg -n "setQueryData|refetchInterval|onMutate" apps/web/src/features/unit-assignments/api/remove-assignment.ts` é vazio.
- [x] CA2.3 — `apps/web/src/features/unit-assignments/components/unit-people-list.tsx`: cada linha da lista renderiza um `Button` de `@/components/ui/button/button` com `variant="ghost"`, `size="icon"`, `type="button"` e `aria-label` **e** `title` iguais a `Remover ${person.name} desta unidade`. O ícone é um `<svg>` local de lixeira com `aria-hidden="true"` e `focusable="false"`. `rg -n "<button" apps/web/src/features/unit-assignments/components/unit-people-list.tsx` é vazio (todo botão é o `Button` compartilhado). Não existe na linha um texto visível "Remover" fora do `aria-label`/`title`, nem classe de `hover` que esconda a ação (`rg -n "group-hover|opacity-0" apps/web/src/features/unit-assignments/components/unit-people-list.tsx` é vazio).
- [x] CA2.4 — Diálogo, com os textos literais: o arquivo usa **um** `ConfirmationDialog` para a lista inteira, **sem** prop `trigger` (`rg -c "<ConfirmationDialog" apps/web/src/features/unit-assignments/components/unit-people-list.tsx` devolve 1 e não há `trigger=` nessa chamada). Os textos presentes no arquivo são exatamente: `Remover da unidade?`; a descrição contendo `sai de`, `A pessoa continua na instância e continua lotada nas outras unidades em que estiver; nada além desta lotação é apagado.` e `Para voltar atrás, basta lotar de novo pela busca acima.`, com o nome da pessoa e o nome da unidade interpolados; o rótulo de confirmar `Remover` e o de pendente `Removendo…`. A notificação de sucesso tem título `Pessoa removida` e mensagem `${person.name} saiu de ${unitName}.`; a de 404 tem título `Lista atualizada` e mensagem `A lista foi atualizada: essa pessoa já não estava lotada nesta unidade.`, do tipo `info`. **A mensagem "Pessoa não encontrada." não aparece em nenhum literal do componente** (`rg -n "Pessoa não encontrada" apps/web/src/features/unit-assignments/components/unit-people-list.tsx apps/web/src/features/unit-assignments/api` é vazio (em arquivo de teste o literal é permitido e esperado: ele é a carga do 404 simulado e a asserção de que **não** chega à tela)).
- [x] CA2.5 — Estado e foco, lendo `unit-people-list.tsx`: existe um estado `removing` guardando `personId`, `personName` e `neighbourId` (uma cópia, não uma referência ao item da lista) e um `isRemoveOpen` separado dele; `handleConfirmRemove` começa com uma guarda de clique duplo baseada em `isPending` da mutação; há um `useRef` com um `Map<string, HTMLButtonElement>` preenchido pelo `ref` de cada linha (prop comum, sem `forwardRef`); `onCloseAutoFocus` implementa as quatro regras — vizinho de cima, nova primeira, o `<h2>` (que tem `tabIndex={-1}`) quando a lista esvazia, e o botão que abriu o diálogo quando a interação foi cancelada, com checagem de `isConnected`. O `neighbourId` é calculado no manipulador que **abre** o diálogo, não depois do sucesso. Só o sucesso e o 404 fecham o diálogo; qualquer outra falha o mantém aberto (lendo o tratamento de erro). O nome da unidade vem de `query.data.orgUnit.name`: o componente **não** ganhou prop nova.
- [x] CA2.6 — `apps/web/src/app/routes/app/admin/org-unit-people.tsx`: o `<h2>` "Pessoas lotadas" **saiu** da rota e está agora em `unit-people-list.tsx` (`rg -n "Pessoas lotadas" apps/web/src/app/routes/app/admin/org-unit-people.tsx` é vazio; `rg -n "Pessoas lotadas" apps/web/src/features/unit-assignments/components/unit-people-list.tsx` encontra). A rota continua com **um único** `<h1>`, continua renderizando o `<section>` de "Lotar alguém" com o `AssignPersonSearch` e o aviso de que a lotação não dá acesso a documento, e `git diff feature/010-unit-assignments... -- apps/web/src/app/routes/app/admin/org-unit-people.tsx` não altera nenhuma linha desses blocos.
- [x] CA2.7 — Acabamento (piso de `interface-design`, verificável lendo o código): os quatro estados da consulta continuam existindo, cada um estilizado e no mesmo lugar do conteúdo, dentro do `aria-live="polite"` que já existia — carregando com `role="status"`, erro com `role="alert"` e botão de tentar de novo, vazio com o texto que já existe **"Ninguém está lotado nesta unidade ainda. Use a busca acima para lotar a primeira pessoa."**, todos com os mesmos textos de antes (`git diff feature/010-unit-assignments... -- apps/web/src/features/unit-assignments/components/unit-people-list.tsx` não altera esses três estados). Na linha, o nome fica em bloco com `min-w-0` e o e-mail em tom mais suave; a coluna de ações fica à direita e desce a 360px sem rolagem horizontal; o botão tem alvo de clique de 40px (`size-10` da receita "Botão só com ícone"), com `hover`, `focus-visible` e `disabled` vindos do `Button`. `rg -n "forwardRef|style=\{\{|: JSX\.|!important" apps/web/src/features/unit-assignments apps/web/src/app/routes/app/admin/org-unit-people.tsx` é vazio e todo componente declara retorno `React.JSX.Element`. `git diff --stat feature/010-unit-assignments... -- docs/design.md` é vazio (nenhuma receita nova).
- [x] CA2.8 — API simulada: `apps/web/src/testing/mocks/db.ts` exporta `removeAssignment(orgUnitId: string, personId: string): boolean`, que usa `splice` no array de lotações **já existente** (sem reatribuir o array: não há linha `assignments = ` dentro da função) e devolve `false` quando o par não está lá. Em `apps/web/src/testing/mocks/handlers/unit-assignments.ts` existe um `http.delete` em `${env.API_URL}/org-units/:orgUnitId/people/:personId` declarado **depois** do `http.post` de `/org-units/:orgUnitId/people`, com `networkDelay()`, `devOverride('unit-assignments')`, 401 sem cookie ou sem instalação, 403 para não-admin, a unidade resolvida antes da pessoa (`unitNotFound()`), e respondendo `new HttpResponse(null, { status: 204 })` ou `personNotFound()`; nenhuma função de resposta nova foi criada no arquivo. `git diff feature/010-unit-assignments... -- apps/web/src/testing/mocks/utils.ts` só tem linhas iniciadas por `//` além do contexto.
- [x] CA2.9 — Intocados nesta fase (base é o commit da fase 1, porque `packages` e `apps/api` mudaram legitimamente lá): `git diff --stat <commit da fase 1> -- apps/web/src/lib apps/web/src/types apps/web/src/components apps/web/src/config apps/web/src/utils apps/web/src/features/auth apps/web/src/features/connection apps/web/src/features/installation apps/web/src/features/documents apps/web/src/features/org-units apps/web/src/features/invitations apps/web/src/testing/setup-tests.ts apps/web/src/features/unit-assignments/api/get-unit-people.ts apps/web/src/features/unit-assignments/api/search-people.ts apps/web/src/features/unit-assignments/api/assign-person.ts apps/web/src/features/unit-assignments/components/assign-person-search.tsx apps/web/e2e vitest.config.ts eslint.config.js packages apps/api docs` é vazio. (`docs/roadmap.md` e `docs/features/108-unit-assignments-remove/plan.md` são editados pela orquestração; se aparecerem, não reprovam — qualquer outro caminho de `docs/` reprova.) A feature não importa de outra feature nem de `app/`: `rg -n "@/features/(?!unit-assignments)|@/app/" -P apps/web/src/features/unit-assignments --glob '!**/__tests__/**'` é vazio.
- [x] CA2.10 — `npx vitest run --project web` termina com exit code 0 e existem, pelos nomes de T2.4: os 3 casos de `remove-assignment.test.tsx` (`sends DELETE to /org-units/:orgUnitId/people/:personId`, `a successful remove invalidates the unit people query key`, `a 404 rejects without a global notification`); os 10 casos novos de `unit-people-list.test.tsx` (entre eles `Cancelar calls no API and returns the focus to the button`, `removing the second of two focuses the button above`, `removing the first of two focuses the new first button`, `removing the only person focuses the heading and shows the empty state`, `a 404 closes the dialog, refetches and notifies a neutral message`, `a 500 keeps the dialog open` e `two Enter presses send a single request`); e os 3 casos novos de `app/routes/app/admin/__tests__/org-unit-people.test.tsx` (`an admin removes one person and still sees the other`, `the Pessoas lotadas heading is still on the page`, `the notice that an assignment grants no document access is still on the page`). Conferir os nomes com `rg -n "^\s*(it|test)\(" apps/web/src/features/unit-assignments apps/web/src/app/routes/app/admin/__tests__/org-unit-people.test.tsx`. Os casos preexistentes desses arquivos continuam listados e **não foram renomeados** (asserções de caso preexistente só podem ter sido ajustadas pela exceção declarada nas regras gerais, referente ao `<h2>` que mudou de arquivo). Nenhum `.only(` nem `.skip(`.
- [x] CA2.11 — Lendo `two Enter presses send a single request`: há um contador de chamadas no handler e uma asserção de que ele vale **1** depois de dois `Enter` seguidos no botão de confirmar. Lendo os três casos de foco: cada um termina com uma asserção `toHaveFocus()` sobre o elemento esperado (o botão da linha de cima, o botão da nova primeira linha e o `<h2>` "Pessoas lotadas", respectivamente), e o terceiro afirma também o texto do estado vazio. Lendo `a 500 keeps the dialog open`: há asserção de que o diálogo **continua** na tela depois da falha. Lendo `a 404 closes the dialog, refetches and notifies a neutral message`: há asserção do título `Lista atualizada` e de que o texto `Pessoa não encontrada.` **não** está na tela.
- [x] CA2.12 — Esperas: `remove-assignment.test.tsx`, `unit-people-list.test.tsx` e `org-unit-people.test.tsx` declaram `LAZY_TIMEOUT` e a passam em **toda** espera **dos casos novos** logo após abrir ou trocar de rota (`findBy…` e `waitFor`). `rg -n "setTimeout\(|sleep\(" apps/web/src/features/unit-assignments/api/__tests__ apps/web/src/features/unit-assignments/components/__tests__ apps/web/src/app/routes/app/admin/__tests__/org-unit-people.test.tsx` é vazio. Nenhum arquivo de teste desta fase contém senha literal. `rg -n "eslint-disable" apps/web/src apps/api/src | rg -v -- "--"` é vazio.
- [x] CA2.13 — Cobertura ≥ 80% de linhas para `apps/web/src/features/unit-assignments/api/remove-assignment.ts`, `apps/web/src/features/unit-assignments/components/unit-people-list.tsx` e `apps/web/src/app/routes/app/admin/org-unit-people.tsx`. A medição vale por `coverage/coverage-summary.json`, gerado na raiz com `npx vitest run --coverage --coverage.reporter=json-summary --coverage.reporter=text` **sem `--project`**: o reporter `text` do v8 omite arquivos com 100%, então ausência da tabela de texto não reprova — reprova só quem aparecer no `coverage-summary.json` com `lines.pct < 80` ou não aparecer em lugar nenhum.
- [x] CA2.14 — `git log feature/010-unit-assignments..HEAD --format=%s` mostra exatamente dois commits (um por fase), ambos em inglês e no formato Conventional Commits.

## Fase 3 — e2e da jornada de remoção e documentação: a fatia utilizável de ponta a ponta

Caminhos relativos à raiz do repositório.

- [x] T3.1 — Documentação: a entrega na arquitetura
  - Arquivos: `docs/architecture.md` (alterar)
  - O que fazer, em pt_BR, na §4, parágrafo novo **"Entrega `unit-assignments-remove` (fatia 108)"**, sem reescrever o resto:
    - `DELETE /org-units/{orgUnitId}/people/{personId}` responde **204**, entra no `UnitAssignmentsController` e **herda** os guards da classe;
    - **por que `DELETE` aqui e `POST …/revoke` na fatia 087**: lá a linha muda de estado (`revokedAt`) e não é apagada; aqui a linha de `OrgUnitAssignment` é apagada de verdade e a PK composta `(orgUnitId, personId)` é o próprio endereço do recurso;
    - a identidade de caminho foi conferida contra o YAML inteiro: nenhum outro caminho de quatro segmentos sob `/org-units`, e o parâmetro novo entra sob o prefixo literal `people`;
    - `deleteMany` atômico, sem consulta a `Person`, e **um** 404 com "Pessoa não encontrada." para pessoa inexistente, de outra organização, com id malformado e já não lotada — **não** 204 idempotente, que exigiria distinguir os casos e transformaria a rota em oráculo sobre quais pessoas existem; "já resolvido" é decisão de tela, não de HTTP;
    - **nenhuma migration nova** e nenhuma linha nova em `org-units.service.ts` — a recusa de apagar unidade com gente lotada cai sozinha porque `_count.assignments` volta a zero, o que encerra a dívida **109**.
  - Skills: —
  - Complexidade: baixa

- [x] T3.2 — Testes da fase 3 (e2e: a administração tira uma pessoa da unidade pelo teclado)
  - Arquivos: `apps/web/e2e/tests/unit-assignments-remove.spec.ts` (criar)
  - O que fazer (D7): contra a API simulada; localizar por papel e nome acessível em pt_BR. Estado inicial por `page.addInitScript` gravando as chaves `mock-installation=signed-in` e `mock-org-units=sample`. No topo, `const ROUTE_TIMEOUT = { timeout: 10_000 }`; **toda** espera logo após mudança de rota (`toHaveURL`, primeira visibilidade da tela nova) o recebe; nenhum `waitForTimeout`. Depois de cada `Tab`/seta e antes do `Enter`, `await expect(locator).toBeFocused()`. Nenhum literal com cara de senha ou token. `apps/web/e2e/a11y.ts` e os outros specs não são tocados; nenhuma regra do axe é desativada.
    - Caso `an admin removes a person from an org unit using the keyboard`: chega à página de pessoas da unidade pela árvore da página "Estrutura" (ação "Pessoas" na linha da unidade), como `unit-assignments.spec.ts` já faz; lota alguém pela busca; alcança o botão "Remover {nome} desta unidade" **pelo teclado**; confirma no diálogo; vê a notificação "Pessoa removida" com o nome e a lista **sem** aquela pessoa. `expectNoSeriousA11yViolations(page)` é chamado **duas vezes**: com a lista e a ação na tela, e com o diálogo aberto.
    - Os e2e da 010, 064, 065, 066, 085, 086, 087 e 088 **não** mudam. Se algum deles quebrar, o conserto é no código de produção, não no spec antigo.
    - Fecha a fase com um commit, por exemplo `test(e2e): cover removing a person from an org unit`.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [x] CA3.1 — `pnpm test:e2e` na raiz termina com exit code 0 (o Playwright sobe o próprio servidor; não precisa de API nem de Postgres), e continuam terminando com exit code 0 e **sem aviso**, com `docker compose up -d` rodando e o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta).
- [x] CA3.2 — `apps/web/e2e/tests/unit-assignments-remove.spec.ts` contém o caso `an admin removes a person from an org unit using the keyboard`, e `pnpm --filter web exec playwright test --list` o lista junto com todos os casos já existentes de `unit-assignments.spec.ts`, `org-units-view.spec.ts`, `org-units-create-rename.spec.ts`, `org-units-delete.spec.ts`, `invitations-create.spec.ts`, `invitations-accept.spec.ts`, `invitations-list.spec.ts` e `invitations-revoke.spec.ts` (nenhum removido nem renomeado). `rg -n "test\.skip|test\.only|waitForTimeout" apps/web/e2e` é vazio.
- [x] CA3.3 — Lendo o caso: a página de pessoas da unidade é alcançada **navegando pela interface** (página "Estrutura" → ação "Pessoas" na linha da unidade), não por URL direta; uma pessoa é lotada pela busca antes da remoção; o botão "Remover {nome} desta unidade" é alcançado por teclado, com `toBeFocused()` antes do `Enter`; há asserção da notificação "Pessoa removida" com o nome e de que a pessoa **não está mais** na lista; `expectNoSeriousA11yViolations` aparece **duas vezes** no caso — uma com a lista e a ação visíveis e outra com o diálogo aberto.
- [x] CA3.4 — Esperas e segredos do e2e: o spec declara `ROUTE_TIMEOUT = { timeout: 10_000 }` no topo e **toda** asserção logo após mudança de rota (`toHaveURL` e a primeira visibilidade da tela nova) o recebe; entre cada `Tab`/seta e o `Enter` seguinte há `toBeFocused()`; não há espera fixa; não há valor literal de senha nem de token no arquivo.
- [x] CA3.5 — `git status --porcelain -uall -- apps/web/e2e` (antes do commit) ou `git diff --stat feature/010-unit-assignments... -- apps/web/e2e` (depois) lista **só** `apps/web/e2e/tests/unit-assignments-remove.spec.ts`; o spec não desativa regra do axe (`rg -n "disableRules" apps/web/e2e/tests/unit-assignments-remove.spec.ts` é vazio); os arquivos de `apps/web/e2e/**` passam no `pnpm lint` e no `pnpm typecheck`.
- [x] CA3.6 — `docs/architecture.md` tem, na §4, o parágrafo "Entrega `unit-assignments-remove` (fatia 108)" dizendo: `DELETE /org-units/{orgUnitId}/people/{personId}` com **204**, no `UnitAssignmentsController`, herdando os guards da classe; por que `DELETE` aqui e `POST …/revoke` na 087; a conferência de identidade de caminho contra o YAML; `deleteMany` atômico sem consulta a `Person`; **um** 404 com "Pessoa não encontrada." para os quatro casos, e a recusa explícita ao 204 idempotente por causa do oráculo; **nenhuma migration nova** e nenhuma linha nova em `org-units.service.ts`, com o encerramento da dívida **109**. Nenhuma outra seção do arquivo foi reescrita.
- [x] CA3.7 — A fatia está utilizável de ponta a ponta sem navegador aberto por pessoa: `npx vitest run --project api` (exit code 0) inclui `removing an assigned person answers 204 with no body`, `removing the same pair twice answers the same not found`, `an admin removes itself and stays an admin`, `deleting a unit works again after the last person is removed`, `a non-admin answers 403` e `an anonymous request answers 401` em `apps/api/src/unit-assignments/__tests__/unit-assignments.integration.test.ts`; `npx vitest run --project web` (exit code 0) inclui `an admin removes one person and still sees the other` em `apps/web/src/app/routes/app/admin/__tests__/org-unit-people.test.tsx`; e `pnpm test:e2e` (exit code 0) inclui a jornada de `unit-assignments-remove.spec.ts`.
- [x] CA3.8 — `git log feature/010-unit-assignments..HEAD --format=%s` mostra exatamente três commits (um por fase), todos em inglês e no formato Conventional Commits; `git diff --stat feature/010-unit-assignments... -- docs/roadmap.md docs/features/108-unit-assignments-remove/plan.md` pode ser não vazio só pelo registro da orquestração.

## DoD da entrega

- [x] DoD1 — Todas as tarefas e critérios do plano marcados
- [x] DoD2 — Suíte de testes inteira passa
- [x] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [x] DoD4 — Tipos de todos os `tsconfig` sem erros
- [x] DoD5 — Console dos testes sem erro nem aviso
- [x] DoD6 — `build` passa
- [x] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [x] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [x] DoD9 — Nenhuma worktree ou branch temporária sobrando
