# PLAN 065 — org-units-create-rename

Branch: `feature/065-org-units-create-rename` (sai de `feature/064-org-units-view`, que é a base real; toda comparação de "arquivo intocado" é contra ela, nunca contra `develop`).

Fonte: `docs/features/065-org-units-create-rename/spec.md` (as siglas D1…D11 são as decisões técnicas da SPEC e R1…R10 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho que o próprio Vitest imprime sobre o ambiente jsdom é da ferramenta e **não conta**. O aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam de verdade, com o cache do `tsc` limpo: `pnpm exec tsc -b --clean` e conferir que não sobrou nenhum `*.tsbuildinfo` fora de `node_modules` antes de `pnpm typecheck`.

## Regras que valem para todas as tarefas

- **Uma única dependência nova**: `@radix-ui/react-dialog`, só em `apps/web/package.json` (T2.1). Nenhuma outra, em nenhum `package.json`. Nomes de API de biblioteca (Prisma, NestJS, Zod, TanStack Query, React Hook Form, Radix, MSW, Playwright) vêm dos tipos do pacote instalado; os critérios medem comportamento e estrutura essencial.
- **A migration desta fatia é escrita à mão** e aplicada por `prisma migrate deploy` (é o que `apps/api/test/global-setup.ts` já roda antes dos testes). Ninguém — agente, teste ou critério — roda os subcomandos `diff`, `reset` ou `dev` do `prisma migrate`. A limpeza entre testes da API é `resetDatabase(prisma)`.
- Todo componente devolve `React.JSX.Element` (nunca o `JSX` global); `ref` é prop comum (sem `forwardRef`); **todo botão nosso é o componente `Button`** de `apps/web/src/components/ui/button/button.tsx`; navegação interna é `<Link>`/`<NavLink>`; imports absolutos com `@/`; sem barrel files; arquivos em kebab-case. Textos de tela em pt_BR; código, comentários, caminhos de URL e nomes de teste em en_US.
- Nenhum literal com cara de senha, token ou hash em arquivo versionado. Sessões de teste da API vêm de `createPersonWithSession` (`apps/api/test/create-person.ts`) ou dos ajudantes já existentes; valor inválido é `randomUUID()`.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado. Não contam arquivos só de tipos, arquivos com 0 de 0 linhas instrumentáveis, nem os já excluídos pelo `vitest.config.ts` da raiz (`**/types/**`, `**/generated/**`, `**/src/testing/**`, `**/test/**`, `**/*.config.*`). Nenhuma exclusão nova.
- **Toda** espera logo após mudança de rota ou chunk `lazy` tem timeout explícito: nos testes unitários/integração da web pela constante `LAZY_TIMEOUT` declarada no arquivo (`findBy…(…, {}, LAZY_TIMEOUT)`, `waitFor(…, LAZY_TIMEOUT)`); no e2e pela constante `ROUTE_TIMEOUT` declarada no topo do spec. Nunca `setTimeout`/sleep fixo nem `waitForTimeout`.
- "É administração?" é decidido pelo **servidor**. As rotas novas nascem cobertas pelo `@UseGuards(SessionGuard, AdminGuard)` que **já está na classe** do controller; nada em `apps/api/src/auth/**` muda.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa.
- Nenhuma tarefa acrescenta escopo: sem apagar nem mover unidade (`parentId` é imutável); sem seleção de nó, busca por digitação, tecla `*` ou atalhos de letra no `Tree`; sem `DialogTrigger`; sem atualização otimista nem `setQueryData`; sem simular o espaço `UNIT` na API falsa; sem mexer no `UUID_PATTERN` de `apps/api/src/access/access.service.ts`.

Desvios em relação à tabela de arquivos da SPEC, sem mudar escopo: (1) `docs/design.md` sai da fase 3 e entra na fase 2 (T2.1 e T2.2) — a skill `interface-design` manda a receita nova entrar na mesma entrega da tela que a usa; (2) `docs/roadmap.md` (item 069 — dívida do `Tree`: o slot de ações foi entregue aqui, restam busca por digitação e `*`), o status da fatia e o registro das dívidas **não** são tarefa nem critério de fase: a orquestração os registra no encerramento.

## Fase 1 — API: `POST /org-units` e `PATCH /org-units/{orgUnitId}`, com nome único por nível garantido no banco

Caminhos relativos à raiz do repositório.

- [x] T1.1 — Contrato primeiro: as duas rotas de escrita e os schemas novos
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D1): na tag `org-units`, mesmo prefixo e mesma segurança de sessão do `GET /org-units`.
    - `POST /org-units` (`operationId: createOrgUnit`): corpo obrigatório `CreateOrgUnitInput` = objeto com `parentId` (string) e `name` (string, `minLength: 1`, `maxLength: 120`), **ambos em `required`**, `additionalProperties: false`. Respostas: `201` com `OrgUnitResponse`; `400`, `401`, `403`, `404` e `409` reaproveitando o schema `Error` (e os componentes de resposta de erro) que já existem.
    - `PATCH /org-units/{orgUnitId}` (`operationId: updateOrgUnit`): parâmetro de caminho `orgUnitId` (string, obrigatório); corpo obrigatório `UpdateOrgUnitInput` = objeto só com `name` (mesmos limites), obrigatório, `additionalProperties: false` — `parentId` **não** é aceito. Respostas: `200` com `OrgUnitResponse`; `400`, `401`, `403`, `404` e `409`.
    - `OrgUnitResponse` = `{ data: OrgUnit }`, `data` obrigatório. `OrgUnit` e `OrgUnitsResponse` **não mudam**.
    - Descrições em pt_BR: criar grava a unidade e o espaço dela na mesma operação; nome é aparado e único entre irmãs sem diferenciar maiúsculas (409 "Já existe uma unidade com esse nome neste nível."); 404 "Unidade não encontrada." para id da rota ou `parentId` inexistente ou malformado; renomear a raiz renomeia a organização; ordem das checagens 401 → 403 → 404/400 → 409.
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`; o `.d.ts` nunca é editado à mão.
  - Skills: —
  - Complexidade: baixa

- [x] T1.2 — Unicidade no banco: migration à mão com os dois índices
  - Arquivos: `apps/api/prisma/migrations/0007_org_unit_name_uniqueness/migration.sql` (criar); `apps/api/prisma/schema.prisma` (alterar)
  - O que fazer (D2): o `migration.sql` tem **só** estas duas instruções (mais comentário SQL curto, se quiser):
    - `CREATE UNIQUE INDEX "OrgUnit_single_root_key" ON "OrgUnit"(("parentId" IS NULL)) WHERE "parentId" IS NULL;`
    - `CREATE UNIQUE INDEX "OrgUnit_parentId_lower_name_key" ON "OrgUnit"("parentId", lower("name"));`
    - `schema.prisma`: **só** comentário `///` acima do modelo `OrgUnit` (en_US) descrevendo os dois índices (uma raiz por banco; nome único entre irmãs sem diferenciar caixa), dizendo que o Prisma não os expressa, que moram na migration `0007` e que por isso `migrate diff` acusaria "drift" e não deve ser rodado. Nenhum campo, atributo ou modelo muda. As migrations `0001`–`0006` ficam intocadas.
    - Aplicar com `prisma migrate deploy` (o `global-setup` dos testes já faz) e conferir que os testes existentes da API continuam verdes.
  - Skills: —
  - Complexidade: média

- [x] T1.3 — Escrita no módulo `org-units`: schemas, 404 de domínio, `create` e `rename` transacionais, 409 vindo do banco
  - Arquivos: `apps/api/src/common/is-uuid.ts` (criar); `apps/api/src/org-units/org-units.schema.ts` (criar); `apps/api/src/org-units/org-unit-not-found.ts` (criar); `apps/api/src/org-units/org-units.service.ts` (alterar); `apps/api/src/org-units/org-units.controller.ts` (alterar)
  - O que fazer (D3, R2, R3, R5, R6, R7, R8):
    - `is-uuid.ts`: `export const isUuid = (value: string): boolean` — função pura (expressão regular de UUID, sem diferenciar caixa), sem dependências.
    - `org-units.schema.ts` (Zod, no padrão de `apps/api/src/documents/documents.schema.ts`): `export const ORG_UNIT_NAME_MAX_LENGTH = 120`; `orgUnitNameSchema` = string com mensagem de tipo/ausência "Informe o nome.", **aparada**, **normalizada para NFC**, mínimo 1 ("Informe o nome.") e máximo 120 ("O nome pode ter no máximo 120 caracteres."); `createOrgUnitSchema` = objeto **estrito** `{ parentId: string (mensagem "Informe a unidade mãe."), name: orgUnitNameSchema }`; `updateOrgUnitSchema` = objeto **estrito** `{ name: orgUnitNameSchema }`. Chave desconhecida (inclusive `parentId` no `PATCH`) gera item com a mensagem "Campo não permitido.". Exportar os tipos inferidos.
    - `org-unit-not-found.ts` (espelho de `apps/api/src/documents/document-not-found.ts`): `export const orgUnitNotFound = ()` → a exceção de domínio 404 já usada pelo projeto, com "Unidade não encontrada.". É a **única** origem de 404 do módulo.
    - `OrgUnitsService.create(organizationId: string, body: unknown): Promise<OrgUnit>`: valida o corpo com o `parseBody` que já existe (`apps/api/src/common/parse-body.ts`, 400 "Dados inválidos.") → `isUuid(parentId)` **e** `orgUnit.findFirst` por `id: parentId` **e** `organizationId`, senão `orgUnitNotFound()` → uma transação que cria a `OrgUnit` (`organizationId`, `parentId`, `name`) **e** o `Space` de tipo `UNIT` com o `orgUnitId` novo (mesma forma que a instalação usa para a raiz) → devolve `{ id, parentId, name }`.
    - `OrgUnitsService.rename(organizationId: string, orgUnitId: string, body: unknown): Promise<OrgUnit>`: `isUuid(orgUnitId)` e `findFirst` por `id` e `organizationId`, senão 404 (**antes** de validar o corpo) → `parseBody` com `updateOrgUnitSchema` → uma transação que atualiza o nome e, se a unidade tem `parentId === null`, atualiza também `Organization.name` com o mesmo nome → devolve a unidade. Renomear para o próprio nome (ou só trocar a caixa) passa.
    - Conflito: `catch` em volta das duas transações; função local `isUniqueViolation(error: unknown): boolean` (erro conhecido do Prisma com código `P2002`) → `ConflictException('Já existe uma unidade com esse nome neste nível.')`; qualquer outro erro é **relançado**. **Sem** `findFirst` prévio de duplicidade: o 409 sai só do índice do banco (é o que fecha a corrida). Se a integração mostrar que o Prisma entrega a violação do índice por expressão com outra forma, amplia-se **só** essa função, com o caso coberto por teste.
    - Controller: `@Post()` respondendo **201** e `@Patch(':orgUnitId')` respondendo 200, id cru (sem pipe de formato, como em `documents`), corpo cru repassado ao serviço, organização tirada da pessoa da sessão (`@CurrentPerson()`), ambos devolvendo `{ data }` (`Promise<OrgUnitResponse>`). O `@UseGuards(SessionGuard, AdminGuard)` da **classe** não muda e nenhum guard é posto por método. `org-units.module.ts` não muda. `/auth/me` não muda (já lê `organization.name` do banco a cada pedido).
    - Conferir que `apps/api/src/access/__tests__/document-access-boundary.test.ts` continua verde **sem alteração**.
  - Skills: security
  - Complexidade: alta

- [x] T1.4 — Testes da fase 1
  - Arquivos: `apps/api/src/common/__tests__/is-uuid.test.ts` (criar); `apps/api/src/org-units/__tests__/org-units.schema.test.ts` (criar); `apps/api/src/org-units/__tests__/org-units.service.test.ts` (alterar); `apps/api/src/org-units/__tests__/org-units.integration.test.ts` (alterar); `apps/api/src/org-units/__tests__/org-units.contract.test.ts` (alterar)
  - O que fazer: integração e contrato contra o Postgres real, sem mock do Prisma, `resetDatabase(prisma)` em `beforeEach`, instalação e sessões pelos ajudantes já usados nesses arquivos; não-admin por `createPersonWithSession`. `apps/api/test/**` **não muda**. Os casos já existentes ficam como estão. Nenhuma espera fixa. Pedidos de escrita mandam o que o CSRF global do projeto exige, como os testes de `documents` já fazem.
    - `is-uuid.test.ts`: `accepts a lowercase uuid`; `accepts an uppercase uuid`; `rejects an empty string`; `rejects a uuid with extra characters`; `rejects a plain word`.
    - `org-units.schema.test.ts`: `trims the name`; `normalizes the name to NFC` (entrada decomposta "Área" → saída composta "Área"); `rejects an empty name with the message`; `rejects a name of only spaces with the message`; `accepts a name of 120 characters`; `rejects a name of 121 characters with the message`; `rejects a missing name with the message`; `create rejects a missing parentId with the message`; `create rejects an unknown key`; `update rejects parentId as an unknown key`.
    - `org-units.service.test.ts` (Prisma falso; casos novos): `create looks the parent up by id and organization`; `create throws not found when the parent is not in the organization` (Prisma falso devolvendo `null`); `create throws not found for a malformed parentId without querying`; `create writes the unit and its UNIT space in one transaction`; `create turns a unique violation into ConflictException with the message`; `create rethrows any other error`; `rename looks the unit up by id and organization`; `rename throws not found before validating the body`; `rename of the root also updates the organization name`; `rename of a child does not touch the organization`; `rename turns a unique violation into ConflictException with the message`; `rename rethrows any other error`.
    - `org-units.integration.test.ts` (casos novos, `POST /api/org-units` e `PATCH /api/org-units/:id`): `creates a child under the root, lists it and creates its UNIT space`; `stores the name trimmed`; `answers 400 for an empty name and for a name of only spaces`; `answers 400 for 121 characters and 201 for 120`; `answers 400 without parentId`; `answers 404 with the message for an unknown parentId`; `answers 404 with the message for a malformed parentId`; `answers 409 with the message for siblings differing only by case` ("Acervo"/"acervo"); `answers 409 for accented siblings differing only by case` ("ÁREA"/"área"); `accepts siblings differing only by accent` ("Área"/"Area" → 201 nas duas); `accepts the same name under different parents`; `two identical concurrent creates end as one 201 and one 409 with a single row` (`Promise.all`; status ordenados `[201, 409]`; `count` = 1); `renames a child and the list reflects it`; `answers 409 when renaming to a sibling name with different case`; `renames only the case of its own name`; `renaming the root renames the organization and auth me returns the new name`; `PATCH answers 400 when the body carries parentId`; `PATCH answers 404 for an unknown id`; `PATCH answers 404 for a malformed id`; `a person who is not admin gets 403 on POST and on PATCH`; `answers 401 without a session cookie on POST and on PATCH`; `the database refuses a second root` (`prisma.orgUnit.create` direto com `parentId: null` rejeita com código `P2002`).
    - `org-units.contract.test.ts` (ajudantes de `apps/api/test/contract.ts`; casos novos): `POST org-units answers the documented 201`; `POST org-units answers the documented 400`; `POST org-units answers the documented 403`; `POST org-units answers the documented 404`; `POST org-units answers the documented 409`; `PATCH org-units answers the documented 200`; `PATCH org-units answers the documented 400`; `PATCH org-units answers the documented 403`; `PATCH org-units answers the documented 404`; `PATCH org-units answers the documented 409`.
    - "`parentId` de outra organização → 404" **não tem integração** (`Organization.singleton` impede a segunda organização); o filtro é provado nos unitários do serviço acima.
  - Skills: unit-testing, integration-testing, security
  - Complexidade: alta

### Critérios de aceite da fase 1

- [x] CA1.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` passam **sem erro nem aviso** (sem aviso de peer no install; sem aviso de chunk grande no build; o aviso de desempenho do Vitest sobre jsdom não conta). A saída de `pnpm test` não tem `console.error` nem `console.warn` vazando, nem aviso do Nest ou do Prisma. `git diff feature/064-org-units-view... -- package.json apps/api/package.json apps/web/package.json packages/api-contract/package.json pnpm-lock.yaml` é vazio nesta fase.
- [x] CA1.2 — `packages/api-contract/openapi.yaml` descreve `POST /org-units` (`operationId: createOrgUnit`) com corpo `CreateOrgUnitInput` (`parentId` e `name` obrigatórios, `name` com `minLength: 1` e `maxLength: 120`, `additionalProperties: false`) e respostas `201` (`OrgUnitResponse`), `400`, `401`, `403`, `404`, `409`; e `PATCH /org-units/{orgUnitId}` (`operationId: updateOrgUnit`) com corpo `UpdateOrgUnitInput` (só `name`, obrigatório, `additionalProperties: false`, **sem** `parentId`) e respostas `200` (`OrgUnitResponse`), `400`, `401`, `403`, `404`, `409`. `OrgUnitResponse` tem `data` (`OrgUnit`) obrigatório; o schema `OrgUnit` é o mesmo de `feature/064-org-units-view`. `packages/api-contract/src/generated/openapi.d.ts` reflete o YAML (`apps/api/test/generated-types.test.ts` passa, sem ter sido alterado).
- [x] CA1.3 — Existe `apps/api/prisma/migrations/0007_org_unit_name_uniqueness/migration.sql` e suas únicas instruções são dois `CREATE UNIQUE INDEX`: `"OrgUnit_single_root_key"` sobre `("parentId" IS NULL)` com `WHERE "parentId" IS NULL`, e `"OrgUnit_parentId_lower_name_key"` sobre `("parentId", lower("name"))`. `git diff feature/064-org-units-view... -- apps/api/prisma/schema.prisma` só acrescenta linhas de comentário `///` no modelo `OrgUnit` (nenhum campo, atributo ou modelo muda). `git diff --stat feature/064-org-units-view... -- apps/api/prisma/migrations` lista **só** o arquivo novo.
- [x] CA1.4 — `apps/api/src/common/is-uuid.ts` exporta `isUuid(value: string): boolean`, pura. `apps/api/src/org-units/org-units.schema.ts` exporta `ORG_UNIT_NAME_MAX_LENGTH` (= 120), `orgUnitNameSchema` (apara, normaliza NFC, 1 a 120, mensagens "Informe o nome." e "O nome pode ter no máximo 120 caracteres."), `createOrgUnitSchema` e `updateOrgUnitSchema`, ambos objetos estritos (chave desconhecida é rejeitada com "Campo não permitido."; `parentId` ausente no criar dá "Informe a unidade mãe."). `apps/api/src/org-units/org-unit-not-found.ts` exporta `orgUnitNotFound()` com o texto "Unidade não encontrada.", e é a única origem de 404 do módulo: `rg -n "NotFoundException" apps/api/src/org-units --glob '!**/__tests__/**'` só acha esse arquivo.
- [x] CA1.5 — `apps/api/src/org-units/org-units.service.ts` tem `create(organizationId: string, body: unknown): Promise<OrgUnit>` e `rename(organizationId: string, orgUnitId: string, body: unknown): Promise<OrgUnit>`. Lendo o código: os dois procuram a unidade (mãe, no criar; a própria, no renomear) filtrando por `id` **e** `organizationId`, depois de checar o formato com `isUuid`, e respondem com `orgUnitNotFound()`; o `rename` resolve o id **antes** de validar o corpo; o `create` grava a `OrgUnit` e o `Space` de tipo `UNIT` dentro de **uma** transação; o `rename` atualiza `Organization.name` na mesma transação só quando `parentId === null`; a violação de unicidade (`P2002`) vira `ConflictException` com "Já existe uma unidade com esse nome neste nível." e qualquer outro erro é relançado; **não há** consulta prévia de nome duplicado.
- [x] CA1.6 — `apps/api/src/org-units/org-units.controller.ts` tem um `@Post()` que responde 201 e um `@Patch(':orgUnitId')`, ambos devolvendo `{ data }` e usando a organização **da pessoa da sessão**. Os guards continuam só na classe, `SessionGuard` e `AdminGuard` nessa ordem: `rg -n "UseGuards" apps/api/src/org-units/org-units.controller.ts` acha uma única linha (além do import). `rg -n "isAdmin" apps/api/src/org-units --glob '!**/__tests__/**'` é vazio.
- [x] CA1.7 — Intocados: `git diff --stat feature/064-org-units-view... -- apps/api/src/auth apps/api/src/access apps/api/src/documents apps/api/src/installation apps/api/src/common/parse-body.ts apps/api/src/org-units/org-units.module.ts apps/api/src/app.module.ts apps/api/test apps/web vitest.config.ts` é vazio.
- [x] CA1.8 — Existem e passam, pelos nomes listados em T1.4: os 5 casos de `is-uuid.test.ts`; os 10 de `org-units.schema.test.ts`; os 12 casos novos de `org-units.service.test.ts` (entre eles `create throws not found when the parent is not in the organization`, `create rethrows any other error`, `rename of the root also updates the organization name` e `rename of a child does not touch the organization`); os 22 casos novos de `org-units.integration.test.ts` (entre eles `creates a child under the root, lists it and creates its UNIT space`, `answers 409 for accented siblings differing only by case`, `accepts siblings differing only by accent`, `two identical concurrent creates end as one 201 and one 409 with a single row`, `renaming the root renames the organization and auth me returns the new name`, `a person who is not admin gets 403 on POST and on PATCH` e `the database refuses a second root`); e os 10 casos novos de `org-units.contract.test.ts`. A integração usa o Postgres real (`rg -n "vi\.mock\(.*prisma" apps/api/src/org-units/__tests__/org-units.integration.test.ts apps/api/src/org-units/__tests__/org-units.contract.test.ts` é vazio). Os casos que já existiam em `feature/064-org-units-view` continuam presentes e passando. Conferir com `npx vitest run --project api --reporter=verbose`.
- [x] CA1.9 — Nenhuma espera fixa nem credencial literal nos testes da fase: `rg -n "setTimeout\(|sleep\(" apps/api/src/org-units/__tests__ apps/api/src/common/__tests__/is-uuid.test.ts` é vazio; `rg -n "folioteca_session=[A-Za-z0-9]" apps/api/src/org-units/__tests__` é vazio.
- [x] CA1.10 — O relatório de cobertura de `pnpm test` lista com ≥ 80% de linhas cada: `apps/api/src/common/is-uuid.ts`, `apps/api/src/org-units/org-units.schema.ts`, `apps/api/src/org-units/org-unit-not-found.ts`, `apps/api/src/org-units/org-units.service.ts` e `apps/api/src/org-units/org-units.controller.ts`; arquivo ausente do relatório conta como reprovado (salvo 0 de 0 linhas instrumentáveis).

## Fase 2 — Web: criar unidade filha e renomear pela árvore da página "Estrutura", em diálogo acessível

Caminhos relativos a `apps/web/`, salvo os que começam por `docs/` ou `pnpm-lock.yaml` (raiz).

- [ ] T2.1 — Primitivos de interface: `Dialog` compartilhado, `Button` com `size="icon"` e as receitas novas
  - Arquivos: `package.json` (alterar); `pnpm-lock.yaml` (alterar, raiz, só pelo pnpm); `src/components/ui/dialog/dialog.tsx` (criar); `src/components/ui/button/button.tsx` (alterar); `docs/design.md` (alterar)
  - O que fazer (D4, D9, R9):
    - Dependência `@radix-ui/react-dialog` em `apps/web` (conferir antes que os `peerDependencies` aceitam `react@^19`; a instalação **não pode emitir aviso**). Nenhuma outra dependência.
    - `dialog.tsx`, no formato shadcn da skill `ui-components` e no mesmo espírito de `src/components/ui/confirmation-dialog/`: reexporta `Dialog` (a raiz do Radix, **controlada** por `open`/`onOpenChange`) e `DialogClose`; exporta `DialogContent` (portal + fundo + caixa, `className` combinável por `cn`, repassa as demais props da caixa do Radix — é por elas que quem usa recebe `onOpenAutoFocus` e `onCloseAutoFocus`), `DialogTitle` e `DialogDescription`, com as classes da receita. **Sem** `DialogTrigger`. Fecha com `Esc` e clique fora; o Radix prende o foco e o devolve a quem o tinha quando abriu. `ConfirmationDialog` fica intocado (é `alertdialog`, para ação destrutiva).
    - `button.tsx`: variante `size` no `cva` — `md` (padrão; resultado **idêntico** ao de hoje) e `icon` (quadrado de 40px, sem respiro horizontal); nenhum uso existente muda. `ButtonProps` aceita `size`.
    - `docs/design.md`, tabela "Padrões acrescentados pelas entregas", Fatia = 065: "Diálogo de formulário" — fundo, caixa, título e rodapé iguais aos de "Diálogo de confirmação"; descrição `mt-2 text-sm text-gray-600 break-words`; formulário `mt-4` (o `Form` já dá `space-y-4`); rodapé dentro do `<form>`, "Cancelar" (secundário) antes do envio (principal). "Botão só com ícone (`size="icon"`)" — base do botão + `size-10 px-0`; ícone `<svg aria-hidden="true" focusable="false" className="size-4">`, traço `currentColor`; sempre com `aria-label`.
  - Skills: ui-components, interface-design, security
  - Complexidade: média

- [ ] T2.2 — `Tree`: slot de ações por nó e foco por comando, preservando o teclado do APG
  - Arquivos: `src/components/ui/tree/tree.tsx` (alterar); `docs/design.md` (alterar)
  - O que fazer (D6, R1, R4) — tudo opcional; sem as props novas o componente se comporta exatamente como hoje e os testes existentes seguem verdes **sem alteração**:
    - `renderActions?: (node: TreeNode, state: { tabIndex: 0 | -1 }) => React.ReactNode`. O resultado é desenhado **dentro da linha do nó**, depois do rótulo, num contêiner marcado com o atributo `data-tree-actions`, que não encolhe (quem cede espaço a 360px é o rótulo, que continua truncando). `state.tabIndex` é `0` só no nó que é a parada de `Tab` da árvore e `-1` nos outros; quem chama o repassa aos botões.
    - `ref?: React.Ref<TreeHandle>` como prop comum, com `export type TreeHandle = { focusNode: (id: string) => void }`. `focusNode` move o `tabIndex=0` **e** o foco para o nó, se ele estiver visível; id desconhecido ou escondido não faz nada.
    - Teclado: a árvore continua com **uma** parada de `Tab` de entrada (o `treeitem` ativo). Dele, `Tab` vai à primeira ação **daquele** nó, depois à segunda, depois sai; `Shift+Tab` volta ao item. Setas, `Home`, `End`, `Enter` e `Espaço` só valem com o foco no próprio `treeitem`: evento cujo alvo não é o item é ignorado, então `Enter`/`Espaço` num botão aciona o botão e não alterna a expansão, e seta com o foco num botão não move o foco. Foco dentro de uma ação não muda o nó ativo.
    - Clique: quando o alvo está dentro de `[data-tree-actions]`, **não** alterna a expansão; só marca o nó como ativo, **sem** chamar `.focus()` no item (não rouba o foco do botão clicado).
    - Ações **sempre visíveis** (nunca só no `hover`). Sem `style={{…}}`, sem `forwardRef`, sem importar de `@/features` nem de `@/app`.
    - `docs/design.md`: linha nova da fatia 065 "Ações do nó da árvore" — `<div data-tree-actions className="flex shrink-0 items-center gap-1">` depois do rótulo, na linha do nó; botões `ghost` + `size="icon"`; sempre visíveis — e a receita "Árvore" (064) passa a citar as ações.
  - Skills: ui-components, component-robustness, interface-design
  - Complexidade: alta

- [ ] T2.3 — Base de dados e erros: `ConflictError` no cliente HTTP, tipo `OrgUnitResponse` e a API simulada de escrita
  - Arquivos: `src/lib/errors.ts` (alterar); `src/lib/api-client.ts` (alterar); `src/types/api.ts` (alterar); `src/testing/mocks/db.ts` (alterar); `src/testing/mocks/handlers/org-units.ts` (alterar)
  - O que fazer (D5, D10, R3, R8):
    - `errors.ts`: `export class ConflictError extends Error` (nome `ConflictError`) e `export const isConflictError = (error: unknown): error is ConflictError`, espelho de `NotFoundError`/`isNotFoundError`.
    - `api-client.ts`: status **409** rejeita com `new ConflictError()`, na mesma posição do 404 (depois da notificação de erro, que continua respeitando `silentError`). O tratamento do 401 não muda.
    - `types/api.ts`: `OrgUnitResponse`, tirado do contrato gerado como os demais.
    - `db.ts`: ajudantes `addOrgUnit({ parentId, name }): MockOrgUnit` (id por `crypto.randomUUID()`) e `renameOrgUnit(id, name): MockOrgUnit` ao lado dos `seed*`, para o handler não manipular `state` direto. Renomear a **raiz** altera também `installation.organization.name` **no objeto que já está no banco falso** (nunca numa cópia — regra do comentário do arquivo), para o `GET /auth/me` simulado devolver o nome novo. O espaço `UNIT` não é simulado (comentário curto).
    - `handlers/org-units.ts`: `POST ${env.API_URL}/org-units` e `PATCH ${env.API_URL}/org-units/:orgUnitId`, com o mesmo preâmbulo do `GET` (`networkDelay`, `devOverride('org-units')`, 401 "Sessão não encontrada." sem cookie/instalação, **403** "Apenas a administração pode fazer isso." para não-admin) e as mesmas regras do servidor: corpo inválido → 400 "Dados inválidos." (nome aparado vazio ou com mais de 120; chave desconhecida, inclusive `parentId` no `PATCH`; `parentId` ausente no `POST`); `parentId`/id que não está no banco falso → 404 "Unidade não encontrada." (no `PATCH`, antes de validar o corpo); irmã com `name.toLowerCase()` igual (caixa sim, acento não; no `PATCH` a própria unidade fica fora da comparação) → 409 "Já existe uma unidade com esse nome neste nível."; senão 201/200 com `OrgUnitResponse`, nome gravado aparado e em NFC. Sem chave de desenvolvimento nova. `handlers/auth.ts`, `installation.ts`, `documents.ts` e `index.ts` não mudam.
  - Skills: error-handling, api-client, api-requests, api-mocking
  - Complexidade: média

- [ ] T2.4 — Mutações da feature: regra de nome única, `createOrgUnit` e `updateOrgUnit`
  - Arquivos: `src/features/org-units/utils/org-unit-name-schema.ts` (criar); `src/features/org-units/api/create-org-unit.ts` (criar); `src/features/org-units/api/update-org-unit.ts` (criar)
  - O que fazer (D7, R2, R4, R6, R7), no formato das mutações existentes (ex.: `src/features/documents/api/`):
    - `org-unit-name-schema.ts`: `export const ORG_UNIT_NAME_MAX_LENGTH = 120` e `export const orgUnitNameSchema` — string aparada, normalizada para NFC, mínimo 1 ("Informe o nome."), máximo 120 ("O nome pode ter no máximo 120 caracteres."). Mesmas regras e mensagens do servidor.
    - `create-org-unit.ts`: `createOrgUnitInputSchema = z.object({ name: orgUnitNameSchema })`; `type CreateOrgUnitInput`; `createOrgUnit({ parentId, data }: { parentId: string; data: CreateOrgUnitInput }): Promise<OrgUnitResponse>` → `POST /org-units` com corpo `{ parentId, ...data }` e `silentError: true`; `useCreateOrgUnit({ mutationConfig }: { mutationConfig?: MutationConfig<typeof createOrgUnit> } = {})`. `parentId` não é campo de formulário.
    - `update-org-unit.ts`: `updateOrgUnitInputSchema`, `type UpdateOrgUnitInput`; `updateOrgUnit({ orgUnitId, data }: { orgUnitId: string; data: UpdateOrgUnitInput }): Promise<OrgUnitResponse>` → `PATCH /org-units/${orgUnitId}` com `silentError: true`; `useUpdateOrgUnit({ mutationConfig })`.
    - `onSuccess` interno das duas: **espera** (`await`) a invalidação de `getOrgUnitsQueryOptions().queryKey` e **só depois** chama o `onSuccess` de quem usou o hook — a mutação fica pendente até a lista nova chegar. No `update`, quando `response.data.parentId === null`, invalida também `getUserQueryOptions().queryKey` de `@/lib/auth`; para unidade filha, não. `src/lib/auth.tsx` e `get-org-units.ts` ficam intocados. Sem `setQueryData`, sem atualização otimista, sem `try/catch`.
  - Skills: api-requests, forms, authentication
  - Complexidade: média

- [ ] T2.5 — Os dois formulários de um campo: criar unidade filha e renomear
  - Arquivos: `src/features/org-units/components/create-org-unit-form.tsx` (criar); `src/features/org-units/components/rename-org-unit-form.tsx` (criar)
  - O que fazer (D8, R2, R3, R6), seção "Interface" da SPEC copiada aqui:
    - `CreateOrgUnitForm({ parentId, onSuccess, onCancel, … }): React.JSX.Element` com `parentId: string`, `onSuccess: (unit: OrgUnit) => void`, `onCancel: () => void`. `RenameOrgUnitForm` com `unit: OrgUnit`, `onSuccess: (unit: OrgUnit) => void`, `onCancel: () => void`. Dois componentes, não um com `mode`.
    - Ambos: `Form` + `Input` compartilhados de `src/components/ui/form/` (que ficam intocados), schema vindo do arquivo da mutação, `defaultValues` (`''` no criar; `unit.name` no renomear). Campo com rótulo "Nome", `autoComplete="off"` e **sem** `maxLength` (quem limita é o schema: colar 130 caracteres mostra a mensagem, não corta em silêncio). `onSubmit` só chama `mutate`, com **retorno antecipado se `isPending`** (`Enter` duas vezes e clique duplo fazem um pedido só). `form.reset()` no sucesso, depois chama `onSuccess(response.data)`.
    - Para o diálogo poder focar o campo sem `document.querySelector` nem `autoFocus`: cada formulário expõe por prop (ex.: `focusRef`, um `React.Ref<{ focusName: () => void }>` como prop comum) um comando que chama `form.setFocus('name')` — no renomear, **selecionando o texto**. A forma exata da prop é decisão do implementer; o essencial é o foco sair do `setFocus` do React Hook Form.
    - Rodapé **dentro do `<form>`**, receita "Diálogo de formulário": "Cancelar" = `Button variant="secondary"` `type="button"` dentro de `DialogClose asChild`, desabilitado enquanto `isPending`; envio = `Button` `type="submit"` com `isLoading` — criar: "Criar unidade" / enviando "Criando…"; renomear: "Salvar" / enviando "Salvando…".
    - Mensagens de validação abaixo do campo: "Informe o nome." e "O nome pode ter no máximo 120 caracteres.".
    - Erro do servidor, no `onError` da chamada de `mutate`: `isConflictError(error)` → `form.setError('name', { message: 'Já existe uma unidade com esse nome neste nível.' }, { shouldFocus: true })` — aviso **no campo** (ligado por `aria-describedby`, campo com `aria-invalid`), formulário segue montado. Qualquer outra falha → alerta do formulário (`role="alert"`, receita "Alerta dentro de formulário"), acima dos botões, visível quando a mutação está em erro **e** o erro não é conflito: criar "Não foi possível criar a unidade. Tente de novo em instantes."; renomear "Não foi possível renomear a unidade. Tente de novo em instantes.". Sem `error.response.status` na feature.
  - Skills: forms, error-handling, component-robustness, interface-design
  - Complexidade: média

- [ ] T2.6 — `OrgUnitsTree`: ações por nó, diálogo único, expandir a mãe e focar o nó novo
  - Arquivos: `src/features/org-units/components/org-units-tree.tsx` (alterar)
  - O que fazer (D9, R1, R4, R9), seção "Interface" da SPEC copiada aqui. Carregando, erro e a página `src/app/routes/app/admin/structure.tsx` ficam como estão.
    - Cada linha da árvore passa a ser: seta (ou espaçador) · nome truncado com `title` · à direita, dois `Button variant="ghost" size="icon"` `type="button"` com ícone SVG inline `aria-hidden` (sinal de mais; lápis), recebendo o `tabIndex` de `renderActions`: criar filha com `aria-label` e `title` "Criar unidade filha em {nome da unidade}"; renomear com `aria-label` e `title` "Renomear {nome da unidade}".
    - Estado local (`useState`) no componente interno que só monta com dados: `dialog: { mode: 'create' | 'rename'; unitId: string } | null` e `pendingFocusId: string | null`. Guarda o **id**; a unidade é derivada da lista a cada render, e se ela sumir da lista o diálogo fecha por derivação (`open` = unidade encontrada). **Sem `useEffect`** de sincronização.
    - **Um** `Dialog` para a árvore inteira (não um por nó). Conteúdo no modo criar: título "Criar unidade filha"; descrição "A nova unidade ficará dentro de “{nome da mãe}”."; `CreateOrgUnitForm`. No modo renomear: título "Renomear unidade"; descrição "Nome atual: “{nome}”." e, quando a unidade é a raiz (`parentId === null`), a segunda frase "Esta é a raiz: o novo nome também passa a ser o nome da organização."; `RenameOrgUnitForm`. O formulário leva `key` composta de modo e id.
    - Foco: ao abrir, o diálogo previne o foco padrão e foca o campo "Nome" pelo comando do formulário (no renomear, com o texto selecionado). Ao fechar: se há `pendingFocusId`, previne o padrão, chama `focusNode(pendingFocusId)` na `ref` do `Tree` e limpa o estado; senão deixa o Radix devolver o foco ao botão que abriu.
    - Sucesso do criar: acrescenta o `parentId` ao conjunto de expandidos **na forma funcional** do `setState`, guarda `pendingFocusId = unit.id`, notificação de sucesso "Unidade criada" (sistema de notificações que já existe), fecha. Sucesso do renomear: notificação "Unidade renomeada", fecha (o foco volta ao botão "Renomear …" do nó).
    - Texto de "só a raiz" (receita "Vazio", abaixo da árvore) passa a ser: "Por enquanto só existe a raiz. Use “Criar unidade filha” na linha dela para começar a estrutura." — e some quando existe a primeira filha.
  - Skills: interface-design, client-state, component-robustness
  - Complexidade: alta

- [ ] T2.7 — Testes da fase 2
  - Arquivos: `src/lib/__tests__/api-client.test.ts` (alterar); `src/components/ui/button/__tests__/button.test.tsx` (alterar); `src/components/ui/dialog/__tests__/dialog.test.tsx` (criar); `src/components/ui/tree/__tests__/tree.test.tsx` (alterar); `src/features/org-units/utils/__tests__/org-unit-name-schema.test.ts` (criar); `src/features/org-units/api/__tests__/create-org-unit.test.tsx` (criar); `src/features/org-units/api/__tests__/update-org-unit.test.tsx` (criar); `src/features/org-units/components/__tests__/create-org-unit-form.test.tsx` (criar); `src/features/org-units/components/__tests__/rename-org-unit-form.test.tsx` (criar); `src/features/org-units/components/__tests__/org-units-tree.test.tsx` (alterar); `src/app/routes/app/admin/__tests__/structure.test.tsx` (alterar)
  - O que fazer: banco falso por `seedInstalled({ signedIn: true })` (admin) ou `seedInstalled({ signedIn: true, isAdmin: false })` e `seedSampleOrgUnits()`; interação por `userEvent`; localizar por papel e nome acessível em pt_BR. Os casos antigos de `tree.test.tsx` ficam **intocados**; em `org-units-tree.test.tsx` os casos antigos só mudam onde afirmam o texto antigo de "só a raiz". `structure.test.tsx` já declara `LAZY_TIMEOUT` e o passa em **toda** espera logo após mudança de rota ou chunk `lazy`. Nenhuma espera fixa; nenhum `console.error`/`console.warn`/aviso de `act(...)` vazando. O projeto não testa handlers de `org-units` em arquivo próprio: os handlers são provados pelos testes das mutações e da rota. Os guards de erro são testados em `api-client.test.ts` (é onde `isNotFoundError` já é testado; não há `errors.test`).
    - `api-client.test.ts` (novos): `a 409 rejects with ConflictError and notifies`; `a 409 with silentError rejects with ConflictError and notifies nothing`; `isConflictError is true only for ConflictError`.
    - `button.test.tsx` (novos): `size icon renders a square button without horizontal padding`; `the default size keeps the current classes`.
    - `dialog.test.tsx`: `renders nothing while closed`; `opens by the open prop and is named after its title`; `links the description to the dialog`; `Escape calls onOpenChange with false`; `keeps the focus trapped inside while open`; `DialogClose calls onOpenChange with false`.
    - `tree.test.tsx` (novos): `renders no actions container without renderActions`; `renders the actions inside the row of each node`; `gives tabIndex 0 only to the actions of the active node`; `Tab from the item moves to the first action and then to the second`; `Shift+Tab from the first action returns to the item`; `Enter on an action does not toggle expansion`; `Space on an action does not toggle expansion`; `arrow keys on an action do not move the focus`; `click on an action does not toggle and makes the node active`; `focusNode focuses a visible node and moves the tab stop`; `focusNode ignores a hidden node`; `focusNode ignores an unknown id`.
    - `org-unit-name-schema.test.ts`: `trims the name`; `normalizes the name to NFC`; `rejects an empty name with the message`; `rejects only spaces with the message`; `accepts 120 characters`; `rejects 121 characters with the message`.
    - `create-org-unit.test.tsx`: `posts parentId and name to org-units`; `returns the created unit envelope`; `invalidates the org-units query`; `calls the caller onSuccess only after the list was refetched`; `rejects with ConflictError on a sibling name`; `rejects with 403 for a person who is not admin` (handler padrão); `rejects with NotFoundError for an unknown parent`.
    - `update-org-unit.test.tsx`: `patches the name to the unit path`; `invalidates the org-units query`; `calls the caller onSuccess only after the list was refetched`; `renaming the root invalidates the authenticated user`; `renaming a child does not invalidate the authenticated user`; `rejects with ConflictError on a sibling name`; `rejects with 403 for a person who is not admin`.
    - `create-org-unit-form.test.tsx`: `submits the trimmed name and calls onSuccess with the unit`; `shows the required message and focuses the field when empty`; `shows the length message for 121 characters`; `disables the buttons and shows Criando… while sending` (`delay('infinite')`); `pressing Enter twice sends a single request` (handler contador); `shows the conflict message on the field and stays mounted on 409` (`aria-invalid` no campo); `shows the form alert on 500`; `Cancelar calls onCancel`.
    - `rename-org-unit-form.test.tsx`: `starts with the current name`; `submits the trimmed name and calls onSuccess with the unit`; `shows the required message when cleared`; `shows the length message for 121 characters`; `disables the buttons and shows Salvando… while sending`; `pressing Enter twice sends a single request`; `shows the conflict message on the field and stays mounted on 409`; `shows the form alert on 500`.
    - `org-units-tree.test.tsx` (novos): `every node has the create and rename actions named after the unit`; `creating from the keyboard closes the dialog, expands the parent and focuses the new node` (partindo da mãe **recolhida**; `Tab` → `Enter` → digitar → `Enter`); `the create dialog opens with the focus on Nome and Escape returns it to the opener`; `the rename dialog opens with the current name selected and Escape returns the focus to the opener`; `a 409 keeps the create dialog open with the message on the field`; `renaming a child changes its label and notifies`; `the rename dialog of the root explains that the organization is renamed too`; `shows the new root-only text and hides it after the first child is created`.
    - `structure.test.tsx` (novos, `renderApp`): `renaming the root changes the organization name in the sidebar identity`; `createOrgUnit called directly by a person who is not admin gets 403 from the default handler`; `updateOrgUnit called directly by a person who is not admin gets 403 from the default handler`.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: alta

### Critérios de aceite da fase 2

- [ ] CA2.1 — Com `docker compose up -d` rodando, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` passam **sem erro nem aviso** (sem aviso de peer no install; o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta e não pode aparecer). A saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)` vazando. `git diff feature/064-org-units-view... -- apps/web/package.json` acrescenta **só** `@radix-ui/react-dialog`; `git diff feature/064-org-units-view... -- package.json apps/api/package.json packages/api-contract/package.json` é vazio.
- [ ] CA2.2 — `apps/web/src/components/ui/dialog/dialog.tsx` exporta `Dialog`, `DialogClose`, `DialogContent`, `DialogTitle` e `DialogDescription`, construídos sobre `@radix-ui/react-dialog`; **não** exporta gatilho (`rg -n "Trigger" apps/web/src/components/ui/dialog/dialog.tsx` é vazio); `DialogContent` desenha portal, fundo e caixa, aceita `className` e repassa as props da caixa do Radix; fundo, caixa e título usam as classes da receita "Diálogo de confirmação" do `docs/design.md` e a descrição `mt-2 text-sm text-gray-600 break-words`. O arquivo não importa de `@/features` nem de `@/app`, e não usa `style={{`, `forwardRef` nem o `JSX` global.
- [ ] CA2.3 — `apps/web/src/components/ui/button/button.tsx` tem a variante `size` com `md` (padrão) e `icon`; `icon` dá um botão quadrado de 40px sem respiro horizontal (`size-10 px-0`); sem `size`, as classes resultantes são as mesmas de `feature/064-org-units-view`. Nenhum uso de `Button` que já existia passou a informar `size`.
- [ ] CA2.4 — `apps/web/src/components/ui/tree/tree.tsx` aceita `renderActions?: (node: TreeNode, state: { tabIndex: 0 | -1 }) => React.ReactNode` e `ref?: React.Ref<TreeHandle>`, e exporta `TreeHandle = { focusNode: (id: string) => void }`. Lendo o código: as ações são desenhadas dentro da linha do nó, depois do rótulo, num contêiner com `data-tree-actions` que não encolhe; `tabIndex` é `0` só no nó ativo; o tratamento de teclas ignora evento cujo alvo não é o próprio `treeitem`; o clique com alvo dentro de `[data-tree-actions]` não alterna a expansão e não chama `.focus()` no item; `focusNode` só age em nó visível; as ações não dependem de `hover` para aparecer. `rg -n "style=\{\{|forwardRef|: JSX\.|@/features|@/app" apps/web/src/components/ui/tree/tree.tsx` é vazio.
- [ ] CA2.5 — `apps/web/src/lib/errors.ts` exporta `ConflictError` e `isConflictError(error: unknown): error is ConflictError`; `apps/web/src/lib/api-client.ts` rejeita com `ConflictError` quando o status é 409, respeitando `silentError` para a notificação. `apps/web/src/types/api.ts` exporta `OrgUnitResponse` a partir do contrato. Nenhum código de produção da feature olha status HTTP: `rg -n "response\?*\.status|isAxiosError" apps/web/src/features/org-units --glob '!**/__tests__/**'` é vazio.
- [ ] CA2.6 — `apps/web/src/features/org-units/utils/org-unit-name-schema.ts` exporta `ORG_UNIT_NAME_MAX_LENGTH` (= 120) e `orgUnitNameSchema` (apara, NFC, "Informe o nome.", "O nome pode ter no máximo 120 caracteres."). `api/create-org-unit.ts` exporta `createOrgUnitInputSchema`, `CreateOrgUnitInput`, `createOrgUnit({ parentId, data }): Promise<OrgUnitResponse>` (`POST /org-units`, corpo com `parentId` e `name`, `silentError: true`) e `useCreateOrgUnit({ mutationConfig })`. `api/update-org-unit.ts` exporta `updateOrgUnitInputSchema`, `UpdateOrgUnitInput`, `updateOrgUnit({ orgUnitId, data }): Promise<OrgUnitResponse>` (`PATCH /org-units/{id}`, `silentError: true`) e `useUpdateOrgUnit({ mutationConfig })`. Lendo o código: as duas **esperam** a invalidação da chave de `getOrgUnitsQueryOptions()` antes de chamar o `onSuccess` de quem usou o hook; o `update` invalida também a chave de `getUserQueryOptions()` só quando `parentId === null`; não há `setQueryData` nem atualização otimista.
- [ ] CA2.7 — `create-org-unit-form.tsx` (`CreateOrgUnitForm`, props `parentId`, `onSuccess(unit)`, `onCancel`) e `rename-org-unit-form.tsx` (`RenameOrgUnitForm`, props `unit`, `onSuccess(unit)`, `onCancel`), em `apps/web/src/features/org-units/components/`: usam `Form` e `Input` compartilhados e o schema do arquivo da mutação; campo com `<label>` "Nome", `autoComplete="off"` e **sem** `maxLength`; envio com retorno antecipado quando a mutação está pendente; "Cancelar" é `Button variant="secondary"` `type="button"` dentro de `DialogClose`, desabilitado durante o envio; o envio é `Button` `type="submit"` com `isLoading` e textos "Criar unidade"/"Criando…" e "Salvar"/"Salvando…"; rodapé dentro do `<form>` com "Cancelar" antes do envio; no 409 (`isConflictError`) a frase "Já existe uma unidade com esse nome neste nível." vai para o campo por `setError` com foco; outra falha mostra `role="alert"` com as classes de "Alerta dentro de formulário" e o texto "Não foi possível criar a unidade. Tente de novo em instantes." ou "Não foi possível renomear a unidade. Tente de novo em instantes."; o renomear começa com `unit.name`. O foco do campo sai do `setFocus` do formulário: `rg -n "querySelector|autoFocus" apps/web/src/features/org-units --glob '!**/__tests__/**'` é vazio.
- [ ] CA2.8 — `apps/web/src/features/org-units/components/org-units-tree.tsx`: cada nó tem dois `Button variant="ghost" size="icon"` `type="button"` com SVG `aria-hidden`, `aria-label` e `title` "Criar unidade filha em {nome}" e "Renomear {nome}", recebendo o `tabIndex` do `renderActions`; há **um** `Dialog` para a árvore (não um por nó); o estado guarda modo e **id** e a unidade é derivada da lista (diálogo aberto só se a unidade existe); títulos "Criar unidade filha" e "Renomear unidade"; descrições "A nova unidade ficará dentro de “{nome da mãe}”." e "Nome atual: “{nome}”.", mais "Esta é a raiz: o novo nome também passa a ser o nome da organização." quando `parentId === null`; ao abrir, o foco vai ao campo "Nome"; ao fechar depois de criar, chama `focusNode` do `Tree` com o id novo, e nos demais casos o foco volta a quem abriu; criar com sucesso expande a mãe por `setState` funcional; notificações "Unidade criada" e "Unidade renomeada"; texto de só a raiz "Por enquanto só existe a raiz. Use “Criar unidade filha” na linha dela para começar a estrutura." em tom suave, abaixo da árvore; o texto antigo não existe mais (`rg -n "serão criadas aqui" apps/web/src apps/web/e2e` é vazio). Sem `useEffect` de sincronização. Não há `<button` cru: `rg -n "<button" apps/web/src/features/org-units apps/web/src/components/ui/tree apps/web/src/components/ui/dialog --glob '!**/__tests__/**'` é vazio.
- [ ] CA2.9 — API simulada: `apps/web/src/testing/mocks/db.ts` tem `addOrgUnit` e `renameOrgUnit`, e renomear a raiz muda `organization.name` no objeto da instalação que já está no estado; `handlers/org-units.ts` tem `POST /org-units` e `PATCH /org-units/:orgUnitId` com o mesmo preâmbulo do `GET` e respondem 401 "Sessão não encontrada.", 403 "Apenas a administração pode fazer isso.", 400 "Dados inválidos.", 404 "Unidade não encontrada.", 409 "Já existe uma unidade com esse nome neste nível." (comparação por caixa, não por acento; no `PATCH` a própria unidade fica fora) e 201/200 com `{ data }`.
- [ ] CA2.10 — `docs/design.md` tem, em "Padrões acrescentados pelas entregas", as três linhas da fatia 065 — "Diálogo de formulário", "Botão só com ícone (`size="icon"`)" e "Ações do nó da árvore" — e a receita "Árvore" cita as ações; `dialog.tsx`, `button.tsx` e `tree.tsx` usam essas classes.
- [ ] CA2.11 — Intocados: `git diff --stat feature/064-org-units-view... -- apps/web/src/lib/auth.tsx apps/web/src/lib/authorization.tsx apps/web/src/components/ui/confirmation-dialog apps/web/src/components/ui/form apps/web/src/components/ui/notifications apps/web/src/components/layouts apps/web/src/app/router.tsx apps/web/src/app/routes/app/admin/structure.tsx apps/web/src/features/org-units/api/get-org-units.ts apps/web/src/features/org-units/utils/build-tree.ts apps/web/src/testing/mocks/handlers/auth.ts apps/web/src/testing/mocks/handlers/installation.ts apps/web/src/testing/mocks/handlers/documents.ts eslint.config.js vitest.config.ts apps/api packages` só mostra, no máximo, arquivos dentro de `__tests__/`. `rg -n "@/features/(?!org-units)|@/app" -P apps/web/src/features/org-units --glob '!**/__tests__/**'` é vazio.
- [ ] CA2.12 — Existem e passam, pelos nomes listados em T2.7: os 3 casos novos de `api-client.test.ts`; os 2 de `button.test.tsx`; os 6 de `dialog.test.tsx`; os 12 novos de `tree.test.tsx` (entre eles `Tab from the item moves to the first action and then to the second`, `Enter on an action does not toggle expansion`, `click on an action does not toggle and makes the node active` e `focusNode ignores a hidden node`); os 6 de `org-unit-name-schema.test.ts`; os 7 de `create-org-unit.test.tsx` e os 7 de `update-org-unit.test.tsx` (entre eles `calls the caller onSuccess only after the list was refetched`, `renaming the root invalidates the authenticated user` e `renaming a child does not invalidate the authenticated user`); os 8 de `create-org-unit-form.test.tsx` e os 8 de `rename-org-unit-form.test.tsx` (entre eles `pressing Enter twice sends a single request` e `shows the conflict message on the field and stays mounted on 409`); os 8 novos de `org-units-tree.test.tsx` (entre eles `creating from the keyboard closes the dialog, expands the parent and focuses the new node`); e os 3 novos de `structure.test.tsx`. Os casos de `tree.test.tsx` que já existiam em `feature/064-org-units-view` estão presentes e sem alteração (o diff desse arquivo só acrescenta). Nenhum teste removido nem pulado. Conferir com `npx vitest run --project web --reporter=verbose`.
- [ ] CA2.13 — Esperas: `apps/web/src/app/routes/app/admin/__tests__/structure.test.tsx` declara `LAZY_TIMEOUT` e o passa em toda espera logo após abrir ou trocar de rota (`findBy…` e `waitFor`), inclusive nos casos novos; `rg -n "setTimeout\(|sleep\(" apps/web/src/app/routes/app/admin/__tests__ apps/web/src/features/org-units apps/web/src/components/ui/tree apps/web/src/components/ui/dialog` é vazio.
- [ ] CA2.14 — O relatório de cobertura de `pnpm test` lista com ≥ 80% de linhas cada: `apps/web/src/lib/errors.ts`, `apps/web/src/lib/api-client.ts`, `apps/web/src/components/ui/button/button.tsx`, `apps/web/src/components/ui/dialog/dialog.tsx`, `apps/web/src/components/ui/tree/tree.tsx`, `apps/web/src/features/org-units/utils/org-unit-name-schema.ts`, `apps/web/src/features/org-units/api/create-org-unit.ts`, `apps/web/src/features/org-units/api/update-org-unit.ts`, `apps/web/src/features/org-units/components/create-org-unit-form.tsx`, `apps/web/src/features/org-units/components/rename-org-unit-form.tsx` e `apps/web/src/features/org-units/components/org-units-tree.tsx`; arquivo ausente do relatório conta como reprovado (salvo 0 de 0 linhas instrumentáveis).

## Fase 3 — e2e das jornadas de criar e renomear e documentação: a fatia utilizável de ponta a ponta

Caminhos relativos à raiz do repositório.

- [ ] T3.1 — Documentação: a entrega `org-units-create-rename` na arquitetura
  - Arquivos: `docs/architecture.md` (alterar)
  - O que fazer, em pt_BR, sem reescrever o resto:
    - §4 (árvore de unidades), parágrafo novo "Entrega `org-units-create-rename` (fatia 065)": os dois índices únicos escritos à mão na migration `0007` (raiz única; `parentId` + `lower(name)`) e por que o Prisma não os expressa (por isso `migrate diff` acusaria "drift" e não é rodado); regra de nome — caixa colide ("Acervo"/"acervo", "ÁREA"/"área"), acento não ("Área"/"Area"), nome aparado e normalizado para NFC, dependência do `LC_CTYPE` UTF-8 do banco; o 409 "Já existe uma unidade com esse nome neste nível." vem **do banco** (sem consulta prévia), o que fecha a corrida; criar grava a unidade e o espaço `UNIT` na mesma transação; renomear a raiz renomeia a organização na mesma transação e a web relê `/auth/me`; `parentId` é imutável (o `PATCH` o recusa com 400); 404 único "Unidade não encontrada."; modelo de teclado das ações do `Tree` (uma parada de `Tab` de entrada; do item, `Tab` vai às ações do nó ativo; teclas da árvore só com o foco no item; `focusNode` imperativo).
    - §6 (autenticação): as rotas de escrita de `org-units` herdam `SessionGuard` + `AdminGuard` da **classe** do controller; ordem observável CSRF → 401 → 403 → 404/400 → 409.
    - O status da fatia no roadmap, o item 069 e o registro das dívidas **não** são desta tarefa (a orquestração registra no encerramento).
  - Skills: —
  - Complexidade: baixa

- [ ] T3.2 — Testes da fase 3 (e2e: criar pelo teclado, conflito de nome e renomear a raiz)
  - Arquivos: `apps/web/e2e/tests/org-units-create-rename.spec.ts` (criar); `apps/web/e2e/tests/org-units-view.spec.ts` (alterar)
  - O que fazer: contra a API simulada; localizar por papel e nome acessível em pt_BR. Estado inicial por `page.addInitScript` gravando `mock-installation` = `signed-in` e `mock-org-units` = `sample` no `localStorage`. No topo do arquivo novo: `const ROUTE_TIMEOUT = { timeout: 10_000 }`; **toda** espera logo após mudança de rota (`toHaveURL`, primeira visibilidade da tela nova) o recebe; nenhum `waitForTimeout`. `apps/web/e2e/a11y.ts` e os outros specs não são tocados; nenhuma regra do axe é desativada. Nenhum literal com cara de senha.
    - Caso `an admin creates a child unit using only the keyboard`: chega a `/admin/structure` pelo link "Estrutura" acionado por teclado (`focus()` + `Enter`); leva o foco à árvore, recolhe pelo teclado uma unidade com filhas ("Atendimento ao Público"), `Tab` até "Criar unidade filha em Atendimento ao Público" e `Enter`; o diálogo "Criar unidade filha" abre com o foco no campo "Nome"; roda `expectNoSeriousA11yViolations(page)` **com o diálogo aberto**; digita "Hemeroteca" e `Enter`; o diálogo some, a mãe está `aria-expanded="true"`, o `treeitem` "Hemeroteca" existe sob ela e está em foco (`toBeFocused`); a notificação "Unidade criada" aparece; roda `expectNoSeriousA11yViolations(page)` de novo com a árvore com ações. Sem `.click()` no caso.
    - Caso `a sibling name in another case is refused on the field and can be corrected`: abre o criar em "Acervo e Processamento Técnico", envia "catalogação" → o diálogo continua aberto, o campo "Nome" está inválido e mostra "Já existe uma unidade com esse nome neste nível."; troca o nome por um livre e envia → o diálogo fecha e o nó novo aparece sob a mãe.
    - Caso `renaming the root renames the organization in the sidebar`: abre "Renomear {nome da raiz}"; o diálogo "Renomear unidade" mostra "Esta é a raiz: o novo nome também passa a ser o nome da organização." e o campo vem com o nome atual; roda `expectNoSeriousA11yViolations(page)` **com o diálogo aberto**; `Escape` fecha e o foco volta ao botão "Renomear …" (`toBeFocused`); reabre, envia um nome novo → o nó raiz e a identidade da barra lateral mostram o nome novo; notificação "Unidade renomeada".
    - `org-units-view.spec.ts`: ajustar **só** o que o `Tab` novo (as ações do nó entram na ordem de `Tab` depois do item) ou o texto de "só a raiz" exigirem; os dois casos mantêm os nomes.
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [ ] CA3.1 — `pnpm test:e2e` na raiz passa (o Playwright sobe o próprio servidor; não precisa de API nem de Postgres), e continuam passando **sem erro nem aviso**, com `docker compose up -d` rodando e o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules` antes do typecheck): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` (o aviso do Vitest sobre jsdom não conta; o aviso de chunk grande do Vite conta e não pode aparecer).
- [ ] CA3.2 — `apps/web/e2e/tests/org-units-create-rename.spec.ts` contém os casos `an admin creates a child unit using only the keyboard`, `a sibling name in another case is refused on the field and can be corrected` e `renaming the root renames the organization in the sidebar`, e `pnpm --filter web exec playwright test --list` os lista junto com os casos já existentes dos outros specs, inclusive os dois de `org-units-view.spec.ts` com os mesmos nomes (nenhum removido nem renomeado). Não há `test.skip`, `test.only` nem `waitForTimeout` em `apps/web/e2e`.
- [ ] CA3.3 — Lendo o primeiro caso: não há `.click()`; o botão "Criar unidade filha em …" de uma unidade **recolhida** é alcançado por `Tab` e acionado por `Enter`; confere o foco no campo "Nome" ao abrir; chama `expectNoSeriousA11yViolations` com o diálogo aberto e outra vez depois de fechar; depois de criar "Hemeroteca" confere `aria-expanded="true"` na mãe e `toBeFocused()` no nó novo. Lendo o segundo: confere que o diálogo segue aberto com "Já existe uma unidade com esse nome neste nível." depois de enviar "catalogação" sob "Acervo e Processamento Técnico", e que a correção cria o nó. Lendo o terceiro: confere a frase "Esta é a raiz: o novo nome também passa a ser o nome da organização.", chama `expectNoSeriousA11yViolations` com o diálogo de renomear aberto, confere que `Escape` devolve o foco ao botão "Renomear …" e que o nome novo aparece na identidade da barra lateral.
- [ ] CA3.4 — Esperas do e2e: `org-units-create-rename.spec.ts` declara `ROUTE_TIMEOUT` no topo e **toda** asserção logo após mudança de rota (`toHaveURL` e a primeira visibilidade da tela nova) o recebe; o mesmo continua valendo em `org-units-view.spec.ts`; não há espera fixa.
- [ ] CA3.5 — Acessibilidade sem atalho novo: `git diff --stat feature/064-org-units-view... -- apps/web/e2e` lista **só** `tests/org-units-create-rename.spec.ts` e, no máximo, `tests/org-units-view.spec.ts`; nenhum dos dois desativa regra do axe. Os arquivos de `apps/web/e2e/**` passam no `pnpm lint` e no `pnpm typecheck`. Nenhuma senha, token ou hash literal no spec novo.
- [ ] CA3.6 — `docs/architecture.md`: a §4 tem o parágrafo "Entrega `org-units-create-rename` (fatia 065)" dizendo: dois índices únicos à mão na migration `0007` e por que o Prisma não os expressa; caixa colide, acento não, nome aparado e em NFC; 409 vindo do banco; unidade e espaço `UNIT` na mesma transação; renomear a raiz renomeia a organização; `parentId` imutável; modelo de teclado das ações do `Tree`. A §6 diz que as rotas de escrita de `org-units` herdam `SessionGuard` e `AdminGuard` da classe do controller.
- [ ] CA3.7 — A fatia está utilizável de ponta a ponta sem navegador aberto por pessoa: existem e passam `apps/api/src/org-units/__tests__/org-units.integration.test.ts` com `creates a child under the root, lists it and creates its UNIT space` e `renaming the root renames the organization and auth me returns the new name` (`npx vitest run --project api`); `apps/web/src/features/org-units/components/__tests__/org-units-tree.test.tsx` com `creating from the keyboard closes the dialog, expands the parent and focuses the new node` e `apps/web/src/app/routes/app/admin/__tests__/structure.test.tsx` com `renaming the root changes the organization name in the sidebar identity` (`npx vitest run --project web`); e as três jornadas de `org-units-create-rename.spec.ts` (`pnpm test:e2e`).

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
