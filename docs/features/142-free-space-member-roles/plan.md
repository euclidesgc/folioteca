# PLAN 142 — free-space-member-roles

Branch: `feature/142-free-space-member-roles`, empilhada sobre 141/137/136/135/134.

Fonte: `docs/features/142-free-space-member-roles/spec.md` (D1…D10 são as decisões técnicas da SPEC e R1…R14 os requisitos do PRD; o texto necessário de cada uma está copiado na tarefa). Ambiente: Node 24, pnpm 11, TypeScript 5.9 strict, Prisma 6, NestJS 11, Vite 8, React 19, Vitest com os projetos `api` e `web`, Playwright.

O dono de um espaço livre dá a cada membro o nível "Pode editar" ou "Pode ver". O leitor vê o espaço, a lista de documentos e as pessoas, e abre os documentos em somente leitura; não cria documento nem adiciona pessoas. Membros existentes continuam editores. Fora desta fatia: nível em espaço de unidade, derrubar a conexão aberta de quem foi rebaixado.

Pré-condição de todas as fases: `docker compose up -d` na raiz (Postgres local; banco de teste `folioteca_test`, porta 5433, usuário `folioteca`, sem senha). Comandos de verificação, sempre na raiz e sempre a suíte inteira: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. "Sem aviso" vale para os cinco. O aviso de desempenho do próprio Vitest sobre o ambiente jsdom **não conta**; o aviso de tamanho de chunk do Vite (`Some chunks are larger than…`) **conta**. O typecheck e o lint finais de cada fase — inclusive de quem só escreve testes — rodam com o cache do `tsc` limpo: `pnpm exec tsc -b --clean`, conferir que não sobrou `*.tsbuildinfo` fora de `node_modules`, e só então `pnpm typecheck`.

**Critérios sobre o disco.** Os critérios são conferidos **antes** do commit da fase, sobre os arquivos em disco (commitados ou não). Nenhum critério depende de `git diff` contra a base. Onde um critério prova **ausência**, o padrão exige código (não casa com comentário nem com atributo parecido) e é restrito aos arquivos criados ou alterados na fase, excluídos os que já tinham o padrão antes desta fatia. O commit de cada fase é feito pela orquestração depois da revisão; nenhum agente de fase commita.

**Suíte instável.** Se `pnpm test` ou `pnpm test:e2e` inteiro falhar num teste **antigo** (não criado nem alterado nesta fatia), o critério permite repetir o comando **uma vez**; passa se a repetição sair com 0. Falha em teste desta fatia não tem repetição.

## Desvios registrados

- **DV1** — Testes e dados simulados que montam `SpaceDetail` ou `SpaceMember` literal ganham os campos novos, **sem renomear** o teste nem mudar outra asserção: `SpaceDetail` ganha `canCreateDocuments` e `canAddPeople` (valores coerentes com o perfil do caso: dono `true/true`; membro editor `true/membersCanInvite`; unidade direta `true/false`; unidade herdada `false/false`); `SpaceMember` ganha `level` (`null` para `owner` e `assigned`, `'edit'` para `member` salvo quando o caso é de leitor). Vale para as asserções `toEqual` de `GET /spaces/{spaceId}` e `GET /spaces/{spaceId}/members` em `apps/api/src/spaces/__tests__/spaces.integration.test.ts` e `spaces.contract.test.ts`, para `apps/web/src/features/spaces/components/__tests__/space-view.test.tsx`, `space-members.test.tsx`, `apps/web/src/features/spaces/api/__tests__/get-space.test.tsx`, `get-space-members.test.tsx`, `apps/web/src/app/routes/app/__tests__/space.test.tsx`, e para qualquer outro que `pnpm typecheck` ou a suíte apontar.
- **DV2** — A SPEC cita `apps/api/src/access/__tests__/access.service.test.ts`, que não existe: os unitários de `levelOf` vão para o arquivo existente `apps/api/src/access/__tests__/access-level.test.ts` e os de integração para `apps/api/src/access/__tests__/access.integration.test.ts`. Os casos existentes de `access-level.test.ts` que montam a decisão com `isSpaceMember: true` passam a usar `spaceLevel: 'edit'` (e `false` vira `spaceLevel: null`), **sem renomear** nem mudar a asserção.
- **DV3** — A SPEC põe a prova de colaboração em `apps/api/src/documents/__tests__/`; ela vai para o arquivo onde a colaboração já é testada, `apps/api/src/collab/__tests__/collab.integration.test.ts`.
- **DV4** — Refatoração da 141 (D8): em `apps/web/src/features/spaces/components/__tests__/space-view.test.tsx`, os casos `a FREE space shows Adicionar pessoa to its owner`, `a FREE space shows Adicionar pessoa to a member when open` e `a FREE space shows the member description without Adicionar pessoa when closed` passam a controlar o botão pelo `canAddPeople` da resposta simulada (`true`, `true`, `false`), **sem renomear** nem mudar as asserções.
- **DV5** — A SPEC põe a receita "Seletor na linha da lista" do `docs/design.md` na Fase 3; ela entra na Fase 2, na tarefa que cria o seletor (skill `interface-design`: receita nova vai com a tela).
- Conferidos e **mantidos** (continuam verdadeiros porque o nível padrão é `EDIT`): `levelOf returns edit for a member of a FREE space` (com o ajuste de DV2), `resolveAccess returns edit for a member of the free space`, `canWrite is true for a member of the free space`, `a free space member connects and an update is stored`, `POST documents with a free spaceId answers 201 to a member in that space owned by the caller`, `PUT space member answers 200 to a member of an open space and the person sees the space in GET spaces`, e os e2e de `free-space-invite.spec.ts`, `free-space-documents.spec.ts` e `free-space-restrict-invite.spec.ts`. Se algum desses falhar, o conserto é no código de produção, nunca no teste.

## Regras que valem para todas as tarefas

- **Nenhuma dependência nova** em nenhum `package.json`; `pnpm-lock.yaml` não muda.
- **Uma migration só, escrita à mão** (`apps/api/prisma/migrations/0019_space_member_level/migration.sql`). Os únicos subcomandos de Prisma permitidos são `prisma validate`, `prisma generate`, `prisma migrate status` e `prisma migrate deploy`, sempre contra `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test`; **nunca** `migrate diff`, `migrate dev` ou `migrate reset`. As migrations `0001…0018` não mudam.
- **Nenhum teste remove, desativa ou recria restrição do banco** (`DROP CONSTRAINT`, `ALTER TABLE … DISABLE`, `session_replication_role`).
- `packages/api-contract/src/generated/openapi.d.ts` só muda por `pnpm --filter @folioteca/api-contract generate`; nunca editado à mão.
- **Sem Prettier reformatando arquivo existente**: só as linhas necessárias mudam; nada de `prettier --write` em arquivo inteiro. Correções depois de revisão são **edições pontuais**, nunca regravação do arquivo inteiro.
- Todo componente devolve `React.JSX.Element` (ou `React.JSX.Element | null`), nunca o `JSX` global; `ref` é prop comum (sem `forwardRef`); **todo botão nosso é `Button`** de `apps/web/src/components/ui/button/button.tsx`; imports absolutos com `@/`; sem barrel files; kebab-case; **nenhum import entre features** (`features/documents` não importa de `features/spaces` e vice-versa; quem junta é a rota). Textos de tela em pt_BR; código, comentários, URLs e nomes de teste em en_US.
- **Seletor sem `disabled` nativo**: durante o envio o `<select>` usa `aria-disabled="true"`, para o foco ficar nele.
- Nenhum `eslint-disable` sem `-- <motivo>`; nenhuma regra desativada; nenhum `any` sem comentário.
- Cobertura ≥ 80% de linhas **por arquivo** novo ou alterado, medida **só** por `coverage/coverage-summary.json`, gerado **na raiz** com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`, sem `--project` e sem filtro de caminho. Não contam arquivos só de tipos nem os já excluídos pelo `vitest.config.ts`. Nenhuma exclusão nova.
- Espera após mudança de rota ou `lazy`: nos testes da web pela constante `LAZY_TIMEOUT` do arquivo; no e2e pela constante `ROUTE_TIMEOUT = { timeout: 10_000 }` no topo do spec. A **primeira** asserção após mudança de rota usa `ROUTE_TIMEOUT`; asserção seguinte com timeout maior é aceita. Nunca `setTimeout`/sleep fixo nem `waitForTimeout`.
- Integração e contrato de API contra o Postgres real (sem mock do Prisma); só `*.service.test.ts` e `access-level.test.ts` usam Prisma falso. "Teste passa" = exit code 0.
- **Escopo por sessão**: `organizationId` e `personId` de quem pede vêm de `@CurrentPerson()`, nunca do corpo; `isAdmin` não é lido em `apps/api/src/spaces`. Outra organização é provada chamando o **serviço real** com `randomUUID()` como `organizationId`.
- **Fronteira de acesso**: a decisão sobre **documento** continua só no `AccessService`. Se `apps/api/src/access/__tests__/document-access-boundary.test.ts` recusar a mudança de `DocumentsService.create`, o agente **para e relata**; nunca altera a regra nem o teste de fronteira.
- Intocados de propósito: `apps/web/src/features/documents/components/document-view.tsx`, `readableDocumentsWhere`, `trashedDocumentsWhere`, `removeMember`, `updateSettings`, `space-invite-mode-control.tsx`, `add-space-member-dialog.tsx`, `apps/web/src/components/ui/**`, `apps/web/e2e/a11y.ts`.
- O agente derruba tudo o que subir (servidores, watchers) ao fim da tarefa.

## Fase 1 — API: nível do membro, `PATCH` do membro, leitor sem criar nem adicionar

Caminhos relativos à raiz do repositório.

- [ ] T1.1 — Contrato primeiro: `SpaceMember.level`, flags do `SpaceDetail` e `updateSpaceMemberLevel`
  - Arquivos: `packages/api-contract/openapi.yaml` (alterar); `packages/api-contract/src/generated/openapi.d.ts` (alterar, **só** pelo script)
  - O que fazer (D1, R2, R5, R7, R8):
    - `SpaceMember` ganha `level` em `required`: `type: string`, `enum: [view, edit]`, `nullable: true`, descrição pt_BR "Nível do membro no espaço livre; null para o dono e para lotados de unidade".
    - `SpaceDetail` ganha `canCreateDocuments: { type: boolean }` e `canAddPeople: { type: boolean }` em `required`, com descrições pt_BR ("Se quem pede pode criar documentos neste espaço"; "Se quem pede pode adicionar pessoas; permissão calculada, diferente da configuração membersCanInvite"). `membersCanInvite` continua.
    - No item `/spaces/{spaceId}/members/{personId}` (o mesmo que já tem `put` e `delete`): `patch` com `operationId: updateSpaceMemberLevel`, tag `spaces`, parâmetros `spaceId` e `personId`, `requestBody` obrigatório com `$ref` `UpdateSpaceMemberInput`, respostas **200** (`SpaceMemberLevelResponse`), **400**, **401**, **403**, **404** (`Error`). `description` pt_BR: só o dono do espaço livre muda o nível de um membro.
    - Schemas novos: `UpdateSpaceMemberInput` (`type: object`, `required: [level]`, `additionalProperties: false`, `level` `enum: [view, edit]`); `SpaceMemberLevelResponse` (`type: object`, `required: [data]`, `data` com `$ref` `SpaceMember`).
    - Regenerar com `pnpm --filter @folioteca/api-contract generate`.
  - Skills: security
  - Complexidade: baixa

- [ ] T1.2 — Banco: coluna `level` com migration 0019 escrita à mão
  - Arquivos: `apps/api/prisma/migrations/0019_space_member_level/migration.sql` (criar); `apps/api/prisma/schema.prisma` (alterar)
  - O que fazer (D2, R1):
    - `migration.sql`, à mão, comentário em pt_BR, exatamente este SQL:
      ```sql
      ALTER TABLE "SpaceMember" ADD COLUMN "level" "ShareLevel" NOT NULL DEFAULT 'EDIT';
      ```
    - `schema.prisma`: `SpaceMember` ganha `level ShareLevel @default(EDIT)` com `///` "Nível do membro no espaço livre: EDIT cria e edita, VIEW só lê. O dono do espaço não tem linha aqui nem nível." Nenhum enum novo.
    - Conferência: `pnpm --filter api exec prisma validate`, `prisma generate`, `migrate deploy` + `migrate status` no banco de teste. Se `validate` apontar diferença que exigiria outro SQL, o agente para e relata.
  - Skills: security
  - Complexidade: baixa

- [ ] T1.3 — Acesso a documento e criação: o leitor lê e não escreve nem cria
  - Arquivos: `apps/api/src/access/access.service.ts` (alterar); `apps/api/src/documents/documents.service.ts` (alterar)
  - O que fazer (D3, D5, R6, R7, R9–R12). Assinaturas públicas inalteradas.
    - `access.service.ts`: `findDecision` seleciona `members: { where: { personId }, select: { level: true } }`. `DocumentDecision.isSpaceMember: boolean` vira `spaceLevel: 'edit' | 'view' | null`: espaço UNIT com lotação direta → `'edit'`; FREE e a pessoa é dona do espaço → `'edit'`; FREE e membro → `level === 'EDIT' ? 'edit' : 'view'`; senão `null`. `levelOf`: dono do documento → `'owner'`; na lixeira → `'none'`; depois o **maior** entre o nível do share e `spaceLevel` (`'edit'` vence `'view'`); nenhum → `'none'`. `canWrite` continua derivando de `levelOf`. `readableDocumentsWhere` e `trashedDocumentsWhere` não mudam.
    - `documents.service.ts`, `create`: o `tx.space.findFirst` que já existe passa a selecionar também `type`, `ownerId` e `members: { where: { personId: person.id }, select: { level: true } }`; se `type === 'FREE'`, quem pede não é o dono e o nível do membro é `VIEW` → `ForbiddenException('Só quem pode editar cria documentos neste espaço.')`, dentro da transação, **antes** do `document.create`. Nenhuma consulta nova.
  - Skills: authorization
  - Complexidade: alta

- [ ] T1.4 — Espaços: `updateMemberLevel`, `level` na lista, leitor sem adicionar e os dois flags do detalhe
  - Arquivos: `apps/api/src/spaces/spaces.schema.ts` (alterar); `apps/api/src/spaces/spaces.service.ts` (alterar); `apps/api/src/spaces/spaces.controller.ts` (alterar)
  - O que fazer (D1, D4, R2, R3, R5, R7, R8, R10):
    - `spaces.schema.ts`: `updateSpaceMemberSchema = z.strictObject({ level: z.enum(['view', 'edit'], { error: 'Escolha o nível do membro.' }) }, { error: 'Campo não permitido.' })`.
    - Método novo `updateMemberLevel(requester, spaceId: string, personId: string, body: unknown)`, nesta ordem: `isUuid(spaceId)` senão 404 → o mesmo `findFirst` de alcance do `removeMember` (FREE, `organizationId: requester.organizationId`, dono ou membro; `select { ownerId }`) senão 404 → não dono → `ForbiddenException('Só o dono do espaço pode mudar o nível de um membro.')` → `personId === ownerId` → `BadRequestException('O dono do espaço não tem nível.')` → `parseBody(updateSpaceMemberSchema, body)` (400 "Dados inválidos.") → `isUuid(personId)` e `spaceMember.findUnique` pela chave `spaceId_personId`, senão `NotFoundException('Esta pessoa não é membro deste espaço.')` → `spaceMember.update({ level: 'VIEW' | 'EDIT' })` → `{ data }` com o `SpaceMember` (`role: 'member'`, `level`, `isCurrentPerson: false`). Idempotente.
    - `listMembers`: dono e lotados com `level: null`; membros com `level` `'view'`/`'edit'` (mapeado de `VIEW`/`EDIT`).
    - `addMember`: `select { ownerId, membersCanInvite, members: { where: { personId: requester.id }, select: { level: true } } }`; depois do 403 de espaço fechado (141, ordem das recusas inalterada), membro com nível `VIEW` → `ForbiddenException('Só quem pode editar adiciona pessoas a este espaço.')`.
    - `getDetail`: FREE seleciona `members where personId select level`; dono → `canCreateDocuments: true, canAddPeople: true`; editor → `true` / `membersCanInvite`; leitor → `false` / `false`. UNIT: `canCreateDocuments = reach === 'direct'`, `canAddPeople = false`.
    - `spaces.controller.ts`: `@Patch(':spaceId/members/:personId')` + `@HttpCode(200)`, ao lado do `@Put`/`@Delete` do membro, com `@CurrentPerson() person`, `@Param('spaceId') spaceId: string`, `@Param('personId') personId: string`, `@Body() body: unknown`, chamando `updateMemberLevel(person, spaceId, personId, body)`. `SessionGuard` só na classe (intocado).
  - Skills: authorization, security
  - Complexidade: alta

- [ ] T1.5 — Testes da fase 1
  - Arquivos: `apps/api/src/access/__tests__/access-level.test.ts` (alterar); `apps/api/src/access/__tests__/access.integration.test.ts` (alterar); `apps/api/src/spaces/__tests__/spaces.service.test.ts` (alterar); `apps/api/src/spaces/__tests__/spaces.integration.test.ts` (alterar); `apps/api/src/spaces/__tests__/spaces.contract.test.ts` (alterar); `apps/api/src/documents/__tests__/documents.integration.test.ts` (alterar); `apps/api/src/collab/__tests__/collab.integration.test.ts` (alterar)
  - O que fazer (D10): `resetDatabase(prisma)` em `beforeEach` e os ajudantes existentes; `apps/api/test/**` não muda. Aplicar DV1, DV2 e DV3; casos preexistentes não são renomeados. O nível do leitor é gravado por `prisma.spaceMember.update` ou `create` com `level: 'VIEW'` (sem remover restrição).
    - `access-level.test.ts`: `levelOf returns view for a viewer member of a FREE space`; `levelOf returns edit for a viewer member with an edit share`; `levelOf returns owner for a viewer member on their own document`; `levelOf returns none for a viewer member on a trashed document`.
    - `access.integration.test.ts`: `resolveAccess returns view for a viewer member of the free space`; `canWrite is false for a viewer member of the free space`; `an edit share to a viewer member of the free space resolves to edit`; `a space member created without level is EDIT` (cria `SpaceMember` sem `level` e lê `EDIT`).
    - `spaces.service.test.ts`: `updateSpaceMemberSchema accepts view and edit`; `updateSpaceMemberSchema rejects a missing value with Escolha o nível do membro.`; `updateSpaceMemberSchema rejects extra fields with Campo não permitido.`; `updateMemberLevel throws not found for a malformed space id`; `updateMemberLevel throws forbidden to a member`; `updateMemberLevel throws bad request for the owner`; `updateMemberLevel throws not found for a person who is not a member`; `updateMemberLevel updates the level by spaceId and personId`; `listMembers returns level null for the owner and the member level`; `addMember throws forbidden to a viewer of an open space`; `getDetail returns canCreateDocuments and canAddPeople false to a viewer`.
    - `spaces.integration.test.ts`: `PATCH space member answers 200 to the owner and GET space members reflects the level`; `PATCH space member is idempotent`; `PATCH space member answers 403 to a member with Só o dono do espaço pode mudar o nível de um membro.`; `PATCH space member answers 400 when the owner targets himself with O dono do espaço não tem nível.`; `PATCH space member answers 404 for a person who is not a member with Esta pessoa não é membro deste espaço.`; `PATCH space member answers 404 to a stranger`; `PATCH space member answers 404 for a malformed space id`; `PATCH space member answers 404 for a UNIT space`; `PATCH space member rejects an empty body with 400`; `PATCH space member rejects an invalid level with 400`; `PATCH space member rejects extra fields with 400`; `PATCH space member answers 401 without session`; `updateMemberLevel with another organization id answers 404` (serviço real, `randomUUID()`); `PUT space member answers 403 to a viewer of an open space with Só quem pode editar adiciona pessoas a este espaço.`; `PUT space member answers 200 to an editor of an open space`; `GET space answers canCreateDocuments and canAddPeople true to the owner`; `GET space answers canCreateDocuments true and canAddPeople false to an editor of a closed space`; `GET space answers canCreateDocuments and canAddPeople true to an editor of an open space`; `GET space answers canCreateDocuments and canAddPeople false to a viewer of an open space`; `GET space answers canCreateDocuments true and canAddPeople false to a direct unit member`; `GET space answers canCreateDocuments false to an inherited unit member`.
    - `spaces.contract.test.ts` (cada caso cita literalmente a chave do YAML que valida): `PATCH space member answers the documented 200` (`SpaceMemberLevelResponse`); `PATCH space member answers the documented 400`; `PATCH space member answers the documented 403`; `PATCH space member answers the documented 404`; `GET space members answers the documented 200 with level` (`SpaceMember`); `GET space answers the documented 200 with canCreateDocuments and canAddPeople` (`SpaceDetail`). Todos chamam o caminho exato `/spaces/{spaceId}/members/{personId}` ou `/spaces/{spaceId}`.
    - `documents.integration.test.ts`: `POST documents with a free spaceId answers 403 to a viewer member with Só quem pode editar cria documentos neste espaço.` (e nenhuma linha nova em `Document`); `POST documents with a free spaceId answers 201 again after the viewer is promoted to edit`; `a viewer member GETs a colleague document of the free space with accessLevel view`; `a viewer member PATCH on a colleague document of the free space is refused and keeps the title`; `a viewer member renames and trashes their own document in the free space`.
    - `collab.integration.test.ts`: `after demoting the free space member to view the next connection is read only and an update is not stored`.
  - Skills: unit-testing, integration-testing
  - Complexidade: média

### Critérios de aceite da fase 1

- [ ] CA1.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo (`pnpm exec tsc -b --clean`; nenhum `*.tsbuildinfo` fora de `node_modules`): `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` saem com 0 e sem aviso (repetição única permitida só para teste antigo instável).
- [ ] CA1.2 — `packages/api-contract/openapi.yaml`: o item `/spaces/{spaceId}/members/{personId}:` tem `patch` com `operationId: updateSpaceMemberLevel`, parâmetros `spaceId` e `personId`, `requestBody` com `$ref` para `UpdateSpaceMemberInput` e respostas exatamente `200` (`SpaceMemberLevelResponse`), `400`, `401`, `403`, `404`; `UpdateSpaceMemberInput` tem `required: [level]`, `additionalProperties: false` e `level` com `enum: [view, edit]`; `SpaceMember` tem `level` em `required`, com `enum: [view, edit]` e `nullable: true`; `SpaceDetail` tem `canCreateDocuments` e `canAddPeople` (`type: boolean`) em `required` e mantém `membersCanInvite`. `rg -n "updateSpaceMemberLevel|UpdateSpaceMemberInput|SpaceMemberLevelResponse|canCreateDocuments|canAddPeople" packages/api-contract/src/generated/openapi.d.ts` encontra os cinco.
- [ ] CA1.3 — Existe `apps/api/prisma/migrations/0019_space_member_level/migration.sql` com `ALTER TABLE "SpaceMember" ADD COLUMN "level" "ShareLevel" NOT NULL DEFAULT 'EDIT';`; `ls apps/api/prisma/migrations` lista só `0001…0019` e `migration_lock.toml`. `apps/api/prisma/schema.prisma` tem `level ShareLevel @default(EDIT)` em `SpaceMember` com `///` e nenhum enum novo. `pnpm --filter api exec prisma validate` sai com 0 e `DATABASE_URL=postgresql://folioteca@localhost:5433/folioteca_test pnpm --filter api exec prisma migrate status` reporta o banco em dia.
- [ ] CA1.4 — `apps/api/src/access/access.service.ts` declara `spaceLevel: 'edit' | 'view' | null` e seleciona `level: true` nos membros em `findDecision`; `rg -n "isSpaceMember" apps/api/src` é vazio. `apps/api/src/documents/documents.service.ts` contém `Só quem pode editar cria documentos neste espaço.` numa `ForbiddenException` e seleciona `members` com `level: true` no `tx.space.findFirst` de `create`; `apps/api/src/access/__tests__/document-access-boundary.test.ts` está sem alteração e passa.
- [ ] CA1.5 — `apps/api/src/spaces/spaces.schema.ts` exporta `updateSpaceMemberSchema` (`z.strictObject`) com `Escolha o nível do membro.` e `Campo não permitido.`. `apps/api/src/spaces/spaces.service.ts` tem o método `updateMemberLevel(` com `type: 'FREE'` e `organizationId` no `where`, e contém `Só o dono do espaço pode mudar o nível de um membro.` (`ForbiddenException`), `O dono do espaço não tem nível.` (`BadRequestException`), `Esta pessoa não é membro deste espaço.` (`NotFoundException`), `Só quem pode editar adiciona pessoas a este espaço.` (`ForbiddenException`), `canCreateDocuments` e `canAddPeople`. `rg -n "^\s*[^/*].*\bisAdmin\b" apps/api/src/spaces/spaces.service.ts apps/api/src/spaces/spaces.controller.ts` é vazio.
- [ ] CA1.6 — `apps/api/src/spaces/spaces.controller.ts` tem `@Patch(':spaceId/members/:personId')` com `@HttpCode(200)`, `@Param('spaceId')` e `@Param('personId')`, chamando `updateMemberLevel(`; `rg -n "@UseGuards" apps/api/src/spaces/spaces.controller.ts` devolve uma ocorrência, acima da classe, com `SessionGuard`.
- [ ] CA1.7 — `pnpm exec vitest run --project api` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/api/src/access/__tests__ apps/api/src/spaces/__tests__ apps/api/src/documents/__tests__ apps/api/src/collab/__tests__`, todos os casos nomeados em T1.5 com os nomes literais (`access-level.test.ts`: 4; `access.integration.test.ts`: 4; `spaces.service.test.ts`: 11; `spaces.integration.test.ts`: 21; `spaces.contract.test.ts`: 6; `documents.integration.test.ts`: 5; `collab.integration.test.ts`: 1 — 52 casos). Os casos listados como mantidos em "Desvios registrados" continuam existindo com o mesmo nome. `rg -n "isSpaceMember" apps/api/src/access/__tests__/access-level.test.ts` é vazio (DV2). `rg -n "vi\.mock\(.*prisma" apps/api/src/spaces/__tests__/spaces.integration.test.ts apps/api/src/spaces/__tests__/spaces.contract.test.ts apps/api/src/access/__tests__/access.integration.test.ts apps/api/src/documents/__tests__/documents.integration.test.ts apps/api/src/collab/__tests__/collab.integration.test.ts` é vazio.
- [ ] CA1.8 — Lendo os testes: `updateMemberLevel with another organization id answers 404` chama o serviço real (ligado ao Prisma de teste) com `randomUUID()` como `organizationId` sobre um espaço e um membro existentes; `PATCH space member answers 200 to the owner and GET space members reflects the level` assere `level: 'view'` na linha do membro em `GET /spaces/{spaceId}/members`; `after demoting the free space member to view the next connection is read only and an update is not stored` rebaixa por `PATCH` (ou pelo serviço) e só depois abre a conexão; `POST documents with a free spaceId answers 403 to a viewer member …` assere a contagem de `Document` inalterada; os casos de contrato citam literalmente `SpaceMemberLevelResponse`, `SpaceMember` e `SpaceDetail`. `rg -n "DROP CONSTRAINT|DISABLE TRIGGER|session_replication_role" apps/api/src apps/api/test` é vazio.
- [ ] CA1.9 — Cobertura ≥ 80% de linhas para `apps/api/src/access/access.service.ts`, `apps/api/src/documents/documents.service.ts`, `apps/api/src/spaces/spaces.schema.ts`, `apps/api/src/spaces/spaces.service.ts` e `apps/api/src/spaces/spaces.controller.ts`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 2 — Web: seletor de nível na lista de pessoas e a página do espaço seguindo as permissões do servidor

Caminhos relativos à raiz do repositório. A ordem importa: API simulada e mutação antes da tela.

- [ ] T2.1 — API simulada com o nível e a mutação `useUpdateSpaceMemberLevel`
  - Arquivos: `apps/web/src/testing/mocks/db.ts` (alterar); `apps/web/src/testing/mocks/handlers/spaces.ts` (alterar); `apps/web/src/testing/mocks/handlers/documents.ts` (alterar); `apps/web/src/features/spaces/api/update-space-member-level.ts` (criar)
  - O que fazer (D6, D9, R4–R8):
    - `db.ts`: membro de espaço livre ganha `level` (`'view' | 'edit'`, padrão `'edit'`); `setSpaceMemberLevel(spaceId: string, personId: string, level: 'view' | 'edit')`; semente `mock-space-members=free-viewer`: espaço livre de outra dona, a pessoa da sessão é membro com `level: 'view'`, `membersCanInvite: true`, e um documento de outra pessoa nesse espaço.
    - `handlers/spaces.ts`: `GET /spaces/:spaceId` calcula `canCreateDocuments` e `canAddPeople` pela regra de D4 (dono `true/true`; editor `true/membersCanInvite`; leitor `false/false`; UNIT direto `true/false`, herdado `false/false`); `GET …/members` devolve `level` (`null` para dono e lotados); `http.patch(\`${env.API_URL}/spaces/:spaceId/members/:personId\`)` com o mesmo preâmbulo dos outros handlers (`networkDelay()`, `devOverride`, 401) e 404 (não alcança ou UNIT), 403 `Só o dono do espaço pode mudar o nível de um membro.`, 400 `O dono do espaço não tem nível.`, 400 de corpo inválido, 404 `Esta pessoa não é membro deste espaço.` e 200 `{ data }`; `PUT …/members/:personId` recusa leitor com 403 `Só quem pode editar adiciona pessoas a este espaço.`.
    - `handlers/documents.ts`: `POST /documents` com `spaceId` de espaço livre recusa leitor não dono com 403 `Só quem pode editar cria documentos neste espaço.`; `GET /documents/:id` devolve `accessLevel: 'view'` ao leitor que não é dono do documento.
    - `update-space-member-level.ts`: `updateSpaceMemberLevel({ spaceId, personId, level }: { spaceId: string; personId: string; level: 'view' | 'edit' })` → `api.patch(\`/spaces/${spaceId}/members/${personId}\`, { level }, { silentError: true })`; `useUpdateSpaceMemberLevel({ spaceId, mutationConfig })` cujo `onSuccess` **aguarda** `invalidateQueries({ queryKey: ['space-members', spaceId] })` e só então chama o `onSuccess` do chamador.
  - Skills: api-mocking, api-requests
  - Complexidade: média

- [ ] T2.2 — Selos "editor"/"leitor" e o seletor "Nível de {nome}" na lista de pessoas
  - Arquivos: `apps/web/src/features/spaces/components/space-members.tsx` (alterar); `docs/design.md` (alterar)
  - O que fazer (D7, Interface, R2–R4, R13, R14). Aparência pelo `docs/design.md` e pelo piso de `interface-design`, com as receitas "Selo de status", "Lista", "Notificação" e "Campo de formulário"; padrão de valor derivado (sem importar): `space-invite-mode-control.tsx`.
    - `docs/design.md`: receita nova **"Seletor na linha da lista"** — `<label>` visualmente oculto + `<select>` nativo com as classes do campo da receita "Campo de formulário", trocando a largura total por largura automática, com estilo esmaecido e cursor de bloqueio quando `aria-disabled`; fica entre os selos e as ações da linha, com o "Salvando…" em texto pequeno e cinza ao lado.
    - `FreeMembersList`: em toda linha `role === 'member'`, selo **"editor"** (`level === 'edit'`) ou **"leitor"** (`level === 'view'`), receita "Selo de status", no mesmo par de cores de "dono"/"você". Linha do dono inalterada.
    - `RemovableFreeMembersList` (montado só para o dono): `renderAction` devolve, em linha `role === 'member'`, o seletor de nível **antes** do botão de remover. Componente interno `MemberLevelSelect` no mesmo arquivo, um por linha, cada um com seu `useUpdateSpaceMemberLevel`: `<label>` oculto **"Nível de {nome}"** ligado por `useId`; `<select>` nativo com as opções **"Pode editar"** (`edit`) e **"Pode ver"** (`view`); valor = `isPending ? variables.level : member.level`; durante o envio `aria-disabled="true"` (nunca `disabled`) e `onChange` ignorado; também ignorado se o valor não muda; **"Salvando…"** em `<span aria-live="polite">` ao lado durante o envio; sucesso → notificação **"Nível de {nome} atualizado"**; falha → notificação de erro **"Não foi possível mudar o nível de {nome}. Tente de novo."** e o valor volta sozinho.
    - A 360px a linha quebra (`flex-wrap`) e o bloco da direita desce, sem rolagem horizontal. Carregando/erro/vazio da lista inalterados. Membro (editor ou leitor) vê os selos, sem seletor e sem remover.
  - Skills: interface-design, component-robustness, error-handling
  - Complexidade: alta

- [ ] T2.3 — Página do espaço: "Adicionar pessoa" e "Novo documento" pelos flags do servidor
  - Arquivos: `apps/web/src/features/spaces/components/space-view.tsx` (alterar); `apps/web/src/features/documents/components/space-documents.tsx` (alterar); `apps/web/src/app/routes/app/space.tsx` (alterar)
  - O que fazer (D8, R7, R8, Interface):
    - `space-view.tsx`: `canAddPeople = space.type === 'free' && space.canAddPeople` (substitui a derivação por `reach` + `membersCanInvite` da 141). O controle da 141 continua só para o dono.
    - `space-documents.tsx`: prop nova `canCreate: boolean`; com `false`, sem `NewDocumentButton` e o estado vazio mostra **"Nenhum documento neste espaço ainda."** (receita "Vazio", tom suave); com `true`, tudo como hoje. Lista com dados, carregando e erro inalterados.
    - `app/routes/app/space.tsx`: passa `canCreate={space.canCreateDocuments}` ao `SpaceDocuments` (a rota já lê o `SpaceDetail`; as features não se importam).
  - Skills: interface-design, authorization, routing
  - Complexidade: média

- [ ] T2.4 — Testes da fase 2 e os e2e antigos
  - Arquivos: `apps/web/src/features/spaces/api/__tests__/update-space-member-level.test.tsx` (criar); `apps/web/src/features/spaces/components/__tests__/space-members.test.tsx` (alterar); `apps/web/src/features/spaces/components/__tests__/space-view.test.tsx` (alterar); `apps/web/src/features/documents/components/__tests__/space-documents.test.tsx` (alterar); `apps/web/src/app/routes/app/__tests__/space.test.tsx` (alterar); testes que montam `SpaceDetail`/`SpaceMember` literal (DV1)
  - O que fazer (D10): banco falso pelos ajudantes; `userEvent`; papel e nome acessível em pt_BR; `LAZY_TIMEOUT` onde houver espera após rota ou `lazy`; sem espera fixa; sem `console.error`/`console.warn`/aviso de `act(...)`. Aplicar DV1 e DV4; demais casos preexistentes não são renomeados.
    - `update-space-member-level.test.tsx`: `updateSpaceMemberLevel sends PATCH to the member path with the level body`; `useUpdateSpaceMemberLevel invalidates the space members query before onSuccess`.
    - `space-members.test.tsx`: `a FREE space shows the editor or leitor badge on every member row`; `the owner sees the Nível de select on member rows and never on the owner row`; `a member sees no level select`; `choosing Pode ver sends view and shows Salvando`; `the select is aria-disabled and keeps focus while sending`; `a success shows the Nível de name atualizado notification`; `a failure restores the level and shows the error notification`; `choosing the current level sends nothing`.
    - `space-view.test.tsx`: `a FREE space hides Adicionar pessoa when canAddPeople is false even if open`.
    - `space-documents.test.tsx`: `hides Novo documento when canCreate is false`; `shows Nenhum documento neste espaço ainda. when canCreate is false and the list is empty`; `shows Novo documento when canCreate is true`.
    - `space.test.tsx`: `passes canCreateDocuments false to the documents and hides Novo documento`.
    - Rodar `pnpm test:e2e` com **todos** os e2e existentes; se algum quebrar, o conserto é no código de produção, nunca no spec.
  - Skills: unit-testing, component-testing, integration-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [ ] CA2.1 — Com `docker compose up -d`, na raiz e com o cache do `tsc` limpo: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` saem com 0 e sem aviso; a saída de `pnpm test` não tem `console.error`, `console.warn` nem aviso de `act(...)`. `pnpm test:e2e` (todos os e2e) sai com 0. Repetição única permitida só para teste antigo instável.
- [ ] CA2.2 — `apps/web/src/features/spaces/api/update-space-member-level.ts` exporta `updateSpaceMemberLevel` (chama `api.patch` em `` `/spaces/${spaceId}/members/${personId}` `` com `{ level }` e `silentError: true`) e `useUpdateSpaceMemberLevel`, cujo `onSuccess` faz `await` da invalidação de `['space-members', spaceId]` antes do `onSuccess` do chamador. `rg -nP "from '@/features/(?!spaces/)" apps/web/src/features/spaces` e `rg -nP "from '@/features/(?!documents/)" apps/web/src/features/documents` são vazios.
- [ ] CA2.3 — `apps/web/src/features/spaces/components/space-members.tsx` define `MemberLevelSelect`, usa `useUpdateSpaceMemberLevel` e `useId`, tem um `<select>` com `aria-disabled`, um `<label>` com `sr-only` e um elemento com `aria-live="polite"`, e contém literalmente `Nível de`, `Pode editar`, `Pode ver`, `editor`, `leitor`, `Salvando…`, `atualizado` e `Tente de novo.` (com `Não foi possível mudar o nível de`); o valor do seletor deriva de `isPending`/`variables` e de `member.level`. `rg -nP "(?<![-\w])disabled=\{" apps/web/src/features/spaces/components/space-members.tsx` é vazio.
- [ ] CA2.4 — `apps/web/src/features/spaces/components/space-view.tsx` lê `space.canAddPeople` para mostrar "Adicionar pessoa" e não calcula mais a condição com `membersCanInvite` (`rg -n "membersCanInvite" apps/web/src/features/spaces/components/space-view.tsx` só casa com a prop passada a `SpaceInviteModeControl`). `apps/web/src/features/documents/components/space-documents.tsx` declara a prop `canCreate: boolean` e contém `Nenhum documento neste espaço ainda.`; `apps/web/src/app/routes/app/space.tsx` passa `canCreateDocuments` ao `SpaceDocuments`.
- [ ] CA2.5 — Acabamento (piso de `interface-design`, lendo o código): `docs/design.md` tem a receita "Seletor na linha da lista"; o `<select>` de `MemberLevelSelect` usa as classes dessa receita (inclui `aria-disabled:` para o estado de envio e foco visível); os selos "editor"/"leitor" usam as mesmas classes do selo "dono"; a linha de membro tem `flex-wrap`; o vazio de `space-documents.tsx` usa a receita "Vazio". `rg -n "<button|<a href|style=\{\{|!important|forwardRef|: JSX\." apps/web/src/features/spaces/components/space-members.tsx apps/web/src/features/spaces/components/space-view.tsx apps/web/src/features/documents/components/space-documents.tsx apps/web/src/app/routes/app/space.tsx` é vazio; nenhuma largura fixa em px nesses arquivos.
- [ ] CA2.6 — API simulada: `apps/web/src/testing/mocks/db.ts` exporta `setSpaceMemberLevel(spaceId: string, personId: string, level: 'view' | 'edit')` e contém a semente `free-viewer`. `apps/web/src/testing/mocks/handlers/spaces.ts` tem `http.patch` em `${env.API_URL}/spaces/:spaceId/members/:personId` com `networkDelay()` e as respostas 400, 401, 403 (`Só o dono do espaço pode mudar o nível de um membro.`), 404 e 200, contém `canCreateDocuments`, `canAddPeople` e `Só quem pode editar adiciona pessoas a este espaço.`. `apps/web/src/testing/mocks/handlers/documents.ts` contém `Só quem pode editar cria documentos neste espaço.`.
- [ ] CA2.7 — `pnpm exec vitest run --project web` sai com 0 e existem, conferidos com `rg -n "^\s*(it|test)(\.each)?\(" apps/web/src/features/spaces apps/web/src/features/documents apps/web/src/app/routes/app/__tests__/space.test.tsx`, todos os casos nomeados em T2.4 com os nomes literais (15 casos), e os três casos de DV4 com os nomes de antes.
- [ ] CA2.8 — Lendo os testes: `useUpdateSpaceMemberLevel invalidates the space members query before onSuccess` assere a ordem; `the select is aria-disabled and keeps focus while sending` tem `toHaveAttribute('aria-disabled', 'true')` e `toHaveFocus()` no seletor durante o envio; `a failure restores the level and shows the error notification` assere "Pode editar" selecionado de novo e o texto `Não foi possível mudar o nível de`; `the owner sees the Nível de select on member rows and never on the owner row` assere a ausência do seletor na linha com o selo "dono".
- [ ] CA2.9 — `rg -n "setTimeout\(|sleep\(|waitForTimeout" apps/web/src/features/spaces apps/web/src/features/documents/components/space-documents.tsx apps/web/src/features/documents/components/__tests__/space-documents.test.tsx apps/web/src/app/routes/app/space.tsx apps/web/src/app/routes/app/__tests__/space.test.tsx` é vazio; `rg -n "eslint-disable" apps/web/src | rg -v -- "--"` é vazio.
- [ ] CA2.10 — Cobertura ≥ 80% de linhas para `apps/web/src/features/spaces/api/update-space-member-level.ts`, `apps/web/src/features/spaces/components/space-members.tsx`, `apps/web/src/features/spaces/components/space-view.tsx`, `apps/web/src/features/documents/components/space-documents.tsx` e `apps/web/src/app/routes/app/space.tsx`, lida em `coverage/coverage-summary.json` gerado na raiz com `pnpm exec vitest run --coverage --coverage.reporter=json-summary`.

## Fase 3 — e2e da jornada e documentação: a fatia utilizável de ponta a ponta

- [ ] T3.1 — Documentação de arquitetura
  - Arquivos: `docs/architecture.md` (alterar)
  - O que fazer, em pt_BR, só acrescentando linhas: em §3, parágrafo **"Entrega `free-space-member-roles` (fatia 142)"**: membro de espaço livre tem nível `EDIT` ou `VIEW` em `SpaceMember.level` (0019, padrão `EDIT`); o dono muda por `PATCH /spaces/{spaceId}/members/{personId}`; o `AccessService` dá `view` ao leitor e mantém o maior entre share e espaço; quem cria documento e quem adiciona pessoa é decidido no servidor e chega à tela por `canCreateDocuments` e `canAddPeople` do `SpaceDetail`; rebaixar vale a partir da próxima conexão.
  - Skills: —
  - Complexidade: baixa

- [ ] T3.2 — Testes da fase 3 (e2e: o dono rebaixa pelo teclado e o leitor só lê)
  - Arquivos: `apps/web/e2e/tests/free-space-member-roles.spec.ts` (criar)
  - O que fazer (D10): API simulada, estado inicial por `page.addInitScript` como os specs existentes. `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo; a primeira asserção após cada mudança de rota o usa (asserção seguinte com timeout maior é aceita); `toBeFocused()` antes de cada `Enter`/`Space`/`Escape`/seta; nenhum `waitForTimeout`; nenhuma regra do axe desativada; outros specs e `apps/web/e2e/a11y.ts` intocados.
    - `the owner makes a member a viewer using only the keyboard`: dono abre a página do espaço livre, alcança o seletor "Nível de {nome}" com `Tab`, escolhe "Pode ver" pelo teclado, vê "Salvando…", o seletor continua focado, vê "Nível de {nome} atualizado" e o selo "leitor" na linha; `expectNoSeriousA11yViolations(page)`.
    - `a viewer sees the space without Novo documento nor Adicionar pessoa and opens a document read only`: segundo contexto com a semente `mock-space-members=free-viewer`; na página do espaço não há "Novo documento" nem "Adicionar pessoa"; abre o documento da lista e vê "Somente leitura".
  - Skills: e2e-testing
  - Complexidade: média

### Critérios de aceite da fase 3

- [ ] CA3.1 — `pnpm test:e2e` na raiz sai com 0 (antigos e novos), e `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` continuam saindo com 0 e sem aviso, com o cache do `tsc` limpo. Repetição única permitida só para teste antigo instável.
- [ ] CA3.2 — `apps/web/e2e/tests/free-space-member-roles.spec.ts` contém `the owner makes a member a viewer using only the keyboard` e `a viewer sees the space without Novo documento nor Adicionar pessoa and opens a document read only`, e `pnpm --filter web exec playwright test --list` os lista. `rg -n "test\.skip|test\.only|waitForTimeout|disableRules" apps/web/e2e/tests/free-space-member-roles.spec.ts` é vazio.
- [ ] CA3.3 — Lendo o spec: `const ROUTE_TIMEOUT = { timeout: 10_000 }` no topo, usado na primeira asserção após cada mudança de rota (asserção seguinte com timeout maior é aceita); `toBeFocused()` antes de cada tecla de ação; `expectNoSeriousA11yViolations(page)` no primeiro caso; o primeiro caso assere `Salvando…`, o seletor `Nível de …` focado, o texto `atualizado` e o selo `leitor`; o segundo usa `free-viewer`, assere a ausência de "Novo documento" e de "Adicionar pessoa" e o texto `Somente leitura`.
- [ ] CA3.4 — `docs/architecture.md` tem o parágrafo "Entrega `free-space-member-roles` (fatia 142)" citando `SpaceMember.level`, `PATCH /spaces/{spaceId}/members/{personId}`, `canCreateDocuments` e `canAddPeople`.
- [ ] CA3.5 — Utilizável de ponta a ponta: `pnpm exec vitest run --project api` (0) inclui `POST documents with a free spaceId answers 403 to a viewer member with Só quem pode editar cria documentos neste espaço.` e `PATCH space member answers 200 to the owner and GET space members reflects the level`; `pnpm exec vitest run --project web` (0) inclui `choosing Pode ver sends view and shows Salvando`; `pnpm test:e2e` (0) inclui os dois casos de `free-space-member-roles.spec.ts`.

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
